# Intentional weight-regression acknowledgements.
#
# Format (one function/removal per line):
# <repo-relative-weight-path> <function>: <non-empty justification>
# <repo-relative-weight-path> *: <non-empty whole-file-removal justification>
#
# Example (keep commented unless the regression is intentional):
# runtime/bleavit-runtime/src/weights/pallet_constitution.rs set_param: storage proof grew after adding validation
# runtime/bleavit-runtime/src/weights/pallet_retired.rs *: pallet retired by approved migration
#
runtime/bleavit-runtime/src/weights/pallet_epoch.rs decide: SQ-494: 270 -> 527 reads, and the +257 decomposes exactly - 128 Rounds scanned, 64 RoundSchedules read, 64 ComponentValues read, 1 dis.merit_min Params read. Only the 64 ComponentValues reads are this change; the other 193 are 07 §12's ProcessHold predicate being measured for the first time. The decide benchmark seeded ZERO Rounds, so the predicate iterated an empty map and a decision-critical permissionless crank was charged nothing for a scan bounded at 128 rounds with two reads per matching one (the fourth instance of the fixture-instead-of-work shape here: SQ-489, SQ-490, SQ-495, SQ-492). The fixture now saturates Rounds, split 64/64 across two frozen versions because 02 §7.2 derives the 128-round bound as 16 components x <= 4 settling epochs x <= 2 concurrent versions - seeding all 128 on one version charges decide for a state that decomposition says cannot exist, and measured 655 reads instead of 527. The seeded rounds clear the merit floor and are money-settled, which is the true worst case: `.any()` short-circuits on the first holding dispute, so a map full of holding disputes is the CHEAP case and a full scan returning false is the expensive one. Proof estimate 183,055 -> 356,514 B, equal to drive_oracle_boundaries and inside the normal-class budget (pov_budgets passes).
