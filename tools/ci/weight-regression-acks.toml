# Intentional weight-regression acknowledgements.
#
# Format (one function/removal per line):
# <repo-relative-weight-path> <function>: <non-empty justification>
# <repo-relative-weight-path> *: <non-empty whole-file-removal justification>
#
# Example (keep commented unless the regression is intentional):
# runtime/bleavit-runtime/src/weights/pallet_constitution.rs set_param: storage proof grew after adding validation
# runtime/bleavit-runtime/src/weights/pallet_retired.rs *: pallet retired by approved migration
#
runtime/bleavit-runtime/src/weights/pallet_epoch.rs decide: SQ-494: 270 -> 655 reads, and the +385 decomposes exactly - 128 Rounds scanned, 128 RoundSchedules read, 128 ComponentValues read, 1 dis.merit_min Params read. Only the 128 ComponentValues reads are this change; the other 257 are 07 §12's ProcessHold predicate being measured for the first time. The decide benchmark seeded ZERO Rounds, so the predicate iterated an empty map and a decision-critical permissionless crank was charged nothing for a scan bounded at 128 rounds with two reads per matching one (the fifth instance of the fixture-instead-of-work shape here: SQ-489, SQ-490, SQ-495, SQ-492). All 128 seeded rounds carry the proposal's own frozen version: 02 §7.2 decomposes the bound as 16 components x <= 4 settling epochs x <= 2 versions, which suggests <= 64 per version, but that 4-epoch factor holds only once a retained round is eventually reaped - and 07 §11(1)'s retention has no implemented deadline until SQ-492 (PR #175), so successive epochs accumulate money-settled rounds on one long-lived version to the 128 cap. A 64/64 split measured 527 reads and undercharged by half (#176 review). The seeded rounds clear the merit floor and are money-settled, which is the true worst case: `.any()` short-circuits on the first holding dispute, so a map full of holding disputes is the CHEAP case and a full scan returning false is the expensive one. Proof estimate is 356,514 B at both 64/64 and 128/0 - identical, because ComponentValues and RoundSchedules are already accounted at their ceilings - so the PoV pin is unaffected and pov_budgets passes.
