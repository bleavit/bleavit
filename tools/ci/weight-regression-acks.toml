# Intentional weight-regression acknowledgements.
#
# Format (one function/removal per line):
# <repo-relative-weight-path> <function>: <non-empty justification>
# <repo-relative-weight-path> *: <non-empty whole-file-removal justification>
#
# Example (keep commented unless the regression is intentional):
# runtime/bleavit-runtime/src/weights/pallet_constitution.rs set_param: storage proof grew after adding validation
# runtime/bleavit-runtime/src/weights/pallet_retired.rs *: pallet retired by approved migration
#
runtime/bleavit-runtime/src/weights/pallet_registry.rs file: SQ-489 — the bounded cohort-escrow exposure fold (4 schedules x 5 vaulted proposals) is now benchmarked instead of being skipped by a runtime-benchmarks fixture, so reads 87 -> 108 (+24.14%) and ref_time +13.89% are a previously unmeasured cost becoming visible, not new work. Understating a permissionless call is a block-capacity DoS surface (R-7).
runtime/bleavit-runtime/src/weights/pallet_oracle.rs reserve_probe_result: SQ-489 — pre-existing staleness surfaced by regeneration, not caused by SQ-296. Main's committed weight omits three storage items the function actually touches (Epoch::Schedule, Welfare::XcmTrafficEpochs, Welfare::ReserveProbeDaily); ReserveProbeDaily was added by SQ-195 in PR #159 and the runtime weights were never regenerated for it. reads 21 -> 24, writes 3 -> 5 restores the true cost.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs crank_reserve_probe: SQ-489 — same SQ-195 omission as reserve_probe_result. The folded-slot path writes the day-resolved outcome store, so writes 11 -> 13 (+18.18%) is the real per-slot cost. The pallet's PostDispatchInfo refund still returns the slots a healthy crank does not record.
