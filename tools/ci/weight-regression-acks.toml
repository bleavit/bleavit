# Intentional weight-regression acknowledgements.
#
# Format (one function/removal per line):
# <repo-relative-weight-path> <function>: <non-empty justification>
# <repo-relative-weight-path> *: <non-empty whole-file-removal justification>
#
# Example (keep commented unless the regression is intentional):
# runtime/bleavit-runtime/src/weights/pallet_constitution.rs set_param: storage proof grew after adding validation
# runtime/bleavit-runtime/src/weights/pallet_retired.rs *: pallet retired by approved migration
#
runtime/bleavit-runtime/src/weights/pallet_oracle.rs register_reporter: the B15-partial full-pallet regeneration replaces a stale pre-RoundSchedules/pre-live-Params weight and measures the current bounded oracle hydration path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs deregister_reporter: the B15-partial full-pallet regeneration replaces a stale pre-RoundSchedules weight and measures the current bounded oracle hydration path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs report: the B15-partial full-pallet regeneration replaces a stale pre-CohortSchedules/pre-RoundSchedules/pre-live-Params weight and measures the current bounded oracle hydration path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs challenge: the B15-partial full-pallet regeneration replaces a stale pre-RoundSchedules weight and measures the current bounded oracle hydration path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs recompute_proof: the B15-partial full-pallet regeneration replaces a stale pre-RoundSchedules weight and measures the current treasury/custody-backed oracle close path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs register_watchtower: the B15-partial full-pallet regeneration replaces a stale pre-RoundSchedules/pre-live-Params weight and measures the current bounded oracle hydration path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs ack_observed: the B15-partial full-pallet regeneration replaces a stale pre-RoundSchedules weight and measures the current treasury/custody-backed acknowledgement path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs crank_round_close: the B15-partial full-pallet regeneration replaces a stale pre-RoundSchedules/pre-live-Params weight and measures the current full-batch close plus treasury/custody path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs crank_reserve_probe: SQ-114/SQ-205 add atomic reserve-health mirrors, the dedicated treasury-line debit, local XCM health accounting and the bounded XCMP queue write; the benchmark covers timeout, one missed cadence slot and a new dispatch.
runtime/bleavit-runtime/src/weights/pallet_oracle.rs adjudicate: the B15-partial full-pallet regeneration replaces a stale pre-RoundSchedules weight and measures the current bounded terminal-adjudication hydration path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_futarchy_treasury.rs fund_budget_line: SQ-114's bounded bootstrap handover adds the stored authority and latch plus the full live reserve-envelope Params and cap proof; the generated reference-host path also measures the existing custody-backed Keeper funding branch.
runtime/bleavit-runtime/src/weights/pallet_futarchy_treasury.rs note_coretime_quote: the B15-partial full-pallet regeneration replaces a stale pre-live-Params weight and measures the current quote-authority and live-Params path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_futarchy_treasury.rs prune_coretime_quote: the B15-partial full-pallet regeneration replaces a stale weight and measures the current expiry, authorization, live-Params and custody-backed prune path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_futarchy_treasury.rs sweep_insurance: the B15-partial full-pallet regeneration replaces a stale weight and measures the current live-Params plus custody-backed INSURANCE-to-MAIN path on the reference host.
runtime/bleavit-runtime/src/weights/pallet_execution_guard.rs apply_authorized_upgrade: B16 validates and binds the committed recovery image before scheduling the primary code, adding bounded recovery/preimage reads and the measured 4 MiB runtime-code path.
runtime/bleavit-runtime/src/weights/pallet_execution_guard.rs execute: B16 atomically transfers the qualified recovery commitment into the execution record and schedules its mandatory terminal path alongside primary upgrade authorization.
runtime/bleavit-runtime/src/weights/pallet_guardian.rs propose_action: the B15 full-pallet regeneration replaces a stale pre-live-Params weight and measures the current review-deadline Param read on the reference host.
runtime/bleavit-runtime/src/weights/pallet_guardian.rs renew_playbook: B15 extends LedgerFreeze renewal across the exact ledger/market/constitution effect tuple; the generated worst case measures the additional live-condition reads and atomic effect writes.
runtime/bleavit-runtime/src/weights/pallet_guardian.rs set_members: the full-pallet regeneration replaces a stale pre-seat-release weight; the existing worst-case fixture clears seven bonded outgoing members and now measures their bounded hold/account release writes on the reference host.
runtime/bleavit-runtime/src/weights/pallet_market.rs try_state: the final B15 full-pallet regeneration measures the existing saturated retained-market audit over SeededMarkets and RerunSeededMarkets; the prior generated artifact incorrectly reported one read for each map instead of one per retained market.
