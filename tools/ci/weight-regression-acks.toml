# Intentional weight-regression acknowledgements.
#
# Format (one function/removal per line):
# <repo-relative-weight-path> <function>: <non-empty justification>
# <repo-relative-weight-path> *: <non-empty whole-file-removal justification>
#
# Example (keep commented unless the regression is intentional):
# runtime/bleavit-runtime/src/weights/pallet_constitution.rs set_param: storage proof grew after adding validation
# runtime/bleavit-runtime/src/weights/pallet_retired.rs *: pallet retired by approved migration
#
runtime/bleavit-runtime/src/weights/pallet_epoch.rs settle_cohort: SQ-483 terminalization now clears three market auxiliary maps and updates the active-market counter once per terminal market; the full reference-host benchmark measures those additional writes.
runtime/bleavit-runtime/src/weights/pallet_epoch.rs void_cohort: SQ-483 terminalization now clears three market auxiliary maps and updates the active-market counter once per terminal market; the full reference-host benchmark measures those additional writes.
runtime/bleavit-runtime/src/weights/pallet_conditional_ledger.rs merge_baseline: the SQ-483 reference-host regeneration captures the live freeze guard and dynamic market protocol-account classification now used by the Baseline merge path.
runtime/bleavit-runtime/src/weights/pallet_conditional_ledger.rs redeem_baseline: the SQ-483 reference-host regeneration captures the live freeze guard and dynamic market protocol-account classification now used by Baseline redemption.
runtime/bleavit-runtime/src/weights/pallet_conditional_ledger.rs redeem_baseline_pair: the SQ-483 reference-host regeneration captures the live freeze guard and dynamic market protocol-account classification now used by paired Baseline redemption.
runtime/bleavit-runtime/src/weights/pallet_conditional_ledger.rs split: the SQ-483 reference-host regeneration captures the live pause, inflow-cap, freeze and dynamic market protocol-account checks on proposal splits.
runtime/bleavit-runtime/src/weights/pallet_conditional_ledger.rs split_baseline: the SQ-483 reference-host regeneration captures the live pause, inflow-cap, freeze and dynamic market protocol-account checks on Baseline splits.
runtime/bleavit-runtime/src/weights/pallet_conditional_ledger.rs split_gate: the SQ-483 reference-host regeneration captures the live pause, inflow-cap, freeze and dynamic market protocol-account checks on gate splits.
runtime/bleavit-runtime/src/weights/pallet_conditional_ledger.rs split_scalar: the SQ-483 reference-host regeneration captures the live pause, inflow-cap, freeze and dynamic market protocol-account checks on scalar splits.
runtime/bleavit-runtime/src/weights/pallet_conditional_ledger.rs sweep_dust: SQ-483 makes protocol-account classification include the bounded market ownership index; the full-batch benchmark uses the valid worst distribution of 100 distinct claimant owners across 100 proposal rows, measuring every owner-specific lookup, counter, system-account and deposit-refund path.
runtime/bleavit-runtime/src/weights/pallet_conditional_ledger.rs sweep_dust_baseline: SQ-483 makes protocol-account classification include the bounded market ownership index; the full-batch benchmark uses the valid worst distribution of 100 distinct claimant owners across 100 Baseline rows, measuring every owner-specific lookup, counter, system-account and deposit-refund path.
runtime/bleavit-runtime/src/weights/pallet_market.rs close: the SQ-483 full-pallet reference-host regeneration replaces a stale close weight and captures its existing DecisionWindows read/write path.
runtime/bleavit-runtime/src/weights/pallet_market.rs crank_observe: the SQ-483 full-pallet reference-host regeneration replaces a stale crank weight and captures the existing epoch, decision-window, freeze, and treasury storage paths.
runtime/bleavit-runtime/src/weights/pallet_market.rs create_market: SQ-483 adds active/stored capacity preflights, active-count accounting, protocol-account registration, and current auxiliary-state writes to market creation.
runtime/bleavit-runtime/src/weights/pallet_market.rs reap: SQ-483 adds the atomic fixed-universe protocol-inventory discard (28 proposal cells at the benchmark maximum), account unregister, retained-row cleanup and treasury mirror before a market row is removed.
runtime/bleavit-runtime/src/weights/pallet_market.rs seed: the SQ-483 full-pallet reference-host regeneration replaces a stale seed weight and captures the existing protocol-account, pause, parameter, POL, and treasury initialization reads.
runtime/bleavit-runtime/src/weights/pallet_market.rs try_state: SQ-483 extends try-state with retained-capacity, canonical custody, active-count, protocol-account, bounded inverse terminal-marker, seed/rerun/POL and terminal-auxiliary identities; the reference-host fixture saturates all 2,240 retained books, seed markers and rerun markers, all 4,480 protocol accounts, and all 196 active books/POL commitments with full checkpoint/window/owner vectors plus the matching owner/latch indexes (24,464 measured reads).
