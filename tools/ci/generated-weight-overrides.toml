# Deliberate hand-written weights inside frame-omni-bencher-generated files.
#
# 15 §4.5 forbids these by default: the generator does not emit them, so a
# regeneration deletes them, and the deletion presents as a weight *decrease* —
# invisible to a growth-only gate. Every entry here is therefore a live hazard
# with a named owner, not a settled exception.
#
# Format (one function per line):
# <repo-relative-weight-path> <function>: <non-empty justification>
#
# Entries expire mechanically: `check-generated-weights.py` fails once the
# function carries a measured value or disappears, so an override cannot outlive
# its reason.

runtime/bleavit-runtime/src/weights/pallet_xcm.rs teleport_assets: protocol-disabled by the production XCM filters (09 §6.2); retains a fail-closed Weight::MAX because no valid benchmark fixture may bypass those filters, and Weight::MAX cannot undercharge
runtime/bleavit-runtime/src/weights/pallet_xcm.rs execute: protocol-disabled by the production XCM filters (09 §6.2); retains a fail-closed Weight::MAX for the same reason as teleport_assets
# (SQ-500, closed 2026-07-26) `pallet_guardian::on_initialize` was here: a
# hand-written 1.95 ms / 220 r / 150 w envelope held above a fixture that
# measured ONE settle where production could run 128, because the sweep had no
# per-block bound and no weight value is the right fix for that. The bound now
# exists (13 §2 `GuardianMaintenanceBatch`, 06 §5.4) and the fixture saturates
# it, so the function is a real measurement and needs no override.

# NOTE (SQ-490): the gate checks two things, and only the second was in the
# first draft of it. (1) the generator's `Minimum execution time:` line must be
# present, and (2) the body must contain **nothing outside the generator's own
# grammar** — because a term spliced into an *already generated* function keeps
# that line, so a marker-only check would pass it. That was literally half of the
# SQ-490 defect: a hand-written function (no marker) plus three
# `.saturating_add(Self::collator_compensation())` call sites inside functions
# that had one. Codex raised it on #180; the grammar check closes it, validated
# against all 356 real functions with zero false positives.
#
# Still out of scope of *this* gate, deliberately: a function *preserved verbatim*
# from an older generation is indistinguishable from a fresh one — it has the
# marker and clean grammar. That is staleness, not hand-writing.
# `pallet_assets::migration_v2_foreign_asset_set_reserve_weight` is exactly that
# case.
#
# Staleness is now gated separately, per function, by
# `tools/ci/regenerate-weights.py --check` (CI job `benchmark-smoke`), which
# re-measures every pallet and diffs the committed storage dimensions. It reads
# the `**Preserved, not re-measured` marker on that function and reports it on
# every run, so the one function backed by no fresh measurement stays visible
# instead of resting on a prose comment nobody re-reads.

# TR6 (2026-08-10): `fund_trading_rewards` is a new `pallet-futarchy-treasury`
# call whose runtime `TradingRewardFunding` adapter refuses every move until
# TR7 wires `pallet-trading-rewards` into `construct_runtime!` — there is no
# real call path yet for `frame-omni-bencher` to measure. It expires the moment
# TR7's real adapter lands and B5/TR7 regenerates this file. TR7 must also
# implement `BenchmarkHelper::prime_trading_reward_headroom`, or the
# regenerated figure measures the authorization leg alone and lands *downward*,
# which the growth-only regression gate cannot see.
runtime/bleavit-runtime/src/weights/pallet_futarchy_treasury.rs fund_trading_rewards: TR6 — the TradingRewardFunding adapter is an unwired refusal stub until TR7 wires pallet-trading-rewards into construct_runtime!; no real call path exists yet to benchmark either the return leg or the authorization leg.
