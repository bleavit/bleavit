---
paths: ["app/**"]
---

# App code rules (canonical cross-platform Bleavit client)

The frontend invariants INV-FE-1…15 (`docs/architecture/15 §2`) are normative and
certification binds to their exact texts. The app lives at `app/` (10 §10.1) — the single client monorepo, moved there from
`frontend/` on 2026-08-03 with N10's `packages/bleavit-client-ts` unchanged.
Practical consequences:

1. **Authoritative reads (INV-FE-1).** Transaction-critical values come only from
   finalized, light-client-verified state. RPC-fallback, provider, or **host-routed
   (Product SDK)** data is never promoted to verified; verified status requires a
   light-client re-read. `transport-host` reads are `provider` forever — there is no
   promotion path, so a host-routed build cannot sign in normal mode.
2. **Provenance typing (INV-FE-9, 10 §2.1).** Every displayed value carries a typed
   status — six of them: `verified-finalized` / `verified-best` / `derived-local` /
   `provider` / `stale-cache` / `external-proposal`. **`Finalized<T>` is constructible
   only inside `app/packages/chain-client`** — its brand is a module-private
   `unique symbol` **in the type itself** (a structural intersection over
   `status.kind` alone is satisfiable by any object literal, which is the defect an
   earlier draft of 10 §2.1 shipped). The single `as Finalized<T>` lives in
   `chain-client/src/provenance.ts`, and `as unknown as` is banned across `app/`.
   **The brand does not stop assertions** — `x as Finalized<T>` is a narrowing
   assertion TypeScript permits, proven empirically by the corpus — so
   `app/tools/check-finalized-casts.ts` is the other half of the control, not a
   belt-and-braces extra. Never put the brand in
   `shared-types`: if the universal sink package can construct it, 10 §2.1 is void
   silently, with green CI. UI components reject unlabeled values by type.

   **A third control covers the render edge**: `<Panel title={`Referendum ${id.value}`}>`
   typechecks perfectly and puts a chain read on screen with no badge, because the payload
   of a `Verified<string>` is a `string`. `app/tools/check-render-provenance.ts`
   (+ `:witness`) is type-aware for a reason — a syntactic version fires on `key={...}`,
   on `event.currentTarget.value` and on the verification panel's release-constant rows,
   and a gate that fires on correct code gets switched off. Its **rule B** is the one that
   is easy to write by accident: a value derived from two reads carrying *one* input's
   status promotes provider data to verified **by arithmetic**, which no badge type and no
   firewall rule can see. Use `combine`/`combine2` from `@bleavit/shared-types` — the
   result takes the weakest input's status, and two verified reads at **different blocks**
   refuse outright rather than claiming a block neither describes. Render the refusal with
   `<Derived>`; a missing figure must look missing, since rendering nothing is how "we
   cannot say" becomes indistinguishable from zero.
3. **Package firewall (INV-FE-3, 10 §10).** Respect the dependency-cruiser boundaries:
   `signing` and `transaction-builder` never import `providers`/`local-index`;
   nothing above `chain-client` bypasses it; `src/features/tx/**` never imports
   `src/features/analysis/**` or `src/features/handoff/**`. Provider data never
   satisfies a precondition. These are separate TypeScript projects — a forbidden
   import must fail **compilation**, with dependency-cruiser as the second gate.
4. **Pre-sign refresh (INV-FE-2, 11 §11.4).** Every submit path goes through the
   structural `refreshAndGate` — never add a code path that reaches a signer without
   it. The handoff import path's only output is a `TxPreparation` entering **Draft**;
   it adds no edge to the tx machine.
5. **Zero infrastructure (INV-FE-4/6).** Every protocol workflow must work with no
   indexer, no RPC, no provider, no external tool, cleared storage. If a feature needs
   a server, it is out of scope — do not centralize it. **No MCP** in any form: no
   local or hosted server, no tunnel, no sidecar, no background service (D-21).
   **The handoff is the *default* surface (11 §11.2) but never load-bearing:
   demoting a screen behind "Advanced" is permitted, removing one is not.** The two
   properties are independent, and the one that matters is asserted by the 15 §4.8
   no-infra certification run — which executes with the handoff surfaces disabled, so
   a screen that only exists on the imported-action path fails it. When simplifying the
   front door, move surfaces; never delete them, and never make one reachable *only*
   through an external tool.
6. **No telemetry, no remote config (INV-FE-13).** No analytics, no fetch-to-configure
   patterns; behavior changes only by shipping a new verifiable release. The handoff
   packages contain **no network primitive at all** — `fetch`, `XMLHttpRequest`,
   `WebSocket`, `EventSource`, `sendBeacon` and dynamic URL `import()` are gated out
   by CI. Never add a `connect-src` entry for an external tool vendor (12 §5.1).
7. **No hardcoded chain constants.** Everything in 02 §9 is read from chain
   metadata/storage; the no-literal lint gate fails the release otherwise.
   **`CRITICAL_SURFACE` and `SUPPORTED_RUNTIMES` are generated and compared, never typed** (F4):
   the first re-derives from `tools/release/surface-manifest.json`, the second is checked in
   both directions against `app/fixtures/chain-feed/`. Run `pnpm -C app run surface:generate`;
   never hand-edit the output and never edit a source to match an output. The TS
   protocol math (`packages/protocol`) must match the CI-regenerated vector corpus
   (04 §5, 15 §4.4) — never hand-adjust an expected value. `CRITICAL_SURFACE` is
   generated from `tools/release/surface-manifest.json`, never hand-listed.

   **The rule governs chain *tunables*, not the math kernel** (classified
   2026-08-03, PLAN.md · Decision log). `packages/protocol` compiles in the 64-entry
   `exp2` factor table, `ln 2` to 96 bits, the guard-bit count and
   `LMSR_DOMAIN_BOUND = 48` because the package **is** the 04 §4 kernel: none of
   those has a 02 §9 row, a `Params` key, or a governance track that can move it,
   and there is nowhere to read them from. `USDC_ONE` (D-17 identity pin) and
   `BPS_DENOMINATOR` (the unit 02 §9 publishes `Market::Fee` in) are classified the
   same way. Everything that *is* a tunable — `mkt.fee`, `mkt.kappa`,
   `mkt.obs_interval`, `MinTrade`, `MaxTradeRatio` — is a **function argument with
   no default**, so a caller that forgets one gets a type error rather than a stale
   launch value baked into a quote.

   **The no-literal gate landed with F11**: `pnpm -C app run check:chain-literals`
   (+ `:witness`). It parses 02 §9's own frozen-constant table and applies two rules —
   **A**, a frozen constant's *name* bound to a numeric literal (which stays a defect
   when the value is currently right), and **B**, a distinctive frozen *value* appearing
   bare, restricted to four-digit-plus values because `32` and `64` are hash widths and
   array sizes everywhere. Exemptions are **classified groups** in
   `app/tools/release/sources/chain-literal-classification.json` — a value, in named
   files, for a stated reason — never line waivers, so a new file cannot inherit one by
   copying a number. The kernel constants are one such group, as this rule required.

   **Corollary — the port reproduces the runtime's integer path, deliberately.**
   Arbitrary-precision decimals would be *more accurate* and would be wrong: 04 §6.1
   refuses a trade when `cost + fee > max_cost`, and three roundings decide the last
   base unit (`fx` floors, the ×10⁶ rescale truncates, the charge ceils). Never
   "improve" the precision of `packages/protocol` — a quote a base unit under the
   chain's own figure hands the user a transaction that reverts.
8. **Two ledger domains, never merged (11 §11.2a, 10 §11 — contract v23).** The client serves
   external/hosted books as ordinary S3/S4 surfaces. Domain is a property of the **datum**, derived
   by comparing an id you already hold against the **`ConditionalLedger::ServiceIdBase` metadata
   constant** — never from the call site, the cache key, or a name, and never from the literal
   `1n << 63n`, which rule 7 forbids and which is why 02 §9 gives the boundary a metadata home at
   all. **No selector, store slice or component may produce a cross-domain total**:
   solvency (I-4) holds per instance against its own sovereign account, so a merged figure asserts a
   backing pool that does not exist. `account_positions()` ↔ instance `()`, `service_positions()` ↔
   `ServiceLedger`; the FE-P2 conservative cross-check runs against that domain's own prefix, never
   the other's. **Writes route the same way**: the two ledgers are two pallets, so a service-domain
   split/merge/transfer/redeem is built against `ServiceLedger.*` and a primary one against
   `ledger.*`. Only `market.buy/sell` are domain-agnostic (the market pallet routes internally).
   Never give a transaction builder a default instance — and do not offer the gate or `*_baseline`
   calls on a hosted position, which has neither leg. External activity renders as an operational
   diagnostic, never as governance participation or protocol health.
9. **Local storage is disposable (INV-FE-7).** The transaction path never reads
   IndexedDB; rebuilds are automatic; treat eviction as a performance event.
10. **Fail safe (INV-FE-12).** Unknown runtime ⇒ explicit `restricted`/`read-only-
    incompatible` modes; undecodable data renders as raw SCALE with a warning; never
    guess at encodings. Platform and signer capabilities are a fail-closed lattice: an
    unproven capability is **absent**, and absence disables the dependent surface with
    a named reason — never a silent fallback.
11. **Imported intents are input, not data (10 §10.2, 11 §11.14).** An intent supplies
    a choice among a closed action set, an id, and ceilings — nothing else. It carries
    no free text and no bytes-typed field; `action` and `limits` are closed objects and
    an unknown key inside them is refused (`FE-HANDOFF-004`). Bleavit **never widens a
    limit, only narrows it**. Never accept an encoded call from any external source.
    A capsule MUST carry a labelled book `kind` so an external book's prices can never
    leave the app looking like a governance market's (11 §11.2a).
12. **The chain feed is generated, committed and drift-gated — never hand-edited (F2, 10 §5.1).**
    `app/fixtures/chain-feed/<spec_version>/`, `app/fixtures/chainhead/` and
    `app/packages/papi-descriptors/` are all **produced** by `tools/release/` and PAPI,
    and all three are committed because 10 §5.1 requires descriptors committed per
    `spec_version` and because `install --frozen-lockfile && tsc -b` must work offline
    (the Arweave distribution and the INV-FE-4 no-infra run depend on it). Regenerate
    from `app/fixtures/chain-feed/README.md`; never patch a file in place, and never
    "fix" a red drift gate by editing the artifact it is comparing.
    **The pair is not optional**: a primary runtime and its terminal-recovery runtime
    are separate live-capable `spec_version`s and both must be present, because the
    recovery image can become current under `OnlyInherents` and treating its descriptors
    as operator-only strands the canonical frontend during exactly the incident they
    exist for. `tools/ci/check-chain-feed.py` enforces the pairing itself — half a pair
    reads as a complete feed to any consumer that opens one directory.
    **Assert bounds against the specification, not against the recording.** Reading 32
    out of a fixture and asserting it equals 32 proves nothing; 02 §9's frozen table is
    the expected value, so a runtime that changed a bound fails even though its own
    metadata is self-consistent. Likewise a **whitelist is not a surface check**: PAPI's
    `applyWhitelist` filters silently, so an entry naming an absent surface yields a
    smaller descriptor set rather than an error.
13. **One chain connection, one home (10 §2.1/§4.1, F3).** `packages/chain-client` is the only
    package that may import `polkadot-api` or `smoldot`, enforced by
    `only-chain-client-opens-a-chain-connection`. The reason is not tidiness: a second package
    able to construct a chain connection would not need to forge the brand — it could serve reads
    that never passed the finalized-only discipline. `bleavit-client-ts` and `papi-descriptors`
    are exempt because they are not part of the canonical client. Inside the package, the
    transport is **injected** and reads take the block **explicitly** (`storage(at, …)`), so
    "read at the block I pinned" is unbypassable rather than checked around — a guard that
    re-checks the head before issuing a read does not prevent the read happening after it (V-84).
    A chain spec is trusted input to smoldot: verify the **bundled bytes** against the release
    pin first, apply any expert bootnodes after, and treat the §3.1 genesis check as a separate
    obligation the hash pin does not discharge.

    **dependency-cruiser records any specifier it cannot resolve verbatim** (V-86, V-92) — an
    uninstalled external package *and* a workspace subpath export like
    `@bleavit/signing/testing`, whose `exports` map enhanced-resolve does not follow. A rule
    written against only the resolved path can never fire. Use `EXTERNAL()` /
    `WORKSPACE_SUBPATH()` from `app/tools/depcruise-external.ts`, and add a witness module —
    a rule proven only by a green run is not proven.

    **The same applies to the negative-compilation corpus** (V-91): a fixture must declare the
    error it produces (`// expect-error: TSxxxx` on line 1), because "did not compile" is also
    what a missing dependency looks like.
14. **No control characters in source, and `cat -A` is how you find them.** Twice in one
    session a byte below 0x20 reached a source file and broke something in a way that read
    as a logic error: a literal **NUL** in `tests/receipts` (which made git classify the
    file as binary, so its diffs showed no lines and `grep` skipped it silently), and a
    literal **backspace** inside a regex, produced by writing `\b` through a shell heredoc
    — the pattern then matched nothing while the assertion failed on a string that plainly
    contained the word. Neither is visible in an editor or in a diff. **Prefer writing
    files with the Write tool or a Python heredoc over shell interpolation for anything
    containing backslash escapes**, and reach for `cat -A` the moment an assertion fails
    against a value that obviously satisfies it. A sweep is four lines of Python over every
    source file — **run it before claiming the tree is clean, because the third instance
    arrived through the Write tool rather than a shell** (2026-08-06, F9): six raw NULs
    reached `packages/providers/src/snapshot.ts` and `tests/providers/snapshot.test.ts`,
    every suite passed, and the only reason they were found is that a test asserting a
    separator was a *space* passed anyway. Two lessons beyond "sweep": a passing test is
    not evidence about bytes it never inspects, and where the byte was a **key separator**
    the repair was to delete the separator, not to fix it — `JSON.stringify([a, b, c])` has
    no separator to corrupt, and it also closes the collision an ordinary space would have
    left behind in any label containing one. **A fourth arrived the same day through the
    *Edit* tool** (V-168), so "prefer the Write tool over shell interpolation" is
    insufficient rather than wrong: a template literal `` `${TAG}\0` `` resolves on the way
    to disk exactly as a heredoc does. Its tell was new and worth knowing — `grep` on the
    file returned **nothing** for a function plainly in it, because git and grep classify a
    file holding a NUL as binary and report no matches rather than an error, which reads as
    *the symbol does not exist*. **Write a separator as a byte, never into a string**
    (`preimage[tag.length] = 0`, or `String.fromCharCode(0)` as `handoff-envelope` does),
    and run the sweep before claiming the tree is clean.

15. **Pinned versions.** The stack pins live in 01 §9 / 10 — PAPI 2.x, smoldot 3.x,
    Vite 8, Dexie 4, Tauri 2.x. Do not bump majors without a PLAN.md decision-log
    entry. `app/` is its own pnpm workspace and its own cargo workspace (excluded from
    the root one); never let its dependency tree reach the runtime pins.

    **The Tauri entry is the loose one, and it is stated here rather than glossed**
    (corrected 2026-08-06, F22): 01 §9's frontend-stack list enumerates exact versions
    and **names no Tauri version at all**; 10 §10.1 says "the Tauri stack" without one.
    So "Tauri 2.x" above is this file's own reading, not a quotation. `src-tauri`
    therefore pins `=`-exactly in `Cargo.toml` — `tauri-codegen` decides *what bytes get
    embedded*, so a minor bump changes the artefact F22 attests and must be a reviewed
    diff rather than a resolver decision. Whether the Tauri stack belongs inside 01 §9's
    exact-pin regime is an open question for the doc set (PLAN.md · F22 residuals).
16. **The release tree is derived, and its inputs are what you edit (F11, 12 §1/§5).**
    `app/dist/` and `app/release-out/` are build output. The committed inputs are
    `app/tools/release/sources/`: where each `connect-src` class comes from, the
    INV-FE-11 chain-identity pins, the signing keyring, the 15 §4.8 diff baseline.
    **Never widen the allowlist by editing the emitted policy** — it is substituted into
    `index.html` at build time — and never add an external-tool vendor host to any
    source (D-21, 12 §5.1). An intended addition is a diff to *two* files, the source
    and `incumbent-connect-src.json`; that second edit is the entire control, because
    nothing mechanical distinguishes a gateway from a vendor endpoint dressed as one.
    A pin that cannot exist yet is a **readiness blocker**, never a `null` that ships:
    `release:build --production` refuses while any stands.

## Quality gates for `app/` (moved here from AGENTS.md · Quality gates, 2026-08-06)

These were one 38k-character table cell in the always-loaded `AGENTS.md`, paid for by
every session including those that never open `app/`. They are binding exactly as before —
this file loads whenever a session touches `app/**`, which is when they apply.

**Run:** `pnpm -C app install --frozen-lockfile` first, then the gates below.

`pnpm -C app install --frozen-lockfile`

· **`pnpm run build`** — `tsc -b` over the project-reference graph, which is the *primary* firewall gate: a forbidden import fails module resolution rather than being reported

· `pnpm run depcruise` (the redundant second gate)

· **`pnpm run depcruise:witness`** — a known-forbidden edge that MUST still be caught, because a checker that can no longer detect anything reports success, and a config change once made every rule vacuous under a fully green run

· **`pnpm run test:firewall`** — the 10 §10.2 negative-compilation corpus, the only suite that tests the firewall *rejects* rather than that the app works, with an anti-vacuity positive control

· **`pnpm run check:casts`** — the 10 §2.1 assertion gate, since the brand stops object literals but cannot stop `as Finalized<T>`

· **`pnpm run test:protocol`** — the 04 §5 / 15 §4.8 market-math differential, which reads `reference-model/fixtures/vectors.json` **in place** rather than a copy (04 §5's single-generator rule) and asserts 04 §4's four *distinct* error bounds separately, since the loosest would hide a violation of the tightest. It fails closed if the corpus is missing **or if its exact integers lose precision on load** — the raw 64.64 values run past 2⁵³ and `JSON.parse` corrupts them silently (V-74), so any future JS/TS consumer of that corpus must load through the same discipline. It also binds the client to **`crates/market-core/fixtures/chain-quote-agreement.json`** — written by the runtime's own `quote()` and checked on the Rust side (`cargo test -p market-core quote_agreement`; regenerate with `BLEAVIT_WRITE_QUOTE_FIXTURE=1`) — because the reference corpus certifies the *mathematics* and says nothing about which rounding applies in which order or which failure fires first. Both defects that gate was built from passed all 1,286 corpus rows. Neither CI job needs the other's toolchain; whichever side moved is the side that goes red

· **`python3 tools/ci/check-chain-feed.py`** (F2) — the cheap per-commit half of the 02 §11 descriptor-drift gate: no build, no node, ~1 s. Sweeps every `app/fixtures/chain-feed/<spec_version>/` directory against the *runtime source* (metadata ≥ v15, contract version vs `futarchy-primitives`, pallet set vs `construct_runtime!` honouring the `bootstrap`-gated `Sudo`, every frozen `surface-manifest.json` entry present and shaped as frozen, **and the *surface* set: every `#[pallet::storage]` item declared by an in-repo pallet must appear in the feed's metadata — SQ-582, because the pallet-set check compares *which pallets exist* and never *what they publish*, so a runtime that grew two storage items kept 45 pallets, `spec_version` 2 and contract v23 and went stale invisibly. One-directional and in-repo only: feed entries with no in-repo declaration are SDK pallets, not drift, and claiming that direction would make the gate fire on every upstream pallet and be switched off) and then enforces the **10 §5.1 pairing rule** across directories — exactly one primary and one recovery, recovery at primary + 1, profiles declaring each other, matching contract versions, directory name equal to the `spec_version` inside it. Both halves matter: the per-directory checks catch a blob that drifted from the runtime (a v14/contract-v9 one sat in the tree for eleven days under green CI), and the pairing check catches a feed shipping half a pair, which reads as complete to any consumer that opens one directory

· **`pnpm run descriptors:check`** (F2) — regenerates the PAPI descriptor set into a scratch tree and byte-compares all 16 files against the committed `packages/papi-descriptors`. `papi generate` is deterministic given (metadata, cliVersion), verified by regenerating into two unrelated directories. PAPI's own `generated.json` check is **not** a substitute: it hashes the metadata *input* and never reads `dist/`, so a hand-edited descriptor passes it. Regenerate with `pnpm run descriptors:generate`

· **`pnpm run test:mock-runtime`** (F2) — the 02 §11 row-4 consumer, replaying `app/fixtures/chainhead/`'s 222 transcripts with no node and no network. Asserts FE-R1's bounds against **02 §9's frozen table, never against the recording** (reading 32 out of a fixture and asserting it equals 32 proves nothing), surface completeness bidirectionally against the manifest, all 13 `FutarchyApi` methods callable, both ledger domains served separately per 11 §11.2a, and the whole metadata surface re-checked against the paired recovery runtime. The mock **refuses** an unrecorded request, because a test double that answers everything turns a missing surface into a green run. Still growing with the track: the INV-FE-4 no-infra run with providers, RPC **and the handoff surfaces** disabled (the 02 §9 no-literal gate, the hostile-intent corpus, the handoff no-network and CSP-diff gates and the `MockSigner` release-exclusion assertion all landed with F20/F11 and are listed separately below)

· **`pnpm run test:chain-client`** (F3) — the light-client layer: the §3.1 boot reducer against the spec's own mermaid diagram, the 10 §11 two-domain rules, and the chainHead transport driven at the **wire level** by the same recorded transcripts through a provider shaped exactly like `getSmProvider`'s, so the production request/operation code path runs per commit with no node. Its failure branches (`limitReached`, `operationError`, `stop`, an unpinned block, a paused operation) are injected, because the recorder only ever met a healthy node. **F18 added the storage-key builder, whose oracle is the third Rust-written fixture in this repo** — `runtime/bleavit-runtime/fixtures/storage-keys.json`, produced by the runtime's own `hashed_key_for` and read **in place** (`cargo test -p bleavit-runtime storage_key`; regenerate with `BLEAVIT_WRITE_STORAGE_KEY_FIXTURE=1`), because the two available TypeScript oracles are both blind: `app/fixtures/chainhead/`'s 65 keys are all 32-byte *prefixes* (the recorder reads whole maps with `descendantsValues`), and deriving vectors from `@polkadot-api/substrate-bindings` tests the library the builder is made of against itself. A wrong storage key is the reason this needs an oracle at all — it returns **no value**, which is indistinguishable from an account holding nothing. `storageKey` takes keys **pre-encoded, one per position**, because each is hashed separately and PAPI's `args` codec encodes the whole tuple as one buffer: that is the right pre-image for a single map over a tuple key (`Welfare.Snapshots`) and the wrong one for a double map (`ConditionalLedger.Positions`), which doc 02 writes identically as `(A, B) → V`. The fixture carries both shapes, both ledger instances (same key, different prefix), both `PositionId` variants (encodings of different length), a `Twox64Concat` entry, and `ForeignAssets.Account`, whose key 1 is an XCM `Location` and **not** the `u32` Asset Hub uses for the same asset — asserted equal to the 02 §8 pinned `USDC_LOCATION_ENCODED`. The **concat suffix** has its own test: a digest emitted without its input is a strict prefix of the right key, and `descendantsValues` answers a prefix with the whole map, so the near miss returns everybody's balance rather than nobody's; an unknown hasher therefore throws rather than degrading to it. Fixture *coverage* is asserted on both the Rust and TypeScript sides, since a fixture-driven test is only as strong as its fixture. **F18's composition root closed the other end**: `storageKeyBuilder(codecs, metadata, pallet, item)` takes keys as typed **values**, because `args.enc` encodes the whole tuple but **`args.inner` is one codec per hashed position** — measured over **739** storage items on both pinned chains and all 11 fixture pre-images, `Welfare.Snapshots` included, where it is **1 and not 2**. That is a `@polkadot-api/substrate-bindings` **internal with no compatibility promise**, so the builder **refuses unless metadata and codecs agree on the arity** — neither presumed right — and the agreement is re-checked per commit over both chains' whole surfaces. It also refuses an **absent** storage item rather than reading it as a plain value: a plain item has zero hashers, so a typo and a real item would build the same 32-byte prefix, which the node answers with the whole map. `StorageHasher` lives in `metadata.ts` and not beside `storageKey` — a `no-circular` violation `depcruise` caught. And a hand-written SS58 encoder was never needed and would have been wrong: `getSs58AddressInfo` calls **20-byte and 33-byte** public keys valid, so *"check `isValid`, take `publicKey`"* hashes a non-`AccountId32` into `System.Account` and yields a well-formed key returning no value — *this account holds nothing*. **F8's per-event account extraction lives here too** (`event-accounts.ts`), because `local-index` may not import the chain SDK and the discrimination has to come from **metadata type ids**: doc 02 §6 gives field lists only for the events it bolds (31 of the ingest set carry none), so no table can be written from it, and PAPI decodes `AccountId32` to a plain SS58 **string**, so a walk over a decoded event cannot tell an account from any other string field. The account is one resolved type id — **zero and more-than-one are both refused at construction** — and the walk descends the declared type tree beside the decoded value, so `Vec`/`BoundedVec` are not special cases. Two things it pins that the obvious implementation gets wrong, both silently: accounts leave as a branded **`AccountKey`** (32-byte public key), never the SS58 string PAPI produced, because that string carries **this chain's** prefix (22622, not the generic 42) and the ingest decision is a string comparison — a watched set in any other rendering matches nothing ever, presenting as an **empty transaction history** with no error (V-164); and the type-id premise is cross-checked against FRAME's own `typeName` text over every event field, which found **two** events a name-based table would have dropped (`Session.ValidatorDisabled` / `ValidatorReenabled`, whose `T::ValidatorId` *is* `AccountId` — V-165). The recorded corpus cannot serve as the oracle here and that was measured: `storage.system.events.json` decodes to two `System.ExtrinsicSuccess` events and names no account at all. **The firewall's external-package matcher is witnessed** (`tests/depcruise-witness/forbidden-external.ts`, sharing `app/tools/depcruise-external.ts` with production): a `node_modules/…` `to.path` can never fire, since an unresolvable external specifier is recorded by its bare name — which had silently voided three CI-fatal rules including D-21's no-network-primitive-in-handoff (V-86). **F18 added the second light client** (11 §11.9.1's Asset Hub connection) and the whole of it is asymmetry: a relay or parachain failure aborts `startTopology` and tears the topology down, while **every** Asset Hub failure is a returned value that leaves the other two running — no failure of the deposit leg may become a failure of the app. It attaches **lazily**, through `Topology.attachAssetHub` rather than a `startTopology` option, because 11 E17 says *"the AH chain is not connected at boot"* and 10 §9.3 budgets its chain spec the same way; a method rather than a free function so it closes over this topology's own client, relay `Chain` object and teardown list, which makes *"same smoldot instance"* structural and lets `stop()` reach a lazily-attached chain with nobody registering it. `AssetHubLeg`'s refusing arms carry **no `chain` field**, and `wrong-chain` **keeps the observed genesis** while removing the chain — `classifyForeign` reads *"a different chain, retrying will not change this"* off that hash, so dropping it would report a permanent condition as a transient one. An Asset Hub bundle pinning **our own** genesis is refused: it passes every other check and would put futarchy balances on screen under an Asset Hub label. The connect step lives in `asset-hub.ts`, **out** of `light-client.ts` (the one module no test executes) and injected, because both its rules fail silently — a transport that fails to open **detaches** its chain, and a second connect never hands the same chain to `getSmProvider` twice, which refuses a repeat with a *console warning, not an error* and would yield a transport connected to nothing reporting success

· **`pnpm run surface:check`** (F4) — `CRITICAL_SURFACE` re-derived from `tools/release/surface-manifest.json` and byte-compared, plus `SUPPORTED_RUNTIMES` compared **in both directions** against `app/fixtures/chain-feed/`, so a stale metadata hash or half a 10 §5.1 primary/recovery pair is a build failure rather than a runtime surprise (it caught a placeholder hash on its first run). Regenerate with `pnpm run surface:generate` — never hand-edit the output, and never edit the manifest to match it

· **`pnpm run test:descriptors`** (F4) — the 10 §5.2 three-mode classifier, tested on the probe outcomes a healthy chain never produces, which is why it is a pure function over probe results with the PAPI shape adapted separately. It pins two things that are easy to invert: `read-only-incompatible` is about the **`spec_version`**, not about how much broke, and an **unprobed** surface is refused rather than counted as passing. It also imports PAPI's real `CompatibilityLevel` enum and asserts the ordering, because that ordering is what `isCompatible()` compares against and its default threshold is `BackwardsCompatible` — so `Partial` is not a pass (V-87)

· **`pnpm run check:budget`** (F1/FE-P4) — the 10 §9.3 smoldot transfer budget, **measured** rather than asserted (2.21 MiB gz against ≤ 3.5 MB, V-88) and fail-closed if the pinned package's layout moved, since a version bump that measured nothing would otherwise pass

· **`pnpm run test:tx`** (F6) — the 11 §11.3–§11.4 tx machine, the gate and the signer boundary. What it asserts is mostly the **shape of the state space**, because rule 1 asks for the gate to be unbypassable *structurally*: `AwaitingSignature` has exactly one inbound edge and requires a branded `GatePassed` no caller can write, and the lifecycle is parsed out of 11 §11.3's own text. **The negative-compilation corpus now declares the error each fixture must produce** (`// expect-error: TSxxxx` on the fixture's first line): asserting only "did not compile" let two fixtures pass while failing with `TS2307, module not found` (V-91) — and `TS2307` is still the *intended* proof for the three where module resolution is the gate, so the expectation is per fixture rather than a blanket ban · It also binds the client to **`runtime/bleavit-runtime/fixtures/multisig-derivation.json`** — written by `pallet_multisig` itself and read **in place**, the same single-generator discipline the vector corpus follows. `as_multi` executes the inner call as an account *nobody chooses*, and that account is what every §11.5 precondition row reads; taking it as a caller-supplied field failed in the dangerous direction, reporting a healthy balance for an address the transaction never acts as. So it is **derived** and the result is branded (`MultisigAccount`, mintable only by `deriveMultisigAccount`). The fixture publishes the SCALE **pre-image as well as** the account, because a wrong hash, a missing compact length prefix and a byte-order slip otherwise all present identically — and it carries a **64-signatory** case, since SCALE's compact length widens at 64 and a hardcoded one-byte prefix derives a wrong, well-formed address for every large committee while passing every small one. The Rust side checks the file still describes that runtime (`cargo test -p bleavit-runtime multisig_derivation`; regenerate with `BLEAVIT_WRITE_MULTISIG_FIXTURE=1`), and asserts its own published pre-image hashes to its own published account, so the two fields cannot disagree in a way only the client would notice **`pnpm run test:verify`** (F10) — the INV-FE-8/11 release self-check and verification panel. Three properties, each of which fails silently: a **genesis mismatch is terminal** rather than INV-FE-12's `restricted` (those modes are for a runtime whose *surface* is partly unknown — the same chain further along; a wrong genesis is a *different chain*, where every balance rendered belongs to someone else), so the verdict carries no severity and no override to weigh; the **chain-spec check runs before the genesis check**, because bytes the release did not choose yield a genesis that describes the wrong thing; and **divergence is surfaced, never repaired**, enforced structurally — the package exports no repair/refetch path and a test asserts it, since a refetch asks the channel that just served wrong bytes for better ones. The self-check reports an **`unexpected` served file** as its own finding, the direction a manifest-driven loop cannot see. `buildPanel` is **synchronous and takes no chain handle**, because 10 §3.2 lists the panel among what still renders when smoldot never started

· **`pnpm run test:local-index`** (F8) — 10 §6.3 gap-tolerant coverage, whose load-bearing rule is a **refusal**: adjacent ranges of different provenance are never merged, because a joined range must claim one origin and claiming `self` promotes provider blocks to verified, which §2.2 gives no path for. Holes are first-class, and the suite replays §6.3's **1,200-block gap** to show it is not interpolated over. Stated precisely, because the difference matters: the `self` brand is a **compile-time** control, so an untyped caller — a record rehydrated from IndexedDB, most obviously — can still write `origin: 'self'` and be believed, and nothing local can prove a range came from a light client. What makes that harmless is INV-FE-7 plus the firewall (**the transaction path never reads this package**), and the suite asserts that rule is written down rather than assumed. The rest is fail-closed arithmetic: block heights are **u32-bounded** (`Number.isInteger(2**53)` is `true` and `2**53 + 1 === 2**53`, so unchecked "integers" let two ranges one apart join and claim a block nobody ingested); an inverted range **or** span refuses rather than returning `[]`, which reads as *complete coverage* in the one module whose job is to make missing data visible; and `holes` is checked as **derived, never supplied**, so a span-bounded coverage cannot be stored and then silently lose its edges on the next mutation

· **`pnpm run test:intents`** (F20) — the hostile-intent corpus, over the 11 §11.14.1 **admission** checks (properties of a *file*, never preconditions). The load-bearing one is the **digest**, which shipped *defined and unreachable*: `FE-HANDOFF-010` was a constant no path emitted, so an altered or truncated document was admitted exactly like an intact one and no happy-path test could see it — the suite therefore mutates a signed document and requires the refusal. The gate is **unskippable by construction**: `admitIntent` is the only entry point and its hash function is a *required* argument, since an optional one is a digest check that defaults off (the `assertCheckable` defect again). The rule most easily got backwards is 10 §13.2's asymmetry: an unknown key at the **top level** is a producer annotation and tolerated, while one inside `binding`/`action`/`limits` is refused (`FE-HANDOFF-004`) because there it is a proposed semantic and *"precisely where an encoded call would be placed"* — both halves asserted on one document so neither passes for the wrong reason. Also pinned: **`FE-HANDOFF-009` stays retired** (the family is `001..013` *with the gap*, since a reused code makes two failures indistinguishable in every record on either side of the reuse), a **newer** runtime is refused while an **older** one is rebuilt, expiry is **-008 on arrival and -011 at the refresh** rather than one code for both, the `id` is a canonical **u64 decimal** (which is what gives 11 §11.14.4's id-substitution defence something to resolve), the parser bounds are **computed** from 02-frozen type widths and counted in **UTF-8** bytes, and every limit **narrows only** — a stated limit with no recomputed chain value is refused, never dropped, since dropping it encoded a trade with no cost bound at all

· **`pnpm run test:ui`** and **`pnpm run test:screens`** (F7) — the render layer and the default surface. INV-FE-9 is made structural by a fact about React rather than by a convention: `Verified<T>` is an **object** and `ReactNode` does not accept one, so a screen holding a model whose leaves are `Verified<T>` cannot put a chain value on screen except through a `ui` component, which derives the badge from the status it was handed — a caller-supplied label would let a `provider` figure render under the word *verified*. The residual is stated rather than hidden: `{x.value}` unwrapped by hand still typechecks, and what covers it is the corpus fixture plus the fact that the correct path is the easy one. 11 §11.2 constraint 3's five never-defer facts are enforced **twice, because either alone is weak** — `AboveTheFold` is an object so a `Disclosure` cannot take one at compile time, and `AlwaysVisible` **throws** under a disclosure context, which is what catches the fact handed down three components and rendered inside a collapsed region by a caller that never saw both ends. The fact list is bound to the **section citations in doc 11's own paragraph**, so a sixth fact fails the suite rather than quietly not existing. `test:screens` additionally asserts 11 §11.2 constraint 1 in the direction that matters: with the handoff surfaces **disabled** (the 15 §4.8 no-infrastructure posture) every other screen is still reachable *and the client still has a front door* — a release whose entire navigation was optional would otherwise pass every happy-path test. Two spec rules are unreachable by construction rather than remembered: S2's `ProposalView` has **no field** for decision statistics on its pre-decision arm (verified — TypeScript narrows a discriminated union by its discriminant *before* excess-property checking, so an in-Trade uplift preview does not typecheck), and the confirm summary is a **branded** `DecodedCall` whose only producer takes a hex string, with the surface additionally refusing a decode whose `fromHex` is not the preparation's — 11 §11.3's substitution attack in its exact shape. **F18 added S12/S13's readers, the only ones in this client that span two chains.** Every other reader takes one reader and stamps one pin onto every leaf; a deposit model that did so would be one that had **mixed the chains**, so `assertOnePin` deliberately has no analogue and the check is per **leg**. `readDepositInputs(reader, reader, …)` is a slip that typechecks and after which every Asset Hub figure is a futarchy-chain read under an Asset Hub label — true about the wrong chain, which no badge and no later check can detect — so the reader pair is **branded** (a module-private `unique symbol`, the `Finalized<T>` construction, because a structural `{ local, assetHub }` is satisfiable by exactly the literal being avoided) and a negative-compilation fixture proves the hand-assembled form does not compile. The D-13 caps arrive already stamped and are the one place a foreign value could still enter the local leg, so their chain is checked too. **The two legs fail differently by spec**: §11.9.1 makes the Asset Hub connection a precondition row and E17 blocks the flow with diagnostics, while §11.9.2 degrades withdraw's destination check to a warning — so `readWithdrawInputs` takes **no Asset Hub reader at all**, which is that rule written into a signature rather than remembered. Fail-closed throughout: an unreadable `PhaseFlags` means D-13's caps **apply** (V-115's unsafe direction is skipping them), an absent Asset Hub account is a **real zero** and not a decode failure, and `assetHubReady` needs the foreign compat verdict **and** a viable account. **Its fixtures must vary chain and block independently** — deriving one from the other welded two properties into one and let a block-comparing mutant pass the entire suite (V-155). **Their composition root is `funding-composition.ts`, and it is per chain, twice**: `fundingKeys`/`fundingDecoders` build each of the four frozen surfaces from exactly one chain's metadata and descriptors. The decoders had to be **split per surface**, not per shape — one `assetAccount` served both Asset Hub's `Assets.Account` and this chain's `ForeignAssets.Account`, which is `readDepositInputs(reader, reader, …)` in different clothes, since a decoder is bound to one chain's codecs at composition time. It worked only by coincidence: measured, the two `AssetAccount` types are byte-identical on the pinned pair, so each decodes the other's bytes. Nothing gates that, and a field added ahead of `balance` would render a wrong balance under a `verified-finalized` badge. The USDC **Location** is a required parameter with **no default** — 02 §7.7 pins the Asset Hub per release, so a compiled-in default is a release constant that stops tracking the release — and both halves are enforced, a negative-compilation fixture for omission and a runtime test proving an `undefined` one **refuses** rather than falling back. Two of the four surfaces are structurally chain-bound (`Assets.Account` is Asset Hub-only, `Constitution.PhaseFlags` local-only) so a chain swap throws at construction; the other two exist on **both** runtimes and no artifact check can catch a swap there — the suite says so rather than implying full coverage, because `fundingReaders`' read-time chain refusal is the control that does cover it. The shape narrows are reached through **stub codecs**, since each guards against a value a real codec cannot produce: `providers`-only viability blocks exactly the users S12 exists for (a USDC-only account is `providers: 0, sufficients: 1`, and USDC is a *sufficient* asset), and `1.5 & (1 << 4)` is 0, so a `typeof number` bitset check silently reports *sudo absent* and skips D-13's caps

· **`pnpm run check:handoff-emitted`** (+ `:witness`, F7) — every import in the **emitted** modules on a handoff path, and the reason it exists is that the two documented gates are *both* blind to the case. Measured, not reasoned: `tsc --traceResolution` on a handoff-unit file importing `react/jsx-runtime` resolves it to `@types/react` at the workspace root and **compiles clean**, so 10 §10.2's *"module resolution cannot see it"* does not hold for any external package with a hoisted `@types` counterpart; and dependency-cruiser cruises **source** with `tsPreCompilationDeps`, so a compiler-injected import is absent from its graph entirely. A handoff screen written the obvious way would therefore have carried an external import into the D-21 trust domain under fully green CI, first failing at `vite build` as a packaging error. `jsxImportSource: "@bleavit/ui"` is the repair — the emitted specifier becomes a workspace one, so §10.1 stays literally true and the gate keeps full sensitivity — and this gate is what keeps the repair from being a convention. It reads the emitted trees because that is the only artefact reflecting **source and compiler**, so whatever a future JSX transform or helper-emit setting injects shows up here. Specifiers come off the **TypeScript AST**, not a regex: the first version matched `from '…'` and immediately reported `contexts` importing `"opt-in"` out of a doc comment

· **`pnpm run check:handoff-network`** (+ `:witness`) — D-21's *global* network primitives. The dependency-cruiser rule that claimed to enforce this matches **imports** (`axios`, `undici`, `ws`), and every primitive rule 6 names is a **global** — `fetch`, `XMLHttpRequest`, `WebSocket`, `EventSource`, `sendBeacon`, dynamic `import()` — so none was reachable by it and a handoff package could have called `fetch()` under fully green CI. The two are complements: one catches a library, the other the platform. The witness leg runs the same patterns over a fixture using every primitive and fails unless each is flagged

· **`pnpm run skills:check`** (F21) — `app/skills/`, the **producer** side of the handoff, whose two artifacts are generated rather than written. The **vendor instruction files** are assembled from `SKILL.md` + `reference/`, because the identical safety rules must reach a directory-shaped Agent Skill, a ChatGPT instruction **text box with no filesystem**, and a plain prompt — three hand-written copies agree on day one and diverge at the first amendment, *invisibly*, since nobody diffs a ChatGPT instruction box against a repository, and a stale copy is a producer operating under the previous version of the safety argument. The generator also **fails on a surviving pointer** to a file the inlined reader cannot open, because an instruction that cannot be followed teaches the reader that instructions here are optional. The **examples** carry their expected verdict in the *filename* and every one is executed against the **real parser**: a published example that no longer does what it says is worse than none, since somebody debugging against a stale corpus concludes the *client* is wrong and then loosens the document until it passes — exactly what `reference/safety.md` rule 7 exists to prevent. The label lives in the filename rather than a manifest because a filename cannot drift from itself. Coverage is asserted over the **nine codes an inbound document can cause**, with the exclusions named (`-011` needs live chain state; `-012`/`-013` are export-side), so *"we cover the family"* cannot quietly become *"we cover the easy ones"*. `skills/` and `schemas/` are **copied into the release tree before anything is hashed**, so the service worker pins them: a skill nobody can obtain from the client they are running is not delivered, and copied in *after* the map is baked they would be same-origin paths the worker refuses

· **`pnpm run schemas:check`** (F20) — `app/schemas/`, the three published formats, **generated from the parser's own declarations and byte-compared**. The way a hand-written schema breaks is not a missing field: JSON Schema's `additionalProperties` defaults to **true**, so omitting it on `action` publishes the *opposite* of 10 §13.2's asymmetry to the one audience that is writing producers — absence reads as permission. The generator also fails when the parser declares a field the schema does not publish, the direction that shrinks the format to whatever was written down, and a stray file in the directory is drift the per-file loop cannot see. The byte-compare is only **determinism**; what makes the schema *true* is the differential in `test:intents`, one corpus through both the parser and a validator that **throws on any keyword it does not implement** (with a negative control per keyword, since a validator that accepts everything passes every "valid documents validate" test perfectly). It found a real defect on its first run: the schema had `limits` all-optional while the parser **requires** a direction-matching monetary limit — prepare ⇒ `maxCost`, close ⇒ `minProceeds`, each refusing the other. *"There is no safe default for money"* is a property of the **file**, so the schema owed it, and a producer following the published version would have shipped `"limits": {}` and been refused at the user

· **`pnpm run test:contexts`** (F20) — export scope and the digest. Consent is **per export and never inherited** (`defaultScope()` takes no argument it could inherit from — opt-in alone is satisfied by a checkbox that remembers), pseudonymization is **labelled for what it does not do** (it leaves the holdings, which remain a fingerprint; what leaks is linkage, not content), and the package exports **nothing that could pass for authentication** — capsules are deliberately unsigned, because signing one with the user's chain key manufactures an artifact that looks authoritative (10 §13.1). **`buildCapsule` checks scope in both directions**, and the second is the one that lies: data outside the consented scope is *refused* (`FE-HANDOFF-012` — until F20 closed it, a code with **zero emitting call sites**, which no green run distinguishes from one that never fires), and a consented scope with **no read supplied** is refused too, because a capsule announcing `positions` and carrying none reads as *this user holds no positions* — a false claim about the chain from the document whose only job is true ones. Empty is data, absent is not. A book's `kind` is **checked against its id band, never derived** (deriving exports a label the user never saw on screen), and positions are keyed **by domain**, so 11 §11.2a rule 2's merged total has nowhere to live rather than being a rule to remember. The **`FE-HANDOFF-*` family lives in `handoff-envelope`**, not in the parser: `receipts` cannot import the parser across the §10.1 firewall, so it had declared its own sentence for `FE-HANDOFF-013`, and because no call site takes both the compiler could never see the drift — the one table now also carries the **`recovery` 10 §13.3 requires per code**

· **`pnpm run check:chain-literals`** (+ `:witness`, F11) — 10 §5.4's *"no numeric chain constant may appear as a literal in FE source"*, in **two rules because either alone is blind or useless**. Rule A catches a frozen constant's **name** bound to a numeric literal (`const MAX_POSITIONS_PER_ACCOUNT = 64`) and catches it *when the value is currently correct*, which is precisely what a value scan waves through — the client stops tracking the chain the moment governance moves the bound. Rule B catches a distinctive frozen **value** appearing bare, restricted to values of **four decimal digits or more**: `32` and `64` are hash widths and array sizes on every other line, so an unrestricted value rule fires constantly on nothing and gets switched off, while above a thousand the collision rate collapses. The narrowing is written down rather than hidden — a hardcoded `196` that is never *named* passes both rules, and what covers it is `CRITICAL_SURFACE` failing the build when a required constant is absent from metadata. Exemptions are **classified groups with a stated reason and a file scope**, never line waivers, so a new file cannot inherit one by copying a number: the 04 §4 kernel (`USDC_ONE`, `BPS_DENOMINATOR` — the package *is* the kernel and there is nowhere to read them from), the generated release pins (`critical-surface.ts`/`spec-versions.ts` carry the contract version the bundle *expects*, which is one half of 10 §5.2's comparison), and `PPM` (a unit of the 10 §13.2 document format, colliding with `MinTrade` only because one USDC is 10⁶ base units). The frozen set is **parsed out of 02 §9's own frozen-constant table**, scoped to that section (§7's storage table has the same four-column shape and was being read as constants, which also kept the fail-closed *parsed-nothing* check from ever firing) — and the scan runs on the **TypeScript AST**, not on lines: every hole adversarial review found was a *tokenizer* hole (a string containing a comment opener swallowed the following lines, a regex character class read as a comment, template interpolation was blanked, and `1n << 63n` and `0x10000` were invisible), so comments and string bodies are simply not literal nodes and both rules constant-fold. The witness asserts **per declared line** with negative controls that must produce nothing, because requiring only *each rule fired once* is satisfied by the two easiest fixtures

· **`pnpm run release:build`** and **`pnpm run test:release`** (F11) — the 12 §1/§5 release train, run **per commit**, because a release pipeline that only runs at a tag is one first debugged during a release. Seven ordered steps, and the order is the design: the `connect-src` substitution and the SRI injection precede the service worker's baked hash map (which pins `index.html`, so the file must be final before it is hashed), and `release.json` is written last (it pins `sw.js`, which the map substitution rewrote) — get either backwards and the release ships a map that refuses its own files, fail-closed but *at the user*. The gates it enforces: the **`connect-src` allowlist is derived** from declared sources (§6.2 bootnode manifests projected from libp2p multiaddrs, the baked ar.io gateway set, release-listed providers/RPC — the last two empty at launch, and *empty is not absent*: a deleted section is refused while an empty one is 10 §8.1's opt-in posture) and **diffed against a committed incumbent** (15 §4.8), so an addition fails the build rather than arriving one release at a time — which is the only mechanical form D-21's rule can take, since nothing can tell an ar.io gateway from a vendor endpoint dressed as one; an **unsubstituted placeholder fails** rather than shipping a control silently not applied; **SRI is emitted only where a browser enforces it** (`<script>`, and a `<link>` whose `rel` is `stylesheet`, `modulepreload` or `preload`) and a skipped `rel=manifest` is *reported*, because a decorative `integrity` attribute is worse than an absent one — a reader counts it; the **determinism check reports rather than rewrites**, since a normalizer that edits the tree makes two environments agree by erasing the evidence that they disagreed; the **SBOM is built from the lockfile, never the installed tree** (the tree describes the machine, the lockfile describes the recipe — and if they disagree the install was not `--frozen-lockfile`, which is the failure the SBOM exists to expose) and carries **no timestamp and no serialNumber**, which would otherwise make it the one file that differs between two environments building the same commit; and the emitted chunks are scanned for a **test-only signer**, the one 10 §10.1 rule dependency-cruiser structurally cannot answer because it is about the bundle rather than the import graph. The **service worker** (12 §5.2) bakes the per-file SHA-256 map so integrity never depends on a fetch, takes its cache name from `release.json`'s `arweaveManifestTxId` at install — which cannot be baked, since 12 §1.2 only learns the TXID after uploading the tree the worker is part of, and needs no trust because it *names a cache and does not authorize a byte* — refuses a same-origin path the release does not contain rather than forwarding it, and exempts exactly one file (`release.json`, whose detached minisign signatures are a stronger check than a hash the worker could only have learned from the same response). Three of its refusals were added after adversarial review and are the ones easiest to get wrong: a **non-2xx body is refused too**, because a `404` carrying attacker HTML for `index.html` is a response the browser *renders* and the status code decides nothing about what a user sees; the **cache is re-verified on read**, because `caches` is same-origin storage any script on the origin can write, so a forged entry would otherwise be served as verified forever; and the activate message requires an **explicit `pinned` boolean**, since a missing one defaulted to *not pinned* and let the shortest message anyone would try override the user's own statement. `test:release` drives the **built `dist/sw.js` in a `vm`** — a suite over the pure policy alone stayed green with the worker's digest check deleted — at `--test-concurrency=1`, because two `node --test` workers rebuilding `dist/` in parallel tear the tree and the failure reads as a worker defect. `release.json` is the producer for `packages/verify`'s `ReleaseIdentity` consumer, and the required field names are **parsed out of that interface**: a hand-written list beside the producer made the test agree with itself while the consumer required `releaseTxid` and the document emitted only `arweaveManifestTxId`. What is **deliberately absent**: the live Arweave upload (`arweave.mjs` is a pure driver over an injected uploader; the Turbo-SDK adapter is prototype gate **FE-P7**, whose `[VERIFY]` R-2 forbids resolving by assumption), and signing/attestation/the two-environment byte-identical proof (F13). `release.json` carries a **`readiness` block naming every unresolved blocker** and `--production` refuses while any stand

· **`pnpm run foreign:check`** (F4) — the 02 §7.7 foreign-chain feed: the Asset Hub pin and, load-bearingly, that **every §7.7 surface is present in the pinned metadata**. It is the one surface gate that **cannot** be Python: `extract-metadata.py` gets v15 by booting the runtime in `bleavit-node`, and a foreign runtime has neither the genesis config nor the pallets it expects, so PAPI reads **v16** straight from the wasm and `tools/release/scale_metadata.py` stops at v15. That makes TypeScript the right home anyway — what must be proven present is what the *client* will be able to name. 10 §5.2's foreign verdict probes exactly `FOREIGN_SURFACE`, so a surface that quietly left the upstream runtime would make that verdict **unable to fail** on it. It also checks the directory name against the `spec_version` inside, the metadata hash against the record, `FOREIGN_CHAIN_PINS` against the feed in **both** directions, that each pinned chain has descriptors, and that the genesis hash carries **at least two distinct sources** — genesis is the one field no artifact can produce (it is chain-spec state, not runtime), so it is read live per R-2 while descriptors still come from the wasm per 10 §5.1, and one operator agreeing with itself is not a cross-check. An upstream **deprecation** on a frozen call is *reported, not failed* — a deprecated call still dispatches and taking the deposit leg offline for a working runtime is the wrong failure; what must not happen is the deprecation going unseen until the call is gone (`limited_reserve_transfer_assets` is live, while `teleport_assets`/`reserve_transfer_assets` are already deprecated in the same runtime). Produce a pin with `tools/release/pin-foreign-runtime.py`, which refuses a wasm whose sha256 disagrees with its srtool digest

· **`pnpm run check:smoldot-surface`** (+ `:witness`, F1/FE-P2) — the pinned PAPI's issued JSON-RPC methods against the pinned smoldot's dispatch table. Both ends of the client's read path live in `node_modules` and neither is visible to any suite here: `tests/chain-client` replays *recorded transcripts*, which by construction answer whatever was recorded, so a bump that removed `chainHead_v1_storage` would first fail in a browser. **Both sides are derived, never listed** — the issued set is reconstructed from `@polkadot-api/substrate-client`'s own `methods.js` (including its `_vN_` infix, so a version change is picked up rather than silently mismatched), the served set from the base64+zlib wasm the smoldot package ships. It is a **removal** detector and says so: presence in the dispatch table proves the dispatcher knows a name, not that it serves one — measured, since smoldot 3.3.2 carries `system_localPeerId` in the same sorted run and answers it `-32000 "Not implemented in smoldot yet"` (V-137). The unimplemented case is covered at runtime instead, by reading a failed call as *unknown* rather than as a value

· **`pnpm run test:providers`** (F9) — 10 §8's optional-acceleration layer, and the **forged-snapshot corpus 15 §4.8 asks for, rejected per class**. The never-promote rule needs no test here (`Finalized<T>` is unnameable outside `chain-client`, so this package cannot produce one whatever it does); what F9 owns is what the client *says* about a provider. `SnapshotFinding` therefore names its screens rather than returning a boolean — a corpus proving only *"bad snapshots are rejected"* cannot tell you which screen was load-bearing when one regresses, and a screen that has stopped firing is invisible under a fully green run. **The corpus deliberately contains a document that is admitted**: a self-consistent deep forgery, which 10 §8.4 and 14 TH-50 both state plainly is *not* detected, so a corpus made only of rejections would be evidence for a guarantee the mechanism declines to make. The format lives in **one module the producer and the consumer share** — they must agree on *which bytes are hashed* or the pin is decoration, and a second serializer fails every pin from that producer, which a user reads as a corrupt download; it reuses `handoff-envelope`'s canonical JSON under a distinct domain tag, since 10 §13.1 already gives this repository exactly one answer to *which bytes*. Four things it pins that the obvious implementation gets wrong, all silently: `admitSnapshot` takes the **file text**, not a parsed object, because §8.2's *"reproducible byte-identically by anyone"* is a claim about **bytes** and a consumer that parsed first would accept two different files for one history — at which point a content pin no longer addresses content; the conservation replay checks **at every step**, since a forger who drives an account negative and back ends in a state a final-state check waves through; it deliberately does **not** assert I-1's cross-branch equality, which stops holding at the first `redeem` and would flag every settled vault as forged; and the hash function is a **required argument**, because an optional one is a pin that defaults off — the `admitIntent` defect again. Amounts are decimal **strings** (`u128` base units run past 2⁵³, and a rounded JSON number then fails the document's own replay for reasons that look like forgery — V-74's shape), and composite map keys are `JSON.stringify` tuples rather than joined strings, since any separator can occur inside an account label and two holdings colliding on one key silently become one row in the screen whose whole job is to notice a missing one. **Canonical form reaches the arrays too** (added while writing the producer, which is where it surfaced): canonical JSON sorts object *keys* and leaves array order exactly as given, so `vaults`, each vault's `branches` and `balances` had two legal spellings apiece and two honest producers would have published two pins for one history — the promise in §8.2 is *byte-identical reproduction by anyone*, and the way anybody finds out it is false is a user being told a correct snapshot is corrupt. `coverage` is **maximally merged** for the same reason, since `checkCoverage` permitted 10..11 beside 12..13 *and* 10..13. `ops` is deliberately **exempt** and that is not laxity: its order is the chain's, which is semantic — the replay is order-sensitive, so sorting would let an invalid history be reordered into a valid-looking one

· **`pnpm run test:snapshot-tool`** (F9) — `app/tools/snapshot`, the **producer** half of the format `test:providers` consumes, and it lives at `app/tools/` because 10 §10.1 says so in as many words (the repository root already has a `tools/release/` for chain-release tooling). Two properties carry the suite and neither is the happy path. **Byte-identical reproduction is proven by shuffling, not asserted**: the same history handed in with its arrays reversed and its movements out of sequence must yield the same file and the same pin, because two readers of one chain will not enumerate vaults, accounts or events in the same order and if that reached the file each producer's snapshots would look corrupt to the other's users. **The balance sheet is a differential against chain state, not a restatement of the fold** — `ArchiveExport.balances` is read at `range.toBlock` independently of the movements, and the driver refuses to publish when they disagree. A producer that folded its own ops would agree with itself by construction and §8.4's derived-row screen could never fail on anything it emitted, while the failure that actually happens to a snapshot tool is an **incomplete op set** (a variant not decoded, a range answered short), which is perfectly self-consistent and invisible to a self-fold. Three more refusals, each a case where the obvious implementation publishes something wrong: **coverage is what the reader *observed***, never the requested span — a reader that fails part-way and reports the request anyway publishes a document claiming history it never saw, which passes every screen because the movements it does carry are consistent, a forgery produced by accident; **two movements at one chain position refuse** rather than being tie-broken, since the replay is order-sensitive and no tie-break here can be right; and a **JSON number amount is refused at the input boundary**, because `BigInt` accepts a `number` and an amount past 2⁵³ would fold as its rounded value with nothing thrown (V-74). The last step of every build runs the **client's own `admitSnapshot`** over the bytes it just produced with the pin it just computed, so the tool cannot ship a document that fails at the user — including for reasons the driver does not know about, which is why it runs the real consumer rather than a checklist of what the consumer was believed to check. The **archive-node adapter is deliberately absent** and named as such: writing it by assumption is what R-2 forbids, and the client's own `chain-client` offers no archive path at all (smoldot serves `chainHead` only, 10 §4.2). The tool is its own **workspace package** so `@bleavit/providers` resolves under `tools/snapshot/node_modules` — declaring it at the app root instead hoists it and turns `tests/firewall/fixtures/forbidden-package-edge.ts` from TS2307 into TS2305, voiding the fixture whose whole proof is that the package is *unresolvable* there

· **`pnpm run test:platform`** and **`pnpm run check:embedded-tree`** (+ `:witness`, F22) — the desktop
channel. `platform` is the fail-closed capability lattice of rule 10 made structural: there is no boolean,
`absent` **carries the sentence the surface renders** (an empty one throws), the record is **total** so a
forgotten capability is a type error rather than an `undefined` that reads as *probably fine*, and `meet`
keeps **both** reasons — a user told one reason fixes it and meets the next. **A capability that has
evidence is *derived* from it, never written beside it** (added after review, PR #252): `desktopPlatform`
deliberately returns on a reported divergence, since INV-FE-8 surfaces and never repairs and a thrown
adapter is a divergence nobody can render — and the literal `proven()` next to that branch enabled every
`embedded-tree-attestation` surface in the one state the host said the tree had failed. It is now a total
`switch` over the attestation arm, so the two cannot disagree and a new arm fails to compile. The same
review found its two siblings in the same function: the desktop channel's `external-navigation` was the web
adapter's argument about *browsers* copied to a shell that grants no such permission, and a
`reported-verified` report over **zero pinned files** was believed, which is the empty-findings refusal's
exact mirror. It imports **no host SDK**, and
that is a decision: the permission 10 §10.1 grants would force host code into the web bundle or a second
build path, and the second is what F22 forbids, so the host arrives as an injected `HostBridge`. The
consequence is that `only-platform-touches-host-sdks` has **no real edge anywhere in the tree** and the
depcruise witness is its entire liveness proof — both spellings, since `EXTERNAL` takes an alternation and a
typo in either arm leaves half the rule dead. The **embedded-tree assertion** is the milestone's point: a
downloaded shell is covered by 12 §1's promises only if the tree it embeds *is* the published tree, so it is
compared against `release.json.perFileHashes` — the attested `dist/`, never the Arweave path manifest —
through `packages/verify`'s own `runSelfCheck`, at build time and again inside the binary **before it creates
a window** (exit `70`, no flag that downgrades it, no repair path; INV-FE-8's *"surfaced, never repaired"*
with the stronger option a shell has). Two implementations of one comparison agree only if something makes
them, so both read **`crates/embedded-tree/fixtures/self-check-cases.json` in place**, and it publishes file
**content** rather than digests — a corpus of digests would never exercise either side's hash function.
**The gate's first version was vacuous and it was measured, not suspected:** `assertPublishable` compares
digests too and **throws on the first disagreement**, so running it first made `runSelfCheck` unreachable and
the witness stayed green with the self-check replaced by a hardcoded clean verdict. The self-check therefore
runs **first**, 12 §1.2's gate runs only over a tree that already matched, and the witness requires an
**enumerated finding of the right kind** — a throw is a witness *failure*, because it means the wrong leg
decided. Two shell-configuration facts are gated and both were read off the pinned Tauri sources rather than
assumed: a configured **CSP re-serialises every embedded HTML file** at build time, so `index.html` inside
the binary would no longer be the file the release signed (12 §5.1 already delivers the release's own
meta-CSP), and `frontendDist` accepts a **URL**, which is application code arriving over the network. The
Rust half is a separate CI job (`desktop-shell`) because it needs a toolchain and webkit2gtk; `crates/embedded-tree`
is deliberately **pure** so the comparison that decides whether the shell may start is testable on a machine
with no GUI toolkit

## What lives here (moved from AGENTS.md · Repository layout, 2026-08-06)

Kept out of the always-loaded `AGENTS.md` because it only matters under `app/`;
this file loads whenever a session touches it. Status claims here are point-in-time —
**PLAN.md is the single source of implementation status (R-4)**.

### `app/`

**Status:** Track F Phase 0 + F2/F5 done; N10 client kit

The single 10 §10.1 monorepo, moved here from `frontend/` on 2026-08-03 — pnpm workspace plus its own cargo workspace (excluded from the root one). **`packages/protocol` (F5) is the normative TS market-math port**: a BigInt 64.64 reproduction of the runtime's *integer* path — the guarded-Q96 `exp2`/`log2`/`ln`, the log-sum-exp LMSR, the base-unit quote pipeline and the 1e9-grid κ-slew TWAP — because the client's job is to state what the chain will charge, and a more precise answer is the wrong answer when three roundings decide the last base unit. Certified by `app/tests/protocol` against the reference corpus read in place. **F2 landed the 02 §11 artifact feed and its consumers**: `fixtures/chain-feed/{2,3}/` (the primary runtime *and* its paired terminal-recovery runtime, metadata v15 — 10 §5.1 makes the pair a gate, not a nicety), `fixtures/chainhead/` (222 recorded chainHead-v1 transcripts at one pinned block), `packages/papi-descriptors` (PAPI 2.1.8 output for both chains, **committed** per 10 §5.1 and byte-compared by `descriptors:check`), and `packages/mock-runtime` + `tests/mock-runtime`, which replay the transcripts with no node and no network. **F4 added `fixtures/foreign-chain-feed/` (2026-08-04)** — chains this release *reads* but does not build. One directory per foreign chain per `spec_version`, holding PAPI-extracted metadata and a pin record produced by `tools/release/pin-foreign-runtime.py`. It is a **separate feed** because `check-chain-feed.py` compares against the *runtime source*, which does not exist for a chain somebody else builds — the same split 02 §13 rule 8 already makes normative on the contract side, where `INTEGRATION_CONTRACT_VERSION` deliberately does not move for §7.7. Regenerate the whole feed from `app/fixtures/chain-feed/README.md` (and the foreign one from its own README); the descriptor config and its two deliberate choices (no `@polkadot-api/descriptors` dependency injection, no whitelist for the Bleavit set) are documented in `app/.papi/README.md`. `packages/bleavit-client-ts` is N10's proof-backed PAPI facade for off-chain hosted-question clients and rides along unchanged. `src/features/{tx,analysis,handoff}` are three separate TypeScript projects — the 10 §10.2 firewall is a compilation boundary, not a lint. `packages/chain-client` is the sole home of `Finalized<T>` (note `bleavit-client-ts/src/provenance.ts` is the hosted-report SCALE preimage, a different sense of the word, and defines no provenance *type*). **F7 added the render layer and the default surface**: `packages/ui` is the only package that names React (and `react-dom`), so *what can render* is one `package.json` away — and `jsxImportSource` points at it, which is what keeps the compiler-injected JSX import a **workspace** specifier on the handoff path. `src/application` holds the shell (S1, the screen inventory as data, the hash routes) and mounts through `ui.mount()`; `src/features/tx` holds S2 and the confirm-and-sign surface; `src/features/handoff` holds S21/S22. The screens are pure components over `Verified<T>` models — the read layer that feeds them is F7's named remainder. `packages/{contexts,intents,receipts,llm-handoff}` + `skills/` are the D-21 serverless handoff: files, clipboard and OS share only, **no MCP in any form** and no network primitive at all. **F20/F21 completed both sides**: `packages/contexts` gained the exporter (scope checked in *both* directions — supplied-but-unconsented refuses, and consented-but-unsupplied refuses too, because a capsule announcing `positions` and carrying none reads as *this user holds no positions*), the `FE-HANDOFF-*` family moved to `handoff-envelope` as its single home with the `recovery` 10 §13.3 requires, **`app/schemas/`** publishes the three formats generated from the parser's own declarations, and **`app/skills/`** is the producer side — one canonical `SKILL.md`, vendor instruction files derived from it, and an example corpus whose every document is executed against the real parser. Both directories are copied into the release tree before hashing, so the service worker pins them. **F11 added the release train**: `index.html` + `vite.config.ts` (Vite 8.1.4, `base: './'`, content-hash-only filenames, no sourcemaps), `src/application/` (the shell package — F11 owns the *document*, F7 owns what renders inside it) with the service-worker entry, `public/manifest.webmanifest`, and `tools/release/` — the pipeline, its committed `sources/` (the `connect-src` classes, the INV-FE-11 pins, the keyring, the 15 §4.8 diff baseline, the 10 §5.4 literal classification) and its README. `dist/` and `release-out/` are derived and gitignored; **never widen the emitted policy by editing output — edit the source, and the second edit to `incumbent-connect-src.json` is the control**

