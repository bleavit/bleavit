// Compilation unit `analysis` (10 §10.2). Its reference set is its package.json dependencies.
export {
  BlockScanError,
  blockScanner,
  watchedAccounts,
  type BlockScanner,
} from './block-scan.js';
