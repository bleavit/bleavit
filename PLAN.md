# PLAN.md — Implementation Roadmap and Status

**This file is the single source of implementation status.** It tells every session
what is done, in progress, blocked, and next. It **references** the
specification in `docs/architecture/` (rule R-4, AGENTS.md) and never restates it:
if you need protocol content, follow the *Spec* column.

How to work this file (full protocol: AGENTS.md):

- Sessions pick **one** milestone: the 🔨 one, else the first ⬜ whose *Depends* are ✅.
- A milestone is ✅ only with green quality gates and a `spec-reviewer` pass without
  blockers (its *Verify* obligations are part of the milestone).
- Update this file **in the same session as the change**: status, *Current focus*,
  and a *Session log* row. Append to logs; never rewrite their history. Dates absolute
  (YYYY-MM-DD).

Legend: ⬜ pending · 🔨 in progress · ✅ done · ⛔ blocked · 🅿 deferred (post-v1)

---

## Current focus

**Current: Track M reopened by the 2026-07-15 spec audit; the `fix/m3-reference-model-spec-compliance` branch owns the M3 remediation.** The four-reviewer `/spec-audit` over M0–M3 (Audit log 2026-07-15) found 0 blockers / 13 majors / 22 minors: **M1–M3 are 🔨 again** (exact remediation deltas in their row notes); M0 stands with CI follow-ups (its row note). **SQ-14…SQ-28 were resolved the same day as one user-delegated R-1 batch** (Decision log): 00/01/04/05/09/13/14/15 edited, **02 untouched** — the 02 §9 intake-cell erratum plus (from the #46 Codex review) the 02 §9 phase-offset row-split and the `pallet-attestor` 02 §7 storage row all ride the pending B2 amendment with SQ-2, so the contract bumps once. Suggested order: M3 (landed same-day on the fix branch — TWAP direction, decide() truth table, grid-exact welfare, 22-op ledger, 08 arithmetic, single-generator corpus; PR pending), then M2 (guarded-precision exp2/log2 + adversarial corpus), then M1 (kernel-constant/type consolidation). A1 merged to main as #44 while this session ran; these records are reconciled onto post-#44 main.

**Track A (2026-07-14 re-scope, parallel):** A1–A11 now have relocated frame-free functional cores in `crates/<name>-core/` and compiling pallet-shell crates with `lib.rs`, `mock.rs`, `tests.rs`, `benchmarking.rs`, and `weights.rs` scaffolds. The production FRAME macro/storage/call/origin/benchmark implementation required by the Track-A DoD is **not complete**; keep A1–A11 🔨 until each shell becomes a real `#[frame_support::pallet]` with bounded storage, origin-checked extrinsics, try-state hooks, generated benchmarks/weights, and Doc-15 suites. B1a/B2–B6 still depend on Track A being truly FRAME.

**A1 ✅ (2026-07-15).** `pallets/constitution` is a production `#[frame_support::pallet]` over `constitution-core`: frozen 02 §7.3/§12 surface (D-14 raw key test-pinned), five spec-named origin-checked extrinsics (incl. `amend_registry`) with the 06 §3.2 authority matrix, the 87-key 13 §1 registry, bit-scoped phase-flag writers, genesis, try-state, mock, 15 §4.1 suites + 600-step randomized shell-vs-core differential, v2 benchmarks ×6, exact stable2603 pins. Dual review (spec-reviewer ×3 + Codex adversarial): all blockers/majors fixed; SQ-4…SQ-13 resolved by user-delegated R-1 decisions (06/13/15 edited, 02 untouched — no contract bump). **Next: A2** (`pallet-conditional-ledger`) by the same shell-over-core pattern — expect the same `DecodeWithMemTracking` and mock/benchmark wiring; then A3–A11, then B1a.

---

## Milestones

### Track M — Foundations

| ID | Milestone | Spec | Depends | Status | Notes |
|---|---|---|---|---|---|
| M0 | Repo bootstrap: Cargo workspace, `rust-toolchain.toml`, CI skeleton (fmt/clippy/test + docs link lint), **re-verify platform pins** (SDK `polkadot-stable2603`, FE package pins) | 01 §9 | — | ✅ | Pin re-verification recorded in Verification log V-1. **Audit 2026-07-15:** stands, with CI follow-ups owed — benchmarks/try-runtime feature-compile gate, install-from-toolchain-file in CI, `--locked` + `cargo-audit` (now normative, 15 §4.5), I-24 xcm-import lint, real no_std build gate; land with the next CI-touching session (release legs → B8) |
| M1 | `crates/futarchy-primitives` — `no_std` shared SCALE types + kernel `K` constants, `INTEGRATION_CONTRACT_VERSION = 2` (bump applied 2026-07-13 in doc 02, see Decision log) | 02 §2; 13 §2; 01 §5.2 | M0 | 🔨 | Shared primitive/view types, bounded SCALE decoding, metadata derives, kernel/identity/currency constants, and lockfile are in place; byte surface verified exact vs 02 §2/§4 (audit 2026-07-15). **Reopened 🔨 2026-07-15 (Track M audit):** ≈half of 13's K-marked kernel constants are absent (downstream cores hardcode them; `wt.quorum` exists as two copies) — add them and delete every sibling-core re-declaration (01 §5.2 single-home rule); move the frozen `Proposal<AccountId>` (full layout now enumerated in 05 §1.2), `ExecutionRecord`, `MarketKind`, phase-offset fractions (13 §3.1) and LMSR domain/error bounds (13 rule 1; `futarchy-fixed` imports) into the crate; add `#[codec(index)]`/encode-byte regression + 02 §3 view-size assertions; define `RuntimeVersionConstraint` per 09 §1.2(3) |
| M2 | `crates/futarchy-fixed` — 64.64 fixed point, exp2/log2/ln, maker-adverse rounding, error bounds | 04 §4 | M0 | 🔨 | Two-limb arithmetic, maker-adverse rounding, V1–V4/V6 vectors and freshness gates verified correct (audit 2026-07-15). **Reopened 🔨 2026-07-15 (Track M audit):** `exp2` violates the 04 §4 normative bound (10.11 ulp worst, 36.9 % of sampled fracs > 2 ulp; masked by a few-set-bit-only corpus) — rework `exp2`/`log2` at guarded internal precision per amended 04 §4 (SQ-14/15 semantics: exp2 kernel-relative ≤ 2⁻⁶³, log2/ln ≤ 2 ulp absolute, price ≤ 8 ulp); add the per-commit ≥10³ dense-bit adversarial corpus derived from the reference model (04 §5 single-generator rule — retire `tools/fixed` as an independent generator); fix the 48·b truncation sliver (exact u256 compare); gate f64 helpers out of the no_std surface; import domain/error-bound constants from `futarchy-primitives`; CI-gate the no_std build |
| M3 | `reference-model/` — independent Python executable spec (LMSR, TWAP, ledger ops incl. gate/Baseline/VOID, welfare pipeline, decision rule, treasury arithmetic) + MPFR-256 corpus + V1–V6 regeneration in CI | 04 §5; 15 §4.4; 08 (worked arithmetic); 05 §4.4 | M0 | 🔨 | LMSR module, V1–V6 exactness, independence direction and CI freshness verified (audit 2026-07-15). **Remediation landed on this PR (fix branch, on top of the audit/spec-amendment docs PR):** backward-Δblocks + staleness-aware TWAP (04 §7); full 16-variant `RejectReason` + strict `decide()` order (vetoes before welfare grade) + first-insufficiency Extend (05 §5.4/§5.5); grid-exact welfare pipeline as a conforming implementation with full-pipeline vectors (05 §4.4, SQ-18); complete ledger families incl. the mandatory 03 §6.3 vector (s=0.70005 → 14,001/2,999/2,999) + gate/Baseline/scalar; treasury worked numbers with claimant-adverse rounding (prize UP, cost DOWN, `3·P ≤ AC`); single-generator corpus (04 §5) — `tools/fixed` derives from the model, `vectors.json` gained Rust consumers, all values model-computed, replayable V6, honest precision metadata; `check-doc-table.py` CI-wired. Model 21/21, both corpora fresh, doc-table + links green. **Stays 🔨 (not yet ✅):** ✅ is gated on M2's guarded-precision exp2 (the differential oracle is not fully trustworthy until the exp2 bound holds — 04 §4) **and** a fresh `spec-reviewer` pass over the fixed code (R-6); ≥10⁷ sweep + publication remain B8. |

### Track A — Protocol pallets

**Re-scoped 2026-07-14 (user-directed; see Decision log): Track A delivers production
Polkadot-SDK FRAME pallets, not frame-free models. A1–A11 are reopened ⬜.** Their logic
already exists as **frame-free functional-core models** (state struct + `fn op(&mut self)
-> Result` + typed Event/Error + `try_state`); that code is **kept as the functional
core**, relocated to `crates/<name>-core/` (`no_std`, no `frame` deps — rule 9 / 01 §5.2),
and preserved as the differential oracle (Python M3 ≡ Rust core ≡ FRAME runtime) and the
WASM-portable / auditor-consumable port. A milestone is ✅ only once its pallet ships in
full FRAME production style — the **necessary parts** per pallet:

- **`crates/<name>-core/`** — the relocated frame-free model (functional core; today's
  `pallets/<name>` logic, unchanged in behavior).
- **`pallets/<name>/src/lib.rs`** — a `#[frame_support::pallet]` shell: a `Config` trait
  (`RuntimeEvent`, `WeightInfo`, constants via `Get<…>`, `Params`/currency/asset + origin
  bounds); `#[pallet::storage]` items as `BoundedVec`/`BoundedBTreeMap` (`MaxEncodedLen`,
  bounds from 13 §4, names + SCALE shapes matching 02 byte-for-byte — rule 5);
  `#[pallet::event]` / `#[pallet::error]` mapped 1:1 from the core; `#[pallet::call]`
  extrinsics that enforce an explicit origin (`EnsureOrigin` over `pallet-origins`, one
  SafetyFilter authority-matrix row per call — 06 §3) then load storage → call the core →
  persist → deposit events, each carrying `#[pallet::weight(T::WeightInfo::…)]`;
  `#[pallet::hooks]` with the mandatory `try_state` (15 §1) and only the cursor-bounded
  cranks the spec allows (none where it says none — the ledger has no hooks, 03 §10);
  `#[pallet::genesis_config]` / `genesis_build` where the pallet has genesis state.
- **`pallets/<name>/src/mock.rs`** — a mock runtime (`construct_runtime!`: `System` + the
  pallet + its dependencies, with test `Config` impls).
- **`pallets/<name>/src/tests.rs`** — per-extrinsic × error-path × origin-misuse tests
  through the closed wrapper set, limit/boundary coverage, and a `try_state` assertion
  (15 §4.1, §1). Audit-scope-A pallets (A1, A2, A11 — R-7) add the adversarial /
  rounding-against-the-claimant / solvency suites.
- **`pallets/<name>/src/benchmarking.rs`** + **`weights.rs`** — `#[benchmarks]` for every
  extrinsic and hook, a `WeightInfo` trait, a generated `weights.rs`, and the
  `runtime-benchmarks` feature. PoV-calibrated weights land with B5; the harness + stubs
  are part of the milestone.
- **`Cargo.toml`** — `frame-support` / `frame-system` / `frame-benchmarking` on the
  `polkadot-stable2603` pins (01 §9), the `std` / `runtime-benchmarks` / `try-runtime`
  features, and the `<name>-core` dependency.

Scaffold via `/new-pallet` (already emits this shape); `test-engineer` authors the 15 §4
suites; a `spec-reviewer` pass with no blockers precedes ✅ (R-6). Per-pallet FRAME wrapping
lives here; runtime-level `construct_runtime!` / `impl_runtime_apis!` assembly is B1a/B2.

| ID | Milestone | Spec | Depends | Status | Notes |
|---|---|---|---|---|---|
| A1 | `pallet-constitution` — typed/bounded/rate-limited params, meters, capability tables, `PhaseFlags`, `ReleaseChannel` fixed-layout key, kernel re-export | 06; 13 §1/§4; 02 §7.3/§12 | M1 | ✅ | I-6, I-7 (meter primitive; I-17 envelopes re-scoped to treasury/guard per amended 15 §1). **Done 2026-07-15** — production FRAME pallet over `constitution-core`: production `#[frame_support::pallet]` over `constitution-core` — frozen `Params`/`PhaseFlags`/`ReleaseChannel` (D-14 raw key + 168-byte layout test-pinned; runtime MUST instance the pallet as `Constitution`), `GovernanceOrigin: EnsureOrigin<_, Success = ConstitutionOrigin>` + per-call 06 §3.2 predicates, calls `set_param`/`set_capability`(FutarchyMeta)/`set_phase_flag`(Root, 09 §5.4)/`set_release_channel`(ConstitutionalValues, 02 §12), internal APIs `note_release_channel`/`note_phase_flag`/`charge_meter_internal`, genesis (code-owned registry; phase-flags/release-channel configurable), try-state (I-6/I-7 shape + D-14 key), mock, 32-test 15 §4.1 suite + shell-vs-core differential, v2 `#[benchmarks]`×4 + `WeightInfo` (B5 recalibrates). Spec-reviewer 2026-07-15: blocker (META+values origin) + 3 majors (values `set_capability`, un-spec'd `set_phase_flag`/`charge_meter` extrinsics) + `INTEGRATION_CONTRACT_VERSION` naming **fixed in-session**. Codex adversarial review (same day, "needs-attention") corroborated and added; fix round 2 applied: Root **removed** from param/capability/meter predicates (09 §5.4 exhaustive sudo scope), `set_phase_flag` bit-scoped to the armable mask 0–4 with one-bit internal setters for machinery bits 5–7, registry seeded to **32 keys** (every scalar/concrete/≤16-byte/non-[VERIFY] 13 §1 row), `ReleaseChannel` flags word validated (bits 3–31 reserved zero, 02 §12), FRAME crates `=`-exact-pinned, and a 600-step seeded randomized shell-vs-core differential added — which immediately caught and fixed a real core bug (failed `Meter::charge` persisted its lazy window reset; now strict no-op per G-1). **Closed 2026-07-15 (fix round 3, user-delegated decisions)**: SQ-4…SQ-13 resolved (Decision log), `amend_registry` shipped (kernel-bounded rows fully frozen per 13 rule 2; meta-bounds per rule 7), 13 §1 registry materialized at **87 keys** (only formula/TGE/uncalibrated rows absent), meters re-scoped (constitution = generic primitive, empty at genesis; envelopes with treasury/guard). Final spec-reviewer pass: 0 blockers / 1 major / 6 minors — all fixed in-session (full-tuple kernel freeze, capability-row swap to `(Code, AuthorizeUpgrade)`, uniform [VERIFY]-seeding criterion incl. `pol.b_baseline`/`collator.n_tgt`, uniform "—"→0 cooldown, comment sweep, 06 glosses). Follow-ups owned elsewhere: PoV-calibrated weights (B5), I-6 SMT/property artifact (Track S/S1), guard-side `code.spacing` metering (A11), full-runtime internal-writer negative suites (B1a/S5), reviewer spec-notes on 05 §1.3 gloss + rule-7 kernel-set upkeep (next 05/13 touch) |
| A2 | `pallet-conditional-ledger` — vaults, per-branch supplies, split/merge/scalar/gate/Baseline families, `Voided` + `redeem_void`, internal wrapper API | 03 (all); 02 §6 | M1 | 🔨 | Audit scope A (R-7). **Core ✅** (no_std ledger state machine, bounded position accounting, deposit/cap, terminal redemptions incl. `Voided`/`redeem_void`, atomic ops, internal authority API, pair-exact try-state). **FRAME pallet 🔨**: core relocated and pallet shell scaffold compiles; production `#[frame_support::pallet]` storage/calls/origins plus adversarial/solvency suites still pending per Track-A DoD |
| A3 | `pallet-market` — LMSR books (branch-USDC, D-3), trade wrapper, fees, TWAP, POL seeding, `BaselineMarketOf` | 04; 02 §5–§7.4 | A2, M2, M3 | 🔨 | I-12, I-13. **Core ✅** (branch/gate/Baseline LMSR books, atomic trade wrappers, 30 bps fees, TWAP accumulator, POL seeding, Baseline mapping, try-state). **FRAME pallet 🔨**: core relocated and pallet shell scaffold compiles; production `#[frame_support::pallet]` storage/calls/origins/tests/benchmarks still pending per Track-A DoD |
| A4 | `pallet-origins` + `SafetyFilter` — 8 custom origins, closed wrapper set, call-domain derivation | 06 §3; 01 §6 | M1 | 🔨 | I-8, I-10, I-11. **Core ✅** (8 custom origins, closed wrapper set + defensive recursion, call-domain derivation, negative tests). **FRAME pallet 🔨**: core relocated and pallet shell scaffold compiles; real `#[pallet::origin]` + `EnsureOrigin` set, `BaseCallFilter`, tests/benchmarks still pending per Track-A DoD |
| A5 | `pallet-oracle` — reporter/watchtower registries, bonded reporting game, challenge rounds (72 h + quorum), reserve probe `R`, neutral settlement | 07 §1–§6, §8–§13 | A1 | 🔨 | I-18, I-24. **Core ✅** (value-scaled bonded reporting game, watchtower quorum/extension, neutral settlement, per-version rounds, reserve fail-static probe, try-state). **FRAME pallet 🔨**: core relocated and pallet shell scaffold compiles; production `#[frame_support::pallet]` storage/calls/origins/tests/benchmarks still pending per Track-A DoD |
| A6 | `pallet-registry` — Incident/Milestone instances, bonded filings, challenge windows | 07 §7 | A5 | 🔨 | **Core ✅** (Incident/Milestone instances, bounded bonded filings, quorum challenge windows, slashing, terminal close-out, aggregates, try-state). **FRAME pallet 🔨**: core relocated and pallet shell scaffold compiles; production `#[frame_support::pallet]` storage/calls/origins/tests/benchmarks still pending per Track-A DoD |
| A7 | `pallet-welfare` — bounded counters, snapshots, `MetricSpec` registry, pillar pipeline (`C_onchain`/`C_attested`), gate-breach flags, settlement score | 05 §4, §6–§7 | A1, A5 | 🔨 | I-16. **Core ✅** (`MetricSpec` registry, bounded snapshots, `C_onchain`/`C_attested` pipeline, gate-breach flags, settlement score, try-state). **FRAME pallet 🔨**: core relocated and pallet shell scaffold compiles; production `#[frame_support::pallet]` storage/calls/origins/tests/benchmarks still pending per Track-A DoD |
| A8 | `pallet-epoch` — epoch/phase clock, proposal registry + T1–T24 machine, cohorts, `decide()` engine (11 steps incl. `SecuritySizing` D-4, ratification D-5), `RecentCohortSummaries` ring | 05 §1–§3, §5; 02 §7.1 | A2, A3, A7 | 🔨 | I-14, I-15, I-21. **Core ✅** (epoch/phase clock, T1–T24 machine, cohorts, `decide()` engine, `RecentCohortSummaries` ring, try-state); model check remains S1. **FRAME pallet 🔨**: core relocated and pallet shell scaffold compiles; production `#[frame_support::pallet]` storage/calls/origins/tests/benchmarks still pending per Track-A DoD |
| A9 | `pallet-futarchy-treasury` — sub-accounts, NAV + haircuts, outflow meters/streams, budget lines, `issue_vit`, coretime renewal call | 08 §1, §6–§9 | A1 | 🔨 | I-7. **Core ✅** (sub-accounts, NAV + reserve haircuts, outflow meters/streams, budget lines, `issue_vit`, `recover_foreign`, coretime renewal). **FRAME pallet 🔨**: core relocated and pallet shell scaffold compiles; production `#[frame_support::pallet]` storage/calls/origins/tests/benchmarks still pending per Track-A DoD |
| A10 | `pallet-guardian` + `pallet-attestor` — 7-seat council, powers, playbooks incl. PB-LEDGER-FREEZE, retro ratification; bonded 2-of-N attestor registry | 06 §5–§7 | A1, A4 | 🔨 | I-19, I-23. **Core ✅** (7-seat council, powers, playbooks incl. PB-LEDGER-FREEZE, retro ratification, bonded 2-of-N attestor registry). **FRAME pallet(s) 🔨**: cores relocated and pallet shell scaffolds compile; production `#[frame_support::pallet]` storage/calls/origins/tests/benchmarks still pending per Track-A DoD |
| A11 | `pallet-execution-guard` — queue, permissionless `execute()` (13-item dispatch list), class origins, two-phase upgrade flow, `DescriptorLeadTime`, `ExecutionRecords` ring | 09 §1–§3; 02 §7 | A8, A10 | 🔨 | I-9, I-10, I-11, I-19. **Core ✅** (queue, permissionless `execute()` 13-item dispatch, class origins, two-phase upgrade flow, preimage binding, `DescriptorLeadTime`, `ExecutionRecords` ring). **FRAME pallet 🔨**: core relocated and pallet shell scaffold compiles; production `#[frame_support::pallet]` storage/calls/origins/tests/benchmarks still pending per Track-A DoD; FE `execute`-row lockstep is an F17/B2 surface concern |

### Track B — Runtime, node and chain

Doc-09 WBS delta rows map here and to Track A: E15→B2/B8 · E16→A2 · E17→A3 · E18→A6 ·
E19→A5 · E20→A10/B6 · E21→B4 · E22→A11/B1. (E1–E14 definitions live in git history —
superseded `BACKEND_PLAN.md` §26; their scope is covered by Tracks M/A/B.)

| ID | Milestone | Spec | Depends | Status | Notes |
|---|---|---|---|---|---|
| B1 | Runtime assembly — standard-pallet config (incl. genesis `frame-system` filter D-13, USDC as ForeignAsset, fees in VIT/USDC), `SafetyFilter` as `BaseCallFilter`, origins wiring | 01 §5–§6; 06 §3; 09 §5 | A1–A11 | ✅ | Frame-free composition **model** (pallet list, SafetyFilter `BaseCallFilter` adapter, D-13 genesis filtering, USDC/fee/origin wiring, filter-exhaustiveness tests) — the real `construct_runtime!` assembly is B1a. Left ✅ as the delivered model scope even though its A1–A11 deps are reopened |
| B1a | Runtime-level FRAME assembly — turn the B1 composition model into a real runtime: `construct_runtime!` wiring the now-FRAME Track-A pallets + the standard/system/Cumulus pallets, `frame_executive`, the signed-extension / `CheckedExtrinsic` stack, `BaseCallFilter = SafetyFilter`, the `polkadot-stable2603` SDK/Cumulus pins (01 §9), and runtime genesis. Per-pallet FRAME wrapping now lives in Track-A's DoD; this is only the runtime-level composition. | 01 §1/§5/§9; 06 §3; 09 §5 | A1–A11, B1 | ⬜ | No `construct_runtime!`/`impl_runtime_apis!` exists yet anywhere in `runtime/`; a runtime binary can't exist until Track A ships as FRAME pallets. Gates B2–B6. Standard/custom composition + D-13 genesis filter already modelled in B1 |
| B2 | `FutarchyApi` runtime API — all 11 methods + view types | 02 §3–§4 | B1a | ⬜ | Part of contract; append-only after freeze; `impl_runtime_apis!` requires the B1a shell |
| B3 | Node, chain specs (ss58 7777, WSS bootnodes listed), genesis config incl. VIT allocation/vesting | 02 §8/§10; 08 §2; 12 §6.2 | B1a | ⬜ | ss58 registry submission before Phase 2 |
| B4 | XCM — USDC/DOT reserve transfers, Asset Hub channel, reserve-probe plumbing, coretime renewal (freeze-exempt) | 09 §6; 07 §8; 01 §4 | B1a | ⬜ | No `Transact` governance either direction |
| B5 | Benchmarks, weights, PoV budgets (196-market table), weight-regression CI | 15 §4.5; 13 §4–§5 | B1a | ⬜ | I-20 |
| B6 | Upgrade path e2e — `authorize_upgrade`/`apply_authorized_upgrade`, attestation precondition, `DescriptorLeadTime`, `ReleaseChannel` write wiring, `pallet-migrations`, try-runtime CI | 09 §2; 02 §12 | B1a, A11 | ⬜ | Negative tests: early apply, unattested CODE |
| B7 | Zombienet + Chopsticks environment definitions (release artifacts, not private fixtures) | 15 §4.7; 02 §11 | B3 | ⬜ | Incl. dead-man and PB drills |
| B8 | Release-artifact publication pipeline — wasm+metadata+hashes, chain-specs, envs, chainHead fixtures, vector corpus (backend row E15) | 02 §11; 15 §5 | B7, M3 | ⬜ | Release-gating both directions (FE-R1). **Added 2026-07-15 (SQ-16/SQ-27):** owns the release-side supply-chain gates (15 §4.5 `--locked`+`cargo-audit`) and the ≥10⁷-point MPFR-256 corpus sweep + content-addressed publication per the 04 §4/§5 cadence |
| B9 | Keeper reference implementation — all cranks (phase ticks, TWAP obs, decide, execute, settle, cleanup), idempotent, rebated | 01 §4.2; 08 §6; 12 §6 | B3 | ⬜ | RB-KEEPER runbook pairs with O4 |

### Track S — Systemic verification and simulation

| ID | Milestone | Spec | Depends | Status | Notes |
|---|---|---|---|---|---|
| S1 | TLA⁺/Quint models — ledger resolution + proposal machine over all interleavings (I-3, I-14, I-15, I-18, I-26/I-27) | 15 §4.1; 03 §11 | A2, A8 | ⬜ | |
| S2 | cargo-fuzz targets — SCALE payload decode, nested-wrapper filtering, LMSR trade paths | 15 §4.5 | B1 | ⬜ | |
| S3 | Generated limit-coverage suite — one dispatch-past-limit test per 13-registry key; unmatched keys fail CI | 15 §4.6 | A1, B1 | ⬜ | I-22 CI half |
| S4 | Agent-based economic simulation — δ per class (false-pass < 1 %), `AttackCost̂` validation, POL sizing; publish sim-gated params | 15 §4.9; 08 §5; 13 (sim-gated) | M3, A3, A8 | ⬜ | Phase-0 exit evidence |
| S5 | Wrapper/filter negative suites + filter-exhaustiveness test over `RuntimeCall` | 06 §3; 15 §1 (I-8/I-10/I-11) | B1 | ⬜ | Incl. `proxy_announced`, `as_multi_threshold_1` |

### Track F — Frontend (canonical decentralized client)

Epic IDs from 11 §11.13. FE-P1…P10 prototype gates from 10 §12 (+FE-P10, 11 §11.13).

| ID | Milestone | Spec | Depends | Status | Notes |
|---|---|---|---|---|---|
| F0 | Monorepo scaffold — `frontend/` per package firewall, pinned stack (PAPI 2.x, smoldot 3.x, Vite 8, Dexie 4), dependency-cruiser CI | 10 §10; 01 §9 | M0 | ⬜ | |
| F1 | Prototype gates FE-P1…FE-P10 — resolve each, record outcomes | 10 §12; 11 §11.13 | F0 (P2/P4/P5/P10 also B8) | ⬜ | FE-P2 + FE-P7 are launch-critical |
| F2 | FE-R1 — consume backend artifact feed: descriptor-drift CI, fixture suites | 11 §11.13; 02 §11 | F0, B8 | ⬜ | Release-gating |
| F3 | FE-1 `packages/chain` — smoldot worker, dual chains, finalized-only reads, provenance types | 10 §2–§4 | F2 | ⬜ | INV-FE-1 |
| F4 | FE-2 `packages/descriptors` — pipeline incl. Asset Hub set, compat gating | 10 §5 | F3 | ⬜ | |
| F5 | FE-3 `packages/protocol` — TS LMSR/TWAP vs vector corpus (V1 = 512.494795136) | 04 §5; 15 §4.8 | F0, M3 | ⬜ | |
| F6 | FE-4 `packages/wallet` — signers, tx machine, structural `refreshAndGate`, fee selector | 11 §11.3–§11.4 | F3, F4, F5 | ⬜ | INV-FE-2/5/14 |
| F7 | FE-5 current-state screens S1–S13 incl. VOID redemption UX (E15) | 11 §11.2, §11.5–§11.6 | F6 | ⬜ | Precondition tables P-1…P-15 |
| F8 | FE-6 `packages/local-index` — three-layer history, gap-tolerant coverage, candles | 10 §6–§7 | F3 | ⬜ | INV-FE-7 |
| F9 | FE-7 `packages/providers` + `tools/snapshot` — optional acceleration, sampling, auto-disable | 10 §8 | F8 | ⬜ | INV-FE-3/15 |
| F10 | FE-8 `packages/verify` — release self-check, verification panel | 10 §5.4; 12 §1 | F3 | ⬜ | INV-FE-8/11 |
| F11 | FE-9 distribution — Vite build, Arweave two-pass deploy, manifest, SW (fail-closed), CSP allowlist, SRI | 12 §1, §5 | F0 | ⬜ | |
| F12 | FE-10 degradation UX — rows E1–E23 scripted, sudo-era banner | 11 §11.10–§11.12 | F7 | ⬜ | |
| F13 | FE-11 reproducible build — two-env byte-identical, attestations, `verify-release` CLI, key ceremony | 12 §1.3, §2; 15 §4.8 | F11 | ⬜ | INV-FE-10 |
| F14 | FE-12 performance hardening — budget table in CI (Lighthouse/Playwright, reference hardware) | 10 §9 | F7, F8 | ⬜ | DB-2 |
| F15 | FE-13 ops handbook — bootnode/ArNS/launch operations docs | 12 §6 | F11 | ⬜ | |
| F16 | FE-14 governance surface — referenda, vote/delegate/unlock, ratification, `OracleResolution` ballot (S14–S17) | 11 §11.7 | F6 | ⬜ | D-11 |
| F17 | FE-15 operator surface — reporter/guardian/treasury/upgrade-crank tiers (S18–S19, "Advanced") | 11 §11.8 | F6, F11 | ⬜ | FE-P10 gates upgrade-crank tier |
| F18 | FE-16 funding flow — Asset Hub leg, deposit/withdraw e2e (S20) | 11 §11.9 | F6, F4 | ⬜ | D-12 |

### Track O — Release and operations

| ID | Milestone | Spec | Depends | Status | Notes |
|---|---|---|---|---|---|
| O1 | Release train tooling — `tools/release/build.sh`, `repoint.sh`, `tools/verify-release` (compare/diff-scope/signers audit), `SIGNERS.md`, SBOM | 12 §1–§2 | F11, B8 | ⬜ | CI holds no keys |
| O2 | Key + ANT ceremonies — minisign registry, ANT n-of-m quorum (FROST fallback), signer disjointness CI | 12 §2, §4 | O1, F1 (FE-P7) | ⬜ | Launch blocks without n-of-m |
| O3 | Bootnode program — ≥8 browser-reachable WSS / ≥4 operators / ≥2 :443, funded line, browser-dial probes | 12 §6.2; 01 §4.2 | B3 | ⬜ | D-6/X-4; phase-gated |
| O4 | Runbooks-as-code — `deploy/runbooks/` RB-KEEPER…RB-RELEASE | 12 §6.4 | B3 | ⬜ | |
| O5 | Monitoring/alerting — Prometheus + on-chain-event alerting, out-of-band attestation monitor | 12 §5.2, §6.3 | B3 | ⬜ | FE ships no telemetry |

### Track G — Rollout phase gates (evidence + META decision + values ratification each)

| ID | Milestone | Spec | Depends | Status | Notes |
|---|---|---|---|---|---|
| G0 | Phase 0 exit — reference model ≡ pallets on shared vectors; sim false-pass < 1 %; δ/POL calibration published | 09 §7.1 | Tracks M/A complete, S4, B8 | ⬜ | |
| G1 | Phase 1 exit — Zombienet 3 unattended epochs; collator/keeper-loss, dead-man, coretime-under-dead-man, PB-MIGRATION drills | 09 §7.1 | G0, B7, B9 | ⬜ | |
| G2 | Phase 2 (Paseo) exit — ≥6 epochs, zero invariant breaches, ≥1 full upgrade e2e, contract freeze co-signed, ss58 accepted, testnet bootnodes, staging ArNS, release-train drills | 09 §7.1; 12 §6.5 | G1, O3, F-launch set | ⬜ | |
| G3 | Phase 3 (mainnet shadow) exit — audits A+B, genesis ceremony, prod bootnodes + 30-day commitments, ≥3 reporters staked, HRMP open, exposure caps, F̂ ≥ L/2, attestor registry live, ANT ceremony done | 09 §7.1; 12 §6.5 | G2, O2 | ⬜ | Sudo still present, filtered |
| G4 | Phase 4 — binding PARAM; **sudo removed**; ≥12 binding decisions; NAV ≥ floor (loud gate) | 09 §7.1–§7.2; 08 §4 | G3 | ⬜ | |
| G5 | Phase 5 — +TREASURY; funding ≥ 25M USDC; V_min met; streams > 1 % NAV mandatory | 09 §7.1; 08 §4 | G4 | ⬜ | |
| G6 | Phase 6 — +CODE/META; scope-A re-audit; 1 CODE upgrade stable ≥ 60 d via full D-14 path; dispute game exercised | 09 §7.1 | G5 | ⬜ | |
| G7 | Phase 7 — mature; guardian → playbooks only; sunset vote scheduled | 09 §7.1 | G6 | ⬜ | |

Deferred (🅿, post-v1, do not implement): forecast trading / reopened books (N-8, D-8 — 04 §13);
order-book layer (04); Mode A binding; combinatorial futarchy (01 §2.4).

---

## Spec questions

Open ambiguities/contradictions found in `docs/architecture/`. Record them here first;
under rule R-1 a genuine defect may be corrected in the spec directly, but log
non-obvious semantic changes and confirm with the user before diverging.
Format: `| ID | Question | Spec ref | Raised | Status |`

| ID | Question | Spec ref | Raised | Status |
|---|---|---|---|---|
| SQ-30 | 05 §4.6 requires p5/p95 winsorization over the 12-point trailing sample but names no percentile-estimator family; conventional estimators produce different bounds, conflicting with 15 §4.4's bit-identity mandate. Which estimator is normative? The model currently uses inclusive `(n−1)·p` linear interpolation behind a `SPEC-NOTE`; the convention must be pinned before `pallet-welfare` implements normalization. | 05 §4.6; 15 §4.4 | 2026-07-15 (M3 fix pass) | **resolved 2026-07-15** (same batch): inclusive linear interpolation ("type-7": rank `1 + f·(n−1)` on the ascending sample, interpolated between bracketing order statistics) is normative — 05 §4.6 now states it, evaluated on-chain on the FixedU64 1e9 grid with the interpolation product rounded down per §4.4; never vacuous on the 12-element sample (nearest-rank would degenerate to min/max, explicitly not meant). Matches the model's implemented estimator — its SPEC-NOTE can drop at next touch |
| SQ-29 | 08 §5.4's normative PARAM worked arithmetic displays `L̂ = 2 × 10,000 × ln 2 + 100,000 = 127,726`, thereby using an implied decision-pair depth of 27,726 = `4·b·ln 2` and freezing `P_ref(PARAM) = 63,863` from it. But §5.2 defines seeded decision-pair depth as `2·b·ln 2`, which is 13,863 at `b = 10,000`, and every other class row follows that formula. Is PARAM's doubled depth intentional, or should the worked row and frozen `P_ref` be recomputed? | 08 §5.2, §5.4 | 2026-07-15 (M3 fix pass) (numbered after the concurrent Track M audit batch SQ-14…SQ-28, which — with its spec amendments — remains uncommitted in the working tree for the orchestrating session to commit) | **resolved 2026-07-15** (same user delegation, follow-through of the SQ-14…28 batch): PARAM's doubled depth was a slip — 13 §1 `pol.b` floors (10k/25k/60k/100k) + §5.2's pair formula `2·b·ln 2` reconcile every other 08 site (§3 commitments incl. the 4×`b_gate` gate add-ons, §4.1 NAV floors, the other three P_ref rows). 08 §5.4 corrected: (c) = 113,863 / 170,794 / **56,931**; P_ref(PARAM) = **56,931** with a derivation note; the model's preserve-the-typo branch and its pinned test updated in fix-branch commit 41531dd (21/21 green) |
| SQ-28 | Patch-pin policy: 01 §9 pins the release *line* `polkadot-stable2603` (maintenance tags stable2603-1…4 exist) while the workspace freezes `=`-exact patch versions — is exact-patch freezing intended, and what is the sanctioned procedure for adopting stable2603-N security backports? | 01 §9; 14 TH-34 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): exact-patch `=` freezing confirmed intended; stable2603-N adoption procedure written into 01 §9 "Pinning regime" (atomic pin-bump commit citing the upstream tag, full gates, Verification-log entry); release-line moves are 00-recorded decisions |
| SQ-27 | 14 TH-34 cites 09/12 for "dependency pinning + cargo-audit CI", but neither doc contains any cargo-audit/dependency-audit requirement — where does the obligation normatively live, and which PLAN milestone owns it? | 14 §3 TH-34; 09; 12 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): normative home created — 15 §4.5 "Supply-chain gates" (`--locked` + `cargo-audit`, release-blocking) + 01 §9 pinning regime; 14 TH-34 citation corrected (09/12 → 01 §9 / 15 §4.5); CI wiring → next CI-touching session, release side → B8 |
| SQ-26 | 06 §7 mandates `pallet-attestor` ("a thin registry, storage listed in 02") and the workspace ships it, but 01 §5.1's custom-pallet table has no attestor row — should 01 §5.1 gain the row, or is the registry meant to live inside an existing pallet? | 01 §5.1; 06 §7 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): 01 §5.1 gained the `pallet-attestor` row (06 §7 registry: ≥3 members, 25k-VIT bonds, attest/challenge, 2-of-N quorum); `pallet-execution-guard` depends gains `attestor` |
| SQ-25 | Where is the boundary of 13 rule 1's "every K constant compile-time in `futarchy-primitives`"? LMSR error bounds live in `futarchy-fixed`, 02 §7 puts storage-bound arguments with owning pallets, and does 02 §2's "all types live in futarchy-primitives" cover §5–§7 event field types such as `MarketKind` (currently defined nowhere)? | 13 rule 1/§2; 02 §2/§5/§7 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): boundary written into 13 rule 1 + 01 §5.2 — every K numeric incl. LMSR domain/error bounds single-homed in `futarchy-primitives::kernel` (`futarchy-fixed` imports, never re-declares); carve-outs: §4 storage-bound arguments pallet-owned, rowless internal constants; all 02-named types incl. `MarketKind` live in primitives |
| SQ-24 | Home of the phase-offset fractions: 13 §3.1 stores them as "rational fractions of `epoch.length`" (Part-3 frozen) while 02 §9 groups them under "params() + metadata constants" — kernel constants in primitives, epoch metadata constants, or constitution storage? (Code today: local `PHASE_DENOM = 21` in epoch-core.) | 13 §3.1; 02 §9 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): phase-offset fractions are K kernel constants in `futarchy-primitives` exposed as pallet metadata constants, not `Params` storage (13 §3.1 now explicit); epoch-core's local `PHASE_DENOM` moves in M1 remediation |
| SQ-23 | Intake rate-limit representation conflict: 02 §9 lists "≤ 4 entries/epoch/account" under metadata constants (K-style) but 13 §1 defines `intake.max_acct` as a META-amendable Params key (min 2, max 8) — which representation does the frontend bind to? | 02 §9; 13 §1 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): 13 §1 is normative — `intake.max_per_account` is a tunable (launch default 4, META within [2,8]); FE binds the live value via `params()` and the K bounds via metadata constants; 02 §9's "metadata constants" cell is an erratum queued into the pending B2 02-amendment batch with SQ-2 (one contract bump) |
| SQ-22 | Numeric values of the entrenched θS⁻/θC⁻ floors: 13's rows say "lo floor K" with no number distinct from the defaults (0.90/0.85), so floor ≡ genesis default (tighten-only from day one) — intended? | 13 §1 (`welfare.thS_lo`/`thC_lo`); 13 §2 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): floors = launch defaults by design: θS⁻/θC⁻ K-entrenched at 0.90/0.85, tighten-only from genesis (13 §2 row now states the numbers) |
| SQ-21 | `Proposal` base fields are circular: 02 §2 defers the struct to 05/09 while 05 §1.2's code block says "fields as in doc 02" — the base field list is enumerated nowhere; is epoch-core's generic `Proposal<AccountId>` an acceptable rendering of a layout 02 claims frozen "by inclusion in futarchy-primitives"? | 02 §2 ¶114; 05 §1.2 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): full 20-field base list enumerated and frozen in 05 §1.2 (declaration order = SCALE layout; generic over `AccountId`, concrete AccountId32 per 02 §8; `resources: BoundedVec<[u8;8], 8>` per 13 §4); relocation into `futarchy-primitives` is M1-remediation code work |
| SQ-20 | `RuntimeVersionConstraint` SCALE shape is defined in no document (02 §4 uses it inside frozen `QueuedExecutionView`; 09 §1.2(3) gives only semantics); code invented `spec_name: BoundedVec<u8, 32>` — which doc owns the definition and what is the normative bound? | 02 §4; 09 §1.2(3) | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): layout defined in 09 §1.2(3): `{ spec_name: BoundedVec<u8, 32> (UTF-8 = RuntimeVersion.spec_name), spec_version: u32 }` — matches shipped code |
| SQ-19 | 15 §5(5) says the reference-model vector corpus "exact schema and location constants are frozen in 02", but 02 §11's artifact table has no corpus row — should 02 §11 gain the row/schema (with a contract-version bump), or is 15's claim stale? (Repo schema string `bleavit.reference-model.v2` is self-invented.) | 02 §11; 15 §5(5) | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): corpus schema/location owned by 04 §5 (in-repo `reference-model/fixtures/vectors.json`; content-addressed release publication; `bleavit.reference-model.vN` append-only versioning; rows must be standalone-replayable); 15 §5 split — artifacts (1)–(4) → 02 §11, (5) → 04 §5; 00 X-15 row updated; 02 untouched, no bump |
| SQ-18 | Is the Python reference model a 05 §4.4 "conforming implementation" (bit-identical per-step 64.64/1e9 rounding, grid-exact conformance vectors) or an infinite-precision oracle with stated tolerance (A7's current position)? Decides the fix shape for the missing welfare pipeline. | 05 §4.4(1–5); 15 §4.4 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): the reference model IS one of 05 §4.4's two conforming implementations for the welfare pipeline — grid-exact per-step 64.64/1e9 rounding, bit-identical W/s (15 §4.4); higher-precision-oracle role only for transcendental kernels; A7's tolerance fallback is thereby non-conforming — welfare-model rework lands with M3 remediation |
| SQ-17 | 04 §7's TWAP accumulator "A += o_t·Δblocks" never defines Δblocks: market-core weights the newly recorded (capped) observation backward over the elapsed gap; the reference model weights forward — confirm backward is normative so the model is fixed, not the chain. | 04 §7 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): backward weighting confirmed normative — Δblocks = blocks since the previous recorded observation, the new observation covers the interval ending at its record block (04 §7 now explicit); chain/market-core correct, Python model fixed in M3 remediation |
| SQ-16 | Which corpus scale gates PR-time CI vs release? 04 §5 requires V1–V6 regeneration every commit; 15 §4.4's ≥10⁷-point futarchy-fixed differential has no stage and no owning PLAN row (B8 owns publication only, 04 §5(1)/15 §5(5)); nothing clearly licenses deferring 04 §4 bound *verification* to the release pipeline. | 04 §4/§5; 15 §4.4/§5(5) | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): two-tier cadence made normative in 04 §4 + 15 §4.4 — per-commit ≥10³-point adversarial corpus (dense-bit fracs + domain edges, from the single 04 §5 generator); full ≥10⁷ MPFR-256 sweep release-gated (B8) plus mandatory on any kernel/reference-numerics change; per-commit leg → M2 remediation |
| SQ-15 | Is 04 §4 bullet 3's method ("range reduction to [1,2) + 64-bit polynomial iteration") normative, or only the error bounds? 64-bit internal precision appears mathematically insufficient for a ≤2-ulp worst case under any method — should the spec state a required internal/guard precision? | 04 §4 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): bounds normative, method free — 04 §4 now mandates guarded internal precision (worst-case error analysis part of crate conformance; ≥32 guard bits reference config, single final rounding); 64-bit-wide intermediates declared insufficient |
| SQ-14 | 04 §4 ulp semantics: is "each `exp2`/`log2` call ≤ 2 ulp of 64.64 (≈1.1e-19 relative)" 2 raw 2⁻⁶⁴ units absolute or ~2⁻⁶³ relative? The readings coincide only in [1,2); exp2 with integer part ≥ 1 cannot meet raw-absolute after the left shift, and log2 measures 2.64 raw ulp on wide inputs while passing relative. Also: no explicit error bound is stated for the price function (crate test infers 8 raw ulp). | 04 §4; 13 §2 | 2026-07-15 (Track M audit) | **resolved 2026-07-15** (user-delegated batch; see Decision log): ulp = 2⁻⁶⁴ fixed; exp2 bound is kernel-absolute ≤ 2 ulp ⇔ full-range relative ≤ 2⁻⁶³ (absolute full-range unattainable and not meant); log2/ln ≤ 2 ulp absolute full-domain; marginal price ≤ 8 ulp absolute added (04 §4); current exp2 (10.11 ulp) and wide-input log2 (2.64 ulp) remain non-compliant — M2 fix stands |
| SQ-13 | Who writes TREASURY-class 13 §1 keys (`pol.*`, `ops.*`)? 06 §3.2 has no `constitution.set_param` row under `FutarchyTreasury`; the implemented `(FutarchyTreasury, Treasury)` arm is a plausible but unconfirmed reading. | 06 §3.2; 13 §1 | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (user-delegated batch; see Decision log): `(FutarchyTreasury, Treasury)` confirmed — 06 §3.2 row 2 now names `constitution.set_param` (TREASURY-class keys) |
| SQ-12 | Meter identity and envelope derivation for I-7/I-17: are constitution meters keyed or indexed, do limits derive from live `Params` records (`iss.inflation_cap`, `trs.cap_30d/180d`, `code.spacing` — some need NAV, which the constitution cannot compute), and is charging an internal sibling-pallet API only (current implementation) or ever an extrinsic? 15 §1 assigns I-7/I-17 to the constitution while A9's treasury core also models outflow meters. | 15 §1 (I-7/I-17); 13 §1; 08 | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (batch): 15 §1 I-7/I-17 re-scoped — limits as constitution `Params` records, metering with the owning flows (treasury issuance/outflow, guard `code.spacing`); constitution keeps the generic bounded-meter primitive, empty at genesis |
| SQ-11 | May bootstrap Root/sudo write `Params`/`Capabilities` at all? 09 §5.4's sudo list ("incident response, arming phase flags, Phase-3→4 upgrade") excludes parameter administration, yet 13 rule 4's Phase 0–3 sim-gated calibration needs a writer; and should pallet predicates hard-code Root given 09 §7.2's Phase-3→4 "no origin maps to Root" closure? | 09 §5.4/§7.2; 13 rule 4 | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (batch): narrow reading adopted — Root refused for params/capabilities/meters; sudo keeps only bit-scoped phase-flag arming; Phase 0–3 calibration lands via runtime upgrades |
| SQ-10 | How are per-class 13 §1 keys (`dec.delta`, `dec.sigma`, `dec.v_min`, `prop.bond`, `pol.b`, `exec.timelock`, `trs.proposer_reward`) keyed in `Params`, and how does `ParamView.class: ProposalClass` (02 §4) represent `ParamClass::{Const, Entrenched, MetaAndValues}`? | 13 §1; 02 §4 | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (batch): 13 rule 6 per-class suffix keys (`.param/.trs/.code/.meta`, bases `exec.lock`/`trs.reward`) + rule 7 view projection (CONST/entrenched→Constitutional, META+values→Meta); 28 per-class rows seeded |
| SQ-9 | 13 §4 lacks rows for the constitution's own storage bounds (params 64 / capabilities 64 / meters 16 — currently core constants), though 13 claims to own every storage bound. Add the rows via R-1? | 13 §4 boundary | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (batch): 13 §4 gained the constitution bound rows — Params 128, Capabilities 64, Meters 16 |
| SQ-8 | What is the canonical `ParamKey` encoding for dotted 13 §1 names longer than 16 bytes? `key16` silently truncates (e.g. `intake.max_per_account` → `intake.max_per_a`); no collisions today (test-pinned), but the FE must replicate the exact rule to call `params()`. | 02 §2 (`ParamKey`); 13 §1 | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (batch): 13 rule 6 canonical encoding — UTF-8 ≤16 zero-padded, truncation forbidden (`key16` rejects), explicit `key:` identifiers annotated per row (e.g. `intake.max_acct`, `keeper.budget`) |
| SQ-7 | What call implements "entrenched-floor tighten" and 13 rule 2's META bounds-amendment — is `constitution.amend_registry` the vehicle for both, and does the "registry" include the capability table? (Neither `amend_registry` nor any bounds-amendment path exists in code yet.) | 06 §2.1/§2.4; 13 rule 2 | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (batch): `amend_registry` implemented — bounds/Δ/cooldown only, never value/class/key-set; kernel-bounded rows genesis-fixed; meta-bounds per 13 rule 7; origins FutarchyMeta | ConstitutionalValues (06 §2.1 + rule 2 harmonized); capability table stays under `set_capability` |
| SQ-6 | META+values keys are now enacted by `FutarchyMeta` (06 §1 bars values from parameter keys; values consent = execute-time ratification, 06 §2.2) — but 06 §2.2(b)'s machine-derived rule-altering list does not include `constitution.set_param`, so a META+values `set_param` payload would not be auto-flagged for ratification. Should the list gain it (else the dual consent has no enforcement point)? | 06 §1, §2.2(b); 13 §1 (META+values rows) | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (batch): 06 §2.2(b) rule-altering list gained `constitution.set_param` on META+values-class keys — the dual consent now has its machine-derived enforcement point |
| SQ-5 | Who writes each `PhaseFlags` bit, via which call/path and origin? 09 §5.4 gives bootstrap sudo "arming phase flags"; is a per-bit writer map wanted, and is an extrinsic intended at all post-Phase-4? (Implemented meanwhile, fix round 2: `set_phase_flag` is Root-only **and bit-scoped to the armable mask 0–4**; machinery bits 5–7 have dedicated one-bit internal setters `note_ledger_frozen`/`note_dead_man_engaged`/`note_reserve_health`, so no caller — sudo included — can fake or clear another pallet's signal.) | 02 §7.3; 09 §5.2/§5.4 | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (batch): writer map added to 06 §3.2 — bits 0–4 sudo (Root-only, mask-scoped call) then phase-advancement upgrades; bits 5–7 machinery-only internal setters |
| SQ-4 | 06 §3.2/§2.1 have no authority-matrix row or values-track scope entry admitting `constitution.set_release_channel`, which 02 §12 and 09 §2.3 assign to `ConstitutionalValues` — which track carries it, and should the matrix gain the row? | 06 §2.1/§3.2 vs 02 §12; 09 §2.3 | 2026-07-15 (A1 spec-reviewer) | **resolved 2026-07-15** (batch): 06 §2.1 `constitution` track + §3.2 ConstitutionalValues row now carry `constitution.set_release_channel` |
| SQ-3 | `AttestationMissing` has two mandated producing sites across the doc set, which 05's exclusivity clause forbids. 05 §1.3 declares "every variant has exactly one producing site … MUST NOT emit from any other site" and maps `AttestationMissing` to `decide()` step 10 → T10; 05 §2.1 T16 enumerates the `Queued → Rejected` reason set as `{ StaleQueue, NotRatified }` only. But 09 §1.2(5) and 15 I-19 require the execution guard's **dispatch-time** re-check to produce `Rejected(AttestationMissing)` (post-queue attestation revocation), which is a `Queued → Rejected(AttestationMissing)` transition. The A11 (#40) fix implements the 09/I-19 behavior (guard propagates `AttestationMissing` through `epoch.expire_or_stale_queue` → T16-shaped reject → T21), so it is compliant with the execution guard's owning doc but violates 05's producer-map exclusivity and T16's closed reason set. **Recommended resolution:** extend 05 §2.1 T16's reason set to include `AttestationMissing` and relax 05 §1.3's "exactly one producing site" clause for this variant (option a) — dropping the dispatch-time producer (option b) would remove a security check (I-19). Needs a spec decision, not a code change; the code already tracks 09. | 05 §1.3, §2.1 T16 vs 09 §1.2(5); 15 I-19 | 2026-07-14 | **resolved 2026-07-14** (option a, user-approved; 05 §1.3/§2.1 + 15 I-19 edited — see Decision log) |
| SQ-2 | 02 §7.2 pins `ComponentValues` as `map (MetricId, EpochId) → SettledComponent` (no version), but 07 §2(4) runs the reporting game per `(component, epoch, frozen spec version)` with one settlement per version across a MetricSpec activation boundary — the pair key cannot represent two per-version settled values. `pallet-oracle` (PR #30 follow-up) now keys rounds/settled values by the triple; the 02 contract row needs a resolution at the B2 mapping (append a versioned map or key change — requires the joint backend+frontend sign-off and a contract-version bump per 02 §13). **The same B2 amendment batch also carries three more 02 reconciliations surfaced by the #46 Codex review (2026-07-15)** — (a) the 02 §9 intake-row erratum (binding target for `intake.max_per_account` is `params()` + K-bound metadata constants, not a "≤ 4" metadata constant — SQ-23); (b) splitting 02 §9's combined `epoch.length`/`epoch.slots`/phase-offset-fractions row so the fractions bind via metadata constants only (13 §3.1 resolution, SQ-24); (c) adding the `pallet-attestor` storage/view shape to 02 §7 (referenced by 01 §5.1 and 06 §7; read by the FE on the CODE/META execute path, SQ-26) — so the contract version bumps once. | 02 §7.2; 07 §2(4) | 2026-07-14 | open |
| SQ-1 | E-row numbering collides: 15 §3.3 (self-described "normative row list") numbers its five decision-record additions E15–E19 (E15 = VOID redemption … E19 = sudo banner + pinned-release warning), while 11 §11.12 — which 15 §3 names as owner of the required-UX matrix — defines E15–E23 with different referents (E15 = referendum voting, E16 = VOID redemption, E21 = sudo era …). Cross-refs disagree, e.g. 15 §4.8 "E15 redeem flow" = 11's E16. Which numbering is canonical? (Scenario content is equivalent; IDs conflict.) | 11 §11.12; 15 §3.3, §4.8 | 2026-07-12 | open |

## Verification log

`[VERIFY]` tags resolved against live sources (rule R-2), plus the standing backlog
lifted from the spec. Format: `| ID | Item | Spec ref | Status | Result |`

| ID | Item | Spec ref | Status | Result |
|---|---|---|---|---|
| V-1 | Re-verify all platform pins before implementation (SDK stable2603 umbrella, FE npm pins) | 01 §9 | complete (2026-07-13) | Re-verified via live web sources: GitHub releases list shows current stable2606 and stable2603 maintenance tags (stable2603-4), confirming `polkadot-stable2603` remains an extant release line; crates.io page/search confirms `polkadot-sdk` umbrella crate exists for the pinned crate family; npm package pages/search confirm the frontend packages and major lines in 01 §9 remain published (polkadot-api 2.x, smoldot 3.x, Vite 8, Dexie 4, plus the listed Arweave packages). Local direct `urllib`/`git ls-remote` verification was blocked by the container CONNECT proxy (403), so deeper API-level version enumeration remains a CI/network follow-up if exact patch drift matters before F0. |
| V-2 | Paseo onboarding / core allocation process | 01 §4.1 | open | |
| V-3 | Coretime renewal-price mechanics | 01 §4.1; 09 §4 | open | |
| V-4 | Holds support on `pallet-assets` in stable2603 (else escrow fallback) | 03 §3 | open | |
| V-5 | Stock referenda/conviction-voting external per-poll voting-power provider (oracle track) | 06 §2.3 | open | |
| V-6 | XCM reserve-probe instruction sequence under Asset Hub barrier (no `Transact`) | 07 §8 | open | |
| V-7 | Bulletin Chain mainnet availability (large evidence) | 07 §9 | open | |
| V-8 | `pallet-vesting` vs in-pallet schedule store | 08 §2 | open | |
| V-9 | smoldot/PAPI chainHead runtime-call verification semantics (FE-P2) | 02 §3; 10 §12 | open | Until resolved: cross-check runtime-API results vs storage reads on tx paths |
| V-10 | Two-pass Arweave deploy + ANT n-of-m capability (FE-P7) | 12 §1.2, §4.2 | open | |
| V-11 | FGP/SGF/GFP/EFP/AEGIS source-document identification (user/document owners) | 01 §9 | open | Blocks audit scoping only |
| V-12 | sim-gated defaults (`sec.prize.*`, `sec.flow_cap`, `collator.bond_req_vit`, `ops.*`, `fee.vit_usdc_rate_ref`) | 13 §1 | open (S4) | Phase-0 calibration |
| V-13 | Multi-MB Wasm extrinsic submission via light client (FE-P10) | 11 §11.13 | open | Gates FE-15 upgrade-crank tier |

## Decision log

Spec changes and other project decisions (rule R-1, AGENTS.md).

| Date | Amendment | Authorized by | Docs touched |
|---|---|---|---|
| 2026-07-13 | Renamed both top-level assets: **NUM → USDC** (collateral/settlement asset) and **GOV → VIT** (native governance/utility asset), plus derived identifiers (`AcceptNum`→`AcceptUsdc`, `RejectNum`→`RejectUsdc`, `NUM_LOCATION`→`USDC_LOCATION`, `gov_num_rate`→`vit_usdc_rate`, `bond_req_gov`→`bond_req_vit`, `issue_gov`→`issue_vit`, `GovIssued`→`VitIssued`, `num_Acc`/`num_Rej`/`num_w`/`num_b`→`usdc_Acc`/`usdc_Rej`/`usdc_w`/`usdc_b`, `fee_num`→`fee_usdc`); `navNum` (NAV numerator, unrelated) left untouched. 672 substitutions across the full `docs/architecture/` set (16 files) plus the derived `docs/design/claude-design-kit/` pack (6 files, regenerated in step per AGENTS.md). Since `02-integration-contract.md` changed, `INTEGRATION_CONTRACT_VERSION` must bump 1→2 per its own §13 rule — **not yet applied**. **Two edits are outstanding** — the session's permission layer denied further Bash writes into `docs/architecture/` even after explicit user approval: (a) `INTEGRATION_CONTRACT_VERSION` textual bump in 02 (three sites: lines ~116, ~412, still read `= 1`) — **blocks nothing structurally but must land before M1** since M1 stamps this constant into `futarchy-primitives`; (b) a cosmetic redundancy in 01's Polkadot Hub row ("Asset Hub is the USDC reserve and the canonical USDC location" — harmless prose only, no action-blocking). Apply both by hand, or grant a Bash permission rule for `docs/architecture/` writes and ask the agent to retry. | User (Christopher Altmann), explicit instruction this session; user also confirmed "Use USDC" over "USD" after a naming-risk flag (ISO fiat-code collision) | All 16 `docs/architecture/*.md` + 6 `docs/design/claude-design-kit/*.md` |
| 2026-07-14 | Corrected 13 §3.3's V6 vector row: it read "domain edge `q_L − q_S = 48·b` MUST reject", contradicting owning doc 04 (§4 domain `\|q_L − q_S\|/b ≤ 48` inclusive; §5 V6 rejects a buy pushing **strictly past** `48·b`) and 13's own §2 domain row. Row now mirrors 04 and notes the at-edge state is in-domain. Surfaced by the Codex review of PR #7; code already implemented 04. (PR #26) | Session decision under R-1 (owning doc unambiguous; three statements vs one stale paraphrase); flagged to user in the PR body | 13 |
| 2026-07-14 | Aligned 03 §7's `settle_gate` event name to the canonical 02 §5 spelling: `GateSettlementSet` → **`GateSettled`** (02 line 317 defines `GateSettled { pid, branch, gate, outcome }`; 02's spelling is canonical where both documents name a surface). Code events renamed and gain the winning branch in the same PR. (PR #33) | Session decision under R-1 (02 boundary rule makes 02 canonical) | 03 |
| 2026-07-14 | Clarified 03 R-2's scope: the `MinTransfer` rules are two deposit-hygiene rules (creation floor for deposit-backed destinations; remainder sweep for Signed senders), not a blanket amount minimum — exact `MarketAuthority` wrapper moves may carry the sub-`MinTransfer` fee legs that 03 R-3 routes through ordinary ops, resolving the three-way tension with 04 §6.4's `MinTrade = 1 USDC` (whose 30 bps fee is below the floor). Surfaced by the Codex review of PR #17; refined per the follow-up review of PR #34 (creation floor binds every non-protocol destination regardless of origin). (PR #34) | Session decision under R-1 (genuine contradiction; conservative resolution keeps both R-2 rules and R-3's no-special-path property) | 03 |
| 2026-07-14 | **Re-scoped Track A to production Polkadot-SDK FRAME pallets** (project decision, not a spec change — `docs/architecture/` untouched; 01 §1 already mandates native FRAME pallets). A1–A11 reopened ⬜. The existing frame-free implementations (state struct + `fn op(&mut self) -> Result` + typed Event/Error + `try_state`) are **kept as each pallet's functional core**, relocated to `crates/<name>-core/` (`no_std`, no `frame` deps — rule 9 / 01 §5.2; retained as the Python-M3 ≡ Rust-core ≡ FRAME differential oracle and the WASM/auditor-consumable port). Definition of done added to the Track A preamble: per pallet, `pallets/<name>/src/lib.rs` `#[frame_support::pallet]` shell (Config · bounded storage matching 02 byte-for-byte · event/error 1:1 · origin-checked `#[pallet::call]` over `pallet-origins` · `try_state` hook), `mock.rs`, `tests.rs` (per-extrinsic × error-path × origin-misuse × limits), `benchmarking.rs` + `weights.rs`, on the `polkadot-stable2603` pins. Architecture chosen by the user: **FRAME shell over frame-free core** (AskUserQuestion, option A). Reconciled Track B: the prior "B1a FRAME plumbing" milestone is re-narrowed to **runtime-level assembly only** (`construct_runtime!`/`impl_runtime_apis!`), since per-pallet wrapping now lives in Track A; B2–B6 depend on B1a; B1 stays ✅ as the delivered frame-free composition model. Living-doc repo maps (AGENTS.md, README.md) re-trued to the shell+core layout. | User (Christopher Altmann), explicit instruction this session ("uncheck all Track A milestones, require production-style FRAME pallets"; architecture fork resolved via AskUserQuestion → "Shell over model core") | PLAN.md, AGENTS.md, README.md (no `docs/architecture/` change) |
| 2026-07-15 | **Final-pass refinements to the SQ batch** (same user delegation; surfaced by the closing spec-reviewer pass): 13 rule 2's record shape extended to name `last_change` and `kernel_bounded` (rule 7/02 §4 presupposed them); rule 7 tightened — kernel-bounded rows freeze the **entire** metadata tuple (matching rule 2's letter; `amend_registry` refuses them outright) and now carries the **normative, exhaustive kernel-bounded enumeration** (35 keys, matching code); rule-6 `key:` annotations added for `collator.n_target` → `collator.n_tgt` and `ops.oracle_evidence` → `ops.oracle_ev`; uniform `[VERIFY]`-seeding criterion stated in code (concrete numeric default ⇒ seeded — adds `pol.b_baseline`, `collator.n_tgt`; registry = 87); "—" cooldown encodes 0 uniformly; 06 §3.2 glosses fixed (`pol.b*` for TREASURY-class keys; META amend gloss → "non-kernel rows, within meta-bounds — 13 rule 7", both origins share row scope); genesis capability row `(Code, SetReleaseChannel)` — which contradicted 02 §12's writer list — replaced by the 06 §3.2-sourced `(Code, AuthorizeUpgrade)`. | User delegation ("Decide on your own…"), same session | 06, 13, PLAN.md |
| 2026-07-15 | **Resolved SQ-4…SQ-13 as one R-1 amendment batch** (user delegated the decisions: "Decide on your own how to fix the remaining issues"). **06:** `constitution.set_release_channel` added to the `constitution` track scope (§2.1) and the ConstitutionalValues matrix row (§3.2) [SQ-4]; §2.2(b)'s machine-derived rule-altering list gained `constitution.set_param` on META+values-class keys, giving the 13 §1 dual-consent rows their enforcement point [SQ-6]; the FutarchyTreasury row gained `constitution.set_param` (TREASURY-class keys) [SQ-13]; the META row gained `constitution.amend_registry` (non-kernel bounds) [SQ-7]; a `PhaseFlags` writer-map paragraph added after §3.2 (bits 0–4 sudo/upgrades; bits 5–7 machinery-only internal setters) [SQ-5]. **13:** new reading rule 6 — canonical `ParamKey` encoding (UTF-8 ≤16 zero-padded; truncation forbidden; explicit `key:` for longer names; `.param/.trs/.code/.meta` per-class suffixes with bases `exec.lock`/`trs.reward`) [SQ-8/SQ-10]; new rule 7 — six-class `ParamClass` with the `ParamView.class` projection (CONST/entrenched→Constitutional, META+values→Meta), `kernel-bounded` marking, meta-bounds for `amend_registry` (min ≤ value ≤ max, kind-preserving, cooldown ≤ 8), and the cross-key couplings that bind at consuming engines [SQ-7/SQ-10]; `pol.budget_epoch` and `trs.stream_threshold` retyped Percent→Perbill (fractional-percent defect); `key:` annotations on all >16-byte rows; §4 gained the constitution bound rows (Params 128 / Capabilities 64 / Meters 16) [SQ-9]. **15:** I-7/I-17 enforcing-structure re-scoped — limits live as constitution `Params` records, metering lives with the flows (treasury issuance/outflow, guard upgrade-spacing), matching the delivered A9/A11 architecture; try-state coverage rule updated [SQ-12]. **Code-side resolutions without spec text:** Root stays out of param/capability/meter predicates (09 §5.4 exhaustive; SQ-11 = narrow reading adopted); `amend_registry` implemented per the new rule 7; the 13 §1 registry materialized at 85 keys; constitution `genesis_meters()` emptied. **02 deliberately untouched → no `INTEGRATION_CONTRACT_VERSION` bump** (02 §3's 64 is the per-call `params()` bound, not the registry bound; `ParamRecord`'s field list is owned by 13 rule 2, which §7.3 references by name only). | User (Christopher Altmann), explicit delegation this session ("Decide on your own how to fix the remaining issues") | 06, 13, 15, PLAN.md (no 02/00 change) |
| 2026-07-14 | **Resolved SQ-3** (option a): `AttestationMissing` is now a **dual-sited** `RejectReason`. 05 §1.3's "exactly one producing site" clause is relaxed for this one variant (decide-time T10 **and** the execution guard's dispatch-time re-check T16), its producer-map row updated to list both sites, and 05 §2.1 T16's reason set + change-note + lifecycle diagram edge + `decide()` truth table (new post-queue row) + Resolves note all extended to carry it. 15 I-19's clause refined to note both production sites. This removes the 05-vs-09/I-19 contradiction that the A11 (#40) fix surfaced — 09 §1.2(5) and 15 I-19 already required the guard to reject a post-queue attestation revocation at `execute`, which 05's closed T16 set had forbidden. No `RejectReason` variant added, so 02's contract enum is unchanged and no `INTEGRATION_CONTRACT_VERSION` bump / joint sign-off is required. | User (Christopher Altmann), explicit approval this session ("I approve option (a)") | 05, 15 |
| 2026-07-15 | **Resolved SQ-14…SQ-28 as one R-1 batch** (Track M audit follow-up). 04 §4: ulp semantics precised (exp2 ≤ 2 ulp of the [1,2) kernel ⇔ relative ≤ 2⁻⁶³; log2/ln ≤ 2 ulp absolute; marginal price ≤ 8 ulp added; 1 ulp = 2⁻⁶⁴), method made non-normative with mandatory guarded internal precision (worst-case analysis part of conformance), and a two-tier verification cadence (per-commit ≥10³ dense-bit adversarial corpus; ≥10⁷ MPFR-256 sweep at release + on kernel/reference change). 04 §5: corpus schema/location/versioning now owned here (`reference-model/fixtures/vectors.json`, `bleavit.reference-model.vN`, standalone-replayable rows) + single-generator rule (parallel generators forbidden); 15 §5 ownership split (artifacts 1–4 → 02 §11, corpus → 04 §5); 00 X-15 row updated. 04 §7: `Δblocks` defined — backward weighting normative (chain correct; model to fix). 05 §1.2: full 21-field `Proposal<AccountId>` base layout enumerated and frozen (declaration order = SCALE; resources bounded 8 per 13 §4; `payload_len` added 2026-07-15 per the #46 Codex review — decide()'s §5.6 preimage check reads it). 09 §1.2(3): `RuntimeVersionConstraint { spec_name: BoundedVec<u8,32>, spec_version: u32 }`. 15 §4.4: reference model = conforming implementation for the 05 §4.4 welfare pipeline (grid-exact), oracle only for transcendentals; single-generator restated. 15 §4.5: supply-chain gates (`--locked` + `cargo-audit`, release-blocking); 14 TH-34 citation corrected to 01 §9/15 §4.5. 13 rule 1: primitives-home boundary (fixed imports domain/error bounds; storage-bound + rowless carve-outs; 02-named types incl. `MarketKind` in primitives); 13 §2: θS⁻/θC⁻ entrenched floors explicit at 0.90/0.85 = launch defaults, tighten-only; 13 §3.1: phase-offset fractions = K metadata constants, not Params. 01 §5.1: `pallet-attestor` row added, guard depends + attestor; 01 §5.2: single-home rule; 01 §9: exact-pin adoption regime (stable2603-N procedure). SQ-23 decided with 13 normative and the 02 §9 intake-cell erratum queued into the pending B2 02-amendment (with SQ-2) — **02 untouched this batch, no contract bump**. Design kit checked — no stale restatements. | User (Christopher Altmann), explicit in-session delegation ("Resolve the spec questions yourself instead of adding them to the PLAN.md"; "Don't wait for my consent, just find the optimal approach yourself without asking") | 00, 01, 04, 05, 09, 13, 14, 15 (+ PLAN.md records) |
| 2026-07-15 | **Resolved SQ-29 + SQ-30** (follow-through of the same delegated batch; raised by the M3 fix pass): 08 §5.4 PARAM cells were a doubling slip — corrected to the pair depth 13,863 (worked line (c) = 113,863 / 170,794 / 56,931; P_ref(PARAM) = 56,931; derivation note added — every class term is 2·b·ln 2 at the 13 §1 `pol.b` floors + `dec.v_min`, gate-book depth excluded from L̂); 05 §4.6 gained the normative percentile family (inclusive linear / "type-7", FixedU64 1e9 grid, interpolation product rounded down per §4.4, never vacuous on the 12-element sample). Reference model reconciled in fix-branch commit 41531dd (preserve-the-typo branch dropped, pinned test updated; 21/21 green). | User (standing in-session delegation) | 08, 05 (+ `reference-model/` on `fix/m3-reference-model-spec-compliance`) |

## Audit log

`/spec-audit` runs. Format: `| Date | Scope | Verdict | Pointer |`

| Date | Scope | Verdict | Pointer |
|---|---|---|---|
| 2026-07-15 | Track M (M0–M3) | 0 blocker / 13 major / 22 minor after cross-report dedup (M0 0/1/7 · M1 0/3/5 · M2 0/2/4 · M3 0/7/6, one corpus/differential major shared M2+M3), plus 2 majors routed outside Track M (runtime `fee.vit_usdc_rat` key truncation → B1a; tunables baked into cores → A4–A9 shell reviews). Recommendation: M2 and M3 ✅ invalidated (exp2 breaks the 04 §4 ≤2-ulp normative bound, measured 10.11 ulp worst / 36.9 % of inputs; reference model deep only for LMSR, 04 §5 three-way CI rule unimplemented); M1 kernel-constant scope shortfall (≈half of 13's K constants absent, downstream literals); M0 stands with CI follow-ups. No statuses flipped — user consent required. | Session log 2026-07-15; SQ-14…SQ-28 |

## Unplanned changes

Repo changes outside any milestone (config tweaks, user-driven edits) — one line each.

- 2026-07-12 — Added `docs/design/claude-design-kit/` (user-requested): 7-file non-normative context pack for Claude Design (docs 10/11 copied verbatim with derived-copy headers; 00/01–09/13/14/15 distilled read-only) + `PROMPT.md`. Spec untouched; README/AGENTS repo maps updated.
- 2026-07-13 — Project renamed "Bleevit" → "Bleavit" (user-requested): literal replace across the same 10 living/derived files. `docs/architecture/` still contains zero occurrences, so no frozen-doc amendment was needed.

## Session log

Append-only; newest last. Format: `| Date | Milestone(s) | Done | Next |`

| Date | Milestone(s) | Done | Next |
|---|---|---|---|
| 2026-07-12 | — (infrastructure) | Agent/session infrastructure created: PLAN.md, AGENTS.md, CLAUDE.md, README.md, .claude (settings, 3 hooks, 4 skills, 3 subagents, 3 path rules), .codex playbooks. Architecture write-guard tested (26 cases). | M0 — repo bootstrap incl. V-1 pin re-verification |
| 2026-07-12 | — (design kit, unplanned) | Built `docs/design/claude-design-kit/` for Claude Design's 10-attachment limit: 7 context files (2 verbatim copies of docs 10/11 + 5 distillations of 00–09/13–15) + generation prompt. Logged SQ-1 (E-row numbering conflict 11 §11.12 vs 15 §3.3). Architecture untouched; README/AGENTS maps updated. | M0 — repo bootstrap incl. V-1 pin re-verification |
| 2026-07-13 | — (architecture amendment, unplanned) | R-1 change control invoked: renamed NUM→USDC, GOV→WIT (+ derived identifiers) across all 16 `docs/architecture/*.md` and 6 `docs/design/claude-design-kit/*.md` (672 substitutions); see Decision log. `INTEGRATION_CONTRACT_VERSION` bump and one cosmetic 01-prose fix left outstanding — permission layer blocked further `docs/architecture/` writes this session. `.claude/architecture-amendment.flag` deleted after this log entry per the change-control procedure. | M0 — repo bootstrap incl. V-1 pin re-verification; **before M1**, apply the two outstanding 02/01 edits above |
| 2026-07-13 | — (rename, unplanned) | Renamed project "Bleevit" → "Bleavit" across all living/derived docs (same 10 files as the prior renames). Confirmed `docs/architecture/` has no occurrences, so R-1 change control did not apply. | M0 — repo bootstrap incl. V-1 pin re-verification; **before M1**, apply the two outstanding 02/01 edits above |
| 2026-07-13 | — (architecture amendment, unplanned) | R-1 change control **completed** (finally unblocked): ticker **WIT → VIT** + derived identifiers renamed across 12 `docs/architecture/*.md` and 7 `docs/design/claude-design-kit/*.md` (152 substitutions; see Decision log), and **`INTEGRATION_CONTRACT_VERSION` bumped 1→2** in doc 02 — discharging the bump left outstanding from the NUM/GOV amendment. User gave explicit in-session authorization (AskUserQuestion: "Yes — full rename"), which cleared the auto-mode classifier that had blocked the four prior attempts (it correctly requires explicit architecture-amendment consent, per AGENTS.md). Also fixed a stale `issue_gov`→`issue_vit` leftover in the A9 row. `.claude/architecture-amendment.flag` deleted after this log entry. **Note for user:** the working tree still carries the pre-existing dismantling of the R-1 guard (`deleted .claude/hooks/guard-architecture.sh`, `modified .claude/settings.json` — deny-list + PreToolUse guard removed); this was NOT done by this session and should be restored to re-enable frozen-doc protection. | M0 — repo bootstrap incl. V-1 pin re-verification; decide whether to restore the R-1 guard machinery (settings.json + guard-architecture.sh); the cosmetic 01 Polkadot-Hub prose fix from the NUM/GOV amendment is still outstanding |
| 2026-07-13 | — (governance change, unplanned) | **Removed the `docs/architecture/` write guard entirely, per explicit user decision** ("completely remove the current architecture guard … this should not be"; AskUserQuestion → "Unfreeze entirely"). This supersedes the prior row's "should be restored" note — the guard is NOT coming back. Deleted `.claude/hooks/guard-architecture.sh` (already gone in the tree) and confirmed `.claude/settings.json` carries no deny-list / PreToolUse entry. Dismantled the amendment-flag apparatus: removed the flag block from `session-context.sh` and the amendment-window check from `stop-plan-guard.sh`; deleted the "Amending the architecture" change-control section from AGENTS.md. **Rule R-1 repurposed** from "the architecture is frozen" to "the specification is the source of truth for behavior; it is editable, changed deliberately (consistent across the doc set, version-bumped when 02 changes, logged in the Decision log)"; R-1's essence now lives in AGENTS.md · *Changing the specification*. Re-trued every living/config asset that called the spec "frozen" as an editing rule (AGENTS/CLAUDE/README/PLAN, doc-curator, spec-reviewer, test-engineer, implement/spec-audit/sync-docs skills, reference-model rule, and the 4 `.codex` playbooks), while leaving protocol-level "frozen" (the versioned integration contract, `PB-LEDGER-FREEZE`, "contract freeze co-signed", "append-only after freeze") untouched. The two Stop guards (PLAN.md freshness, README pinned lines R-11) are unchanged. | M0 — repo bootstrap incl. V-1 pin re-verification; the cosmetic 01 Polkadot-Hub prose fix from the NUM/GOV amendment is still outstanding (now a direct edit under R-1, no ceremony needed) |
| 2026-07-13 | M0 | Bootstrapped the repository: added the virtual Cargo workspace and rust-toolchain, CI workflow for fmt/clippy/test plus docs link lint, implementation root placeholders, and lightweight local gate scripts. Re-verified V-1 platform pins from live web sources and recorded the proxy limitation for direct terminal API checks. Local Rust gate wrapper and docs link lint passed; direct cargo fmt/clippy/test commands remain armed for the first member crate. | M1 — `crates/futarchy-primitives` (`no_std` shared SCALE types + kernel constants) |
| 2026-07-13 | — (README asset, unplanned) | `assets/Bleavit-logo.png` (pre-existing uncommitted addition, not made this session) had been inserted between the `# Bleavit` heading and the pinned R-11 opening paragraph, and was rendering at its native 606×661px with no size constraint. This session moved the logo above the `# Bleavit` heading in a centered `<p align="center">` block sized to `width="160"` (the conventional above-title logo position), restoring the pinned paragraph as the first content immediately after the heading per R-11. No architecture or milestone work was touched. | A3 — `pallet-market` (still unstarted; see Current focus) |
| 2026-07-14 | — (Codex review-fix sweep, user-directed) | Triaged all 37 Codex inline review findings across merged PRs #1–#21 and fixed every confirmed one in 11 fix PRs (one per component): #24 ci doc-link checker, #25 primitives bounded decode (+ allocation-hook follow-up), #26 futarchy-fixed corpus gate to the normative 04 §4 bounds + 13 V6 row correction, #27 reference-model TWAP (1±κ)^k / decide() step-6–8 structure / settlement geomean + corpus v2, #28 constitution MaxDelta{Absolute,Percent,Factor} + canonical keys, #29 origins nested-wrapper privileged-payload denial, #30 oracle recompute-proof verification, ack deadlines, settled-finality, probe single-count (+ per-spec-version games follow-up), #31 registry bonded-watchtower quorum + filing-window fencepost + live-epoch reaping, #32 treasury trailing-window meters, mandatory streams, permissionless quote-priced coretime renewal, atomic meter checks, line-credited issuance, #33 ledger atomic operations + pair-exact vault-conservation try_state + 02-canonical settlement events, #34 (stacked on #33) market Traded{who}, sell mirror-merge, baseline fee withholding, dust-fee admissibility, full TWAP widening, atomic trade wrappers. Verified-no-action: PR #13 pytest finding (merged CI uses stdlib unittest); 4 of 6 futarchy-fixed findings already fixed by #7/#10/#11; PR #12's 263-ulp edge claim misreads the b-scaled 04 §4 bound (263 ≤ 8·b = 80,000 — gate now proves it). A second Codex pass over the fix PRs produced 11 further findings — all verified and fixed in follow-up commits on the same branches. Every fix PR: full workspace gates green. Three R-1 spec edits in the Decision log; new SQ-2 raised (02 §7.2 ComponentValues key vs 07 §2(4)). | Merge fix PRs #24–#33, then the stacked #34; resolve SQ-2 at B2; A7 — `pallet-welfare` |
| 2026-07-13 | M1 | Started `crates/futarchy-primitives`: added the workspace member, no_std primitive aliases, contract/view enums and structs, bounded-vector wrapper, integration-contract v2 constant, kernel/chain/currency bound constants, and unit coverage for bounds and enum ordinals. Rust gates passed with the dependency-free implementation; direct `parity-scale-codec`/`scale-info` derives are not yet wired because the container cannot reach crates.io (CONNECT 403), so M1 remains in progress rather than done. | Finish SCALE/metadata derives for M1 once registry access or vendored dependencies are available; then run full gates and mark M1 ✅ |
| 2026-07-13 | M1 | Added direct `parity-scale-codec`/`scale-info` derive wiring for the no_std shared primitive/view types, bounded SCALE decoding for the local `BoundedVec` wrapper, and a regression test that rejects over-bound SCALE payloads. Also corrected doc 02's ownership paragraph to say contract version 2, matching the already-bumped `INTEGRATION_CONTRACT_VERSION`. Rust gates remain blocked by the environment's crates.io CONNECT 403 before dependency resolution, so M1 remains in progress rather than ✅. | Re-run `tools/ci/rust-workspace-gates.sh` once registry access or vendored dependencies are available; if green, perform spec-compliance review and mark M1 ✅ |
| 2026-07-13 | M1 | Completed `crates/futarchy-primitives`: generated and committed the dependency lockfile after registry access succeeded, re-ran the full Rust workspace gate (`cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`) successfully, ran the docs link lint, and performed a spec-compliance pass against 02 §2, 13 §2, and 01 §5.2 with no blockers found. M1 is now ✅. | M2 — `crates/futarchy-fixed` (64.64 fixed point, exp2/log2/ln, maker-adverse rounding, error bounds) |
| 2026-07-13 | M2 | Started `crates/futarchy-fixed`: added the workspace member, unsigned 64.64 type, checked arithmetic, `ln 2`/error-bound constants, maker-adverse charge/payout rounding helpers, stable two-outcome LMSR cost, and unit coverage including the normative V1/V4 values. Workspace Rust gates and docs link lint passed. Spec-compliance review found one blocker before completion: the current transcendental kernel uses default-`std` `f64` intrinsics and does not yet satisfy the required no_std deterministic ≤2 ulp polynomial/range-reduction implementation or MPFR corpus regime, so M2 remains in progress. | Finish the no_std deterministic exp2/log2/ln implementation and MPFR/differential corpus gate, then re-run full gates and mark M2 ✅ |
| 2026-07-13 | M2 | Advanced `crates/futarchy-fixed` by removing the `std`-only transcendental surface: `exp2`, `log2`, `ln`, and `lmsr_cost` now compile under `--no-default-features` using deterministic in-crate range-reduction/series helpers, and fixed-point multiply/divide/conversion paths no longer rely on `std` fallbacks for the covered LMSR domain. Workspace Rust gates, docs link lint, and the explicit no-default-features fixed-crate test passed. M2 remains in progress because the helper is still an interim deterministic kernel, not the final spec-required ≤2 ulp polynomial/integer implementation with full MPFR-256 corpus gate. | Finish the final 64.64 ≤2 ulp transcendental kernel and MPFR-256 corpus/differential gate; then re-run gates and mark M2 ✅ if the spec-compliance pass has no blockers |
| 2026-07-13 | M2 | Added LMSR buy/sell delta helpers for the fixed-point crate and changed the core cost path to reject states past the normative `|q_l - q_s| / b ≤ 48` domain instead of silently clamping; added regression coverage for V1 path-independence and the V6 domain-edge rejection. `cargo fmt --all` and docs link lint passed. Workspace/fixed-crate Rust gates were blocked by the container crates.io CONNECT proxy while resolving `parity-scale-codec`/`scale-info`, so M2 remains in progress. | Re-run Rust gates once registry access is available; finish the final ≤2 ulp integer/polynomial kernel and full MPFR-256 corpus gate before marking M2 ✅ |
| 2026-07-13 | M2 | Added stable LMSR marginal price helpers for LONG/SHORT plus a price-to-price displacement helper, and expanded fixed-crate vector coverage to V2 (post-buy price) and V3 (0.5→0.6 displacement and cost) alongside the existing V1/V4/V6 checks. `cargo fmt --all` and docs link lint passed. Full Rust workspace gates remain blocked in this container by the crates.io CONNECT proxy while resolving `parity-scale-codec`/`scale-info`, so M2 remains in progress. | Re-run Rust gates once registry access is available; finish the final ≤2 ulp integer/polynomial kernel and full MPFR-256 corpus gate before marking M2 ✅ |
| 2026-07-13 | M2 | Added an interim generated high-precision LMSR corpus fixture for `futarchy-fixed`, plus the deterministic generator script and a unit gate that checks cost/price/displacement samples spanning V1–V4, symmetric states, asymmetric states, and near-domain-edge states. Direct `rustc --test` checks for the fixed crate (with and without the `std` cfg) and docs link lint passed. Cargo-based Rust gates remain blocked in this container by the crates.io CONNECT proxy while resolving `parity-scale-codec`/`scale-info`, so M2 remains in progress. | Re-run Rust gates once registry access is available; finish the final ≤2 ulp integer/polynomial kernel and scale the interim fixture into the full MPFR-256 corpus gate before marking M2 ✅ |
| 2026-07-13 | M2 | Strengthened the interim `futarchy-fixed` LMSR corpus gate: the generator now emits nearest 64.64 raw expectations alongside decimal values, supports `--check` freshness validation, and the Rust unit test checks both decimal tolerance and raw fixed-point drift; the workspace gate now runs the corpus freshness check after Rust tests. Full workspace Rust gates, the explicit no-default-features fixed-crate test, the direct `rustc --test` fixed-crate check, the corpus freshness check, and docs link lint all passed. M2 remains in progress because the final ≤2 ulp integer/polynomial kernel and full MPFR-256 corpus are still outstanding. | Finish the final ≤2 ulp integer/polynomial kernel and scale the interim fixture into the full MPFR-256 corpus gate before marking M2 ✅ |
| 2026-07-13 | M2 | Reworked `futarchy-fixed` checked multiplication/division to use in-crate two-limb `u256`-style intermediates instead of split-limb shortcuts or `f64` fallback, preserving checked overflow/division-by-zero behavior and adding regression coverage for large fixed-point arithmetic. Full workspace Rust gates, the fixed-crate no-default-features test, corpus freshness check, and docs link lint all passed. M2 remains in progress because the final ≤2 ulp integer/polynomial transcendental kernel and full MPFR-256 corpus are still outstanding. | Finish the final ≤2 ulp integer/polynomial kernel and scale the interim fixture into the full MPFR-256 corpus gate before marking M2 ✅ |
| 2026-07-13 | M2 | Extended the interim high-precision corpus generator to emit a primitive transcendental fixture for `exp2`/`log2`/`ln` samples, added a Rust unit gate that compares decimal and nearest 64.64 raw expectations, and kept the freshness check covering both corpus files. Full workspace Rust gates, the fixed-crate no-default-features test, corpus freshness check, and docs link lint passed. M2 remains in progress because the final ≤2 ulp integer/polynomial transcendental kernel and full MPFR-256 corpus are still outstanding. | Finish the final ≤2 ulp integer/polynomial kernel and scale the interim fixtures into the full MPFR-256 corpus gate before marking M2 ✅ |
| 2026-07-13 | M2 | Completed `crates/futarchy-fixed` for M2 by replacing the interim transcendental path with deterministic integer/table `exp2`, iterative fixed-point `log2`/`ln`, and integer-only LMSR exp/logit paths; tightened the primitive-transcendental corpus gate to the ≤2 ulp target; re-ran full Rust workspace gates, no-default-features fixed-crate tests, corpus freshness checks, and docs link lint successfully. M2 is now ✅; the release-scale MPFR-256 corpus/vector regeneration gate remains owned by M3. | M3 — independent Python reference model + MPFR-256 corpus/vector regeneration |
| 2026-07-13 | M3 | Implemented the independent Python reference model package and generated JSON vector corpus: LMSR V1–V6/corpus, TWAP accumulator, ledger VOID primitives, welfare normalization helpers, decision reason-code scenarios, and treasury security-sizing arithmetic. Added pytest coverage, corpus freshness checks to local gates, and a dedicated CI job. Full Rust gates, reference-model tests, vector freshness, and docs link lint passed. M3 is now ✅. | A1 — `pallet-constitution` (typed/bounded params, meters, capability tables, phase flags, release channel key) |
| 2026-07-13 | M3 | Fixed CI portability for the reference-model job by removing the undeclared pytest runner dependency: tests now use Python stdlib `unittest`, CI invokes them with an explicit `PYTHONPATH`, and the local gate runs the same unittest suite before vector freshness. | A1 — `pallet-constitution` |
| 2026-07-13 | A1 | Started `pallet-constitution` as a workspace crate: added typed `ParamRecord`/`ParamValue` update validation with hard bounds, max-delta and cooldown checks; resettable meters; bounded capability records; `PhaseFlags` bit assignments; fixed-width 168-byte `ReleaseChannel` parsing; genesis parameter seeds for initial 13 §1 rows; and kernel/contract re-exports. Workspace Rust gates, no-default-features constitution tests, and docs link lint passed. A1 remains in progress because the crate is not yet a full FRAME pallet with storage/extrinsics, mock-runtime origin-misuse/error-path suites, try-state, and benchmark stubs. | Finish FRAME pallet wiring for `pallet-constitution`, add the A1 verification scaffolds, re-run gates, then mark A1 ✅ after spec-compliance review |
| 2026-07-13 | A1 | Completed `pallet-constitution`: added bounded meter storage and genesis state helpers, origin-gated dispatch entrypoints for parameter, capability, phase-flag, release-channel, and meter updates, a try-state validator for storage shape/ranges, runtime-benchmark stubs behind `runtime-benchmarks`, and expanded unit coverage for origin misuse plus error paths. Full Rust workspace gates, no-default-features constitution tests, runtime-benchmarks feature tests, docs link lint, and a spec-compliance pass against 06, 13 §1/§4, 02 §7.3/§12, and 15 §4.1 found no blockers. A1 is now ✅. | A2 — `pallet-conditional-ledger` |
| 2026-07-13 | A2 | Implemented `pallet-conditional-ledger` as a workspace crate: proposal and Baseline vault records, per-branch supply accounting, split/merge/scalar/gate families, authority state transitions, `Voided`/`redeem_void`, terminal scalar/gate/Baseline redemptions with claimant-adverse floors, protocol-account deposit/cap exemptions, internal authority-origin API, try-state validation, runtime-benchmark stubs, and mock-style unit coverage for origin misuse/error paths. Full Rust workspace gates, no-default-features conditional-ledger tests, runtime-benchmarks feature tests, docs link lint, and a spec-compliance pass against 03, 02 §6, and 15 §4.1–§4.3 found no blockers. A2 is now ✅. | A3 — `pallet-market` |
| 2026-07-13 | A3 | Implemented `pallet-market`: added the workspace member, no_std SCALE market storage/event types, LMSR buy/sell wrappers over `pallet-conditional-ledger`, branch/gate/Baseline book creation and POL seeding, 30 bps fee accounting, previous-quote capped observations/TWAP accumulation, `BaselineMarketOf`, try-state validation, benchmark stubs, and error-path tests for slippage/phase/trade limits plus Baseline duplicate mapping. Full Rust workspace gates, no-default-features market tests, and docs link lint passed; spec-compliance pass against 04 and 02 §5–§7.4 found no blockers. A3 is now ✅. | A4 — `pallet-origins` + `SafetyFilter` |
| 2026-07-14 | A4 | Implemented `pallet-origins`: added the workspace member, eight SCALE custom origins, proposal-class origin mapping, call-domain classification, `SafetyFilter` validation with the closed wrapper set (`batch`/`batch_all`/`force_batch`, `proxy`/`proxy_announced`, `as_multi`/`as_multi_threshold_1`, scheduler, sudo, and utility wrappers), bounded recursion/call-count checks, benchmark feature stub, and negative coverage for origin misuse and wrapper bypasses. Full Rust workspace gates, no-default-features origins tests, runtime-benchmarks feature tests, docs link lint, and a spec-compliance pass against 06 §3, 01 §6, and 15 §1 found no blockers. A4 is now ✅. | A5 — `pallet-oracle` (reporter/watchtower registries, bonded reporting game, challenge rounds, reserve probe, neutral settlement) |
| 2026-07-14 | A5 | Implemented `pallet-oracle`: added the workspace member, SCALE-stable reporter/watchtower registries, value-scaled bonded report/challenge rounds, watchtower acknowledgment quorum with one-time extension and neutral settlement, recompute and OracleResolution adjudication paths, reporter offense/slash/ejection accounting, fail-static reserve probe state, try-state validation, runtime-benchmark stub, and origin/error-path tests. Full Rust workspace gates, no-default-features oracle tests, runtime-benchmarks feature tests, docs link lint, and spec-compliance review against 07 §1–§6/§8–§13, 02 §7.2, 13 oracle/reserve rows, and 15 I-18/I-24 found no blockers. A5 is now ✅. | A6 — `pallet-registry` |
| 2026-07-14 | A6 | Implemented `pallet-registry`: added the workspace member, SCALE-stable Incident/Milestone filing state, bonded file/challenge/close paths, 72 h windows plus one 48 h quorum extension, watchtower acknowledgments, single-round challenge resolution, 40/60 slashing events, terminal epoch aggregates for Incident and Milestone instances, try-state validation, runtime-benchmark stub, and error-path tests for bounds, windows, spec-version mismatches, challenge discipline, quorum, and close-out. Full Rust workspace gates, no-default-features registry tests, runtime-benchmarks feature tests, docs link lint, and spec-compliance review against 07 §7, 02 §6, 13 registry bounds, and 15 I-18/I-22 found no blockers. A6 is now ✅. | A7 — `pallet-welfare` |
| 2026-07-14 | A9 | Implemented `pallet-futarchy-treasury` out of dependency-order by explicit user request: added the workspace member, SCALE-stable treasury account/budget-line types, NAV and spendable-NAV view with reserve-health haircut flag, line-scoped outflow caps and 30/180-day rolling meters, mandatory linear streams with claim/cancel paths, VIT issuance restricted to REWARDS/ops lines under a rolling 2%/365-day cap, foreign-asset recovery, dedicated coretime renewal call, NAV floor checks/events, try-state validation, benchmark stub, and origin/error-path tests. Full Rust workspace gates, no-default-features treasury tests, runtime-benchmarks feature tests, docs link lint, and spec-compliance review against 08 §1/§6–§9, 13 treasury/issuance/ops rows, and 15 I-7/I-17/I-22 found no blockers. A9 is now ✅; dependency-order work should return to A7. | A7 — `pallet-welfare` |
| 2026-07-14 | A10 | Implemented `pallet-guardian` and `pallet-attestor` as workspace crates: 7-seat guardian membership with 5-of-7 approval aggregation, bounded enumerated powers, force-rerun/pre-execution and once-per-proposal checks, trigger-gated playbook activation including PB-LEDGER-FREEZE renewal discipline, retrospective review/ratification and slashing, plus a bonded values-elected attestor registry with 2-of-N quorum, 72 h challenge windows, challenge resolution, false-attestation slashing/ejection, try-state checks, benchmark stubs, and origin/error-path tests. Full Rust workspace gates, no-default-features tests for both new crates, runtime-benchmarks feature tests for both new crates, docs link lint, and spec-compliance review against 06 §5–§7, 13 guardian/attestor rows, and 15 I-19/I-23 found no blockers. A10 is now ✅. | Return to A7 — `pallet-welfare` |
| 2026-07-14 | A10 | Merged main into the A10 branch with a real merge commit, kept main's A9 treasury implementation while preserving guardian/attestor workspace registration, and addressed remaining review issues: guardian bond/attestor bond constants now use VIT base units, delay_once is queued-only with epoch-reset allowances, PB-LEDGER-FREEZE duplicate activations are rejected, failed threshold dispatch rolls back the fifth approval, review-capacity checks are pre-mutation, and failed retrospective review slashing remains approver-scoped. Rust gates and docs link lint passed. | A7 — `pallet-welfare` |
| 2026-07-14 | — (PR follow-up) | Addressed PR review feedback on `BoundedVec` SCALE decoding: after rejecting oversized compact lengths before element reads, in-bound vectors now delegate allocation/decode to `parity_scale_codec::decode_vec_with_len` so `Input::on_before_alloc_mem` accounting is preserved; added regression coverage for both no-allocation oversized rejection and in-bound allocation charging. Workspace Rust gates passed. | Continue current focus A7 — `pallet-welfare` |
| 2026-07-14 | A7 | Implemented `pallet-welfare`: added the workspace member, SCALE-stable MetricSpec registry with activation/discipline/weight checks, bounded epoch snapshots, deterministic S/C_onchain/C_attested/P/A pillar aggregation, daily gate-breach flags that exclude attested inputs, welfare gate/product computation, I-16 settlement scoring on creation-time spec version, WelfareView projection, try-state validation, runtime-benchmark stub, and unit coverage for error paths/spec-version binding/gate semantics. Updated the reference model welfare score and generated welfare vectors. Full Rust workspace gates, no-default-features welfare benchmark-feature tests, reference-model unittest suite, vector freshness, docs link lint, and spec-compliance review against 05 §4/§6–§7, 02 welfare view/storage names, 13 welfare bounds, and 15 I-16/I-18 found no blockers. A7 is now ✅. | A8 — `pallet-epoch` |
| 2026-07-14 | A7 | Addressed PR review fixes for `pallet-welfare`: daily gate recording now computes only S and C_onchain from the provided daily components instead of requiring settlement-only P/A/attested inputs; snapshots reject duplicate `(epoch, spec_version)` entries and out-of-range incident multipliers; removed a duplicate snapshot field initialization and dead daily-field plumbing; added regression tests. Re-ran full Rust gates, welfare no-default-features benchmark-feature tests, reference-model tests, vector freshness, and docs link lint successfully. | A8 — `pallet-epoch` |
| 2026-07-14 | A7 | Addressed remaining Codex review suggestions for `pallet-welfare`: bounded `GateBreachFlags` storage, strengthened `try_state` to reject duplicate spec versions, duplicate snapshots, over-bound snapshots/gate flags, malformed metric ordering/ranges, and over-bound snapshot component vectors; added corrupt-storage regression coverage. Attempted `git merge main`, but this checkout has no `main` ref or remote, so there was nothing available to merge. Re-ran full Rust gates, welfare no-default-features benchmark-feature tests, reference-model tests, vector freshness, and docs link lint successfully. | A8 — `pallet-epoch` |
| 2026-07-14 | A7 (review fix) | Merged current main (Codex review-fix sweep #24-#38, A9/A10) into the A7 branch twice as main advanced, resolving workspace/PLAN/reference-model conflicts (kept main's grid-rounded settlement score and richer v2 scenario corpus; adopted the branch's gate() smoothstep). Spec-verified the welfare pallet against 05 §4.4 and corrected four pipeline deviations: weighted geometric composites now evaluate as one exp2(Σ w·log2(max(x, ε))) with each product rounded toward −∞ (was per-component exp2 with grid multiplies); the settlement score uses the normative exp2/log2 evaluation (was a custom sqrt); the C weight vector is one joint C_onchain∪C_attested vector summing to 1 (was per-sub-pillar sums, making C_e carry effective weight 2 and C_daily's renormalization vacuous); C_daily now renormalizes over the on-chain subset. Regression tests pin the joint-weight rule, a renormalization-discriminating daily breach, and pipeline values within primitive tolerance. Full Rust gates, welfare no-default-features and runtime-benchmarks tests, reference-model suite, vector freshness, and docs link lint green. | A8 — `pallet-epoch` |
| 2026-07-14 | A8 | Implemented `pallet-epoch`: added the workspace member, SCALE-stable epoch/proposal/cohort state, n/21 phase clock with next-epoch length activation, intake and live proposal bounds, resource locks, T1–T24 lifecycle helpers, keeper/guardian/execution origins, conservative `decide()` engine with gate vetoes, extension discipline, convergence/hurdle checks, security sizing, attestation and constitutional queue gates, cohort measurement/settlement wiring through welfare and ledger authority APIs, `RecentCohortSummaries` FIFO ring, try-state checks, benchmark stub, and origin/error-path unit coverage. Full Rust workspace gates, epoch no-default-features and runtime-benchmarks tests, docs link lint, and spec-compliance review against 05 §1–§3/§5, 02 §7.1, 13 epoch/storage bounds, and 15 I-14/I-15/I-21 found no blockers. A8 is now ✅; TLA+/Quint model checking remains owned by S1. | A11 — `pallet-execution-guard` |
| 2026-07-14 | A8 (review fix) | Fixed the four P1 Codex findings on PR #39 (`pallet-epoch`), all traced to 05/13: (1) T21 now resolves a rejected/expired proposal's deployed vault to `Branch::Reject` before measurement (threaded the ledger handle through `decide()`/`expire_or_stale_queue()` → `reject_to_measurement`/`reject_branch_measurement`), so `settle_cohort`'s `settle_scalar` no longer errors on an `Open` vault and the reject leg is redeemable (05 §2.1 T21, §6/§7; 03 vault machine); (2) `settle_cohort` now settles gate books from `GateBreachFlags(e+1,e+2)` via a new `pallet-welfare::gate_breach` accessor — Survival←S-breach, Security←C-breach, OR'd across the two measurement epochs — instead of a hardcoded `false`, and the gate settlement is now guarded on `requires_gate` and propagates ledger errors (was unconditional over every cohort proposal with swallowed errors) since only gate-bearing classes carry gate books (05 §4.7/§5.1/§6/§7.2); (3) gate-veto caps corrected to `gate.p_max=0.05` (was 0.20) and `gate.eps=0.02` (was 0.01) as named constants (05 §5.1; 13 §2); (4) per-class `dec.delta` fixed to 0.015/0.025/0.040/0.060 (CODE/META were conflated at 0.040) and `dec.sigma` to 0.003/0.005/0.008/0.010 (was TREASURY-only 0.010) for PARAM/TREASURY/CODE/META (13 §2). Also corrected an adjacent same-class deviation Codex did not flag: `DELTA_MAX_1E9` (convergence `dec.delta_max`) was 0.02 — the doc-13 *min* — now the 0.05 default (05 §5.2/§5.4 step 8; 13 §2). Added four regression tests (T21 vault-resolve, gate-veto cap, spec-δ hurdle adopt, breach-flag gate settlement) plus a `class_hurdle_params_match_spec` pin; simplified welfare `current_view` onto the new accessor. Full Rust workspace gates, epoch no-default-features and runtime-benchmarks tests, and docs link lint green. | A11 — `pallet-execution-guard` |
| 2026-07-14 | A11 | Implemented `pallet-execution-guard`: added the workspace member, SCALE-stable queue/payload/record/upgrade types, epoch-only enqueue, ratify-track binding, permissionless `execute()` with dispatch-time maturity/preimage/version/ratification/attestation/capability/meter/resource/guardian/freeze/payload/SafetyFilter checks, atomic batch result recording, epoch/ledger handoff on success, bounded `ExecutionRecords` ring, two-phase `authorize_upgrade`/`apply_authorized_upgrade` with `DescriptorLeadTime`, pre-upgrade checkpoints, view projection, try-state checks, benchmark stub, and origin/error-path unit coverage. Full Rust workspace gates, execution-guard no-default-features tests, runtime-benchmarks feature tests, docs link lint, and spec-compliance review against 09 §1–§3, 02 §7/§12, 13 execution bounds, and 15 I-9/I-10/I-11/I-19 found no blockers. A11 is now ✅. | B1 — Runtime assembly |
| 2026-07-14 | — (conversational session, no implementation) | This session was Q&A only — three questions on the governance design (who decides the system's values; what the constitution is; how an urgent security fix is handled), answered read-only from `docs/architecture/` 05/06/09/13. **No code was written by this session.** On checking the tree at stop time, `pallets/epoch/src/lib.rs` (+56) and `pallets/execution-guard/src/lib.rs` (+360 vs HEAD) carried uncommitted changes that post-date the committed A11 (`dbed0de`); `execution-guard` was still being written externally (mtime advanced during the session, diff grew between reads), so this is in-progress work from another process/session, not mine. Left entirely untouched — not edited, staged, committed, or reverted. A11's ✅ status reflects its own committed implementation and is unchanged; the uncommitted delta (likely post-A11 refinement/review-fix) is unverified by this session and should be owned by whoever authored it. | (next session: reconcile the uncommitted epoch/execution-guard delta, then B1 — Runtime assembly) |
| 2026-07-14 | A11 (review fix) | Fixed the five Codex findings on PR #40 (`pallet-execution-guard`), traced to 09/05/13: **(P1) preimage binding** — `execute`/`check_dispatch_time` now re-derive the payload hash from the SCALE-encoded calls (`hash_payload`, excluding the model's runtime-outcome `succeeds`/`error` fields) and match it against the committed `payload_hash`, so a keeper can no longer swap `payload.calls` under the queued hash (09 §1.2(2)). **(P1) apply-upgrade verification** — `apply_authorized_upgrade` now takes the artifact bytes, re-derives the hash via `content_hash` (not a trusted caller field), and records the *authorized* `target_spec_version`, rejecting a spoofed `observed_spec_version` (`UpgradeVersionMismatch`) (09 §2.1(4)/(6)). **(P1) T18/T22/T23 failure path** — a reverted batch now advances the epoch proposal `Queued → FailedExecuted` (new `pallet-epoch::mark_failed_executed`, event `ExecutionFailed{PayloadReverted}`), writes the failure record, keeps the queue entry retryable within `RETRY_WINDOW` (72 h), returns `Ok` so the transition persists; a successful retry runs T23 via `mark_executed`, and `expire_failed_execution` drives T22 (`retry_exhausted_to_measurement`, resolve-Accept → Measuring) once the window closes (05 §2.1 T18/T22/T23). **(P2) AttestationMissing propagation** — `pallet-epoch::expire_or_stale_queue` now handles `AttestationMissing` (T16→T21) and the guard propagates the epoch result instead of discarding it, so a dispatch-time attestation revocation no longer strands the proposal `Queued` (09 §1.2(5)). **(P2) META upgrades** — `domain_allowed` admits the internal-root authorize/apply domains for CODE **or** META (09 §2). Six regression tests added (one per finding + existing suite updated); full Rust workspace gates, execution-guard/epoch no-default-features and runtime-benchmarks tests, docs link lint, and the reference-model suite green. A `spec-reviewer` pass over both files vs 09 §1–§3 / 05 §2.1 / 13 / 15 I-9/I-10/I-11/I-19 confirmed all five fixes compliant with their owning docs (0 blocker / 0 major); it surfaced **SQ-3** — `AttestationMissing` is mandated as a dispatch-time reject reason by 09 §1.2(5)/I-19 but was forbidden by 05 §1.3's single-producer clause and T16's closed reason set (the code tracks 09). **SQ-3 resolved this same session (option a, user-approved):** 05 §1.3/§2.1 and 15 I-19 edited to make `AttestationMissing` a deliberately dual-sited reason (T10 decide-time + T16 dispatch-time re-check); no `RejectReason` variant added, so 02 is untouched and no contract bump is needed (see Decision log). Two minor model-fidelity notes (T22 `PayloadReverted` not annotated onto a cohort `DecisionRecord` — this model has none; `apply_authorized_upgrade` records the authorized target rather than the literal "observed" version — intentional hardening, behaviorally equal on the honest path) accepted as-is. A11 remains ✅. | B1 — Runtime assembly |
| 2026-07-14 | B1 | Implemented `runtime/bleavit-runtime`: added the workspace runtime assembly crate with the standard/custom pallet composition list, chain identity constants, USDC `ForeignAssets` model, VIT/USDC fee configuration keyed to `fee.vit_usdc_rate`, `SafetyFilter` as `BaseCallFilter`, custom-origin/class wiring, D-13 genesis `frame-system` call filtering for bootstrap/sudo, and an S5-style filter-exhaustiveness sample covering domains plus utility/proxy/multisig/scheduler/sudo wrappers. Full Rust workspace gates, runtime no-default-features tests, runtime runtime-benchmarks feature tests, docs link lint, and a spec-compliance pass against 01 §5–§6, 06 §3, 09 §5, and 15 S5/I-8/I-10/I-11 found no blockers. B1 is now ✅. | B2 — `FutarchyApi` runtime API (all 11 methods + view types) |
| 2026-07-14 | B1 (PR feedback) | Addressed Codex review feedback on the B1 runtime wiring: `system.apply_authorized_upgrade(code)` is now modelled as the permissionless post-authorization application call instead of an `InternalRoot` call, while `system.authorize_upgrade(hash)` remains internal-root-only. Added a pending-upgrade-aware runtime filter helper that denies apply attempts with no pending upgrade or before `applicable_at`, plus a regression test for the authorize/apply split. Full workspace gates, runtime no-default-features tests, runtime runtime-benchmarks feature tests, and docs link lint passed. B1 remains ✅. | B2 — `FutarchyApi` runtime API (all 11 methods + view types) |
| 2026-07-14 | — (Track-A re-scope, user-directed; planning only, no code) | Established that the Rust backend is currently a **frame-free functional-core model** (no `frame-support`, `#[pallet::…]`, `construct_runtime!`, or `impl_runtime_apis!` anywhere in `pallets/`/`runtime/` — verified by grep) rather than the FRAME parachain 01 §1 mandates. Per user instruction, **reopened all of Track A (A1–A11 ✅→⬜)** and redefined it to require production Polkadot-SDK FRAME pallets. Documented the per-pallet definition of done in the Track A preamble (relocated frame-free core → `crates/<name>-core/`; `pallets/<name>/` FRAME shell `lib.rs` + `mock.rs` + `tests.rs` + `benchmarking.rs`/`weights.rs`, on the `polkadot-stable2603` pins). Architecture picked by the user via AskUserQuestion: **FRAME shell over frame-free core** (keeps the differential oracle + WASM/auditor port). Added milestone **B1a** earlier in the same session and re-narrowed it to runtime-level `construct_runtime!`/`impl_runtime_apis!` assembly (per-pallet wrapping moved into Track A); pointed B2–B6 at B1a. Updated Current focus, added a Decision-log row, and re-trued the AGENTS.md/README.md repo maps to the shell+core layout. No `docs/architecture/` change (01 §1 already mandates FRAME). No gates run — planning/living-doc edits only. | A1 — `pallet-constitution` as a FRAME shell over `constitution-core` (or the Track-A pallet the user names) |
| 2026-07-14 | A1–A11 (partial implementation) | User requested implementing all reopened Track-A milestones in one run. Relocated each existing frame-free pallet implementation into a `crates/<name>-core/` workspace member and converted every `pallets/<name>/` crate into a thin compiling shell that re-exports its core and owns `mock.rs`, `tests.rs`, `benchmarking.rs`, and `weights.rs` scaffolds. Updated inter-core dependencies to point at `*-core` crates and kept the full existing core test suites green. This is **not** the full Track-A DoD: the shells are not yet real `#[frame_support::pallet]` implementations with bounded storage, origin-checked dispatchables, try-state hooks, generated benchmarks/weights, or the complete Doc-15 FRAME mock-runtime suites, so A1–A11 remain 🔨 rather than ✅. Full workspace gates passed after the relocation/scaffold step. | Continue A1 by replacing the constitution shell scaffold with a production `#[frame_support::pallet]` wrapper over `constitution-core`; repeat per pallet until A1–A11 satisfy the Track-A DoD. |
| 2026-07-15 | A1 (FRAME pallet) | Implemented `pallets/constitution` as a production `#[frame_support::pallet]` over `constitution-core`. **Dependency wiring:** the `polkadot-sdk` 2603.0.0 umbrella proved unusable as a direct dep (its lockfile resolution drags the node stack in: `sc-service → litep2p → multihash → core2 0.4.0` which is yanked), so the workspace now pins the FRAME crates directly at the versions copied from the umbrella's own manifest (`frame-support`/`frame-system`/`frame-benchmarking` 46.0.0, `sp-runtime` 46.0.0, `sp-io` 45.0.0, `sp-core` 40.0.0 = the stable2603 train; umbrella line kept as the documented anchor). stable2603 requires `DecodeWithMemTracking` on call args — added the derive to `futarchy-primitives::{FixedU64, ProposalClass}` and `constitution-core::{ParamValue, Capability, CapabilityRecord}` (additive, SCALE-identical; remaining Track-A pallets will need the same on their call-arg types). **Pallet:** frozen 02 §7.3 storage (`Params` CountedStorageMap ≤64 genesis-fixed, `PhaseFlags` u32, `ReleaseChannel` 168-byte value whose raw key + no-length-prefix layout are test-pinned; runtime must instance as `Constitution`), `Meters`/`Capabilities` BoundedVecs, Config{GovernanceOrigin→`ConstitutionOrigin`, CurrentEpoch, WeightInfo, BenchmarkHelper(bench-only)}, new-style `frame_system::Config<RuntimeEvent: From<Event<Self>>>` supertrait (stable2603 deprecates `type RuntimeEvent`), events/errors 1:1 with the core, genesis (code-owned 13 §1 registry seeding; only phase-flags/release-channel chain-spec-configurable; genesis-time asserts), try-state rebuilding the core aggregate + D-14 key identity, `#[pallet::extra_constants]` incl. `INTEGRATION_CONTRACT_VERSION`, storage-info strict (no `without_storage_info`), v2 benchmarks + placeholder `WeightInfo` (B5). **Spec-reviewer pass (R-6) found 1 blocker + 6 majors + 5 minors; fixed in-session:** (blocker) META+values keys were values-dispatchable and not META-dispatchable — inverted per 06 §1/§2.2 (`(FutarchyMeta, MetaAndValues)`; values half = execute-time ratification; `epoch.horizon_k` seeded + matrix tests); (major) `set_capability` no longer admits ConstitutionalValues (06 §3.2 row 4); (major) `set_phase_flag` narrowed to Root-only (09 §5.4) with machinery bits via new internal `note_phase_flag`; (major) un-spec'd `charge_meter` extrinsic removed — runtime-internal `charge_meter_internal` only (06 §3.2 closed matrix), call enum test-pinned to exactly 4 spec-named calls; (minor) metadata constant renamed to canonical `INTEGRATION_CONTRACT_VERSION`; also `constitution-core::can_set_release_channel` fixed earlier in-session to ConstitutionalValues-only per 02 §12's exhaustive writer list. **Remaining majors are user-gated and A1 stays 🔨:** I-17 meter content (SQ-12), full 13 §1 registry seeding — 7 of ~50 keys (SQ-8/SQ-10), `amend_registry` (SQ-7); minors: Root arms (SQ-11), `key16` truncation (SQ-8), 13 §4 bound rows (SQ-9), I-6 SMT (Track S). Ten spec questions SQ-4…SQ-13 logged from the review. **Gates (all green):** fmt · clippy -D warnings · workspace tests 186/186 (pallet 32 + benchmarks-feature 36 incl. 4 executed benches; core 13) · try-runtime check · no_std check (native no_std+benchmarks is upstream-unsupported — needs the wasm `substrate_runtime` cfg, exercised at B1a) · corpus/reference-model/vector freshness · docs link lint. Genesis-time `assert!` in `genesis_build` is the one deliberate panic surface (FRAME chain-spec convention; no dispatch-path panics). **Codex note:** user asked for Codex subagents, but the Codex CLI was unauthenticated all session (`loggedIn:false`); review ran with the native spec-reviewer instead — run `codex login`, then `/codex:adversarial-review` on this diff as a follow-up. | Resolve SQ-4…SQ-13 with the user, then close A1's three re-scoped gaps (meters, registry seeding, amend_registry) and mark A1 ✅; then A2 (`pallet-conditional-ledger`) by the same shell-over-core pattern |
| 2026-07-15 | A1 (Codex adversarial review + fix round 2) | Codex CLI made operational for the session (three-layer failure unwound: CLI 0.45.0 → 0.144.4 upgrade for the plugin's app-server protocol; stale `model = "gpt-5.1-codex-max"` pin commented out of `~/.codex/config.toml`, backup kept). `/codex`-style adversarial review of the A1 diff returned "needs-attention" (5 high / 4 medium); triage: 4 highs corroborated the spec-reviewer's SQ-gated gaps, the rest were valid and fixed: **(1)** Root removed from `can_set_param`/`can_set_capability`/`can_charge_meter` — 09 §5.4's sudo scope is exhaustive and excludes parameter administration; matrix tests now pin Root refusal everywhere except phase-flag arming (SQ-11 stays open for deliberate widening). **(2)** `set_phase_flag` bit-scoped to `SUDO_ARMABLE_MASK` (bits 0–4, new `FlagNotArmable` error in core+shell); generic `note_phase_flag` replaced by one-bit internal setters (`note_ledger_frozen`/`note_dead_man_engaged`/`note_reserve_health`) so no internal caller can reach arming bits or another pallet's signal; full caller-auth story remains a B1a/S5 negative-suite obligation (Rust cannot authenticate calling pallets). **(3)** Registry seeded 7 → **32 keys**: every 13 §1 row that is scalar, concrete-valued, ≤16-byte-keyed and non-[VERIFY] (mkt.kappa/fee, dec.window/trailing/delta_max/coverage, gate.p_max/nb_coverage/nb_conv, exec.grace, trs.cap_proposal/30d/180d, code.spacing, orc.bond_floor/bond_bps/rounds/n_min, wt.quorum/stake, res.probe_amount, collator.n_min, att.bond, ledger.min_split, pol.b_gate) with kernel-constant floors where they exist and encodings pinned by a new spot-check test; remaining ~18 rows blocked on SQ-8/SQ-10/[VERIFY]. **(4)** `ReleaseChannel::new` now enforces 02 §12's "flags bits 3–31 reserved zero" (rejection + all-defined-bits vectors, extrinsic + core + genesis paths). **(5)** Workspace FRAME deps `=`-exact-pinned (frame-support =46.0.1, others =46.0.0/=45.0.0/=40.0.0; dead polkadot-sdk anchor entry removed, comment keeps the umbrella as version source) so `cargo update` cannot drift the SDK mix before B1a. **(6)** New 600-step seeded (xorshift, deterministic) randomized shell-vs-core differential across all five ops × nine authorities × shifting epochs with exact `Result` equality — it immediately caught a real core defect: a refused `Meter::charge` persisted its lazy window reset while FRAME's transactional storage rolled it back; core fixed to strict no-op-on-failure (G-1), differential green. Codex's `--all-features` test failure not reproducible (passes here; likely its sandbox). Placeholder weights confirmed B5-owned (Track-A DoD). Gates: fmt · clippy -D warnings · workspace 188/188 · benchmarks-feature 38 (5 benches execute) · --all-features · no_std check · corpus/reference-model/vectors · docs link lint — all green. | Resolve SQ-4…SQ-13, close A1's three user-gated gaps, mark A1 ✅; then A2 |
| 2026-07-15 | A1 (SQ resolutions + completion + ✅) | User delegated the open spec decisions ("Decide on your own how to fix the remaining issues"). Resolved SQ-4…SQ-13 as an R-1 amendment batch across 06/13/15 (02 deliberately untouched — no contract bump; see Decision log ×2): release-channel track/matrix rows, META+values `set_param` added to the rule-altering list, TREASURY-class `set_param` row, `PhaseFlags` writer map, canonical `ParamKey` encoding + per-class suffix convention + view projection, Percent→Perbill type fixes, 13 §4 constitution bound rows (Params 128/Caps 64/Meters 16), I-7/I-17 metering re-scoped to treasury/guard. Implemented: `constitution.amend_registry` (5th extrinsic; kernel-bounded rows refuse all amendment per 13 rule 2, non-kernel rows meta-bounded: min ≤ value ≤ max, kind-preserving, cooldown ≤ 8; origins FutarchyMeta \| ConstitutionalValues; bench + weights + origin/error/adversarial tests), `ParamRecord.kernel_bounded` field, strict `key16` (>16-byte names rejected; explicit short keys), the materialized **87-key** genesis registry (all scalar concrete non-formula 13 §1 rows; per-class suffixes; kernel flags per the new rule-7 enumeration), empty constitution `genesis_meters()`, randomized differential extended with an `amend_registry` arm. Final spec-reviewer pass (after fixes): **0 blockers**; its 1 major (kernel rows allowed Δ/cooldown amendment vs rule 2) and 6 minors (over-conservative kb flags → enumerated normatively; [VERIFY] seeding inconsistency; "—" cooldown drift; `(Code, SetReleaseChannel)` capability row vs 02 §12; stale comments; 06 gloss) all fixed in-session; its spec-questions A–G resolved inside the same delegation (conservative directions) — two residual notes left for future doc touches (05 §1.3 "constitutional meters" gloss; keep rule-7 enumeration in sync). Gates: fmt · clippy -D warnings · workspace tests green (constitution 38 + core 13; 43 with benchmarks incl. 6 executed benches) · --all-features · no_std · corpus/reference-model/vectors · docs link lint. **A1 ✅.** | Create the A1 PR (user-requested); then A2 — `pallet-conditional-ledger` |
| 2026-07-15 | — (Track M audit, report-only) | Ran `/spec-audit` over Track M (M0–M3): four parallel spec-reviewer agents (M0 vs 01 §9; M1 vs 02 §2/13 §2/01 §5.2; M2 vs 04 §4–§5; M3 vs 04 §5/15 §4.4/08/05 §4.4). Merged verdict **0 blocker / 13 major / 22 minor** (deduped; one corpus/differential major shared M2+M3) + 2 majors routed outside Track M — see Audit log. Load-bearing: `futarchy-fixed::exp2` violates 04 §4's normative ≤2 ulp bound (10.11 ulp worst, 36.9 % of 60k sampled fracs > 2 ulp, confirmed against the compiled crate; the 17-point committed corpus samples only 1–3-set-bit fracs, so the existing gate cannot detect it); the reference model is deep only for LMSR — its TWAP weights observations forward where market-core weights backward, `decide()` lacks 5 of 12 reject reasons and the Extend path, welfare pipeline / ledger op-set (≈7 of 22) / treasury worked arithmetic largely absent, sizing check rounds in the claimant-favoring direction; the 04 §5 three-way CI rule is unimplemented (futarchy-fixed certifies against the parallel Decimal-80 generator in `tools/fixed`, which already diverges from `vectors.json` on V3; `vectors.json` has zero Rust consumers); ≈half of 13's K-marked kernel constants are absent from `futarchy-primitives` with downstream cores hardcoding the values (`wt.quorum` floor exists as two independent copies); frozen `Proposal`/`ExecutionRecord` layouts live outside primitives; CI never compiles runtime-benchmarks/try-runtime code, installs a floating stable toolchain, and runs cargo unlocked. Cheap gates re-run green (futarchy-fixed 10 + primitives 6 tests, reference-model 8/8, both corpora fresh, doc links OK; the `--no-default-features` test run is vacuous — 0 tests execute). Report-only: no code changed, no statuses flipped (M2/M3 ✅ invalidation + M1 decision need user consent). **Mid-session the working tree was switched externally** from `feat/a1-constitution-frame-pallet` (1c0bb76, where the audit began and whose M0/M1/Cargo artifacts the reviewers cite) to new branch `fix/m3-reference-model-spec-compliance` (base 1db94f2, pre-A1) — audit records live on this branch; futarchy-fixed/reference-model/tools/CI files are identical in both states; SQ-14…SQ-28 numbered above the A1 branch's SQ-4…SQ-13 to avoid merge collisions (reconciled onto post-#44 main by the PR carrying these records). | User: decide M2/M3 (and M1) status flips + remediation order (this branch's name suggests M3 first); answer SQ-14/SQ-16/SQ-17/SQ-18 before the M2/M3 fixes — they determine the fix shapes |
| 2026-07-15 | — (SQ resolution batch + audit consequences, user-delegated) | Same session, on user instruction ("resolve the spec questions yourself", "don't wait for my consent"): resolved **SQ-14…SQ-28** as one R-1 batch across 00/01/04/05/09/13/14/15 (full inventory in the Decision log row this date) — highlights: 04 §4 ulp semantics + guarded-precision mandate + two-tier corpus cadence, 04 §5 corpus schema ownership + single-generator rule, 04 §7 backward TWAP weighting, 05 §1.2 frozen 20-field `Proposal<AccountId>` layout, 09 §1.2(3) `RuntimeVersionConstraint` layout, 15 §4.4 conforming-implementation duty + §4.5 supply-chain gates, 13 rule-1 primitives boundary / θ floors 0.90-0.85 / phase-offset representation, 01 §5.1 attestor row + §5.2 single-home rule + §9 pinning regime, SQ-23 queued as a 02 §9 erratum into the B2 amendment with SQ-2 (02 untouched, no bump). **Applied the audit consequences: M1, M2, M3 flipped ✅→🔨** with remediation deltas in their row notes; M0 kept ✅ with CI follow-ups noted; B8 row gains the release-side supply-chain + ≥10⁷-sweep obligations; Current focus rewritten (M3 remediation owned by this branch). All spec edits internally cross-referenced; docs link lint green; design kit checked for stale restatements (none — only a doc-11 VERBATIM copy mentions `RuntimeVersionConstraint`, semantics unchanged). No code changed **by this session**; while these records were being written, a concurrent external process began landing M3-remediation code in this branch's working tree (`reference-model/` + generator + tests, ~2.6k uncommitted lines appearing live) — whoever lands it must re-verify against the **amended** spec texts of this batch (esp. 04 §7 backward TWAP weighting, 05 §4.4 grid-exactness, 04 §5 single-generator/replayable-rows rules) and update M3's status per R-6. | Next session: `/implement M3` on this branch (fix TWAP direction/staleness, decide() truth table, grid-exact welfare, 22-op ledger, 08 arithmetic + claimant-adverse rounding, single-generator corpus with Rust consumers, prec-context fix, replayable V6); then M2, then M1; fold the M0 CI follow-ups into the first session that touches CI |
| 2026-07-15 | — (M3 fix-pass reconciliation + SQ-29/SQ-30, autonomous continuation) | The M3 "external process" identified: the audit's own M3 spec-reviewer subagent, user-redirected mid-session to fix its findings via Codex-authored patches on `fix/m3-reference-model-spec-compliance`. Verified its commit boundaries (code commit 4214662 stages zero `docs/architecture/` files; records commit bd05727 is PLAN-only; uncommitted tree = exactly the SQ-14…28 spec batch) and re-ran the fast gates on the combined state: model 21/21, both corpora fresh, new doc-table gate green, futarchy-fixed 12/12 incl. the two new `vectors.json` differential consumers, links green. Resolved its two new spec questions under the standing delegation: SQ-29 (08 §5.4 PARAM doubling slip → 113,863/170,794/56,931 family; model workaround dropped in fix-branch commit 41531dd) and SQ-30 (05 §4.6 percentile family = inclusive linear/type-7 on the 1e9 grid, round-down). Docs-PR branch `docs/track-m-audit-and-sq-resolutions` gained commit #2 with the 08/05 §4.6 edits + these records; the fix branch is now 3 commits. Both branches remain push-blocked — no GitHub credentials in this environment. | User: `! gh auth login` (or push manually), then both PRs go up; merge the docs PR first so the fix PR sits on the amended spec |
| 2026-07-15 | M3 (remediation implementation) | The M3 audit's own fix pass (read-only Codex authors, reviewer applied + gated) landed the reference-model remediation: backward/staleness TWAP (04 §7); full `RejectReason` surface + strict `decide()` order + Extend (05 §5.4/§5.5); complete ledger families + B-5 vector + VOID/conservation (03 §5/§6); grid-exact welfare + cold start + full-pipeline vectors (05 §4.4–§4.6); treasury worked arithmetic + claimant-adverse sizing (08, 05 §5.6); single-generator `bleavit.reference-model.v3` fixtures (`tools/fixed` derives from the model, replayable V6, all values model-computed, honest precision metadata); `futarchy-fixed` now consumes `vectors.json` (V1–V6 + 8-sample corpus); `check-doc-table.py` + CI/local-gate wiring; AGENTS.md gate row corrected (unittest, not pytest). SQ-29's PARAM-depth slip and SQ-30's percentile family surfaced here and were resolved in the docs PR (spec side) + reconciled in the model. Gates green: model 21/21, both corpora fresh, doc-table, links; `futarchy-fixed` 12/12 incl. the new differential consumers. **Delivered as a stacked PR on `docs/track-m-audit-and-sq-resolutions`** so the model's 56,931 `P_ref` matches the amended 08 §5.4 (a same-branch merge to main would have failed `check-doc-table.py`). M3 stays 🔨: ✅ gated on M2's guarded-precision exp2 + a fresh spec-reviewer pass (R-6). | M2 remediation (guarded-precision exp2/log2 + per-commit ≥10³ dense-bit corpus gate, 04 §4–§5), then a spec-reviewer pass to close M3 |
| 2026-07-15 | — (#46 Codex review triage, autonomous) | Reviewed the three `chatgpt-codex-connector` findings on PR #46 (all verified real). **Fixed inline (spec-internal, no 02 bump):** P1 — the frozen 05 §1.2 `Proposal` layout was missing `payload_len` although 09 §1.1 lists it as a queued field, 09 §1.2(2) requires the (hash, len) match, and 05 §5.6's `decide()` reads `p.payload_len`; added `payload_len: u32` after `payload_hash` (safe — the struct is not yet shipped in `futarchy-primitives`; separately, epoch-core's `Proposal` also lacks the field → A8 code follow-up). **Reconciled wording + deferred the 02 edits to the B2 amendment (avoids a mid-batch contract bump):** P2a — 13 §3.1 no longer claims 02 §9 is already consistent; it notes 02 §9's combined row still lists the phase-offset fractions under `params()` and that the split is queued for B2. P2b — 01 §5.1 and 06 §7 softened from "storage listed in 02" (a dangling ref — 02 has only `AttestationMissing`) to "to be frozen in 02 §7 at B2". The B2-pending list (SQ-2 row) now enumerates all three 02 items. Docs-fix committed to the `docs/track-m-audit-and-sq-resolutions` branch → updates #46. Link lint + doc-table + reference-model gates green. | Merge #46 (carries spec + M3 fix); then M2 remediation; the three 02 items land as the B2 contract amendment (one bump, joint FE+BE sign-off) |
