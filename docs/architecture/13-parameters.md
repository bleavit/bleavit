# 13 — Parameters, Bounds and Constants (single reconciled table)

**Status: normative component specification. Supersedes the corresponding sections of BACKEND_PLAN.md/FRONTEND_PLAN.md** (BE §21 in full; every bound, constant or default restated anywhere else in either source).

**Boundary.** This document is the **single source of truth for every tunable parameter, kernel constant, storage bound and frozen protocol constant**. Other component documents reference these values and MUST NOT restate different ones (quoting for readability is permitted when marked *(normative value: §13)*). Chain-identity constants and the integration surface are jointly owned with [02](02-integration-contract.md) (D-17); where both documents state a value they are byte-identical by construction of the D-2 freeze.

Normative language: RFC 2119. Blocks at 6 s (14,400/day); USDC 6 decimals, VIT 12 decimals; `Fixed` = `FixedU64` (1e9 scale) at API boundaries.

**Reading rules (X-11e/X-11h — no hardcoding, anywhere):**

1. **K** = kernel constant: compile-time in `futarchy-primitives`, no storage representation, changeable only by a Wasm upgrade that the attestation regime surfaces ([09](09-execution-upgrades-and-rollout.md)). **Boundary of the primitives home:** every K-marked numeric value in this document — the LMSR domain bound and error-bound constants included, which `futarchy-fixed` imports and asserts rather than re-declares — lives in `futarchy-primitives::kernel`; sibling crates MUST import it, never re-declare it. Two carve-outs: per-pallet **storage-bound arguments** (§4) live with their owning pallets ([02 §7](02-integration-contract.md)), and pallet-internal implementation constants with no row in this document need no primitives home. Contract-surface **types** — every type named in 02 §§2–7, event-field enums such as `MarketKind` included — likewise live in `futarchy-primitives` (02 §2).
2. Everything not marked K lives in `pallet-constitution::Params` as a typed record `{ value, min, max, max_delta_per_decision, cooldown, last_change, class, kernel_bounded }`; min/max/max-delta/cooldown/class are genesis-fixed for kernel-bounded keys and META-amendable within compile-time meta-bounds otherwise.
3. **Every value in this document — K constants included — is machine-readable by clients** via the runtime **constants API** (metadata) or the named storage item, per the [02](02-integration-contract.md) contract. The frontend MUST read, never hardcode (this closes X-11h's backend side: there is no value the FE re-checks that the chain does not expose).
4. Every default is a **simulation hypothesis** unless marked frozen; Phase 0–3 calibration obligations are tagged *sim-gated*.
5. `ProposalClass::Emergency` is deleted (D-7): no row in this document carries an EMERGENCY value, and none may be added without reopening D-7.
6. **`ParamKey` encoding (canonical).** The on-chain `ParamKey` ([02](02-integration-contract.md) §2, `[u8; 16]`) is the row's dotted name, UTF-8-encoded and zero-padded to 16 bytes. Names longer than 16 bytes carry an explicit canonical key, written `key:` in their row — truncation is forbidden (the encoder rejects over-long names). **Per-class rows** (`dec.delta`, `dec.sigma`, `dec.v_min`, `gate.v_min`, `prop.bond`, `pol.b`, `exec.timelock` → base `exec.lock`, `trs.proposer_reward` → base `trs.reward`) materialize as four keys with the class suffixes `.param` / `.trs` / `.code` / `.meta`. `gate.v_min` is per-class because its default and both its bounds are expressed as multiples of `dec.v_min`(class) — a single key could not carry a class-derived value (added 2026-07-20, SQ-194).
7. **Record classes and the view projection.** `ParamRecord.class` uses the six-class set {PARAM, TREASURY, META, CONST, entrenched, META+values}; `ParamView.class: ProposalClass` ([02](02-integration-contract.md) §4) projects CONST and entrenched onto `Constitutional` and META+values onto `Meta`. Rows whose min or max is a kernel floor/ceiling are marked `kernel-bounded`: per rule 2 their **entire** governance-metadata tuple (min/max/max-Δ/cooldown/class) is genesis-fixed and `amend_registry` refuses them outright; all other rows are META-amendable within the compile-time meta-bounds (the amendment must keep `min ≤ value ≤ max`, preserve the value kind, and keep `cooldown ≤ 8` epochs). The kernel-bounded set is, normatively and exhaustively (per-row K/floor/ceiling markers plus the §1 safety rationale; conservative where a bound is cross-key-derived or marked "never lowered"/"down only"): `att.window`, `code.spacing`, `dec.delta.code`, `dec.delta.meta`, `dec.delta.param`, `dec.delta.trs`, `dec.sigma.code`, `dec.sigma.meta`, `dec.sigma.param`, `dec.sigma.trs`, `dec.window`, `epoch.horizon_k`, `epoch.length`, `exec.grace`, `exec.lock.code`, `exec.lock.meta`, `exec.lock.param`, `exec.lock.trs`, `gate.eps`, `gate.p_max`, `intake.slash_pct`, `iss.inflation`, `keeper.budget`, `ledger.archive`, `ledger.min_split`, `ledger.pos_dep`, `mkt.kappa`, `orc.bond_bps`, `orc.n_min`, `orc.window`, `pol.budget_epoch`, `res.fail_thr`, `res.probe_amount`, `res.probe_int`, `res.probe_to`, `res.recover_thr`, `sec.prize.code`, `sec.prize.meta`, `sec.prize.param`, `trs.cap_180d`, `trs.cap_30d`, `trs.cap_proposal`, `welfare.thC_lo`, `welfare.thS_lo`, `wt.quorum`. Cross-key couplings that a static record cannot express (`dec.sigma ≤ δ/2`, `gate.eps ≤ p_max/2`, `welfare.wP + wA = 1`, the classless Baseline book grading at the `dec.v_min.trs` floor — [05](05-welfare-and-decision-engine.md) §5.2, SQ-232) bind at the consuming engine; the seeded static bounds are conservative over-approximations. **The `gate.v_min` coupling is the exception and binds earlier** (corrected 2026-07-20, SQ-194): `0.1 × dec.v_min(class)` is its *default*, not an invariant — the row admits the band `[×0.05, ×0.5]` — and that band, evaluated against the **live** `dec.v_min(class)`, is enforced **at the amendment boundary** jointly over the pair, so an amendment of either `dec.v_min.<class>` or `gate.v_min.<class>` that would carry the pair outside it is refused rather than left for a consumer to reconcile. The same relation is asserted in `try-state`. Two constraints therefore bind a `gate.v_min` amendment and the admissible set is their **intersection**: this live coupling band, which is the standing invariant, and the record's own `min`/`max`, which are ordinary META-amendable metadata seeded at genesis from the same multiples of the *then-current* `dec.v_min`. The seeded bounds coincide with the band at genesis but do not track later `dec.v_min` amendments, so the `×0.05` / `×0.5` columns of the §1 row state the coupling invariant, not a promise about the stored metadata. **`ledger.redeem_fee` ≤ `mkt.fee` is the second such coupling** (added 2026-07-29, milestone E1) and takes the identical shape: the relation is the standing invariant, it is enforced **jointly over the pair at the amendment boundary** — so an amendment of *either* key that would carry the pair outside it is refused rather than left for the ledger to reconcile — and it is asserted in `try-state`. It binds earlier than the consuming engine for the same reason `gate.v_min` does: the consumer is a payout deduction in audit-scope-A code that has no admissible way to fail closed on a bad rate without stranding a claimant, so the only safe place to refuse is before the value is stored. The row's own static `[0, 100]` bounds are ordinary META-amendable metadata; the live coupling is strictly tighter whenever `mkt.fee < 100` and is what actually binds. Derivation of the relation: [08](08-treasury-and-economics.md) §10.6.

**One asymmetry against the `gate.v_min` precedent is deliberate and is recorded rather than smoothed over.** `gate.v_min` and `dec.v_min` are both META; `ledger.redeem_fee` and `mkt.fee` are both **PARAM**, so this pair is jointly screened at a *lower* governance class than the pair it is modelled on. That is correct for what these two keys are — fee rates are the responsiveness case PARAM exists for, and both are ordinary-class in [08](08-treasury-and-economics.md) §10 — but it has a consequence a reader should not have to derive: a PARAM decision can move **either** side of the relation, so the screen MUST refuse a *lowering* of `mkt.fee` that would carry the pair out of band exactly as it refuses a *raising* of `ledger.redeem_fee`. Screening only the second key would leave the invariant breakable from the first, which is precisely the "left for a consumer to reconcile" failure rule 7 exists to prevent. Both directions are in scope, and the `try-state` assertion is the backstop for both.
8. **The `Unit` column is human/display notation, not `ParamView` encoding.** The §1 table's Default/Hard min/Hard max/Max Δ figures are written in the human unit printed in that column. By contrast, [02 §4](02-integration-contract.md) `ParamView.value/min/max` carry the raw inner scalar of the stored `ParamValue`: `Fixed` on the 1e9 grid, `Perbill` in parts per billion, `Percent` as integer percent, and integer/balance variants in their native scalar. In particular, converting raw `Perbill` to displayed basis points divides by **100,000**, floored; market-fee recomputation uses the frozen `Market::Fee` bps metadata constant and cross-checks it against that raw projection.

---

## 1. Constitution keys (typed, bounded, rate-limited)

Per-class value lists are ordered **PARAM / TREASURY / CODE / META** unless stated. "vf" = values floor applies.

Scope of the existing gate parameters is **every market-bearing class: PARAM, TREASURY, CODE, and META**. The `gate.*` rows and `pol.b_gate` are reused unchanged for all four classes; no new parameter or value is introduced by universal gating.

| Key | Type | Unit | Default | Hard min | Hard max | Max Δ/decision | Cooldown | Class | Doc |
|---|---|---|---|---|---|---|---|---|---|
| `epoch.length` | u32 | blocks | 302,400 (21 d) | 201,600 (14 d floor) | 604,800 | 10% | 2 epochs | META | [05](05-welfare-and-decision-engine.md) |
| `epoch.slots` (N_active) | u8 | — | 5 | 1 | 12 | 2 | 1 | META | [05](05-welfare-and-decision-engine.md) (§3 cross-check) |
| `epoch.horizon_k` | u8 | epochs | 2 | 1 | 2 (K ceiling = `MAX_NON_TERMINAL_COHORTS − 2`) | 1 | 4 | META+values | [05](05-welfare-and-decision-engine.md) §3.3 |
| `mkt.obs_interval` | u32 | blocks | 10 | 5 | 50 | 5 | 1 | PARAM | [04](04-markets-and-pricing.md) (§3 cross-check) |
| `mkt.kappa` κ | Fixed | /interval | 0.005 | 0.001 | 0.02 | 0.002 | 2 | META | [04](04-markets-and-pricing.md) |
| `mkt.fee` | Perbill | bps | 30 | 5 | 100 | 10 | 1 | PARAM | [04](04-markets-and-pricing.md) |
| `dec.window` | u32 | blocks | 43,200 (72 h) | 14,400 | 86,400 | 20% | 2 | META (vf) | [05](05-welfare-and-decision-engine.md) |
| `dec.trailing` | u32 | blocks | 14,400 (24 h) | 3,600 | 28,800 | — | 2 | META | [05](05-welfare-and-decision-engine.md) |
| `dec.delta` δ per class | Fixed | s-units | 0.0375 / 0.0375 / 0.060 / 0.090 — **Phase-0-calibrated (V-12; ×1.5 the pre-calibration 0.015/0.025/0.040/0.060 for TREASURY/CODE/META, ×2.5 for PARAM — its small floor + unbacked capability-envelope prizes make marginal flips cheap) floors of the Ask-scaled schedule, [08](08-treasury-and-economics.md) §5.3; [15](15-invariants-and-testing.md) §4.9** | 0.005 | 0.10 (K cap incl. scaling) | 0.005 | 2 | META (vf) | [05](05-welfare-and-decision-engine.md), [08](08-treasury-and-economics.md) |
| `dec.sigma` σ per class | Fixed | s-units | 0.003 / 0.005 / 0.008 / 0.010 | 0 | δ/2 | — | 2 | META | [05](05-welfare-and-decision-engine.md) |
| `dec.delta_max` (convergence) | Fixed | — | 0.05 | 0.02 | 0.10 | — | 2 | META | [05](05-welfare-and-decision-engine.md) |
| `dec.coverage` | Percent | % | 95 | 90 | 99 | — | 2 | META (vf) | [05](05-welfare-and-decision-engine.md) |
| `dec.v_min` per class | Balance | USDC | 100k / 250k / 600k / 1.2M — **floors; effective value = max(floor, 2·InCapPrize), [08](08-treasury-and-economics.md) §5.3** | ×0.1 | ×10 (floors only; the 2·P term is K) | ×2 | 2 | META (vf) | [05](05-welfare-and-decision-engine.md), [08](08-treasury-and-economics.md) |
| `gate.p_max` (S, C) | Fixed | prob | 0.05 | — | **0.10 K ceiling** | 0.01 | 4 | META+values | [05](05-welfare-and-decision-engine.md) |
| `gate.v_min` (gate-book contest floor, per book) | Balance | USDC | **0.1 × `dec.v_min`(class)** | ×0.05 | ×0.5 | ×2 | 2 | META | [05](05-welfare-and-decision-engine.md) §5.2 |
| `gate.nb_coverage` (GB-NB near-boundary coverage) | Percent | % | 98 | 95 | 100 | — | 2 | META | [05](05-welfare-and-decision-engine.md) §5.2 |
| `gate.nb_conv` (GB-NB spot-vs-TWAP bound) | Fixed | — | 0.01 | 0.005 | 0.02 | — | 2 | META | [05](05-welfare-and-decision-engine.md) §5.2 |
| `gate.eps` ε | Fixed | prob | 0.02 | 0.005 | p_max/2 | — | 2 | META | [05](05-welfare-and-decision-engine.md) |
| `welfare.thetaS` lo/hi (keys: `welfare.thS_lo` / `welfare.thS_hi`) | Fixed | — | 0.90 / 0.98 | **lo floor K-entrenched** | — | 0.01 | 4 | CONST (loosen: entrenched) | [05](05-welfare-and-decision-engine.md) |
| `welfare.thetaC` lo/hi (keys: `welfare.thC_lo` / `welfare.thC_hi`) | Fixed | — | 0.85 / 0.95 | lo floor K | — | 0.01 | 4 | CONST | [05](05-welfare-and-decision-engine.md) |
| `welfare.wP/wA` (keys: `welfare.wP` / `welfare.wA`) | Fixed | — | 0.60 / 0.40 | 0.30 | 0.70 | 0.05 | 4 | CONST | [05](05-welfare-and-decision-engine.md) |
| `prop.bond` per class | Balance | USDC | 1k / 5k + 0.5%·Ask / 25k / 50k — the TREASURY **`+ 0.5%·Ask` surcharge is a kernel constant** `kernel::TREASURY_BOND_ASK_BPS` (= 50 bps; [06](06-governance-and-guardians.md) §4), **not** part of this `Params` row; the Default/Hard-min/Hard-max/Max-Δ columns govern the class **base only** (SQ-186) | ×0.1 | ×10 | ×2 | 2 | META | [06](06-governance-and-guardians.md) |
| `intake.max_per_account` (key: `intake.max_acct`) | u8 | entries/epoch | **4** (frozen, Part 3) | 2 | 8 | 2 | 2 | META | [06](06-governance-and-guardians.md), [08](08-treasury-and-economics.md) §7 |
| `intake.slash_fraction` (key: `intake.slash_pct`; slashed to INSURANCE, never burned) | Percent | % of bond | **10** (frozen, Part 3) | 5 (K floor) | 25 | 5 pp | 2 | META | [06](06-governance-and-guardians.md), [08](08-treasury-and-economics.md) §7 |
| `pol.b` decision, per branch, per class | Balance | USDC | 10k / 25k / 60k / 100k — **floors of the Ask-scaled schedule** | budget-capped | — | 25% | 1 | TREASURY | [04](04-markets-and-pricing.md), [08](08-treasury-and-economics.md) |
| `pol.b_gate` | Balance | USDC | 7,500 | — | — | 25% | 1 | TREASURY | [04](04-markets-and-pricing.md) |
| `pol.b_baseline` | Balance | USDC | **25,000** *(sim-gated — [VERIFY via Phase-0/3 calibration])*; funded from `POL_BASELINE`, **outside** `pol.budget_epoch` | 10,000 | 100,000 | 25% | 1 | TREASURY | [04](04-markets-and-pricing.md), [08](08-treasury-and-economics.md) §4.3 |
| `pol.budget_epoch` | Perbill | NAV | 0.75% | — | **1.5% K** | — | 2 | META | [08](08-treasury-and-economics.md) |
| `exec.timelock` per class | u32 | blocks | 2 d / 3 d / 7 d / 14 d | **24 h K floor** | 30 d | ×2 | 2 | META | [09](09-execution-upgrades-and-rollout.md) |
| `exec.grace` | u32 | blocks | 14 d | **7 d K floor** | 30 d | — | 2 | META | [09](09-execution-upgrades-and-rollout.md) |
| `trs.cap_proposal` | Percent | NAV | 5% | — | **10% K** | 1 pp | 2 | META | [08](08-treasury-and-economics.md) |
| `trs.cap_30d` / `trs.cap_180d` | Percent | NAV | 10% / 30% | — | 15% / 40% K | — | 2 | META | [08](08-treasury-and-economics.md) |
| `trs.stream_threshold` (key: `trs.stream_thr`) | Perbill | NAV | 1% | 0.5% | 5% | — | 2 | META | [08](08-treasury-and-economics.md) |
| `trs.proposer_reward` | Balance | USDC | PARAM 500; TREASURY/CODE min(0.05%·Ask, 25k); META 25k | ×0.1 | ×10 | ×2 | 2 | META | [08](08-treasury-and-economics.md) |
| `iss.inflation_cap` (key: `iss.inflation`) | Percent | /yr | 2% | — | amendable **down only** (K) | — | — | CONST | [08](08-treasury-and-economics.md) §2.3 |
| `fee.vit_usdc_rate` (key: `fee.vit_usdc`) | Fixed | USDC/VIT | 1.0 × `fee.vit_usdc_rate_ref` (ref is K, set at genesis from launch price — **[VERIFY at TGE; placeholder ref 0.05 USDC/VIT]**) | **0.1 × ref (K)** | **10 × ref (K)** | ×2 | 1 | PARAM | [08](08-treasury-and-economics.md) §9 |
| `code.spacing` | u32 | blocks | 30 d | **14 d K floor** | — | — | 2 | META | [09](09-execution-upgrades-and-rollout.md) |
| `orc.bond_floor` (round-1 floor) | Balance | USDC | 10k | 2.5k | 100k | — | 2 | META | [07](07-oracle-and-disputes.md) §6 |
| `orc.bond_bps` (value scaling: `B_1 = max(orc.bond_floor, ceil(orc.bond_bps × StakeAtRisk / 10,000))`; `B_r = B_1·2^(r−1)`; both `B_1` and `R_max` freeze per game at round-1 creation — [07](07-oracle-and-disputes.md) §6.1. **Two consumers since SQ-296:** the same rate, multiplied by the §6.3 ladder factor `(2^orc.rounds − 1)`, also prices the registry's one-round value-scaled filing bond `F(kind, m)` (§7, floored on `reg.bond_*` instead of `orc.bond_floor`), likewise frozen at creation, so an amendment here moves registry filing cost as well as oracle round cost — in the safe direction, since a raise makes false claims dearer on both surfaces. **Screened at the amendment boundary since SQ-495:** together with `orc.rounds` this key determines §6.3's coverage rate, and a lowering that would carry it below the `Δs_max` of any already-admitted attested component is refused — the static bounds below cannot express that, because the binding constraint is the live MetricSpec set) | Perbill | bps | **250** | **150** (hard min — keeps the §6.3 coverage rule ≥ 10.5% at `R_max = 3`) | 1,000 | ×2 | 2 | META | [07](07-oracle-and-disputes.md) §6, [07](07-oracle-and-disputes.md) §7 |
| `orc.rounds` R_max (**screened at the amendment boundary since SQ-495**: with `orc.bond_bps` it determines §6.3's coverage rate `(2^R_max − 1) · orc.bond_bps`, and a lowering that would carry it below the `Δs_max` of any already-admitted attested component is refused. Note this row carries **no max-Δ**, so 3 → 2 is a single lawful step and the screen is the only thing standing between it and an under-collateralized live component) | u8 | — | 3 | 2 | 4 | — | 2 | META | [07](07-oracle-and-disputes.md) §6.1, §6.3 |
| `orc.window` (challenge) | u32 | blocks | **43,200 (72 h — frozen, D-18)** | 43,200 (72 h kernel floor — never lowered) | 72,000 (120 h) | — | 2 | META | [07](07-oracle-and-disputes.md) §5 |
| `orc.reporter_stake` (key: `orc.rep_stake`) | Balance | USDC | 100k | 25k | 500k | ×2 | 2 | META | [07](07-oracle-and-disputes.md) |
| `dis.merit_min` (ProcessHold dispute-bond threshold) | Balance | USDC | **`orc.bond_floor` (10k)** — the floor of the value-scaled `B_1(c, m)` ([07](07-oracle-and-disputes.md) §6.1); genesis-seeded as a scalar (SQ-158), and the consumer binds the **effective** floor `max(live dis.merit_min, frozen B_1)` so value-scaling above the floor still holds and a META lowering can never drop below the game's own round-1 bond (R-7) | floor: `orc.bond_floor` | — | ×2 | 2 | META | [07](07-oracle-and-disputes.md) §12 |
| `wt.quorum` (watchtower ack) | u8 | of N registered | 2 | 2 (kernel floor) | 5 | 1 | 2 | META | [07](07-oracle-and-disputes.md) §4 |
| `wt.stake` (watchtower bond) | Balance | USDC | 25,000 | 10k | 100k | ×2 | 2 | META | [07](07-oracle-and-disputes.md) §4 |
| `orc.n_min` (reporters before attested admission / Phase-3 arming) | u8 | — | 3 | 3 (K floor) | 16 | 1 | 2 | META | [07](07-oracle-and-disputes.md) §3, [05](05-welfare-and-decision-engine.md) §4.3.1 |
| `reg.bond_incident` / `reg.bond_milestone` (keys: `reg.bond_inc` / `reg.bond_mile`; the **floor** of the value-scaled filing bond `F(kind, m) = max(reg.bond_{kind}, ceil((2^orc.rounds − 1) · orc.bond_bps × Exposure(kind, m) / 10,000))` — the §6.3 *coverage* rate, since a one-round game must post the whole ladder up front — since SQ-296 these price a filing only when the exposure term does not, and the rate is the existing `orc.bond_bps`, not a key of their own) | Balance | USDC | 5,000 / 2,500 | ×0.5 | ×10 | ×2 | 2 | META | [07](07-oracle-and-disputes.md) §7 |
| `res.probe_interval` / `res.probe_timeout` (keys: `res.probe_int` / `res.probe_to`) | u32 | blocks | 14,400 / 600 | **1 / 1 block (K structural floor)** | — | — | 1 | PARAM | [07](07-oracle-and-disputes.md) §8 |
| `res.probe_amount` | Balance | USDC | 0.10 (10 USDC-cents) | **1 µUSDC (K structural floor)** | — | — | 1 | PARAM | [07](07-oracle-and-disputes.md) §8 |
| `res.fail_threshold` / `res.recover_threshold` (keys: `res.fail_thr` / `res.recover_thr`) | u8 | consecutive probes | 2 / 3 | **1 / 1 probe (K structural floor)** | — | — | 2 | META | [07](07-oracle-and-disputes.md) §8 |
| `grd.bond` / allowances | — | — | 50k VIT / [06](06-governance-and-guardians.md) table | — | scope K | — | — | entrenched | [06](06-governance-and-guardians.md) |
| `keeper.rebate` | Balance | USDC | **0.000255** (= 3× fee cost per sanctioned crank). **Was 0.09 until 2026-07-30 (milestone E5, SQ-531), from an assumed 0.03 USDC fee basis that was never measured and is wrong by 353×.** Now genesis-seeded from the measured basis **85 µUSDC** — derived in 08 §6.2 from the committed generated weights (`crank_observe` 1,240,920,000 ps call + 352,392,000 ps extension + 108,157,000 ps base ⇒ 0.00170146912 VIT) at 08 §9's documented 0.05 USDC/VIT placeholder, floored to µUSDC against the claimant (R-7). The measurement was impossible before SQ-528: `FeeMultiplierUpdate = ()` pinned the fee multiplier to **zero** from block 2, so weight was not priced at all. **The `[VERIFY]` tag is load-bearing, and the claim that 3× holds at any price was wrong** (corrected 2026-07-31, raised independently by review): the fee is fixed in **VIT** by weight while this row is a **stored USDC** value, so only one side moves when VIT reprices — at 4× the derivation price the rebate is 0.75× the fee and cranking becomes loss-making (the silent A-1 failure 08 §6.2 describes). **This row MUST be re-derived at the launch `fee.vit_usdc_rate`** and PARAM-amended to track material later moves; the [1×, 10×] envelope bounds the exposure to roughly a 10× price move, beyond which the kernel basis itself must change (CODE). Screening the coupling at the amendment boundary, the shape rule 7 uses for `ledger.redeem_fee` ≤ `mkt.fee`, is **SQ-534**. What the correction did fix is that the derivation is anchored to a measured weight rather than an invented fee, so re-deriving at any price now gives the right answer; before, no price did — for 0.03 to have been right VIT would trade at ≈ 17.6 USDC (SQ-117, SQ-531) | 1× (0.000085) | 10× (0.00085) | — | 1 | PARAM | [08](08-treasury-and-economics.md) §6 |
| `keeper.budget_epoch` (key: `keeper.budget`) | Balance | USDC | **12,000** (raised per keeper-medium; derivation [08](08-treasury-and-economics.md) §6.2) | 6,000 (floor covers decision-critical load — see §3 note) | 60,000 | ×2 | 1 | PARAM | [08](08-treasury-and-economics.md) |
| `collator.comp_epoch` (key: `collator.comp`) | Balance | USDC/collator | **2,000** (frozen default, D-15) | 500 | 10,000 | ×2 | 1 | PARAM | [08](08-treasury-and-economics.md) §2.4 |
| `collator.n_min` (K component of `C_onchain`) | u8 | — | 4 | 3 | 12 | 1 | 2 | META | [05](05-welfare-and-decision-engine.md) §4.3 |
| `collator.bond_req_vit` (key: `collator.bond`; E-coverage requirement, per collator) | Balance | VIT | **[VERIFY — set with the collator program before Phase-3 arming; sim-gated]** | — | — | ×2 | 2 | META | [05](05-welfare-and-decision-engine.md) §4.3.1 |
| `collator.n_target` (key: `collator.n_tgt`; E-coverage denominator) | u8 | — | 5 at launch, phase-scheduled upward **[VERIFY schedule at phase gates]** | 4 | 12 | 1 | 2 | META | [05](05-welfare-and-decision-engine.md) §4.3.1 |
| `sec.prize.param` / `sec.prize.code` / `sec.prize.meta` (InCapPrize capability-envelope proxies; an undefined proxy ⇒ the proposal MUST NOT pass sizing) | Balance | USDC | **50,000 / 300,000 / 600,000** *(**sim-gated — adopted 2026-07-24 from the Phase-0 calibration**; the values stay bound to `simulation/results/phase0-calibration.json` · `published.candidates`, `designation: published`, `violations: []`, so a change away from that artifact must re-run the 09 §7.1 Phase-0 gate; SQ-173)* (CODE/META effective prize additionally floored at `trs.cap_proposal`·NAV for upgrade payloads, [08](08-treasury-and-economics.md) §5.2) | = default (K — [05](05-welfare-and-decision-engine.md) §5.6 names these kernel floors. The guarantee is the **floor itself**, not monotonicity: the ×2 Δ is symmetric, so a proxy raised above the calibrated value may later be lowered back toward it, but no amendment can carry it **below** the floor — which is what would admit an unsecured payload at step 9. An undefined or zero proxy is not a prize of zero: it fails sizing) | none (K) — the row is deliberately open above its floor: a higher prize proxy only makes step 9 **harder** to pass, so no ceiling is needed to bound risk, and the ×2 Δ plus 2-epoch cooldown rate-limit any raise. A governance body that over-raises a class's proxy stalls that class's own proposals and nothing else | ×2 | 2 | META | [05](05-welfare-and-decision-engine.md) §5.6, [08](08-treasury-and-economics.md) §5 |
| `sec.flow_cap` (ceiling on the measured non-POL depth term, × of `(b_acc + b_rej)` — gate-bearing since the SQ-231 amendment: bounds the contest-capital term inside step 9's `L̂` **and** `C_hold` of the `ManipFloor̂` diagnostic) | Fixed | × of `(b_acc + b_rej)` | **16** *(**sim-gated — adopted 2026-07-25 from the Phase-0 calibration**; `simulation/results/phase0-calibration.json` · `publication_evidence.flow_cap`: the 0.995 quantile of min-book 04 §7a contest capital over decision-pair depth `2b`, 15.637 over 4,303 samples, rounded up to 16 — and the 15 §4.9 security criterion was met **at this calibration**, `violations: []`, so a change away from that artifact must re-run the 09 §7.1 Phase-0 gate; SQ-486)* | 7 (K — the [08](08-treasury-and-economics.md) §5.3 identity: below ×7 the ceiling could reject honest exactly-grade proposals) | 32 (one governed Δ ×2 step above the calibration; the **unsafe direction is upward** — a higher ceiling counts more contest capital into step 9's `L̂`, easing the gate — and beyond ×32 the ceiling stops binding on observed flow at all, making the `C_hold` wash bound vacuous) | ×2 | 2 | META | [05](05-welfare-and-decision-engine.md) §5.6, [08](08-treasury-and-economics.md) §5.2–§5.3, [14](14-threat-model.md) |
| `ops.*` budget lines (`ops.bootnodes`, `ops.rpc_archive`, `ops.collators`, `ops.keepers`, `ops.oracle_evidence` (key: `ops.oracle_ev`), `ops.monitoring`, `ops.arweave`, `ops.watchtowers`, `ops.coretime`, `ops.reserve_probe` (key: `ops.reserve_prb`)) | Balance | USDC/epoch | **[VERIFY — sized in Phase-2/3 ops planning; ops-gated]** (`ops.collators` = `collator.comp_epoch` × collator count; `ops.keepers` per [08](08-treasury-and-economics.md) §6.3; `ops.reserve_probe` funds the bounded [07](07-oracle-and-disputes.md) §8 maintenance envelope and is release-required before that probe is armed) | — | — | — | 1 | TREASURY | [08](08-treasury-and-economics.md) §1.1, [12](12-release-and-operations.md) §6.1 |
| `ops.coretime_dot_rate` (key: `ops.ct_dot_rate`; DOT→USDC conversion for the `ops.coretime` line debit — budget-envelope accounting only, no NAV term depends on it, [09](09-execution-upgrades-and-rollout.md) §4) | Balance | µUSDC/DOT | 5,000,000 (5 USDC/DOT) **[VERIFY against live DOT/USDC before Phase-3 arming; ops-gated]** | 500,000 (0.5) | 500,000,000 (500) | ×2 | 1 | TREASURY | [09](09-execution-upgrades-and-rollout.md) §4, [08](08-treasury-and-economics.md) §1.1 |
| `ops.coretime_fee_dot` (key: `ops.ct_fee_dot`; DOT fee budget withdrawn beside each renewal quote for the two remote XCM legs) | Balance | planck (DOT) | 5,000,000,000 (0.5 DOT) **[VERIFY against live relay/Coretime fees at Phase-2/3 onboarding; ops-gated]** | 100,000,000 (0.01 DOT) | 100,000,000,000 (10 DOT) | ×2 | 1 | TREASURY | [09](09-execution-upgrades-and-rollout.md) §4 |
| `ops.probe_dot_rate` (key: `ops.probe_rate`; dedicated DOT→USDC conversion for the `ops.reserve_probe` line debit — budget-envelope accounting only, independent of the coretime rate; the local first-arm and ongoing recovery runway is `(res.fail_threshold + res.recover_threshold) × ceil(ops.probe_fee_dot × ops.probe_dot_rate / 10^10)`) | Balance | µUSDC/DOT | 5,000,000 (5 USDC/DOT) **[VERIFY against live DOT/USDC before Phase-3 arming; ops-gated]** | 500,000 (0.5) | 500,000,000 (500) | ×2 | 1 | TREASURY | [07](07-oracle-and-disputes.md) §8, [08](08-treasury-and-economics.md) §1.1 |
| `ops.probe_fee_dot` (key: `ops.probe_fee`; maximum DOT admitted to one reserve-probe holding for Asset Hub execution plus response delivery; JIT withdrawal is prohibited; the matching `ops.reserve_probe` debit is USDC accounting only, so the remote sovereign account separately carries at least `res.fail_threshold + res.recover_threshold` full DOT envelopes plus a refill margin) | Balance | planck (DOT) | 5,000,000,000 (0.5 DOT) **[VERIFY against the live bounded program and response-delivery fee before Phase-3 arming; ops-gated]** | 100,000,000 (0.01 DOT) | 100,000,000,000 (10 DOT) | ×2 | 1 | TREASURY | [07](07-oracle-and-disputes.md) §8 |
| `ops.coretime_quote_ttl` (key: `ops.ct_quote_ttl`; executable-freshness window of an open renewal quote; expiry is the sole permissionless prune trigger) | u32 | blocks | 100,800 (~7 d) | 7,200 (~12 h) | 403,200 (~28 d) | ×2 | 1 | TREASURY | [09](09-execution-upgrades-and-rollout.md) §4 |
| `grd.review_deadline` (key: `grd.review_dl`; guardian retro-ratification deadline) | u32 | epochs | 2 | 1 | 4 | 1 | 2 | META | [06](06-governance-and-guardians.md) §5.4 |
| `att.bond` (attestor bond) | Balance | VIT | 25,000 | ×0.5 | ×10 | ×2 | 2 | entrenched | [06](06-governance-and-guardians.md) §7 — **binds at seating**, not live: the challenge floor and the *attestor's* slash base read the bond held by that member (the challenger instead forfeits 50% of its own posted bond), so an amendment reaches a member only at the next `attestor.set_members` (SQ-248) |
| `att.challenge_window` (key: `att.window`) | u32 | blocks | 43,200 (72 h) | 43,200 | 72,000 | — | 2 | META | [06](06-governance-and-guardians.md) §7, [09](09-execution-upgrades-and-rollout.md) §2.4 |
| `ledger.min_split` (= MinTransfer) | Balance | USDC | 0.01 (10⁴ base units) | 0.01 (K floor) | 1 | — | 2 | META | [03](03-conditional-ledger.md) |
| `ledger.redeem_fee` (key: `ledger.rdm_fee`; the [03](03-conditional-ledger.md) §5.3a redemption fee — charged on `redeem_scalar`, `redeem_scalar_pair`, `redeem_gate`, `redeem_baseline`, `redeem_baseline_pair`; **exempt**: `redeem`, `redeem_void`, every `merge*`, every `ProtocolAccount`, and any payout whose **net** would fall below `ledger.min_split` — the waiver tests the net, not the gross ([03](03-conditional-ledger.md) §5.3a(2), corrected 2026-07-29 before implementation; a gross-based test leaves a band that nets below `min_balance`). **Live coupling `ledger.redeem_fee ≤ mkt.fee`, screened at the amendment boundary** in both directions like `gate.v_min` ↔ `dec.v_min` (rule 7), and asserted in `try-state`. A missing or malformed record reads as **0** — the fail-open direction is the claimant-favouring one here) | Perbill | bps | **30** — derived, not chosen: the largest **exit-neutral** rate. Exiting a position through the book costs `mkt.fee` on proceeds; holding to settlement costs this rate on the payout **when that payout is a charged one**. Above `mkt.fee`, holding is dearer than round-tripping **conditional on the position surviving to a charged redemption**, so the schedule pays traders to close before d18 — draining exactly the contest capital [08](08-treasury-and-economics.md) §5.2's `L̂` measures and `dec.v_min` requires, which destroys this instrument's own base and degrades the security input together ([08](08-treasury-and-economics.md) §10.6). **The "every state of the world" form was an over-claim and is corrected (2026-07-29, milestone E1):** a wrapper buyer whose branch loses exits the mirror leg through the exempt `redeem` at zero cost, so the comparison holds in expectation — `E[hold exit] ≈ ½·rate·payout` vs `mkt.fee·proceeds` — putting exact neutrality nearer `2·mkt.fee`. The value and the coupling are **unchanged**: `≤ mkt.fee` is a deliberately conservative ceiling about a factor of two inside the expected neutral point, which is the right direction for a key whose unsafe direction is upward | 0 (off; the safe direction) | 100 — the static ceiling mirrors `mkt.fee`'s, but the **binding** bound is the live coupling above, which is strictly tighter whenever `mkt.fee < 100`. **The unsafe direction is upward** | 10 | 1 | PARAM | [03](03-conditional-ledger.md) §5.3a, [08](08-treasury-and-economics.md) §10 |
| `ledger.archive_delay` (key: `ledger.archive`) | u32 | blocks | 1 yr | 90 d | **1 yr (K ceiling)** | — | 2 | META | [03](03-conditional-ledger.md) |
| `ledger.position_deposit` (key: `ledger.pos_dep`) | Balance | USDC/entry | **0.1** (frozen, Part 3; raised from 0.01) | 0.1 (K floor) | **0.1** (frozen — max = min, see the deposit-unit freeze note in §2) | — | 2 | META | [03](03-conditional-ledger.md) |
| `phase3.tvl_cap` (global real-USDC exposure, D-13) | Balance | USDC | 2,000,000 *(sim-gated **[VERIFY before Phase-3 arming]**)* | — | raised only by phase-gate META + values ratification | — | — | META+values | [09](09-execution-upgrades-and-rollout.md) |
| `phase3.deposit_cap` (key: `phase3.dep_cap`; per account, D-13) | Balance | USDC | 20,000 *(sim-gated **[VERIFY before Phase-3 arming]**)* | — | as above | — | — | META+values | [09](09-execution-upgrades-and-rollout.md) |
| `xcm.trade_dot_per_sec` (key: `xcm.dot_per_sec`; XCM `WeightTrader` DOT rate, ref-time dimension) | Balance | planck / s of ref-time | 100,000,000,000 (10 DOT/s) *(fee-sizing sim-gated **[VERIFY against live AH/relay fees before Phase-3 HRMP arming]**)* | 1,000,000,000 (0.1 DOT/s) | 10,000,000,000,000 (1,000 DOT/s) | ×2 | 1 | PARAM | [09](09-execution-upgrades-and-rollout.md) §6.1 |
| `xcm.trade_dot_per_mb` (key: `xcm.dot_per_mb`; XCM `WeightTrader` DOT rate, proof-size dimension) | Balance | planck / MiB of proof | 10,000,000,000 (1 DOT/MiB) *(sim-gated, as above)* | 100,000,000 (0.01 DOT/MiB) | 1,000,000,000,000 (100 DOT/MiB) | ×2 | 1 | PARAM | [09](09-execution-upgrades-and-rollout.md) §6.1 |
| `xcm.trade_usdc_per_sec` (key: `xcm.usdc_per_sec`; XCM `WeightTrader` USDC rate, ref-time dimension) | Balance | µUSDC / s of ref-time | 50,000,000 (50 USDC/s) *(sim-gated, as above)* | 500,000 (0.5 USDC/s) | 5,000,000,000 (5,000 USDC/s) | ×2 | 1 | PARAM | [09](09-execution-upgrades-and-rollout.md) §6.1 |
| `xcm.trade_usdc_per_mb` (key: `xcm.usdc_per_mb`; XCM `WeightTrader` USDC rate, proof-size dimension) | Balance | µUSDC / MiB of proof | 5,000,000 (5 USDC/MiB) *(sim-gated, as above)* | 50,000 (0.05 USDC/MiB) | 500,000,000 (500 USDC/MiB) | ×2 | 1 | PARAM | [09](09-execution-upgrades-and-rollout.md) §6.1 |

**Existing-chain introduction of new rows (normative).** Adding a row to the genesis `Params` template is not an upgrade path: any runtime that first consumes a newly introduced key MUST carry a versioned constitution-state migration that inserts the complete bounded record on existing chains without overwriting a lawful value already present. This requirement applies to `ops.probe_rate` and `ops.probe_fee`; their first production runtime seeds both records by migration as well as by genesis, and its try-runtime checks prove the records are present and valid after upgrade.

Safety rationale (row-wise, carried forward): kernel floors/ceilings exist so no captured decision sequence can walk a defense to zero — δ cannot reach 0, windows cannot reach one block, κ cannot open flash manipulation, timelocks/grace cannot vanish, p_max cannot exceed 0.10, guardian scope cannot grow, the intake slash fraction cannot reach 0, and the Ask-scaling `2·InCapPrize` term of `dec.v_min` is kernel, not a key.

**Rows without a seeded `Params` record (normative; amended 2026-07-20, SQ-158).** Not every §1 row materializes as a genesis-seeded record; two shapes are exempt, and reading rule 2 binds every other row.

1. **Entrenched, kernel-scoped rows with no amendment path.** `grd.bond` — an `entrenched` row whose bound is scope-K and which carries neither a max-Δ nor a cooldown — is carried as a **kernel constant exposed through pallet metadata** rather than as a `Params` record: a record bearing governance metadata would misdescribe a value that cannot be governed. Reading rule 3 is satisfied for it by the constants API rather than by a storage item. This exemption covers the **bond only**. The guardian *allowances* sharing its row are kernel constants that are **not** individually metadata-exposed, so rule 3 is not currently satisfied for them; their exposure is unresolved and is not ratified here.
2. **Rows that gate nothing and have no number yet.** A row carrying **no dispatch-path limit of its own** — a pure economic, operating-budget or diagnostic value that no extrinsic checks against — may remain unseeded while its default is still `[VERIFY]`- or *sim-gated* under rule 4; reading rule 2 binds it **from adoption onward**, at which point it is seeded like any other. The tag is **not** itself the exemption, and this is the load-bearing half: a tagged row whose value *does* gate a dispatch or bound is seeded regardless — which is why the phase-3 exposure caps, the four XCM trader rates and the coretime-quote keys all carry live records despite carrying tags, and why the sentinel note below can rely on the phase-3 caps having seeded hard maxes.

   **Derivation is not an exemption.** Where a value is computable today and the implementation nonetheless derives it at its consuming engine while the §1 row still advertises an amendment path — a max-Δ, a cooldown, an amendable class — the missing record is a **defect**, not an exempt shape: the row promises a governance lever that derivation silently removes. (A row still awaiting its first number is the case above, not this one.)

**Unbounded sentinel (normative; amended 2026-07-20, SQ-196).** The "unbounded sentinels" of [09 §5.2](09-execution-upgrades-and-rollout.md) are the **maximum representable value of the key's `Balance` type**, which is also the seeded hard max of `phase3.tvl_cap` and `phase3.dep_cap` — so arming one is an ordinary amendment of the row to its own ceiling, requiring no distinguished value and no separate mechanism. (The phase at which each is installed is owned by [09](09-execution-upgrades-and-rollout.md), not by this row.) The two sentinels are **independent, and each disables only its own check**: `phase3.dep_cap` at the sentinel retires the per-account cumulative meter — which then neither reads nor writes that cumulative — while `phase3.tvl_cap` at the sentinel lifts only the global-issuance admission test. A bounded cap on either axis continues to reject inflows that breach it, so containment disappears entirely only when **both** sentinels are installed. Entries already recorded are **retained, never reaped**; they stay visible to `try-state` and bind again unchanged if a bounded cap is later restored.

---

## 2. Kernel constants (K — compile-time, constants-API-exposed)

| Constant | Value | Doc |
|---|---|---|
| `MinTrade` / `MaxTrade` | 1 USDC / b/4 per extrinsic (single-trade impact ≤ 0.25 logit) | [04](04-markets-and-pricing.md) |
| `dec.extension` | 43,200 blocks (3 d), at most once per proposal | [05](05-welfare-and-decision-engine.md) |
| `prop.max_calls` / `max_bytes` / `max_weight` | 16 / 64 KiB / 25% of block limit | [09](09-execution-upgrades-and-rollout.md) |
| `MAX_NESTED` (SafetyFilter recursion; wrapper set closed incl. `proxy_announced`, `as_multi_threshold_1`) | 4 levels, ≤ 16 calls — the call bound counts **nodes** (each wrapper and each leaf) against one budget shared by the whole evaluation, never reset per wrapper (SQ-31; [06](06-governance-and-guardians.md) §3.3) | [06](06-governance-and-guardians.md), [09](09-execution-upgrades-and-rollout.md) |
| `MAX_PAYLOAD_DECODE_DEPTH` (SCALE decode-recursion bound at the guard's preimage boundary) | 256 — equal to substrate's own extrinsic decode-depth limit | [09](09-execution-upgrades-and-rollout.md) §1.2(11) |
| LMSR domain bound | `\|q_L − q_S\|/b ≤ 48`; quoting clamp [0.001, 0.999] | [04](04-markets-and-pricing.md) |
| LMSR error bounds | exp2/log2 ≤ 2 ulp (64.64); composed cost ≤ 8 ulp; per-trade cost error ≤ 8·2⁻⁶⁴·b | [04](04-markets-and-pricing.md) |
| Rounding discipline | charges round up, payouts round down (maker-adverse/escrow-favoring); ledger divisions round against the claimant | [03](03-conditional-ledger.md), [04](04-markets-and-pricing.md) |
| **VOID payout rules** | complete pairs recover par via `merge`/`merge_scalar`/`merge_gate` (100%); `redeem_void`: unpaired branch-USDC pays `floor(a/2)`; unpaired LONG/SHORT **and unpaired gate legs (`GateYes`/`GateNo`)** pay `floor(a/4)` (the consistent gate extension of the D-1 rule, [03](03-conditional-ledger.md) §5.3); residue swept per dust rule | [03](03-conditional-ledger.md), [15](15-invariants-and-testing.md) I-26 |
| **Scalar redemption rules** | LONG `floor(a·s)`; **unpaired SHORT `floor(a·(1−s))`**; paired via atomic `redeem_scalar_pair` = exactly `a` | [03](03-conditional-ledger.md) |
| `DescriptorLeadTime` | 43,200 blocks (72 h) between `UpgradeAuthorized` and permissionless application | [09](09-execution-upgrades-and-rollout.md) |
| `MIGRATION_STALL_BLOCKS` | Active cursor stalled **> 900 blocks** raises the PB-MIGRATION halt | [09](09-execution-upgrades-and-rollout.md) §3.2 |
| PB-LEDGER-FREEZE | ≤ 14 days, one renewal only (values referendum); admissible only under the I-4 drift flag | [06](06-governance-and-guardians.md), [09](09-execution-upgrades-and-rollout.md) |
| Expedited CODE lane | 72 h gate market + 3-day fast-track values ratification; admissible only while **PB-LEDGER-FREEZE is active or a PB-MIGRATION halt is raised** (both arms normative — 09 §3.1; the second was omitted here and is restored by the SQ-132(d) sweep, 2026-07-20) | [06](06-governance-and-guardians.md), [09](09-execution-upgrades-and-rollout.md) |
| Watchtower window extension (`orc.ext_window`) | one +48 h (28,800-block) extension per `(component, epoch)` lifecycle if `wt.quorum` acknowledgments absent | [07](07-oracle-and-disputes.md) |
| `OracleSettleDeadline(m)` | start of epoch(m+1) Housekeeping (**d20** at the default length): any `(component, m)` not challenge-closed settles **neutrally** for every consuming cohort; late verdicts settle bonds only (I-18) | [07](07-oracle-and-disputes.md) §11, [05](05-welfare-and-decision-engine.md) §7 |
| `orc.max_proof_bytes` | 256 KiB per `recompute_proof` | [07](07-oracle-and-disputes.md) §9 |
| `reg.max_filings_epoch` / `wt.max` / attestor registry floors (`att.min_members` = 3, `att.quorum` = 2) | 64 filings/epoch/instance; ≤ 16 watchtower seats; ≥ 3 attestors, 2-of-N quorum | [07](07-oracle-and-disputes.md) §4/§7, [06](06-governance-and-guardians.md) §7 |
| Kernel attestation | bonded attestor registry (values-elected, ≥ 3); 2-of-3 signed attestations + challenge window | [06](06-governance-and-guardians.md), [09](09-execution-upgrades-and-rollout.md) |
| Dead-man switch | relay best advances ≥ 4,800 relay blocks without a new anchored parachain block (~8 h; the on-chain `relay_parent_gap` of §4.3 `F`, not GRANDPA finality — SQ-282) or snapshot > 4 d overdue ⇒ queue freeze, clock pause; coretime-renewal call **exempt** (D-9) | [05](05-welfare-and-decision-engine.md), [09](09-execution-upgrades-and-rollout.md) |
| `StaleEpochBound` | 7 days ⇒ force-reject all in-flight on next tick | [05](05-welfare-and-decision-engine.md) |
| Crank batch bounds | `TickBatch` = 10; `ReapBatch` = 100; `settle_cohort` ≤ 100 items/call; `OracleDeadlineCatchup` = 4 measurement epochs of [07](07-oracle-and-disputes.md) §11(1) money-deadline catch-up per clock-syncing epoch crank (= the ≤ 4 non-terminal cohorts of §1, since only a measurement epoch a live cohort consumes can carry a round); the excess resumes on the next crank; `ComponentReapBatch` = 32 settled component values retired per oracle-boundary crank (= MAX_COMPONENT_VALUES / (retained epochs + 1) = 128 / 4, i.e. exactly one measurement epoch's arrivals, so the sweep keeps pace with production without being picked — [07](07-oracle-and-disputes.md) §13), the excess likewise resuming on the next crank; `GuardianMaintenanceBatch` = 10 overdue reviews settled — and 10 failed-action records retired — per block by the mandatory guardian maintenance hook ([06](06-governance-and-guardians.md) §5.4), the excess carried to the next block and **never skipped**, because dropping a due review forfeits an accountability action outright while deferring one only delays a slash (the SQ-45(c) asymmetry). Derived, not chosen: it is the number of reviews the §5.2 allowance-capped powers can bring to one shared deadline — delay-once 2/epoch + force-rerun 1/epoch + pause-intake 1 per 4-epoch window + one activation of each of the six enumerated playbooks — so under a live clock the hook never lags, and a saturated 128-review backlog drains in 13 blocks. Measured at the bound, the hook costs 105 reads / 110 writes / 125,304 B of proof / 6.08 ms, against the hand-written 220 / 150 / 183,055 B / 1.95 ms envelope it replaces — so the bound lowers the mandatory per-block storage and PoV charge and raises the ref_time charge to a measured value (0.30 % of the 2-second block), the earlier guess having over-declared storage and under-declared time by ~3.1x (SQ-500) | [05](05-welfare-and-decision-engine.md), [03](03-conditional-ledger.md), [06](06-governance-and-guardians.md) |
| Entrenched floors | θS⁻/θC⁻ K-entrenched at **0.90 / 0.85** — deliberately equal to the [05](05-welfare-and-decision-engine.md) launch defaults: the welfare-gate knees are tighten-only from genesis and can never be amended below launch values; guardian scope K; annulment requirement K; VOID rule K | [05](05-welfare-and-decision-engine.md), [06](06-governance-and-guardians.md) |
| Keeper-budget floor note | the 6,000 USDC hard min of `keeper.budget_epoch` is a kernel floor: below it the metered budget cannot cover decision-critical cranks at the 1× rebate bound and A-1 fails silently | [08](08-treasury-and-economics.md) §6 |
| Position-deposit freeze note | (added 2026-07-21, SQ-36) `ledger.position_deposit` carries **max = min = default = 0.1 USDC**: the row keeps its META class and its record shape, but the admissible interval is a single point, so the unit is frozen in fact as well as in name. Reason: the ledger charges, refunds and reconciles `DepositsHeld` at the **live** unit, and [03](03-conditional-ledger.md) §10 gives it no hook to rebase deposits already held. With a movable unit, an entry funded at the old unit is refunded at the new one — L-6 (`DepositsHeld == unit × entries`) breaks immediately for every pre-existing entry, `reap` over-refunds out of pooled collateral before it underflows, and a `transfer` that deletes one entry while creating another keeps L-2 algebraically balanced while moving the difference between two users. Per-entry deposit vintages are the only design that preserves tunability and would require an unbounded migration over live positions; that is refused under R-7. Re-opening tunability is a dedicated milestone with its migration, never a bounds amendment | [03](03-conditional-ledger.md) §4/§9/§10, [15](15-invariants-and-testing.md) §1 L-2/L-6 |

---

## 3. Frozen protocol constants (Part 3 of the [decision record](00-decision-record.md))

### 3.1 Epoch schedule — offsets as **fractions of `epoch.length`** (d-labels at the 21-day default)

Phase offsets are stored as rational fractions of `epoch.length` (B-med fix), so the schedule survives `epoch.length` changes. **Representation: the fraction pairs (numerator, denominator = 21) are kernel constants (K) in `futarchy-primitives`, exposed to clients as the [02 §9](02-integration-contract.md) `Epoch::PhaseOffsets` metadata constant — not `Params` storage.** The genuinely tunable `epoch.length`/`epoch.slots` values remain in `Params`. **Changes take effect next epoch; in-flight cohorts keep their creation-time schedule.** Day labels below are the 302,400-block default.

| Phase | Fraction of epoch | Blocks (default) | Label |
|---|---|---|---|
| Intake | [0, 3/21) | 0 – 43,200 | d0–d3 |
| Qualify | [3/21, 4/21) | 43,200 – 57,600 | d3–d4 |
| Seed | [4/21, 5/21) | 57,600 – 72,000 | d4–d5 |
| **Trade** | [5/21, 18/21) | 72,000 – 259,200 | **d5–d18 (13 days — corrected label; was “d4–d18”)** |
| Decide window (accrual) | **not a fraction** — [18/21·L − `dec.window`, 18/21·L), anchored to Trade close ([05](05-welfare-and-decision-engine.md) §3.1) | 216,000 – 259,200 | d15–d18 (final 72 h; trailing = final 24 h) |
| Decide | 18/21 | 259,200 | d18 |
| Review (timelock) | d18 + `exec.timelock(class)` | — | per class |
| Execute | per-proposal maturity, within `exec.grace` | — | — |
| Housekeeping | [20/21, 1) | 288,000 – 302,400 | d20–d21 |

Related frozen values: measurement horizon k = 2 epochs; measured cohort settlement at e+3; ≤ 4 non-terminal cohorts; **maturity worked example B+288,000** (corrected from B+287,000); capital duration example ~63–66 days.

**The decision window is the one row that is not a kernel fraction (corrected 2026-07-31; SQ-535).** Every other row above scales with `epoch.length` by construction, which is the property this table exists to state. The decision window does not: [05](05-welfare-and-decision-engine.md) §3.1 anchors it *absolutely* to Trade close — "`dec.window` and `dec.trailing` remain absolute block-count parameters" — and the SQ-501 clarification below confirms the accrual boundaries are computed from the then-live `dec.window`/`dec.trailing` and stored per book. Its `15/21` is therefore a **rendering at the genesis registry**, not an offset: it equals the governing window at `(epoch.length, dec.window) = (302,400, 43,200)` and at no other admissible pair. At `epoch.length = 604,800` the fraction gives `[432,000, 518,400)` where the governing window is `[475,200, 518,400)`; at `dec.window = 86,400` and the default length it gives a 43,200-block window where the governing one is 86,400. The row previously stated the fraction without qualification, which contradicted the document that owns the schedule. `reference-model`'s `lifecycle` module carries both readings and pins the divergence (`decide_window_absolute` / `decide_window_by_fraction`). **The kernel constant `phase_offsets::DECIDE_WINDOW_NUM = 15` is published to clients in the [02](02-integration-contract.md) §9 `Epoch::PhaseOffsets` list and has the same property; whether it belongs there is SQ-535**, since removing it changes a frozen 02 surface.

**Scope of "keep their creation-time schedule" (normative clarification; SQ-501, 2026-07-27).** The sentence above pins **phase boundaries**: the K fraction pairs, the epoch length a cohort was created under, its `decide_at` anchor, and the decision-window accrual boundaries, which are computed from the then-live `dec.window`/`dec.trailing` and **stored per book** when the window is registered at Seed. TWAP accrual, observation counts, staleness and coverage all read those stored boundaries, so for them the pin is real.

It does **not** say that every consumer of a schedule-shaped parameter reads a frozen copy, and one does not: the **decision-critical crank classification** — which decides whether an observation draws on the metered `keeper.budget_epoch` (§5 item 4; [08](08-treasury-and-economics.md) §6.2) — recomputes `decide_at − dec.window` from the **live** registry on every crank. Raising `dec.window` therefore re-classifies cranks on books already trading, immediately and retroactively, without moving any accrual boundary. That is why §5's occupancy screen treats `dec.window` as live-consumed and composes against in-flight book counts rather than trusting this pin; the earlier revision of that paragraph read this sentence as covering the classification too, which it never did. Nothing here licenses adding a *new* live read of a pinned value: a consumer that needs the creation-time schedule MUST read the stored boundaries.

### 3.2 Markets and TWAP

- **TWAP slew cap κ applies per 10-block observation interval** (`mkt.obs_interval`) — the ADR-11 “per 60-block” wording is corrected; the widening rule over k missed intervals is `(1±κ)^k`.
- Observations read the **previous block's** stored quote; staleness counts gaps > 50 blocks **inside the decision window only** (first ⇒ one 3-day extension, second ⇒ reject).
- Gate books are **exempt from the [0.02, 0.98] sanity band** (near-boundary validity rule instead); the band applies to welfare (decision + Baseline) books only; `V_min` is per-book ([05](05-welfare-and-decision-engine.md)).
- Maker worst-case loss `b·ln 2` per book; **worked maker-loss example ≈ 180 USDC** (TREASURY b = 25,000 walked 0.5 → 0.56; corrected from ≈ 1,507).

### 3.3 LMSR authoritative test vectors (b = 10,000 USDC, 64.64; regenerated from the reference model in CI — B-6)

| # | Action | Frozen value |
|---|---|---|
| V1 | cost of buying 1,000 LONG from q=(0,0) | `10000·ln((e^{0.1}+1)/2)` = **512.494795136…** USDC (**corrected** from 512.925464970) |
| V2 | price after V1 | 0.524979187479… |
| V3 | displace 0.5 → 0.6 | Δ = 4,054.65108108… LONG; cost = 2,231.43551314… USDC |
| V4 | worst-case loss | 6,931.47180560… USDC |
| V5 | V1 round trip net of 2 × 30 bps fees | net = **−3.074969…** USDC (**corrected** from −3.077552) |
| V6 | domain edge | a buy pushing `q_L − q_S > 48·b` MUST be rejected (`PriceBoundExceeded`); the state at exactly `48·b` is in-domain per [04](04-markets-and-pricing.md) §4 |

On-chain results MUST match within the §2 error bound plus one base unit of rounding.

### 3.4 Oracle, disputes, governance

- Challenge window **72 h (43,200 blocks)** with watchtower quorum (`wt.quorum` = 2-of-N registered, bonded; one +48 h extension); components not challenge-closed by `OracleSettleDeadline` (d20) settle neutrally ([07](07-oracle-and-disputes.md) §11).
- Oracle latency budget: report 2 d + rounds + 7-day terminal track — reconciled table in [07](07-oracle-and-disputes.md).
- **Oracle adjudication track: 60% approval / 10% support / 7-day decision**, tally on a **pre-cohort conviction snapshot** (VIT locked before the subject cohort's creation) — raised from 50%/3%/5-day (B-19/D-18).
- **Expedited `ratify` schedule** (admissible only while PB-LEDGER-FREEZE is active, for the expedited-CODE-lane referendum only): prepare 0 / decision 3 d / confirm 12 h ([06](06-governance-and-guardians.md) §2.1). `frn.window` (force-rerun Extended window) = `dec.extension` = 43,200 blocks (shared K, [06](06-governance-and-guardians.md) §9).
- **`UndecidingTimeout` = 7 days** (added 2026-07-20, SQ-229) — the `pallet-referenda` window, measured from submission, within which a referendum must begin deciding; past it a referendum that is neither deciding nor queued for a decision slot times out, with its submission and decision deposits left refundable. It is a pallet metadata constant, hence client-visible, which is why it is homed here rather than treated as internal plumbing. **Normative constraint:** `UndecidingTimeout` MUST be ≥ the longest track prepare period of [06 §2.1](06-governance-and-guardians.md), or referenda on that track could time out before they first become eligible to decide. At the launch schedule it binds with **zero margin** against the 7-day `entrenched` prepare period: the practical effect is that an entrenched referendum's decision deposit must be placed within its prepare period, and raising that prepare period without raising this value would make the entrenched track un-decidable. Benchmark fixtures may fork the value upward; the release runtime is unaffected.
- Slashing split 40% challenger / 60% INSURANCE; reporter-stake slash 50% on second adjudicated-false report, ejection on third.
- **Canonical v1 welfare `MetricId` assignments** (frozen, append-only, never reused; semantics owned by [05](05-welfare-and-decision-engine.md) §4.3, added 2026-07-17 with SQ-113): `C_onchain` X = 1, R = 2, E = 3, H = 4, Π = 5, K = 6; `S` U = 10, F = 11, D_eff = 12; `P` 20/21/22 (fees / qualified users / settled value); `A` 30/31/32 (shipped upgrades / runtime performance / integrations). Code mirror: `futarchy_primitives::metric_ids`.
- Intake: ≤ 4 entries/epoch/account; 10% bond slash **to INSURANCE** (never burned — USDC is bridged USDC) on non-decision-grade and preimage-missing outcomes (§1 keys; economics [08](08-treasury-and-economics.md) §7).
- Keeper metered budget 12,000 USDC/epoch; collator compensation 2,000 USDC/collator/epoch ([08](08-treasury-and-economics.md)).

### 3.5 Chain identity and supply (owned by [02](02-integration-contract.md); values frozen, D-17)

The heading's ownership applies to the identity/supply table. The genesis protocol-account derivation mirror below is owned by [08 §2.1](08-treasury-and-economics.md) and deliberately does not enlarge the [02](02-integration-contract.md) surface.

| Constant | Value |
|---|---|
| ss58 prefix | 7777 (registry submission before Phase 2) |
| paraId | assigned at onboarding; test fixtures 4242 |
| USDC | `ForeignAssets`, Location `{parents: 1, X3(Parachain(1000), PalletInstance(50), GeneralIndex(1337))}` (asset index 1337 verified Circle-native sufficient USDC, 2026-07-16, PLAN V-17); 6 decimals; `min_balance` 10⁴ |
| Counterparty system chains | Asset Hub = `Parachain(1000)` (USDC reserve, [09](09-execution-upgrades-and-rollout.md) §6.1); **Coretime chain = `Parachain(1005)`** (renewal-funding target, [09](09-execution-upgrades-and-rollout.md) §4; verified 2026-07-16, PLAN V-18); relay = parent. Single-homed as numbers in `futarchy-primitives::chain_identity` |
| VIT | native; **total supply 10⁹, 12 decimals**; existential deposit 0.01 VIT |
| VIT vesting | SDK `pallet-vesting`; min vested transfer **1 VIT**; community schedule duration **24 months = 2 × BLOCKS_PER_YEAR** from the exact Phase-4 arming block; genesis-allocation schedules per [08](08-treasury-and-economics.md) §2.1 |
| USDC treasury target | ≥ 25M USDC before Phase-5 arming; per-class min-viable NAV floors per [08](08-treasury-and-economics.md) §4 |
| Phase flag | `pallet-constitution::PhaseFlags` (bitset) — the FE trading-enablement key |
| `ReleaseChannel` | fixed-layout raw storage key in `pallet-constitution`, SCALE layout frozen forever (D-14) |
| Bootnodes | ≥ 8 browser-reachable WSS across ≥ 4 operators, ≥ 2 on :443; operator served-state window 30 days | 

**Genesis protocol-account derivations (mirror of [08 §2.1](08-treasury-and-economics.md)).** The VIT allocation pots use `PalletId(*b"bl/trsry")`: `into_account_truncating()` for treasury `MAIN`, and `into_sub_account_truncating(b"communty")` / `into_sub_account_truncating(b"incentiv")` for the community and Phase-3–4 incentive pots. The exact [03 §7](03-conditional-ledger.md) R-4 USDC `min_balance` endowment set is: the ledger sovereign `PalletId(*b"bl/ledgr").into_account_truncating()`; `insurance_account()` / `book_account()` / `pol_account()` / `pol_baseline_account()` / `fee_account()` / `treasury_protocol_account()` under that `PalletId` with exact seeds `*b"INSURANC"`, `*b"BOOK____"`, `*b"POL_____"`, `*b"POL_BASE"`, `*b"FEES____"`, and `*b"TREASRY_"`; `treasury_account()` (`MAIN`); and `treasury_keeper_account()` / `treasury_oracle_account()` / `treasury_collators_account()` with seeds `*b"KEEPER__"`, `*b"ORACLE__"`, and `*b"COLLATOR"` under `PalletId(*b"bl/trsry")`. Per-market book/fee accounts are excluded because they do not exist until `create_market` and are reaped at close. The market, epoch, execution-guard, welfare-settlement, guardian and both registry sovereigns are excluded because R-4 does not name them; registry payouts deliberately use `Expendable`. The owning derivation table and membership rule are [08 §2.1](08-treasury-and-economics.md); both copies MUST change together.

---

The A13 proposer-reward extension adds `treasury_rewards_account()` (`PalletId(*b"bl/trsry").into_sub_account_truncating(*b"REWARDS_")`) to this exact genesis-endowed set; its `REWARDS` line is custody-synced to that pot when funded.

## 4. Reconciled storage bounds (D-10 — one table, all budgets derive from it)

| Bound | Value | Scope (the reconciliation) | Doc |
|---|---|---|---|
| `IntakeQueue` | **64** | intake-family cap and metadata-constant name: the frozen direct-read `IntakeQueue` contains Submitted IDs; internal `IntakeProposals` contains Submitted/Screening records plus current-epoch Cancelled admission records; overflow ⇒ `IntakeFull`, bond refused | [05](05-welfare-and-decision-engine.md), [06](06-governance-and-guardians.md) |
| `MaxLiveProposals` | **32** | internal/frozen `Proposals`: post-qualification non-terminal working set; disjoint from `IntakeProposals` | [05](05-welfare-and-decision-engine.md) |
| Books per proposal | **≤ 6** (2 decision + 4 gate; the old “7 + margin” is deleted) | per proposal | [04](04-markets-and-pricing.md) |
| Baseline books | no independent count; consume the active/POL and retained-book capacities | per chain | [04](04-markets-and-pricing.md) |
| `MaxLiveMarkets` | **196**, design-sized as `32·6 + 4` but enforced over the actual proposal/Baseline mix; counts books without a durable terminal latch and therefore live POL obligations | per chain | [04](04-markets-and-pricing.md) |
| `MaxStoredMarkets` | **2,240** = `196 + (ceil(1 yr / 14 d) + 1)·(12·6 + 1)`; counts every retained `Markets` row, including terminal books awaiting archive reap | per chain | [04](04-markets-and-pricing.md) |
| Concurrently *trading* books | 31 = 5·6 + 1 (forecast trading cut, D-8) | per epoch | [04](04-markets-and-pricing.md) |
| `RecentCohortSummaries` ring | **32** cohorts (chain-served history, layer 1 of D-6) | `pallet-epoch` | [02](02-integration-contract.md) |
| `TwapCheckpoints` | 8 per market | `pallet-market` | [04](04-markets-and-pricing.md) |
| `MaxPositionsPerAccount` | **64**, counter-enforced; **protocol accounts (POL/books/treasury subs) exempt** from cap and deposit | `pallet-conditional-ledger` | [03](03-conditional-ledger.md) |
| Positions deposit | **0.1 USDC/entry** (`ledger.position_deposit`) | — | [03](03-conditional-ledger.md) |
| Positions key order | **`(PositionId, AccountId)`** — per-vault prefix-drainable for reaping (was `(AccountId, PositionId)`) | — | [03](03-conditional-ledger.md) |
| `MaxSettlingCohorts` | 4 non-terminal (2 measuring + 1 awaiting oracle + 1 settling) | `pallet-epoch` | [05](05-welfare-and-decision-engine.md) |
| Resource locks | ≤ 32 proposals × 8 domains | `pallet-epoch` | [05](05-welfare-and-decision-engine.md) |
| Oracle games (live rounds) | ≤ 16 components × 4 settling epochs × 2 concurrent frozen versions = **128** — one live round per `(component, epoch, version)` game (the 3-round escalation ladder is sequential *within* a game, not concurrent storage; ≤ 2 versions overlap only across a MetricSpec activation boundary — [07](07-oracle-and-disputes.md) §2(4)). Within 02 §3's `open_oracle_rounds` view cap of 192. `MAX_ROUNDS = 128` (SQ-59) | `pallet-oracle` | [07](07-oracle-and-disputes.md) |
| `MetricSpecs` | ≤ 16 versions | `pallet-welfare` | [05](05-welfare-and-decision-engine.md) |
| Snapshots | ≤ 20 epochs (H + challenge + 12) — **60 records**, because `Snapshots` is keyed `(epoch, spec_version)` and each retained epoch can need one record per cohort measuring it ([05](05-welfare-and-decision-engine.md) §3.3's `epoch.horizon_k` = 2) **plus one for its own active spec**, which need not be either of theirs. The epoch figure governs retention and the prune cutoff (`current − 19`); the record figure governs capacity, and conflating the two left more lawful records than slots at every activation boundary (`MAX_SNAPSHOTS = 60`; [02](02-integration-contract.md) contract v16). Gate-breach flags and the 21-epoch shared prefix index below are epoch-keyed and take the epoch figure unchanged | `pallet-welfare` | [05](05-welfare-and-decision-engine.md) |
| Welfare block-weight samples | one accumulator per `(epoch, day)` — a running utilization sum and a block count, never the per-block series, because 05 §4.3's `H` is a **mean** and a mean is exactly reconstructible from its accumulator (contrast `F`'s median, §4.3.2, which does require the series). ≤ 256 day slots per epoch over the retained welfare window, retired by the shared bounded reaper; a sample the reaper cannot reach is dropped rather than written, and a window with no sample resolves `H` **absent**, never 1 | `pallet-welfare` | [05](05-welfare-and-decision-engine.md) §4.3 |
| Welfare integrity-failure counter | one saturating `u32` per `(epoch, day)` — 05 §4.3's `Π = max(0, 1 − 0.25 · events)`, so the value saturates the score at **4** events and the integer bound is defensive only. ≤ 256 day slots per epoch over the retained welfare window, keyed and retired exactly like the block-weight samples; one write path so a single event cannot be counted twice (§4.3.2) | `pallet-welfare` | [05](05-welfare-and-decision-engine.md) §4.3/§4.3.2 |
| `ExecutionRecords` | ring 256 (canonical history is event-derived within the committed window, D-2/D-6 — “pruned to indexer” language deleted) | `pallet-execution-guard` | [09](09-execution-upgrades-and-rollout.md), [02](02-integration-contract.md) |
| `MIGRATION_SERVICE_WEIGHT_PERCENT` | **50%** of maximum block weight | `pallet-migrations` per-block service budget | [09](09-execution-upgrades-and-rollout.md) §2.1/§3.2 |
| `MIGRATION_CURSOR_MAX_LEN` | **65,536 bytes** encoded | `pallet-migrations` active cursor | [09](09-execution-upgrades-and-rollout.md) §2.1/§3.2 |
| `MIGRATION_IDENTIFIER_MAX_LEN` | **256 bytes** encoded | `pallet-migrations` migration identifier | [09](09-execution-upgrades-and-rollout.md) §2.1/§3.2 |
| Registry filings (Incident/Milestone) | bounded per `pallet-registry` spec | `pallet-registry` | [07](07-oracle-and-disputes.md) |
| `Params` registry | **128** keys (genesis-fixed set; ≥ the ~87 currently-concrete §1 rows plus headroom for `[VERIFY]`-gated rows as they resolve; the `params()` runtime API keeps its own 64-keys-per-call bound, [02](02-integration-contract.md) §3) | `pallet-constitution` | [06](06-governance-and-guardians.md) |
| `Capabilities` table | 64 rows | `pallet-constitution` | [06](06-governance-and-guardians.md) |
| `Meters` | 16 (generic bounded-meter primitive; empty at genesis — envelope meters live with their owning pallets, [15](15-invariants-and-testing.md) I-17) | `pallet-constitution` | [06](06-governance-and-guardians.md) |
| Vesting schedules per account | **8** (`MAX_VESTING_SCHEDULES`; the genesis allocation uses exactly one per founding-team beneficiary) | `pallet-vesting` | [08](08-treasury-and-economics.md) §2.1 |
| Community distribution schedules | **4,096** successful schedules over the lifetime of the Phase-4 community allocation; completed schedules do not replenish the bound | `pallet-futarchy-treasury` | [08](08-treasury-and-economics.md) §2.1 |
| Treasury `Streams` | **128** open vesting streams (recipient-claimable grants > `trs.stream_threshold`, §1.3); ≥ `epoch.slots` new grants/epoch over multi-epoch vesting horizons, with headroom | `pallet-futarchy-treasury` | [08](08-treasury-and-economics.md) §1.3 |
| Pending collator authored-share entries | **120** distinct accounts per epoch accumulator (the runtime's 100-candidate plus 20-invulnerable active-session ceiling), in at most **two** bounded accumulators transiently — the current epoch's plus one completed epoch's pending slot awaiting payout (**240** entries worst case); overflow authors are not recorded and the payout remains fail-closed | `pallet-futarchy-treasury` | [08](08-treasury-and-economics.md) §2.4 |
| Welfare collator-authorship entries | **120** distinct authors per window — the same active-session collator ceiling as the authored-share accumulator above, because both count the same population and 05 §4.3's `K` must not disagree with 08 §2.4's payout about how many collators exist. Two windows per epoch series: each `(epoch, day)` slot (≤ 256 day slots per epoch over the retained welfare window) plus **one per-epoch aggregate maintained on write**, so a component reading the epoch pays one bounded read instead of folding up to 256 day slots. Both retired by the shared bounded reaper. An overflowing author is not recorded and the affected window latches a truncation sentinel: the drop lowers a *count*, so `K` (and later `U`) read it as less healthy — the safe direction — but it does **not** lower a *distribution*, so `D_eff` (§4.5) MUST treat a truncated window as unavailable rather than read a retained distribution that looks more uniform than the real one | `pallet-welfare` | [05](05-welfare-and-decision-engine.md) §4.3 |
| Welfare block-production entries | **256** day slots per epoch (the `u8` second key), under the shared 21-epoch prefix index (the 20 retained snapshot **epochs** + the live epoch — `SNAPSHOT_RETENTION_EPOCHS`, not the 60-record `MAX_SNAPSHOTS`) that already carries the XCM-traffic and reserve-probe series and is retired by the same bounded reaper. The value is a fixed-width counter triple — non-empty blocks, empty blocks, and the summed per-block relay-parent delta ([05](05-welfare-and-decision-engine.md) §4.3.2) — so the series introduces no variable-length collection of its own. Each indexed epoch additionally carries **one** fixed-width epoch total of the same triple, maintained on write and retired by the same reaper, so that `U` at epoch granularity is a single dispatch read rather than a fold of up to 256 day slots — at most 21 totals exist, one per indexed epoch, and each must equal the field-wise sum of its own day slots. An epoch that cannot be admitted to the shared index records nothing at all — neither day slot nor total — which leaves that window's relay-slot denominator zero and resolves `U` **absent**: the numerator and the denominator are refused together, so backpressure can never make an unmeasured window look healthy | `pallet-welfare` | [05](05-welfare-and-decision-engine.md) §4.3.2 |
| Treasury budget lines | **32** — ≥ the enumerated `POL`/`POL_BASELINE`/`KEEPER`/`ORACLE`/`REWARDS`/`ops.*` lines (§1.1) with headroom; upsert-keyed, so occupancy ≤ the line enumeration | `pallet-futarchy-treasury` | [08](08-treasury-and-economics.md) §1.1 |
| Treasury pending outflows | **64** — queued in-cap proposal outflows awaiting meter grace (§1.3); matched to the `IntakeQueue` pre-qualification ceiling | `pallet-futarchy-treasury` | [08](08-treasury-and-economics.md) §1.3 |
| Treasury POL commitments | **196** = `MaxLiveMarkets` — one obligation per book without a durable terminal latch that NAV nets against (§1.2/§8.2); terminal retained books do not consume this bound | `pallet-futarchy-treasury` | [08](08-treasury-and-economics.md) §8 |
| Treasury coretime obligations | **8** each: ≤ 8 funded-period idempotency keys **and** ≤ 8 open renewal quotes (two separately-bounded collections, D-9); ~8 renewal periods of retained history | `pallet-futarchy-treasury` | [09](09-execution-upgrades-and-rollout.md) §4 |

**Why 64 and 32 are jointly satisfiable:** the intake family admits ≤ 64 candidates per epoch across Submitted/Screening bookkeeping (and preserves current-epoch Cancelled records solely for the admission counter); qualification passes ≤ `epoch.slots` = 5 per epoch into the post-qualification non-terminal pipeline. Live occupancy = 5 trading + ≤ 20 in measurement/settlement (5 × 4 cohort stages) + extended/suspended/rerun/queued stragglers ≤ 7 of margin ⇒ 32 suffices with headroom; the 64 cap prices the intake waiting room and its screening bookkeeping (bonds + slash, [08](08-treasury-and-economics.md) §7).

---

## 5. Derived-value cross-checks (recomputed from 32/196/2,240 — normative derivations)

1. **Market state:** the active/POL envelope is 196 books; the retained `Markets` map ceiling is 2,240 books × sizeof(`MarketBook<AccountId>`) = 2,240 × **205 B** (measured `MarketBook<AccountId>` `MaxEncodedLen`, B10 re-measurement 2026-07-17 after the accumulator widened to the [04 §7](04-markets-and-pricing.md) two-limb u256 shape; was 189 B at the B5 benchmark run) = **459,200 B ≈ 448.4 KiB**, within a **512 KiB** retained-map budget. This conservative archive envelope combines the independent worst-case 73-book creation batch with the 14-day epoch floor, one-year `ledger.archive` ceiling and one extra boundary batch; it deliberately exceeds the tighter cross-coupled occupancy and gives a healthy keeper a whole creation boundary to reap each fixed-work eligible batch. Per-extrinsic touch bounds do not grow with the retained-map ceiling: `decide(pid)` reads ≤ 6 proposal books + 1 Baseline + O(10) params; `settle_cohort` ≤ 100 (market, total) items/call; `market.reap` reads/removes at most 28 protocol-position cells and never waits for claimant cleanup. `decide` additionally walks [07](07-oracle-and-disputes.md) §12's ProcessHold predicate over the oracle round map — ≤ `MAX_ROUNDS` = 128 rounds scanned, each costing a `RoundSchedules` and a `ComponentValues` read when it carries the proposal's frozen version. 02 §7.2's `16 × ≤4 × ≤2` decomposition suggests ≤ 64 can share one version, but its 4-settling-epoch factor holds only once [07](07-oracle-and-disputes.md) §11(1)'s retained rounds are actually reaped — which they were not before SQ-492 gave that retention a deadline, so successive epochs accumulated money-settled rounds on one long-lived version to the cap. The charged worst case is therefore all 128, which with SQ-492 in place is **conservative rather than exact** and is kept for that reason (SQ-494). That is a **kernel constant, not a map ceiling**, so the clause above still holds in the sense it was written for: no per-call touch bound scales with `Markets`, `Vaults` or any other growing map. The clause was silent about the round scan until SQ-494 measured it — the `decide` benchmark seeded no rounds at all, so the predicate ran against an empty map. PoV per production call remains bounded. The pinned figures are **dispatched** weights — the 50×20 generated function plus the A13 collator-compensation term each clock-syncing crank composes at its `#[pallet::weight]` attribute: `decide` **745,551 B** = 356,514 + 389,037, `settle_cohort(5)` **725,862 B** = 183,055 + 5 × 30,754 + 389,037, pinned as exact regression values in the runtime's `pov_budgets` suite. `decide`'s figure is 19.0 % of the 3,932,160 B normal-class budget, beside `settle_cohort(5)`'s 18.5 % and `settle_cohort(12)`'s 23.9 %; its local drift ceiling is **1 MiB** (`settle_cohort`'s **1.25 MiB**), still comfortably above the measurement, so both remain detectors rather than formalities. The collator-compensation term moved 48,000 → 389,037 B when it stopped being a hand-written estimate and became a benchmark: that 8× is the generator's **synthetic per-key envelope**, which charges each of the 120 payees' `ForeignAssets::Account` keys as an independent maximum-depth trie path and ignores their shared asset prefix — the benchmark's *recorded* proof was 17,874 B, ~22× smaller. The estimate is nonetheless what is declared, because the two directions of error are not symmetric: over-declaring PoV costs block capacity, while under-declaring it yields blocks that exceed their proof budget at execution, and a sparse benchmark trie under-represents production path depth. The resulting capacity cost is real — a crank reserves ~389 KB of proof for a payout that fires once per epoch — and its remedy is the post-dispatch refund §4.5 of [15](15-invariants-and-testing.md) already mandates for payload-executing extrinsics, tracked as its own PLAN row. The `try-runtime`-only market `try_state` is intentionally a full retained-map audit, measured at the saturated 2,240-book/4,480-account fixture with all 2,240 seed/rerun markers and all 196 active books/POL commitments carrying maximum checkpoint/window/owner auxiliaries (17,079,651 B estimated proof, 24,464 reads); it executes out of band against a snapshot and is not a normal-class block call.
2. **Vaults:** ≤ 32 live + 4 cohorts × 5 settling = **≤ 52** × **160 B** (measured `VaultInfo` `MaxEncodedLen`, B5 2026-07-17; ~256 B stays the pinned ceiling) ≈ **8.1 KiB**, within the ≤ 13 KiB budget; per-branch supply fields (B-4) add two `Balance` words/vault. The budget is that occupancy at the pinned ceiling — 52 × 256 B = 13,312 B — which is why the item-6 screen measures a proposed `epoch.slots` against the **pinned** figure rather than the current measurement: an occupancy admitted only because today's struct happens to be small would be an envelope that the next field addition breaks.
3. **Positions:** globally priced by the 0.1 USDC deposit (dusting an account to its 64-cap now costs 6.4 USDC/victim-account, cf. threat row in [14](14-threat-model.md)); per-vault reap drains the `(PositionId, *)` prefix in `ReapBatch` = 100 chunks.
4. **Keeper crank load** (feeds `keeper.budget_epoch`): 31 trading books × (43,200/10) = **133,920 decision-critical** observations/epoch; × (187,200/10) = **580,320 full-window** — derivation and budget fit in [08](08-treasury-and-economics.md) §6. This derivation binds: it assumed `epoch.slots` = 5, books ≤ 6, `mkt.obs_interval` = 10, `dec.window` = 43,200 and — for the full-window figure alone, whose 187,200-block Trade phase is `13/21` of the epoch (§3.1) — `epoch.length` = 302,400. The 31 is `epoch.slots`·6 + 1: one book set per active slot plus the epoch's single unconditional Baseline book. `epoch.length` was omitted from this list until SQ-501 made the derivation executable and the omission visible; it was an oversight, not a claim that the full-window figure is length-independent.
5. **POL/NAV floors** (feed the [09](09-execution-upgrades-and-rollout.md) phase gates): commitments 34,657 / 55,452 / 103,972 / 159,424 for PARAM / TREASURY / CODE / META (+ 17,329 Baseline); floors ≈ 4.62M / 7.39M / 13.86M / 21.26M; five-PARAM slate ≈ 23.10M and five-META worst slate ≈ 106.3M — arithmetic normative in [08](08-treasury-and-economics.md) §3–4.
6. **Parameter-coupling rule (normative):** a decision changing `epoch.slots`, `mkt.obs_interval`, `dec.window`, `epoch.length`, `ledger.archive`, `pol.budget_epoch`, any `pol.b.{param,trs,code,meta}`, `pol.b_gate` or `pol.b_baseline` MUST carry re-derivations of items 1–5 in its committed artifact. **The obligation is enforced at the parameter boundary, by value (normative; SQ-303 resolution, 2026-07-26, extended to the occupancy family by SQ-501).** `constitution.set_param` refuses a change whose derivations would not survive it. Both families are screened the same way — re-derive from the proposed parameter set, refuse exactly when the result exceeds the frozen figure the runtime compiles against — and they differ only in *which* figures: 08 §4.1's per-class NAV floors for one, items 1–4's occupancy envelopes for the other. For the **class-floor** keys — `pol.budget_epoch`, `pol.b_gate` and the four `pol.b.*` — the runtime re-derives [08](08-treasury-and-economics.md) §4.1's per-class floors from the *proposed* parameter set and refuses exactly when one would exceed the frozen literal the treasury enforces, rounding the derived floor **up** so the error direction is against the proposal. That is what makes §4.1's paired-**CODE** route usable rather than theoretical: a direction test refuses every raise and every cut unconditionally, so a CODE proposal that has correctly updated the literals still cannot carry its values change through, and the keys are frozen in fact rather than screened. Under a value test the same change simply stops being unsafe once the literals fit — no artifact schema, no pairing record, no verifier, none of which could have delivered the property anyway, since §4.1 states that no governance artifact can move a compile-time floor. For the **occupancy** keys — `epoch.slots`, `mkt.obs_interval`, `dec.window` and `epoch.length` — the same value test applies (normative; SQ-501 resolution, 2026-07-26): items 1–4's envelopes are frozen figures of this document, the runtime re-derives them from the *proposed* parameter set, and the change is refused exactly when one would exceed its frozen figure — see the occupancy rule below. An equal write is not a change and is never screened. `ledger.archive` can only move downward from its one-year K ceiling, so the compiled 2,240-row storage envelope remains safe; its artifact must nevertheless recompute expected retained occupancy. It is therefore **not** screened at the boundary — that safety argument is exactly a reason not to refuse it, and refusing it was an over-rejection the SQ-303 wiring removed. For the six **class-floor** keys — `pol.budget_epoch`, the four `pol.b.*` keys and `pol.b_gate` — the operative control meanwhile is the paired **CODE** proposal [08](08-treasury-and-economics.md) §4.1 requires, since a re-derivation carried only in an artifact cannot move compile-time floors. That substitute does **not** extend to `pol.b_baseline`, which moves no frozen literal (below): for it the artifact obligation is simply unenforced — and it is **not** screened against the §4.1 floors, which would refuse a key the same sentence says moves none of them (SQ-303). The **seven** POL keys are triggers, for two distinct reasons. Six of them — `pol.budget_epoch`, the four `pol.b.*` class keys and `pol.b_gate` — are triggers because [08](08-treasury-and-economics.md) §4.1's per-class NAV floors are **frozen constants derived from them**: lowering `pol.budget_epoch` or raising a `pol.b` raises the true floor above the frozen literal, so without re-derivation the loud §4.2 arming gate would pass a class below its real minimum-viable NAV. **`pol.b_baseline` is the seventh (SQ-305 resolution)**, on the narrower ground that item 5 above expressly carries the 17,329 USDC Baseline commitment among the values item 6 requires re-deriving. Its inclusion is *not* a claim that it moves the four class floors — [08](08-treasury-and-economics.md) §4.3 deliberately keeps the Baseline book outside the §4.1 floor arithmetic and outside `pol.budget_epoch` — but raising it does raise a real standing obligation against NAV on its own dedicated `POL_BASELINE` line, and item 6 promises re-derivation of item 5, not of §4.1 alone. Omitting it left a value that item 5 derives with no trigger obliging anyone to re-derive it; the omission was undocumented rather than reasoned. Books-per-proposal (6), `MaxLiveProposals` (32) and either market-cap constant are not keys — changing them is a CODE change that reopens this document.
**Occupancy admission rule (normative; SQ-501, 2026-07-26).** `constitution.set_param` screens the four occupancy keys **by value**, against the frozen figures items 1–4 publish above; the earlier unconditional refusal of every timing/capacity-key change is superseded, and with it the last direction test at this boundary. The refusal is `BudgetDerivationRequired`, raised exactly when the re-derivation at the proposed parameter set — the amended key at its proposed value, every other key at its live one — exceeds one of:

| # | Envelope re-derived | Frozen figure | Inputs |
|---|---|---|---|
| 1 | `MaxLiveMarkets + (ceil(ledger.archive / epoch.length) + 1)·(epoch.slots·6 + 1)` retained `Markets` rows, and that count × 205 B | **2,240** rows / **512 KiB** | `epoch.length`, `epoch.slots`, `ledger.archive` |
| 2 | `MaxLiveProposals + MaxSettlingCohorts·epoch.slots` vaults, and that count × the pinned 256 B | **52** vaults / **13 KiB** | `epoch.slots` |
| 3 | `market.reap` protocol-position cells (14 instruments × 2 protocol accounts) | **28** cells | none — no §1 key moves item 3 |
| 4 | `(epoch.slots·6 + 1)·ceil(dec.window / mkt.obs_interval)` and `(epoch.slots·6 + 1)·ceil(epoch.length·13/21 / mkt.obs_interval)` observations/epoch | **133,920** / **580,320** | all four |

Every division rounds **up**, so the error direction is against the proposal (R-7); an overflow, a zero `epoch.length`, a zero `mkt.obs_interval` or an unreadable registry row refuses rather than reading as "no envelope was breached" (G-1). Equal writes are not changes and are never screened, and the 13 §1 bounds, max-Δ and cooldown checks run **before** the screen, so an out-of-registry value still fails as an ordinary registry violation.

**In-flight state (normative; SQ-501 amendment, second revision).** The re-derivation above reads the proposed registry, and a registry is not the live chain. Whether a key's proposed value is already in force depends on how the *code* consumes it, so the screen composes the registry with what is actually in flight, and each key's classification is **audited against its consumer** rather than assumed:

| Key | Consumed | Evidence | Consequence for the screen |
|---|---|---|---|
| `epoch.slots` | **Pinned** | Read only at qualification to decide admissions; thereafter the cohort's book count is fixed state (the post-qualification `Proposals` working set, §4's `MaxLiveProposals`). No consumer scales an existing cohort's load off the registry value. | A reduction binds only cohorts that form later, so the screen takes the **larger** of the proposed value and the largest live cohort. |
| `epoch.length` | **Pinned** | Snapshotted per epoch into the epoch clock and staged next length, per proposal into its schedule (with `decide_at`), and per cohort into its creation-time length; try-state binds the three together. | As above: the screen takes the **larger** of the proposed value and the largest length in flight. |
| `mkt.obs_interval` | **Live** | Re-read on every observation, both for the crank cadence and for the coverage requirement at decision-grade evaluation. | The proposed value *is* the live value, and it applies to every book already trading. |
| `dec.window` | **Live** | The decision-critical crank classification computes `decide_at − dec.window` from the **live** registry on every crank, and that classification is what draws on the metered `keeper.budget_epoch`. The TWAP *accrual* boundaries are separately pinned when the window is registered at Seed, so §3.1's schedule-pinning holds for accrual and **not** for the metered classification item 4 sizes. | As above — and this is the correction: an earlier revision of this paragraph claimed `dec.window` was pinned, which the code does not implement. |
| `ledger.archive` | **Live** | Read at reap as `terminal + archive delay`. | Only an input to item 1, never a trigger; its one legal direction (down, from the K ceiling) shortens retention. |

Two consequences follow, and they are why the fix is a composition rather than a list of forbidden transitions. **First**, a *reduction* of a pinned key is credited by a registry-only screen before it has materialised: lowering `epoch.slots` 5 → 4 and then `mkt.obs_interval` 10 → 9 each pass such a screen (`25 × 4,800 = 120,000`) while a still-live five-slot cohort needs `31 × 4,800 = 148,800`. **Second**, that unmaterialised saving can be laundered through a key whose *safe* direction is the other one: from the same state, `mkt.obs_interval` 10 → 11 is genuinely safe and admitted, and it manufactures exactly enough registry headroom for `dec.window` 43,200 → 51,840 (its 20 % max-Δ) to read `25 × ceil(51,840/11) = 117,825` while the live cohort immediately incurs `31 × 4,713 = 146,103`. Any live-consumed key has that shape in whichever direction increases load, so enumerating transitions closes instances while composing against reality closes the class.

The screen therefore takes the **worst case over what is live**: the book count from the largest cohort in flight (bounded — the post-qualification working set is `MaxLiveProposals`-bounded and the cohort schedules are bounded by `MaxSettlingCohorts`), the epoch length from the longest in force, and the three live keys at their proposed values. An in-flight maximum that cannot be established — state unreadable, or a bound already exceeded — is a **refusal**, never a fallback to the registry value, which is the defect itself (G-1). With the book count taken from reality the value test is *exact* for the binding leg rather than merely conservative: every cohort shares one live `ceil(window / interval)`, so `books_max × ceil(window / interval)` is the true worst-case decision-critical load. No direction rule is needed for any key, and none is imposed.

Only **item 4** carries this exposure, and the other three are stated rather than guarded so that no reader mistakes their absence for an oversight. **Item 1** cannot be breached by any history of in-bounds values: its **2,240** is the same formula at each input's compiled bound, and an archive window spans at most `ceil(1 yr / 14 d) + 1 = 28` creation boundaries of at most `12·6 + 1 = 73` books each, so `196 + 28·73 = 2,240` dominates every reachable history — and `set_param` cannot move those bounds. **Item 2** depends on `epoch.slots` alone, so it admits no compensating route; the screen refuses every raise above 5, hence every historical slot count is ≤ 5 and vault occupancy is ≤ 52 permanently. **Item 3** has no 13 §1 input at all.

Three consequences are normative, and all three are subject to the in-flight rule below. **(a)** `ledger.archive` is an *input* to item 1's re-derivation but not a trigger, for the reason item 6 already gives; it is read live and never screened. **(b)** The genesis registry sits exactly on three of the four figures (52 vaults, 133,920 and 580,320 observations), so each key's admission boundary is its own default: raising `epoch.slots`, lowering `mkt.obs_interval`, raising `dec.window` or raising `epoch.length` is refused **at that registry**, and the opposite direction is ordinary business — subject in both directions to the in-flight composition below, which is what decides the verdict. **(c)** The refused direction reopens the same two ways the class floors do — a **CODE** change that moves the frozen figure (items 1–4 are figures of this document, so that change reopens it), or a compensating amendment of another input, since the test is on the derived value and not on the direction. Two worked cases, both landing exactly on the frozen figure: with `mkt.obs_interval` raised to 20, `dec.window` = 86,400 passes the screen at exactly 133,920, and `epoch.length` = 604,800 — its 13 §1 hard max — passes at exactly 580,320. (Passing the screen is not the same as reaching the value in one decision; the row's own max-Δ still applies, and `epoch.length` carries 10 %.) That second route is what a direction test could not express, and its absence is why these four rows were declaratory before SQ-501: no value of any of them was admissible, in either direction. Screening item 4's **full-window** figure deserves its own note, because [08](08-treasury-and-economics.md) §6.3 states that full-window coverage is deliberately *not* metered — out-of-window observation gaps degrade chart density, never decisions. It is screened anyway, and on the narrower ground that item 6 obliges its re-derivation: the figure sizes the funded `ops.keepers` continuity line, and a published normative figure that silently goes stale under a lawful amendment is exactly what this boundary exists to prevent. It is also, at the current registry, the only envelope `epoch.length` can breach: item 1's **2,240** is that same formula evaluated at each input's compiled bound (the archive ceiling, the 14-day epoch floor, the 12-slot registry maximum), so every in-range parameter set lands strictly under it and item 1 binds only if one of those bounds is later widened. Omitting the full-window figure would therefore leave `epoch.length` screened by nothing at all.

7. **Chain-served history budget:** `RecentCohortSummaries` max-encodes to **1 + 32 × 158 = 5,057 B** (the one-byte SCALE compact collection-length prefix plus 32 measured `CohortSummary` values, whose `MaxEncodedLen` includes the 12-proposal hard maximum) + 8 TWAP checkpoints × 196 active markets at the [04 §7](04-markets-and-pricing.md) spec shape (8 × (4 B `u32` block + 32 B two-limb cumulative) + 1 length byte = 289 B/market; terminal observation deletes that book's checkpoint and decision-window auxiliaries before releasing its active slot) = **5,057 + 56,644 = 61,701 B ≈ 60.3 KiB** of always-served light-client state, within the ≤ 70 KiB D-6 layer-1 budget stated in [02](02-integration-contract.md). The 2,240 retained-book bound does not multiply this history envelope because terminal rows carry no auxiliary series.

---

## 6. Resolves

| Finding | Resolution in this document |
|---|---|
| B-med IntakeQueue vs MaxLive / D-10 | §4: 64 = Submitted/Screening intake bookkeeping, 32 = post-qualification non-terminal working set, disjoint internal maps; frozen direct-read `IntakeQueue` remains Submitted-only; joint-satisfiability argument; `MaxLiveMarkets` 196 = 32·6+4 bounds active/POL obligations while `MaxStoredMarkets` 2,240 independently bounds archived books — every budget re-derived in §5 |
| X-11h (backend side; with X-11e) | Reading rules: every FE-rechecked value (position bound, per-trade min/max, `MinSplit`, all §1 keys, all K constants) is constants-API- or storage-readable per [02](02-integration-contract.md); the FE hardcodes nothing |
| B-6 (owned by [04](04-markets-and-pricing.md)) | §3.3: V1 = 512.494795136, V5 = −3.074969 frozen; CI regeneration required |
| B-low drift batch | §3.1 Trade = d5–d18 with fractional offsets; §3.1 maturity B+288,000; §3.2 κ per 10-block interval (ADR-11 corrected), maker-loss ≈ 180 USDC; §3.4 latency reconciliation pointer |
| B-med epoch.length | §3.1: offsets as fractions, next-epoch effectivity, creation-time schedule for in-flight cohorts, 14-day floor |
| B-med Positions map (owned by [03](03-conditional-ledger.md)) | §4: key order `(PositionId, AccountId)`, 0.1 USDC deposit, 64-cap + protocol exemption recorded as the single normative values |
| B-med keeper budget / D-15 / D-16 values | §1: `keeper.budget_epoch` 12,000 (floor 6,000 kernel-justified), `collator.comp_epoch` 2,000, `intake.*`, `fee.vit_usdc_rate`, `pol.b_baseline`, `orc.bond_bps`, `orc.window` 72 h, watchtower/attestor constants — all keys named with owning docs |
| D-7 | Reading rule 5: no EMERGENCY parameter rows exist |
| D-17 / X-11a/b (with [02](02-integration-contract.md)) | §3.5: identity constants frozen once, jointly owned |
