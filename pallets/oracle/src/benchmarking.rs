//! Benchmark harness placeholder for `oracle`.
//!
//! B5 replaces these zero-cost stubs with PoV-calibrated generated weights.

/// Returns the number of benchmark cases currently covered by this shell.
pub fn benchmark_case_count() -> usize {
    1
}
