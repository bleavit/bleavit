# Decisions — 2026-08-14

## Lighthouse 13.4.1 retains FCP and the published reference profiles
authorized_by: Session, under R-1 (spec rulings delegated 2026-07-25). Doc 10 is not doc 02, so no `INTEGRATION_CONTRACT_VERSION` bump. The derived `docs/design/` copy is synchronized
docs_touched: 10 §9.4; `app/tools/render-budget/check.ts`; `docs/design/claude-design-kit/03-frontend-architecture-VERBATIM.md`

SQ-1006 is resolved by adopting Lighthouse 13.4.1. Its default performance config still contains
FCP and LCP but no `first-meaningful-paint`, and its desktop and mobile presets retain the dimensions,
pixel ratios and throttling assumptions §9.4 publishes. FCP therefore remains the gate's measurable
proxy and no timing or hardware budget changes. Lighthouse's declared minimum requires the repository
Node floor and both digest-pinned CI containers to move from 22.18.0 to 22.19.0.

## Patched npm versions replace advisory waivers
authorized_by: Session, under 14 §3.6 TH-44 and 15 §4.5's fail-closed supply-chain regime
docs_touched: `app/package.json`; `app/pnpm-lock.yaml`; `tools/ci/npm-advisory-waivers.toml`

Lighthouse 13.4.1 no longer resolves the vulnerable `extract-zip` path or old
`@opentelemetry/core` version, so both version-specific waivers are deleted. The same full scan found
GHSA-2v37-7h3g-55p8 in PostCSS's `nanoid` 3.3.17 path; the workspace now overrides `nanoid` to the
smallest patched 3.x release, 3.3.18. The override is exact so a future lock refresh cannot silently
restore a vulnerable 3.x version.
