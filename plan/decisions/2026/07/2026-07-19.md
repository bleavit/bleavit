# Decisions — 2026-07-19

## G0/S4 Phase-0 δ recalibration (V-12) + sim-faithfulness…
authorized_by: User AskUserQuestion ruling (profitable-exploit criterion + genesis-only propagation), 2026-07-19; no 02 change ⇒ no contract bump
docs_touched: 13 §1; 15 §4.9; 05 §5.6; 08 §5.5; 04 §12; 09 §2.5; reference-model (`treasury.py`, corpus); simulation (`proposals.py`, `calibration.py`); `constitution-core`, `epoch-core`, `pallet-constitution`

G0/S4 Phase-0 δ recalibration (V-12) + sim-faithfulness decoupling + 15 §4.9 profitable-exploit exit criterion.

Three linked changes completing the 15 §4.9 δ-calibration under a user AskUserQuestion ruling. **(1) `dec.delta` recalibrated per class** — 0.015/0.025/0.040/0.060 → **0.0375/0.0375/0.060/0.090** (13 §1 genesis values; ×1.5 for TREASURY/CODE/META, **×2.5 for PARAM** — the first full 10k at PARAM ×1.5 left exactly one profitable flip, **PARAM id=3568**, a marginal near-boundary proposal on an unbacked capability-envelope prize (SQ-173); PARAM's small floor makes such flips cheap, so it needs ×2.5 to reach 0 profitable exploits, proven via the exact bracket logic 0.35× → 6.06× prize; the 0.005 `DECISION_DELTA_FLOOR` kernel floor left unchanged, genesis-only per the ruling) — driving every class's decidable-harm false-pass < 1 % and making the profitable marginal-flip exploits (CODE-5996 and PARAM id=3568) unprofitable griefing. Propagated across doc-13, `reference-model` `DELTA_FLOORS`, `constitution-core` genesis (37.5M/37.5M/60M/90M on the 1e9 grid), `EpochParams::DEFAULT` + its two 13 §1 tripwire tests + the two PARAM-margin decision tests, and design-kit 05; reference vectors regenerated (one `decision_delta` cell crossing the 0.10 cap); worked examples 04 §12 (TREASURY) and 09 §2.5 (META) re-worked to the new δ. **(2) Sim-faithfulness decoupling:** the synthetic proposal true-effect magnitude now anchors to a frozen `EFFECT_SCALE` (= the pre-calibration floors) in `simulation/proposals.py`, DECOUPLED from `decision_delta` — the prior `magnitude = decision_delta × stratum` made "calibrate δ" scale-invariant (a model bug: a proposal's welfare effect is a fact about the proposal, not the market's decision margin). Population byte-identical at the old calibration; δ now flows into `decide()` as a genuine lever. **(3) 15 §4.9 profitable-exploit exit criterion:** the Phase-0 sub-3P/`AttackCost̂` gate scores a wrong-PASS flip as a failure only when *realized* attacker cost < prize (profitable capture); a flip whose realized cost is ≥ prize but < `3·InCapPrize` is deep-pocket griefing (TM-18), δ-immune for thin-capture/gate-suppression strategies, recorded as a diagnostic (`calibration.py` griefing-by-cost exclusion, extending the wrong-reject/th2/th7 exclusions). The `3·InCapPrize ≤ AttackCost̂` step-9 certificate and the 05 §5.6 `ManipFloor̂ ≥ 3·InCapPrize` Phase 3–4 sizing target are UNCHANGED; 05 §5.6 + 08 §5.5 gained reconciling cross-references distinguishing the runtime 3× margin from the Phase-0 realized-profitability line. Liveness cost reported honestly (honest clearly-good adoption ~28 % → ~16 %; not the R1 reject-everything mode). Spec-reviewer (0 blocker/2 major/3 minor) drove the `EpochParams::DEFAULT`, worked-example, and doc-reconciliation fixes; the 1× vs 3× realized-cost threshold is **SQ-273** (ratification requested). Deferred latent minor: `effect_scale` `_class_name` normalization (lowercase-only call sites; reverted to keep the in-flight calibration artifact's source digest valid).

## G0/S4 universal PARAM gate extension folded into…
authorized_by: User explicit directive, speaking for both backend and frontend under R-1, 2026-07-19
docs_touched: 00 D-2/D-4; 01 §3; 02 §4/§7/§13; 03 §2.1; 04 §1.1/§9; 05 §4.7/§5.1–§5.4; 08 §2.5/§3–§4.1; 13 §1; 14 TH-4/TH-6

G0/S4 universal PARAM gate extension folded into contract v5 (R2b).

Every PARAM proposal now carries the four-book `(S,C)×(adopt,reject)` gate, making EVERY market-bearing class gate-bearing (predicate `PARAM | TREASURY | CODE | META`). PARAM settles against the same deterministic system-wide `GateBreachFlags` facts as the other classes — a **correlated-harm proxy, not causal attribution or a claim that PARAM capture is closed** (the gate-attacking re-calibration must confirm). PARAM POL commitment 13,863 → 34,657 USDC; runtime NAV arming floor 1,848,400 → 4,620,989 USDC. No structural type, `gate.*`/`pol.b_gate` value, SQ-272 MIN, or `MaxLiveMarkets = 196` changed; the frozen optional-four-ID gate array + four-slot class-floor array already represent PARAM, so the semantic/value change folds into the same pre-genesis **v5** entry (no v6). Tests prove six physical PARAM books, both vetoes, AND that an honest organic-depth PARAM stays decision-grade and Adopts (the R1 reject-everything failure mode is excluded). Residual: SQ-173 (PARAM's unbacked step-9 capability-envelope prize) must land before production PARAM adoption.

## G0/S4 universal TREASURY gate extension + integration…
authorized_by: User explicit directive, speaking for both backend and frontend under R-1 (joint contract sign-off), 2026-07-19
docs_touched: 00 D-2/D-4; 01 §1/§3; 02 §4/§8/§13; 03 §2.1/§6.2; 04 §1/§9; 05 §5; 08 §2.5/§3–§5; 13 §1/§5; 14 TH-4

G0/S4 universal TREASURY gate extension + integration contract v5 (R2a).

Every TREASURY proposal now carries the existing four-book `(S,C)×(adopt,reject)` gate set irrespective of ask/NAV ratio; the former ≤1 % NAV static-classification exception and the 4,620,981-USDC small-Treasury floor are removed — Treasury uses one 55,452-USDC POL commitment and one 7,393,600-USDC class floor. `MarketSet.gates: Option<[MarketId;4]>`, all `gate.*`/`pol.b_gate` values, the SQ-272 MIN aggregation, and `MaxLiveMarkets = 196` are unchanged. `INTEGRATION_CONTRACT_VERSION` bumps 4 → 5 pre-genesis (no deployed runtime ⇒ no migration; 02 §13(3) append-only clause N/A); v5 recorded in 02 §13 history. Validated direction: gated classes hold at 0 profitable exploits under the faithful gate-attacking adversary.
