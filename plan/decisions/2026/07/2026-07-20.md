# Decisions — 2026-07-20

## Batch B6 (ratify 13/15) — 18 rows ratified across five documents, 1 refused, and 9 rows corrected by verification before a word of spec text was written.
authorized_by: Claude (user-delegated batch authority, 2026-07-20); ratification requested
docs_touched: `docs/architecture/05-welfare-and-decision-engine.md`, `docs/architecture/09-execution-upgrades-and-rollout.md`, `docs/architecture/12-release-and-operations.md`, `docs/architecture/13-parameters.md`, `docs/architecture/15-invariants-and-testing.md`, `fuzz/README.md`, `tools/phase-gates/`, `runtime/bleavit-runtime/src/tests.rs`, AGENTS.md, PLAN.md

*Amendments, all making shipped conservative behaviour normative rather than changing it:* **15 §4.6** gained three normative blocks — the registry is the three *tabular* registries (13 §1/§2/§4), 13 §3 being out of scope because its values are chain-identity/supply or aliases of already-covered keys (SQ-157); the enforcement unit is the individual **numeric bound**, not the documentation row, with PB-LEDGER-FREEZE's I-4 drift-flag clause named as a gate rather than a bound (SQ-160); and a limit may be discharged by asserting the **specified boundary behaviour** where it enforces by clamp/no-op/latch/eviction instead of an error, exactly one binding per key (SQ-156). **15 §1's I-22 row was amended in lockstep** — it read "asserts failure", which is false for every clamp-enforced limit and would have left the document contradicting its own §4.6. **15 §4.5** gained the advisory-source rule (RustSec **and** GHSA/OSV, partitioned so the two legs never duplicate, waivers expiring mechanically — SQ-219), the exhaustiveness of the three fuzz areas plus permission to fuzz frame-free cores **provided** the production boundary carries its own hardening and a named runtime regression (SQ-226), and a weights block ratifying the committed-artifact diff gate *including its absolute noise floors and acknowledgement escape* (SQ-147), the pre-charge-and-refund model with its declared-inner-weight fallback (SQ-148), and worst-case heterogeneous batch weighing (SQ-149). **15 §4.9 and 09 §7.1** now state the same Phase-0 criterion: per class independently, **decidable-harm**, strictly below 1 % (SQ-269), with the nine-key calibration set resolved against 13's sim-gated tags so it cannot drift and publication explicitly distinguished from 13 adoption (SQ-268); the 1× profitability line is ratified as the exit threshold (SQ-273). **13** gained `gate.v_min` in reading rule 6 plus a corrected rule 7 (SQ-194), notes on rows carrying no seeded record (SQ-158) and on the unbounded sentinel (SQ-196), and a new §3.4 row for `UndecidingTimeout` (SQ-229). **12 §6.3** gained alert-condition semantics for the Upgrades and Liquidity-floors rows (SQ-265, SQ-266). *Corrections verification forced:* SQ-273's flip count (14 → **62**) and its strategy label; SQ-269's dropped **decidable-harm** qualifier; SQ-227's whole premise (I-13 was already covered by an S1 property suite, so 15 §1's I-13 Verification column was corrected away from "fuzz" instead of a new obligation being invented); SQ-226's stale pin (stable2603 → **stable2606**) and its "cannot link" (policy, not impossibility); SQ-157's `frn.window` example (it is 13 §3.4, and an alias of the covered §2 `dec.extension`); SQ-149's citations (`13 §5.1` does not exist; 09 §7.3 is Preimage discipline); SQ-229's premise (13 has no every-value-needs-a-row rule — that is AGENTS.md, not spec — and the value is not derived from 06 §2.1); SQ-265's union (`MIGRATION_FAILURE_HALT` is not an input); and SQ-268's timing premise (the producer had already frozen and passes). *Refusal (R-10):* **SQ-158** split — `grd.bond` ratified as a kernel constant, but `dis.merit_min` reclassified to batch **X** as code, because ratifying the shipped hardcoded equality to `B_1` would silently delete the independent values-layer raise that both 07 §12 and 13 §1's governance metadata assert. *New finding riding the batch:* `UndecidingTimeout` equals the entrenched track's 7-day prepare period exactly — verified benign under `pallet-referenda`'s `!in_queue` exemption, but now pinned in 13 §3.4 as the normative constraint that it MUST be at least the longest track prepare period. No 02 surface touched, so **no contract bump**; no D-n affected. **Dual review (native `spec-reviewer` 1 blocker/1 major/7 minor + adversarial Codex 2 blockers/3 majors) — every finding fixed in-session, and the reviews earned their keep three times over.** The `spec-reviewer` blocker was self-inflicted and real: appending the SQ-194 rationale sentence to 13 reading rule 6 **broke the G0 Phase-0 gate**, whose extractor bound the class-suffix list with a greedy `$`-anchored `(.*)`, so the added prose was read as phantom suffixes and `tools/phase-gates/tests` went red. Fixed code-side — the suffix list is now matched structurally so rule 6 may carry trailing prose — plus a regression test. Codex's two blockers were both **over-ratifications in my own text**: (1) the unseeded-rows note claimed reading rule 3 is satisfied "by the constants API" for `grd.bond` **/ allowances**, but only `GuardianBond` is metadata-exposed — the delay/rerun/pause/renewal allowances are core constants with no metadata surface, so the exception was narrowed to the bond and their exposure left explicitly unresolved; (2) the sentinel note said that under a sentinel "every inflow is admissible", but `phase3.tvl_cap` and `phase3.dep_cap` gate **independent** checks — one sentinel retires only its own, and containment lifts only when both are installed. Codex also caught a genuine cross-doc contradiction the batch exposed rather than created: **05 §5.2** still stated `gate.v_min = 0.1 · dec.v_min(class)` as an identity while the key is independently amendable within the band, so 05 §5.2 was amended to bind the *live* value. Majors fixed: `sec.prize` is three keys, not "per binding class" (TREASURY carries no prize row); the I-22/§4.6 CI claim was narrowed to what CI mechanically establishes (classification, an enabled marker, lexical binding) with "drives a dispatch past it" kept as the review half; the enforcement unit is the independent *dispatch limit*, not every numeric field; the fuzz MUST was scoped to core types whose production counterpart has a different shape (LMSR fuzzes the production path itself); TM-18 → canonical TH-6. The new `UndecidingTimeout` MUST is now machine-checked by a runtime test rather than left un-gated. One reviewer finding was **rejected as a false positive** (`01 §9` is not a broken citation — §9 spans 258–284 and line 279 is the normative pinning regime, cross-referencing 15 §4.5 back), and one reviewer restatement was itself wrong (the sim artifact is not "0.000000 for all four classes"; it is 0.000/0.145/0.135/0.563 %, all passing). **Corrected again 2026-07-21 after the Codex PR review of #120 (P2 valid, and sharp):** the "rows without a seeded `Params` record" note's second shape had made `[VERIFY]`/sim-gated status the exemption, which is **false** — `phase3.tvl_cap`/`phase3.dep_cap`, the four `xcm.*` trader rates and the three `ops.ct_*` coretime keys all carry those tags *and* live genesis records, so the note contradicted the sentinel paragraph directly beneath it, which relies on the phase-3 caps having seeded hard maxes. Ground truth checked against `genesis-keys.json` and `registry.toml` rather than either reviewer's summary (an earlier review had wrongly called `ops.*` unseeded — there are two `ops` families, and only the nine operating-budget rows are unseeded): the real discriminator is **whether the value gates a dispatch**. All 18 unseeded §1 rows classify `value` or `diagnostic`; every `[VERIFY]`-tagged row that classifies `dispatch-limit` or `param-bounds` is seeded. The note now states that, with the converse made explicit. The same edit removed a second self-contradiction the review caught: the clause exempting values "wholly derived at the consuming engine" would have blessed exactly the `dis.merit_min` shape this batch **refused** to ratify — derivation is now stated as a **defect**, not an exemption, whenever the row still advertises an amendment path, which writes SQ-158's refusal into the spec instead of leaving it implicit.

## Batch B3 (ratify 07) — 10 spec amendments under delegated batch authority; 1 row refused, 1 split-ruled, 2 blocking spec defects ruled.
authorized_by: Claude (user-delegated batch authority, 2026-07-20; user delegated the two judgment calls: \"decide on your own what is best for the protocol\"); ratification requested
docs_touched: `docs/architecture/07-oracle-and-disputes.md`, `docs/architecture/05-welfare-and-decision-engine.md`, `docs/architecture/13-parameters.md`, `docs/architecture/15-invariants-and-testing.md`, `docs/design/claude-design-kit/05-data-naming-and-parameters.md`, PLAN.md

**07 §6.1** gained *Units and rounding* — the `B_1` formula carried neither the `/10,000` divisor nor any rounding direction, so both are now specified, rounding **up**, resolved in the direction of custody on the I-4/I-28 principle (over-custody is dust, under-custody an unbacked claim; SQ-260) — and *Per-game freezing of `B_1` and `R_max`* (SQ-249). §13's freezing line extended from escrow movement to parameter amendment; 13 §1's `orc.bond_bps` row, §6.2's escalation table and §15.2's mapping row all restated consistently, and the derived design kit re-trued. **07 §7** gained the missing `ack_observed` and `reap_epoch` call rows, a funding line on `crank_close`, five errors and five normative notes — *Fixed windows and quorum* (SQ-74), *Watchtower acknowledgment and its rebate* (SQ-121), *Crank funding lines* (SQ-116), *Consumption model* (SQ-72) and *Milestone normalization* (SQ-71) — plus a storage-table correction (reap is time-gated, not consumption-gated). **05 §4.3** now reads `min(1, milestone points ÷ target)`, and **05 §4.3's MetricSpec field list and `register_spec` rejection rule were amended to own `target`** — 07 had declared a field in a doc it does not own, which the adversarial review caught. **15 §1** gained **I-28** (registry bond custody, `≤` mirroring I-4), added `pallet-registry` to the try-state coverage rule, and moved the range to I-1…I-28. **Two blocking defects in the oracle escalation ladder were ruled and amended** (found while designing SQ-293's fix, which proved the ladder unimplementable as written): **§5.3** gains `counter_report` as a consenting signed call with non-defense resolving the round against the reporter, and forbids any crank from posting collateral on a party's behalf — §6.2 priced rounds 2–3 \"(each side)\" while no call let the reporter post their half; **§11(1)** now **retains** the round non-money-bearing until the terminal verdict instead of removing it, because removal destroyed the very stack §11(2) and §11(4) dispose of and would silently refund a griefer. **No contract bump, verified against 02 §1:** the contract surface is enumerated there in ten items — SCALE primitives, `FutarchyApi` + view types, the frozen event schema, storage items the frontend reads, chain-identity constants, constant/parameter binding, the bootnode requirement, test artifacts, the `ReleaseChannel` key and change control. **Extrinsic call lists are not among them**, and 02 carries no calls/dispatchables section for any pallet, so adding `counter_report` to 07 §13 is a 07-only amendment. The §11(1) retention reuses existing state, and 02 §6 freezes only registry *event* names. **Refusals (R-10):** SQ-70 refused outright (every clause failed verification; the shipped path is the *less* constrained one); SQ-116 split-ruled. **Adversarial review of these amendments caught one blocker of my own** — the registry ack row had been given `WindowAcknowledged`, frozen in 02 §7.2 for the *oracle*, deliberately unemitted by the registry, and pre-empting open contract row SQ-68; corrected to `—` with the contract question left open.

## B2 closure — R-6 repair round: three blockers in the same day's own amendments, fixed.
authorized_by: Claude (user delegation, 2026-07-20: "Decide on your own what is best for Bleavit"); ratification requested
docs_touched: 05 §1.3/§2.1/§5.5; 06 §5.4, §7; 09 §1.1(3), §1.2(5), §2.1(7), §2.4, §2.5, §3.2(2), §3.2(d), §9; 11 §11.5; 14 TH-28; 15 I-19; `deploy/runbooks/RB-UPGRADE.md`; `chopsticks/README.md`

The compliance review of the B2 rulings returned 22 deviations (3 blocker / 12 major / 7 minor). Recorded here because two of them were spec defects I introduced, not pre-existing ones. **(1) The SQ-97 ruling opened a hole.** As first written, the for-cause revocation clause was *conditional* ("if those attestations are to be invalidated") and never mentioned recall, while the dispatch check had already been relaxed — so two attestors ejected for adjudicated-false attestations could still carry an upgrade to execution on records that can no longer be challenged, the 72 h window having closed. Strictly weaker than the roster read it replaced, and precisely the 14 TH-28 outcome. Rewritten as a **mandatory MUST** covering ejection and declared-cause removal, atomic with the departure, per-record event, explicitly load-bearing for the relaxation and forbidden to ship without it; plus a new rule that a departure must not foreclose an already-open challenge window (unseating an attestor currently makes their in-window attestations unchallengeable by anyone). **(2) The new try-state rule was unsatisfiable.** `PendingUpgrade.is_some() ⇒ anchor present` fails continuously on a healthy chain, because `PendingUpgrade` is written at `execute()` and the anchor at application — the ≥ 72 h `DescriptorLeadTime` gap the same bullet describes. Inverted to one-way in the other direction (*anchor present ⇒ latch ∨ halt source ∨ live cursor*), with an added clearing clause for the `type Migrations = ()` case where no completion callback ever fires and the anchor would otherwise leak. **(3) R-1 consistency was broken.** 15 I-19, 14 TH-28 and 11 §11.5 row 6 still stated the live-registry reading the ruling forbids; all three amended. The `ChallengeStatus` polarity fix had also been left as a remote reading-directive over five sites still written backwards (05 §1.3/§2.1/§5.5, 15 I-19, 09 §1.1(3)) — those are now trued in place. Majors fixed with them: 09 §2.1(7)'s abort arm still cleared a checkpoint that cannot exist at abort time; §2.5's worked example and §9's Resolves row still described the retired formulation; 06 §7 named three different binding points for one check (bound uniformly to queue time, dropping a wrong T5 citation); 06 §5.4's repair claim was overstated (rule (ii) binds the failure test only, and repairs two of the ordering paragraph's three clauses, not all three — the third is SQ-310); 09 §3.2(d)'s `max_steps ≤ MIGRATION_STALL_BLOCKS` had to become **strict**, since at equality this backstop fires before the SDK budget it is meant to sit behind; (d)'s stated harm was corrected from "opens the expedited lane" — unreachable, by SQ-309's own argument — to the false page and queue freeze. RB-UPGRADE: the reconstructed anchor was **off by one block** (the `UpgradeApplied` block *is* the last pre-migration block, not its parent), the trigger was still defined by the retired byte-equality predicate, and the repair procedure pointed at a lane that cannot be submitted while a cursor exists — now carrying an explicit SQ-309 warning. **No contract bump**; 11 §11.5's change is the mechanical X-11i mirror of 09 §1.2(5), not an independent frontend decision.

## Batch B2 closed — the six refused rows ruled rather than parked, five milestones scheduled, three of the refusals' own claims retracted.
authorized_by: Claude (user delegation, 2026-07-20: "Decide on your own what is best for Bleavit"); ratification requested
docs_touched: 06 §5.4, §7; 09 §1.1, §1.2(5), §2.1(3), §2.4, §3.2(2), §3.2(d); 13 §2; `deploy/runbooks/RB-UPGRADE.md`; PLAN.md

Batch B2 had already refused ratification on six rows and moved them to X. Closing the batch to B1's standard meant answering them, so five parallel spec-reviewer passes re-verified every residual against the code with file:line evidence. **Three rulings the spec could settle directly.** *SQ-97* — 06 §7 gains *What binds where*: live-registry state (the ≥ 3-active floor, distinct-active-signer counting) binds at **qualification**; the dispatch-time re-check is over the committed **records** only. The artifact hash is pinned at qualification (09 §7.3), so a membership change carries no information about the artifact, while a challenge does and is still read; and the shipped reading let one `attestor.set_members` rotation terminally reject every ratified, matured CODE/META mandate the departing member attested, with no re-bind path. Ejection for cause must revoke explicitly, never through an emergent roster read. 09 §1.2(5)/§2.4 restated, and the `ChallengeStatus::Upheld` polarity — which 09 and 06 had attached to opposite subjects — fixed. *SQ-132(d)* — 09 §3.2 gains (d): "stalled" is `now − cursor.started_at > 900`, never a cursor-motion test, because the pinned SDK imposes no cursor-advance obligation and `Cursor = ()` is a first-class idiom; every registered MBM must declare `max_steps() ≤ MIGRATION_STALL_BLOCKS` at integrity-test time. 13 §2's expedited-lane row restored to 09 §3.1's two arms. *SQ-45(c)* — 06 §5.4: the review deadline is the **duration** the text states, not an epoch index; block-denominated is exact and until it ships the index comparison must be strict, because an under-run confiscates bonds for a review still legitimately in progress — the G-1 test set by SQ-79/SQ-285. **One joint ruling.** *SQ-127 + SQ-144* — 09 §3.2(2) rewritten: the PB-MIGRATION anchor is captured at code **application** (guard `on_initialize`, first block after the swap), is `(block_number, block_hash)` with the state root deleted, and is single-homed in a dedicated guard item rather than the queue row (§1.2(13) deletes it first) or `UpgradeAuthorized` (02-frozen at two fields). try-state moves from equality to implication-plus-latch — the equality is precisely what hid the vacuity, since both items were killed together one block before any migration could step. §3.2(2)'s "exactly the migration's writes" claim, false for any authorize-time capture given ≥ 72 h of `DescriptorLeadTime`, becomes true. 09 §2.1(3)'s three-field `UpgradeAuthorized` trued down to 02 §6's frozen two-field spelling. **No ruling needed a contract bump**; none of the amended texts is in 02. **Three claims retracted (R-10).** The SQ-97 refusal's recommended second fix (gate the attestation branch on `grace_end`) would itself have deviated from 05 §1.3 T16; SQ-132(d)'s "will kill a live, lawfully-progressing cursor" is structurally unreachable; SQ-45(a)'s "widening" is fail-closed in production. Each row now records what was refuted and why. **Escalated rather than ruled:** SQ-309 — `KeepStuck` plus a live cursor forces `OnlyInherents`, so 09 §3.2(3)'s recovery lane cannot be included in a block, and §3.2(1) forecloses `ForceUnstuck`. That is a safety-versus-liveness posture question with a D-level flavour and it is deliberately left open.

## Batch B5 ratification (ratify 12 — ops, monitoring, release evidence): 16 spec questions resolved, 6 rulings corrected against the code, 2 strengthened beyond the row.
authorized_by: The user (delegated: "Complete batch B5 fully. Decide on your own.", 2026-07-20); ratification requested
docs_touched: `08` §2.1 · `09` §3.2, §4 · `12` §1.4, §3.1, §6.1, §6.3, §6.5 · `15` §4.5, §5

Every row was verified against the implementation before its amendment was written, and six rows turned out to describe the code inaccurately. **08 §2.1** — the no-fees-before-cliff property is bound to the **fee adapter**, not to `pallet-vesting`'s withdraw-reason set: the shipped `UnvestedFundsAllowedWithdrawReasons = TRANSACTION_PAYMENT` would permit fee payment under a `Currency`-based adapter, and the strictness is emergent from `FungibleAdapter`'s `fungible` semantics, so the exemption set is declared inert and non-authorizing (SQ-109). **12 §1.4** — a **≥ 2 release-key signature floor** from distinct active keys of the current keyring generation, counted after revocation exclusion; deployments MAY require more, MUST state their minimum, MUST NOT go lower (SQ-242 — a new requirement, not a ratification: 12 fixed ≥ 2 attestations but never a signature count). **12 §3.1** — reader discipline for `ReleaseChannel`: reserved bits are frozen zero for the life of the layout, so readers **reject rather than mask**, rejection surfaces as degraded and never as healthy, and readers accept values longer than 168 bytes (SQ-241; settled in 12 because 02 §12 is complete on the writer side — **no 02 edit, no contract bump, contract stays v5**). **12 §6.1** — keeper parameter sourcing: live `Constitution.Params` tracking is mandatory under the precedence override > live > documented default, with live-or-nothing rows where no default is safe (SQ-126 — added text; the row's cited 01 §4.2/12 §6.1 imposed no such obligation). **12 §6.3** — five row semantics fixed: trigger cells name the owning 13 key and evaluate against the live value, a literal being admissible only where doc 12 owns it (SQ-238, half its premise refuted); the Proposal-state row names the **intake** queue (SQ-240); keeper inactivity is daemon-side per-role with its blind spot declared (SQ-124); alert ownership follows the §6.1 row with a tie-break for runbooks spanning two (SQ-239); and the two implicit observables are operationalized (SQ-243). **12 §6.5 + 09 §4** — the coretime quote authority and renewal account become explicit unfilled genesis seats and a Phase-3 entry gate (SQ-264 — strengthened, because fail-closed there is silent). **09 §3.2** — PB-MIGRATION paging begins at the machine trigger, not at activation, reconciling 09 with 12 §6.3's newly marked Upgrades row (SQ-237). **15 §4.5** — normative supply-chain waiver discipline (annotation + exit criterion, pin-forced proof, mechanical expiry and from-scratch re-triage per train bump, blast-radius containment, disclosure in every publishable manifest), written **source-agnostic** so SQ-219 stays open (SQ-135). **15 §5** — publication ordering with "publishable" = the prerelease and canonical requiring operator mirror evidence (SQ-136); chainHead-fixture coverage defined as per-entry metadata presence + layout agreement plus one per-fixture both-transport events proof, explicitly **not** per-variant emission (SQ-137); the env-evidence contract with `tier` frozen and byte-for-byte validation (SQ-139); and suite tiering ratified as an **explicit, bounded narrowing** of §4.7 under four conditions (SQ-202).

## Spec-question cleanup pass — four residuals ruled as shipped, one reclassified out of the backlog, and B1 closed.
authorized_by: User ("Cleanup pass", 2026-07-20), under the standing delegated ruling authority for the SQ backlog
docs_touched: 04 §8.4; 05 §4.8, §5.4; 06 §4 (new scoping paragraph), §5.3, §6.3

Prompted by the user asking why the backlog was gaining rows: *"Why did you add new SQ questions instead of just reducing."* Fair challenge — three of the four residuals raised over the previous two sessions were rulable under the standing delegated authority and should not have been filed. Re-examined and ruled: **SQ-285** (recovery-epoch withdrawal deferral) ratified — the original reasoning misapplied G-1, which constrains what the protocol *does* on a failure path, whereas declining to process an exit is inaction that forfeits nothing; relaxing the guard would be the active choice. **SQ-286** ratified on both halves — the freeze sweep covers `Queued` (force-rejection *is* the status quo, and the alternative needs maturity/grace extension machinery that exists only for the dead-man), and the freeze wins over a concurrent dead-man pause (a solvency anomaly dominates a liveness pause); 09 §1.2(10) is not left vacuous because the keeper sweep is per-proposal and bounded. **SQ-287** ruled for the code against the spec — 06 §4(5)'s 100 % is now scoped to screening-stage *verified* findings, while a decide-time preimage mismatch carries rule 2's 10 %, decisively because 09 §7.3 pins the preimage at qualification so the proposer *cannot* unnote it. **SQ-288** reclassified to milestone **B14**: 03 §7 R-4 is unambiguous and the code simply does not implement it, so there was nothing to rule. Separately, B1's two deferred rows were closed after real verification: **SQ-188** — guardian `force_rerun` sets the *same* durable `Proposal.rerun` flag as T13 despite never passing through the `Rerun` state, uniqueness is enforced producer-side, and the δ+1 pp increment has exactly one consuming site and one consuming window; **SQ-187** — the shared Baseline stores one window per distinct boundary triple with per-proposal ownership on top, so a rerun registers its own window, a sealed earlier window is consumed alongside it rather than blocking it, and no proposal can read another's boundaries. Both were under-documented, not defective.

## SQ-92 + SQ-98 landed as code fixes — the two B1-reclassified defects, plus the spec truing each exposed.
authorized_by: User ("fixing SQ-92 and SQ-98 to fix batch B1"), 2026-07-20, under the standing delegated ruling authority for the SQ backlog
docs_touched: 03 §2.3/§5; 05 §1.3, §2.1 T20, §7; 06 §6.3; `models/tla/proposal/Proposal.tla` (comment only)

**(1) SQ-92 (audit scope A, stranded funds).** 03 §2.3/§5 normatively require the epoch VOID to settle the epoch's Baseline vault at `s = 0.5`, and justify `BaselineState` having no `Voided` variant by it; `void_cohort` never did, so the vault stayed `Open` forever and single-sided holders (whose redemptions require `Settled`) could never redeem, invisibly to every solvency invariant because pair holders still exit at par via `merge_baseline`. Fixed by giving `WelfareOps` a `settle_baseline_void(epoch)` method — welfare remains the sole settlement authority (05 §6); epoch may request a settlement but never choose its score — with the score single-homed as the new kernel constant `futarchy_primitives::kernel::VOID_BASELINE_SCORE` and a `LedgerSettlement::baseline_open(epoch)` precondition making "no vault" and "already settled" silent no-ops (G-1) without swallowing genuine ledger errors. Wiring the settlement also **exposed a latent `pallet-market` defect**, fixed here: `observe_baseline_terminal` latched `SettlementObservedAt` without closing the book (its proposal counterpart does), while try-state requires every observed entry to be `Closed` — unreachable before, because only the VOID path reaches a settlement with the Baseline book still open. A legal `void_cohort` would therefore have produced a try-state violation: the 03 §9 drift class that arms the I-4 flag and PB-LEDGER-FREEZE eligibility under D-9. Docs trued: 03 §5 gained the owning-transition paragraph (epoch-VOID path only; per-proposal `void(pid)` settles no Baseline) and 05 §7 gained item (5) separating "a VOID skips welfare *scoring*" from the one settlement it still performs — the 03-vs-05 contradiction the row flagged. **(2) SQ-98 (crank-order race).** A ledger freeze produced two different ledger outcomes depending on which crank observed the proposal first: `tick` par-voided the vault, `decide_with` resolved it onto the Reject branch — deciding whether Accept-branch holders recovered ½ or nothing. `decide_with`'s freeze arm now shares `tick`'s exact predicate and force-rejects with a par void. Par void is also the only self-consistent choice: the freeze halts `market.crank_observe`, so the measurement path would await a score the freeze itself prevents producing. Docs trued: 06 §6.3's "fail decision-grade (staleness)" wording was replaced — it implied `NotDecisionGrade`, which forfeits 10 % of bond under §4(3) and would fine proposers for an operational emergency — and the precedence cost was documented normatively (the freeze test precedes the static guards, so a freeze forgoes the §4(5) slash; evaluating the guards first would reintroduce the divergence, and this was already race-dependent before the fix). 05 §2.1 T20 and §1.3 now name the freeze as a producer. **(3) Doc hygiene.** Eight references to doc 03 §7.4/§7.5 — sections that do not exist (§7 is "Rounding, dust, fees, ED" with no subsections) — were retargeted to §6.4/§6.5; six were pre-existing, two were mine. Five stale refs in code comments were corrected the same way. **No 02 change and no contract bump:** `BaselineSettled` is an existing frozen event, newly reachable. **Residuals raised rather than ruled:** SQ-286 (should a freeze force-reject `Queued` items or let them wait out the bounded window per 09 §1.2(10); plus the unstated precedence between the freeze sweep and the dead-man hold) and SQ-287 (decide-time preimage mismatch slashes 10 % in code where 06 §4(5) says 100 % — pre-existing and independent), and SQ-288 (03 §7 R-4 requires the ledger sovereign, treasury sub-accounts and book accounts to be genesis-endowed with USDC; `genesis.rs` sets `accounts: vec![]`, so the last full redeemer of the last open vault hits `Token(NotExpendable)` under `Preservation::Preserve` — found while building the SQ-92 runtime fixtures).

## SQ-129 ruled — inflow caps bind before the mint; a cap refusal never traps locally.
authorized_by: Claude (user delegation, 2026-07-20: "decide on your own what is best for the protocol"); ratification requested
docs_touched: `docs/architecture/09-execution-upgrades-and-rollout.md` §5.2/§6.1, PLAN.md

Decided under the user's explicit delegation ("can you decide on your own what is best for the protocol Bleavit?") after batch B2 refused to ratify the row as a drill-routing question. **The defect:** 09 §5.2 promised a `deposit_cap`-refused depositor a self-scoped `claim_assets` recovery that 09 §6.1 and the runtime prove impossible — an inbound message's trap is keyed under Asset Hub's origin, and `pallet_xcm.send`/`execute` are `Nobody` for every origin including sudo, so an ordinary over-cap Phase-3 deposit was permanently unrecoverable from this chain's side. **Ruling: enforce both caps before any local mint** (09 §5.2 amended; §6.1's mint-leg mitigation restated to cover both). The per-account check binds the beneficiary extracted from the inbound program — §5.2's premise that the beneficiary is unknowable pre-mint holds only for the `AssetTransactor` interface, not for the message, which carries `DepositAsset{beneficiary}`. **Why not re-key the trap to the beneficiary (`SetAssetClaimer`):** verified incoherent for a *cumulative per-account* cap — a recovery claim's deposit leg stays metered (`TrapRecoveryInflows::deposit_asset` → `CappedInflows`, `runtime/bleavit-xcm/src/caps.rs:308-316`) by the very cap the beneficiary exceeded, and that cap never clears within the phase, so the holding re-traps on every attempt. **Why not document AH-governance-only recovery:** it would enshrine fund-stranding as intent, contradicting §6.1's own amendment rationale. G-1 requires a refusal to restore the status quo rather than manufacture a worse state; R-7 forbids the reading that strands a claim. Spec-ahead-of-code by design: the bleavit-xcm implementation is owed in batch X and the stranding path stays live until it lands, so SQ-129 remains open. No 02 surface touched ⇒ no contract bump.

## Batch B2 (ratify 06/09) — 10 spec amendments under delegated batch authority; 6 rows refused ratification, 1 deferred to the contract bump.
authorized_by: Claude (user-delegated batch authority, 2026-07-20); ratification requested
docs_touched: `docs/architecture/06-governance-and-guardians.md`, `docs/architecture/09-execution-upgrades-and-rollout.md`, `docs/architecture/13-parameters.md`, PLAN.md

Amendments, all making shipped conservative behaviour normative rather than changing it, except where noted: **06 §3.3** pins the `MAX_NESTED` "≤ 16 calls" bound as a **node** count against one budget shared by the whole filter evaluation, and its reference pseudocode was corrected from the per-`batch` form that generated the question (that form bounds no total and cannot deliver §3.3's own bounded-weight guarantee); 13 §2 carries the note (SQ-31). **06 §6.2** scopes "every activation" to effect-dispatching activations and adds the no-effect rule (SQ-278), and pins activation-expiry bounds including the rule that **an activation record MUST NOT serve as another playbook's trigger source** — this one is a *change*, not a ratification, closing a live widening where `void_in_flight` kept PB-HALT-INTAKE activatable for up to 14 days after a one-shot VOID (SQ-45a). **06 §6.3** pins renewal measurement from the renewal block (SQ-45b). **06 §7** pins the held-bond challenge and slash basis with binding-at-seating, plus the 13 §1 row note (SQ-248). **09 §1.2(11)** pins the decode-depth invariant and mandatory full consumption, with a new 13 §2 `MAX_PAYLOAD_DECODE_DEPTH` row so the I-22 extractor can see it (SQ-225). **09 §2.1(7)** adds the relay-Abort arm (SQ-131). **09 §2.3** pins `updated_at` as last-write-by-any-writer — settled **without a contract bump**, because 02 §12's parentheticals grant no writer exclusive title to an offset and 12 §106 routes write authority to 09 (SQ-133). **09 §3.2** amends the closed trigger list: retires the "post-upgrade `try-state` signal" arm as **unimplementable** (try-state never runs in production block execution on any Substrate runtime), binds the freeze-lift sentence to the guardian playbook freeze, and enumerates the two shipped non-enumerated arms (SQ-132a–c). **09 §5.2** scopes the split-path read to Signed extrinsics and the mint-step gate to issuance-increasing mints (SQ-252, SQ-253). **Refusals (R-10):** SQ-97, SQ-127, SQ-129, SQ-144, SQ-45(c) and SQ-132(d) were reclassified to code (batch **X**) rather than rubber-stamped, and SQ-134 to **C**; in every case code verification contradicted the row's own premise or the shipped path was an active adverse choice rather than a status-quo default (the G-1 test set by SQ-79/SQ-285 in batch B1). No 02 surface touched, so **no contract bump**; no D-n affected. One new row raised rather than resolved: SQ-289.

## Batch B1 (ratify 05/03) — 8 spec amendments under delegated batch authority; 3 rows refused ratification.
authorized_by: Claude (user-delegated batch authority, 2026-07-20); ratification requested
docs_touched: `docs/architecture/05-welfare-and-decision-engine.md`, `docs/architecture/06-governance-and-guardians.md`, PLAN.md

Amendments, all making shipped conservative behaviour normative rather than changing it: **05 §3.3** replaced the ambiguous "prune snapshot e−20" sentence with normative arithmetic anchored on the current epoch index — prune removes `<= current − 20` via cutoff **`current − 19`**, retaining 19 plus one free slot, because a `current − 20` cutoff jams `record_snapshot` and deadlocks settlement into the dead-man (SQ-200; the 20-deep `MAX_SNAPSHOTS` bound governs, consistent with 13 §4's `Snapshots ≤ 20` row). **05 §4.8** gained three paragraphs: the recovery epoch's exact blocked set and observed-clock carry-forward (SQ-90), narrow snapshot-overdue suppression under an active pause with the reachability argument (SQ-254), and straddle semantics binding the *registered market window*, extended in place (SQ-255). **05 §2.1** gained three normative notes: the T4 disposition taxonomy on the principle that confiscation requires a **verified culpable act** (SQ-191), T5/T6 bond-descending/pid-ascending ranking with a per-proposal (not per-cause) single rollover (SQ-91), and the T20 stale-epoch anchor as epoch staleness with a self-clearing latch suppressed under dead-man/recovery (SQ-86). **06 §2.2** gained guard-record reaping ownership — the epoch terminal callback as single, universal owner with **normative idempotency** (SQ-192). **Refusals (R-10):** SQ-92, SQ-98 and SQ-79 were reclassified from ratification to code (batch X) rather than rubber-stamped — code verification contradicted two row premises and the third is a live G-1 question with economic consequences. No 02 surface touched, so no contract bump; no D-n affected. **Corrected 2026-07-20 before merge, after the Codex-connector P2 review of PR #114 (finding valid):** the SQ-90 amendment's open set over-ratified — it asserted withdrawal and expiry stay available during recovery. Re-verified against `epoch-core`: execution callbacks and guardian transitions carry no recovery guard and are genuinely open, but `withdraw`'s `proposal.epoch == CurrentEpoch` guard is **unsatisfiable for the entire recovery epoch** (resume increments the clock to R = P+1 and `submit` is refused throughout), and expiry survives only through the on-demand `expire_or_stale_queue` entry point while the automatic `tick` sweep no-ops. 05 §4.8 now states all three precisely. The design question the correction exposes — whether recovery *should* offer an intake-bond exit — was split out as **SQ-285** (batch X) rather than ratified, on the SQ-79 test: the shipped path defers a fund-releasing exit, so ratifying it would be an active choice against G-1, not a status-quo default.

## Spec-questions triage sweep — three delegated rulings, one production change, no spec edit.
authorized_by: Claude (user-delegated batch authority, 2026-07-20: "delegate batches to me"); ratification requested
docs_touched: none (`docs/architecture/` untouched); `pallets/epoch/src/lib.rs`, `pallets/epoch/src/tests.rs`, PLAN.md

**(1) SQ-267 (criterion-A suite ownership):** crate-level differential consumers (`futarchy-treasury-core`, `market-core`) satisfy 15 §4.4 for the treasury/twap corpus families — §4.4 obliges agreement with the reference model over shared JSON vectors, not a pallet-level replay; the ledger families additionally carry a pallet-level sweep consumer where the spec does demand one. This records a decision that G0 had made de facto. **(2) SQ-230 (SQ-144 label collision):** `SQ-144` refers solely to the PB-MIGRATION checkpoint state-root question; the younger "SQ-144-effects" usage labelled the guardian downstream-effects gap that B1b closed (SQ-143 now resolved), carries no live question and no surviving code marker, so the label is **retired rather than renumbered** — minting a new id for an answered question would have inflated the backlog. **(3) SQ-82 (epoch-0 sentinel):** `pallet-epoch`'s `GenesisConfig::default()` moved `index: 0 → 1` (R-7 fail-closed, pre-genesis, zero migration cost) so a chain spec omitting the `epoch` patch section can no longer seat the live clock on the pre-launch sentinel and inherit the genesis activation relaxation (05 §4.6, I-16); the ambient-sentinel coupling in `welfare-core` is left as the row's narrowed residual rather than refactored under a triage sweep.

## G1 SQ-282 resolution (dead-man on-chain observability) + PB-MIGRATION doc reconciliation (SQ-279/280/281) — spec changes, no runtime-code change.
authorized_by: Session decisions under the user's standing decide-yourself delegation ("Finish milestone G1 … Decide on your own"); SQ-282 was flagged decide-yourself, 2026-07-20
docs_touched: docs: 05 §4.3/§4.8, 06 §6.2, 09 §2.1/§3.2/§7.1, 13 §2, 14 TH-37; env: `zombienet/drills/04-dead-man.zndsl` + `05-coretime-renewal-under-dead-man.zndsl`, `js/{wait-relay-advance,assert-dead-man}.js`, `tools/env/generate-relay-specs.sh`, `zombienet/networks/bleavit-fast-coretime.toml`, `tools/env/suites.json`

**SQ-282:** SDK verification over the stable2606 pin (three checks) is definitive — the parachain runtime cannot observe the relay GRANDPA finalized head (`PersistedValidationData` = four fields, no `well_known_keys` finality key, `cumulus_pallet_parachain_system` surfaces only the relay best/anchor), so a pure relay *finality* stall is not on-chain-detectable and a finalized-head detector input (option b) is impossible on the pin. The 05 §4.3 `F`-signal `[VERIFY]` is closed (accessible field = `relay_parent_number`; `F` is inclusion/liveness — matching the already-correct `relay_parent_gap` detector code, which needed no change), and 05 §4.8 trigger 1 / 13 §2 / 14 TH-37 re-scoped to the observable relay-parent-gap semantics; the finality-stall residual is honestly assigned to an off-chain relay best-over-finalized monitor (SQ-283, owner O5) and bounded by XCM isolation (I-24). Drill 04 re-pointed from the (undetectable) relay-finality stall to a real relay-parent-gap **collator outage** (all collators paused → relay best advances ≥ 48 → catch-up jump engages the dead-man); it asserts the durable engaged signal (PhaseFlags bit 6 + detector `incident_active`), the acute queue-freeze staying unit-tested (`pallets/epoch` `dead_man_pauses_phase_and_rejects_submission`). **PB-MIGRATION (SQ-279/280/281):** behaviour already ruled by SQ-274; 06 §6.2 + 09 §3.2/§7.1/§2.1 reconciled to "no guardian-dispatchable migration-recovery call on stable2606; freeze = automatic `MigrationHalt` execution-queue bridge; FRAME MBM lockdown = the inflow-freeze; retry/rollback = the expedited-CODE lane", plus four stale `(§4.2)→(§3.2)` and one `§4.1→§3.1` reference fixes. No 02 change (the dead-man VIEW surface + PhaseFlags bit 6 are unchanged — only the trigger-condition prose) ⇒ no `INTEGRATION_CONTRACT_VERSION` bump. Spec-reviewer: 0 blocker (the 1 major finality-lag-alert pointer + 2 minor citation defects all fixed).

## G1 drill-09 fast-timing test runtime — SQ-128 implemented (no `docs/architecture/` change; the release runtime is byte-frozen).
authorized_by: User-selected via AskUserQuestion ("Fast-timing test runtime") then "Do the next step", under the standing decide-yourself delegation, 2026-07-20
docs_touched: No `docs/architecture/` change. Code: `crates/futarchy-primitives/{src/lib.rs,Cargo.toml}`, `crates/constitution-core/{src/lib.rs,Cargo.toml}`, `pallets/constitution/Cargo.toml`, `runtime/bleavit-runtime/Cargo.toml`; env: `tools/env/generate-relay-specs.sh`, `zombienet/networks/bleavit-fast.toml`, `zombienet/drills/09-three-unattended-epochs.zndsl`, `zombienet/drills/js/assert-epoch-progression.js`, `tools/env/suites.json`

A default-off `fast-timing` cargo feature compresses the epoch clock for the 09 §7.1 "three unattended epochs" machinery proof so it advances three REAL epochs (genuine Aura+relay consensus, the keeper cranking `Epoch.tick`) in ~minutes instead of the release-cadence 907,200 blocks / ~63 days. A two-Explore-agent coupling audit reduced the earlier ~15-constant estimate to a minimal set: two `futarchy-primitives` kernel floors (`MIN_EPOCH_LENGTH_BLOCKS`, `DECISION_WINDOW_FLOOR_BLOCKS`) plus three `constitution-core` registry seeds (`epoch.length`/`dec.window`/`dec.trailing`), all derived from one gated knob `kernel::FAST_DAY_BLOCKS = 4` ⇒ `epoch.length = 84` (21 × FAST_DAY), forwarded primitives → constitution-core → pallet-constitution → runtime. `BLOCKS_PER_DAY` and every emergency/execution/oracle/dead-man window stay at production values — they cannot fire in a 252-block empty drill and `attack_cost_hat`/`t_dec` is never called, so D-4 economics are untouched. Every `cfg(not(fast-timing))` arm is textually identical to the frozen 13 §1 value; the release/deploy pipelines build default features only, and release-invariance is proven by the `production_epoch_timing_floors_are_frozen` guard test + the unchanged `genesis_registry_matches_13_1_row_encodings` byte-assert + genesis-keys fixture (`cargo test --workspace` never enables the feature). Not a spec change; drill-environment staging against the byte-identical release runtime, analogous to the PB-MIGRATION row below (SQ-276 extended).

## G1 fast-timing extended to drills 04 & 08 — the SQ-128 feature widened to two more kernel constants (no `docs/architecture/` change; the release runtime stays byte-frozen).
authorized_by: User: "Can we also use fast-runtime for 04 and 08? … I don't want to wait 3 days and also not 16 hours", under the standing decide-yourself delegation, 2026-07-20
docs_touched: No `docs/architecture/` change. Code: `crates/futarchy-primitives/src/lib.rs` (two gated kernel consts + guard tests), `crates/constitution-core/src/lib.rs` + `pallets/constitution/src/tests.rs` (gated re-export pins), `runtime/bleavit-runtime/src/tests.rs` (literal → kernel expr); env: `zombienet/drills/04-dead-man.zndsl`, `zombienet/drills/08-expedited-code-under-freeze.zndsl`, `zombienet/drills/js/expedited-code-lane.js`, `tools/env/suites.json`

The default-off `fast-timing` feature now also compresses `futarchy-primitives::kernel::DEAD_MAN_RELAY_BLOCKS` (4,800 → 48; drill 04's dead-man relay-stall threshold) and `DESCRIPTOR_LEAD_TIME_BLOCKS` (43,200 → `3 × FAST_DAY_BLOCKS` = 12; drill 08's D-14 expedited-CODE lead), so those drills no longer wait the release cadence (~16 h and ~3 days). An Explore coupling audit confirmed both compress in isolation: `DEAD_MAN_RELAY_BLOCKS` has one production consumer (`pallets/epoch` `observe_dead_man`), `DESCRIPTOR_LEAD_TIME_BLOCKS`'s consumers all compute relative to it, and neither is tied by any invariant/property/try-state/TLA/limit-coverage to a magnitude — the two hardcoded `assert_eq!(…, 43_200)` re-export pins were `#[cfg(not(fast-timing))]`-gated and a `Some(4_800)` runtime-test literal rewritten to the kernel expression (R-4). `DEAD_MAN_RELAY_BLOCKS`'s fast arm is an independent floor (48), deliberately NOT day-scaled — faithful ⅓-day scaling would underflow to ~1 relay block and false-latch. Every `cfg(not(fast-timing))` arm is textually identical to the frozen 13 §2 value; `production_epoch_timing_floors_are_frozen` now also pins 4,800/43,200 and `chain-spec-builder --verify` re-ran `EpochParams::validate` on the compressed wasm. Not a spec change; drill-environment staging against the byte-identical release runtime (SQ-128 extended). **Drill 04's first-ever run proved the timing (16 h → 8 min) but surfaced SQ-282**: `observe_dead_man`'s relay-parent-jump detector does not fire on a relay-*finality* stall (the open 05 §4.8 `F`-signal `[VERIFY]`), independent of the compression.

## G1 PB-MIGRATION drill staging — the SQ-274 mechanism, with no production fault-injection surface.
authorized_by: Session decisions under the user's standing decide-yourself delegation ("implement milestone G1 … Decide on your own"), 2026-07-20; ratification requested (SQ-274)
docs_touched: `pallets/execution-guard/src/lib.rs` (+ `mock.rs`/`tests.rs`), `tools/env/generate-relay-specs.sh`, `zombienet/networks/bleavit-migration.toml`, `zombienet/drills/06-pb-migration.zndsl`, `zombienet/drills/js/pb-migration.js`

SQ-274 resolved (see its SQ row): the drill exercises the 09 §3.2 PB-MIGRATION guardian recovery path by staging the guard trigger `MigrationHalt = true` at genesis, decoupled from a live FRAME migration cursor (whose multi-block-migration lockdown would pause the very guardian workflow the drill runs — the B12-flagged unsatisfiability). Two implementation decisions: **(1)** a new **`pallet_execution_guard::GenesisConfig::migration_halt: bool`** field (defaults `false`; every production preset leaves it so — the shipped chain never boots halted) engages `MigrationHalt` at genesis when set, letting the drill spec stay a PLAIN chain spec the pinned zombienet can register/schedule (a raw storage-injected spec is not schedulable by it). No production `migrations.force_failure`/`retry` surface exists or should (R-7); no dispatch/event/error/call-name change; genesis-only and fail-safe (engaging a freeze is the G-1 conservative direction). **(2)** the drill genesis-funds all seven seeded guardian seat bonds by MOVING VIT out of the largest ops stand-in (Alice/Bob), never minting, preserving the exact 08 §2.1 1,000,000,000-VIT supply / 150M-ops / 200M-founding totals the deploy validator enforces. Both are drill-environment staging against the byte-identical release runtime (SQ-276 extended).

## Batch B4 (ratify 04/08) — 12 rows ratified under delegated batch authority; 8 refused and reclassified to X; no 02 surface touched, so no contract bump.
authorized_by: User (delegated ratification-batch authority, 2026-07-20; owner for both backend and frontend under R-1)
docs_touched: 03 §5.4; 04 §2, §7, §8.3; 05 §4.7; 08 §1.1, §1.2, §1.3, §1.4, §4.1, §4.4, §6.3, §7; 13 §5

Every row was code-verified before amendment. **08:** §1.1 gains *Where per-line budgets bind* (funding is allocation, consumers enforce per-epoch budgets); §1.2 gains *INSURANCE is outside NAV* (not a summand of `NavView.total`; excluding it is the fail-static direction) — the accounting half of SQ-207 only; §1.3 gains *Enforcement layering* (the per-proposal `trs.cap_proposal` guarantee is the decide-time **aggregate**, re-derived from the call batch and failing closed when not statically sizable; the per-call check is a backstop); §1.4 writes `KeeperBudgetLow { remaining: Balance }` and records that neither keeper event is in 02; §4.1 gains *Rounding and tolerance* (floors are frozen constants mixing three conventions, normative content is the direction — each ≥ the exact requirement, ±10 USDC conformance; multi-slot rows are illustrations, not gates) and *Freeze and re-derivation*; §4.4 gains *Reruns are not budget-charged* (exposure structurally bounded at 2× by the one-way `delayed_once`/`rerun` flags); §6.3 gains the closed decision-critical list with an explicit ORACLE-line carve-out, and *Exhaustion is effective, and it latches*; §7's griefing table retires the SQ-231 churn framing for held contest capital. **04:** §2 gains the normative *Reap ordering* rule (terminal markers are swept state; the market-side latch is the predicate); §7 pins `k = max(1, ⌊Δblocks / mkt.obs_interval⌋)` with inward bound rounding; §8.3 replaces "reaped with the market" with the 02 §7.4 retention rule. **03:** §5.4 gains the companion rule that terminal markers must never be another pallet's predicate. **05:** §4.7 gains *Recording cardinality* (`record_daily_gate` repeat-tolerant, rebate state-change-gated). **13:** §5 item 6 adds `pol.budget_epoch`, the four `pol.b` class keys and `pol.b_gate` as `RederiveBudgets` triggers — the rider without which SQ-56's freeze ruling would have left the §4.2 gate armable below the true floor.
