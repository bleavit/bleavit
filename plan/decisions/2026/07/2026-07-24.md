# Decisions — 2026-07-24

## SQ-324 — Baseline funding is standing, physical…
authorized_by: User standing autonomous protocol delegation; R-1/R-7 conservative reading of shipped behavior, 2026-07-24
docs_touched: 04 §1.1/§1.2/§2/§8; 05 §5.3/§7(6); 08 §4.3; PLAN.md

SQ-324 — Baseline funding is standing, physical provisioning is lazy.

The runtime's `open_markets` adapter is proposal-keyed, so an epoch with zero qualified proposals cannot and does not create a Baseline vault/book. The conservative ruling is to preserve that shipped behavior: the logical Baseline obligation and dedicated `POL_BASELINE` funding remain outside `pol.budget_epoch`, while the physical book is created at Seed only when the first qualified proposal opens markets. 05 §5.3's carry fallback and §7(6)'s absent-vault no-op make the proposal-free epoch well-defined; the one-book capacity envelope remains a worst-case bound. This clarifies 04/08 only, touches no frozen 02 surface and requires no contract bump.

## A13 security terms are epoch-owned and frozen at…
authorized_by: User standing autonomous protocol delegation; conservative R-7 implementation of ruled SQ-173/SQ-270/SQ-300 sequencing, 2026-07-24
docs_touched: 05 §5.2/§5.6; 08 §5.2–§5.4; 13 §1; PLAN A13; no 02 change

A13 security terms are epoch-owned and frozen at qualification; no contract bump.

Implemented the ruled SQ-173 ownership shape as an internal `ProposalSecurityTermsOf` map rather than changing the frozen `Proposal` layout. The certificate is captured atomically at qualification, projected into decision sizing and POL seeding, retained through execution/measurement, and reaped with terminal state; checked P_ref arithmetic scales `pol.b` and δ with claimant-adverse rounding. Existing TREASURY ask derivation is the only certified runtime prize today. PARAM/CODE/META capability-envelope valuation remains fail-closed and is explicitly not replaced by the published Phase-0 candidates until its payload-specific envelope proof lands.

## SQ-384 least-authority capability ruling (A13).
authorized_by: User standing delegation under R-1/R-7
docs_touched: 06 §3.2; 08 §1.2/§1.4; constitution core/runtime

`sweep_insurance` is an INSURANCE→MAIN inflow with no budget line; a dedicated trailing `Capability::InsuranceSweep` now authorizes it separately from `TreasurySpend`, preserving existing SCALE discriminants. 06 §3.2 and 08 §1.2/§1.4 are aligned; no 02 or frontend surface changed (contract remains v11 on this branch).

## SQ-303 temporary control ruling (A13).
authorized_by: User standing decide-yourself delegation under R-1/R-7; 2026-07-24; valid Codex connector review on PR #147
docs_touched: 13 §5 item 6; 08 §4.1; constitution core/pallet/runtime

The final re-derivation artifact is not yet representable or verifiable, so G-1 requires a conservative admission seam now: reject every timing/capacity change, `pol.budget_epoch` lowering, and POL-floor raising direction; retain ordinary bounds/cooldowns and safe POL directions. The predicate is enforced in both the frame-free constitution oracle and production runtime so differential/reference-model consumers cannot bless states the pallet rejects; `BudgetDerivationRequired` remains append-only and does not change the v11 integration contract. The temporary brake is removed only after the artifact schema, verifier, cross-proposal pairing lifetime and execution ordering exist; SQ-303 stays open for that successor.

## A13 REWARDS custody-sync amendment (PR #145 Codex…
authorized_by: R-1: the code-backed behavior was already shipped in A13; this closes spec drift surfaced by the valid connector review.
docs_touched: 03 §7 R-4; 08 §1.4/§2.1; 13 §3.5

A13 REWARDS custody-sync amendment (PR #145 Codex review).

The proposer-reward implementation uses a dedicated `REWARDS_` real-USDC pot and custody-syncs `fund_budget_line(REWARDS, amount)` from `MAIN`; the pre-existing 08/13/03 account and custody prose named only KEEPER/ORACLE. The normative set now includes the REWARDS derivation and the same preservation/line≤pot rule. No 02 surface or contract bump is involved.
