# Decisions — 2026-07-17

## G0: the Phase-0 exit-gate evidence contracts.
authorized_by: Session decision under the user's standing delegation this session ("Don't ask questions. Decide on your own."); producer-side shape flagged to S4 via SQ-268/SQ-269 before any freeze
docs_touched: none (code + PLAN)

G0 ships `tools/phase-gates/check-phase0-exit.py`, which publishes **`bleavit.phase0-evidence.v1`** (per-criterion legs with executed-test counts, corpus-family attestation, tree/HEAD binding, overall `phase0_exit`) and freezes the **consumer** side of **`bleavit.sim-calibration.v1`** — the S4-owned artifact (15 §4.9): full `git_commit` **equal to the checked HEAD** (conservative fail-closed: stale sim evidence cannot qualify newer code), `synthetic_proposals ≥ 10⁴`, per-class `false_pass_rate` each strictly < 0.01, `attack_cost_validation`, and a `calibration` map covering the doc-13 Phase-0 sim-gated key set (δ per class, `pol.b_baseline`, `sec.prize.*`, `sec.flow_cap` — superset reading, SQ-268) within 13's hard bounds and unsigned domains. Absent artifact ⇒ `pending-s4`; invalid ⇒ `fail`; a dirty tree refuses to produce evidence (matches the B7 `run-evidence.py` model). Tooling contracts, not spec text — 09 §7.1 "published evidence" left undefined; precedent: B7's `bleavit.env-evidence.v1`.

## 13: `phase3.dep_cap` sim-gated tag made self-contained…
authorized_by: Session decision under R-1 (genuine ambiguity in the owning doc; internally consistent — mirrors the sibling row verbatim) + the user's standing delegation this session
docs_touched: 13

13: `phase3.dep_cap` sim-gated tag made self-contained (clarity, non-semantic).

The row's Default-cell annotation `*(sim-gated **[VERIFY]**)*` was the doc's only *unqualified* sim-gated tag — its Phase-3 context lived in the adjacent `phase3.tvl_cap` row and a different column's "as above", which the G0 gate's adversarial review showed classifiable only by accident. Amended to the tvl_cap row's exact wording, `*(sim-gated **[VERIFY before Phase-3 arming]**)*`. No value, bound, owner, or D-13 semantics changed; 09 §7.1 already places both `phase3.*` caps behind phase-gate raises. Enables strict tag-cell-only phase classification in `check-phase0-exit.py`.

## B10: 04 §7 checkpoint-series spelling aligned to 13…
authorized_by: Session decision under R-1 (spec-internal naming inconsistency; 13 owns the row), user standing delegation this session
docs_touched: 04 §7; PLAN.md

B10: 04 §7 checkpoint-series spelling aligned to 13 §4's `TwapCheckpoints`.

04 §7 spelled the series `window_checkpoints` while 13 §4's registry row (and now the implementation) use `TwapCheckpoints`; 13 is the bounds home, so 04's prose now cites the 13 §4 spelling. Naming truing only (spec-reviewer minor, B10 dual review).

## B10: 13 §5 items 1 and 7 re-trued after the…
authorized_by: Session decision under R-1 (doc records a measurement the code change superseded), user standing delegation this session ("Don't ask questions. Decide for yourself.")
docs_touched: 13 §5; PLAN.md

B10: 13 §5 items 1 and 7 re-trued after the TwapCheckpoints implementation.

Item 1's measured `MarketBook` `MaxEncodedLen` updated 189 B → **205 B** (the in-book accumulator widened to 04 §7's normative two-limb u256 shape; 196-book map ceiling 39.2 KiB, still far under the ≤ 98 KiB budget and the pinned ~512 B growth ceiling); item 7's "the checkpoint series is not yet implemented — A3-deferred" replaced with the implemented-at-spec-shape statement (the 289 B/market figure was already the spec-derived number and is now the measured one, asserted in the runtime PoV suite). Factual re-truing only — no semantics changed; both figures are runtime-test-asserted.

## B1b/PR #104: 06 §5.4 dual verdict referenda for…
authorized_by: session decision under the user's standing decide-yourself directive — **ratification requested**
docs_touched: 06

B1b/PR #104: 06 §5.4 dual verdict referenda for `delay_once` reviews.

The auto-scheduled review submits BOTH `guardian.ratify_action` and `guardian.uphold_veto` at scheduling (deposits for both fronted from held bonds); enacting either verdict atomically cancels the other referendum and refunds its deposits — closing the connector P1 (the upheld-veto verdict previously had no deterministic path and raced the automatic referendum).

## B1b: 05 §1.4 added — canonical resource-domain key…
authorized_by: session decision under the user's standing decide-yourself directive — **ratification requested**
docs_touched: 05, 15

B1b: 05 §1.4 added — canonical resource-domain key encoding (SQ-172/SQ-183 ruling).

Family-tag byte + first-7-bytes `blake2_256` discriminator over the belief-payload-admissible leaf set; `batch_all`-only payload recursion; set-equality screening (both mismatch directions are a false declaration, 100% slash); collision analysis (over-locking only, never safety). 09/06 cross-refs + the 15 I-11 row updated.

## B1b: 06 §5.4 expanded — review verdict set, deposits,…
authorized_by: same — **ratification requested**
docs_touched: 05, 06

B1b: 06 §5.4 expanded — review verdict set, deposits, slash destination, recall semantics.

`guardian.uphold_veto(action_id)` (ratify track, delay reviews only) is T24's single producer; both referendum deposits fronted from held bonds; deadline-slash proceeds → treasury MAIN net of recall fronting; `guardian.recall(action_id)` removes approvers with vacancy semantics (absolute 5-of-7). 06 §2.1/§3.2 scope rows + 05 §1.3/§2.1 T24 updated.

## B1b: 06 §6.2 substrate note — kernel-enumerated…
authorized_by: same — **ratification requested**
docs_touched: 06

B1b: 06 §6.2 substrate note — kernel-enumerated playbook routines.

The six playbooks' effect batches are kernel routines implementing exactly the §6.2 table (activation-parametric arguments); the values-governed surface is per-playbook availability (`guardian.set_playbook_registered`, guardian track) with all six registered at genesis; a new or amended playbook is a CODE/META change. Pallet-level expiry bounds keep effects kernel-bounded even if the guardian crank stalls.

## B1b: `epoch.veto_upheld` demoted from public extrinsic…
authorized_by: same — **ratification requested**
docs_touched: 05, 06

B1b: `epoch.veto_upheld` demoted from public extrinsic to the runtime-internal review path.

T24 is producible only through `guardian.uphold_veto` enactment; the GuardianHold-gated public call (which would have let guardians self-uphold their own veto) is removed; S5 inventory row removed deliberately; no 02 change.

## S2: guard preimage decode-depth hardening (kernel…
authorized_by: Session decision under R-7 (audit-scope-A "no panics"/G-1 status-quo duty) + R-1 (code-only, no semantics changed; ratification queued as SQ-225), user standing delegation this session ("Don't ask questions. Decide on your own.")
docs_touched: none (code + PLAN; SQ-225 for 09 ratification)

S2: guard preimage decode-depth hardening (kernel constant, no doc change).

S2's decode-fuzz review (both reviewers, blocker) surfaced that `pallet-execution-guard::decode_batch` decoded preimage-sourced batches with the unbounded derived `Decode` — the top-level `MAX_CALLS_BOUND` count guard does not bound *nesting* depth, so a hash-committed ≤`MAX_BYTES` preimage of one deeply-nested call recurses to the wasm stack-height trap (native abort), a G-1/rule-1 violation in audit-scope-A code. Added kernel `MAX_PAYLOAD_DECODE_DEPTH = 256` (`futarchy-primitives`; substrate's conventional extrinsic decode-depth limit, ≫ the ≤4 semantic nesting of 06 §3.3, ≪ the stack budget) and switched `decode_batch` to `decode_all_with_depth_limit`; over-deep input fails closed to `BadPreimage`. Runtime regression `deep_preimage_batch_decode_fails_closed_at_the_depth_limit`; all 42 guard tests still green. Fail-closed and stricter-than-spec. **Observable-behavior scope (PR #92 bot P1):** no *spec-valid* payload changes outcome — a spec-valid batch nests ≤ `MAX_NESTED_LEVELS = 4` (06 §3.3) and decodes far below 256, and any payload nesting beyond 4 is already rejected by the SafetyFilter/domain gate; only decode-bomb inputs change, from a wasm-trap/native-abort to a clean `BadPreimage` (the R-7 improvement). The value is kernel-homed in `futarchy-primitives::kernel` (rule 4) and mirrors substrate's conventional 256 extrinsic decode-depth limit rather than an invented number, but no `docs/architecture/` prose yet *names* the decode-depth invariant — that ratification (and the "before enforcing it" ordering the bot asks for) is **SQ-225**; the enforcement stays live in the interim because R-7 forbids leaving the latent trap in audit-scope-A code.

## S1: 15 §1 I-27 amended — housekeeping sweeps are…
authorized_by: Session decision under R-1 (owning-doc contradiction; the I-26/03 §6.4 reading is the only internally consistent one), user standing delegation this session ("Don't ask questions. Decide on your own.")
docs_touched: 15, PLAN.md

S1: 15 §1 I-27 amended — housekeeping sweeps are dispatchable in `Voided`.

The I-27 row's "exactly five ops" surface omitted the 03 §5.4 sweeps although 03 §5.4 admits them on any terminal vault post-`RedemptionArchiveDelay`, I-26's own text presupposes the sweep ("rounding residue is swept per the dust rule"), and 03 §6.4 routes residual E → INSURANCE after reaping. I-27 now names the post-archive-delay sweeps in the dispatchable set. Resolves an S1 dual-review blocker (model/PT/differential had opposite readings); all S1 artifacts aligned to the amended row.

## S1: 04 §6.1 sell wrapper merges payout-sized, not…
authorized_by: Session decision under R-1/R-7 (plain-meaning reading; strictly weakens the failure precondition of a legal trade, no new outflow), user standing delegation this session
docs_touched: market-core; PLAN.md

S1: 04 §6.1 sell wrapper merges payout-sized, not amount-sized.

"The book raises the payout by `merge_scalar` of complete sets" is implemented as merging exactly `net + fee` sets (the payout being funded), not the full received `amount` — merging `amount` required mirror-side book inventory ≥ amount and made **legal sells fail** with `InsufficientPosition` after asymmetric buys depleted the mirror leg (found by the honest S1 I-12 property suite; the first-round tautological suite hid it). Applied to decision/gate/Baseline sell paths; fee custody unchanged (§6.1). No spec text changed.

## S1: 04 §7 TWAP clamp bounds are the exact…
authorized_by: Session decision under R-1/R-7 (I-13's bound text + the reference model's exact semantics), user standing delegation this session
docs_touched: market-core; PLAN.md

S1: 04 §7 TWAP clamp bounds are the exact real-arithmetic envelope; the lower bound now rounds UP.

`pow_1e9` per-step flooring drifted the computed `(1−κ)^k` lower clamp below the exact value, admitting extra downward drift (the adverse direction; S1 I-13 suite: 476,078,797 vs exact-lower 476,078,798). The Python reference already computes the envelope in exact 100-digit Decimal, sealing the exact reading. `mul_1e9_up`/`pow_1e9_up` added for the lower bound only; the upper bound keeps flooring — the computed clamp now sits inside the exact envelope on both sides (a clamp may be tightened, never loosened).

## B4-residual: 13 §1 gains the four governed XCM…
authorized_by: Session decision under the user's B4-residual "decide on your own" mandate (2026-07-17); defaults sim-gated, not final
docs_touched: 13 §1; 09 §6.1

B4-residual: 13 §1 gains the four governed XCM trader-rate PARAM keys

(SQ-112 resolution): `xcm.trade_dot_per_sec`/`_per_mb` (keys `xcm.dot_per_sec`/`xcm.dot_per_mb`), `xcm.trade_usdc_per_sec`/`_per_mb` (keys `xcm.usdc_per_sec`/`xcm.usdc_per_mb`) — Balance, ×2 Δ, cooldown 1, PARAM, defaults 10 DOT/s · 1 DOT/MiB · 50 USDC/s · 5 USDC/MiB **fee-sizing sim-gated `[VERIFY]` before Phase-3 HRMP arming**; 09 §6.1's fee row names them.

## B4-residual: 09 §5.2 `phase3.tvl_cap` re-based to total…
authorized_by: Same mandate; strictly tighter, status-quo-safe
docs_touched: 09 §5.2; 01 §5.4; 15

B4-residual: 09 §5.2 `phase3.tvl_cap` re-based to total local USDC issuance

(SQ-111): strict superset of the old "ledger + treasury sovereign accounts" base, making the mint step the binding enforcement point and the ledger split-path check an explicit defense-in-depth read (SQ-105/A2 side); SQ-110's trap-plus-self-scoped-recovery reading codified in the same passage; the per-account meter homed in the new `pallet-inflow-caps` (01 §5.4 row + 15 try-state coverage rule extended accordingly).

## B4-residual: 09 §6.4 `[VERIFY XCM-health data…
authorized_by: Same mandate
docs_touched: 09 §6.4; 05 §4.3; 13 §3.4

B4-residual: 09 §6.4 `[VERIFY XCM-health data availability]` closed

(SQ-113): v1 X = locally-accepted sends ÷ (accepted + local send failures + reserve-probe timeouts); remote outcomes are not runtime-readable on stable2603 and v1 deliberately omits per-message response tracking (I-24); X is partial by construction, so the R fail-static fallback is load-bearing. 05 §4.3's X row updated to match; **canonical v1 `MetricId` table added** (05 §4.3, mirrored in 13 §3.4 and `futarchy_primitives::metric_ids`).

## B4-residual: 08 §1.4 custody-sync passage
authorized_by: Same mandate
docs_touched: 08 §1.4

(SQ-123): `fund_budget_line` atomically transfers real USDC MAIN→pot for the KEEPER/ORACLE pot-backed lines and fails whole on insufficient MAIN custody; other lines keep custody in MAIN (A9 scope); the §6.3 try-state drift alarm stays as the backstop.

## B4-residual code-side rulings (no further doc edits):
authorized_by: Session decisions, all fail-closed/conservative
docs_touched: — (code + PLAN only)

(a) R left **unbound** in the welfare `MetricInputs` binding — the current-latch read would be fail-open against 07 §8/I-24 (adversarial blocker; SQ-195); (b) the Phase-5 `u128::MAX` sentinel short-circuits the deposit meter — no reads/writes once unbounded (SQ-196 ratification pending); (c) `Preservation::Expendable` on the MAIN→pot transfer so exact-amount funding succeeds; (d) `XcmTraffic` stored as a per-epoch double map with prefix-bounded reaping wired to cohort reap + epoch-roll (welfare/epoch seam); (e) welfare full-prune cutoff `current − 19` (05 §3.3 e−20 reading; the re-review's settlement-deadlock blocker fix, SQ-200).

## B2: 02 contract reconciliation — v3 → v4 (the queued…
authorized_by: User (Christopher Altmann) — standing instruction to complete B2 including the queued 02 amendment batch and decide open questions autonomously; joint backend+frontend sign-off under R-1 ("the user speaks for both sides")
docs_touched: 00 (D-2), 01 §5.1, 02 §4/§6/§7.3/§7.5/§8/§9/§13, 06 §7, 08 §1.2, 10, 11 §11.8.3, 13 §3.1, `crates/futarchy-primitives`, `docs/design/claude-design-kit/`

B2: 02 contract reconciliation — v3 → v4 (the queued amendment batch, user-delegated joint sign-off).

One pre-genesis bump discharges the long-queued 02 items: **§4 `NavView`** gains three trailing fields (`spendable_nav`, `meter_utilization_bps`, `class_floors`) so the frozen view can carry what 08 §1.2/§4.1 mandate and FE-15 renders — resolving the SQ-55 contradiction where 08 described `{nav, spendable_nav, reserve_impaired, meter_utilization}` and 02 froze a 10-field decomposition with nowhere to put two of them (`haircut_flag` ≡ `reserve_impaired`); **§4 `CohortSummary.proposals`** bound 5 → 12 to track 13 §1's `epoch.slots` hard-max, so a governance raise can no longer silently truncate the D-6 fallback surface (SQ-43); **§6** completes the `pallet-conditional-ledger` event row with the seven 03 §5 events the shipped pallet emits and 02 omitted, plus `BaselineVaultReaped` (SQ-37 — all 22 variants now represented, restoring the "event-derived history is complete by construction" property); **§7.5** freezes the shipped `pallet-attestor` storage the FE reads on the CODE/META execute path (SQ-26/SQ-2c; 01 §5.1 and 06 §7 re-pointed from their dangling "to be frozen at B2"); **§7.3** records `ParamRecord.last_change_block` (below); **§9** splits the phase-offset fractions onto the metadata-constant side and fixes the intake-row erratum (SQ-23/SQ-24/SQ-125 — 13 §1/§3.1 are normative), and adds the frozen metadata-constant **name** table SQ-138 asked for, so descriptor/fixture gating keys on frozen names instead of B8's invented placeholders; **§8/§13** version 4 + history entry; **00 D-2** records the bump. `INTEGRATION_CONTRACT_VERSION` 3 → 4 in `futarchy-primitives`, mirrored in the runtime `transaction_version` and the release manifest. **Pre-genesis** (no deployed runtime, no FE), so 02 §13(3)'s post-genesis append-only/migration clause does not apply — the v3 precedent governs.

## B2: five proposed metadata constants rejected from the…
authorized_by: Orchestrator under R-1 (13 §1 normative over a draft 02 table); surfaced by the Codex constants author refusing to wire a contradiction
docs_touched: 02 §9, `tools/release/surface-manifest.json`

B2: five proposed metadata constants rejected from the 02 §9 freeze — 13 §1 is normative (R-1 reconciliation).

The draft name table carried `IntakeMaxPerAccountFloor`/`IntakeMaxPerAccountCeiling`/`DecisionTrailingFloor`/`DecisionCoverageFloor`/`DecisionVMinFloors` as kernel metadata constants, but 13 rule 7's kernel-bounded set is normatively **exhaustive** and omits `intake.max_acct`, `dec.trailing`, `dec.coverage` and the `dec.v_min` floors — their registry bounds are META-amendable, so exposing them as immutable kernel constants would have contradicted 13 and given the FE a binding target the chain can move underneath it. They bind via `params()` (`ParamView.min`/`max`) instead; only genuinely kernel-bounded floors (`dec.window`, `dec.delta.*`, `dec.sigma.*`, `dec.extension`) stay metadata constants. Same principle as the SQ-23 resolution.

## B2: `ParamRecord` gains `last_change_block` (02 §7.3,…
authorized_by: Orchestrator under R-1 (a frozen view field that no chain state could produce is a spec/impl contradiction); recorded in 02 §13's v4 entry
docs_touched: 02 §7.3/§13, `crates/constitution-core`

B2: `ParamRecord` gains `last_change_block` (02 §7.3, within the v4 bump).

02 §4's frozen `ParamView.last_change` is a **block number**, but the record stored only `last_changed_epoch` (the cooldown clock) and historical epoch boundaries are not retained — so the frozen field was not computable from chain state. The record now carries both. This changed the rendered SCALE layout of `storage.constitution.params`, a live frozen release-manifest row: the layout was re-frozen from real runtime metadata, and a `param_record_fields_match_contract_02_section_7_3` drift-lock (mirroring A5's `RoundState` precedent, mutation-verified) now fails if the shape moves again. Pre-genesis, so no migration.

## B6: 06 §3.2 authority-matrix rows for the guard's…
authorized_by: Session decision under R-1 (rule-6 matrix-completeness duty; additive), user standing delegation this session ("Don't ask questions. Decide on your own.")
docs_touched: 06, PLAN.md

B6: 06 §3.2 authority-matrix rows for the guard's permissionless surface

(rule 6 mandates a matrix row per call): `execution_guard.apply_authorized_upgrade` / `system.apply_authorized_upgrade` (both application surfaces named; the system call noted as gated by the 09 §2.2 DescriptorLeadTime SafetyFilter), `execution_guard.expire_failed_execution`, `execution_guard.reject_stale` — all Signed ✔ (permissionless keeper cranks, 09 §2.1(4)/§1.2), ten-column table shape preserved. Purely additive: documents implemented+test-pinned behavior; no existing row changed.

## B6: 13 single-homing for the migration machinery.
authorized_by: Session decision under R-1 (13's single-home rule; adds homes, changes no semantics), user standing delegation this session
docs_touched: 13, PLAN.md

(a) Kernel row `MIGRATION_STALL_BLOCKS` — PB-MIGRATION halt raised when an active cursor is stalled **> 900 blocks** (the value existed only in 09 §3.2 prose; 13 is the only home of parameter values). (b) Three §4 bounds rows for the production `pallet-migrations` config: `MIGRATION_SERVICE_WEIGHT_PERCENT` 50% of max block weight, `MIGRATION_CURSOR_MAX_LEN` 65,536 B, `MIGRATION_IDENTIFIER_MAX_LEN` 256 B (previously invented literals — now `futarchy-primitives` constants referenced by the runtime). Values conservative, unchanged from the reviewed code.

## B8 release-pipeline rulings (no `docs/architecture/`…
authorized_by: Session decisions under the user's standing in-session delegation ("Implement milestone B8 … Don't ask questions. Decide on your own."); ratification requested via SQ-135/SQ-136/SQ-139
docs_touched: PLAN.md; code (`.github/workflows/`, `tools/release/`, `tools/ci/`, `.cargo/audit.toml`, `tools/reference-model/`, `crates/futarchy-fixed/tests/`)

B8 release-pipeline rulings (no `docs/architecture/` edits — every ruling is code-side, fail-closed, and logged for ratification via SQ-135…SQ-139).

(1) **Supply-chain waiver mechanism (SQ-135):** 15 §4.5's "clean cargo-audit" is satisfied via per-entry-annotated `.cargo/audit.toml` ignores for the 24 stable2603-pin-forced advisories (each entry names the pinning component + the clear-at-next-SDK-bump obligation), keeper audited with no inherited exceptions, and the waived-ID list disclosed in every release manifest — the alternatives were a permanently red gate or an out-of-scope SDK bump. (2) **Two-step publication (SQ-136):** tag releases publish as **prerelease** with an explicit not-canonical-until-mirror-evidence notice; CI holds no Arweave keys (12/O1); operator evidence = JSON sha256→TXID map. (3) **Layout-expectation freezing policy:** frozen `layout` expectations only for surface the current runtime wires (validated against a live node in-session); blocked entries carry none until their owning milestone freezes them from the real runtime — the deliberate exception is the SQ-101 `ForeignAssets` trio, whose 02-frozen Location-keyed expectation now makes a strict tag release **fail** on today's u32-keyed runtime (the gate enforcing the known open contract violation). (4) **B7 interface (SQ-139):** B8 defines `bleavit.env-evidence.v1` (byte-exact artifact inventory + release wasm/commit binding + named passing suites) so placeholder env dirs can never satisfy strict assembly. (5) **Sweep cost bound:** the release sweep asserts the pure composed-cost 8-ulp bound (04 §4) without the in-crate trade-path base-unit allowance — deliberately stricter; measured worst case 2 ulp over 10⁷ points.

## B5 code/design decisions
authorized_by: Session decisions under R-1/R-7 (conservative, reject-by-default readings; ratification asks logged as SQ-147…SQ-149)
docs_touched: 13 §5 (measured values); code: tools/ci, runtime, pallets/{epoch,execution-guard}

(no doc-set semantics changed except 13 §5): (1) the weight-regression gate covers ONLY `runtime/bleavit-runtime/src/weights/*.rs` — the consensus weights; the per-pallet `weights.rs` placeholders are macro-based mock defaults a parser cannot soundly gate (a perturbation test proved the blind spot) and are excluded by design; (2) the >10 % rule (15 §4.5) is applied to **worst-case evaluated totals** (fixed + Σ slope×component-max, per quantity: ref_time/proof/reads/writes), removals fail without an ack, and a push to main compares against `HEAD~1`; (3) `pov_mode = MaxEncodedLen { Preimage::PreimageFor: Measured }` on exactly `epoch::{tick,decide}` + `execution_guard::execute`, justified by the new bounded `(hash, recorded_len)` fetch + record-time 64 KiB caps that make an oversize read structurally impossible; (4) `execute()` pre-charges `WeightInfo::execute(16) + kernel prop.max_weight` and refunds actuals — the only reading of 09 §1.2(11) that can never undercharge (SQ-139 asks for ratification of the exact semantics); (5) benchmark exclusions: `pallet_xcm` stays `TestWeightInfo` until the B4 runtime integration; tx-payment pallets have no dispatchables; hook-only consensus pallets have no upstream harness; (6) 13 §5 items 1/2/7 rewritten with measured values (V-27…V-29) under R-1 — no D-n affected, no 02 change, no contract bump.

## D-19: the SDK release line moved `polkadot-stable2603`…
authorized_by: User (standing B11 delegation, 2026-07-17: "Implement milestone B11 … Don't ask questions. Decide for yourself."); the 00-decision requirement is 01 §9's own rule
docs_touched: 00, 01, 03, 05, 06, 08, 09; `.cargo/audit.toml`, `tools/ci/ghsa-waivers.toml`; root/pallet/runtime manifests, `tools/env/pins.env`, `.github/{ci.yml,dependabot.yml}`, `tools/deploy`, `deploy/`, `fuzz/README.md`, README/AGENTS

D-19: the SDK release line moved `polkadot-stable2603` → `polkadot-stable2606` (B11).

01 §9 itself classifies a line move as a 00-recorded project decision (unlike a `stable2603-N` maintenance tag), so 00 gained **D-19** and the doc set was made consistent: 01 §1/§5.1/§9 re-pointed to the new pinned line (`2606.0.0` umbrella; maintenance-tag example now `stable2606-N`, next-line example stable2609); 09 §6.1's XCM row re-verified on 2606 (v5 still latest; `staging-xcm 24.0.0`) and 09 §6.4 + 05 §4.3's "remote outcomes not runtime-readable" claims re-checked (XCM v5 unchanged); the open `[VERIFY]` tags naming the train (03 §2 holds-support, 05 §4.3 validation-data fields, 06 §2.3 conviction-voting provider, 09 §2.2 go-ahead mechanics, 09 §7.2 pausing surface) re-targeted to stable2606 **without resolving them**; 08 §2.1's `pallet-vesting` citation `=46.0.0` → `=49.0.0`. Code side: the vendored `core2 0.4.0` + `[patch.crates-io]` removed (unused in the 2606 closure — the yank-workaround provenance stays in git history); both waiver files re-triaged from scratch (16 `wasmtime` entries deleted; the 7 survivors + the yamux GHSA entry re-annotated under per-family exit criteria — libp2p-forced / litep2p-forced / sc-tracing-forced — each re-proven pin-forced by a fresh `cargo update --precise` attempt on the new lockfile; `lru` stays deliberately unwaived as informational). SQ-135 stays open (mechanism still in use; ratification is the user's).
