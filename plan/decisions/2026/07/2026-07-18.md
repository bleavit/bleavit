# Decisions — 2026-07-18

## SQ-272 resolution — step-9 decision-pair contest capital is the MIN of the Accept and Reject books, never their sum.
authorized_by: User ruling in the G0 audit-scope-A task, 2026-07-18 (R-7 conservative reading)
docs_touched: 00 D-4; 04 §7a; 05 §5.6; 08 §5.2/§5.4

The shallower book is binding because an attacker can move the cheaper side; `b_acc + b_rej` remains summed solely for the `sec.flow_cap` ceiling. 08 §5.4(b) pins the reading with `L̂ = 34,657 + 400,000 = 434,657`, adding one `dec.v_min` despite both books grading individually. Runtime, reference model, generated differential vectors, and simulation are aligned; the asymmetric 400k/100k vector mutation-proves SUM would Adopt where MIN rejects. Simulation economics are unchanged (the sim already used MIN; only the runtime `measured_depth` SUM was the audit-scope-A under-security bug). Independently re-verified by the orchestrator before commit (reference 36, differential decision/treasury vectors, runtime pair-minima pin, fmt/clippy, calibration `--check` digest-consistent).

## `settlement_score` made exactly-rounded on both sides of the differential (G0 criterion-A defect sweep).
authorized_by: Session decisions under R-1/R-7 (spec-exact rounding; divergences were implementation bugs vs the §5.4/§5.5 text), user-directed G0/S4 completion drive 2026-07-18
docs_touched: reference-model (`welfare.py`), corpus; Rust: welfare-core, epoch-core, pallet-epoch, pallet-market, runtime (no doc text changed)

The 05 §4.4(4) geometric-mean settlement score is now computed as the exact floor `isqrt(A·B)` in grid units in `welfare-core` (replacing an exp2/log2 path that lost 1 ulp on on-grid results; unit tests tightened from ±2 ulp to exact) — and the **Python reference model shared the defect class at the ε_W corner**: its 100-digit exp2/log2 round-trip floored `settlement_with_self` at W=0 to `0` where the spec-exact value of the doubly-ε-floored pair is exactly one base unit; fixed to the correctly-rounded `(a·b).sqrt()` (independent derivation, not a Rust port). Corpus delta: exactly one field (`settlement_with_self` `0.000000000` → `0.000000001`); vectors regenerated, freshness + doc-table green. Companion decide-path fixes (same sweep, no spec change — code brought to the 05 §5.4/§5.5 text): per-gate validity-then-veto ordering restored (a Survival veto is no longer mis-reported as `NotDecisionGrade`, which wrongly slashed 10 % of the intake bond), and a first-pass Invalid welfare grade now rejects instead of extending; both pinned by corpus rows and new pallet-epoch bond-treatment tests.

## 04 §7a accrual-grid admissibility (SQ-231 implementation follow-up).
authorized_by: Session decisions under R-1 (conservative-direction codifications of the implementing sessions' findings), user-directed G0/S4 completion drive 2026-07-18
docs_touched: 04, 08

The 10-block grid is the coarsest admissible recording for the contest-capital accumulator; a finer per-event integral is explicitly admissible (strictly conservative — only under-counts), and the literal backward interval sample MUST NOT credit a position across a boundary it did not span (a 1-block flash position would earn a full 10-block credit — the pass-widening direction). Surfaced by the runtime implementation, which uses the per-event integral the A8-remediation review had already established for the gross measure; the reference accumulator is grid-agnostic, so differential agreement is unaffected. Also: 08 §5.4(b) gained its normative-seeding recomputation (b ≈ 35,130; ceiling not binding at ×7) after the reference-model implementation showed the printed floor-`b` illustration is not a configuration the §5.3 seeding produces (ratio 8 there — the source of the ×7-vs-8 tension, resolved in favor of ×7 + the illustration note); 04 §7a's marking (raw previous-block quote, USDC base-unit grid, two outcome legs) made explicit.

## SQ-232 resolution — the classless Baseline book grades at the TREASURY-tier contest floor `dec.v_min.trs` (no new 13 key).
authorized_by: Session decision under R-1 (owning-doc rationale extended to its only consistent reading; the sim's assumption codified) in the user-directed S4-completion drive, 2026-07-18
docs_touched: 05, 13

05 §5.2 now states the binding explicitly; 13 rule 7's consuming-engine coupling list gains the row. The tier is 08 §4.3's own rationale ("manipulation resistance must be at least mid-class, hence the TREASURY-tier `b`") — the same tier that sizes `pol.b_baseline` — and is exactly the named assumption S4's simulation already ran under, so no simulation semantics change. A dedicated governable key was considered and rejected: the floor is a structural coupling like `gate.v_min = 0.1·dec.v_min`, not an independently tunable value.

## SQ-231 mechanism fix — contest capital replaces gross contest notional in decide steps 5/9 (D-4 input hardening).
authorized_by: **User ruling 2026-07-18 ("mechanism fix first", G0 session)** — chose genuine < 1 % false-pass over gate-weakening; R-1 cross-doc consistency pass done
docs_touched: 00, 04, 05, 08, 13, 14, 15

New 04 §7a: per-book **contest-capital accumulator** — time-weighted marked USDC value of net outstanding trader positions, `noi_t = Σ_branch max(q − q_pol, 0)·price` sampled on the existing 10-block observation grid with the same previous-block read discipline, checkpointing and u256 accumulator as the TWAP series; wash/churn nets out by LMSR path-independence, so inflating the measure requires holding real exposure across time (the `C_hold` bleed). 05 §5.2/§5.4 grade `dec.v_min`/`gate.v_min` over this measure; 05 §5.6 + 08 §5.2 rebuild `L̂`'s non-POL term as `min(contest capital, sec.flow_cap·(b_acc+b_rej))` — the wash ceiling is now **gate-bearing**, closing the S4-demonstrated self-certification of `AttackCost̂` by attack flow (TH-4 flips at 0.76–0.90×3P, PARAM/TREASURY). 08 §5.3's `v_min = 2P` kernel identity restated over the resistant measure with the new **ceiling non-bindingness bound `sec.flow_cap ≥ 7` (K)** (max class ratio `P_ref/b_floor` = 6.7, META); 13's `sec.flow_cap` row re-described + hard min 7; 14 TH-4 mitigations updated; 15 try-state market clause covers the new accumulator; 00 D-4 amendment note. Rounding: `noi_t` rounds down (conservative). No 02 change (nothing frozen exposes the measure).

## B12: 09 §4 quote authority + denomination ruling (SQ-245/SQ-246).
authorized_by: Session decision under R-1 and the user's explicit delegation this session ("Don't ask questions. Decide on your own.") — **ratification requested**
docs_touched: 09 §4; 08 §1.1/§1.4; 13 §1; PLAN.md

The renewal quote `(period_index, price)` is supplied by the **operations multisig** via the new Signed `treasury.note_coretime_quote`, accepted only from a stored, genesis-set, `FutarchyTreasury`-rotatable **coretime quote authority**; quote noting and expired-quote pruning are freeze-exempt like the renewal itself (a starved quote would re-open the B-16 wedge). Freshness: executable while age ≤ new key `ops.coretime_quote_ttl`; supersession in place; expiry is the sole permissionless prune trigger. Denomination: quote `price` is **DOT planck** (what `broker.renew` charges); `execute_coretime_renewal` debits `ops.coretime` by the USDC value of the full DOT outflow (price + new key `ops.coretime_fee_dot`) at new key `ops.coretime_dot_rate` (µUSDC/DOT), **rounded up**, failing closed below the converted debit — budget-envelope accounting only, since DOT is marked 0 in NAV (08 §1.2). The Coretime-side renewal account is an ops-operated account stored beside the authority (it must sign `broker.renew` there; the verified route funds it, no `Transact`) **[VERIFY at Phase-2/3 onboarding]**. Three 13 §1 TREASURY keys added (`ops.ct_dot_rate`, `ops.ct_fee_dot`, `ops.ct_quote_ttl`); no 02 change, D-9 refined not altered.

## B12 audit follow-up: 09 §4 freeze-exemption list scoped to runtime-controlled freezes; reserve-health carve-out made explicit (spec-review major + SPEC-QUESTION 2).
authorized_by: Session decision under R-1 (owning-doc text vs upstream FRAME mechanics; D-9's wedge-closure purpose is the only internally consistent reading), user delegation this session — **ratification requested**
docs_touched: 09 §4; 08 §1.2; PLAN.md

The B12 spec-compliance audit proved 09 §4's "dispatchable during … PB-MIGRATION halt" unsatisfiable: FRAME's multi-block-migration lockdown (`MultiBlockMigrator`) rejects every transaction while a cursor — including the halt-at-fault `Stuck` cursor — exists, so no extrinsic can honor that leg without a custom executive. 09 §4 now names the freezes the runtime controls (dead-man, hard-gate, PB-LEDGER-FREEZE, reserve-health flag) and records the PB-MIGRATION wedge as accepted, bounded by the RB-UPGRADE halt/retire-and-rollback discipline. Companion 08 §1.2(2) amendment: the reserve-health "no new outflows" rule gains the explicit D-9 carve-out for the `ops.coretime` renewal debit (the implementation and its long-standing test already read D-9 this way; NAV carries no DOT term, so the flag's solvency purpose is untouched).

## B13 audit follow-up: 12 §6.3 telemetry-source paragraph amended — fail-closed scope boundary + numerics sourcing (spec-review majors 1–2 + SPEC-QUESTIONs 3–4).
authorized_by: Session decision under R-1 (the review proved the unamended text self-contradictory for exactly the alert it exists to serve), user delegation this session — **ratification requested**
docs_touched: 12 §6.3; PLAN.md

Two clarifications from the B13 spec review: (1) fail-closed degradation covers *collection* failures only — a computed alert condition (a book loss exceeding its b·ln2 bound, an I-12 breach) MUST be carried in the series so its alert fires, never suppressed as a family degradation; (2) the LMSR domain-rejection component cannot be a runtime API or storage counter (rejected extrinsics roll back all storage), so its audited source is the finalized `System.ExtrinsicFailed` event stream (metadata-resolved), exported as a cumulative counter alerted via `increase()`; rounding dust rides `TelemetryApi`.

## B13: 12 §6.3 gains the runtime telemetry source paragraph.
authorized_by: Session decision under R-1 and the user's explicit delegation this session — **ratification requested**
docs_touched: 12 §6.3; PLAN.md

The audited sources for the 9 B13 seam series are the methods of a monitoring-only **`TelemetryApi`** runtime API, explicitly **outside the 02 integration contract** (no FE consumption, no contract version, changeable without a 02 §13 bump; 12 owns it), consumed by the §6.3 exporter via `state_call` under the existing per-family fail-closed degradation; solvency-relevant methods must read the same quantities the owning pallet's try-state compares. No 02 change.

## G1 drill-environment rulings (no `docs/architecture/` edits; logged for ratification — see SQ-276 for the missing normative rule).
authorized_by: Session decisions under the user's standing in-session delegation ("Implement milestone G1 … Don't ask questions. Decide on your own."); ratification via this row + SQ-276
docs_touched: `tools/env/generate-relay-specs.sh`, `zombienet/networks/bleavit-local.toml` (32-bit-safe timeout), `zombienet/README.md`

The first real execution of the B7 Zombienet environment (V-36) forced two Paseo-side decisions in `tools/env/generate-relay-specs.sh`: (1) the pinned Paseo chain-spec-generator is now built with the Paseo tree's own **`fast-runtime`** feature on the relay AND explicitly on the coretime/asset-hub system runtimes (the generator forwards it to the relay only; without the explicit toggles the XCM topology would carry a relay/parachain `TIMESLICE_PERIOD` split, 20 vs 80 — spec-reviewer major, fixed in-session). Full disclosure of what fast-runtime flips in the pinned Paseo tree: session length 600 → 10 blocks (the operative fix — `ValidatorGroups`, the precondition for any parachain being scheduled, are built only at session boundaries, so no drill assertion window could ever see a para block), `TIMESLICE_PERIOD` 80 → 20, `SessionsPerEra` 6 → 1, election phases/fallback and `LeasePeriod` shortenings. Relay block **time** is unconditionally 6 s, so the 13 §2 dead-man 4,800-relay-block threshold and every block-denominated drill measurement are unaffected. (2) The generated paseo-local genesis seeds **`scheduler_params.num_cores = 3`** (headroom for the three-para bleavit-xcm topology) so the first session boundary builds groups covering every injected core. 02 §11's operative text ("genesis overrides matching the release") governs the Bleavit para spec — which is untouched: no Bleavit runtime byte and no 13-owned value is altered; the drill-overrides patch still carries `constitution.phaseFlags` only; SQ-128's uncompressed-epoch conservatism stands. Whether relay-host-side/local-profile overrides are admissible in a drill environment is exactly the rule 02 §11/15 §4.7 never states — raised as SQ-276 for the user to settle; these two settings are the proposed reading.
