---
id: SQ-178
title: **Guardian accountability is not production-complete.** A8 submits a real retrospective referendum and fronts its stock submission deposit pro-rata from approvers' free balances, but: the runtime collapses the ratify/guardian tracks onto the strongest shared ConstitutionalValues track (whose schedule can outlive the two-epoch deadline); the 50,000-VIT seat bonds are an arithmetic ledger, not fungible holds; and no deterministic recall payload/candidate transition exists. R1 now persists the implementable 50% accounting slash, review cleanup and `ReviewFailed` signal even when recall scheduling fails; define the remaining true track split, held-seat-bond/deposit mechanics, and recall call.
spec_ref: 06 §2.1; §5.1; §5.4
raised: 2026-07-17 (B1b compliance review; R1 partial closure)
status: resolved
resolved: 2026-07-17
batch: none
---

## Question

**Guardian accountability is not production-complete.** A8 submits a real retrospective referendum and fronts its stock submission deposit pro-rata from approvers' free balances, but: the runtime collapses the ratify/guardian tracks onto the strongest shared ConstitutionalValues track (whose schedule can outlive the two-epoch deadline); the 50,000-VIT seat bonds are an arithmetic ledger, not fungible holds; and no deterministic recall payload/candidate transition exists. R1 now persists the implementable 50% accounting slash, review cleanup and `ReviewFailed` signal even when recall scheduling fails; define the remaining true track split, held-seat-bond/deposit mechanics, and recall call.

## Status

resolved 2026-07-17 (B1b completion): six-track split live (ratify 1 d/7 d/1 d/immediate meets `grd.review_dl`), SeatBond fungible holds with try-state, both referendum deposits fronted via the guardian sovereign (fixes the latent always-times-out review), real slash→treasury flows, `guardian.recall` with vacancy semantics, `guardian.uphold_veto` T24 producer; the fixed-84 d entrenched enactment residual is SQ-234
