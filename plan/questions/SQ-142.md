---
id: SQ-142
title: **Epoch market deployment/decision telemetry has no complete production projection.** `MarketAccess::open_markets` needs canonical collision-free market ids/accounts/POL parameters, while `decision_grade` needs exact decision-window TWAP coverage, staleness, contest notional/POL, measured depth/flow, second-insufficiency, and previous settled Baseline data; `pallet-market` currently exposes only books, `BaselineMarketOf`, and the last quote. Where should the bounded telemetry/checkpoint state and id allocator live?
spec_ref: 04 §1–§5; 05 §5
raised: 2026-07-16 (B1a A8/A11 wiring)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

**Epoch market deployment/decision telemetry has no complete production projection.** `MarketAccess::open_markets` needs canonical collision-free market ids/accounts/POL parameters, while `decision_grade` needs exact decision-window TWAP coverage, staleness, contest notional/POL, measured depth/flow, second-insufficiency, and previous settled Baseline data; `pallet-market` currently exposes only books, `BaselineMarketOf`, and the last quote. Where should the bounded telemetry/checkpoint state and id allocator live?

## Status

**resolved 2026-07-21 (batch X)** — **row premise no longer describes HEAD.** The state owner is `pallet-market`, and the allocator plus every grade input are production reads. `published_flow_per_day` returns `None` deliberately, and the kernel defines that as a *complete* value because `None` selects the specified `L̂/2` fallback — the projection is not missing data. The adjacent prize and Ask-scaled-seeding defects are separate rows (SQ-173, SQ-270/SQ-300), not residual defects in this telemetry projection.
