---
id: SQ-259
title: **Two trailing `RoundState` fields need the queued 02 §7.2 amendment (v5 batch).** The SQ-249 per-game freeze stores `round_one_bond` and `round_cap` as trailing fields of the 02-frozen `RoundState` (append-only direction, pre-genesis, FE-invisible — the FE reads the `OracleRoundView` projection per 02 §7.2's own note; the release manifest already records the real layout). 02's freeze rule requires the joint sign-off + version bump for any change; queue these two fields in the same v5 batch as SQ-256's event additions.
spec_ref: 02 §7.2/§13
raised: 2026-07-18 (B10 remediation)
status: resolved
resolved: 2026-07-18
batch: none
---

## Question

**Two trailing `RoundState` fields need the queued 02 §7.2 amendment (v5 batch).** The SQ-249 per-game freeze stores `round_one_bond` and `round_cap` as trailing fields of the 02-frozen `RoundState` (append-only direction, pre-genesis, FE-invisible — the FE reads the `OracleRoundView` projection per 02 §7.2's own note; the release manifest already records the real layout). 02's freeze rule requires the joint sign-off + version bump for any change; queue these two fields in the same v5 batch as SQ-256's event additions.

## Status

**resolved 2026-07-18 (PR #107 bot P2-1)** — `RoundState` reverted to the byte-exact v4 layout; the per-game frozen schedule relocated to the internal `RoundSchedules` map (off the 02 surface, 1:1 try-state correspondence). No 02 change needed; SQ-245 remains the only queued v5 item
