---
id: SQ-44
title: `MarketKind` has no formal enum declaration in the spec — it appears only as a prose set in the 02 §5 `MarketCreated` row (`{ DecisionAccept, DecisionReject, GateS_Adopt, GateS_Reject, GateC_Adopt, GateC_Reject, Baseline }`). The canonical frontend decodes `MarketCreated.kind` by its metadata variant **name**, so the identifier spelling is load-bearing. The M1 code initially used idiomatic `GateSAdopt`; should 02 gain a formal declaration, or should the code conform to 02's prose?
spec_ref: 02 §5; rule 5; 02 §13
raised: 2026-07-15 (M1 finish spec-reviewer; re-flagged by the PR #49 Codex bot)
status: resolved
resolved: 2026-07-15
batch: none
---

## Question

`MarketKind` has no formal enum declaration in the spec — it appears only as a prose set in the 02 §5 `MarketCreated` row (`{ DecisionAccept, DecisionReject, GateS_Adopt, GateS_Reject, GateC_Adopt, GateC_Reject, Baseline }`). The canonical frontend decodes `MarketCreated.kind` by its metadata variant **name**, so the identifier spelling is load-bearing. The M1 code initially used idiomatic `GateSAdopt`; should 02 gain a formal declaration, or should the code conform to 02's prose?

## Status

**resolved 2026-07-15** (R-1, code-conforms-to-contract): 02 line 5 makes **02 canonical for any name that appears on the contract surface** and runtime-code rule 5 restates it — so the *code* was conformed to 02's frozen `GateS_Adopt`/`GateS_Reject`/`GateC_Adopt`/`GateC_Reject` (not 02 to the code), with `#[allow(non_camel_case_types)]` on the enum and a byte-for-byte TypeInfo names-lock test (`market_kind_variant_names_match_contract_02_section_5`). **02 is unchanged → no `INTEGRATION_CONTRACT_VERSION` bump and no joint sign-off.** SCALE **index** order was already unambiguous and matched.
