---
id: SQ-94
title: 09 §1.1's `QueuedExecution` sketch omits fields the semantics require — `declared_domains` (§1.2(11) domain-⊆), `failed_at` (05 §2.1 T18), `ratification_passed`; A11 stores them, and the 02 §7.4 value type is frozen by reference. True 09 §1.1 to enumerate them at a future 09 touch.
spec_ref: 09 §1.1; 02 §7.4
raised: 2026-07-16 (A11 review)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

09 §1.1's `QueuedExecution` sketch omits fields the semantics require — `declared_domains` (§1.2(11) domain-⊆), `failed_at` (05 §2.1 T18), `ratification_passed`; A11 stores them, and the 02 §7.4 value type is frozen by reference. True 09 §1.1 to enumerate them at a future 09 touch.

## Status

resolved 2026-07-21 (batch C, contract v6) — 09 §1.1's `QueuedExecution` sketch now enumerates the shipped shape: `ratification_passed`, `declared_domains` (§1.2(11) domain-⊆) and `failed_at` (05 §2.1 T18) are listed with their roles. **The row's second finding is ratified and recorded rather than silently fixed:** the shipped struct still carries `pre_upgrade_checkpoint`, which 09 §3.2 retired under SQ-127/SQ-144 — and verification confirms it is not dead code but still written (`crates/execution-guard-core/src/lib.rs:856`) and cleared (`pallets/execution-guard/src/lib.rs:943`). Removing a live field from a frozen direct-read value is a coordinated storage change under 02 §13(3), so the sketch names it explicitly as owed rather than pretending it is gone.
