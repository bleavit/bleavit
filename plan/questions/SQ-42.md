---
id: SQ-42
title: 05 §4.4 C_daily renormalized weights: must the renormalized weight `w_j / Σ_onchain w` be snapped to the FixedU64 1e9 grid **before** the `w·log2` product, or is only the product floored at 64.64 (the reference model keeps the renormalized weight full-precision, flooring only the product)? Affects bit-identical daily-flag reproducibility at a threshold.
spec_ref: 05 §4.4
raised: 2026-07-15 (M3 finish spec-reviewer)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

05 §4.4 C_daily renormalized weights: must the renormalized weight `w_j / Σ_onchain w` be snapped to the FixedU64 1e9 grid **before** the `w·log2` product, or is only the product floored at 64.64 (the reference model keeps the renormalized weight full-precision, flooring only the product)? Affects bit-identical daily-flag reproducibility at a threshold.

## Status

**resolved 2026-07-21 (batch X)** — **fixed in code.** The Python reference model now performs C_daily weight renormalization through unsigned Q64 division *before* the weighted-log products, matching the runtime's 64.64 evaluation bit-for-bit (05 §4.4; 15 §4.4 requires bit-identical evaluation). Pre-fix Python produced `0.899999999` where the runtime produces `0.900000000` — a kernel↔oracle non-equivalence the corpus was structurally blind to. Added a non-binary 1:5 normalized-weight full-pipeline vector plus adjacent `theta_c_lo` probes; the welfare corpus grew from 3 to 4 scenarios. Tests: `test_daily_c_renormalization_uses_the_q64_quotient_before_products`, `welfare_vectors_match_python_reference_model_grid_exactly`.
