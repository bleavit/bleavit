---
id: SQ-66
title: `pallet-market` reap and frozen `BaselineMarketOf` retention disagreed: 04 tied mapping removal to book reap, while contract v6 tied it to cohort-ring eviction and claimed an unenforced ≤36/≤4 Baseline bound.
spec_ref: 04 §2/§8.3; 02 §7.4
raised: 2026-07-15 (A3 Codex + spec-reviewer)
status: resolved
resolved: 2026-07-22
batch: none
---

## Question

`pallet-market` reap and frozen `BaselineMarketOf` retention disagreed: 04 tied mapping removal to book reap, while contract v6 tied it to cohort-ring eviction and claimed an unenforced ≤36/≤4 Baseline bound.

## Status

**resolved 2026-07-22 with contract v7 (joint backend+frontend sign-off).** The abandoned `CountedStorageMap`/ring-scan design remains reverted. Contract and implementation now agree on the O(1) market-lifetime rule: a mapping and matching Baseline book co-exist, successful reap removes both atomically, and the shared `MaxLiveMarkets = 196` bounds the actual proposal/Baseline mix. `try-state` checks both directions and reap fails static on a corrupt inverse. The old separate four-Baseline constant and every false fan-out were removed. The distinct aggregate-capacity-versus-archive-delay liveness risk exposed by this correction is tracked, not hidden, as **SQ-483**.
