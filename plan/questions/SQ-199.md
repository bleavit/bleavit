---
id: SQ-199
title: **FE deposit pre-check surface (09 §5.2 / 11):** the FE cap pre-check needs `pallet-inflow-caps::CumulativeDeposits` + the two `phase3.*` keys; neither is in 02's frozen FE-read surface. Append them at the next 02 contract amendment (bump + joint sign-off) before Track F consumes the deposit flow?
spec_ref: 09 §5.2; 11; 02 §7
raised: 2026-07-17 (B4-residual spec review)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

**FE deposit pre-check surface (09 §5.2 / 11):** the FE cap pre-check needs `pallet-inflow-caps::CumulativeDeposits` + the two `phase3.*` keys; neither is in 02's frozen FE-read surface. Append them at the next 02 contract amendment (bump + joint sign-off) before Track F consumes the deposit flow?

## Status

resolved 2026-07-21 (batch C, contract v6) — 02 §7 freezes `pallet-inflow-caps::CumulativeDeposits: map AccountId → u128`, and 02 §9 gains a Phase-3 deposit-cap precondition row binding `phase3.tvl_cap` / `phase3.dep_cap` through `params()`. **The row is partly too broad:** the two cap *values* were never missing a frozen read surface — 02 §9 already routes every tunable through `params()`/`Params` and 13 owns the canonical key names. What was genuinely absent was the cumulative meter's direct-read name and the §9 precondition row. No production code needed.
