---
id: SQ-1007
title: **`reaches_bundle` is the npm supply-chain gate's load-bearing claim and nothing mechanizes it.** A waiver declaring `"yes"` is refused at load time, which is the right refusal — but the value is a human assertion, checked today by grepping the built tree by hand. A claim that gates a user-facing bundle should be derived the way the cargo half derives its workspace list.
spec_ref: 15 §4.5
raised: 2026-08-07
status: open
batch: X
---

## Question

**`reaches_bundle` is the npm supply-chain gate's load-bearing claim and nothing mechanizes it.** A waiver declaring `"yes"` is refused at load time, which is the right refusal — but the value is a human assertion, checked today by grepping the built tree by hand. A claim that gates a user-facing bundle should be derived the way the cargo half derives its workspace list.

## Status

open
