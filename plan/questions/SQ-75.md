---
id: SQ-75
title: The 7 registry calls need SafetyFilter authority-matrix rows (06 §3) + closed-wrapper negative tests (rule 6). All are `Signed` except `resolve_challenge` (ResolutionAuthority). B1a obligation, per the A1–A11 precedent.
spec_ref: 06 §3; 15 §1
raised: 2026-07-15 (A6 dual review)
status: resolved
resolved: 2026-07-24
batch: none
---

## Question

The 7 registry calls need SafetyFilter authority-matrix rows (06 §3) + closed-wrapper negative tests (rule 6). All are `Signed` except `resolve_challenge` (ResolutionAuthority). B1a obligation, per the A1–A11 precedent.

## Status

**resolved 2026-07-24 (SQ-75 coverage).** The production authority matrix and bare-leaf behavior were already correct; the residual was test coverage only. The runtime regression now exercises both registry instances’ `resolve_challenge` leaves through `utility.dispatch_as`, `utility.dispatch_as_fallible`, and `utility.as_derivative`, asserting base-filter denial while retaining signed-origin `BadOrigin` checks. No production change or contract bump.
