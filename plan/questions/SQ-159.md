---
id: SQ-159
title: **15 §1-key consumers read kernel constants, not live `Params`** — amending the key moves nothing on the enforcement path (the I-22 convention half + X-11e's no-hardcoding rule): 12 oracle keys (`orc.bond_floor/bond_bps/rounds/window/rep_stake`, `wt.quorum/stake`, `res.probe_int/probe_to/probe_amount/fail_thr/recover_thr`), 2 attestor keys (`att.bond`, `att.window` — tests advance by `CHALLENGE_WINDOW_BLOCKS`), and `grd.review_dl` (`REVIEW_DEADLINE_EPOCHS`). Pre-existing Track-A behavior surfaced by S3; every affected key carries `consumer_binding = "kernel-constant (B10)"` in the manifest, printed on every checker run; the generated suite's doc comment states it covers the amendment dispatch path only. Binding closure is owned by **B10** (the keeper's off-chain side already resolved live Params — SQ-126).
spec_ref: 13 rule 2; 15 §1 (I-22); 06 §7; 07 §§4–8
raised: 2026-07-17 (S3 adversarial review, blocker — scoped)
status: resolved
resolved: 2026-07-20
batch: none
---

## Question

**15 §1-key consumers read kernel constants, not live `Params`** — amending the key moves nothing on the enforcement path (the I-22 convention half + X-11e's no-hardcoding rule): 12 oracle keys (`orc.bond_floor/bond_bps/rounds/window/rep_stake`, `wt.quorum/stake`, `res.probe_int/probe_to/probe_amount/fail_thr/recover_thr`), 2 attestor keys (`att.bond`, `att.window` — tests advance by `CHALLENGE_WINDOW_BLOCKS`), and `grd.review_dl` (`REVIEW_DEADLINE_EPOCHS`). Pre-existing Track-A behavior surfaced by S3; every affected key carries `consumer_binding = "kernel-constant (B10)"` in the manifest, printed on every checker run; the generated suite's doc comment states it covers the amendment dispatch path only. Binding closure is owned by **B10** (the keeper's off-chain side already resolved live Params — SQ-126).

## Status

**resolved 2026-07-20 (triage sweep — code-verified).** B10 rebound the 15 keys to live `Params` (genesis-default fallback, per-game schedule freeze), and `tools/limit-coverage/registry.toml` now carries 0 `consumer_binding` entries — so no key's enforcement path reads a kernel constant behind its own amendment surface. The keeper's off-chain half was already resolved under SQ-126.
