---
id: SQ-47
title: Who owns the registration/keying of the POL-commitment and queued-outflow obligation sets `nav()` nets (08 §1.2/§8.2/§1.3), and by what trigger? A9 exposes bounded `set_pol_commitments`/`set_pending_outflows` sync-setters; the keyed reserve/release lifecycle is cross-pallet (market POL A3, execution-guard queue A11) and wired at B1a.
spec_ref: 08 §1.2, §1.3, §8.2
raised: 2026-07-15 (A9 review)
status: resolved
resolved: 2026-07-20
batch: none
---

## Question

Who owns the registration/keying of the POL-commitment and queued-outflow obligation sets `nav()` nets (08 §1.2/§8.2/§1.3), and by what trigger? A9 exposes bounded `set_pol_commitments`/`set_pending_outflows` sync-setters; the keyed reserve/release lifecycle is cross-pallet (market POL A3, execution-guard queue A11) and wired at B1a.

## Status

**resolved 2026-07-20 (triage sweep — code-verified).** B10 wired both sync setters in production: `RuntimePolCommitmentSync` calls `FutarchyTreasury::set_pol_commitments` (`runtime/bleavit-runtime/src/configs.rs:2746`) and the guard queue calls `set_pending_outflows` (`configs.rs:3030`); the treasury holds the netted view, keyed by the market POL and execution-guard queue lifecycles.
