---
id: SQ-69
title: 07 §7's Calls table lists only `file`/`challenge_filing`/`crank_close`/`close_epoch`, and its `Filing.state` shape omits the §4 quorum fields. A6 also needs `ack_observed` (Signed watchtower), `resolve_challenge` (ResolutionAuthority) and `reap_epoch` (keeper, archive-gated), and stores `Filed{window_end,extended,acks}` / `Challenged{round,window_end,challenger,evidence_hash}`. True 07 §7 to the reviewed core (owning-doc under-specification).
spec_ref: 07 §7
raised: 2026-07-15 (A6 dual review)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

07 §7's Calls table lists only `file`/`challenge_filing`/`crank_close`/`close_epoch`, and its `Filing.state` shape omits the §4 quorum fields. A6 also needs `ack_observed` (Signed watchtower), `resolve_challenge` (ResolutionAuthority) and `reap_epoch` (keeper, archive-gated), and stores `Filed{window_end,extended,acks}` / `Challenged{round,window_end,challenger,evidence_hash}`. True 07 §7 to the reviewed core (owning-doc under-specification).

## Status

**resolved 2026-07-21 (batch D), split.** Doc half done: 07 §7's `Filing.state` trued to `Filed{window_end, extended, acks}` / `Challenged{round, window_end, challenger, evidence_hash}` with the rationale tying each field to a §4/§5.5 rule. Two row claims were **stale**: `ack_observed` and `reap_epoch` were already documented. The `resolve_challenge` table row is deliberately **withheld** — documenting the shipped unconstrained `(epoch, filing_id, uphold)` authority would normatively bless what **SQ-294** already logs as a defect.
