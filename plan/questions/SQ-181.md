---
id: SQ-181
title: **The public on-chain/daily telemetry component source is absent.** Runtime `MetricInputs` keeps those vectors empty (incident multiplier is real); welfare snapshot/daily-gate calls then reject instead of recording incomplete financial inputs. Define and wire the bounded runtime-source surface.
spec_ref: 05 §4.6–§4.7
raised: 2026-07-17 (B1b judgment call)
status: open
batch: X
---

## Question

**The public on-chain/daily telemetry component source is absent.** Runtime `MetricInputs` keeps those vectors empty (incident multiplier is real); welfare snapshot/daily-gate calls then reject instead of recording incomplete financial inputs. Define and wire the bounded runtime-source surface.

## Status

open — batch **X**; owner **A12**. **Re-verified 2026-07-25 against HEAD, and the row over-claims.** "Keeps those vectors empty" has been false since the day it was raised: `X` (XCM health) went live the same day in `63c8172` and `R` (reserve health) on 2026-07-25 in `60495b5`, both epoch *and* daily; attested components are pulled from the oracle at epoch granularity, and correctly absent from the daily vector (05 §4.4: C_daily carries no attested term). **The real remaining set is narrower and should be stated as such:** the four other C_onchain components (`E`, `H`, `Π`, `K`), the whole **S** pillar (`U`, `F`, `D_eff`) and the whole **P** pillar (`P_FEES`, `P_QUALIFIED_USERS`, `P_SETTLED_VALUE`) have no source — the emitter filters on `pillar == COnchain`, so no S or P component can be produced at all. **A second cause the row never named, and today the dominant one:** no production genesis preset registers any `MetricSpec` (`pallet-welfare`'s genesis default is empty and the runtime sets no welfare field), so on a fresh chain both vectors return empty for lack of a *registered spec*, not for lack of a source. Wiring the missing sources without also shipping a genesis spec set changes nothing observable.  **Sized 2026-07-26 (read-only survey): this is milestone-sized, not a row — split into A14 + SQ-502.** Ten producers are missing, and the line they split on is whether 05 §4.6's normalization is needed. **Seven yield a value in [0,1] directly and need none of it** — `U`, `F`, `D_eff`, `E`, `H`, `Π`, `K`. Those seven are **A14**. *(A prior sizing called them "mechanical over state that already exists". A full read-only inventory on 2026-07-26 disproved that for **every one of the seven**, and the corrected findings are in A14's row: the state each needs either is destroyed before a snapshot can read it, was never retained at day granularity, or does not exist at all. Three of the seven additionally need something that is not code — a ruling, a parameter, or a genesis value that is still `[VERIFY]`. Treat the earlier "mechanical" reading as withdrawn.)* **Three are raw unbounded series** — the whole `P` pillar, `P_FEES`, `P_QUALIFIED_USERS` and `P_SETTLED_VALUE` — and every one of them needs the §4.6 winsorize/percentile/log1p/min-max kernel plus genesis `PriorBounds`, neither of which exists (V-43, re-verified 2026-07-26: no such code in the workspace and `prior_bounds` is read by nothing). `P_QUALIFIED_USERS` additionally needs a bounded-state distinct-count sketch, which is a design problem with an adversarial dimension rather than a wiring job. Those three are **SQ-502**. *(An earlier version of this sizing named only eight of the ten, folding `P_FEES` and `P_SETTLED_VALUE` into "the HLL problem", and claimed the sketch blocked the genesis spec set. Both corrected 2026-07-26 after the Codex review of #185: `register_spec` never consults `RuntimeMetricInputs`, so the conforming v1 set registers before any producer exists.)* **Narrowed 2026-07-27 — the row's residual is one unowned item, and naming it is the point of this update.** Everything else has an owner: the seven natively-bounded producers are **A14** (with `F` decided out of v1, `E` and `D_eff` recorded blocks) and the three raw `P` series plus the §4.6 kernel are **SQ-502**. What neither owns is the second cause this row identified and today the dominant one: **no production genesis preset registers any `MetricSpec`**, so on a fresh chain both vectors return empty for want of a *registered spec* rather than a source, and every producer A14 ships changes nothing observable until a set exists. A14's row explicitly disclaims it ("registration is independent of both and is not a dependency in either direction") and SQ-502 does not mention it — so it belonged to no milestone until now. **It is not merely a wiring job**, because membership is a judgement: a registered component whose producer never supplies a value makes `record_snapshot` refuse, which is why 05 §4.3.2 leaves `F` out, so the genesis set may contain only components whose producers exist at launch. That makes it **blocked on A14's three open PRs** (#187 `K`, #190 `U`, #191 `H`+`Π`) — once they land the admissible v1 set is determinate: `X`, `R`, `K`, `U`, `H`, `Π` plus the attested `A` triple, with `E` and `D_eff` excluded on their recorded blocks and the `P` pillar following SQ-502. Verify: a runtime test that boots the production preset and records a complete snapshot at both epoch and day granularity, and a negative test that one absent producer fails the crank status-quo-safe.
