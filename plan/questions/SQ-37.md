---
id: SQ-37
title: 02 §6's frozen `pallet-conditional-ledger` event row omits `GateSplit`/`GateMerged`/`BaselineSplit`/`BaselineMerged`/`BaselineSettled`/`GateRedeemed`/`BaselineRedeemed` that 03 §5 defines and A2 emits. Is 02 §6 intentionally the FE-ingest subset, or should it be completed to match 03 §5?
spec_ref: 02 §6; 03 §5
raised: 2026-07-15 (A2 spec-reviewer)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

02 §6's frozen `pallet-conditional-ledger` event row omits `GateSplit`/`GateMerged`/`BaselineSplit`/`BaselineMerged`/`BaselineSettled`/`GateRedeemed`/`BaselineRedeemed` that 03 §5 defines and A2 emits. Is 02 §6 intentionally the FE-ingest subset, or should it be completed to match 03 §5?

## Status

resolved 2026-07-21 (batch C) — **already resolved by contract v4; the row was stale, not open.** All seven events (`GateSplit`, `GateMerged`, `BaselineSplit`, `BaselineMerged`, `BaselineSettled`, `GateRedeemed`, `BaselineRedeemed`) are in 02 §6 today with their field shapes, and `pallets/conditional-ledger` emits exactly those variants. No v6 amendment was needed.
