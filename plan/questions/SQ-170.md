---
id: SQ-170
title: **03 §8 error name `NotWinningPosition` vs core `WrongBranch`.** The frame-free core derives the winner internally and emits `WrongBranch`; the S1 differential maps the Python error class onto it explicitly. Reconcile the 03 §8 name with the pallet error surface at the next 03 touch — and note the core's `WrongBranch` variant is currently **dead code** (declared + pallet-mapped, produced by no core path; the S1 re-pass removed a fabricated differential witness for it rather than pretend coverage).
spec_ref: 03 §8
raised: 2026-07-17 (S1 differential)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

**03 §8 error name `NotWinningPosition` vs core `WrongBranch`.** The frame-free core derives the winner internally and emits `WrongBranch`; the S1 differential maps the Python error class onto it explicitly. Reconcile the 03 §8 name with the pallet error surface at the next 03 touch — and note the core's `WrongBranch` variant is currently **dead code** (declared + pallet-mapped, produced by no core path; the S1 re-pass removed a fabricated differential witness for it rather than pretend coverage).

## Status

**resolved 2026-07-21 (batch D), split.** Doc half done: `NotWinningPosition` **struck** from 03 §8 — the settled-redemption calls take no branch argument (the ledger derives the winner internally), so no state can raise it; §8 now says so. Both row claims were **false**: the core does not *emit* `WrongBranch` (it only declares it) and the differential deliberately omits it. Dead-variant cleanup raised as **SQ-315** (batch X).
