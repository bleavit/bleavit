---
id: SQ-117
title: `keeper.rebate` is deliberately unseeded in the 13 §1 registry (formula default "≈3× fee", `[VERIFY fee basis at benchmark time]`), so B9's rebate pipeline reads it fail-soft as **0** — the meter, tranches and threshold events are structurally live but every rebate pays nothing until the row exists. Where does the value get seeded: a B5 post-benchmark 13 §1 edit (hypothesis default per 13 rule 4) or a runtime `amend_registry` after launch? Until then A-1's "rebated" incentive is inert (ops-line keepers only).
spec_ref: 13 §1 (`keeper.rebate`); 08 §6.2; B5
raised: 2026-07-16 (B9)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

`keeper.rebate` is deliberately unseeded in the 13 §1 registry (formula default "≈3× fee", `[VERIFY fee basis at benchmark time]`), so B9's rebate pipeline reads it fail-soft as **0** — the meter, tranches and threshold events are structurally live but every rebate pays nothing until the row exists. Where does the value get seeded: a B5 post-benchmark 13 §1 edit (hypothesis default per 13 rule 4) or a runtime `amend_registry` after launch? Until then A-1's "rebated" incentive is inert (ops-line keepers only).

## Status

resolved 2026-07-21 (batch X, A13) — ruled and implemented this session. Verified: B5 landed the benchmarks and weights but never resolved the economic conversion, and the stale "until B5" comments should not be read as completion — HEAD still deliberately tests zero. The zero fallback is correctly fail-soft (it cannot create an unbacked outflow) but leaves the keeper incentive inert. **Ruled: a calibrated genesis seed is a release prerequisite**, computed from a normatively selected benchmark fee basis and the final launch `fee.vit_usdc_rate`, rounded **down** to µUSDC against the claimant. Launch-at-zero-then-amend was refused: the amendment would depend on the very keeper activity whose incentive is disabled. **RESOLVED 2026-07-21 (batch X, A13):** the seeding mechanism is now live — `keeper.rebate` is genesis-seeded (default `3×`, min `1×`, max `10×`) off a single kernel fee-basis constant `kernel::KEEPER_REBATE_FEE_BASIS_USDC`, so B9's pipeline reads a positive value; the runtime `keeper_rebate()` adapter keeps its fail-soft-zero fallback. The **basis value stays `[VERIFY]`** (placeholder 0.03 USDC from 08 §6.2; see Verification log) pending launch `fee.vit_usdc_rate` — the calibrated seed is then rounded **down** to µUSDC against the claimant. registry.toml → `param-bounds`/`genesis=true`; genesis-keys fixture + limit-coverage regenerated. Pallet `sq_117_*` + runtime `sq_117_sq_158_*` regressions (fail pre-fix: row absent).
