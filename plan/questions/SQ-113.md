---
id: SQ-113
title: 09 §6.4 X sub-metric: the retained `[VERIFY XCM-health data availability on stable2603]` is now half-answered — B4's `XcmHealthSink` provides local send/local-send-failure counters (all that is runtime-readable at the router) plus a probe-timeout signal; remote delivery/execution outcomes are **not** runtime-readable without per-message response tracking. Is send/fail/probe-timeout-only the accepted v1 resolution (with `R` alone driving the daily C flag when X is partial, as §6.4's fail-static fallback already provides)? If yes the `[VERIFY]` can be closed at the next 09 touch.
spec_ref: 09 §6.4; 07 §8; 05 §4.3
raised: 2026-07-16 (B4 review)
status: resolved
resolved: 2026-07-17
batch: none
---

## Question

09 §6.4 X sub-metric: the retained `[VERIFY XCM-health data availability on stable2603]` is now half-answered — B4's `XcmHealthSink` provides local send/local-send-failure counters (all that is runtime-readable at the router) plus a probe-timeout signal; remote delivery/execution outcomes are **not** runtime-readable without per-message response tracking. Is send/fail/probe-timeout-only the accepted v1 resolution (with `R` alone driving the daily C flag when X is partial, as §6.4's fail-static fallback already provides)? If yes the `[VERIFY]` can be closed at the next 09 touch.

## Status

**resolved 2026-07-17 (B4-residual)** — 09 §6.4's `[VERIFY]` closed: v1 X = locally-accepted sends ÷ (accepted + local failures + probe timeouts), partial by construction, R fallback load-bearing; welfare `XcmTraffic` counters + `HealthTrackingRouter` wrap + oracle `ProbeTimeoutSink` + the `MetricInputs` X emission are live. R itself stays deliberately unbound (SQ-195).
