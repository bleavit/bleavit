---
id: SQ-157
title: **Limit-registry scope: do 13 §3 frozen-constant rows belong in it?** 15 §4.6 generates from "the parameter/meter registry in 13-parameters.md"; the S3 extractor covers the three registry **tables** (§1 keys, §2 kernel constants, §4 storage bounds). 13 §3 ("frozen protocol constants") also carries dispatch-relevant bounds in prose/small tables: min vested transfer 1 VIT, VIT ED 0.01, USDC `min_balance` 10⁴ (§3.5), `frn.window`, the >50-block staleness gap (§3.2), the 60%/10%/7-day adjudication track (§3.4). Include §3 rows in the extractor (they'd need a stable slug scheme) or confirm the three-table scope.
spec_ref: 15 §4.6; 13 §3
raised: 2026-07-17 (S3 spec-reviewer)
status: resolved
resolved: 2026-07-20
batch: none
---

## Question

**Limit-registry scope: do 13 §3 frozen-constant rows belong in it?** 15 §4.6 generates from "the parameter/meter registry in 13-parameters.md"; the S3 extractor covers the three registry **tables** (§1 keys, §2 kernel constants, §4 storage bounds). 13 §3 ("frozen protocol constants") also carries dispatch-relevant bounds in prose/small tables: min vested transfer 1 VIT, VIT ED 0.01, USDC `min_balance` 10⁴ (§3.5), `frn.window`, the >50-block staleness gap (§3.2), the 60%/10%/7-day adjudication track (§3.4). Include §3 rows in the extractor (they'd need a stable slug scheme) or confirm the three-table scope.

## Status

resolved 2026-07-20 (batch B6): three-table scope (13 §1/§2/§4) ratified in 15 §4.6. Row's `frn.window` example struck — it sits in 13 §3.4 (not §3.2) and is an alias of the already-extracted §2 `dec.extension`, so it was never an uncovered bound
