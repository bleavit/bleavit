---
id: SQ-158
title: **`dis.merit_min` / `grd.bond` remain unseeded in genesis `Params`.** A8 resolved the `gate.v_min` part in code: four live bounded records (`gate.v_min.{param,trs,code,meta}`) are genesis-seeded, and S3 now expands the singular doc row into those four actual keys so its generated amendment-bounds suite covers them. The suffix materialization is intentionally explicit pending SQ-194's rule-6 wording ruling. `dis.merit_min = B_1(c,m)` and entrenched `grd.bond` remain absent; confirm derived-at-consumer status or require conservative static records.
spec_ref: 13 rules 2/6/7 §1; 05 §5.2; 07 §12
raised: 2026-07-17 (S3 spec-reviewer; A8 merge round 2 partial resolution)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

**`dis.merit_min` / `grd.bond` remain unseeded in genesis `Params`.** A8 resolved the `gate.v_min` part in code: four live bounded records (`gate.v_min.{param,trs,code,meta}`) are genesis-seeded, and S3 now expands the singular doc row into those four actual keys so its generated amendment-bounds suite covers them. The suffix materialization is intentionally explicit pending SQ-194's rule-6 wording ruling. `dis.merit_min = B_1(c,m)` and entrenched `grd.bond` remain absent; confirm derived-at-consumer status or require conservative static records.

## Status

resolved 2026-07-21 (batch X, A13). Verified: `grd.bond` is correctly exempt (ratified in B6), but `dis.merit_min`'s hardcoded equality to `B_1` permanently deletes the independent META lever that both 07 §12 and 13 §1's governance metadata assert. A **scalar record is sufficient** after all — the row assumed it could not express `B_1(c,m)`, but the consumer can apply `max(live key, frozen B_1)`. Seed `dis.merit_min` in `constitution-core::genesis_params` (default/floor `orc.bond_floor`, `Balance::MAX` ceiling, factor-two delta, two-epoch cooldown, META), have `RuntimeEpochOracle::any_open_dispute_touching` require `round.bond >= max(stored dis.merit_min, stored round_one_bond)`, and update the genesis-key and limit-coverage manifests. **RESOLVED 2026-07-21 (batch X, A13):** implemented exactly as ruled — `dis.merit_min` seeded (floor/default `orc.bond_floor`, ceiling `Balance::MAX`, factor-2 Δ, 2-epoch cooldown, META); `any_open_dispute_touching` now binds `round.bond >= frozen_b1.max(balance_param(b"dis.merit_min"))` with the malformed-schedule fail-closed path preserved; 07 §12 + 13 §1 trued to the `max(live, B_1)` composition; genesis-keys + registry.toml (`param-bounds`/`genesis=true`) regenerated. Runtime `sq_158_raised_*` / `sq_158_lowered_*` consumer regressions + pallet `sq_158_*` (fail pre-fix: key absent).
