---
id: SQ-73
title: Should 15 §1 assign `pallet-registry` a bond-custody **conservation invariant** (the oracle 07 §13 "Σ held bonds == Σ open-round bonds × sides" analog) so A6's solvency `try_state` is a mandated invariant, not an optional gap?
spec_ref: 07 §13; I-4; 15 §1
raised: 2026-07-15 (A6 dual review)
status: resolved
resolved: 2026-07-20
batch: none
---

## Question

Should 15 §1 assign `pallet-registry` a bond-custody **conservation invariant** (the oracle 07 §13 "Σ held bonds == Σ open-round bonds × sides" analog) so A6's solvency `try_state` is a mandated invariant, not an optional gap?

## Status

resolved 2026-07-20 (batch B3): **ratified as I-28**, and the row's comparison premise was false. 15 §1 gained I-28 (registry bond custody: `Σ escrowed filing bonds ≤ the instance's sovereign USDC balance`, one bond per `Filed`, two per `Challenged`, none terminal), stated as `≤` mirroring **I-4** rather than 07 §13's bare `==` — over-custody is a dust/reconciliation matter, under-custody is an unbacked claim, and the shipped check is `held >= liability` (`pallets/registry/src/lib.rs:1121-1143`). `pallet-registry` added to the try-state coverage rule; boundary + heading moved to I-1…I-28. **Premise corrected:** registry was not "a gap while the oracle is covered" — *neither* pallet appeared in 15 §1, and `pallet-oracle` does **not** implement 07 §13's custody identity at all → **SQ-293**.
