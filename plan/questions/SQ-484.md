---
id: SQ-484
title: **Reserve readiness had no operator-visible proof that either side of the probe route retained its independent runway.** The local `ops.reserve_probe` line needs an F+R-denominated runway view, while the Asset Hub sovereign needs independently collected canonical USDC and usable DOT balances; treating exporter failure as healthy zero would hide the exact outage this control exists to detect.
spec_ref: 07 §8; 12 §6.3; 14 TH-14
raised: 2026-07-23 (B15 release-readiness review)
status: resolved
resolved: 2026-07-23
batch: none
---

## Question

**Reserve readiness had no operator-visible proof that either side of the probe route retained its independent runway.** The local `ops.reserve_probe` line needs an F+R-denominated runway view, while the Asset Hub sovereign needs independently collected canonical USDC and usable DOT balances; treating exporter failure as healthy zero would hide the exact outage this control exists to detect.

## Status

**resolved 2026-07-23 (B15)** — the monitoring-only `TelemetryApi` exposes the local line runway; a separately reconnecting, metadata-decoded Asset Hub collector reports sovereign USDC and usable DOT without trusting local state; malformed/unavailable remote state fails only that readiness family closed. RB-XCM alerts and the series/coverage gates bind all three signals. Live provisioning and calibration remain V-45 release evidence, not an implementation gap.
