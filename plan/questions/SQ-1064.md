---
id: SQ-1064
title: **The reusable client pallet permanently rejects report 129 because its 128-row immutable report store has no lifecycle.**
spec_ref: "[16](../../docs/architecture/16-hosted-question-service.md) §9, §12 · [15](../../docs/architecture/15-invariants-and-testing.md) I-36"
raised: 2026-08-12
status: resolved
resolved: 2026-08-12
batch: none
---

## Question

`pallet-bleavit-client` refuses `receive_report` once `Reports::count() == MaxReports`, and exposes no
remove, prune, rotate or bounded-ring path. The reference runtime and integration quickstart both
set `MaxReports = 128`. Ordinary report 129 therefore disables push and `OnReport` automation
forever absent a runtime migration. Authoritative proof-backed pull remains available.

## Status

**resolved 2026-08-12.** The client pallet has a governance-authorized monotone
prune floor, rejects reports at or below it, and removes eligible retained rows
with a bounded translate charged at `MaxReports`. Replay protection survives row
pruning; benchmarks and lifecycle/rollback tests cover the 128→129 recovery.
