---
id: SQ-590
title: **RULED AND EXECUTED 2026-08-04 (R-1) — contract v27 freezes `Proxy.Proxies` in 02 §7.6. SQ-580's defect class, eighth instance.** 11 §11.3 mandates *"proxies supported as call wrappers under the same precondition system"*, and §11.4 rule 2 makes every row *an exact chain read* — so verifying that the signer actually holds a delegation for `real` is compulsory, and the only item that answers it is `Proxy.Proxies` (`pallet-proxy` is at runtime index 25; the item is `StorageMap<Twox64Concat, AccountId, (BoundedVec<ProxyDefinition<AccountId, ProxyType, BlockNumber>, MaxProxies>, Balance)>`). It appears nowhere in 02. **The consequence is the one this class always has:** a wrapper naming `real = R` had every row evaluated against R — correctly, since that is the acting account — while nothing established the signer may act for R at all, so all rows passed and `pallet_proxy::proxy` returned `NotProxy` after signature. `proxyTypeCovers` does not close it: it asks whether a *claimed* proxy type covers the call, taking the claim on trust, and the claim is exactly the part that must come from the chain. **Why `check-client-surface-obligations.py` missed it:** that gate extracts `Pallet.Item` references from docs 10/11, and §11.3 states this obligation in prose that never names `Proxy.Proxies` — so the inverse gate has a prose blind spot of its own, which is its own finding. **Shipped meanwhile (fail-closed, not deferred):** `proxyAdmits` takes `DelegationEvidence`, a discriminated `read | unreadable` rather than an optional array — an empty list means *nobody may act for this account* and an unperformed read means *we do not know*, and collapsing the two makes a missing check look like a passed one. It enforces the three refusals the runtime does: the delegation must name **this signer**, the **stored** `proxyType` governs rather than the wrapper's claim, and a non-zero `delay` is announce-only (`proxy` → `Unannounced`). Six mutations, all caught. **Executed the same day.** 02 §7.6 gains the row, `INTEGRATION_CONTRACT_VERSION` moves 26 → 27, the feed/descriptors/chainhead transcripts/`CRITICAL_SURFACE` are regenerated (278 frozen entries, was 277), and the evidence's `read` branch now carries **`Finalized<ProxyRead>`** — which is the part that was not merely deferred but *unreachable*: 11 §11.4 rule 4 forbids provider data from satisfying a precondition, and with no frozen surface the one wrapper check that decides whether a signature is lawful was the single place a cached answer would have been believed. It is **keyed** as well as branded (`real` travels with the delegations), because `Proxy.Proxies` is a map and a read taken for another account is not stale evidence but evidence about someone else — its *emptiness* being the dangerous half, exactly as with `Multisig.Multisigs`. Two normative notes ride the row: the **stored** `proxy_type`/`delay` govern, and an unrecognised `ProxyType` variant is **unproven, never permissive** (this runtime declares only `Any`, confirmed from the metadata layout — a value a later runtime may change). Four further mutations, all caught, one of them the compile-time binding itself: deleting the manifest entry makes `PROXY_DELEGATION_SURFACE` fail with `TS2322` at its declaration rather than decaying into a comment. **The method gap it exposed is NOT closed by this bump and is filed as SQ-591** — naming the item in §11.3 fixes this instance, not the class
spec_ref: 11 §11.3, §11.4; 02 §7.6, §13; 10 §5.2; 15 §4.8
raised: 2026-08-04
status: resolved
batch: none
---

## Question

**RULED AND EXECUTED 2026-08-04 (R-1) — contract v27 freezes `Proxy.Proxies` in 02 §7.6. SQ-580's defect class, eighth instance.** 11 §11.3 mandates *"proxies supported as call wrappers under the same precondition system"*, and §11.4 rule 2 makes every row *an exact chain read* — so verifying that the signer actually holds a delegation for `real` is compulsory, and the only item that answers it is `Proxy.Proxies` (`pallet-proxy` is at runtime index 25; the item is `StorageMap<Twox64Concat, AccountId, (BoundedVec<ProxyDefinition<AccountId, ProxyType, BlockNumber>, MaxProxies>, Balance)>`). It appears nowhere in 02. **The consequence is the one this class always has:** a wrapper naming `real = R` had every row evaluated against R — correctly, since that is the acting account — while nothing established the signer may act for R at all, so all rows passed and `pallet_proxy::proxy` returned `NotProxy` after signature. `proxyTypeCovers` does not close it: it asks whether a *claimed* proxy type covers the call, taking the claim on trust, and the claim is exactly the part that must come from the chain. **Why `check-client-surface-obligations.py` missed it:** that gate extracts `Pallet.Item` references from docs 10/11, and §11.3 states this obligation in prose that never names `Proxy.Proxies` — so the inverse gate has a prose blind spot of its own, which is its own finding. **Shipped meanwhile (fail-closed, not deferred):** `proxyAdmits` takes `DelegationEvidence`, a discriminated `read | unreadable` rather than an optional array — an empty list means *nobody may act for this account* and an unperformed read means *we do not know*, and collapsing the two makes a missing check look like a passed one. It enforces the three refusals the runtime does: the delegation must name **this signer**, the **stored** `proxyType` governs rather than the wrapper's claim, and a non-zero `delay` is announce-only (`proxy` → `Unannounced`). Six mutations, all caught. **Executed the same day.** 02 §7.6 gains the row, `INTEGRATION_CONTRACT_VERSION` moves 26 → 27, the feed/descriptors/chainhead transcripts/`CRITICAL_SURFACE` are regenerated (278 frozen entries, was 277), and the evidence's `read` branch now carries **`Finalized<ProxyRead>`** — which is the part that was not merely deferred but *unreachable*: 11 §11.4 rule 4 forbids provider data from satisfying a precondition, and with no frozen surface the one wrapper check that decides whether a signature is lawful was the single place a cached answer would have been believed. It is **keyed** as well as branded (`real` travels with the delegations), because `Proxy.Proxies` is a map and a read taken for another account is not stale evidence but evidence about someone else — its *emptiness* being the dangerous half, exactly as with `Multisig.Multisigs`. Two normative notes ride the row: the **stored** `proxy_type`/`delay` govern, and an unrecognised `ProxyType` variant is **unproven, never permissive** (this runtime declares only `Any`, confirmed from the metadata layout — a value a later runtime may change). Four further mutations, all caught, one of them the compile-time binding itself: deleting the manifest entry makes `PROXY_DELEGATION_SURFACE` fail with `TS2322` at its declaration rather than decaying into a comment. **The method gap it exposed is NOT closed by this bump and is filed as SQ-591** — naming the item in §11.3 fixes this instance, not the class

## Status

resolved
