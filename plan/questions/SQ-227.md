---
id: SQ-227
title: **Home of the I-13 (TWAP drift/slew) fuzz obligation.** 15 §1's I-13 row names "fuzz + reference diff" as its method, but §4.5 (S2's owning section) names only three fuzz areas — payload decode, nested-wrapper filtering, LMSR trade paths — none of which is TWAP. Does I-13's fuzz obligation belong to S2 (whose owning §4.5 does not name it) or to a separate/later milestone (e.g. alongside the accumulator differential tests)? S2 does not implement TWAP fuzzing; its PLAN row claims only I-10/I-11/I-12.
spec_ref: 15 §1 (I-13); §4.5; 04 §7
raised: 2026-07-17 (S2 spec-review)
status: resolved
resolved: 2026-07-20
batch: none
---

## Question

**Home of the I-13 (TWAP drift/slew) fuzz obligation.** 15 §1's I-13 row names "fuzz + reference diff" as its method, but §4.5 (S2's owning section) names only three fuzz areas — payload decode, nested-wrapper filtering, LMSR trade paths — none of which is TWAP. Does I-13's fuzz obligation belong to S2 (whose owning §4.5 does not name it) or to a separate/later milestone (e.g. alongside the accumulator differential tests)? S2 does not implement TWAP fuzzing; its PLAN row claims only I-10/I-11/I-12.

## Status

resolved 2026-07-20 (batch B6) as **doc-truing, not a coverage gap**: I-13 already carries a ≥10⁶-case property suite against an independent exact-rational oracle, plus reference diff and try-state — and that suite found and fixed the `pow_1e9` drift defect. I-13's Verification column corrected away from "fuzz"; the row was raised in the S2 review blind to S1's delivery the same day
