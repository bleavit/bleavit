---
id: SQ-1058
title: **The exported transaction `gate()` mints `GatePassed` from caller-asserted plain pins, compatibility and results rather than owning verified reads.**
spec_ref: "[11](../../docs/architecture/11-frontend-workflows.md) §11.3–§11.4 · [15](../../docs/architecture/15-invariants-and-testing.md) INV-FE-2"
raised: 2026-08-12
status: resolved
resolved: 2026-08-12
batch: none
---

## Question

The transaction-builder root exports `gate(prep, at, live, compat, results)`. Every input is a
caller-constructible plain object; agreement between fabricated values reaches the cast to the only
`GatePassed` brand. The real raw-signing test fixture demonstrates this without any finalized read.
This contradicts the module and INV-FE-2 claim that the gate proof is constructible only after one
proof-backed refresh. Exposure is latent because the current application composition registers no
transaction screen or production signer call site.

## Status

**resolved 2026-08-12.** The structural `gate` predicate is private. The public
`refreshAndGate` accepts a nominal live connection, owns one fresh finalized pin
and all branded reads, and refuses evidence from any other pin. Negative compile
fixtures prove literal transport/evidence objects cannot mint the authorization.
