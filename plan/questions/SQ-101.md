---
id: SQ-101
title: **USDC `ForeignAssets` key type.** B1a instantiated `ForeignAssets` with `AssetId = u32` (id 1337), producing the `Assets.Account(1337, who)` storage shape 02 §7.4/§8/X-11a declares "wrong by contract".
spec_ref: 02 §7.4/§8 (X-11a), 13 §3.5
raised: 2026-07-16 (B1a dual review)
status: resolved
resolved: 2026-07-17
batch: none
---

## Question

**USDC `ForeignAssets` key type.** B1a instantiated `ForeignAssets` with `AssetId = u32` (id 1337), producing the `Assets.Account(1337, who)` storage shape 02 §7.4/§8/X-11a declares "wrong by contract".

## Status

**resolved 2026-07-17.** Runtime `AssetId` is XCM v5 `Location`; the single-homed `usdc_location()` constructor drives genesis, fees, rebates and benchmarks. The frozen 10-byte SCALE identity plus Asset/Metadata raw keys are regression-pinned, and the release-manifest blocker is removed. Pre-genesis, so no state migration. Full gates green ×2; spec review 0/0/1-minor (fixed in-session); real-node recorder e2e confirms all three surface rows record.
