---
id: SQ-1006
title: **10 §9.4 binds a published render budget to Lighthouse 12.8.2 by behavior, and that pin now carries an advisory with no backport.** The cell's reason is behavioral — *"because Lighthouse's own `first-meaningful-paint` audit yields no numeric value in 12.8.2"* — so the bump to 13 that clears `@opentelemetry/core` GHSA-8988-4f7v-96qf is an R-1 spec change rather than a dependency bump. The advisory is waived meanwhile, with containment verified: it reaches one workspace project and no file of the built `dist/`.
spec_ref: 10 §9.4; 15 §4.5
raised: 2026-08-07
status: open
batch: B7
---

## Question

**10 §9.4 binds a published render budget to Lighthouse 12.8.2 by behavior, and that pin now carries an advisory with no backport.** The cell's reason is behavioral — *"because Lighthouse's own `first-meaningful-paint` audit yields no numeric value in 12.8.2"* — so the bump to 13 that clears `@opentelemetry/core` GHSA-8988-4f7v-96qf is an R-1 spec change rather than a dependency bump. The advisory is waived meanwhile, with containment verified: it reaches one workspace project and no file of the built `dist/`.

## Status

open
