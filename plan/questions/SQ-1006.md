---
id: SQ-1006
title: **RULED 2026-08-14 — adopt Lighthouse 13.4.1, retain FCP as the first-meaningful-render proxy, and remove the superseded npm waivers.** Lighthouse 13.4.1 still publishes no `first-meaningful-paint` audit in its default performance config, while FCP and both reference presets remain available. Its Node floor is 22.19.0; upgrading the repository floor and immutable CI images clears the old telemetry and archive-extraction paths without changing §9.4's hardware or timing budgets.
spec_ref: 10 §9.4; 15 §4.5
raised: 2026-08-07
status: resolved
resolved: 2026-08-14
batch: none
---

## Question

**10 §9.4 bound a published render budget to Lighthouse 12.8.2 by behavior, and that pin acquired advisories with no viable in-range repair.** The cell's reason was behavioral — *"because Lighthouse's own `first-meaningful-paint` audit yields no numeric value in 12.8.2"* — so the major upgrade required an R-1 ruling rather than an unexplained dependency bump.

## Status

**Resolved 2026-08-14 (F14, V-390).** Lighthouse 13.4.1 likewise has no default `first-meaningful-paint` audit, so §9.4 continues to use FCP as the measurable proxy. Its desktop and mobile presets retain the published reference dimensions, pixel ratios and throttling inputs. Bleavit now pins Lighthouse 13.4.1, raises its Node 22 floor to 22.19.0, removes both Lighthouse-path npm waivers, and explicitly resolves transitive `nanoid` to patched 3.3.18. The integration contract and every §9.4 budget remain unchanged.
