---
id: SQ-185
title: **“Non-POL contest notional” lacks an operational state definition.** Specify which inventory/liquidity components count, and when, for the time-average that backs decision-grade `AttackCostHat`.
spec_ref: 05 §5.2/§5.6; 08 §5.2
raised: 2026-07-17 (B1b R1 dual review)
status: resolved
resolved: 2026-07-20
batch: none
---

## Question

**“Non-POL contest notional” lacks an operational state definition.** Specify which inventory/liquidity components count, and when, for the time-average that backs decision-grade `AttackCostHat`.

## Status

**resolved 2026-07-20 (B4 ratification).** Stale since the SQ-231 amendment (2026-07-18): 04 §7a already gives the full operational state definition the row asks for — which components count (`noi_t = Σ_branch max(q_branch − q_pol_branch, 0)·price_branch`, POL excluded structurally), when (backward-weighted on the observation grid, previous-block stored `q` and quote, per-event integral admissible, no credit across an unspanned boundary), the pair reduction (min, never sum) and rounding (down). Code implements it faithfully (`crates/market-core/src/lib.rs:431-456`, `pallets/market/src/lib.rs:1842-1877`). One residual truing landed: 08 §7's griefing table still priced the refund path as “3M **churn** × 2 × 30 bps” — the measure SQ-231 retired — so the cell now names held contest capital plus the `C_hold` bleed; the 18,000 figure survives.
