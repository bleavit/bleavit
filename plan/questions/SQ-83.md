---
id: SQ-83
title: 02 §6 lists several epoch events with terse field notation omitting `pid` (`ProposalCancelled { reason }`, `ProposalQueued { payload_hash, maturity }`, `ProposalRejected { reason }`, `ProposalDelayed { justification_hash }`) while spelling `pid` for others; A8 carries `pid` on **all** epoch events (needed for attribution). Is 02 §6's notation shorthand (pid implied) — to be made explicit at a future 02 touch — or literal? Code keeps `pid`; 02 untouched (no contract bump).
spec_ref: 02 §6
raised: 2026-07-16 (A8 review)
status: resolved
resolved: 2026-07-21
batch: none
---

## Question

02 §6 lists several epoch events with terse field notation omitting `pid` (`ProposalCancelled { reason }`, `ProposalQueued { payload_hash, maturity }`, `ProposalRejected { reason }`, `ProposalDelayed { justification_hash }`) while spelling `pid` for others; A8 carries `pid` on **all** epoch events (needed for attribution). Is 02 §6's notation shorthand (pid implied) — to be made explicit at a future 02 touch — or literal? Code keeps `pid`; 02 untouched (no contract bump).

## Status

resolved 2026-07-21 (batch C, contract v6) — the four epoch rows now carry `pid` in 02 §6: `ProposalCancelled { pid, reason }`, `ProposalQueued { pid, payload_hash, maturity }`, `ProposalRejected { pid, reason }`, `ProposalDelayed { pid, justification_hash }`. The row's "no contract bump" premise was correctly refused by batch D's verification, and the fix had in fact **already landed inside batch C's own dual-review remediation**: the adversarial pass raised the identical defect independently (02 §6's amended lead claims to freeze *field shapes*, which those four rows contradicted), so the row arrived from D already satisfied. Cohort-level events correctly carry no `pid` and are unchanged.
