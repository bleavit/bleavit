---
id: SQ-228
title: **`StorageWeightReclaim` successor migration.** The D-19 stable2606 move brought the deprecated Cumulus `cumulus-primitives-storage-weight-reclaim` extension's successor (`frame_system::WeightReclaim`, frame-system 48 — upstream's deprecation note names `cumulus-pallet-weight-reclaim`, NOT in the 2606 closure, as the other candidate; the migration must evaluate both) inside the pinned closure — the B1a-era comment "outside the pinned closure" is no longer the blocker. B11 deliberately kept the deprecated extension: swapping it changes the `TxExtension` stack (signed-payload format + metadata surface the FE reads), which is a reviewed transaction-format change, not maintenance. The deprecation will eventually force the swap at a future train bump. Which milestone owns the migration (a B-track runtime slice with FE-visible metadata re-record, or fold into the next contract-touch batch)?
spec_ref: 01 §9; 02 §6; 09 §2
raised: 2026-07-17 (B11)
status: resolved
resolved: 2026-07-20
batch: none
---

## Question

**`StorageWeightReclaim` successor migration.** The D-19 stable2606 move brought the deprecated Cumulus `cumulus-primitives-storage-weight-reclaim` extension's successor (`frame_system::WeightReclaim`, frame-system 48 — upstream's deprecation note names `cumulus-pallet-weight-reclaim`, NOT in the 2606 closure, as the other candidate; the migration must evaluate both) inside the pinned closure — the B1a-era comment "outside the pinned closure" is no longer the blocker. B11 deliberately kept the deprecated extension: swapping it changes the `TxExtension` stack (signed-payload format + metadata surface the FE reads), which is a reviewed transaction-format change, not maintenance. The deprecation will eventually force the swap at a future train bump. Which milestone owns the migration (a B-track runtime slice with FE-visible metadata re-record, or fold into the next contract-touch batch)?

## Status

resolved 2026-07-20 (batch B6): no code finding — B11 had already corrected the stale "outside the pinned closure" comment. **Home assigned: batch C** (the contract v6 bump) — the `TxExtension` swap changes the signed-payload format and the metadata surface the FE reads, so it belongs in the one planned joint-sign-off bump rather than a second FE re-record
