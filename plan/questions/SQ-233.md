---
id: SQ-233
title: **Playbook trigger sources remain unfed (B1b residual).** The effect substrate is live, but `ledger_drift` (I-4 — needs the doc-03 permissionless reconciliation crank, persistent latch and early-lift lifecycle), `depeg` and `oracle_deadlock` (07 detectors), the dead-man detector (B10) and the reserve-health writer (SQ-205 family) have no production writers, so their playbooks cannot activate (fail-closed; 06 §6.2 — guardians cannot act on judgment alone). PB-MIGRATION's recovery sub-step also stays fail-closed: stable2606 exposes no `EmergencyPlaybook`-safe cursor-retry call.
spec_ref: 06 §6.2/§6.3; 03; 07 §11; 09 §3.2
raised: 2026-07-17 (B1b completion; adversarial review)
status: resolved
resolved: 2026-07-24
batch: none
---

## Question

**Playbook trigger sources remain unfed (B1b residual).** The effect substrate is live, but `ledger_drift` (I-4 — needs the doc-03 permissionless reconciliation crank, persistent latch and early-lift lifecycle), `depeg` and `oracle_deadlock` (07 detectors), the dead-man detector (B10) and the reserve-health writer (SQ-205 family) have no production writers, so their playbooks cannot activate (fail-closed; 06 §6.2 — guardians cannot act on judgment alone). PB-MIGRATION's recovery sub-step also stays fail-closed: stable2606 exposes no `EmergencyPlaybook`-safe cursor-retry call.

## Status

**resolved 2026-07-24 (stale — verified against HEAD)** — every trigger the row names now has a production writer: `ledger_drift` from the permissionless `ledger.reconcile` latch, `oracle_deadlock` from `PendingOracleVoids`, `reserve_health` from the constitution bit, plus dead-man and migration-halt. The one unfed trigger is `depeg`, and that is **specified**, not missing: 07 §1 states no authoritative depeg feed, attestation formula, history or trigger lifecycle exists in v1 and PB-DEPEG therefore remains unavailable until a Phase-6-or-later amendment. PB-MIGRATION's fail-closed recovery sub-step is likewise deliberate on stable2606.
