---
id: SQ-1015
title: **The two epoch resolvers disagree when git cannot answer and nothing is injected.** `tools/release/release_common.py` defaults to `0`; `app/tools/release/source-date-epoch.ts` refuses and names the fix. Harmless today — the new reproducibility refusal catches the divergence — but two implementations of one recipe clause is the shape SQ-620 records, and one of them silently substitutes a value.
spec_ref: 12 §1.1
raised: 2026-08-08
status: open
batch: B7
---

## Question

**The two epoch resolvers disagree when git cannot answer and nothing is injected.** `tools/release/release_common.py` defaults to `0`; `app/tools/release/source-date-epoch.ts` refuses and names the fix. Harmless today — the new reproducibility refusal catches the divergence — but two implementations of one recipe clause is the shape SQ-620 records, and one of them silently substitutes a value.

## Status

open
