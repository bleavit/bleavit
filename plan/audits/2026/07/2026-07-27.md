# Audit log — 2026-07-27

## Repository-wide backend security and code review
verdict: **0 critical / 4 high / 5 medium / 12 low / 9 informational confirmed; 16 candidates refuted.** **5 fixed** (2 high): **AUD-1** the execution guard's post-migration fail-static latch was a no-op — `migration_completed` raised `MigrationHalt` directly while `MigrationHaltSources` is its derived authority, so `clear_migration_halt_sources` erased it in the same call and nothing re-raised it (independently found by the Codex runtime pass as `MIG-01`); **AUD-4** two lawful `register_spec` calls could tie on max `activation_epoch`, and 05 §4.6/I-16 make a tie mean *no active spec* permanently — wedging `SnapshotDeadline`, latching the 05 §4.8 dead-man, blocking new proposals, then going silent while welfare `try-state` fails forever (reproduced through the dispatch path before fixing); **AUD-2** the release attestation monitor's ed25519 small-order rejection compared projective 4-tuples with `==`, so all four canonical small-order points were accepted (demonstrated, then one-line fix); **AUD-3** `check-generated-weights.py` passed on an empty inventory; **AUD-5** missing origin-misuse tests on `commit_recovery_image`/`qualify_recovery_image`/`authorize_phase_four`/`attestor.remove_for_cause`/`guardian.uphold_veto`/`recall` (15 §4.1). **2 high initially left open (PR opened as a draft), then fixed on the same branch at the user's instruction (2026-07-27, later the same day):** **AUD-NUM-001** `treasury.spend`/`claim_stream`/`issue_vit`/`recover_foreign` move no asset at all (the pallet has exactly four custody seams, none on those paths; `claim_stream` consumes the entitlement) — closed **fail-closed**, not by building the custody: 08 §1.4 itself defers that wiring ("their outflow custody wiring is the A9 fungibles follow-up"), so the four calls now refuse via a new `OutflowCustody` seam the runtime binds unwired, and arming TREASURY stops loudly instead of reporting grants that never moved; **AUD-NUM-002** decision-window staleness was measured on the un-clipped global gap and never evaluated for the terminal interval, so a book stale at window close still graded decision-grade (04 §7) — closed by clipping each measurement to the window and charging the terminal gap at seal, paired with an independent window-staleness derivation in the reference model and a new `window_stale_scenarios` differential family. Notable refutations: **FIN-01** (contest capital) is verbatim 04 §7a; **RT-03**/**RT-04** are mandated by 06 §2.1 and 13 §1's ratified unbounded-sentinel rule.
pointer: `docs/reviews/backend-security-and-code-review-2026-07-27.md`; Session log 2026-07-27

(implemented non-frontend code; frontend and G2+ excluded by mandate). Six independent workstreams — five Claude reviewers (spec/invariant, financial/mathematical, runtime/origin/XCM/upgrade, node/keeper/ops/supply-chain, verification adequacy) plus three read-only Codex `gpt-5.6-sol` `xhigh` reviews — with an adversarial refutation pass on every candidate.

## Whole-repository security review at `8a95ef4`
verdict: **0 critical / 5 high / 7 medium confirmed; 29 of 41 candidates refuted.** Highs: **MAX-01** `record_snapshot` admits any activated MetricSpec version while `MAX_SNAPSHOTS` counts records and eviction is by epoch age, so the 05 §3.3 two-version window needs 21 records against a bound of 20 — one permissionless extrinsic wedges `SnapshotDeadline`, latches the dead-man, freezes the epoch clock and therefore the prune cutoff, with no origin able to clear it; **MAX-02** `attest` never validates `pid` and `reap_attestation`'s oracle is false forever for an unknown one, so one seated attestor fills the global 256-record bound and permanently denies the CODE/META attestation quorum, including the B16 recovery lane (the same root cause fires with no attacker once cohorts settle, since `settle_cohort` deletes the proposal); **MAX-03** the paired terminal-recovery lane cannot repair a failed Phase-3→4 transition because `pallet_migrations` auto-onboards a cursor the phase lock then stalls, making `recovery_trigger`'s phase arm structurally unreachable and `TerminalRecoveryTransition` refuse its own trigger — permanent `OnlyInherents` with no dispatchable writer for either flag; **MAX-04** the chain-spec validator rejects no unknown top-level key, so a `codeSubstitutes` entry redirects runtime execution on every node booting the published spec while the `:code`↔`runtime.wasm` binding stays green (it hashes genesis only); **MAX-05** the T13 guardian rerun doubles `book.b` without refreshing `last_quote_1e9`, which `observe_book` reads and `close_spot` mirrors, so an untraded rerun records the pre-rerun manipulated price for its whole window — 4.95 pp against the 4.75 pp raised hurdle, at zero marginal cost to the manipulator. Mediums: **MAX-06** no genesis Root-key or `phaseFlags` validation in the release gate; **MAX-07** the keeper builds and signs every call from unvalidated node-supplied metadata with `CheckMetadataHash` waived; **MAX-08** `record_daily_gate` OR-merges any activated version into epoch-keyed breach flags that settle gate markets; **MAX-09** the `is_terminal` oracle uses `is_some_and` where the guard's twin uses `is_none_or`, so attestor liability bonds become permanently unreleasable through ordinary `remove_for_cause`; **MAX-10** `open_stream` debits pot-backed budget lines with no custody leg and `cancel_stream` reverts to MAIN, stranding real USDC in a keyless pot against 08 §1.4; **MAX-11** the weight-drift gate reads whether a function is constant-weight out of a doc comment in the file it is auditing, and nothing pins the committed fidelity header; **MAX-12** the Phase-0 class gate scores `0.000000` on an empty decidable set and the denominator is never exported. Two verified at confidence 7 and recorded sub-threshold: unbacked non-USDC genesis endowments pass the spec validator (exit path currently closed only by a polkadot-sdk guard documented as temporary pending the Asset Hub Migration), and release evidence binds 2 of the drill chain specs. Notable refutation: the oracle `recompute_proof` XOR-fold commitment is genuinely forgeable but unreachable — `Recomputable` has no production writer and `MetricSpec` has no `recomputable` field, so the claimed trigger cannot occur; recorded as an A7 sequencing constraint.
pointer: `docs/reviews/opus-5-max-security-review-2026-07-27.md`; Session log 2026-07-27

— the post-AUD tree, i.e. taking the merged backend audit's own fixes as baseline, and widening scope from the backend to the entire repository (~101k lines of non-test Rust plus ~42k lines of Python tooling, workflows, deploy assets and gate scripts; `frontend/` is a scaffold with no source). Two stages: eight parallel audit agents partitioned by subsystem, then 23 independent verification agents re-deriving each candidate from source, each required to hunt for the compensating control the reporter missed and to adjudicate the DoS, hardening, test-only-file and trusted-CLI/env exclusion boundaries explicitly. Claims resting on SDK behaviour were checked against the pinned crate sources. Findings below verifier confidence 8/10 were dropped. Report-only: no code changed, no statuses flipped.

## Remediation of the whole-repository security review
verdict: **12 of 12 fixed (5 high, 7 medium), plus MAX-S1 and MAX-S2.** Highs: **MAX-01** `record_snapshot` now admits only the epoch's *admissible* version set (active spec ∪ live cohorts' frozen versions, via a new `SnapshotSchedule::frozen_spec_versions` seam bound to the same runtime projection `pallet-registry` uses), **and** the record bound carries 05 §3.3's version multiplicity — `MAX_SNAPSHOTS` 20 → 60 = `SNAPSHOT_RETENTION_EPOCHS × (MAX_CONCURRENT_FROZEN_VERSIONS + 1)` — the `+ 1` is the epoch's own active version, which no cohort need have frozen (Codex round 1), with the epoch-keyed bounds (`MAX_GATE_FLAGS`, the 21-epoch XCM/probe prefix index) and the prune cutoff decoupled onto the new `SNAPSHOT_RETENTION_EPOCHS_BOUND` so none of them silently doubled. **MAX-02** `attest` refuses a `pid` `pallet-epoch` does not carry (new `AttestorProposalStatus::exists`) and enforces a per-signer `MAX_ATTESTATIONS_PER_ATTESTOR = bounds::MAX_LIVE_PROPOSALS × ARTIFACTS_PER_UPGRADE = 64`, derived from demand rather than from a split of supply — room for every artifact the protocol can require of one signer at once, so any *fixed pair* carries 06 §7's 2-of-N quorum for both 09 §3.2 artifacts of all 32 live proposals while one seat still reaches only a quarter of the 256-record ledger. Two earlier sizings were short in the same direction: `MAX_ATTESTATIONS / MAX_ATTESTORS = 16` divided by the *maximum* roster while the roster that must function is the lawful minimum of 3 (Codex round 3), and `MAX_LIVE_PROPOSALS = 32` counted one artifact per proposal where 09 §3.2 mandates a primary and a paired terminal recovery, each needing its own quorum (Codex round 5). **MAX-03** `recovery_trigger` gives the phase-transition cause precedence over any cursor cause instead of matching it only at `Cursor == None`; the wrapper discards an SDK cursor auto-onboarded while it is refusing to service the migrator (`index == 0`, no inner cursor — the exact shape `onboard_new_mbms` writes, so a cursor that advanced is never dropped); and `TerminalRecoveryTransition` **repairs** the registered ledger segment whenever it is still un-migrated (`on_chain_storage_version() == 0`) before completing the Phase-4 transition, clearing a stray retired cursor only once that segment is terminal. Discarding it unconditionally — the round-3 shape — entered Phase 4 on an unapplied ledger migration, since the backfill's nonterminal steps are read-only and only its terminal step commits `TotalEscrowed`/version 1 (Codex round 7). **MAX-04** the chain-spec validator allowlists top-level `ClientSpec` keys and hard-fails a non-empty `codeSubstitutes`/`forkBlocks`/`badBlocks`. **MAX-05** `market_core::double_depth` makes doubling `b` and refreshing `last_quote_1e9` one kernel operation, with `quote_1e9` exported and a try-state assertion that the cached quote equals the book's own LMSR price — which closes the class, not the instance. Mediums: **MAX-06** genesis `sudo.key` + `constitution.phaseFlags` validated for paseo/polkadot with an unfilled `TODO` seat in the allocation template (the Coretime-seat precedent); **MAX-07** the keeper pins the genesis hash and the dispatch target of every call it will sign — the metadata shape bound to the pallet/call index pair the encoder prepends, since the shape hash alone identifies neither the dispatchable nor the pallet and already collides across real pallets in this runtime (Codex round 4) — refusing before signing and logging observed values when unpinned; **MAX-08** `record_daily_gate` requires the epoch's active spec; **MAX-09** the reap oracle is `is_none_or`, matching the guard's twin; **MAX-10** `cancel_stream` reverts to the originating line per 08 §1.4 and the drift alarm measures `line + outstanding stream obligations` against the pot; **MAX-11** `has_components` reads the fresh regeneration, the canonical 50×20 fidelity is pinned rather than compared header-to-header, and the parser rejects a range binding no slope and no parameter; **MAX-12** a class with `decidable_harm == 0` is a violation rather than a `0.000000` pass, and the counts are exported so `check-phase0-exit.py` can enforce a denominator itself. Sub-threshold: **MAX-S1** the accounts exhaustiveness rule applies per declared asset; **MAX-S2** the release evidence binds every selected suite's own chain spec, derived from each drill's topology.
pointer: `docs/reviews/opus-5-max-security-review-2026-07-27.md` §10; Session log 2026-07-27

(`fix/opus5-max-security-review`, off `8a95ef4`). Every confirmed finding fixed in one PR, plus both sub-threshold items; each fix carries a regression test written to fail at baseline.
