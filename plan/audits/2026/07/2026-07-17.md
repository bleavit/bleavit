# Audit log — 2026-07-17

## B7 evidence-producer rulings (SQ-139 producer side; no…
verdict: Session decisions under the user's standing in-session delegation ("Implement B7's evidence-producer side … Don't ask questions. Decide on your own."); ratification requested via SQ-139 + SQ-202…SQ-204
pointer: PLAN.md; code (`tools/env/`, `.github/workflows/`, `tools/release/README.md`)

B7 evidence-producer rulings (SQ-139 producer side; no `docs/architecture/` edits — logged for ratification via SQ-139 + SQ-202…SQ-204).

(1) **The tag pipeline re-executes**: `release.yml` runs the release-tier suites against the built artifacts in the artifacts job (SQ-139's "commit-CI green" alternative rejected as weaker); strict assembly stays the single release-blocking enforcement point — the producer step soft-fails into the attributed B7 gap so dry-run readiness reports survive. (2) **Evidence completeness**: evidence for a kind requires every release-tier suite of that kind attempted and passed — a gated release-tier suite blocks evidence (fail closed; `--include-gated` is the only route while a gate stands); g1-tier exclusions (04/05/08/09, tier map pinned in `validate-environments.py`) are recorded inside the evidence. (3) **Committed-definitions inventory**: `artifact_hashes` covers the committed definitions after generated state is removed; binding to what actually ran is the wasm↔drill-spec genesis check (re-verified post-suite), the live Chopsticks `:code` read before/after block production, the pinned-Zombienet digest check, HEAD==commit, and a clean zombienet/chopsticks/tools-env tree. (4) **Report-only fencing**: cherry-picked `--suites`, non-release tiers, and any custom executable/command force report-only; every runner start invalidates prior evidence; scenario configs must use the bound genesis and may not carry active `wasm-override` or `:code`/`:heappages` injections.
