# Audit log — 2026-08-13

## Exact chain-spec runtime-class closure

verdict: **blocker found and fixed; focused re-review found no remaining blocker in the source patch**
pointer: SQ-1063; draft PR #304; [15](../../../../docs/architecture/15-invariants-and-testing.md) §5

The continuation review followed every release-tier suite to its declared topology rather than
stopping at the ordinary Bleavit spec. It proved a fresh tag checkout lacked the client-parachain
spec required by drills 10–13 and that the old prefix classifier would incorrectly bind that
separate runtime to the OCI primary. The workflow, prerequisite discovery and exact runtime classes
now agree: all declared files must exist; ordinary/raw/migration specs bind to the shipped primary;
client and fast-timing specs are recognized but not misbound; a release suite cannot consist solely
of a separate test runtime; and unknown `bleavit-*` names fail closed.

Behavioral coverage executes the full relay generator with fake pinned external tools, checks the
exact embedded bytes in every applicable output and preserves the deliberately different fast
runtime. The focused environment/deploy/release and documentation gates are green. Artifact closure
still requires a new clean commit and genuine same-commit OCI regeneration; no previous output is
eligible for reuse.

## Same-source artifact closure

verdict: **green; no artifact corruption or source-binding mismatch**
pointer: SQ-1063; source `a4983e85`; draft PR #304;
[15](../../../../docs/architecture/15-invariants-and-testing.md) §5

The genuine OCI pair was rebuilt from the clean reviewed source. Booted primary/recovery metadata
reports contract v31 and spec versions 2/3; both `:code` hashes equal the shipped Wasm and both
independently recomputed RFC-78 digests equal the embedded values. The canonical and release-tier
ordinary/migration specs carry the primary bytes, while the explicit fast and client classes carry
their separate runtimes. Regenerated feed, transcript, descriptors, skills and explainer consumers
passed their focused gates. Assembly reports 315 artifacts, zero corruption and only the ten
declared evidence/readiness gaps; those gaps remain fail-closed and are not security-review
remediation regressions.

## PR #304 failed-check triage

verdict: **four actionable failures identified; fixes implemented and focused checks green**
pointer: SQ-1063; draft PR #304; [15](../../../../docs/architecture/15-invariants-and-testing.md) §4.5

The failing jobs were not duplicates: deterministic app build setup, a feature-gated Rust import,
generated XCM weight drift, and a new no-fix npm advisory each failed independently. The workflow
now fixes the commit epoch before either app build and grants every release Git subprocess
job-scoped trust in only its exact checkout path, without mutating global config. The Rust
import compiles only with its consumer. `pallet_xcm` was regenerated from a runtime built with
`runtime-benchmarks` at committed fidelity; its removed storage dimensions match the public router's
removal of welfare accounting. The advisory waiver is limited to the isolated Lighthouse tooling
path, records the absence of the dependency from `app/dist`, and expires when a non-affected graph
is available. Same-source runtime artifact regeneration remains mandatory before push.

The expanded XCM run also found a stale source digest, not a behavioral regression: the N8
differential's checksum predated the authorized nine-carrier denial while its two barrier arms both
used the amended production deny component. The guard remains intact but now freezes the explicitly
named non-client legacy branch and requires any future digest change to cite its owning decision.
The market failure was also test-only: an unaligned service ID made the ID-band rejection precede
the reserved-funder rejection. The corrected aligned fixture now exercises the intended independent
guard without weakening the pallet's fail-fast order.

## PR #304 same-source CI artifact closure

verdict: **green; source, runtime bytes, feed and strict transcript agree**
pointer: source `13eaabc6`; draft PR #304; SQ-1063

The final source repair was committed before rebuilding. OCI build-info records that exact commit
and epoch; primary/recovery Wasm (`2fa46f46…5d3b` / `c70a3c54…eadd`) equal their booted `:code`, and
embedded RFC-78 equals the independent calculation. Every ordinary canonical/drill spec embeds the
primary. Strict recording has no missing surface, regenerated consumers pass their focused checks,
and assembly reports zero corruption. Independent review found no remaining source blocker; the
stale-provenance blocker it identified is closed by these exact-source feed/transcript replacements.
