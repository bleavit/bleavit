# Audit log — 2026-08-03

## 16 §8.7 — "live" is normative and means *still accruing*; the starvation probe MUST filter on it.
verdict: Claude Opus 5, under the R-1 spec authority the user delegated 2026-07-25
pointer: `16` §8.7; `runtime/bleavit-runtime/src/configs.rs` (`RuntimeServiceContestHealth`, `contest_ratio_1e9`, `prime_register_scan`); `reference-model/.../slot_pricing.py` (`check_starvation_response_shape`)

Raised by the automated Codex review of #227 (P2, confirmed against HEAD and reproduced by negative control). The section already said *"for each **live** Bleavit decision book"*, but never defined live, and the implementation read any window whose `end` matched the proposal's `decide_at`. `pallet_epoch::Proposals` retains every nonterminal proposal and a `TwapWindow` keeps its final integral forever, so one book that was underfunded **once** stayed the minimum ratio and surcharged every later hosted admission. **This is the same failure §8.6 already refuses — a price outliving the condition that set it — reached through the probe's input rather than through storage, and strictly worse there: the stored contention term decays over `svc.max_window`, a stale input never decays.** The amendment states the test (`!sealed` **and** the current block inside `[start, end)`), requires both halves because each closes a case the other misses (sealed early by a market close while the clock says open; past its end but unsealed because the epoch crank has not run), and states the consequence plainly so it is not later read as a bug: **starvation is exactly 0 for most of an epoch**, since decision windows occupy a minority of it and outside them the service is provably not competing with a decision. No new parameter, no contract bump — a behavioural clarification of an existing `[VERIFY]`-inert mechanism, and inert until `svc.price_cap` is adopted.
