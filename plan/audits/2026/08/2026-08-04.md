# Audit log — 2026-08-04

## The last two `[VERIFY]` `svc.*` rows are adopted, and the hosted service is open: `svc.client_bond` = 100,000 VIT, `svc.price_cap` = 4x.
verdict: Chralt98 (2026-08-04 ruling, via AskUserQuestion: client bond *"100,000 VIT \u2014 conservative"*, price cap *"Seat at 4"*, max_live *"Leave at 16"*), under the values-layer authority the user owns
pointer: `13` §1 (both rows + both notes); `16` §8.6/§8.7; `02` §9; `crates/constitution-core`; `tools/limit-coverage/{registry.toml,genesis-keys.json}`; `reference-model/.../registry.py`; `docs/integration/costs.md`; `docs/design/claude-design-kit/`

User ruling 2026-08-04, taken after the three rows were laid out with their bounds, anchors and error directions. Both are **chosen, not derived**, which is R-2's escalation clause rather than a gap in it: registration-abuse calibration and TH-72's attack-cost cell (SQ-574) would be the anchors and neither exists, so the authority is recorded instead of a derivation being reverse-engineered. **Adoption did not close SQ-574.** *Client bond:* the only in-system anchor is `att.bond` = 25,000 VIT, the sole other VIT-denominated bond, and 100,000 is exactly 4x it — the user chose the conservative rung over the recommended 5,000, treating a hosted client as a higher-risk counterparty than a seated attestor. **Seeding this row is the act that opened the service**: it was the only one of the three that gated admission, since `svc.max_live` already carried its provisional 16 and an absent `svc.price_cap` meant `M = 1` rather than a refusal. The stated cost, flagged before the decision and reaffirmed: at 4x the attestor bond the first cohort is institutions rather than experiments, so 16 §8.4's cannibalization falsifier and `svc.max_live`'s sizing evidence both stay open longer. That is the direction that cannot create an unbacked claim, and Factor(2) with a 2-epoch cooldown makes it ~6 weeks per halving. *Price cap:* 4x bounds the tariff multiplier, and pairs exactly with `svc.max_live` = 16 — the per-admission step is `(4-1)/16` = 0.1875, i.e. 3e9/16 = 187,500,000 grid units with no remainder, so full occupancy lands **on** the ceiling rather than short of it, and 4 is far enough above 1 that the step cannot truncate to zero. That exactness is a property of the **pair**, so the constitution's genesis test asserts the divisibility rather than leaving it to inspection. One row arms both halves of `M` (§8.6 contention, §8.7 starvation). *`svc.max_live` left at 16*, which is load-bearing: the frozen kernel constant `SVC_FEE_FLOOR_USDC` = 393 was derived at 16 slots (`109,281 / (17.39 x 16)` = 392.75), so lowering it would make an unamendable floor under-recover. **Route: pre-genesis, so both rows were seeded directly into `genesis_params()`** — the `svc.fee_bps` precedent — not by the runtime migration 13 §1 describes for a live chain; that paragraph now says which route applies when. **The fail-closed path is kept, not deleted**: the runtime test that proved it now does so by *removing* the row, because the consumer still reads the live row with no fallback and dropping that coverage because the default changed would have been the silent half of this change.

## 02 contract v23 → v24 — the twelve storage reads docs 10/11 mandate and §7 never froze (SQ-580).
verdict: The user (owner for both sides under R-1), standing autonomous-resolution delegation
pointer: `02` §7.1/§7.6/§8/§13; `crates/futarchy-primitives` (the single literal + its pin test); `tools/release/surface-manifest.json`; `AGENTS.md`; `.github/workflows/ci.yml`

§7's own closing rule says unlisted storage "is not contract surface, and their raw decodability through portable metadata is not guaranteed", while 10 §4.2 and 11 §11.3/§11.5/§11.7.2 make twelve specific reads compulsory — so the contract denied status to reads its companion documents required. §7.1 gains `ResourceLocks` (pallet-epoch's own item, absent from its own table while 09 §1.2(8) makes it a dispatch check); a new **§7.6** freezes the eleven upstream reads — `Multisig.Multisigs`, four `Referenda`, two `Preimage`, two `ConvictionVoting`, `Scheduler.Agenda`, `System.Events`. **The ruling that needed making** is whether upstream SDK storage belongs in a Bleavit-owned contract at all, and the answer is that it belongs *more* than Bleavit's own: Bleavit storage moves only when Bleavit changes it, and §13 rule 2 makes that bump this counter, whereas an SDK release-line bump moves an upstream layout with no Bleavit source change and no counter to notice. §7.4's existing `System.Account` / `ForeignAssets.Account` rows are the same judgement already taken. **One deliberate loss of expansion, recorded because it is a weakening:** `System.Events` freezes its `EventRecord` container with `RuntimeEvent` elided, since §6 already freezes that subtree event-by-event and expanding it costs 2.2 MB and a signal that fires on any pallet's new event — admissible only under that condition, which is why `OriginCaller` (frozen by nothing else) stays expanded. Additive: no existing type, key, event, constant, API method or call index moves, no dispatchable is added, `transaction_version` untouched (rule 7), pre-genesis so no migration.
