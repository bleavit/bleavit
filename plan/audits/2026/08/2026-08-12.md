# Audit log — 2026-08-12

## Whole-repository Codex security review

verdict: **0 Critical / 5 High / 8 Medium / 2 Low confirmed; 10 novel findings recorded as SQ-1057…SQ-1066; 5 existing findings revalidated**
pointer: `docs/reviews/codex-repository-security-review-2026-08-12.md`; SQ-504, SQ-1032, SQ-1035, SQ-1057…SQ-1066; B21

A read-only review of commit `08f77ce12a737ef0f2ba8174d829b527ae33f05b` covered the architecture,
runtime/pallet/XCM code, frontend/signing stack, hosted service, node/keeper, release/deploy and
monitoring tooling, workflows, dependencies, reference/simulation models and verification harnesses.
The four new High findings are retained-capacity exhaustion by one admitted hosted-service client,
a caller-fabricable frontend signing gate, replay of a genuine gate proof against independent
bytes/account, and permissionless XCM-exit pumping of a decision-grade health metric. The already
tracked High is SQ-1035's impossible unattended release-credential
source. Mediums include the six-instruction XCM default-deny mismatch, unproved release-monitor RPC,
webhook-secret logging, shared reproducible-build failure domain, permanent client report-capacity
exhaustion, and the already tracked organizational-attestation, metadata-hash and migration-latch
gaps. Two Low hardening findings cover gateway identity/divergence and mutable GitHub Action refs.

The online six-lockfile supply-chain gate passed. Focused evidence also passed: 400 CI-tool tests
(3 skipped), 102 release, 172 deploy (2 skipped), 111 monitoring, 74 phase-gate, 768 reference-model,
110 simulation (1 skipped), 1,000,000-case rewards property shard, 22 feature-enabled runtime tests,
and the repository's limit/weight/alert/runbook/ABI/client-surface/chain-feed controls. No live-chain,
Arweave, key-ceremony, device/browser lab, long fuzz/model-checking or duplicate exhaustive Rust gate
was run. The full report states exact evidence, impact, remediation and limitations.

## Remediation pass for PR #304

verdict: **all 15 review findings implemented; independent adversarial re-review found and closed integration, benchmark, monitoring and documentation blockers before handoff**
pointer: SQ-504, SQ-594, SQ-1032, SQ-1035, SQ-1057…SQ-1066; B21; draft PR #304

The review checkpoint was committed and published first (`3b70e09e`), preserving the requested
review-before-fix ordering. The subsequent pass remediates every finding: derived independent
hosted-service retention partitions; non-fabricable, exact-target signing authorization; protocol-only
XCM health and exact nine-carrier denial; independently pinned release credentials, organization
thresholds, secret-safe logging, exact multi-RPC agreement and any-gateway-dissent handling;
digest-pinned builds/actions; reusable-client report pruning; metadata-hash mode 1 bound to the final
Wasm; and a sticky, repairable recovery-image halt source.

The adversarial re-review caught secondary defects in the first patches and they were fixed before
closure: sealed questions now honor the O(1) pause cutoff; benchmarks no longer encode old scans;
the market snapshot materializes the full 17,984-book state and all heavy auxiliary maps/vaults;
client pruning uses `translate` with conservative full-capacity charging; the monitoring exporter
uses `MaxAllStoredMarkets` for the physical map and separately exposes all four logical partitions;
release assembly compares independent and embedded RFC 78 digests; and the clean-CI tooling job
installs the pinned Node dependency required by that cross-language check. The final monitoring sweep
also closed a browser-route gap: every gateway's raw path manifest must be byte-identical, its
`index.path` must be listed, and both immutable and named root routes must serve that verified index
payload rather than a gateway-specific substitute.

Committed-fidelity 50×20 regeneration moved market `try_state` from 38,603 to 280,782 reads and
18,055,139 to 137,064,675 estimated proof bytes, reflecting the complete reachable retained state.
Question registration/pause shed obsolete scans and settle/void each gained the new cutoff read.
Unrelated timing-only remeasurement noise was reverted per the 2026-08-11 per-function rule. The
source commit is intentionally made before runtime/feed artifacts so build-info v3 cannot falsely
claim an older clean Git SHA for dirty source; genuine digest-pinned OCI regeneration follows that
commit.

A final source-checkpoint review found one release blocker in SR-13 itself: the mandatory
recovery-image retry and migration-completion callback performed the production preimage unpin while
returning only guard/bridge weight, and completion omitted its anchor deletion too. The repair adds a
runtime-supplied bounded unpin seam, binds production to generated `pallet-preimage`
`ensure_updated(1) + unrequest_preimage()`, and accumulates the guard-owned commitment read/delete and
completion-anchor delete on both failure and repaired-success paths. Exact pallet and production-runtime
regressions cover the failed completion, next-block repair and preservation of an independent halt
source.

A second independent source pass then invalidated two earlier closure claims. SR-02's first patch
owned the pin but still accepted caller callbacks, so `derive` could attach arbitrary fabricated
verdicts to an irrelevant genuine read. The corrected production API has no evidence/callback port
and intentionally cannot produce `GatePassed` until its complete closed evaluator exists; the test
mint and transition enumerator are quarantined behind two named, witnessed production firewalls.
SR-06's first patch solved circular credential addressing but still consumed a schema no app producer
emits and treated asset M as final M′. The corrected monitor consumes the actual app-v1 document,
validates both manifest trees/routes separately and binds explicit index M′ to the resolver and
`ReleaseChannel`. The credential-index integration test uses the real producer fixture. A final
read-only adversarial audit found no exploitable blocker in either corrected patch; remaining caveats
are explicit fail-closed staging and organizational/transport trust assumptions, not bypasses. Its
last normative cross-check found §11.4's pseudocode omitted the preparation and immutable signing
target from a passing proof; the snippet and rule set now require both, preserving SR-03's exact
bytes/account binding when the closed evaluator is eventually implemented.
