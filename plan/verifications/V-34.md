---
id: V-34
date: 2026-07-17
milestone: B11
title: **polkadot-stable2606 line adoption (D-19, B11)** — the full 01 §9 pin family re-sourced against live crates.io
---

## Item

**polkadot-stable2606 line adoption (D-19, B11)** — the full 01 §9 pin family re-sourced against live crates.io

## Spec ref

01 §9; 15 §4.5

## Status

verified 2026-07-17

## Result

65 `=`-exact pin sites (63 root `[workspace.dependencies]` + the guard's dev-dep `sp-version` + the runtime's dev-dep `sp-externalities`) plus the 2 SDK tool pins and the env binary tag, all sourced from a 66-entry `polkadot-sdk 2606.0.0` umbrella-manifest map (the crate-local `sp-version` shares its root entry) (the newest umbrella; `polkadot-omni-node-lib 0.18.0` its newest front, confirming the B11-row evidence); at adoption no family crate had a patch release beyond the umbrella-named version, so every `=` pin is umbrella-verbatim; `wasmtime` resolves 36.0.12 under `sc-executor-wasmtime 0.47.0` (all 16 waived advisories gone from a no-ignore `cargo-audit` run); `sc-network 0.58.0`/`sc-tracing 47.0.0`/`libp2p 0.54.1`/`litep2p 0.14.3` confirm the residual-alert predictions; XCM v5 still the latest wire version (`staging-xcm 24.0.0` ships v3/v4/v5 — 09 §6.1 row re-verified); `core2` absent from the 2606 closure (`[[patch.unused]]` → vendored patch + `vendor/` removed); rustc 1.89.0 still builds the tree (no rust-version constraint in the family); `polkadot-stable2606` GitHub release verified present with all four `tools/env` binaries + `.sha256` sidecars (`POLKADOT_SDK_TAG` moved); `staging-chain-spec-builder 19.0.0` + `frame-omni-bencher 0.22.0` are the train's tool versions
