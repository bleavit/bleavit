---
id: V-25
date: 2026-07-17
milestone: —
title: Stable2603 upgrade-path mechanics for B6: (a) `system.apply_authorized_upgrade` consumes `AuthorizedUpgrade` **before** `can_set_code`/`OnSetCode`; (b) Cumulus `schedule_code_upgrade` has exactly five pre-write rejection conditions (missing validation data, upgrade-restriction signal, overlapping pending code, missing host configuration, relay max code size) and no panic path; (c) relay `UpgradeGoAhead::Abort` deletes `PendingValidationCode` **without invoking `OnSystemEvent`** (`cumulus-pallet-parachain-system-0.26.0` src/lib.rs:739); (d) candidate `RuntimeVersion` derivation mirrors `frame_system::can_set_code` via `sp_io::misc::runtime_version` (deterministic consensus-host behavior)
---

## Item

Stable2603 upgrade-path mechanics for B6: (a) `system.apply_authorized_upgrade` consumes `AuthorizedUpgrade` **before** `can_set_code`/`OnSetCode`; (b) Cumulus `schedule_code_upgrade` has exactly five pre-write rejection conditions (missing validation data, upgrade-restriction signal, overlapping pending code, missing host configuration, relay max code size) and no panic path; (c) relay `UpgradeGoAhead::Abort` deletes `PendingValidationCode` **without invoking `OnSystemEvent`** (`cumulus-pallet-parachain-system-0.26.0` src/lib.rs:739); (d) candidate `RuntimeVersion` derivation mirrors `frame_system::can_set_code` via `sp_io::misc::runtime_version` (deterministic consensus-host behavior)

## Spec ref

09 §2.1(4)–(6) [VERIFY go-ahead mechanics + failure semantics]

## Status

complete (2026-07-17, B6)

## Result

Read from the pinned crate sources in the local registry. Consequences implemented: fail-closed apply preflight reproducing all five Cumulus rejections (authorization cannot be consumed by a doomed schedule), applied-detection at `on_validation_code_applied` (not at scheduling), and the SQ-131 abort ruling (status-quo clear + `UpgradeAborted` + guardian-visible flag). 09's two §2.1 `[VERIFY]` tags can be textually cleared at the next 09 touch.
