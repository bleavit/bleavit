---
id: V-37
date: 2026-07-21
milestone: —
title: **ss58-7777 identities of the ten R-4 genesis-endowed protocol accounts** — the chain-spec validator hardcodes these addresses, so they needed an independent derivation before the gate could be trusted
---

## Item

**ss58-7777 identities of the ten R-4 genesis-endowed protocol accounts** — the chain-spec validator hardcodes these addresses, so they needed an independent derivation before the gate could be trusted

## Spec ref

03 §7 R-4; 02 §8

## Status

verified 2026-07-21

## Result

Re-derived from the substrate `AccountIdConversion for PalletId` rule (`b"modl"` + 8-byte `PalletId` + optional 8-byte sub-seed, zero-padded to 32) in **three independent implementations** — mine, the deploy agent's, and the runtime's `r4_account_addresses_are_stable` test — all agreeing byte-for-byte. Control: the three pot addresses already pinned in `PROTOCOL_POTS` (`treasury MAIN`, `community`, `incentives`) reproduce exactly from the same helper, which is what makes the ten new ones trustworthy rather than merely self-consistent
