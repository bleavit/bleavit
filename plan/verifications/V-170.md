---
id: V-170
date: 2026-08-06
milestone: —
title: **10 §9's resource budgets re-derived against 13, 02 §5 and the runtime** (SQ-557's ruling)
---

## Item

**10 §9's resource budgets re-derived against 13, 02 §5 and the runtime** (SQ-557's ruling)

## Spec ref

10 §9.1–§9.4; 13 §1/§3.1/§4/§5; 02 §5

## Status

Verified 2026-08-06

## Result

Every input read from its owning source rather than trusted from the filed question, and two of the filed/derived numbers moved as a result. **Observing count**: `crank_observe` routes through `ensure_trade_admissible` → `market_core::ensure_trade_phase`, so only `Trading`/`Extended` books emit; Trade is `[5/21, 18/21)` and cannot overlap the next epoch's, giving `epoch.slots·6 + 1` = **31** against §9.1's 196 — and 13 §5 item 4 states the same 31 for the keeper load, so the two documents already disagreed. **Slate cap**: 13 §5 item 2's frozen 52 = `32 + 4·epoch.slots` makes 5 the largest admissible slot count, which is what makes 31 a maximum rather than a typical. **Rate model reproduced exactly**: `14,400/10 × 13/21` = 891.428571 rows/book/day regenerates every one of the *old* published cells at 196 books (8.59 d, 106.29 d, 2.15 d, 26.57 d), localising the entire discrepancy to the book count. **Fill ceiling measured, not assumed**: `MAX_POV_SIZE` is **10 MiB** in `polkadot-primitives 25.0.0` (Cargo.lock), not the 5 MiB SQ-557 assumed, and `buy`'s **dispatched** weight is `(9,769,260,000, 111,860)` — the generated file says `(9,719,260,000, 108,804)`, the difference being the `#[pallet::weight]` attribute's two reads and `EXTERNAL_TRADE_ROUTE_PROOF_SURCHARGE`. Against `primary_capacity()` = `(1.5e12, 7,864,320)` that is **70** fills/block by proof size and 153 by ref_time, so proof size binds. SQ-557's 36 and my own first hand-derivation's 72 were both wrong, in opposite directions, from the same class of error the finding is about. **Metadata blob measured**: `gzip -9` over the committed 469,581 B `metadata.scale` is 146,792 B = **0.140 MB**, against §9.3's assumed "~1–2 MB". **Citation checked**: doc 13 contains no 300 MB / 75 MB row, confirming §9.2's normative citation resolved to nothing
