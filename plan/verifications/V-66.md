---
id: V-66
date: 2026-08-02
milestone: N8
title: **N9 hosted-report egress and I-36 closure**
---

## Item

**N9 hosted-report egress and I-36 closure**

## Spec ref

02 §3/§4a/§13; 12 §6.3; 15 I-24/I-36; 16 §2/§9–§12

## Status

verified 2026-08-02

## Result

Final requested gates: `cargo fmt --all` exit 0 with no output; changed Rust gate exit 0 with runtime **429/429**, `bleavit-xcm` **69/69**, client-registry core **7/7**, client-registry pallet **16/16** and question-service pallet **27/27**; reference model **652/652**; monitoring **24 rows / 53 rule metrics**; runbooks **14 / 24 rows**; limit coverage **205 keys / 0 unwired**; PLAN table and spec-question-batch gates green. `tests_n9::i36_egress_is_client_paid_best_effort_non_welfare_across_a_full_epoch` binds all four structural preconditions, forces every measurable-day push to fail `NoChannel`, retains the authoritative report and USDC float on failed delivery, advances only the service diagnostic, and leaves every welfare counter and `X = 1_000_000_000` unchanged. Both widened-storage pallets were regenerated at committed 50×20 fidelity; no pallet or storage item was added. Gate history retained: the first Rust pass rejected an untyped `Result<_, ()>` and constant-false assertion; the next exposed three missing call inventories; the following pass exposed the intentionally additive two-call `ExternalClient` domain in N8's literal expected set. Each was fixed and rerun. A local targeted test without the prescribed short target also hit the documented eCryptfs `File name too long`; the exact required gate used the verified `/tmp` target/libclang/workspace-hint environment and passed. Independent final compliance review: no blocker, major or minor.
