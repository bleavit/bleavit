---
id: V-23
date: 2026-07-16
milestone: B7
title: Relay chain-spec route for `paseo-local` zombienet topologies (B3 chose relay id `paseo-local`)
---

## Item

Relay chain-spec route for `paseo-local` zombienet topologies (B3 chose relay id `paseo-local`)

## Spec ref

02 §8; 15 §4.7

## Status

complete (2026-07-16, B7)

## Result

The stock `polkadot` binary ships **no** `paseo-local` preset; the canonical pattern (docs.polkadot.com "Run a Parachain Network") is `chain_spec_path` → a `paseo-local.json` generated by the `chain-spec-generator` of **paseo-network/runtimes** (latest tag **v2.4.1**, 2026-07-15) with stock `polkadot` + `polkadot-prepare-worker`/`polkadot-execute-worker` from a pinned polkadot-sdk release. Relay/system-para binaries pinned to **polkadot-stable2603-1** — the same maintenance line as the workspace's 01 §9 crate pins (V-15). **Correction (B7 adversarial review):** the `chain-spec-generator` package is **absent** from paseo-network/runtimes v2.4.1 (verified against its Cargo.toml — dropped in the v2.x line); the last tag carrying it is **v1.9.2**, pinned by tag **and commit** `232499a48d77e9780679c42f1277861a17af9a48` in `tools/env/pins.env` (r0gue-io/paseo-runtimes ships cross-platform builds of the same generator per tag).
