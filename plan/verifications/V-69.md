---
id: V-69
date: 2026-08-02
milestone: N7
title: **N7 SECOND FIX ROUND — residual blockers repaired, not closure**
---

## Item

**N7 SECOND FIX ROUND — residual blockers repaired, not closure**

## Spec ref

05 §4.1/§4.3; 13 §1; 15 §4.5/PT-10; 16 §8.5/§12

## Status

verified 2026-08-02 (compliance re-review open)

## Result

`H` now runs the original integer formula in physical `max_block` coordinates with no independently rounded scale/target. The zero-external sweep uses stride **1,000,000** across the 1e9 grid plus ±2 neighborhoods around the kernel target and full-utilization boundary; equality held for every probe, with no deviations to enumerate. The shared `pallet_welfare::block_utilization` helper selects the maximum of normalized ref-time/proof-size utilization for both total and primary samples; the proof-size-bound regression passes. The replay is honestly `n7_service_traffic_containment_sample`, not PT-10: measured registration, every encoded stored snapshot and decision input/outcome, real dispatcher-routed observe, and service traffic between every adjacent primary call are covered over two finite schedules. PLAN records the uncovered unrestricted arbitrary service alphabet/interleaving space required by normative PT-10. The three accepted Normal residual paths are documented and source-tripwired; Mandatory/system primary accounting saturates as an upper-bound estimate. Final evidence: `cargo fmt --all` exit 0; changed Rust after final weight generation runtime **441/441**, primitives **25/25**, welfare **108/108**; fuzz fmt/locked Clippy/locked tests **11/11**; welfare properties **9/9** at **1,000,000** cases; reference model **652**; limit coverage **205 keys / 0 unwired**; weight regression `PASS WITH ACKNOWLEDGEMENTS`; PLAN tables, spec-question batches and 97 local Markdown links pass. At 50×20, `pallet_welfare` **7** functions and `cumulus_pallet_parachain_system` **4** functions were remeasured; storage dimensions matched. Generated-weight purity checked **380** functions and storage-bound coverage checked **22** annotations. The first table-check attempt was RED: `Markdown table structure errors: - PLAN.md:1846: orphaned table row(s) — a pipe block must open with a header row followed by a separator (a blank line above these rows probably severed them from their table)`; removing that blank line made the final table check pass. No code gate was red in this round; N7 remains 🔨 and is not marked ✅.
