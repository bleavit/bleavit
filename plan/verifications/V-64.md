---
id: V-64
date: 2026-08-01
milestone: Instance1
title: **N7 hosted-question pallet, ServiceLedger composition and I-37/PT-9 closure**
---

## Item

**N7 hosted-question pallet, ServiceLedger composition and I-37/PT-9 closure**

## Spec ref

02 §4a/§7.1/§9/§13; 03 §1a; 16 §4–§8/§10–§11; 15 I-37/PT-9

## Status

verified 2026-08-01 (authorized scope; milestone residual)

## Result

Final requested gates: `cargo fmt --all` exit 0 with no output; changed Rust gate exit 0 with runtime **422/422**, conditional-ledger core **15/15**, default ledger **87/87**, Instance1 differential **8/8**, PT-9 **7/7**, question-service pallet **26/26**, service core **18/18**; limit coverage **205** keys / **0 unwired**; reference model **652/652**. Supplemental TLA⁺ composition/service models and both mutation witnesses, service settlement fuzz target, canonical 50×20 question/market weight generation, monitoring/release/deploy surface suites and two independent reviews are green. Gate history is retained: Clippy first rejected constant assertions, then a redundant closure/manual-default form; runtime inventory checks then exposed missing QuestionService/ServiceLedger I-20/S5 rows and the intentional `ExternalClient` local-signed projection; the next full run caught a one-base-unit `C_disp + C_hold` rounding-order regression; standalone limit coverage caught two implementation markers plus the absent `MaxServiceAttestors` classification; the reference suite caught the three newly seeded service records and regenerated crank-weight pins. A final hard-constraint scan removed new test/benchmark `expect`/`panic!` uses; its supplemental benchmark run exposed and fixed a compact-mock primary-account predicate that excluded the canonical POL stand-in (**118/118** after repair). The fuzz gate's first corpus run reached LeakSanitizer and failed only because the ptrace sandbox is incompatible; the approved unsandboxed rerun passed all four corpus legs and one-second smokes. Every failure was repaired and rerun rather than waived. The remaining 16 §8.4–§8.5/PT-10 resource-partition proof requires the user-forbidden welfare scope, so this verification does not claim it.
