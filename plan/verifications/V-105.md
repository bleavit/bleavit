---
id: V-105
date: 2026-08-04
milestone: —
title: **02 §7.7's asset index, on the Asset Hub this release actually targets**
---

## Item

**02 §7.7's asset index, on the Asset Hub this release actually targets**

## Spec ref

02 §7.7, §8; 08 §2.5; 09 §6.3

## Status

**verified 2026-08-04, tag cleared**

## Result

**The tag's scope was wider than V-17 discharged, and that is the finding.** V-17 (2026-07-16) verified index **1337** as Circle-native USDC on **Polkadot** Asset Hub, from Circle's pages and referendum #174. §7.7 pins *the Asset Hub of the relay the release targets*, and 08 §2.5 / 09 §6.3 phase that — Paseo at Phase 2, Polkadot at Phase 3 — so at the current phase the row is about **Paseo** Asset Hub, whose asset registry is its own and whose ids nothing in this repository had checked. Read `Assets.Metadata(1337)` by exact storage key from two independent operators (Dwellir, TurboFlakes); both returned the same bytes, decoding to name `USD Coin`, symbol `USDC`, **6 decimals**, `is_frozen: false`. `Assets` is pallet instance **50** in that runtime's metadata, which independently confirms the `PalletInstance(50)` half of §8's Location. Both 02 tags cleared (§7.7's row and §8's `[VERIFY … at implementation]`), with the two-chain scope written into §8 so the next relay phase re-asks rather than inherits
