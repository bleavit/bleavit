---
id: V-68
date: 2026-08-02
milestone: N7
title: **N7 FIX ROUND — four-defect repair evidence (not closure)**
---

## Item

**N7 FIX ROUND — four-defect repair evidence (not closure)**

## Spec ref

05 §4.1/§4.3; 13 §1; 15 §4.5/PT-10; 16 §8.5/§12

## Status

verified 2026-08-02 (repair; compliance re-review open)

## Result

H now derives `target_primary` and the unbounded primary scale from the existing kernel target, live `max_block`, and live primary capacity; the independent physical crossover test measures H=1 at **0.400000000** and H=0 at **1.000000000**, with the same first falling/last-positive grid points before and after partitioning. Operational/Mandatory signed dispatches bypass only the partition refusal (`prepare` returns no reservation) while residual folding still measures them as primary; the cap-admission and primary-sample `try_state` regressions pass. The extension-order regression pins `CheckWeight` before `ResourcePartition`. PT-10 is now a 21-block replay with traffic before and after every primary point, actual `register`/`open`/`observe`/`seal`, nested ledger work, signed `TransactionExtension` success and failure/refund paths, XCM, and varied proof sizes; primary samples, production components, snapshots, SCALE decision inputs and outcomes remain byte-identical. The four preserved axes and no-new-13-key boundary remain green. `cargo fmt --all` passed. The first changed-scope Rust gate was **RED** on seven Clippy test-harness lints; after fixing those lints the final rerun passed (runtime **439/439**, primitives **25/25**, welfare **108/108**). The exact fuzz fmt/locked Clippy/locked test command passed (**11/11**); welfare property passed at **1,000,000** cases (**9/9**); reference model **652/652**; limit coverage **205 keys / 0 unwired**; weight regression ended `PASS WITH ACKNOWLEDGEMENTS` (including the pinned N7 `sample_block_weight` storage increase). Regenerated at 50×20: `pallet_welfare` **7** functions and `cumulus_pallet_parachain_system` **4** functions; storage dimensions matched fresh measurements. Documentation table, spec-question-batch and local-link gates passed. The initial long-target Rust attempt was **RED** with eCryptfs `File name too long`; the prescribed short `/tmp` target, real `libclang.so` symlink and workspace hint made the release build and changed gate pass. N7 remains 🔨 and is not marked ✅; no compliance re-review has been claimed.
