---
id: V-322
date: 2026-08-07
milestone: M5
title: **Were `ConditionalLedger.Vaults`, `ConditionalLedger.BaselineVaults` and `ServiceLedger.Vaults` ever read, and what did `positions.test.ts`'s S4-row test measure?**
---

## Item

**Were `ConditionalLedger.Vaults`, `ConditionalLedger.BaselineVaults` and `ServiceLedger.Vaults` ever read, and what did `positions.test.ts`'s S4-row test measure?**

## Spec ref

11 §11.2 S4 row, §11.5, §11.6; 02 §3, §7.4; 10 §4.2 FE-P2

## Status

**measured 2026-08-07** (7 mutants M5–M11, 0 survived)

## Result

**None of the three was read, and the test measured an object literal.** `POSITION_READS` declared all three under the comment *\"the frozen 02 surfaces this screen reads\"*; `readBook` issued one `crossCheckedCall` per domain and no `storage` call at all, and `screen-composition.ts` built no key, decoder or splitter for any of them — `PositionsReader` had no `storage` method to make one with. So `vault_state` reached the VOID layout and the redemption-call selection through `PositionView` alone, on the one field that decides which payout call is offered. The guarding test asserted `'baselineVaults' in POSITION_READS.primary`, a property check on a frozen record in the same file. Repaired: per-domain `vault(pallet, pid)` and a single-instance `baselineVault(epoch)` key, per-domain vault decoders plus the branch-free `BaselineVaults` projection, a per-vault memo, and a cross-check that drops the row on disagreement or on a missing entry. The suite now asserts the reader port's **recorded `storage` calls**, which mutant M5 (read dropped, declaration kept) fails
