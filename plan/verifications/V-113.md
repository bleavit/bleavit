---
id: V-113
date: 2026-08-05
milestone: F16
title: **F16's nine governance reads are frozen, manifested and probed — and a first grep said the opposite**
---

## Item

**F16's nine governance reads are frozen, manifested and probed — and a first grep said the opposite**

## Spec ref

02 §7.6; 11 §7.2; 10 §5.2

## Status

**verified 2026-08-05, three ways**

## Result

The spec-first check before building F16, run because the same check on F12 had just found fourteen missing rows. A grep for the bare item names (`ReferendumCount`, `ClassLocksFor`, …) in 02 returned **NOT IN 02 for all nine**, which reads as an SQ-580-class gap across a whole milestone. It was a **grep artifact**: 02 §7.6 lists them in the qualified `` `Pallet.Item` `` form, so a pattern matching the bare item never fires. Confirmed the other way — `tools/release/surface-manifest.json` carries each with `"citation": "02 §7.6"`, and `CRITICAL_SURFACE` carries `storage.referenda.*`, `storage.conviction_voting.*` and `storage.scheduler.agenda`, so the 10 §5.2 lattice can fail on them. **No gap; F16 is buildable.** Recorded because it nearly became a false spec question, and a false SQ costs a later session more than a missing one: **search 02 by the qualified name, never the bare item.** Second time today a first pass over the docs was wrong in the alarming direction — SQ-593 was the other
