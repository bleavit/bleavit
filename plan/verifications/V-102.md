---
id: V-102
date: 2026-08-04
milestone: —
title: **Every F11 control proven to fail by mutation, not by a green run**
---

## Item

**Every F11 control proven to fail by mutation, not by a green run**

## Spec ref

12 §1.1, §5.1–§5.3; 10 §5.4, §10.1; 15 §4.8

## Status

verified 2026-08-04

## Result

Nine injections, each reverted and the tree re-verified clean. **No-literal gate:** appending `MAX_LIVE_MARKETS = 196` to a real scoped package fires rule A; `201600` fires rule B; the `--witness` leg fires both against its fixture and exits 1 if either stops matching. **connect-src no-growth diff:** adding `https://vendor.example` to the declared gateways aborts the build naming the addition (15 §4.8). **Placeholder substitution:** replacing the service worker's asset-map token aborts with *"already substituted (a stale tree) or the source stopped emitting it — both would ship a release whose control was silently not applied"*. **SRI:** stripping `integrity=` from a built `dist/index.html` fails `release:check`. **MockSigner exclusion:** `assertNoTestOnlySigner` throws against `tools/fixtures/release-tree-witness/`, a tree that contains one — the gate is about the *emitted chunk*, so it is proven against a chunk. **Determinism:** the probe reports an embedded build path and a home directory as two separate findings. **Worker policy:** an empty asset map, an empty cache name, a non-hex digest and a `__proto__` entry are each refused rather than skipped
