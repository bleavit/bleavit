---
id: V-386
date: 2026-08-02
milestone: —
title: **N10 client integration kit and executable quickstart binding**
---

## Legacy ID

V-72

## Item

**N10 client integration kit and executable quickstart binding**

## Spec ref

02 §3/§4a/§12; 09 §6.5; 10 §10; 15 §4.5/§4.7; 16 §2/§3/§5/§11–§12

## Status

verified 2026-08-02

## Result

`cargo fmt --all` exit 0; changed Rust gate exit 0 with ABI **5/5**, client pallet **6/6** and standalone client runtime **2/2**; ABI and client-pallet no-std checks green; required fuzz workspace command first refused because `fuzz/Cargo.lock` was stale under `--locked`, then the lockfile was refreshed offline and the exact fmt, Clippy and test command passed with fuzz **11/11**; reference model **652/652**; environment definitions valid; Markdown links **98/98**; PLAN tables and spec-question batches green; quickstart binding green; TypeScript typecheck and all drill-helper `node --check` runs green. Supplemental standalone-runtime no-std check remains red in the upstream `cumulus-primitives-proof-size-hostfunction` crate because its no-std build references std-only `ProofSizeExt` and `Vec`; the client ABI and pallet no-std checks are green.
