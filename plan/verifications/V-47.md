---
id: V-47
date: 2026-07-23
milestone: P1
title: **B19 bonded attestor lifecycle and handoff verification**
---

## Item

**B19 bonded attestor lifecycle and handoff verification**

## Spec ref

02 §7.5; 06 §7; 09 §1.2(5); 15 I-19

## Status

verified 2026-07-23

## Result

Targeted `attestor-core` **11/11**, `pallet-attestor` **26/26**, and runtime **326/326** passed before handoff; the changed-crate Clippy pass caught and fixed one redundant-pattern test lint, then the exhaustive `tools/ci/rust-workspace-gates.sh` passed once on the coherent code state (workspace tests, weight regression, release/runtime-benchmarks/try-runtime/no_std profiles, keeper, reference model, doc-table and limit coverage **184/184**). A valid Codex P1 then identified `FailedExecuted` as retryable; commit `ea330b0` removes it from both runtime attestor predicates and adds `failed_execution_remains_reap_protected_during_retry_window`. Post-review targeted runtime **327/327**, pallet-attestor **26/26**, attestor-core **11/11** and changed-crate Clippy all pass. The local gate script has a locked `--changed [PACKAGE...]` mode; `bash -n`, `--help` and `git diff --check` pass. PR #141 is ready and the review thread is replied-to/resolved; no redundant exhaustive Cargo run was started for the focused fix.
