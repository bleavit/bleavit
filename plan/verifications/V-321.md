---
id: V-321
date: 2026-08-07
milestone: TS2322
title: **Does `redemption-ticket.ts` still name `Verified<T>` anywhere after the retype, and does the new firewall fixture fail for the reason it declares?**
---

## Item

**Does `redemption-ticket.ts` still name `Verified<T>` anywhere after the retype, and does the new firewall fixture fail for the reason it declares?**

## Spec ref

10 §2.1, §10.2; 11 §11.4 rule 4; 15 §4.8

## Status

**measured 2026-08-07** (4 mutants M1–M4, 1 survived the first pass and was killed)

## Result

**Nowhere, and yes.** `withoutComments(txSource('redemption-ticket.ts'))` contains no `Verified<`. `tests/firewall/fixtures/provider-cannot-satisfy-a-redemption-precondition.ts` compiles to exactly `error TS2322: Type 'Verified<bigint>' is not assignable to type 'Finalized<bigint>'. Property '[FINALIZED]' is missing …`, which is the code its first line declares. **The first form of the guarding assertion was itself the defect class**: `assert.match(source, /minSplit: Finalized<bigint>/)` matched the interface, so mutant M3 — reverting only `redemptionRateBlocks`' own parameter — survived it. The assertion is now the module-wide absence, which kills M1, M2 and M3
