---
id: V-17
date: 2026-07-16
milestone: X3
title: USDC asset index **1337** on Asset Hub (D-17 pinned Location)
---

## Item

USDC asset index **1337** on Asset Hub (D-17 pinned Location)

## Spec ref

02 §8; 09 §6.1

## Status

complete (2026-07-16, B4)

## Result

Confirmed via Circle's announcement pages, Polkadot support docs and Polkadot referendum #174 (USDC sufficiency): Circle-native USDC on Polkadot Asset Hub is asset id **1337**, registered **sufficient** — matching 02 §8's pinned `{parents:1, X3(Parachain(1000), PalletInstance(50), GeneralIndex(1337))}` and the sufficiency requirement of 09 §6.3(2). Single-homed as numbers in `futarchy_primitives::chain_identity` (`ASSET_HUB_PARA_ID`/`USDC_PALLET_INSTANCE`/`USDC_ASSET_INDEX`); `bleavit-xcm` builds the typed `Location` from them. The 02 `[VERIFY … at implementation]` annotation can be cleared at the next 02 touch (no shape change → no contract bump now).
