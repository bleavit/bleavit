---
id: V-116
date: 2026-08-05
milestone: —
title: **A gate this repository's own source claimed to have, and did not — plus the promotion it would have caught**
---

## Item

**A gate this repository's own source claimed to have, and did not — plus the promotion it would have caught**

## Spec ref

10 §2.1; INV-FE-1, INV-FE-9

## Status

**verified 2026-08-05 by building it and running it**

## Result

`packages/ui/src/datum.tsx` documented `check-render-provenance.ts` as *"the second half of this control"*, naming the exact hole the type layer cannot close: `<Panel title={`Referendum ${id.value}`}>` typechecks, because the payload of a `Verified<string>` is a `string`. **The file did not exist.** Built type-aware rather than syntactic — 37 `.value` accesses in `.tsx` are mostly `key=` and handlers, and two are not `Verified<T>` at all (`event.currentTarget.value`, and the verification panel's release-constant rows under SQ-592), so a grep fires on correct code and gets switched off. It found **4 real display-position sites** and, via a second rule, **2 promotions**: a value derived from two reads carrying one input's status, which renders provider data with a verified badge **by arithmetic** — a route no badge type and no firewall rule can see. Repaired with `combine`/`combine2` (weakest input wins; two verified reads at *different blocks* refuse outright, since their difference is true of neither). Two further findings came out of the witness: `getPropertiesOfType` returns `[]` for an unresolved generic reference, which had silently turned rule A off; and the fixture is not a workspace package, so pnpm linked nothing beside it and **every rule was inspecting an unresolved type while the run reported only "did not fire"** — now asserted, because that failure is indistinguishable from a rule that broke
