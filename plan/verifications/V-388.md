---
id: V-388
date: 2026-08-04
milestone: —
title: **The manifest froze under half of what docs 10/11 tell the client to read, and twelve surfaces were invisible to every gate**
---

## Legacy ID

V-98

## Item

**The manifest froze under half of what docs 10/11 tell the client to read, and twelve surfaces were invisible to every gate**

## Spec ref

02 §7; 10 §5.2; 11 §11.3, §11.5, §11.7.2

## Status

verified 2026-08-04, closed by contract v24

## Result

The inverse probe — *which client obligations name a surface 02 does not freeze* — returned **twelve**, not the one SQ-580 was filed for. `Multisig.Multisigs` (11 §11.3), the four `Referenda` items, both `Preimage` items, both `ConvictionVoting` items and `Scheduler.Agenda` (11 §11.7.2), `System.Events` (10 §4.2), and `Epoch.ResourceLocks` — the last of which is **pallet-epoch's own storage, missing from its own §7.1 table** while 09 §1.2(8) makes it a dispatch-time check. All twelve exist in the runtime and always did; what was absent was the *contract obligation*, and with it any probe. Confirmed against real metadata via `scale_metadata.surface_layout` before any spec text was written, then re-confirmed by `check-chain-feed.py` accepting all twelve on **both** halves of the 10 §5.1 pair (**268** frozen entries, up from 256)
