---
id: V-16
date: 2026-07-16
milestone: —
title: Exact XCM crate pins + XCM version const on `polkadot-stable2603`
---

## Item

Exact XCM crate pins + XCM version const on `polkadot-stable2603`

## Spec ref

09 §6.1; 01 §9

## Status

complete (2026-07-16, B4)

## Result

Read from the `polkadot-sdk 2603.0.0` umbrella manifest via the crates.io API (the workspace's version-source convention, Cargo.toml comment): `staging-xcm =22.0.0`, `staging-xcm-builder =26.0.0`, `staging-xcm-executor =25.0.0`, `pallet-xcm =26.0.0`, `cumulus-primitives-core =0.24.0` — same family as the existing `frame-support =46.0.1` pins; all five resolve + compile without dragging the node stack (workspace `cargo check` green, std + no_std). Latest stable XCM version on this train = **v5** (docs.polkadot.com XCM-versions page + release notes); `bleavit-xcm` pins `XCM_VERSION_PINNED = 5` with a drift test against the crate const.
