---
id: V-39
date: 2026-07-21
milestone: —
title: **`Preservation::Preserve` floor arithmetic in `pallet-assets 52.0.0`** — the entire B14 failure analysis rests on what `Preserve` actually reserves
---

## Item

**`Preservation::Preserve` floor arithmetic in `pallet-assets 52.0.0`** — the entire B14 failure analysis rests on what `Preserve` actually reserves

## Spec ref

03 §7 R-4

## Status

verified 2026-07-21

## Result

`functions.rs:247-274`: `untouchable = min_balance` when `keep_alive`, `Zero` when `Expendable`, so reducible is exactly `balance − min_balance`. The `is_sufficient` hypothesis was **refuted by execution** — `can_decrease` never reads it; sufficiency only waives the consumer-reference requirement on account *creation*, never the withdrawal floor. Measured directly: leaving 3,000 → `Err(Token(NotExpendable))`, leaving 10,000 → `Ok`, and an `Expendable` drain → `Ok` with residual 0. Genesis `accounts` semantics confirmed at `lib.rs:502/565/570` (mints, increases supply, requires ≥ `min_balance`)
