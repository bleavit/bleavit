---
id: V-80
date: 2026-08-03
milestone: —
title: **The committed 02 §11 feed reproduces byte-for-byte from a fresh build, both profiles**
---

## Item

**The committed 02 §11 feed reproduces byte-for-byte from a fresh build, both profiles**

## Spec ref

02 §11; 10 §5.1

## Status

verified 2026-08-03

## Result

The release-blocking leg, executed rather than described. Built `bootstrap` and `bootstrap-recovery` from the same tree, extracted metadata from a booted node for each (`--embed-wasm` for the recovery image, whose wasm is not the chain spec's own), and re-recorded the chainHead transcripts: `metadata.scale`, `runtime-info.json` and `build-info.json` are **identical** for both spec versions, and all **223** fixture files are identical to the committed set. The recovery build's own gate passed on the way through — `recovery_profile_has_zero_multi_block_migrations`, 1 passed. Descriptor generation is separately deterministic: `papi generate` into two unrelated directories produced identical sha256 for all 16 files, which is what makes the byte-compare drift gate a gate and not a flake.
