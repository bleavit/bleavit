---
id: V-306
date: 2026-08-07
milestone: T1
title: **FE-P9's D-Bulletin triggers T1–T4, recovered verbatim from this repository's own history and quoted into 10 §12**
---

## Item

**FE-P9's D-Bulletin triggers T1–T4, recovered verbatim from this repository's own history and quoted into 10 §12**

## Spec ref

10 §12; 12 §4.1; 07 §9

## Status

**verified 2026-08-07 by `git show`; corrected the same day**

## Result

**This row first recorded the opposite, and the correction is the finding.** It claimed T1–T4 were undefined and that `FRONTEND_PLAN.md` was *"not in the repository or in its history"*. That is false, and two commands settle it: `git merge-base --is-ancestor 6657f438 origin/main` returns true, and `git show 6657f438:FRONTEND_PLAN.md` prints §31 in full, defining the triggers at line 1356. Doc 00 superseded that file rather than deleting it, and a superseded document is still readable — so the original claim resolved a `[VERIFY]` by assumption in the mirror image of the way R-2 forbids, by recording a gate as *unresolvable* without looking. It would also have escalated to the user a question `git show` answers. Verbatim: *"(T1) Bulletin Chain live on Polkadot mainnet; (T2) a non-OpenGov authorization path (PoP or delegated) shipped, or an OpenGov authorization obtained; (T3) retention/renewal mechanics stable and priced; (T4) `tools/verify-release` extended to check CID ↔ release-hash equality"*. The same commit carries a **more specific FE-P9 row** than doc 10 did — chunked upload of `dist/` against the 8 MiB per-transaction limit, CID determinism across re-uploads, Bitswap and gateway retrieval latency, renewal flow, dotNS registration with n-of-m control — and gates the dry run on **T1–T3 only**, because T4 is this repository's own tooling work rather than an external event. All four are now quoted into 10 §12, and its FE-P6 row was corrected in the same pass (it still read as fully open although V-122 resolved its load-bearing half on 2026-08-05). So FE-P9 **is** externally blocked on infrastructure after all: T1–T3 need a live chain, an authorization path and a published price. V-7 tracks the same evidence under 07 §9's `[VERIFY: Bulletin Chain mainnet availability and authorization path]`, which is exactly T1. **SQ-941 is re-scoped** to the one question that survives, which is about status and not content: doc 00 says the component documents supersede `FRONTEND_PLAN.md`, while 12 §4.1 cites §31 as live.
