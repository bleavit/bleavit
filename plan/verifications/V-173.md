---
id: V-173
date: 2026-08-06
milestone: SS58
title: **The wrong SS58 prefix had reached six places including a binding rules file, and the gate that would catch it is not writable**
---

## Item

**The wrong SS58 prefix had reached six places including a binding rules file, and the gate that would catch it is not writable**

## Spec ref

02 §8; `.claude/rules/app-code.md`; PLAN.md V-164

## Status

**measured 2026-08-06**

## Result

#248 corrected five `app/` comments stating this chain's SS58 prefix as 22622 where 02 §8 freezes **7777**. Two more survived, and both are worse than a comment: `.claude/rules/app-code.md` is loaded into **every** session that touches `app/**`, and **V-164's own row** — the findings table a later session trusts for measured facts — stated 22622 twice while quoting an address that is the 7777 encoding. Measured rather than reasoned before correcting: `AccountId(7777).dec(aliceKey)` is `fvJdNW3p1BT1RfWzfZegj9tbVTUAXWf6Zf5kD9sJukyeafVR8`, exactly the address V-164 quotes, and `AccountId(22622).dec(…)` is `fgw8ys7FygTp2jenWCJoYZ8xYrT18A6ipP3WciH8ZiPyExE4G` — a string sharing nothing with it. V-164's *finding* never depended on which prefix, only on it not being 42, so the conclusion stands and only the number moved; both corrections say so inline rather than silently. **A CI gate for this was scoped and rejected on measurement, which is the more useful half.** The natural pattern — a number stated near `ss58`/`prefix` — was run over every `.md`/`.ts`/`.rs`/`.py` in the repository: 42 legitimate 7777 hits against **200+** false positives, because `prefix` in this codebase means storage prefix, key prefix, byte prefix and hash prefix far more often than it means an address format. That is the noise level `check-client-surface-obligations.py`'s own note says gets a gate switched off rather than fixed, so none was built. The residual is named instead: a frozen 02 §8 value restated in **prose** has no mechanical reader, and the only cheap control is that the *data* (fixture addresses, `AccountId(prefix)` calls) is derived rather than typed — which is why the data was right in all six places while the prose was wrong
