---
id: V-342
date: 2026-08-07
milestone: F23
title: **Did the clause-by-clause binding in `tests/providers/health.test.ts` ever pair 10 §8.4's noun with the shipped one?**
---

## Item

**Did the clause-by-clause binding in `tests/providers/health.test.ts` ever pair 10 §8.4's noun with the shipped one?**

## Spec ref

10 §8.4, §2.3; 15 §4.8

## Status

**measured 2026-08-07**

## Result

**No — it paired the section's clause with the substituted rendering.** The row read `['The only available cross-check is diffing two independent snapshot producers', /only cross-check for deep history is comparing two independent sources/]`: the left side was asserted against the document and the right against the constant, and the two named different things, so the pair was **satisfied by the drift**. That is the second occurrence in one table (`labels` for `recommends` was the first), and both survived every green run for the same structural reason. F23's own check was doc→doc only and could not see it either. Repaired in both directions: the pair is re-pointed and `tests/analysis/provider-surface.test.ts` now asserts each load-bearing phrase appears in **both** §8.4's bullet and `SAMPLING_GUARANTEE`
