---
id: V-26
date: 2026-07-17
milestone: —
title: `futarchy-fixed` ≥10⁷-point release sweep vs the 04 §5 single-generator oracle (04 §4 release cadence, B8)
---

## Item

`futarchy-fixed` ≥10⁷-point release sweep vs the 04 §5 single-generator oracle (04 §4 release cadence, B8)

## Spec ref

04 §4; 15 §4.4

## Status

complete (2026-07-17)

## Result

Full 10,000,000-point run, twice (pre- and post-cost-family): 100 content-hashed shards, master seed `0xB1EA5EED256B0001`, exp2/log2/ln/cost families from the reference model's 100-digit-`Decimal` oracle (≈332-bit ≥ the normative 256). Worst case: **log2 0 ulp, ln 1 ulp, composed cost 2 ulp (bound 8), exp2 within the 2⁻⁶³ relative bound**. Generation ≈19 s wall on 32 workers; the dependency-free Rust checker (hash-verifies then parses the same bytes) 10.7 s. Wired as the release-gating leg in `release.yml` and the kernel/numerics-change path-trigger in `sweep.yml`.
