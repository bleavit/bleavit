---
id: V-96
date: 2026-08-03
milestone: P6
title: `polkadot-api/pjs-signer` export names on PAPI 2.1.x (11 §11.3's `[VERIFY]`, FE-P1's adapter half)
---

## Item

`polkadot-api/pjs-signer` export names on PAPI 2.1.x (11 §11.3's `[VERIFY]`, FE-P1's adapter half)

## Spec ref

11 §11.3

## Status

complete (2026-08-03)

## Result

Read off `@polkadot-api/pjs-signer@0.7.3` in the pinned tree: `getInjectedExtensions(): string[]`, `connectInjectedExtension(name, dappName?): Promise<InjectedExtension>`, `getPolkadotSignerFromPjs(address, signPayload, signRaw): PolkadotSigner`; types `InjectedExtension`, `InjectedPolkadotAccount`, `SignPayload`, `SignRaw`, `SignerPayloadJSON`. The signer factory **takes two callbacks and returns a signer** — it constructs neither a chain nor a provider, which is what let `packages/signing` be exempted from the connection firewall on that rule's own stated reason. **FE-P6 (Ledger Generic App + `CheckMetadataHash` for a custom chain) remains open**, so `RAW_PAYLOAD_DESCRIPTOR` still omits `metadata-hash`
