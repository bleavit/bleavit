---
id: V-114
date: 2026-08-05
milestone: F12
title: **F17 and F18's read surfaces are frozen and probed too — and §11.8 states them in prose, which is SQ-591 rather than a new gap**
---

## Item

**F17 and F18's read surfaces are frozen and probed too — and §11.8 states them in prose, which is SQ-591 rather than a new gap**

## Spec ref

02 §7.2, §7.4; 11 §11.8, §11.9; 10 §5.2

## Status

**verified 2026-08-05**

## Result

The same spec-first sweep, run across the rest of Track F after it found F12's missing rows. Extracting qualified `` `Pallet.Item` `` references from §11.8 returns **zero** — for four operator consoles that plainly read chain state — which reads as a milestone-sized SQ-580. It is not. `tools/release/surface-manifest.json` carries `Guardian.Members`/`Allowances` cited to **02 §7.4** and the five `Oracle.*` items to **§7.2**, and `CRITICAL_SURFACE` probes all seven, so the 10 §5.2 lattice can fail on them. 02 lists them under a **per-pallet section heading with the bare item in a table column**, not in the qualified form — the second time in one session a first grep was wrong in the alarming direction, and the second time the fix was to check the manifest instead. **What is genuinely true is narrower: §11.8 does not *enumerate* its reads the way §11.7.2 does**, so `check-client-surface-obligations.py` — which works from qualified references — cannot verify F17's obligations at all. That is exactly **SQ-591** (*"sound but not complete"*), already open, and this is its milestone-sized instance rather than a new question. **F16, F17 and F18 are all buildable**; none carries an F12-shaped hole
