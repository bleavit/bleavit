---
id: V-63
date: 2026-08-01
milestone: N7
title: **N4 bonded client-registry implementation and I-34 closure**
---

## Item

**N4 bonded client-registry implementation and I-34 closure**

## Spec ref

16 §2/§3.1/§9/§11; 13 §1/§4; 15 I-34

## Status

verified 2026-08-01

## Result

`cargo fmt --all` clean; `tools/ci/rust-workspace-gates.sh --changed client-registry-core pallet-client-registry bleavit-runtime` exit 0 with runtime **419/419**, core **5/5**, pallet **14/14**; limit coverage **200/200** with `svc.client_bond` classified `value` and four N7-owned service keys still unwired. Supplemental gates: core `--no-default-features` compiles; pallet `runtime-benchmarks` **16/16** and `try-runtime` **14/14** pass. Canonical 50×20 runtime weights regenerated after the final strict-origin/max-`Location` fixtures. Two independent reviews returned no semantic/spec blocker. The first gate attempt found and fixed core `too_many_arguments` structurally; the second exposed a dev-feature leak into stock FRAME pallets and was fixed by scoping `pallet-origins/runtime-benchmarks` to the feature that needs it.
