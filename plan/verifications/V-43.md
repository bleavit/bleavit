---
id: V-43
date: 2026-07-21
milestone: —
title: **Reserve-probe production wiring (SQ-195 premise) and 05 §4.6 normalization status (SQ-181 premise)** — both rows rest on assumptions about code that exists; checked directly before writing any
---

## Item

**Reserve-probe production wiring (SQ-195 premise) and 05 §4.6 normalization status (SQ-181 premise)** — both rows rest on assumptions about code that exists; checked directly before writing any

## Spec ref

07 §8; 05 §4.6

## Status

verified 2026-07-21 (A12)

## Result

**Both premises were wrong in the same direction — less exists than the rows assume.** (1) The 07 §8 reserve probe is *not wired in production*: `type RuntimeProbeDispatch = ()` under `not(runtime-benchmarks)` and `type ResponseHandler = PolkadotXcm` rather than `ProbeAwareResponseHandler`, so nothing sends a probe and no `QueryResponse` reaches `pallet-oracle::reserve_probe_result`; the only `ProbeSink` impl is in `runtime/bleavit-xcm/src/mock.rs`. A `(epoch, day)` outcome store built now would have no writer and 07 §8's "no probe sent ⇒ fail" would pin `R = 0` forever. (2) 05 §4.6 normalization is *entirely unimplemented*: `MetricSpec.prior_bounds` is stored and round-tripped but read by nothing, and no winsorization/percentile/min-max code exists in the workspace. Separately, production genesis registers **no** `MetricSpec` (`runtime/bleavit-runtime/src/genesis.rs` sets no `pallet_welfare` genesis), so `active_snapshot_spec()` is `None` and no welfare input is consumed on-chain today at all. Both findings recorded in their rows; neither row was coded.
