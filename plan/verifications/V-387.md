---
id: V-387
date: 2026-08-02
milestone: B7
title: **PR #220 N10/N11 review repairs**
---

## Legacy ID

V-73

## Item

**PR #220 N10/N11 review repairs**

## Spec ref

09 §6.5; 15 §4.5; 16 §7/§9/§12; I-36

## Status

verified 2026-08-02 (live drill not-run)

## Result

`cargo fmt --all` exit 0; `tools/ci/rust-workspace-gates.sh --changed bleavit-client-abi market-core pallet-bleavit-client pallet-conditional-ledger pallet-market pallet-question-service bleavit-client-runtime bleavit-runtime` exit 0; `python3 tools/env/validate-environments.py` exit 0 (`B7 environment definitions are structurally valid`); `python3 tools/ci/check-quickstart-drill.py` exit 0 (`quickstart-drill-binding: ... includes ... verbatim and the helper parses`); both client JS helpers pass `node --check`. The 50×20 write regenerated `pallet_conditional_ledger` (29), `pallet_market` (11), and `pallet_question_service` (9); the latter corrected `register` storage dimensions 211→210 reads and 91→90 writes. The matching `regenerate-weights.py --check --changed --steps 50 --repeat 20` exits 0 with `PASS: committed storage dimensions match a fresh measurement.` The live drill 11 execution leg is **not-run** because every HRMP topology in this repository still stalls both parachains at block #0 on the pinned Cumulus `DMQ head mismatch`; no structural result is reported as execution evidence.
