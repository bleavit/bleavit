---
id: V-40
date: 2026-07-21
milestone: G2
title: **`try-runtime-cli` release identity and digest** — the closing try-state leg (SQ-204) needs a pinned checker, and `tools/env/pins.env` is the only committed pin home
---

## Item

**`try-runtime-cli` release identity and digest** — the closing try-state leg (SQ-204) needs a pinned checker, and `tools/env/pins.env` is the only committed pin home

## Spec ref

15 §1; 15 §4.7

## Status

verified 2026-07-21

## Result

Resolved against the live GitHub release API rather than assumed. Latest release is **v0.10.1** (published 2026-01-23), publishing exactly two assets and **no `.sha256` sidecar** — so, like the zombienet/osv-scanner/tla2tools pins already in this file, the digest is pinned directly. Fetched `try-runtime-x86_64-unknown-linux-musl` (33,421,544 bytes, matching the API-reported asset size) and computed `d6fcd586fba4c7245668f4ae2a06ffc94e6da4135fae22cfe24453dd11aee3c3`. The CLI executes the runtime's own Wasm over a fetched state snapshot, so it is runtime-agnostic w.r.t. the pinned stable2606 runtime; v0.10.0's notes record the update to polkadot-sdk master. **Not executed against a real node here** — no built runtime Wasm or booted endpoint exists in this worktree, so first real execution rides G2
