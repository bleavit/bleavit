---
id: V-65
date: 2026-08-01
milestone: N8
title: **N8 exact XCM client-ingress intersection and I-34/I-35/I-38 closure**
---

## Item

**N8 exact XCM client-ingress intersection and I-34/I-35/I-38 closure**

## Spec ref

09 §6.5; 16 §3/§3.1; 15 I-34/I-35/I-38

## Status

verified 2026-08-01

## Result

Final requested gates: `cargo fmt --all` exit 0 with no output; changed Rust gate exit 0 with runtime **425/425**, `bleavit-xcm` **67/67**, `origins-core` **10/10** and client registry **15/15**; five-target fuzz format/Clippy/unit/corpus/smoke gate exit 0; reference model **652/652**. The barrier tripwire pins exactly six admitted positions and all 52 pinned-v5 instruction variants; every one of the nine inner-program carriers, `DescendOrigin`, `AliasOrigin` and `ReserveAssetDeposited` is rejected at every admitted position. The pre-N8 three-deny composition is source-hash-frozen and byte-identical to the new barrier for 2,048 recursively generated programs without `Transact`, comparing the SCALE bytes of the result, mutated instruction slice and properties. The first Rust gate exposed and fixed constant-assertion/generic-bound Clippy errors; the source-freeze hardening rerun caught and fixed `clippy::sliced_string_as_bytes`; the first fuzz gate exposed formatting and then `manual_is_multiple_of`; the sandbox-only fuzz run reached every corpus target before LeakSanitizer refused ptrace, and the exact approved unsandboxed rerun passed all five 30-second smokes. Independent compliance review found no blocker after correcting the negative-origin fixture so `Signed` was genuinely unregistered; its narrow post-hardening follow-up independently matched the frozen digest and cleared the moving-oracle concern.
