---
id: V-100
date: 2026-08-04
milestone: —
title: **Two spec-question rows said `open` and `RESOLVED` in the same cell, and no checker could see it**
---

## Item

**Two spec-question rows said `open` and `RESOLVED` in the same cell, and no checker could see it**

## Spec ref

—

## Status

found and corrected 2026-08-04

## Result

SQ-581 and SQ-582 were completed on 2026-08-04 and their status cells carry `**RESOLVED 2026-08-04.**` — behind a leading `open` token that was never flipped. `check-spec-question-batches.py` tests the **prefix** of the status cell (correctly: an open row legitimately contains the word *resolved* in prose), so the contradiction was invisible to it *and* to the batch index, which went on believing both were open. Surfaced only because closing SQ-580 made the checker speak. The canonical status record disagreed with itself for a day; both rows flipped, and all three dropped from batch X
