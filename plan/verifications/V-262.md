---
id: V-262
date: 2026-08-07
milestone: F18
title: Is `cannotObserve` honest — does anything in this client start a light client?
---

## Item

Is `cannotObserve` honest — does anything in this client start a light client?

## Spec ref

10 §4.1, §6.3

## Status

✅ Measured (2026-08-07)

## Result

**Nothing calls it.** `startLightClient` is defined once (`app/packages/chain-client/src/light-client.ts`) and `startTopology` once beside it; across `app/src`, `app/packages` and `app/tools` there is no call site for either, which is F18's artifact-blocked remainder — no chain spec is bundled, so there is nothing to hand smoldot. So an observer answering *cannot say* for every range states the truth rather than standing in for a reader that exists. `tests/analysis` asserts the absence at both call sites that could change it, so the observer cannot outlive the condition that makes it honest
