# Unplanned changes — 2026-08-13

## Closed the client-parachain release-spec gap

Added client-parachain spec generation to the tag workflow before B7 evidence, derived spec
prerequisites from selected topologies, and replaced the evidence runner's broad Bleavit-name prefix
with exact primary and explicit separate-runtime classes. Unknown Bleavit spec names and release
suites with no primary spec now fail closed. Added behavioral second-handoff, missing-prerequisite,
separate-client, and unknown-class regressions; synchronized release/environment/operator guidance.

## Regenerated the v31 runtime integration artifacts

Built the paired primary/recovery runtimes from clean source `a4983e85` in the pinned OCI recipe,
booted and cross-checked their metadata/RFC-78 bindings, and regenerated the current chain feed,
chainHead transcripts, descriptors, critical surface, skills and exact capacity fixture. The
generated release assembly reports no corruption; its remaining launch/evidence gaps stay explicit.
Updated the feed regeneration guide from its historical-v2 note to the exact OCI-v3 handoff.

## Repaired the first draft-PR CI run

Made app reproducibility independent of container checkout ownership, feature-gated a benchmark-only
market mock import, regenerated `pallet_xcm` after the public-router storage change, and added a
strict versioned waiver for the newly disclosed unpatched `extract-zip` issue in Lighthouse-only
measurement tooling. Added workflow wiring coverage for both deterministic build environments.
Rebased the N8 barrier-source tripwire onto the authorized August 12 closed allowlist while retaining
the differential and making future shared-policy hardening decision-recorded rather than invisible.

## Refreshed PR #304's runtime provenance after the CI repair

Regenerated the paired runtime build/runtime-info records and all strict chainHead fixtures from
clean source `13eaabc6`. The new feed binds primary `2fa46f46…5d3b` and recovery
`c70a3c54…eadd`; 302/300 surfaces were recorded with zero missing and release assembly reports no
corruption.

## Reconciled PR #304's measured metadata size

Updated 10 §9.3 and the artifact-budget gate from 492,363 B to the regenerated 493,122 B
`metadata.scale` measurement. The largest level-9 gzip result is 155,848 B and still publishes as
0.16 MB, so no cache or bundle budget changed. Repinned the mutation test and regenerated the
derived frontend-architecture mirror.
