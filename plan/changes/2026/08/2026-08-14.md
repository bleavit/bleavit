# Unplanned changes — 2026-08-14

## Replaced the Lighthouse advisory waiver with an upgrade

Moved the isolated render-budget tool from Lighthouse 12.8.2 to 13.4.1, raised Bleavit's Node 22
floor to the new package minimum, and repinned both immutable CI containers. Re-verified the FMP
omission, the desktop/mobile reference presets, and the real render gate plus witness before removing
the superseded Lighthouse-path waivers.

## Patched the transitive nanoid advisory exposed by the full scan

The first post-upgrade supply-chain run found a separate high-severity advisory in PostCSS's
`nanoid` 3.3.17. Added an exact workspace override to patched 3.3.18, refreshed the frozen lock, and
reran all six audited workspaces. The final npm scan reports zero advisories.
