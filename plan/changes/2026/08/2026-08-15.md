# Unplanned changes — 2026-08-15

## Repaired PR #306 dependency resolutions

Aligned `subxt-metadata` with the Dependabot-updated `subxt` release in the keeper lockfile, removing
the incompatible duplicate `frame-decode` version. Updated the app lockfile from vulnerable
`nanoid` 3.3.17 to fixed 3.3.18, which remains within PostCSS's declared dependency range.
