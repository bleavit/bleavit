# Unplanned changes — 2026-08-11

## Fixed the open Dependabot alerts, and closed the gate…

Fixed the open Dependabot alerts, and closed the gate class that hid one of them (user-requested).

Neither alert was fixable by a bump: `glib` 0.18.5 (#27, medium) is forced by the archived `gtk 0.18.2` and is provably unreachable, and `@opentelemetry/core` (#28) keeps its 2026-08-07 waiver by the user's ruling. What did move: `event-listener` 5.4.1 → 5.4.2 in the root and keeper lockfiles (RUSTSEC-2026-0221), found only because the whole `unsound` class was triaged; a fifth gate leg (`tools/ci/check-unsound-advisories.py` + `tools/ci/unsound-waivers.toml`) that fails on any untriaged `unsound` advisory; supply-chain summary schema v4 → v5; and the R-1 amendment to 15 §4.5 (SQ-1048) the change required. Rejected: bumping Lighthouse across a major to clear #28, which would re-baseline a published performance budget.
