---
id: M1
track: M
title: `crates/futarchy-primitives` — `no_std` shared SCALE types + kernel `K` constants, `INTEGRATION_CONTRACT_VERSION = 2`
spec: [02 §2, 13 §2, 01 §5.2]
depends: [M0]
status: done
---

**Done 2026-07-15 (Track M finish)** — the three audit majors closed. **Single-home:** `kernel::WT_QUORUM` added and the `wt.quorum` double copy (oracle + registry) collapsed onto it; attestor (`ATT_MIN_MEMBERS`/`ATT_QUORUM`), execution-guard (`MAX_CALLS`/`MAX_BYTES`/`DESCRIPTOR_LEAD_TIME_BLOCKS`), market (`MIN_TRADE_USDC`), oracle (`WT_MAX`/`ORC_MAX_PROOF_BYTES`/`WATCHTOWER_EXTENSION_BLOCKS`), registry (`REG_MAX_FILINGS_EPOCH`/`WATCHTOWER_EXTENSION_BLOCKS`), guardian (`frn.window = DEC_EXTENSION_BLOCKS`, 13 §3.4), epoch (`DEC_EXTENSION_BLOCKS`/`STALE_EPOCH_BOUND_BLOCKS` + every phase-boundary computation wired to the new `phase_offsets` module) de-duplicated onto the kernel single home (01 §5.2, rule 4 — no 13-owned literal survives in code or tests; coincidentally-equal-but-distinct constants kept separate). **Types moved in:** the 21-field `Proposal<AccountId>` (exact 05 §1.2 layout — adds `payload_len` + `resources: BoundedVec<[u8;8],8>`, now derives `MaxEncodedLen` for the ≤512 B pallet-storage bound, 02 §114 / I-20), `ExecutionRecord`, `MarketSet` (re-exported from their old cores, test builders updated); `MarketKind` (7-variant, 02 §5 SCALE-index order) + `codec_index` created; `phase_offsets` (13 §3.1 numerators over 21) added; `RuntimeVersionConstraint` already present (09 §1.2(3)). **Regression:** codec-index stability, `Proposal` SCALE round-trip + **golden field-order lock** + `MaxEncodedLen ≤ 512` + resources-bound-enforced-at-decode, `ExecutionRecord` round-trip, view-size `MaxEncodedLen` pins (02 §3/§4), exact phase-offset numerators. `BoundedVec` gained `len`/`is_empty`/`iter`/`IntoIterator`. Review: **spec-reviewer 0 blocker / 1 major / 8 minor** — the major (`Proposal` `MaxEncodedLen`) + every actionable minor fixed in-session; 2 residual minors are contract-sign-off / pre-existing items logged as **SQ-44** (formal `MarketKind` 02 §5 declaration + variant spelling — **resolved** this session by conforming the code to 02 §5's canonical names, PR #49) and **SQ-43** (`CohortSummary.proposals` bound 5 vs `epoch.slots` max 12). Full workspace gate green.

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track M — Foundations | -->

<!-- source row: | M1 | `crates/futarchy-primitives` — `no_std` shared SCALE types + kernel `K` constants, `INTEGRATION_CONTRACT_VERSION = 2` | 02 §2; 13 §2; 01 §5.2 | M0 | ✅ | **Done 2026-07-15 (Track M finish)** — the three audit majors closed. **Single-home:** `kernel::WT_QUORUM` added and the `wt.quorum` double copy (oracle + registry) collapsed onto it; attestor (`ATT_MIN_MEMBERS`/`ATT_QUORUM`), execution-guard (`MAX_CALLS`/`MAX_BYTES`/`DESCRIPTOR_LEAD_TIME_BLOCKS`), market (`MIN_TRADE_USDC`), oracle (`WT_MAX`/`ORC_MAX_PROOF_BYTES`/`WATCHTOWER_EXTENSION_BLOCKS`), registry (`REG_MAX_FILINGS_EPOCH`/`WATCHTOWER_EXTENSION_BLOCKS`), guardian (`frn.window = DEC_EXTENSION_BLOCKS`, 13 §3.4), epoch (`DEC_EXTENSION_BLOCKS`/`STALE_EPOCH_BOUND_BLOCKS` + every phase-boundary computation wired to the new `phase_offsets` module) de-duplicated onto the kernel single home (01 §5.2, rule 4 — no 13-owned literal survives in code or tests; coincidentally-equal-but-distinct constants kept separate). **Types moved in:** the 21-field `Proposal<AccountId>` (exact 05 §1.2 layout — adds `payload_len` + `resources: BoundedVec<[u8;8],8>`, now derives `MaxEncodedLen` for the ≤512 B pallet-storage bound, 02 §114 / I-20), `ExecutionRecord`, `MarketSet` (re-exported from their old cores, test builders updated); `MarketKind` (7-variant, 02 §5 SCALE-index order) + `codec_index` created; `phase_offsets` (13 §3.1 numerators over 21) added; `RuntimeVersionConstraint` already present (09 §1.2(3)). **Regression:** codec-index stability, `Proposal` SCALE round-trip + **golden field-order lock** + `MaxEncodedLen ≤ 512` + resources-bound-enforced-at-decode, `ExecutionRecord` round-trip, view-size `MaxEncodedLen` pins (02 §3/§4), exact phase-offset numerators. `BoundedVec` gained `len`/`is_empty`/`iter`/`IntoIterator`. Review: **spec-reviewer 0 blocker / 1 major / 8 minor** — the major (`Proposal` `MaxEncodedLen`) + every actionable minor fixed in-session; 2 residual minors are contract-sign-off / pre-existing items logged as **SQ-44** (formal `MarketKind` 02 §5 declaration + variant spelling — **resolved** this session by conforming the code to 02 §5's canonical names, PR #49) and **SQ-43** (`CohortSummary.proposals` bound 5 vs `epoch.slots` max 12). Full workspace gate green. | -->
