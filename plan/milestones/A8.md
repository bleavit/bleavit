---
id: A8
track: A
title: `pallet-epoch` — epoch/phase clock, proposal registry + T1–T24 machine, cohorts, `decide()` engine (11 steps incl. `SecuritySizing` D-4, ratification D-5), `RecentCohortSummaries` ring
spec:
  - 05 §1–§3, §5
  - 02 §7.1
depends:
  - A2
  - A3
  - A7
status: done
---

I-14, I-15, I-21. **Done 2026-07-16** — production `#[frame_support::pallet]` over `epoch-core` (shell-over-core). **Core refactor** (behavior-preserving, R-1): in-process `&mut LedgerState`/`&mut WelfareState` → frame-free `LedgerOps`/`WelfareOps`/`EpochParams` trait seams; **welfare-only settlement** (05 §6 — epoch = `ResolveAuthority` `create_vault`/`resolve`/`void`; `pallet-welfare` alone holds `SettleAuthority`; the core's direct `settle_*` calls removed, no double-settle); rule-4 constants single-homed to `futarchy-primitives::kernel` (`BLOCKS_PER_DAY`/`MIN_EPOCH_LENGTH`/`RERUN_HURDLE_BUMP`/`SECURITY_FACTOR`). **Shell:** frozen 02 §7.1 storage (`Proposals` `CountedStorageMap` / 3-field `EpochOf` byte-for-byte + phase-math extras internal / `IntakeQueue` / `RecentCohortSummaries` ring / `Cohorts`; monotone pallet-assigned `NextProposalId`), 13 origin-checked calls (submit/withdraw/tick/decide/settle_cohort/set_next_epoch_length(ConstitutionalValues — **no `EnsureRoot`**)/delay_once/veto_upheld(T24)/mark_executed/mark_failed_executed/retry_exhausted_to_measurement/expire_or_stale_queue/force_reject/**void_cohort** (VoidAuthority, 06 §6.2 PB-ORACLE-VOID)), the 11-step `decide()` (I-14 gate-veto-before-welfare; enqueue-only-on-Adopt I-15; D-4 security sizing) assembled from Market/Oracle/Guardian/Attestation/Constitution seams, `try_state` covering I-16 (creation-time schedule freeze) + I-21, genesis, mock, v2 benchmarks + weights. **Orchestrated with Codex subagents (gpt-5.6-sol/xhigh, user directive):** implement → **dual review (native `spec-reviewer` + Codex adversarial)** → 2 codex remediation rounds → focused Codex re-review — every finding fixed with a regression test. **2 blockers fixed:** the stale-epoch force-reject was anchored to `submitted_at` (force-VOIDed every healthy proposal ~7 d after submission, before its d18 decision — **protocol-breaking**; re-anchored to an epoch-staleness latch, SQ-86) and the D-4 gate hardcoded `T_dec=3` (→ `dec.window/BLOCKS_PER_DAY`; the tunable window otherwise admitted up to 3× the permitted prize — R-7). Plus fallible-read reason-ordering, ProposalId monotonicity, proposer-`metric_spec` binding, the T20 double-event, the rerun-I-16 hole, Baseline carry, dead-man clock-pause + proposal-free recovery, T5/T6 bond-priority+rollover, and **requires_gate frozen at Seed** (a vetoed Treasury proposal could Adopt on a post-Seed NAV shift). Gates green: fmt · clippy `-D warnings` · workspace **557** (pallet-epoch **53** / 67 w/ benches; epoch-core 17) · runtime-benchmarks · try-runtime · no_std · reference-model 21 · doc-table · doc-links. **Deferred (documented):** real proposal-bond custody → B-track (A10 precedent); PoV-calibrated weights → B5; concrete seam wiring + SafetyFilter matrix rows → B1a. **SQ-83…SQ-92** logged.
