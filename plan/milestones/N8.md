---
id: N8
track: N
title: **XCM ingress** — `runtime/bleavit-xcm/src/client.rs` positional template, barrier change, `OriginConverter` / `SafeCallFilter`
spec: [09 §6.1/§6.5]
depends: [N4, N7]
status: done
---

Completed 2026-08-01. `Transact` is admitted only at the exact six-position template through the intersection of the barrier, exact registered-location/XCM-origin conversion and `ExternalClient` domain filter. The closed per-instruction allowlist is byte-for-byte unchanged; all nine inner-program carriers and both origin-mutators fail the positional matcher; I-34/I-35/I-38 runtime drills, the source-hash-frozen 2,048-case pre-N8 differential, fuzz target and exact requested gates are green. Independent compliance review: no blocker

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track N — External clients: futarchy as a service over XCM | -->

<!-- source row: | N8 | **XCM ingress** — `runtime/bleavit-xcm/src/client.rs` positional template, barrier change, `OriginConverter` / `SafeCallFilter` | 09 §6.1/§6.5 | N4, N7 | ✅ | Completed 2026-08-01. `Transact` is admitted only at the exact six-position template through the intersection of the barrier, exact registered-location/XCM-origin conversion and `ExternalClient` domain filter. The closed per-instruction allowlist is byte-for-byte unchanged; all nine inner-program carriers and both origin-mutators fail the positional matcher; I-34/I-35/I-38 runtime drills, the source-hash-frozen 2,048-case pre-N8 differential, fuzz target and exact requested gates are green. Independent compliance review: no blocker | -->

<!-- source track narrative -->

Opened 2026-08-01 on the "make the futarchy infrastructure accessible to other parachains,
smart contracts and services via XCM" work order. Bleavit hosts conditional decision markets
for external clients and **sells price discovery, not decisions**: it publishes the conditional
TWAPs with provenance plus a manipulation-cost bound, and the client's own on-chain rule decides
and executes locally. Bleavit never runs foreign code, and no external state reaches any Bleavit
decision, welfare or settlement input. v1 scope by user decision — the spec batch, contract bump
and threat rows land together. Full approved design: `.claude/plans/` (session plan, 2026-08-01).

<!-- end source track narrative -->
