---
id: O6
track: O
title: **Environment-evidence execution depth and relay-finality monitoring** — execute every normative Chopsticks scenario card rather than structurally validating it, add the pinned per-suite closing try-state leg the READMEs already mandate, and add the doc-12 relay finality-stagnation alert (relay best advancing while finalized stalls) that no current alert covers
spec:
  - 15 §4.7
  - 12 §6.3
depends:
  - B7
  - O5
status: done
---

**Done 2026-07-21 (worktree `env-evidence`, branch `fix/env-evidence-depth`).** All three rows verified real against the tooling before any code, then fixed. **SQ-203:** scenario cards carry an executable ` ```card-assertions ` block; the runner executes it and records the `card` check only when every assertion ran, and evidence may not name a card-requiring scenario without it — closing the `--include-gated` hole that let a release run attest a scenario whose card never executed. **SQ-204:** `try-runtime-cli` v0.10.1 pinned + digest-verified (V-40) and the closing `--checks try-state` leg executed per suite; the Zombienet keep-alive is `spawn --monitor` + `test <drill> <zombie.json>`, read from the pinned CLI's own source (V-41), with the endpoint taken from the topology's pinned `rpc_port` and failing closed when absent. **SQ-283:** a separate `relay_finality_monitor.py` on an independent relay RPC exporting best/finalized/stagnation, failing closed to absent series, plus a 21st 12 §6.3 row bound to RB-KEEPER with Infrastructure escalation; persistence window deliberately left `[VERIFY]` at a conservative 1,800 s (V-42). Gates green: env validator · env tests 72 · monitoring tests 88 · alert coverage 21/21 · runbooks 13/21 · deploy 82 · release 71 · ci 60 · doc links · PLAN tables · batch index. **Zero Rust.** Not executed against live environments — no zombienet/chopsticks binaries, built runtime Wasm or relay endpoint exist here; first real execution rides G2.
