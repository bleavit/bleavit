---
id: F17
track: F
title: FE-15 operator surface — reporter/guardian/treasury/upgrade-crank tiers (S14–S19, "Advanced")
spec:
  - 11 §11.8
depends:
  - F6
  - F11
status: done
---

**S14–S19's models and screens are built, gate-bound and mutation-proven. The row's own account of what remained was stale, and is corrected here (2026-08-09).** It claimed *"nine spec questions and one cross-milestone blocker"*. Of the **seventeen** spec-question ids this row cites, **fourteen are resolved** — including all four it named as the reason its controls stay closed (SQ-598, SQ-601, SQ-730, SQ-731), which contracts v29 and v30 froze surfaces for. Three stay open, and the 2026-08-09 R-6 review ruled every one a follow-up rather than a blocker: **SQ-609** (10 §8.3/§8.4, whose subject is `packages/providers` rather than the operator surface, so it is in this row by number and not by scope), **SQ-1022** (02 §9 freezes no bound for the guardian pending-action queue, and `propose_action` raises `TooManyPending`, so the cost falls on the proposer rather than on the fifth approver) and **SQ-1031** (three of `registry_core::file`'s five refusals have no readable half). Each costs **one transaction fee and no bond**: all three refuse through `ensure!`, so the dispatch returns `Err` and FRAME reverts the storage layer including the reserve. The cross-milestone blocker was the transport, and #295 closes it. This row **contradicted itself** until 2026-08-06 — its head said the consoles were built and its tail said they remained — and the remediation that corrected it found the reason: everything the tail described *had* been built, and none of it was wired to the transaction machine. **The structural blocker, and the one worth remembering: no operator submit path reached `refreshAndGate`.** Every console took an `onX: () => void` and enabled it on its own module-local `*Blocks()` result — correct checks, over values the screen was handed, at whatever block those were read, with no refresh, no `spec_version` re-check and no proof the set described one state. A guardian approval, a runtime upgrade and a treasury claim were all reachable that way. §11.4 rule 1 asks for the gate *"structurally … not by convention"*, and the hole was reached **by omission**: `PreconditionRowId` closed at P-15, so nine §11.8 calls had no id to declare in `TxPreparation.requires`, `gate()` had nothing to demand of them, and a console gating its own button was the only thing left. The repair is a row family (`O-1`…`O-9`, each clause citing a real `SurfaceId`), an `OPERATOR_SURFACE_ROWS` map binding call→row (§11.8.1's own two calls route to §11.5's P-13/P-14, so no second table exists for one obligation), and `operatorGate`, whose `window` is a `GatePassed` no screen can mint. Every console's `onSubmit` now **takes** that proof, so the old shape does not typecheck; a negative-compilation fixture proves a hand-assembled gate is refused. `operatorGate` adds three refusals the machine cannot see: a preparation declaring **somebody else's row** gates perfectly and would authorise the wrong signature, a **blocking** unreadable obligation closes the control (INV-FE-12), and the model's own blocks still apply. **The second blocker was two disagreeing implementations of P-13 in one shipped client.** `rows.ts` declared the bond recomputed from a cohort escrow and checked USDC headroom against it; `oracle-reporting.ts` had already concluded the bond is structurally uncomputable and shipped that. Nothing bound them, so a bonded and slashable action carried two answers to *"what will this hold?"*. The escrow clause was worse than redundant: it read `storage.epoch.cohorts`, and `CohortInfo { epoch, proposals, status }` has **no escrow field** — a clause reading a map that cannot answer it, which typechecks because a `SurfaceId` says nothing about what an item holds. It is deleted, P-13 cites SQ-598's refusal, and `reportBlocks` now consumes P-13's clause list as its **work list**: a clause with no predicate throws (`UnimplementedClauseError`) rather than being skipped, and the caveat is bound to the table by spec-question id so the two cannot drift again. **Ten majors and six minors closed with it.** The welfare snapshot joined P-15's `CRANK_STALENESS` (§11.8.5 binds it to that row by name while `CrankCall` listed six members without it, so `crankStaleness` **threw** on the one crank §11.8's own text tables); *already taken* is keyed on **`(epoch, spec_version)`**, because a second admissible MetricSpec version needs its own record and the client was refusing a crank the chain accepts — on the crank whose overdue state engages the dead-man rule; `register_reporter`'s stake-hold consequence is a **required** field rendered on the clean path (it had lived inside the short-balance block, so the only reader who never saw it was the one about to commit the stake); §11.8.2's **ratification tracker** is built over the four frozen guardian events plus `Referenda.ReferendumInfoFor`, with `unobserved` a first-class arm because every executed action's review is auto-scheduled and *"we have not seen it"* must never render as *"none is required"*; `registry.challenge` gained its bond-balance precondition; §11.8.4 step 4's fee headroom is computed and displayed, and blocks when **incomparable** as well as short; the artifact hash is **streaming** over an async chunk source with the verified bytes retained **inside** the brand, so a submission cannot carry a parallel copy the hash never saw; `EvidencePanel` renders on the round, challenge and filing panels; **S19's filing screen exists**; and `InsurancePanel` takes a `Combined<InsuranceStanding>` with an `unestablished` arm, since `T_ins` has no chain source and the old signature could only be satisfied by inventing one. The minors: the pending-actions list renders `justification_hash` **and** the resolved document, per-power argument forms are a closed union over the runtime's own field names, E19's fetch progress and lead-time countdown render (the countdown reads the **stored** field and derives nothing — SQ-552 stands), P-13's *round open* and *report window* are two clauses, `TreasuryStreams`' detached buttons became one control per row with a resolving `describedBy`, and `PENDING_SCREENS.S17` no longer waits on FE-P10, which gates its **submit control** rather than the screen. **Verification:** `pnpm test` green (18 suites, 0 failures) plus `build`, `check:types`, `depcruise`, `check:render-provenance`, `check:above-fold`, `check:chain-literals`, `check:casts`; the four root gates green. **42 mutants, 0 survived** — 34 killed by a suite, 8 by the compiler. Six survived the first pass and each was a real test weakness rather than a bad mutant: three field labels asserted by substring (a field renamed `Fee headroom-NOT` passed `/Fee headroom/`), and a justification cell asserted present rather than **visible**. Fixed by asserting the exact `field__label` element and by the concealment checks the call-batch test already used. **The PR #257 review round (2026-08-06) cleared six Codex findings, five P1 and one P2, all confirmed.** Four were one theme — a token certifying less than its holder assumes — and each is repaired at the type level. `GatePassed` carried no preparation, so it proved *some* gate call succeeded and an authentic window paired with other bytes, or with **none**, enabled a privileged control: it now carries `prep`, `reduce` refuses a proof minted for another preparation on the only edge into `AwaitingSignature`, and `operatorGate` refuses a missing preparation instead of waiving it. Coverage compared **row** ids while every clause carried the row's own id, so one passing read minted the window for `O-1`'s 100,000-USDC stake with the stake amount, the balance and the fee headroom unevaluated — coverage is now per clause (`ClauseId` is a `` `${string}/${string}` `` template type a bare row id cannot satisfy, and `TxPreparation` carries the fee asset the obligation set depends on). `VerifiedArtifact.bytes` was `readonly` with writable **contents**, so a valid brand could carry a runtime nobody hashed; the bytes are a `#` private field behind `copyBytes()`. Streamed chunks were retained **by reference**, so a source reusing one buffer produced a concatenation the hash never described; each chunk is snapshotted before it is hashed and kept. **P-13 fails closed** on the unreadable bond (Decision log; §11.5's redemption-fee rule 5 governs; no spec text amended, SQ-620 stays open). **And S17 had never been reachable** — `UNREADABLE['O-6']` cited SQ-615, which contract v28 resolved in this branch's own base, and S15 (SQ-616) and S19 (SQ-619) were shut the same way, which is why their suites asserted the refusal rather than the behaviour. The three seams are retired for ordinary clauses over the v28 surfaces, `O-5` is recited to the open SQ-601, and SQ-730/SQ-731 carry the two genuine gaps. `tools/ci/check-unreadable-obligations.py` now makes the expiry mechanical — the source had claimed it for as long as the defect had existed. **Verification:** `pnpm -C app test` 1,204/1,204, every `app/` gate and root doc gate green, 251 tooling tests, **9 mutants and 0 survivors**; GitHub Actions was in a major outage, so nothing was CI-verified. **Why it is not ✅.** Two reasons, both external to this track. (1) **Spec questions**, now SQ-601, SQ-598, SQ-730 and SQ-731 for the four controls this release still closes, plus the earlier SQ-601…SQ-609 set — five of which needed a 02 addition and three of those landed as contract v28, and three of which remain. **What contract v28 closed**: `ExecutionGuard.PendingUpgrade` and `System.AuthorizedUpgrade` (SQ-615 — the item is in `frame_system`, not `parachain-system`, which carries none), the guardian **pending-action** pair (SQ-616) and both registry instances' `Filings`/`ClosedAt`/`AckRecords` (SQ-619). S15's approve, S17's submit and S19's challenge are open on those reads since the #257 round; before it they were closed by declarations the freeze had already retired. **What WAS still closed is now open** — **contract v29 (2026-08-07) resolved all four** and this sentence is kept as history rather than status: S16's claim (SQ-601, now `treasury_streams`), S19's **file** (SQ-731, now `bond_quote`), the propose-playbook path (SQ-730, now one §11.8.2 row per variant) and `oracle.report` (SQ-598, now the published bond). `T_ins` (SQ-602) gained `NavView.insurance_target`, so `InsurancePanel`'s `unestablished` arm is now a real arm rather than the only reachable one. **What replaced them was worse and was F17's own, and it is now closed** (re-review 2026-08-08, fixed the same day): opening those four controls left **four input shapes that still reached `ready` on a condition nothing evaluated**, all in the §11.8.2 guardian tier and all the class of the blocker v29's own repair created. The **approve** control evaluates no trigger and no allowance at all, though the runtime enforces both at the **fifth** approval (11 §11.12 E20; `guardian-core` `TriggerInactive`/`AllowanceExhausted`) — so the one guardian whose signature the chain refuses is shown nothing about why. Nothing binds the evaluated trigger to the **trigger the call names**, so a `PB-LEDGER-FREEZE` activation passes on a `GateBreach` read. The trigger requirement keys on **`meter.power` rather than `inputs.args.power`**, so a panel titled *Propose a pause on intake* can carry activation bytes. And `TriggerState` has **no cohort field**, so `OracleDeadlock`'s mandated `contains_key(target)` is unexpressible and a *"some cohort is latched"* answer passes for *"this cohort is latched"* — the failure 05 §4.7 and 07 §10 exist to prevent. **Exposure was zero throughout**: `implementedScreens()` is `{}` and S14–S19 are `PENDING_SCREENS`, so none of it was ever reachable — but it had to be right before any transport lands. **The fix removed the root rather than guarding the four instances.** Three descriptions of one action travelled separately — meter, trigger, arguments — each validated by a function that saw only one, which is *how* "nothing evaluated it" survives a review of both halves. `GuardianProposal` is now one value per power, `guardianCall` derives the encoded call from it, and `proposeFormBlocks` is deleted. **Writing the input-shape enumeration is what found the class still open beyond the trigger**: `check_and_consume` runs inside `dispatch`, so the allowance, the hold window and the rerun ledger also fall on the fifth guardian, and `DispatchEvidence` closes those. Seven propose shapes and sixteen approve shapes are enumerated; **one stays open and is named** — `TooManyPending`, where the count is readable and the bound is not (SQ-1022). **F17's remainder is now the cross-milestone transport blocker alone**, and an R-6 re-review is owed against the closed class before any ✅. The remaining rows are doc-truing where the runtime is demonstrably right (SQ-617, SQ-618, SQ-620, SQ-621). (2) **No screen is registered in `implementedScreens()`**, because no live transport exists — a cross-milestone blocker every built screen shares (S2, S9–S13 included), not an F17 remainder. **What the audit got wrong, corrected here rather than filed as found:** it read 02 §6 and §11.8.4 as contradicting each other on `applicable_at`. They do not — §6 describes the **event** (two fields, with the derivation stated for an event consumer) and §11.8.4 the **stored** field, which `execution_guard_core::PendingUpgrade` really holds. The genuine defect is the unfrozen surfaces, plus a third the audit missed: the struct has **four** fields and doc 11 writes three, omitting `target_spec_version`. Filed that way as SQ-601. **Everything the previous entries recorded still stands** and is not repeated: the branded `VerifiedArtifact` with `verifyArtifact` as its only producer and the mismatch that throws; `applicable_at` read and never recomputed, asserted by the absence of lead-time arithmetic; `TriggerState.unread` treated exactly as `inactive`; approving twice as its own refusal; an overrun allowance reading as zero; integer vesting floored against the claimant; not-started and fully-claimed as different zeros; the `no-op` crank arm distinct from `ready`; the `indeterminate` challenge window that refuses to assume the base window; `RegistrationCheck.uncheckable` as a required field; and V-169's supplied registry instance names. FE-P10 still gates the upgrade tier's *submission path* only. **2026-08-08 — the owed R-6 re-review ran and the class was NOT closed. Three blockers, four majors, four minors; ten are fixed here and the eleventh is a contract bump on its own branch.** The previous entry claimed *"F17's remainder is now the cross-milestone transport blocker alone"*, and that was wrong in the way this milestone keeps being wrong: the last fix closed the shapes it was shown and left the conditions that live **one frame above `check_and_consume`** — in `dispatch`, in the pallet, and in the effect dispatcher. **Blocker 1 was the sharpest, because the source asserted its own safety.** 06 §6.2 is normative that a playbook with an empty admissible call set *"has no dispatchable activation: a fifth approval reaching it fails closed, the whole extrinsic reverts, and nothing is recorded"*, and `playbook_calls(Migration, …)` returns `Err`. The client called `PB-MIGRATION` an advisory, wrote a comment saying *"the runtime accepts the activation"* — true of `check_and_consume`, false of `dispatch` — and a **test pinned the wrong claim**. So a 7-member council could be walked to five signatures on a guaranteed revert. Ruled and fixed: **block on both propose and approve**, since §6.2 says nothing is recorded, so there is no record of intent to preserve; the test is renamed to state the reversed claim and why it was false. **Blocker 2**: the approve flow never evaluated `trigger_matches(id, trigger)` or the two-sided target rule, both of which `check_and_consume` refuses at the threshold approval — reachable because `propose_action` validates nothing on chain, so a mispaired action created by any other tool sits in `PendingActions` for three days looking approvable. `PLAYBOOK_TRIGGERS` was in the same module and only the propose side consulted it. Now `playbookBlocks()`, with a test enumerating the whole 6 × 8 pairing space. **Blocker 3 is a fabricated `Verified<T>` deciding a privileged control**, and it is the one this branch does not carry: `AllowanceMeter.limit` had **no producer** — `Guardian.Allowances` stores used-counters only and no `constant.guardian.*` publishes a bound — so the suite shipped `finalized(3)` for all five powers where the kernel's real values are 1, 2, 1 and none. That is INV-FE-1 on the precondition §11.8.2's propose row names by name. **Ruled: freeze them rather than close S15** — `pallets/guardian` already carries a `#[pallet::constant_name]` block, so four more are additive and change no behaviour, and closing a guardian's core control over a compile-time number would be the wrong R-7 trade. That work is `track-f/guardian-allowance-freeze`. **Also fixed**: `AllowanceBook` narrowed to the three **metered** powers, because 06 §5.2 writes *"per-playbook"* and *"condition-gated"* for the other two and `check_and_consume` charges neither — a supplied `used >= limit` was refusing a lawful activation, which is a client refusing what the runtime accepts; `PlaybookNotRegistered` and `PlaybookAlreadyActive` declared as `blocking` obligations citing **SQ-1030**; `TooManyReviews` declared `stated` citing SQ-1022, because closing the approve control on a bound 128 reviews away would refuse what the chain accepts and **naming an unenumerated shape is the whole lesson**; S19's readable half closed with an `AlreadyFinal` block over v28's `ClosedAt` and `FilingInputs` retyped per instance so `InvalidClass` is **unrepresentable** rather than checked; `O-4` gains the pending-actions read the propose row needs; and both allowance clauses move from `subject: 'acting'` to `'chain'`, since the item is a global `StorageValue`. **Verification**: `build`, `check:types`, screens 634/634, tx 166/166, firewall 43/43, and all 35 remaining `app/` gates plus five root checkers. **14 mutants, 0 survivors** — and the honest half: a first pass produced six bad mutants, four killed by the compiler for a *narrowing* reason that says nothing about coverage, and one that survived by renaming a string no test reads, which was a bad mutant rather than a weak test (deleting the whole entry is caught). **The cost, stated rather than absorbed: S15's approve control is now closed outright**, which is INV-FE-12 fail-closed and correct, and which recreates the very condition `check-unreadable-obligations.py` exists to keep temporary. It is mitigated rather than accepted — the tests that folded *"the model is happy"* and *"the gate opens"* into one `!buttonDisabled` assertion are **split**, so `approvalBlocks(clean) === []` is checkable while the gate is shut. That is exactly the property that stopped being checkable the last time a seam closed a screen. Raises SQ-1030 and SQ-1031. **2026-08-09 — ✅ on #295's gate evidence and an R-6 review that returned NO BLOCKER.** The transport blocker closed with F18/F26, and the review's last F17 major closed here. `challenge_filing(epoch, filing_id, evidence_hash)` names one filing, and the model named none. The window, the reason and the bond were all per-filing reads of `Filings[epoch][filing_id]`, with nowhere to record **which** filing. `freeUsdc >= challengeBond` is the **permitting** comparison on a bonded control. So a bond read from a cheaper filing in the same epoch opened a challenge the chain refuses after the signature — the shape `EpochClosure` and `FilingOccupancy` already close, on the one row where the user has a deadline and a single attempt inside it. The subject now travels **with** each read rather than beside it, so there is no second copy to disagree. `challengeFilingBlocks` refuses when `registry`, `epoch` or `filingId` differ, and names both filings. The shortfall comparison is an `else if`, so it never runs against the wrong filing. `RegistryFiling` loses its `filingId: Verified<string>` prop, which had let the panel title itself after one filing and refuse for another. Three mutants, three killed — one of them by a firewall fixture that **compiled**, which the corpus reports as a regression rather than a pass. **SQ-609, SQ-1022 and SQ-1031 stay open as follow-ups**, each costing one transaction fee and no bond, because all three refuse through `ensure!` and FRAME reverts the reserve with the rest of the storage layer
