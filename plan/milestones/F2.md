---
id: F2
track: F
title: FE-R1 — consume backend artifact feed: descriptor-drift CI, fixture suites
spec:
  - 11 §11.13
  - 02 §11
depends:
  - F0
  - B8
status: done
---

Release-gating. **Done 2026-08-03.** The 02 §11 feed exists, is consumed, and is gated in both directions — for the **paired** runtimes 10 §5.1 requires, not just the primary. **Produced (V-80, byte-reproducible):** `app/fixtures/chain-feed/{2,3}/` — the `bootstrap` runtime and its `bootstrap-recovery` pair at exactly the next `spec_version`, metadata **v15**, contract v23, 45 pallets each; and `app/fixtures/chainhead/` — 222 chainHead-v1 transcripts at one pinned block, `strict_ready: true` on both sides for the first time in this repository's history. **Consumed:** `packages/papi-descriptors` (PAPI 2.1.8, both chains, generated from the committed metadata with no node and no network) and `packages/mock-runtime` + `app/tests/mock-runtime` (9 tests: surface completeness bidirectional against the manifest, all 13 `FutarchyApi` methods callable, FE-R1's bounds asserted against **02 §9's frozen table rather than the recording**, both ledger domains separate per 11 §11.2a, the whole surface re-checked against the recovery runtime). **Gated:** `tools/ci/check-chain-feed.py` now sweeps every feed directory and enforces the pairing rule itself (one primary, one recovery at primary + 1, profiles declaring each other, matching contract versions, directory name = `spec_version`, declared hash = actual hash); `app/tools/check-descriptor-drift.ts` regenerates into a scratch tree and byte-compares all 16 descriptor files — PAPI's own `generated.json` check hashes the metadata *input* and never reads `dist/`, so a hand-edited descriptor passes it and fails this. Three CI steps added to the `app` job. **Anti-vacuity, 17 mutations, baseline restored to zero after each:** 6 on the drift gate (incl. repointing it at the stale keeper blob — the V-75 defect — which reddens all 13 files), 6 on the replay suite (incl. a bound moving 32→31 and an API signature drifting), 5 on the pairing gate. **Four defects found and fixed on the way:** V-75 (metadata v14), V-77 (8 drifted manifest layouts), V-78 (a strict failure that counted half of what it gates on; transcripts that embedded a session counter and so were not deterministic), V-79 (the 13 frozen API signatures were the one part of the surface no gate compared). **Not F2's scope and left to F4:** the app-facing `@bleavit/descriptors` wrapper, the Asset Hub set, and 10 §5.2 compat gating — F2 landed the generation *pipeline* early because a drift gate needs something to drift against
