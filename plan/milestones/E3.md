---
id: E3
track: E
title: **Transaction-fee destination** — route USDC transaction fees to `MAIN`; VIT fees keep burning
spec:
  - 08 §9 (new "Fee destination")
  - 13 §1 `fee.vit_usdc`
depends: []
status: done
---

08 §9 specified fee *mechanics* and was **silent on destination** — a genuine spec gap, now owned there. `configs.rs:300` and `:349` both pass `()` as the credit handler and `impl HandleCredit for ()` drops the credit, so **both** currencies burn today. Burning USDC contradicts the anti-burn rationale this doc set states four times (00:279, 08:333, 13:53, 13:208): bridged USDC is a claim against an Asset Hub reserve, and destroying the local claim orphans the remote reserve rather than destroying it. VIT burning is kept deliberately — natively issued, strands no reserve, and routing it would credit an asset 08 §2.2 marks at **0 in NAV**. 08 §2.1's "any change to the fee adapter MUST be re-proven" binds: the locked-account fee test is part of the proof. Fan-out: 05 §4 MetricId 20's "Fees burned/paid | protocol fee sink" label. **Done 2026-07-29 — PR #196 (`76ee39e`); the fee extension's own weight followed under SQ-523.**
