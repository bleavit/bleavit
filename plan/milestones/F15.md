---
id: F15
track: F
title: FE-13 ops handbook — bootnode/ArNS/launch operations docs
spec:
  - 12 §6
depends:
  - F11
status: done
---

**Landed 2026-08-04, completed 2026-08-07.** `deploy/ops-handbook/` — the standing-operations counterpart to O4's incident runbooks. **12 §6.3 and §6.4 had no handbook section at all, and §6.3's ownership rule had no reader anywhere.** Both are written now and `check-ops-handbook.py` binds them, so a §6 subsection with no handbook section is a failing gate rather than a gap somebody notices. The audit's premise held: both are role-keyed standing procedure and both were writable with every holder `VACANT`. **R-6 met on both conjuncts** — `tools/deploy/tests` 142 OK, `tools/ci/tests` 276 OK, `check-ops-handbook.py`, `check-runbooks.py` and all twelve `docs`-job gates green; the spec review returned **0 blockers**, 5 major and 8 minor, all closed except three that became SQ-627…SQ-629 because doc 12 does not settle them, and one deliberately declined — publishing a domain → §6.1-row mapping for all 24 domains would mean inventing rows doc 12 never states for SQ-627's four. `--strict` fails **by design**, listing the twelve vacancies, which is the pre-launch state rather than a defect. **What this ✅ asserts is that the handbook is complete and mechanically bound, not that operations are ready**: 12 §6.5's phase gates are the readiness test and all of them are still unmet. Holder names wait on a ceremony, the `[VERIFY]` relay-finality window waits on observed behaviour on the target relay, and the bootnode and served-state series wait on O3's exporter — each named in the handbook as an open obligation with an owner
