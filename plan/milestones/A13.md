---
id: A13
track: A
title: **Treasury reward lines, prize-scaled POL and frozen proposal-time security terms** — the 08 §1.1 `REWARDS` proposer schedule and 08 §2.4 `collator.comp` pro-rata payment (both normative), `ORACLE` line metering, the 08 §5.3 Ask-scaled `pol.b`/δ seeding that replaces today's flat `Params` values, the epoch-owned `ProposalSecurityTerms` side map frozen at qualification, the frozen `ledger.pos_dep` registry bound, and the calibrated `keeper.rebate` genesis seed
spec:
  - 08 §1.1, §2.4, §5.3–§5.4
  - 13 §1
depends:
  - A9
  - S4
status: done
---

**In progress 2026-07-24.** Security-term capture/backfill/retention/reap and runtime TREASURY `P_ref` scaling are live. The REWARDS line is now a custody-backed budget/payout path, with live class schedule lookup and fail-soft T17 execution payment; genesis provisions its dedicated pot and try-state binds line-to-pot solvency. SQ-384 is also resolved: the INSURANCE→MAIN custody-return path now has a dedicated `InsuranceSweep` capability, separate from `TreasurySpend`. SQ-303's temporary fail-closed budget screen now rejects unsafe timing/capacity and POL-floor directions; the final artifact verifier remains a successor obligation. Collator compensation is now complete: authored shares are bounded, floor-rounded, paid once from the dedicated custody pot at Housekeeping, and deferred atomically when underfunded. ORACLE-line dispute cranks and `keeper.rebate` are live; `ledger.pos_dep` is live/resolved. Remaining A13 scope is the certified PARAM/CODE/META envelope source and adoption of published `sec.prize.*`/`sec.flow_cap` only after that proof. Batch X owner for **SQ-36, SQ-117, SQ-158, SQ-173, SQ-270, SQ-300** (SQ-36/SQ-117/SQ-158 ✅ 2026-07-21; SQ-180/SQ-207/SQ-298 were resolved on PR #148/re-audit and are no longer open members). **✅ done 2026-07-25.** The last scope item — the certified PARAM/CODE/META capability-envelope source (SQ-173) — is implemented and the prize-scaled `pol.b`/δ machinery it gates (SQ-270/SQ-300) is live for those classes. Closed under R-6 by a **ten-round** spec-compliance review that returned `A13 MAY BE MARKED DONE` with **0 blockers / 0 majors / 0 minors / 0 nits** on the final round, after nine rounds that each found something real; the reviewer separately confirmed SQ-303, SQ-486 and SQ-186 sit outside this milestone's scope. Also landed here: SQ-383's phase-advancement NAV gate, SQ-382's shared-bit ruling, and SQ-195's day-resolved reserve-health input. Exhaustive Rust gate green (90 suites, 0 failures) on the final state. Successors: SQ-303's derived-budget artifact/verifier, SQ-486's `sec.flow_cap` values decision (user-gated), and SQ-186's metadata constant (needs contract v12→v13 with joint sign-off).
