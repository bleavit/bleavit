---
id: F26
track: F
title: FE-18 **the compatibility classifier's producer** — `createClient` above the read layer, a `CompatSurface` for both chains, and the first caller 10 §5.2's classifier ever had
spec: [10 §4.1/§5.2, 11 §11.9.1]
depends: [F4, F18]
status: done
---

**10 §5.2's three-mode classifier had never run on either chain.** `probeCriticalSurface` was defined once and called **zero** times, and `createClient` was constructed nowhere — so the compat lattice that `check-client-surface-obligations.py` exists to protect had no consumer at all. **Nobody owned it** (SQ-1010): `probe.ts` waits on F6, whose row never mentions it; the 2026-08-04 log assigned the placement to F7, whose row never mentions it; PLAN called it *an F3/F7 boot-sequence decision* — and F3, F4, F6 and F7 are all ✅, so it was handed forward three times and every recipient closed without taking it. Not a defect in F4, whose row correctly calls the classifier *a pure function over probe results, which is the point rather than a convenience*: until F18 there was no light client, so no client could be constructed. **The sharpest find is the foreign half.** `CompatSurface` declared four of PAPI's six groups and `FOREIGN_SURFACE`'s frozen 02 §7.7 entry is a **call**, which lives in `compat.tx` — so `PolkadotXcm.limited_reserve_transfer_assets`, the deposit leg itself, was the one Asset Hub surface the check structurally could not examine; skipping it would have been F4's own named failure. `tx` added, `view` correctly not, since nothing frozen is a view fn. Built above the read layer per the recorded constraint, so an incompatible runtime stays a boot state rather than a failure to construct. **An R-6 review returned 2 blockers, both reproduced and both fixed.** `#unpin` evicted `#runtimeAt` on window trim, so an upgrade that did not finalize inside the window was dropped and the client kept the **pre-upgrade** `spec_version` forever — classifying it `full` against a live post-upgrade runtime with signing enabled, and FE-TX-007's mitigation re-reads the same poisoned value so it could not fire. Fixed by **lifetime rather than window size**, the cap failing closed rather than evicting. The branch's own new test had demonstrated that eviction and read it as boundedness — the bound was right, the eviction of an un-promoted upgrade was what it hid. The second: PAPI defers compat work to property access, so the probe sat in a `try/finally` with no `catch` and threw instead of returning a verdict, taking `openDepositLeg` down rather than blocking it with diagnostics. Three majors and seven minors closed with them. **Verification:** full `app/` gate list plus twelve root checkers, suites 41/175/575, `surface:check` 288 probed, **21 mutants across two rounds all killed** — one survivor was M11, which asserted through a local copy of `levelOf`, so a mutant returning `identical` for every flat helper passed the whole suite. **Why not ✅:** the classifier cannot be exercised against a real chain — `releaseChainSpecs()` is `unpinned` — and `compat.ts` is untestable without a node, the same seam as `light-client.ts`. Raises SQ-1011, SQ-1012, SQ-1013. **2026-08-08 — the first blocker is retired and the three questions are ruled, but the milestone stays 🔨 on three majors.** **The stated blocker is gone**: F27's drill exercises this classifier against a real chain and reports `compatMode: "full"` at `specVersion 2`, so *"cannot be exercised against a real chain"* is no longer true. **The rulings ratified the implementation rather than replacing it** — 10 §5.2 keeps three modes and 10 §3.1 gains a state, `CompatUnavailable`, under a new `FE-COMPAT-003`; `ForeignMode` keeps five arms because 02 §7.7 already froze the distinction as *"unavailable **or unprobed**"*; and SQ-1013's answer inverted its own question, since smoldot de-duplicates identical chains and **instructs callers not to hand-roll it**, so the cost is CPU share and §9.4's silent row was the mobile-CPU one rather than the memory line (V-365, V-366). **The R-6 review returned 1 blocker, 6 majors and 11 minors, and the sharpest major was found by the normative text written the same hour.** §4.1's new obligation 2 — *"releasing a probe stops **every** chain it added"* — was violated by the code it describes: `getSmProvider` re-invokes its chain factory on every halt (131 calls measured in 300 ms), each call adds a relay **and** a parachain, and `compatProvider().release()` remembered only the newest pair. Two chains leaked per retry, on a path §3.2 re-runs on every `CodeUpdated`. **Closed**: the blocker (both VERBATIM design-kit copies had drifted at exactly the two edited lines, so the `docs` job was red), the leak, `FE-COMPAT-003` stamped on a session that never reached `CompatCheck` (an unpinned release is `FE-BOOT-001/002/004` and no retry can satisfy it — it is now `not-attempted`, carrying no code at all), and the drill recording `ForeignVerdict.kind` instead of the foreign lattice, which reported `"classified"` for a `wrong-chain` Asset Hub — the identical wrapper-not-lattice defect `compatMode` had already fixed for the local leg, left standing beside its own written explanation. The boot reducer now takes both new §3.1 edges, and **both of `transitionEdges`' enumerations are compiler-exhaustive**, because the hand-kept ones would have reported nothing had the state been added and the events forgotten. New: 15 §4.8's per-PR descriptor-compatibility row, which nothing discharged — `descriptors:check` byte-diffs the generated tree and never probes, so the full `CRITICAL_SURFACE` had never run against committed metadata and `bleavit_recovery` had never been probed at all. **Stated rather than implied: crossing the runtime pair still classifies `full`, so that loop cannot distinguish them today** — the two are surface-identical over `CRITICAL_SURFACE`, and a distinctness guard stops it degrading into one runtime probed twice. Verification: `check:types`, chain-client 200/200, screens 632/632, descriptors 44/44, `depcruise` 0 errors, `check:casts`, `check-verbatim-copies`, `check-plan-tables`, `check-spec-question-batches`; **4 mutants killed on the leak accounting and 3 on the FE-COMPAT-003 binding, 2 survivors on the runtime-pair crossing reported above rather than hidden**. **Why still 🔨 — three majors, each named with what it needs.** (1) The two obligations the ruling made normative have **no consumer**: there is no session-scoped compat mode, no re-classification on `CodeUpdated` and no backoff driver, because `startShell` types `connect` as `() => Promise<unknown>` and discards the verdict. Cross-milestone, with the transport. (2) `pallet-inflow-caps::CumulativeDeposits` is frozen in 02 §7.4 and absent from both `surface-manifest.json` and `CRITICAL_SURFACE`, so the lattice cannot fail on the Phase-3 deposit-cap read — **ruled**: 02 §7.6's *"generated from this contract's frozen set"* governs over 10 §5.2's *"the app uses"*, so this is a defect; it is not hand-fixed because manifest entries carry **recorded** layouts and hand-writing one would fabricate the bytes the recorder exists to capture. (3) `verdictAllowsSigning` / `callIsProven` / `verdictProvesSurface` have zero production callers, so INV-FE-12's *"signing is disabled wherever compatibility is unproven"* is not wired to the verdict — same transport dependency as (1). Nine minors carried forward

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track F — The canonical cross-platform client (`app/`) | -->

<!-- source row: | F26 | FE-18 **the compatibility classifier's producer** — `createClient` above the read layer, a `CompatSurface` for both chains, and the first caller 10 §5.2's classifier ever had | 10 §4.1/§5.2; 11 §11.9.1 | F4, F18 | ✅ | **10 §5.2's three-mode classifier had never run on either chain.** `probeCriticalSurface` was defined once and called **zero** times, and `createClient` was constructed nowhere — so the compat lattice that `check-client-surface-obligations.py` exists to protect had no consumer at all. **Nobody owned it** (SQ-1010): `probe.ts` waits on F6, whose row never mentions it; the 2026-08-04 log assigned the placement to F7, whose row never mentions it; PLAN called it *an F3/F7 boot-sequence decision* — and F3, F4, F6 and F7 are all ✅, so it was handed forward three times and every recipient closed without taking it. Not a defect in F4, whose row correctly calls the classifier *a pure function over probe results, which is the point rather than a convenience*: until F18 there was no light client, so no client could be constructed. **The sharpest find is the foreign half.** `CompatSurface` declared four of PAPI's six groups and `FOREIGN_SURFACE`'s frozen 02 §7.7 entry is a **call**, which lives in `compat.tx` — so `PolkadotXcm.limited_reserve_transfer_assets`, the deposit leg itself, was the one Asset Hub surface the check structurally could not examine; skipping it would have been F4's own named failure. `tx` added, `view` correctly not, since nothing frozen is a view fn. Built above the read layer per the recorded constraint, so an incompatible runtime stays a boot state rather than a failure to construct. **An R-6 review returned 2 blockers, both reproduced and both fixed.** `#unpin` evicted `#runtimeAt` on window trim, so an upgrade that did not finalize inside the window was dropped and the client kept the **pre-upgrade** `spec_version` forever — classifying it `full` against a live post-upgrade runtime with signing enabled, and FE-TX-007's mitigation re-reads the same poisoned value so it could not fire. Fixed by **lifetime rather than window size**, the cap failing closed rather than evicting. The branch's own new test had demonstrated that eviction and read it as boundedness — the bound was right, the eviction of an un-promoted upgrade was what it hid. The second: PAPI defers compat work to property access, so the probe sat in a `try/finally` with no `catch` and threw instead of returning a verdict, taking `openDepositLeg` down rather than blocking it with diagnostics. Three majors and seven minors closed with them. **Verification:** full `app/` gate list plus twelve root checkers, suites 41/175/575, `surface:check` 288 probed, **21 mutants across two rounds all killed** — one survivor was M11, which asserted through a local copy of `levelOf`, so a mutant returning `identical` for every flat helper passed the whole suite. **Why not ✅:** the classifier cannot be exercised against a real chain — `releaseChainSpecs()` is `unpinned` — and `compat.ts` is untestable without a node, the same seam as `light-client.ts`. Raises SQ-1011, SQ-1012, SQ-1013. **2026-08-08 — the first blocker is retired and the three questions are ruled, but the milestone stays 🔨 on three majors.** **The stated blocker is gone**: F27's drill exercises this classifier against a real chain and reports `compatMode: "full"` at `specVersion 2`, so *"cannot be exercised against a real chain"* is no longer true. **The rulings ratified the implementation rather than replacing it** — 10 §5.2 keeps three modes and 10 §3.1 gains a state, `CompatUnavailable`, under a new `FE-COMPAT-003`; `ForeignMode` keeps five arms because 02 §7.7 already froze the distinction as *"unavailable **or unprobed**"*; and SQ-1013's answer inverted its own question, since smoldot de-duplicates identical chains and **instructs callers not to hand-roll it**, so the cost is CPU share and §9.4's silent row was the mobile-CPU one rather than the memory line (V-365, V-366). **The R-6 review returned 1 blocker, 6 majors and 11 minors, and the sharpest major was found by the normative text written the same hour.** §4.1's new obligation 2 — *"releasing a probe stops **every** chain it added"* — was violated by the code it describes: `getSmProvider` re-invokes its chain factory on every halt (131 calls measured in 300 ms), each call adds a relay **and** a parachain, and `compatProvider().release()` remembered only the newest pair. Two chains leaked per retry, on a path §3.2 re-runs on every `CodeUpdated`. **Closed**: the blocker (both VERBATIM design-kit copies had drifted at exactly the two edited lines, so the `docs` job was red), the leak, `FE-COMPAT-003` stamped on a session that never reached `CompatCheck` (an unpinned release is `FE-BOOT-001/002/004` and no retry can satisfy it — it is now `not-attempted`, carrying no code at all), and the drill recording `ForeignVerdict.kind` instead of the foreign lattice, which reported `"classified"` for a `wrong-chain` Asset Hub — the identical wrapper-not-lattice defect `compatMode` had already fixed for the local leg, left standing beside its own written explanation. The boot reducer now takes both new §3.1 edges, and **both of `transitionEdges`' enumerations are compiler-exhaustive**, because the hand-kept ones would have reported nothing had the state been added and the events forgotten. New: 15 §4.8's per-PR descriptor-compatibility row, which nothing discharged — `descriptors:check` byte-diffs the generated tree and never probes, so the full `CRITICAL_SURFACE` had never run against committed metadata and `bleavit_recovery` had never been probed at all. **Stated rather than implied: crossing the runtime pair still classifies `full`, so that loop cannot distinguish them today** — the two are surface-identical over `CRITICAL_SURFACE`, and a distinctness guard stops it degrading into one runtime probed twice. Verification: `check:types`, chain-client 200/200, screens 632/632, descriptors 44/44, `depcruise` 0 errors, `check:casts`, `check-verbatim-copies`, `check-plan-tables`, `check-spec-question-batches`; **4 mutants killed on the leak accounting and 3 on the FE-COMPAT-003 binding, 2 survivors on the runtime-pair crossing reported above rather than hidden**. **Why still 🔨 — three majors, each named with what it needs.** (1) The two obligations the ruling made normative have **no consumer**: there is no session-scoped compat mode, no re-classification on `CodeUpdated` and no backoff driver, because `startShell` types `connect` as `() => Promise<unknown>` and discards the verdict. Cross-milestone, with the transport. (2) `pallet-inflow-caps::CumulativeDeposits` is frozen in 02 §7.4 and absent from both `surface-manifest.json` and `CRITICAL_SURFACE`, so the lattice cannot fail on the Phase-3 deposit-cap read — **ruled**: 02 §7.6's *"generated from this contract's frozen set"* governs over 10 §5.2's *"the app uses"*, so this is a defect; it is not hand-fixed because manifest entries carry **recorded** layouts and hand-writing one would fabricate the bytes the recorder exists to capture. (3) `verdictAllowsSigning` / `callIsProven` / `verdictProvesSurface` have zero production callers, so INV-FE-12's *"signing is disabled wherever compatibility is unproven"* is not wired to the verdict — same transport dependency as (1). Nine minors carried forward | -->

<!-- source track narrative -->

Epic IDs from 11 §11.13. FE-P1…P11 prototype gates from 10 §12 (+FE-P10/FE-P11, 11 §11.13).
Rooted at **`app/`** (10 §10.1, amended 2026-08-03); the `frontend/` placeholder is retired.

**Phase grouping** (2026-08-03). **Phase 0** = F19, the spec batch and its two proofs. **Phase 1**
= F0, F2–F6, F20, F21, F7, F10, F11, F22 — web + PWA canonical plus a **direct-download** desktop
Tauri build; no app store, no iOS, no Android, no Product target, therefore **no INV-FE-8/-10
amendment needed**, since store re-signing is the only thing that makes those texts false.
**Phase 2** = F7b, F8, F9, F12, F14, F16–F18 + mobile shells. **Phase 3** = the store question
(scope INV-FE-8/-10 to the canonical channel, add INV-FE-16 channel honesty) + the Product target,
which ships fail-closed read-only until PROD-1/PROD-2 pass.

**Handoff-first navigation** (user ruling, 2026-08-03 — 11 §11.2). The client's two *primary*
surfaces are **prepare a capsule** (S21) and **review/confirm/sign** (§11.3–§11.4 + S22); the
analytical depth and the whole FE-14/FE-15 set sit behind **Advanced**. This reorders Phase 1 —
the handoff packages and the Skill set (F20/F21) now precede the screens (F7) instead of trailing
them — and it is a **presentation change only**: every screen in the 11 §11.2 inventory still
ships, still works with no external tool present, and the no-infrastructure certification run
still executes with the handoff surfaces disabled. Demoting a surface is permitted; removing one
would falsify INV-FE-4 and the 15 §2.1 reading that admits the handoff at all. **Two consequences
are not free and are tracked, not assumed:** SQ-552 (the missing 09 §1.2 ↔ 11 §11.5 precondition
diff) now blocks a *primary* surface rather than a deferrable one, and doc 14's TH-74 residual was
re-assessed for its enlarged exposure (§3.9 preamble).

<!-- end source track narrative -->
