---
id: B10
track: B
title: "Runtime wiring closure — the enforcement surfaces the S3 limit-coverage audit proved unwired: dead-man detector (4,800-relay-block finality stall / overdue snapshot → queue-freeze/clock-pause signal), treasury obligation sync callers (pending outflows ← guard queue, POL commitments ← market lifecycle, coretime quote-note/prune ← B4 seam), production XCM `AssetTransactor` + Params-backed cap/trader adapters (SQ-110…SQ-112), `pol.budget_epoch` consumer, and the §1 keys whose consumer pallets still read kernel constants instead of live `Params` (SQ-159)"
spec: [05 §4.3, 08 §1.3/§6/§8, 09 §4–§5, 13 rules 2–3]
depends: [A8 wiring, B4, B9]
status: done
---

**Done 2026-07-18 (worktree `milestone-B10`).** Every S3 exemption cleared except one: 15 `consumer_binding` keys rebound to live Params (SQ-159; per-game freeze of the oracle bond schedule + R_max per SQ-249), `TwapCheckpoints` at the 04 §7 two-limb spec shape (MarketBook 205 B), production `AssetTransactor` = `CappedInflows` + Barrier/IsReserve/AssetTrap + the 09 §6.2 exit path + the SQ-105 split gate, the dead-man detector (both triggers, latch, proposal-free recovery; pause-window re-registration), treasury pending-outflows/POL-commitment mirrors (fail-loud NAV poison; VOID resync; terminality latch) and `pol.budget_epoch` shrink-to-fit with `SlotsShrunk`. Registry: 65 dispatch-limit, 0 consumer-binding, 1 unwired (`Treasury coretime obligations` → **B12**, no spec-d quote authority — SQ-245/SQ-246). Orchestrated with **Codex subagents (gpt-5.6-sol/xhigh)**: 5 authors → triple spec-review (0/5/2 + 0/3/3 + 0/3/4) + adversarial (3/5/1; 1 blocker rejected on the SQ-101 pre-genesis precedent, 1 pre-existing → SQ-263 escalation + release blocker) → 2 remediation rounds, all majors fixed. New release blockers: `oracle.bond_custody_absent`, `xcm.pallet_xcm_weights_placeholder` (SQ-261). SQ-244…SQ-263 logged; merged onto post-B11 stable2606 main mid-session. O5 (merged concurrently) assigned its 9 runtime telemetry seam series to B10 in `tools/monitoring/series-inventory.toml`; B10 closed before O5 landed, so those seams are re-owned to **B13** (the O5 coverage gate's mechanical expiry otherwise fires on a completed milestone)

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track B — Runtime, node and chain | -->

<!-- source row: | B10 | Runtime wiring closure — the enforcement surfaces the S3 limit-coverage audit proved unwired: dead-man detector (4,800-relay-block finality stall / overdue snapshot → queue-freeze/clock-pause signal), treasury obligation sync callers (pending outflows ← guard queue, POL commitments ← market lifecycle, coretime quote-note/prune ← B4 seam), production XCM `AssetTransactor` + Params-backed cap/trader adapters (SQ-110…SQ-112), `pol.budget_epoch` consumer, and the §1 keys whose consumer pallets still read kernel constants instead of live `Params` (SQ-159) | 05 §4.3; 08 §1.3/§6/§8; 09 §4–§5; 13 rules 2–3 | A8 wiring, B4, B9 | ✅ | **Done 2026-07-18 (worktree `milestone-B10`).** Every S3 exemption cleared except one: 15 `consumer_binding` keys rebound to live Params (SQ-159; per-game freeze of the oracle bond schedule + R_max per SQ-249), `TwapCheckpoints` at the 04 §7 two-limb spec shape (MarketBook 205 B), production `AssetTransactor` = `CappedInflows` + Barrier/IsReserve/AssetTrap + the 09 §6.2 exit path + the SQ-105 split gate, the dead-man detector (both triggers, latch, proposal-free recovery; pause-window re-registration), treasury pending-outflows/POL-commitment mirrors (fail-loud NAV poison; VOID resync; terminality latch) and `pol.budget_epoch` shrink-to-fit with `SlotsShrunk`. Registry: 65 dispatch-limit, 0 consumer-binding, 1 unwired (`Treasury coretime obligations` → **B12**, no spec-d quote authority — SQ-245/SQ-246). Orchestrated with **Codex subagents (gpt-5.6-sol/xhigh)**: 5 authors → triple spec-review (0/5/2 + 0/3/3 + 0/3/4) + adversarial (3/5/1; 1 blocker rejected on the SQ-101 pre-genesis precedent, 1 pre-existing → SQ-263 escalation + release blocker) → 2 remediation rounds, all majors fixed. New release blockers: `oracle.bond_custody_absent`, `xcm.pallet_xcm_weights_placeholder` (SQ-261). SQ-244…SQ-263 logged; merged onto post-B11 stable2606 main mid-session. O5 (merged concurrently) assigned its 9 runtime telemetry seam series to B10 in `tools/monitoring/series-inventory.toml`; B10 closed before O5 landed, so those seams are re-owned to **B13** (the O5 coverage gate's mechanical expiry otherwise fires on a completed milestone) | -->

<!-- source track narrative -->

Doc-09 WBS delta rows map here and to Track A: E15→B2/B8 · E16→A2 · E17→A3 · E18→A6 ·
E19→A5 · E20→A10/B6 · E21→B4 · E22→A11/B1. (E1–E14 definitions live in git history —
superseded `BACKEND_PLAN.md` §26; their scope is covered by Tracks M/A/B.)

<!-- end source track narrative -->
