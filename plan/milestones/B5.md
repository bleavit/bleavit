---
id: B5
track: B
title: Benchmarks, weights, PoV budgets (196-market table), weight-regression CI
spec: [15 §4.5, 13 §4–§5]
depends: [B1a]
status: done
---

I-20. **Done 2026-07-17** — carried the B1a A8/A11 runtime wiring as groundwork, then: `define_benchmarks!` over all 12 futarchy pallets + the standard/Cumulus set with a real `Benchmark` API; 121 failing runtime benchmarks repaired benchmark-only (0 pallets dropped, 312 execute); omni-bencher weights (50×20) for 30 pallets wired into every Config (exceptions documented: `pallet_xcm`→B4, tx-payment no dispatchables, vesting→next sweep); the PoV hardening — bounded `(hash, recorded_len)` preimage fetch closing a real 4 MiB-PoV-read exploit, epoch submit 64 KiB cap, `pov_mode Measured` ×3 with distinct-payload tick fixtures, `execute` pre-charge (`prop.max_weight`) + `PostDispatchInfo` refund — plus the 06 §4 qualification-time preimage pin (SQ-140); `pov_budgets.rs` (13 §5 items 1/2/7 recomputed from measured `MaxEncodedLen` — 189/160/81 B, `[VERIFY]` tags resolved — all 94 fns fit the normal class, decide/settle pins); weight-regression CI gating >10 % on worst-case totals (full-expression parser incl. reads/writes + slopes, removal acks, HEAD~1 main fallback, 16 self-tests) + `weights`/`benchmark-smoke` CI jobs. Dual review (spec-reviewer 0/3/7 + Codex adversarial 4/3/4) — all actionable findings fixed with regressions; SQ-93 extended (apply surface), SQ-147…SQ-149 raised. Local-host calibration; reference-hardware recalibration rides the B8 release pipeline. Follow-ups: `keeper.rebate` fee-basis seed (SQ-117, user-gated 13 §1 edit), vesting weights + `oracle-core` report-window single-home at the next regeneration sweep

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track B — Runtime, node and chain | -->

<!-- source row: | B5 | Benchmarks, weights, PoV budgets (196-market table), weight-regression CI | 15 §4.5; 13 §4–§5 | B1a | ✅ | I-20. **Done 2026-07-17** — carried the B1a A8/A11 runtime wiring as groundwork, then: `define_benchmarks!` over all 12 futarchy pallets + the standard/Cumulus set with a real `Benchmark` API; 121 failing runtime benchmarks repaired benchmark-only (0 pallets dropped, 312 execute); omni-bencher weights (50×20) for 30 pallets wired into every Config (exceptions documented: `pallet_xcm`→B4, tx-payment no dispatchables, vesting→next sweep); the PoV hardening — bounded `(hash, recorded_len)` preimage fetch closing a real 4 MiB-PoV-read exploit, epoch submit 64 KiB cap, `pov_mode Measured` ×3 with distinct-payload tick fixtures, `execute` pre-charge (`prop.max_weight`) + `PostDispatchInfo` refund — plus the 06 §4 qualification-time preimage pin (SQ-140); `pov_budgets.rs` (13 §5 items 1/2/7 recomputed from measured `MaxEncodedLen` — 189/160/81 B, `[VERIFY]` tags resolved — all 94 fns fit the normal class, decide/settle pins); weight-regression CI gating >10 % on worst-case totals (full-expression parser incl. reads/writes + slopes, removal acks, HEAD~1 main fallback, 16 self-tests) + `weights`/`benchmark-smoke` CI jobs. Dual review (spec-reviewer 0/3/7 + Codex adversarial 4/3/4) — all actionable findings fixed with regressions; SQ-93 extended (apply surface), SQ-147…SQ-149 raised. Local-host calibration; reference-hardware recalibration rides the B8 release pipeline. Follow-ups: `keeper.rebate` fee-basis seed (SQ-117, user-gated 13 §1 edit), vesting weights + `oracle-core` report-window single-home at the next regeneration sweep | -->

<!-- source track narrative -->

Doc-09 WBS delta rows map here and to Track A: E15→B2/B8 · E16→A2 · E17→A3 · E18→A6 ·
E19→A5 · E20→A10/B6 · E21→B4 · E22→A11/B1. (E1–E14 definitions live in git history —
superseded `BACKEND_PLAN.md` §26; their scope is covered by Tracks M/A/B.)

<!-- end source track narrative -->
