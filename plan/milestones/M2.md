---
id: M2
track: M
title: `crates/futarchy-fixed` — 64.64 fixed point, exp2/log2/ln, maker-adverse rounding, error bounds
spec:
  - 04 §4
depends:
  - M0
status: done
---

**Done 2026-07-15 (Track M finish).** `exp2`/`log2`/`ln` reworked at **guarded Q96 precision** (32 guard bits, single final rounding; negative `exp2` via the complement identity `2^{-(w+f)} = 2^{-(w+1)}·2^{1-f}` — no reciprocal). Worst case **0 ulp over 10⁶ dense-bit points** vs the 100-digit oracle (was 10.11 ulp / 36.9 %); the worst-case error analysis is in the crate doc-comment (04 §4 conformance). Per-commit **1,286-point dense-bit adversarial corpus** (mean fraction popcount 32; exp2 integer parts 1–63 + log2/ln top octave near 2⁶⁴ + structured edges) generated from the single 04 §5 reference generator — **`tools/fixed` retired**, both CSV fixtures deleted, the corpus lives in `vectors.json` (`transcendental_corpus`). Exact-u256 `48·b` domain compare (truncation sliver closed); `exp2` shift via `checked_mul` (no silent wrap); f64 helpers (`from_f64`/`to_f64`) gated behind `std`; domain/error bounds imported from `futarchy-primitives::kernel` (`LMSR_DOMAIN_BOUND`/`PRIMITIVE_MAX_ULP`/`COMPOSED_COST_MAX_ULP`); real `cargo build --no-default-features` no_std gate wired into CI. Factor table self-checked (`f[i]²=f[i-1]`, `f[0]²=2`) needing no external oracle. Reviews: **spec-reviewer 0 blocker / 0 major / 5 minor** (all applied) + **Codex adversarial review 0 defects** (independently verified U256 helpers, both exp2 paths incl. whole=63 bit-safety, log2 at `u128::MAX`, the exact domain compare, ln ≤1.887 ulp budget, no panics). Full workspace gate green. Follow-up: the ≥10⁷ MPFR-256 sweep remains B8.
