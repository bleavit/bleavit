---
id: A4
track: A
title: `pallet-origins` + `SafetyFilter` — 8 custom origins, closed wrapper set, call-domain derivation
spec:
  - 06 §3
  - 01 §6
depends:
  - M1
status: done
---

I-8, I-10, I-11. **Core ✅.** **Done 2026-07-15 (PR #47)** — production FRAME shell over `origins-core`. **FRAME pallet implemented 2026-07-15** — production `#[frame_support::pallet]` **stateless shim** (no storage/calls/events/genesis, à la OpenGov `pallet-custom-origins`): real `#[pallet::origin]` 8-variant enum (06 §3.1; `DecodeWithMemTracking` per stable2603 `construct_runtime!`; byte-for-byte SCALE-index differential vs `origins-core`), the `EnsureOrigin` set (8 unit ensures via `decl_unit_ensure!` + `EnsureFutarchyOrigin`), `SafetyFilter` as a classifier-parameterized `Contains` (`SafetyClassifier`/`ModelClassifier` — decision logic shared with the reviewed frame-free core; the concrete `RuntimeCall` projection is the one explicit B1a extension point, keeping the wrapper set *closed*), stateless `try_state`, real `construct_runtime!` mock, 19-test 15 §4.1 suite (I-8 scope split, I-10/I-11 closed-wrapper negatives incl. `proxy_announced`/`as_multi_threshold_1`/nested-laundering/scheduler/budgets, G-5 no-escalation via every wrapper) + v2 `#[benchmarks]` (worst-case filter) + `WeightInfo`. B1 frame-free model repointed to `origins_core`. **Routed "tunables baked into cores" major fixed** (spec-reviewer 2026-07-15; Track-M audit routing): `origins-core` imports `kernel::MAX_NESTED_LEVELS/_CALLS` from `futarchy-primitives` instead of re-declaring them (01 §5.2 single-home / SQ-25); naming minor cleared in the same swap. Gates green (workspace fmt/clippy/test + `runtime-benchmarks`/`try-runtime` combos). **Dual review (spec-reviewer + user-requested Codex adversarial), both triaged 2026-07-15.** spec-reviewer 0 blockers / 1 major (single-home — fixed) / 2 minor (naming→fixed; fuzz→S2). Codex 1 "blocker" + 3 majors + 1 minor, triaged: **(1)** the origin-less base filter denying bare governance leaves is **not an A4 defect** — `execution-guard-core` already applies the origin-aware `validate(Some(class_origin))` (I-11); the intended two-check split is base filter = signed-extrinsic guard, origin-aware re-check + `EnsureOrigin` at the guard/scheduler dispatch boundary (§3.4). Documented as the B1a dispatch protocol in `SafetyFilter`; pinned as **SQ-32** (06 §3.3 prose-vs-code). **(2/3)** scheduler-origin authentication + borrow-don't-clone fail-closed visitor = **B1a classifier constraints**, documented on `SafetyClassifier`. **(4)** real-`RuntimeCall` `BaseCallFilter` dispatch / producer-path / XCM / exhaustiveness = **B1a/S5**, nested-wrapper fuzz = **S2**; the one cheap gap (nobody row under `proxy_announced`/`as_multi`/`as_multi_threshold_1`/scheduler) **fixed in the suite**. **(5)** count semantics = **SQ-31**. (Codex's own build/test couldn't run — its sandbox was read-only; local gates authoritative + green.) Deferred by design, A1 precedent: B1a runtime-level negative suites, S2 fuzz, S5 filter-exhaustiveness
