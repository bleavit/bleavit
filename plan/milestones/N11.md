---
id: N11
track: N
title: **`docs/integration/` human documentation** — a third tier beside `docs/architecture/` (normative) and `docs/design/` (derived): plain-language client docs
spec:
  - 16
depends:
  - N10
status: done
---

Docs written 2026-08-01 and updated 2026-08-02 for the adjudicated client-kit findings: the runtime example now shows the fail-closed spending origin and the callback-weight obligation, and the quickstart uses the explicit harness governance path. The quickstart source remains byte-bound to its drill helper by `tools/ci/check-quickstart-drill.py`; structural checks are green. **`integrate-xcm.md` added 2026-08-02 at the user's prompting, and it corrected a framing error worth keeping:** the tier presented three audience-shaped paths (parachain / contract / service) and left out the one thing they all reduce to. Pure XCM **is** the protocol — the drop-in pallet, the ABI crate and the TS package are conveniences that emit exactly those bytes — so the page now publishes the whole surface: the six-position template with its four structural properties, the frozen `[66, index] ++ SCALE(args)` selectors, which three calls the `ExternalClient` filter actually admits, the field-by-field `RegisterInput` shape, both report routes with pull authoritative, and the four refusal stages. It is bound to the ABI by `tools/ci/check-integration-abi.py` (bidirectional on the call table, plus the USDC location, the attestor bound and the push selector) because nothing else in the repository reads that page, so a renumbered selector would otherwise rot silently into every hand-rolled integration. N11 stays open with N10 until the live no-return-channel drill can execute against a producing HRMP topology. **✅ 2026-08-03 (N12 session) — that condition is met: drill 11 executed 4/4 on a producing HRMP topology once SQ-567/SQ-568 unblocked candidate building.** Stated precisely, because the drill was re-scoped in the same breath it started passing: what executed and passed is the **containment** half of I-36 — ingress from an unregistered client moves no XCM-health counter — measured as a controlled experiment against its own control window, not as the byte-identical assertion an earlier revision wrongly made (Bleavit performs its own outbound XCM, so that assertion was over-strong). The **push** half stays unwitnessed because the service is inert by construction: `svc.client_bond` is deliberately `[VERIFY]`-unset, so no drill-reachable admission path exists, and forging the Guardian origin to manufacture one was refused rather than done — `utility.dispatch_as` is denied by the wrapper filter (I-10/I-11), and seeding a bond value to green a drill would be exactly the R-2 fabrication the unset row exists to prevent. **SQ-570 owns that residual**, and it is a limitation of what an inert service can be made to do, not an N11 deliverable gap: N11 ships documentation, and its own binding gate (`check-quickstart-drill.py`, wired into the `docs` job and adversarially verified) is green.
