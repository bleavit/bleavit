---
id: S4
track: S
title: Agent-based economic simulation — δ per class (false-pass < 1 %), `AttackCost̂` validation, POL sizing; publish sim-gated params
spec:
  - 15 §4.9
  - 08 §5
  - 13 (sim-gated)
depends:
  - M3
  - A3
  - A8
status: done
---

**PUBLISHED (V-12, 2026-07-19).** The decoupled-δ recalibration (per-class `dec.delta` **0.0375/0.0375/0.060/0.090** — PARAM ×2.5, others ×1.5; genesis-only, 0.005 kernel floor unchanged) over the sim-faithfulness fix (proposal effect-scale anchored to a frozen `EFFECT_SCALE`, decoupled from `decision_delta` — the prior ∝δ population made "calibrate δ" a scale-invariant no-op) drives every class's decidable-harm false-pass **< 1 %** (param 0.000 / trs 0.145 / code 0.135 / meta 0.563 %) with all four classes adopting (58/156/139/171). The 15 §4.9 profitable-exploit criterion (a flip is a failure only when *realized* cost < prize; deep-pocket griefing with cost ≥ prize is a TM-18 diagnostic) leaves **0 profitable sub-3P flips**, envelope clean, 63 griefing diagnostics. `run-calibration.py --check` green (24 byte-exact subsample rows, Merkle, source digest); artifact `designation: published`, `violations: []`. Two full 10k runs: #1 (PARAM ×1.5) surfaced one profitable PARAM flip (id=3568, marginal proposal on an unbacked SQ-173 prize) → PARAM bumped ×2.5, proven to close it (0.35× → 6.06× prize). Spec-reviewer 0 blocker (2 major/3 minor fixed); SQ-273 (1× vs 3× exit threshold) logged for ratification. See Decision + Session logs.
