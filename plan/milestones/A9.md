---
id: A9
track: A
title: `pallet-futarchy-treasury` — sub-accounts, NAV + haircuts, outflow meters/streams, budget lines, `issue_vit`, coretime renewal call
spec:
  - 08 §1, §6–§9
depends:
  - A1
status: done
---

I-7. **Done 2026-07-15 (PR #51)** — production `#[frame_support::pallet]` over `futarchy-treasury-core`: single bounded `StorageValue<TreasuryState>` aggregate (mirror of the core `Treasury`; every collection a `BoundedVec` bounded by 13 §4 — new §4 rows for streams/lines/pending/POL/coretime, Decision log), 8 origin-checked extrinsics with one `FutarchyTreasury` `EnsureOrigin` for outflows + Signed-permissionless claim/renewal (renewal freeze-exempt, D-9), **rule-4 caps read from `pallet-constitution::Params`** via a `TreasuryParams` provider (mock proves injection; runtime wires B1a), NAV + reserve-haircut fail-static, mandatory-stream vesting, a **rolling trailing-365-day issuance meter** (`RollingMeter<366>`), runtime-internal APIs (`set_reserve_impaired`, `note`/`prune_coretime_renewal_quote`, hard `ensure_nav_floor` + durable-loud `flag_nav_floor`, `set_pol_commitments`/`set_pending_outflows`), genesis (fixed 1B VIT per 08 §2.1), try-state (bounds + solvency + I-7 issuance envelope vs the kernel ceiling), mock (`AccountId32`, overridable Params statics), 24-test 15 §4.1 suite + 600-step shell-vs-core differential, v2 `#[benchmarks]`×8 + weights, exact stable2603 pins. **Core (behavior-preserving):** tunable seams; **G-1 atomicity hardening** (rejected ops strict no-op); `VIT_TOTAL_SUPPLY` → `futarchy-primitives::currency` (rule 4). Triple review (spec-reviewer ×2 + Codex adversarial) — **0 blockers** — plus the **PR #51 Codex-bot round (2 × P2, both fixed):** fixed-window issuance → trailing `RollingMeter<366>` (closes the ~2× year-seam doubling; resolves SQ-50 + SQ-54); `NavFloorUnmet` gate split into hard `ensure_nav_floor` (no doomed event) + durable-loud `flag_nav_floor` (08 §4.2/§4.4). Earlier majors (stream reaping, NAV double-count, POL/queued entry points, try_state envelope) + Codex correctness (zero-price quote, genesis supply, `fund_budget_line` atomicity, quote pruning) all fixed; SQ-47…SQ-57 logged. Gates green: fmt · clippy -D warnings · workspace (core 9 + pallet 24; 32 with benchmarks) · no_std · try-runtime · corpus/reference-model/vectors · doc-links. **Follow-ups (flagged, not blocking):** per-proposal aggregate cap (SQ-51, cross-pallet); POL/queued keying + per-line budgets (SQ-47/SQ-52, B1a/A3/A11); floors frozen-vs-derived (SQ-56); coretime-quote validity (SQ-53, B4); 02↔08 `NavView` reconciliation (SQ-55, B2); keeper-meter events (SQ-49, B9); asset custody / `fungibles`+XCM (B1a/B4); PoV weights (B5); vesting store (08 §2.2, V-8)
