---
id: B1
track: B
title: Runtime assembly — standard-pallet config (incl. genesis `frame-system` filter D-13, USDC as ForeignAsset, fees in VIT/USDC), `SafetyFilter` as `BaseCallFilter`, origins wiring
spec:
  - 01 §5–§6
  - 06 §3
  - 09 §5
depends:
  - A1–A11
status: done
---

Frame-free composition **model** (pallet list, SafetyFilter `BaseCallFilter` adapter, D-13 genesis filtering, USDC/fee/origin wiring, filter-exhaustiveness tests) — the real `construct_runtime!` assembly is B1a. Left ✅ as the delivered model scope even though its A1–A11 deps are reopened
