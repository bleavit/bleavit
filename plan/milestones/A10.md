---
id: A10
track: A
title: `pallet-guardian` + `pallet-attestor` — 7-seat council, powers, playbooks incl. PB-LEDGER-FREEZE, retro ratification; bonded 2-of-N attestor registry
spec: [06 §5–§7]
depends: [A1, A4]
status: done
---

I-19, I-23. **Done 2026-07-15** — two production `#[frame_support::pallet]`s over their frame-free cores by the A1 shell-over-core pattern (load storage → core → persist → drain events). **`pallet-guardian`**: decomposed bounded storage per 06 §8; 5 origin-checked extrinsics (`set_members`/`ratify_action`/`renew_playbook` = `ConstitutionalValues`; `propose_action`/`approve_action` = Signed member); 02 §6 frozen event schema incl. `ReviewScheduled{action,referendum}` via an injected `ReviewScheduler` seam; cross-pallet reads via injected `ProposalStatusProvider`/`TriggerProvider` seams; bounded `on_initialize` crank (playbook expiry + overdue-review 50% slash + **terminal reaping**); genesis, mock, v2 `#[benchmarks]`×6, `WeightInfo`. **`pallet-attestor`** (authored by a Codex subagent mirroring guardian): storage per 06 §8 (`Members`/`Attestations` + `attestations_for`/`open_challenges` projections); calls `set_members`(ConstitutionalValues)/`attest`/`challenge_attestation`(Signed)/`resolve_challenge`(`RatifyOrigin`); 2-of-N quorum view for the guard (09 §1.2); `att.min_members`/`att.quorum` from `primitives::kernel` (single-home). `DecodeWithMemTracking` added to both cores' call/event SCALE types; `T::AccountId↔[u8;32]` bridge via a supertrait assoc-type bound. **Review (R-6): two `spec-reviewer` passes (guardian 0 blocker/5 major/6 minor; attestor 0 blocker/1 major/4 minor) + a Codex adversarial pass (0 blocker / 5 findings).** All fixed-in-scope items landed in-session with tests: guardian **reaping** of dispatched/expired actions + terminal reviews (the 64/128 caps were lifetime → concurrent), **clear pending+approvals on re-election** (closes the stale-approval 5-of-7 bypass), **`RecallScheduler` seam** (06 §5.4 recall referendum), **I-23 try_state** (every dispatched action reviewed), `saturating_add` counters; attestor **try_state recall relax** (historical attestations from recalled members are lawful) + **`NoOpenChallenge`** error. From the Codex pass: attestor **upheld quorum now waits for the 72h window** (06 §7 "windows have closed"), guardian **playbook singletons** (all ids deduped; re-activation renews in place, so duplicate `Depeg`s can't exhaust the six slots and block PB-LEDGER-FREEZE) + a try_state uniqueness check, and a **round-trip account-bridge check** at every Signed authorization (rejects a lossy `AccountId` aliasing → impersonation; the runtime pins `AccountId32`, 02 §8). The remaining bond findings — guardian liability across council rotation and real attestor bond custody across re-election (06 §5.1 "held for the full term plus one epoch") — belong to the deferred per-account `fungible` bond system (B-track), documented in code + Decision log. Gates green: fmt · `clippy --workspace -D warnings` · `cargo test --workspace` (54 suites; guardian 35 unit + core 11 + 41 with benchmarks; attestor 17 + core 5) · `runtime-benchmarks` + `try-runtime` feature builds. **Deferred (B-track, like A1's PoV weights → B5 / origin wiring → B1a):** real `fungible` bond holds/slash — **including cross-rotation/re-election bond liability** (06 §5.1 "held for the full term plus one epoch": the current arithmetic placeholders move no funds, so a recalled guardian approver can evade retrospective slashing; PR #130/SQ-262 separately fixed attestor `false_count` persistence across re-election, while custody remains absent); the guardian effect-dispatch + referenda submission (the injected seams) wired at B1a; per-playbook expiry beyond the safe uniform 14-day cap (SQ-45); K/META/entrenched single-home relocation (M1 + Params reads). **SQ-45/46 raised** (guardian per-playbook expiry; attestor `resolve_challenge`/event-naming freeze). See Decision log + Session log 2026-07-15

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track A — Protocol pallets | -->

<!-- source row: | A10 | `pallet-guardian` + `pallet-attestor` — 7-seat council, powers, playbooks incl. PB-LEDGER-FREEZE, retro ratification; bonded 2-of-N attestor registry | 06 §5–§7 | A1, A4 | ✅ | I-19, I-23. **Done 2026-07-15** — two production `#[frame_support::pallet]`s over their frame-free cores by the A1 shell-over-core pattern (load storage → core → persist → drain events). **`pallet-guardian`**: decomposed bounded storage per 06 §8; 5 origin-checked extrinsics (`set_members`/`ratify_action`/`renew_playbook` = `ConstitutionalValues`; `propose_action`/`approve_action` = Signed member); 02 §6 frozen event schema incl. `ReviewScheduled{action,referendum}` via an injected `ReviewScheduler` seam; cross-pallet reads via injected `ProposalStatusProvider`/`TriggerProvider` seams; bounded `on_initialize` crank (playbook expiry + overdue-review 50% slash + **terminal reaping**); genesis, mock, v2 `#[benchmarks]`×6, `WeightInfo`. **`pallet-attestor`** (authored by a Codex subagent mirroring guardian): storage per 06 §8 (`Members`/`Attestations` + `attestations_for`/`open_challenges` projections); calls `set_members`(ConstitutionalValues)/`attest`/`challenge_attestation`(Signed)/`resolve_challenge`(`RatifyOrigin`); 2-of-N quorum view for the guard (09 §1.2); `att.min_members`/`att.quorum` from `primitives::kernel` (single-home). `DecodeWithMemTracking` added to both cores' call/event SCALE types; `T::AccountId↔[u8;32]` bridge via a supertrait assoc-type bound. **Review (R-6): two `spec-reviewer` passes (guardian 0 blocker/5 major/6 minor; attestor 0 blocker/1 major/4 minor) + a Codex adversarial pass (0 blocker / 5 findings).** All fixed-in-scope items landed in-session with tests: guardian **reaping** of dispatched/expired actions + terminal reviews (the 64/128 caps were lifetime → concurrent), **clear pending+approvals on re-election** (closes the stale-approval 5-of-7 bypass), **`RecallScheduler` seam** (06 §5.4 recall referendum), **I-23 try_state** (every dispatched action reviewed), `saturating_add` counters; attestor **try_state recall relax** (historical attestations from recalled members are lawful) + **`NoOpenChallenge`** error. From the Codex pass: attestor **upheld quorum now waits for the 72h window** (06 §7 "windows have closed"), guardian **playbook singletons** (all ids deduped; re-activation renews in place, so duplicate `Depeg`s can't exhaust the six slots and block PB-LEDGER-FREEZE) + a try_state uniqueness check, and a **round-trip account-bridge check** at every Signed authorization (rejects a lossy `AccountId` aliasing → impersonation; the runtime pins `AccountId32`, 02 §8). The remaining bond findings — guardian liability across council rotation and real attestor bond custody across re-election (06 §5.1 "held for the full term plus one epoch") — belong to the deferred per-account `fungible` bond system (B-track), documented in code + Decision log. Gates green: fmt · `clippy --workspace -D warnings` · `cargo test --workspace` (54 suites; guardian 35 unit + core 11 + 41 with benchmarks; attestor 17 + core 5) · `runtime-benchmarks` + `try-runtime` feature builds. **Deferred (B-track, like A1's PoV weights → B5 / origin wiring → B1a):** real `fungible` bond holds/slash — **including cross-rotation/re-election bond liability** (06 §5.1 "held for the full term plus one epoch": the current arithmetic placeholders move no funds, so a recalled guardian approver can evade retrospective slashing; PR #130/SQ-262 separately fixed attestor `false_count` persistence across re-election, while custody remains absent); the guardian effect-dispatch + referenda submission (the injected seams) wired at B1a; per-playbook expiry beyond the safe uniform 14-day cap (SQ-45); K/META/entrenched single-home relocation (M1 + Params reads). **SQ-45/46 raised** (guardian per-playbook expiry; attestor `resolve_challenge`/event-naming freeze). See Decision log + Session log 2026-07-15 | -->

<!-- source track narrative -->

**Re-scoped 2026-07-14 (user-directed; see Decision log): Track A delivers production
Polkadot-SDK FRAME pallets, not frame-free models. A1–A11 are reopened ⬜.** Their logic
already exists as **frame-free functional-core models** (state struct + `fn op(&mut self)
-> Result` + typed Event/Error + `try_state`); that code is **kept as the functional
core**, relocated to `crates/<name>-core/` (`no_std`, no `frame` deps — rule 9 / 01 §5.2),
and preserved as the differential oracle (Python M3 ≡ Rust core ≡ FRAME runtime) and the
WASM-portable / auditor-consumable port. A milestone is ✅ only once its pallet ships in
full FRAME production style — the **necessary parts** per pallet:

- **`crates/<name>-core/`** — the relocated frame-free model (functional core; today's
  `pallets/<name>` logic, unchanged in behavior).
- **`pallets/<name>/src/lib.rs`** — a `#[frame_support::pallet]` shell: a `Config` trait
  (`RuntimeEvent`, `WeightInfo`, constants via `Get<…>`, `Params`/currency/asset + origin
  bounds); `#[pallet::storage]` items as `BoundedVec`/`BoundedBTreeMap` (`MaxEncodedLen`,
  bounds from 13 §4, names + SCALE shapes matching 02 byte-for-byte — rule 5);
  `#[pallet::event]` / `#[pallet::error]` mapped 1:1 from the core; `#[pallet::call]`
  extrinsics that enforce an explicit origin (`EnsureOrigin` over `pallet-origins`, one
  SafetyFilter authority-matrix row per call — 06 §3) then load storage → call the core →
  persist → deposit events, each carrying `#[pallet::weight(T::WeightInfo::…)]`;
  `#[pallet::hooks]` with the mandatory `try_state` (15 §1) and only the cursor-bounded
  cranks the spec allows (none where it says none — the ledger has no hooks, 03 §10);
  `#[pallet::genesis_config]` / `genesis_build` where the pallet has genesis state.
- **`pallets/<name>/src/mock.rs`** — a mock runtime (`construct_runtime!`: `System` + the
  pallet + its dependencies, with test `Config` impls).
- **`pallets/<name>/src/tests.rs`** — per-extrinsic × error-path × origin-misuse tests
  through the closed wrapper set, limit/boundary coverage, and a `try_state` assertion
  (15 §4.1, §1). Audit-scope-A pallets (A1, A2, A11 — R-7) add the adversarial /
  rounding-against-the-claimant / solvency suites.
- **`pallets/<name>/src/benchmarking.rs`** + **`weights.rs`** — `#[benchmarks]` for every
  extrinsic and hook, a `WeightInfo` trait, a generated `weights.rs`, and the
  `runtime-benchmarks` feature. PoV-calibrated weights land with B5; the harness + stubs
  are part of the milestone.
- **`Cargo.toml`** — `frame-support` / `frame-system` / `frame-benchmarking` on the
  `polkadot-stable2603` pins (01 §9), the `std` / `runtime-benchmarks` / `try-runtime`
  features, and the `<name>-core` dependency.

Scaffold via `/new-pallet` (already emits this shape); `test-engineer` authors the 15 §4
suites; a `spec-reviewer` pass with no blockers precedes ✅ (R-6). Per-pallet FRAME wrapping
lives here; runtime-level `construct_runtime!` / `impl_runtime_apis!` assembly is B1a/B2.

<!-- end source track narrative -->
