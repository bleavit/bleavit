---
id: S5
track: S
title: Wrapper/filter negative suites + filter-exhaustiveness test over `RuntimeCall`
spec: [06 §3, 15 §1 (I-8/I-10/I-11)]
depends: [B1]
status: done
---

**Done 2026-07-17 (worktree `milestone-S5`).** Tests-only layer in `runtime/bleavit-runtime` (`tests_s5.rs` inventory/carriers + `tests_s5_behavior.rs` behavior): hand-pinned 256-row `(pallet, call)` metadata inventory with bidirectional new/stale tripwires (06 §3.3 CI mandate); carrier detection over `TypeDef` fields **and** generic `type_params` plus name-based opaque-carrier detection (`DoubleEncoded`/`VersionedXcm`) — 21 carriers = 19 typed wrapper rows + 2 pinned semantic carriers (`referenda.submit` Public-by-design per 06 §3.4 re-entry; `pallet_xcm.execute` nobody), `approve_as_multi` hash-only negative control incl. a real-dispatch smuggling proof; ACTIVE full origin-aware authority matrix (every privileged/conditional leaf × all 8 class origins, compile-time-exhaustive origin universe; same-origin-only recursion through utility batch/`with_weight`/sudo per 06 §3.3, categorical proxy/multisig privileged-inner denial, outright denial for `dispatch_as`/`as_derivative`/`if_else`/`dispatch_as_fallible`/scheduler/`sudo_as`); `WrapperShape`-keyed constructor closure asserted against the inventory + generated pairwise/three-level/multi-arm compositions; kernel-exact nesting/count boundaries; real-dispatch `CallFiltered` negatives incl. genesis-sudo; values-enactment set equality derived from the inventory with no exceptions; full-inventory I-8 disjointness. The suite has since grown to **23 active tests / 0 ignored**: SQ-150 and SQ-151 are resolved, their ignored counterexamples were replaced by active origin/dispatch/classifier/static-screening regressions, and kernel-bounded registry amendments now fail at T4. Codex-orchestrated author → dual review (spec-reviewer 1/2/4 + adversarial 2/4/1) → remediation (all findings fixed/adjudicated; SQ-152's wrong-criterion ignored test deleted per both reviewers' spec reading) → re-review 0/0/0. SQ-150…SQ-154; gates green at milestone close and maintained by the workspace gate.

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track S — Systemic verification and simulation | -->

<!-- source row: | S5 | Wrapper/filter negative suites + filter-exhaustiveness test over `RuntimeCall` | 06 §3; 15 §1 (I-8/I-10/I-11) | B1 | ✅ | **Done 2026-07-17 (worktree `milestone-S5`).** Tests-only layer in `runtime/bleavit-runtime` (`tests_s5.rs` inventory/carriers + `tests_s5_behavior.rs` behavior): hand-pinned 256-row `(pallet, call)` metadata inventory with bidirectional new/stale tripwires (06 §3.3 CI mandate); carrier detection over `TypeDef` fields **and** generic `type_params` plus name-based opaque-carrier detection (`DoubleEncoded`/`VersionedXcm`) — 21 carriers = 19 typed wrapper rows + 2 pinned semantic carriers (`referenda.submit` Public-by-design per 06 §3.4 re-entry; `pallet_xcm.execute` nobody), `approve_as_multi` hash-only negative control incl. a real-dispatch smuggling proof; ACTIVE full origin-aware authority matrix (every privileged/conditional leaf × all 8 class origins, compile-time-exhaustive origin universe; same-origin-only recursion through utility batch/`with_weight`/sudo per 06 §3.3, categorical proxy/multisig privileged-inner denial, outright denial for `dispatch_as`/`as_derivative`/`if_else`/`dispatch_as_fallible`/scheduler/`sudo_as`); `WrapperShape`-keyed constructor closure asserted against the inventory + generated pairwise/three-level/multi-arm compositions; kernel-exact nesting/count boundaries; real-dispatch `CallFiltered` negatives incl. genesis-sudo; values-enactment set equality derived from the inventory with no exceptions; full-inventory I-8 disjointness. The suite has since grown to **23 active tests / 0 ignored**: SQ-150 and SQ-151 are resolved, their ignored counterexamples were replaced by active origin/dispatch/classifier/static-screening regressions, and kernel-bounded registry amendments now fail at T4. Codex-orchestrated author → dual review (spec-reviewer 1/2/4 + adversarial 2/4/1) → remediation (all findings fixed/adjudicated; SQ-152's wrong-criterion ignored test deleted per both reviewers' spec reading) → re-review 0/0/0. SQ-150…SQ-154; gates green at milestone close and maintained by the workspace gate. | -->
