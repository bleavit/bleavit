---
id: N7
track: N
title: `pallet-question-service` (index 66) + `ServiceLedger` (67) — questions, markets, instrument D, settlement, `hosted_report`, try-state and benchmarks
spec:
  - 16
  - 08 §10
depends:
  - N4
  - N6
status: done
---

Partition implemented 2026-08-02 under explicit authorization of the `pallet-welfare` scope; **flipped back from ✅ to 🔨 by the mandated adversarial review (plan Risk 1), which found four defects that every green gate missed.** Built: the two-dimensional external quota, `PrimaryUsed`, the primary-only P seam, runtime-owned metric provenance, and a `TransactionExtension` that reserves before dispatch so the quota refuses rather than merely accounts. **Second repair round:** `H` stays in physical `max_block` coordinates and calls the original integer formula with the original kernel target; a stride-1,000,000 sweep plus ±2 boundary neighborhoods is asserted bit-identical at zero external usage. Total and primary utilization both use the shared `pallet_welfare::block_utilization` max-of-normalized-dimensions helper, with a proof-size-bound regression. Operational/Mandatory bypass only this partition's refusal and remain measured residual primary work; their unchecked Mandatory accounting is saturating/clamped and documented as an upper-bound estimate. The replay was named `n7_service_traffic_containment_sample`, not PT-10 (superseded 2026-08-03 — see the closure note at the end of this row): it registers inside the measured replay, compares every captured snapshot/decision input/outcome as SCALE bytes, routes observe through the real dispatcher, and interleaves service traffic at every primary boundary over two finite schedules. PLAN records that the unrestricted arbitrary service alphabet/interleaving space required by normative PT-10 remains uncovered. **Normal.max_total = 100%** is intentionally not a FRAME-level 75/25 guarantee: the extension/XCM dispatcher enforce the split for traversing external dispatches, while scheduler, governance-authorized ExecutionGuard payloads and five-of-seven guardian playbooks are authority-gated residual paths; the local raw-dispatch inventory tripwire and exact scheduler-origin binding guard against silent growth. **Closed 2026-08-03 after the compliance re-review it was held open for.** An independent read-only Codex pass (`gpt-5.6-luna`, `max`) against doc 16 returned two blockers and one major gap, all three now discharged — the detail is in the Decision log, and the pattern is that every one of them sat behind green gates. **(a)** External trading fees could not reach `MAIN` at all: `ServiceProtocolAccounts` lacked the `MAIN` carve-out its primary counterpart documents at length, so 16 §7.4's revenue leg failed `TryStateViolation` on every hosted book. Hidden by a mock more permissive than production, and by a runtime assertion that stated the bug as a requirement. **(b)** Terminal recovery armed Phase 4 past 16 §8.4's external bound, because the SQ-575 check had been placed inside the branch that lets recovery skip the *treasury* floor; the bound now sits outside it and the recovery lane **pauses the hosted service** rather than refusing, since refusing under `OnlyInherents` would recreate the brick that exemption exists to avoid. **(c) PT-10 is now implemented rather than sampled.** The interleaving is generated over a six-letter service alphabet — successful XCM-dispatcher work, both failure proof sizes, the signed `TransactionExtension` path, and idle — at all ten schedulable points of all 22 blocks, so the failure and signed paths land at generated points instead of hard-coded ones; the two original hand-written patterns are kept as seed-independent regressions, and a companion assertion requires the swept seeds to reach every letter so narrowing the generator cannot silently narrow the property. It is a runtime **replay** property, so it does not read `PROPTEST_CASES` (a case is two full 22-block replays); it carries `BLEAVIT_PT10_CASES`, small in `cargo test --workspace` and deep in a new `containment` shard of `property-gates.sh` + the CI matrix — the same reduced/deep split PT-1…PT-8 use. The test is renamed `pt10_external_outcome_containment`, which 15 §4.3 permits only now that the property is unscoped-in-interleaving. **Still open and NOT part of this milestone:** `svc.max_live`'s sizing evidence stays `[VERIFY]` under 16 §8.5 — a values-layer obligation with no derivable anchor, which R-2 admits as a legitimate state, not unfinished implementation
