---
id: B6
track: B
title: Upgrade path e2e — `authorize_upgrade`/`apply_authorized_upgrade`, attestation precondition, `DescriptorLeadTime`, `ReleaseChannel` write wiring, `pallet-migrations`, try-runtime CI
spec:
  - 09 §2
  - 02 §12
depends:
  - B1a
  - A11
status: done
---

**Done 2026-07-17 (worktree `milestone-B6`).** `ExecutionGuard: pallet_execution_guard = 62` wired into the real runtime with every Config seam: real `BatchDispatcher` (class origins + dispatch-time SafetyFilter re-check; internal Root constructed inside `dispatch_authorize_upgrade` so it can only ever reach the one committed `system.authorize_upgrade(hash)` — I-10; apply dispatched non-Root through the filtered path), `pallet-attestor` live 2-of-N quorum (queue-time §1.1(3) + dispatch-time §1.2(5)), live constitution capability table (§1.2(6), per-class keyed mapping; unmapped privileged leaves fail closed structurally), guardian state, Params, Preimages, ConstitutionalValues ratify, `ReleaseChannel` writer (a) over `constitution-core`-single-homed 02 §12 offsets (`updated_at` bumped — SQ-133), fail-closed A8 pending `EnqueueAuthority`/`EpochHandoff` (epoch slot 61 stays reserved). Classifier: `PendingUpgradeProvider` reads real `PendingUpgrade` (09 §2.2 lead-time gate; artifact-hash/version/`can_set_code`/five-Cumulus-precondition preflight so a doomed apply can't consume the frame-system authorization — V-25); exhaustive `ExecutionGuard` arms per the amended 06 §3.2. **A11 additive changes:** `validation_code_applied` (pending/release state clears only at actual Cumulus code application, not scheduling), `ScheduledUpgrade` latch + relay-**Abort** status-quo cleanup (`UpgradeAborted`, tolerant writer-(a) clear that survives a writer-(b) rewrite — SQ-131/SQ-134). Production `pallet-migrations` config (13-homed bounds) + PB-MIGRATION bridge: failed-step handler, `>900`-block stall detector (`MIGRATION_STALL_BLOCKS`, weight-accounted), source-scoped `MigrationHalt` (migration vs applied-detection vs abort bits; completion/recovery-image clears only its own sources; halted-cursor retirement scoped to `Stuck`/live-stall). try-runtime CI legs (compile + genesis `TryRuntime_on_runtime_upgrade` + try-state; snapshot `try-runtime-cli` → B7/B8). E2e tests drive the REAL `ParachainSystem::set_validation_data` inherent (sproof-built `GoAhead`/`Abort`); mandated negatives landed (early apply ±1-block boundary, no-pending, wrong artifact, unattested + live-quorum-loss CODE noop, wrapper backdoors, SQ-104 migration-admin denials). **Orchestrated with Codex subagents (gpt-5.6-sol/xhigh):** author → dual review (spec-reviewer **0/1/6** + adversarial **2 blockers/6 majors/2 minors**) → remediation → focused re-pass (2/7/6, mostly deferral-classification) → remediation 2 → final verify (0 blockers/2 majors/1 minor — **fixed in-session by hand** with the writer-b abort regression). Gates green (workspace · runtime 57 + 59 w/ try-runtime · release+benchmarks wasm · no_std · reference-model 21 · doc links). SQ-131…SQ-134 logged; SQ-104 extended (recovery-lane reachability); V-25; two R-1 amendments (06 §3.2 rows, 13 migration rows). Deferred with owners: PB-MIGRATION trigger arm 3 + halt-lift try-state coupling (SQ-132), snapshot try-runtime (B7/B8), real-MBM failure e2e (B7), origin bridge (SQ-104).
