---
id: B16
track: B
title: **Migration stall predicate, MBM lockdown and the inherent-only recovery lane** — derive stall and retirement from `cursor.started_at` (deleting `MigrationProgressMarker` and the cursor hash), require every registered MBM to declare `max_steps() = Some(n)` with `n < MIGRATION_STALL_BLOCKS` plus a checked aggregate run budget, emit the `MigrationHalted` diagnostic, and build the pre-authorized recovery image applied by deterministic inherent machinery — because a stuck cursor makes the chain permanently inherent-only and forecloses 09 §3.2(3)'s own designated recovery lane
spec: [09 §3.1–§3.2]
depends: [B10]
status: done
---

**Done 2026-07-23; PR #140 merged as `2aae113`.** Mandatory paired primary/recovery profiles, bounded Operational qualification/pinning, inherent-only scheduling, exact cursor retirement/Abort restoration, installed-code proof, one-shot Phase-3→4 transition, keeper producer, contract-v9 recovery metadata binding, generated weights and full profile/release gates shipped. Both valid Codex review threads were fixed, answered and resolved; independent final review was 0/0/0 and every GitHub check passed. B15 now consumes the generic transport for the first release-specific MBM repair.

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track B — Runtime, node and chain | -->

<!-- source row: | B16 | **Migration stall predicate, MBM lockdown and the inherent-only recovery lane** — derive stall and retirement from `cursor.started_at` (deleting `MigrationProgressMarker` and the cursor hash), require every registered MBM to declare `max_steps() = Some(n)` with `n < MIGRATION_STALL_BLOCKS` plus a checked aggregate run budget, emit the `MigrationHalted` diagnostic, and build the pre-authorized recovery image applied by deterministic inherent machinery — because a stuck cursor makes the chain permanently inherent-only and forecloses 09 §3.2(3)'s own designated recovery lane | 09 §3.1–§3.2 | B10 | ✅ | **Done 2026-07-23; PR #140 merged as `2aae113`.** Mandatory paired primary/recovery profiles, bounded Operational qualification/pinning, inherent-only scheduling, exact cursor retirement/Abort restoration, installed-code proof, one-shot Phase-3→4 transition, keeper producer, contract-v9 recovery metadata binding, generated weights and full profile/release gates shipped. Both valid Codex review threads were fixed, answered and resolved; independent final review was 0/0/0 and every GitHub check passed. B15 now consumes the generic transport for the first release-specific MBM repair. | -->

<!-- source track narrative -->

Doc-09 WBS delta rows map here and to Track A: E15→B2/B8 · E16→A2 · E17→A3 · E18→A6 ·
E19→A5 · E20→A10/B6 · E21→B4 · E22→A11/B1. (E1–E14 definitions live in git history —
superseded `BACKEND_PLAN.md` §26; their scope is covered by Tracks M/A/B.)

<!-- end source track narrative -->
