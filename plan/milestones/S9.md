---
id: S9
track: S
title: Welfare-pillar reachability, rollout gates, and the execute-checklist diff both documents mandate
spec:
  - 05 §4.3, §4.5, §4.7, §5.1, §5.4–§5.5
  - 09 §1.2, §2.2, §3.1, §5.2, §7.1–§7.2
  - 11 §11.5
  - 08 §1.2, §4.1
  - 06 §6.3
depends:
  - S6
status: done
---

**Done 2026-08-01.** `pillars.py` (836, 36 tests) + `rollout.py` (1,009, 39) + `lifecycle.py` checklist extension (+569, +8). **Reproduced:** §4.5's headline pair exactly (`D_eff` 0.914285714, `g(S)` 0.084274776) and its concentrated example; the C-pillar component-at-zero values; 09 §1.2 parsing to exactly 13 items and 11 §11.5 to 14 rows with nine one-to-one mappings; T16's three published causes; §5.5's 21 rows proving total, deterministic and non-overlapping over all 16 Boolean inputs. **Falsified:** SQ-544 and SQ-545 (above); §4.5's unqualified "an equal five-collator set scores 1.0" holds only in phases 0–3 — Phase 4 gives 0.960, Phase 5 0.933, Phase 6+ 0.914, and four equal authors collapse `g(S)` to zero in phases 4–7, so the unsafe direction is phase advancement without matching set growth; the checklist bijection fails on five rows, and FE row 14 is an `apply_authorized_upgrade` precondition (09 §2.2) that could wrongly block a valid `execute`; 09 §1.2(11)'s `Rejected(BadPreimage)` names a state that **cannot be constructed** — `BadPreimage` is a pallet error, not a frozen `RejectReason` — so the proposal stays `Queued` and reaches T15 `Expired`; and §5.5's `Valid fail` row publishes `HurdleNotMet` where normative §5.4 returns `ConvergenceFailed`. **The brief was corrected twice:** its "six checks have no disposition" is false (T15 covers them and refunds), and "three wrong truth-table rows" is one. The launch-inertness result is recorded **conditionally** (SQ-555): 08 §10's "launch volume is zero" is about trading volume, and `U` counts any non-inherent extrinsic — under an explicit persistent-zero scenario the claimed ordering does hold, `GateVetoSurvival` firing before the sanity band
