---
id: N12
track: N
title: **Economics and values** — 08 §10 instrument D, the `svc.*` registry rows with their R-2 derivations and limit-coverage tests, S4 re-run with a competing-venue term
spec:
  - 08 §10
  - 13 §1
  - 15 §4.9
depends:
  - N7
  - N9
status: done
---

Three of four parts landed with earlier milestones and are verified here, not re-done: 08 §10's instrument-D paragraph (N1, corrected 2026-08-01/02), the five `svc.*` registry rows, and their limit-coverage binding (**0 unwired, 0 consumer-binding** over 205 keys). N12 adds the two that were still prose: **`reference-model/.../service_economics.py`** — the executable form of 16 §5.2/§8.1/§8.2 + 13 §1's fee-floor derivation, pinning `b_min` = 14.2368·S, escrow 19.736·S, instrument D at 77 % of evidenced per-question revenue and the 3,930-USDC floor crossover, and asserting the **superseded** displacement form and per-*market* tariff reading do NOT reproduce them — and the **competing-venue term** in the S4 simulation. The term's arming rung is *anchored* to 16 §8.4's arming condition (`Σ b_ext ≤ Σ pol.b(live)` ⇒ ½ **under proportional-to-depth allocation**, a behavioural model whose error direction is unsafe, so the ladder must probe above it — 0.25/0.50/0.75); it thins organic formation on decision/gate/Baseline books and leaves attacker budgets untouched. **Measured finding, and it redirects the milestone's own premise:** diversion's first-order effect is *denial*, not corruption — books fall below `dec.v_min` and reject `NotDecisionGrade` rather than flipping — so a false-pass-only criterion reads *cleaner* the more governance is destroyed. The arming criterion therefore has a gated security half and a measured-with-falsifier liveness half (15 §4.9, 16 §8.4). Zero diversion is a **strict** no-op (asserted on executed evidence, not on `x·1 == x`), so the published Phase-0 result is untouched
