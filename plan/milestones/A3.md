---
id: A3
track: A
title: `pallet-market` — LMSR books (branch-USDC, D-3), trade wrapper, fees, TWAP, POL seeding, `BaselineMarketOf`
spec:
  - 04
  - 02 §5–§7.4
depends:
  - A2
  - M2
  - M3
status: done
---

I-12, I-13. **Done 2026-07-15** — production `#[frame_support::pallet]` over `market-core`. Storage `Markets` (`CountedStorageMap`, 196-cap) + `BaselineMarketOf` + `ClosedAt`/`SeededMarkets` (02 §7.4 names frozen); frozen 02 §5 events; `buy`/`sell`/`crank_observe`/`reap` Signed (06 §3.2) + internal `create_market`/`seed`/`close` (`MarketAdmin`); `LedgerOps` shim over the real ledger `do_*` API (market = `MarketAuthority`; atomic via `with_storage_layer`, no core snapshot needed); live tunables via Config Gets (rule 4); try-state = I-12 structural collateralization (book↔ledger-vault) + I-13 accumulator sanity. Core refactor: `LedgerOps` trait + `MarketParams` + pub `buy_book`/`sell_book`/`seed_book`/`observe_book`, behavior-identical at defaults (parity 10/10, shell≡core differential green). 26 pallet tests (per-extrinsic/error/origin-misuse, V1/V2/V4 vectors, R-7 atomic-rollback, close→reap lifecycle, multi-book-vault + Baseline-sell regressions) + `#[benchmarks]` (buy/sell/crank/reap) + weights. **Dual review (Codex adversarial + spec-reviewer) + test-engineer, all triaged**: 2 blockers fixed w/ regressions (per-book vault creation; Baseline sell charging the seller), majors fixed (crank phase-guard, 196-cap at dispatch, seed idempotency, try-state I-12/I-13, `STALE_GAP`→kernel). Deferred: **SQ-64** (`MarketCreated.epoch`/spec → A8/B1a), **SQ-65** (POL split count 04 §10 vs 13 §5.5 — code matches 13), **SQ-66** (reap anchor + `BaselineMarketOf` prune 04 §8.3 vs 02 §7.4 → A8), **SQ-67** (`Markets` value type name); TWAP checkpoint ring/window staleness → A8; deterministic book/fee accounts + `Params` binding → B1a. PT-style I-12 loss-bound property suite + ledger↔Python differential remain **S1**
