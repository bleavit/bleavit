---
id: B7
track: B
title: Zombienet + Chopsticks environment definitions (release artifacts, not private fixtures)
spec: [15 §4.7, 02 §11]
depends: [B3]
status: done
---

**Done 2026-07-17** (worktree `milestone-B7`). `zombienet/`: two native-provider topologies (`bleavit-local` — 4 relay validators + 3 `bleavit-node` collators on para 4242; `bleavit-xcm` — + Asset Hub 1000 and Coretime 1005 with both HRMP directions) over generated `paseo-local` relay specs; the 02 §11 genesis-overrides patch (`constitution.phaseFlags` only — **no 13-owned tunable is compressed**, dead-man 4,800 stays kernel); nine `.zndsl` drills + 9 js helpers covering the full 15 §4.7/09 §7.1 inventory (smoke · collator loss with pre/post height deltas · keeper loss (loud B9 gate) · dead-man measured as a real ≥4,800-relay-block finality stall via RPC, never wall-clock · coretime-renewal-under-dead-man (09 §4 exemption) · PB-MIGRATION retry+rollback through the real guardian 5-of-7 propose/approve workflow · XCM v5 reserve-transfer failure/trap/recovery on the spec-legal AH-keyed route (SQ-129) · expedited-CODE-under-freeze with a negative early-apply and the full 43,200-block D-14 wait · the 907,200-block 3-epoch G1 soak). `chopsticks/`: base config + 10 forked-state scenarios (upgrade transition, StaleQueue via `reject_stale` with a genuinely stale constraint, VOID epoch incl. the I-1-consistent `redeem_void` fixture, manufactured precondition failures, and **all six 06 §6.2 playbooks** incl. PB-LEDGER-FREEZE entry/expiry + coretime exemption), every injected cell recomputed against the real pallets — the focused re-pass **byte-verified 123/123 keys+SCALE values** (guard images satisfy A11's try-state exactly; guardian images carry the full membership/bond/review/allowance set). `tools/env/`: six-pin provenance home (`pins.env`: zombienet v1.3.133 **+ in-repo binary sha256** (no upstream sidecars, V-21) · chopsticks 1.5.0 (V-22) · polkadot-stable2603-1 · paseo CSG **v1.9.2 pinned by tag+commit** — the generator was dropped from the v2.x line, V-23); sidecar/pin-verified fetch script (linux-x86_64 only, mismatch-rejecting — functionally tested); relay-spec generator hard-failing on commit drift and on chain-spec-builder 17.0.0's zero-exit-on-error; structural validator (normative drill/scenario/playbook inventory, para ids, HRMP directions, js-path resolution matching zombienet's dirname(testFile) rule, localhost-only endpoints, single-homed pins) + 10 unit tests + the `environments` CI job. **Process:** Codex author (gpt-5.6-sol/xhigh) → dual review (spec-reviewer 1 blocker/6 major/5 minor + adversarial Codex **6 blockers/11 majors**) → one Codex remediation round (crashed on a moderation false-positive after applying its edits; orchestrator finished the last four findings directly) → focused re-pass: **0 blockers/0 majors**, its 4 residuals (in-repo zombienet digest, stall-timeout arithmetic at half-authority cadence, two README numbers, V-row renumber) fixed + re-gated in-session. V-21…V-24; SQ-127…SQ-130. Drills gated on unwired surfaces stay complete definitions with loud failure modes + README runnability matrices (A8/A11 wiring, B6, B9). **Follow-ups:** B8 executes the suites against published artifacts (15 §1 try-state-in-env-CI + 15 §5 content-addressing land there); G1 runs the Phase-1 drills; B3-script nit (no delete-first guard in `tools/deploy/generate-chain-specs.sh`, harmless — outputs fail loudly downstream). **Evidence producer (SQ-139 producer side) ✅ 2026-07-17 (worktree `B7-evidence-producer`):** `tools/env/run-evidence.py` executes the committed suites against built artifacts (per-drill `zombienet -p native test` with full process-group lifecycle incl. BaseException teardown; per-scenario Chopsticks boot + byte-exact injected-state verification + two produced blocks + live `:code`↔release-wasm binding before/after; wasm↔drill-chain-spec genesis binding re-verified post-suite; pinned-Zombienet digest check; free-port probe + per-suite db purge) and emits both `bleavit.env-evidence.v1` files — committed-definitions inventory, clean-tree (`zombienet`/`chopsticks` incl. ignored + `tools/env` porcelain) and HEAD==commit gates, manifest/pins digests embedded, self-checked through the real `validate_run_evidence` consumer, all-or-nothing with prior-evidence invalidation on every start. `tools/env/suites.json` (20 rows; release/g1 tiers **pinned in the structural validator**; explicit `gated_on` — 03 B9-topology, 07 SQ-101/B4, 04/05/09 A8, 08 G1 staging) + config hardening in `validate-environments.py` (tier map, active `wasm-override`/`:code`/`:heappages`/db-placement rejection). `release.yml` re-executes the release tier at tag time (env gates now release-blocking in the gates job; npx pre-warm; assembly remains the single enforcement point; evidence logs uploaded `if: always()`); ci.yml environments job extended. **Evidence is mechanically unproducible today — the intended fail-closed state** (zombienet: 03/07 gated + the SQ-204 try-state block; chopsticks: all 10 scenarios gated on SQ-203 card depth + the same try-state block; the connector-review P1 round converted both former documentation-deferrals into code-enforced gates). SQ-202…SQ-204.

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track B — Runtime, node and chain | -->

<!-- source row: | B7 | Zombienet + Chopsticks environment definitions (release artifacts, not private fixtures) | 15 §4.7; 02 §11 | B3 | ✅ | **Done 2026-07-17** (worktree `milestone-B7`). `zombienet/`: two native-provider topologies (`bleavit-local` — 4 relay validators + 3 `bleavit-node` collators on para 4242; `bleavit-xcm` — + Asset Hub 1000 and Coretime 1005 with both HRMP directions) over generated `paseo-local` relay specs; the 02 §11 genesis-overrides patch (`constitution.phaseFlags` only — **no 13-owned tunable is compressed**, dead-man 4,800 stays kernel); nine `.zndsl` drills + 9 js helpers covering the full 15 §4.7/09 §7.1 inventory (smoke · collator loss with pre/post height deltas · keeper loss (loud B9 gate) · dead-man measured as a real ≥4,800-relay-block finality stall via RPC, never wall-clock · coretime-renewal-under-dead-man (09 §4 exemption) · PB-MIGRATION retry+rollback through the real guardian 5-of-7 propose/approve workflow · XCM v5 reserve-transfer failure/trap/recovery on the spec-legal AH-keyed route (SQ-129) · expedited-CODE-under-freeze with a negative early-apply and the full 43,200-block D-14 wait · the 907,200-block 3-epoch G1 soak). `chopsticks/`: base config + 10 forked-state scenarios (upgrade transition, StaleQueue via `reject_stale` with a genuinely stale constraint, VOID epoch incl. the I-1-consistent `redeem_void` fixture, manufactured precondition failures, and **all six 06 §6.2 playbooks** incl. PB-LEDGER-FREEZE entry/expiry + coretime exemption), every injected cell recomputed against the real pallets — the focused re-pass **byte-verified 123/123 keys+SCALE values** (guard images satisfy A11's try-state exactly; guardian images carry the full membership/bond/review/allowance set). `tools/env/`: six-pin provenance home (`pins.env`: zombienet v1.3.133 **+ in-repo binary sha256** (no upstream sidecars, V-21) · chopsticks 1.5.0 (V-22) · polkadot-stable2603-1 · paseo CSG **v1.9.2 pinned by tag+commit** — the generator was dropped from the v2.x line, V-23); sidecar/pin-verified fetch script (linux-x86_64 only, mismatch-rejecting — functionally tested); relay-spec generator hard-failing on commit drift and on chain-spec-builder 17.0.0's zero-exit-on-error; structural validator (normative drill/scenario/playbook inventory, para ids, HRMP directions, js-path resolution matching zombienet's dirname(testFile) rule, localhost-only endpoints, single-homed pins) + 10 unit tests + the `environments` CI job. **Process:** Codex author (gpt-5.6-sol/xhigh) → dual review (spec-reviewer 1 blocker/6 major/5 minor + adversarial Codex **6 blockers/11 majors**) → one Codex remediation round (crashed on a moderation false-positive after applying its edits; orchestrator finished the last four findings directly) → focused re-pass: **0 blockers/0 majors**, its 4 residuals (in-repo zombienet digest, stall-timeout arithmetic at half-authority cadence, two README numbers, V-row renumber) fixed + re-gated in-session. V-21…V-24; SQ-127…SQ-130. Drills gated on unwired surfaces stay complete definitions with loud failure modes + README runnability matrices (A8/A11 wiring, B6, B9). **Follow-ups:** B8 executes the suites against published artifacts (15 §1 try-state-in-env-CI + 15 §5 content-addressing land there); G1 runs the Phase-1 drills; B3-script nit (no delete-first guard in `tools/deploy/generate-chain-specs.sh`, harmless — outputs fail loudly downstream). **Evidence producer (SQ-139 producer side) ✅ 2026-07-17 (worktree `B7-evidence-producer`):** `tools/env/run-evidence.py` executes the committed suites against built artifacts (per-drill `zombienet -p native test` with full process-group lifecycle incl. BaseException teardown; per-scenario Chopsticks boot + byte-exact injected-state verification + two produced blocks + live `:code`↔release-wasm binding before/after; wasm↔drill-chain-spec genesis binding re-verified post-suite; pinned-Zombienet digest check; free-port probe + per-suite db purge) and emits both `bleavit.env-evidence.v1` files — committed-definitions inventory, clean-tree (`zombienet`/`chopsticks` incl. ignored + `tools/env` porcelain) and HEAD==commit gates, manifest/pins digests embedded, self-checked through the real `validate_run_evidence` consumer, all-or-nothing with prior-evidence invalidation on every start. `tools/env/suites.json` (20 rows; release/g1 tiers **pinned in the structural validator**; explicit `gated_on` — 03 B9-topology, 07 SQ-101/B4, 04/05/09 A8, 08 G1 staging) + config hardening in `validate-environments.py` (tier map, active `wasm-override`/`:code`/`:heappages`/db-placement rejection). `release.yml` re-executes the release tier at tag time (env gates now release-blocking in the gates job; npx pre-warm; assembly remains the single enforcement point; evidence logs uploaded `if: always()`); ci.yml environments job extended. **Evidence is mechanically unproducible today — the intended fail-closed state** (zombienet: 03/07 gated + the SQ-204 try-state block; chopsticks: all 10 scenarios gated on SQ-203 card depth + the same try-state block; the connector-review P1 round converted both former documentation-deferrals into code-enforced gates). SQ-202…SQ-204. | -->

<!-- source track narrative -->

Doc-09 WBS delta rows map here and to Track A: E15→B2/B8 · E16→A2 · E17→A3 · E18→A6 ·
E19→A5 · E20→A10/B6 · E21→B4 · E22→A11/B1. (E1–E14 definitions live in git history —
superseded `BACKEND_PLAN.md` §26; their scope is covered by Tracks M/A/B.)

<!-- end source track narrative -->
