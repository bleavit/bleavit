---
id: E6
track: E
title: "**Split the proposal author from the proposal funder** — `epoch.submit` enforces `proposal.proposer == who` (`pallets/epoch/src/lib.rs:1083`), so one address must both author the payload and lock the 1k–50k USDC class bond. Relax it to a two-identity record: the **submit signer is the funder** (the hold lands on the signer, so no account can be forced into a hold), `proposal.proposer` stays the **author** and may differ. Re-key the per-account intake cap to the funder"
spec: [06 §4 (rules 4–5 + new split paragraph), 05 §2.1 T-table, proposer-only rights, 08 §1.1, 02 §2/§4 + §13 (v17 → **v18**), 14 (sybil row), 15 §4.2]
depends: []
status: done
---

**Why this is worth a milestone: it widens the proposal funnel without touching any pricing mechanism.** Proposing today requires being *both* technically capable of authoring a payload *and* able to lock 1,000–50,000 USDC. The chain's whole output is ~87 decisions/year (08 §10.4) and every one of them is gated on finding a single party at that intersection. **The split is already half-representable in storage**: `ProposalBond { proposer, held }` is a record distinct from `Proposal.proposer`, and the refund already releases to `bond.proposer` (`pallets/epoch/src/lib.rs:3116`) — one identity is custodial, the other authorial, and only `submit`'s equality check forbids them differing. **Four incidence rulings (user, 2026-07-31):** `trs.proposer_reward` → **author**; pre-qualification withdrawal → **either** party; the 06 §4 10 % non-decision-grade slash → **funder**; intake cap → re-keyed to the **funder**. **The fourth is a defect closure, not a preference, and the change is a net regression without it:** `crates/epoch-core/src/lib.rs:776` counts the ≤4-entries/epoch cap on `p.proposer == proposal.proposer` — the *author*. That is harmless only because `submit` currently forces author == signer. Split the identities and leave the key alone, and one funder backs 16 throwaway author addresses at 4 proposals each to take the entire 64-entry `IntakeQueue`, voiding 06 §4 rule 4 and the B-13 monopolization defense outright. Capital at risk is what prices spam, so the cap follows the capital. **The reward ruling needs no new code and must be pinned rather than assumed:** `runtime/bleavit-runtime/src/configs.rs:8381` already pays `&proposal.proposer`, so keeping `proposer` authorial satisfies it by construction — which is exactly the kind of property that silently regresses without a test. **Author-only rights stay author-only:** `bind_ratification` (`pallets/epoch/src/lib.rs:1955`) is the ratification identity 09 §1.1(4) requires the *proposer* to freeze, and nothing about funding touches it. **Contract:** `Proposal` and `ProposalSummaryView` each gain a trailing `funder: AccountId` (02 §13 rule 3 append — no existing field's name, type or offset moves), `INTEGRATION_CONTRACT_VERSION` 17 → **18**; no call index moves so `transaction_version` is unchanged, and the chain is pre-genesis so no migration is required. **No new 13 key**: `intake.max_acct` keeps its value and bounds, only its *subject* changes, so this milestone carries no R-2 parameter derivation. Verify: the sybil regression first (N funders × M authors driven against the cap — it MUST fail on the pre-fix keying and pass after, or the defect closure is unevidenced); withdrawal admitted for author and for funder and refused for a third party; the 10 % slash lands on the funder with the author's balance untouched; the reward lands on the author; `bind_ratification` refuses the funder; try-state binding every `ProposalBond.proposer` to a live proposal's funder; `pallet_epoch` weights regenerated for `submit`'s widened record. **Done 2026-07-31 — PR #201.** All eight legs discharged; nine tests, every one split-identity. Two-sided mutation proof on the cap (reverting the predicate fails *both* cap tests: the evasion succeeds in one and distinct funders are wrongly capped in the other); one-sided on the reward (routing to `&proposal.funder` fails it). The reward leg needed a **PARAM** fixture — TREASURY/CODE pay `min(0.05 %·Ask, 25k)` = 0 at the `ask = 0` every existing execution fixture carries, and a payout of zero pins no recipient; PARAM's flat `trs.reward.param` is `ask`-independent. Execution settles both incidences at once, so that one fixture pins the whole split by exact equality. **Two defects the closing pass found:** the `runtime-benchmarks` build was red (both `Proposal` constructions in `configs.rs` sit behind that cfg, so `--all-targets` never compiled them — #201 as first pushed would have failed CI); and try-state did not bind bond custody to the funder, a state the split made representable while every other assertion still passes on it. **Weights regenerated and the drift was real, not cosmetic:** `worst_case.proof_size` +2,080 B on 11 functions = **65 × 32**, the `MaxEncodedLen` of one extra `AccountId32` propagating through the bounded proposal set. Regenerated at the committed 50×20 fidelity, not the 2×1 check fidelity — at 2×1 the component fit collapses (`settle_cohort` reported a 2.5 × 10¹⁹ proof size) and writing that would have destroyed a linear term the gate cannot see. **Five pallets, not one:** `pallet_attestor`, `pallet_execution_guard`, `pallet_guardian` and `pallet_market` read `Epoch::Proposals` in their benchmarks and each records its `max_size`, so all five moved 326 → 358 (51 annotations). `--changed` selects on *local pallet source* and none of those four had one — the driver was a `futarchy-primitives` type a dependency edge away — so the affected set has to be derived from the storage item, and the committed `max_size` annotations give it in one grep. **Corrected once more before landing:** two of those four (`pallet_guardian`, `pallet_market`) had no charged-dimension change at all, only a stale annotation, and regenerating them under a concurrently-running benchmark sweep manufactured +54 %/+65 % `ref_time` regressions on untouched code. Re-measured on an idle machine the whole set is clean — `check-weight-regression.py` reports **zero regressions** over 364 shared functions. That gate is not part of `rust-workspace-gates.sh` and has to be run deliberately

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track E — Protocol revenue and treasury sustainability | -->

<!-- source row: | E6 | **Split the proposal author from the proposal funder** — `epoch.submit` enforces `proposal.proposer == who` (`pallets/epoch/src/lib.rs:1083`), so one address must both author the payload and lock the 1k–50k USDC class bond. Relax it to a two-identity record: the **submit signer is the funder** (the hold lands on the signer, so no account can be forced into a hold), `proposal.proposer` stays the **author** and may differ. Re-key the per-account intake cap to the funder | 06 §4 (rules 4–5 + new split paragraph); 05 §2.1 T-table, proposer-only rights; 08 §1.1; 02 §2/§4 + §13 (v17 → **v18**); 14 (sybil row); 15 §4.2 | — | ✅ | **Why this is worth a milestone: it widens the proposal funnel without touching any pricing mechanism.** Proposing today requires being *both* technically capable of authoring a payload *and* able to lock 1,000–50,000 USDC. The chain's whole output is ~87 decisions/year (08 §10.4) and every one of them is gated on finding a single party at that intersection. **The split is already half-representable in storage**: `ProposalBond { proposer, held }` is a record distinct from `Proposal.proposer`, and the refund already releases to `bond.proposer` (`pallets/epoch/src/lib.rs:3116`) — one identity is custodial, the other authorial, and only `submit`'s equality check forbids them differing. **Four incidence rulings (user, 2026-07-31):** `trs.proposer_reward` → **author**; pre-qualification withdrawal → **either** party; the 06 §4 10 % non-decision-grade slash → **funder**; intake cap → re-keyed to the **funder**. **The fourth is a defect closure, not a preference, and the change is a net regression without it:** `crates/epoch-core/src/lib.rs:776` counts the ≤4-entries/epoch cap on `p.proposer == proposal.proposer` — the *author*. That is harmless only because `submit` currently forces author == signer. Split the identities and leave the key alone, and one funder backs 16 throwaway author addresses at 4 proposals each to take the entire 64-entry `IntakeQueue`, voiding 06 §4 rule 4 and the B-13 monopolization defense outright. Capital at risk is what prices spam, so the cap follows the capital. **The reward ruling needs no new code and must be pinned rather than assumed:** `runtime/bleavit-runtime/src/configs.rs:8381` already pays `&proposal.proposer`, so keeping `proposer` authorial satisfies it by construction — which is exactly the kind of property that silently regresses without a test. **Author-only rights stay author-only:** `bind_ratification` (`pallets/epoch/src/lib.rs:1955`) is the ratification identity 09 §1.1(4) requires the *proposer* to freeze, and nothing about funding touches it. **Contract:** `Proposal` and `ProposalSummaryView` each gain a trailing `funder: AccountId` (02 §13 rule 3 append — no existing field's name, type or offset moves), `INTEGRATION_CONTRACT_VERSION` 17 → **18**; no call index moves so `transaction_version` is unchanged, and the chain is pre-genesis so no migration is required. **No new 13 key**: `intake.max_acct` keeps its value and bounds, only its *subject* changes, so this milestone carries no R-2 parameter derivation. Verify: the sybil regression first (N funders × M authors driven against the cap — it MUST fail on the pre-fix keying and pass after, or the defect closure is unevidenced); withdrawal admitted for author and for funder and refused for a third party; the 10 % slash lands on the funder with the author's balance untouched; the reward lands on the author; `bind_ratification` refuses the funder; try-state binding every `ProposalBond.proposer` to a live proposal's funder; `pallet_epoch` weights regenerated for `submit`'s widened record. **Done 2026-07-31 — PR #201.** All eight legs discharged; nine tests, every one split-identity. Two-sided mutation proof on the cap (reverting the predicate fails *both* cap tests: the evasion succeeds in one and distinct funders are wrongly capped in the other); one-sided on the reward (routing to `&proposal.funder` fails it). The reward leg needed a **PARAM** fixture — TREASURY/CODE pay `min(0.05 %·Ask, 25k)` = 0 at the `ask = 0` every existing execution fixture carries, and a payout of zero pins no recipient; PARAM's flat `trs.reward.param` is `ask`-independent. Execution settles both incidences at once, so that one fixture pins the whole split by exact equality. **Two defects the closing pass found:** the `runtime-benchmarks` build was red (both `Proposal` constructions in `configs.rs` sit behind that cfg, so `--all-targets` never compiled them — #201 as first pushed would have failed CI); and try-state did not bind bond custody to the funder, a state the split made representable while every other assertion still passes on it. **Weights regenerated and the drift was real, not cosmetic:** `worst_case.proof_size` +2,080 B on 11 functions = **65 × 32**, the `MaxEncodedLen` of one extra `AccountId32` propagating through the bounded proposal set. Regenerated at the committed 50×20 fidelity, not the 2×1 check fidelity — at 2×1 the component fit collapses (`settle_cohort` reported a 2.5 × 10¹⁹ proof size) and writing that would have destroyed a linear term the gate cannot see. **Five pallets, not one:** `pallet_attestor`, `pallet_execution_guard`, `pallet_guardian` and `pallet_market` read `Epoch::Proposals` in their benchmarks and each records its `max_size`, so all five moved 326 → 358 (51 annotations). `--changed` selects on *local pallet source* and none of those four had one — the driver was a `futarchy-primitives` type a dependency edge away — so the affected set has to be derived from the storage item, and the committed `max_size` annotations give it in one grep. **Corrected once more before landing:** two of those four (`pallet_guardian`, `pallet_market`) had no charged-dimension change at all, only a stale annotation, and regenerating them under a concurrently-running benchmark sweep manufactured +54 %/+65 % `ref_time` regressions on untouched code. Re-measured on an idle machine the whole set is clean — `check-weight-regression.py` reports **zero regressions** over 364 shared functions. That gate is not part of `rust-workspace-gates.sh` and has to be run deliberately | -->

<!-- source track narrative -->

Opened 2026-07-29 on the "self-funding permanent institution" work order. Spec layer (03, 04, 08, 13, 14, 15, 02 → contract **v17**) landed in one pass as **PR #195** (`feat/e1-treasury-sustainability`), together with the reference-model implementation of 03 §5.3a; the code layer shipped as E1 → E4 in value order across #195, #197 and #196, all merged 2026-07-29. Normative arithmetic: **08 §10 (Sustainability)** — PLAN carries only the status and the headline figures (R-4).

**E5 opens the cost side (added 2026-07-30).** E1–E4 are the revenue half of 08 §10's crossover; they cannot close it alone, and the arithmetic says the other half is cheaper to move. The derivable cost base is **94.5 % two lines** — keepers 908,408/yr and collators 173,929/yr — and the largest single line, `ops.keepers` at 699,694/yr (61.1 %), funds coverage that 08 §6.3 itself classes as chart density rather than decision quality. E5 derives a reduced operating point for those, re-derives 08 §10's tables from it, and phase-gates the `ops.*` lines Phase 4 does not need. It is a **values-layer** milestone: no protocol code, every figure derived under R-1/R-2, and the frozen 08 §4.1 class floors untouched.

*Two verification obligations in the work order name artifacts that do not exist in this repository and are therefore not dischargeable here: there is no `explainer/` directory (so no third certified `src/protocol/` port to re-certify, and no `npm run verify`), and `frontend/` is still the `.gitkeep` placeholder of Track F (so the 04 §5 TypeScript LMSR port has no code to update). The 04 §5 obligations bind whenever those land; recorded so a later session does not read their absence as a skipped step.*

**The contract is spec-ahead-of-code, deliberately, and must not ship that way.** `02` and `00` now declare **v17**, while `futarchy_primitives::INTEGRATION_CONTRACT_VERSION` is still **16** (`crates/futarchy-primitives/src/lib.rs:12`, pinned by the assertion at `:1694`). That is the normal spec-first state under R-2 — the v17 surface is the *appended* events, calls and metadata constant that E1–E4 implement — but it is a real divergence while it lasts: **no release may go out with the runtime declaring 16 against a v17 spec.** The bump and its assertion land with the last of E1–E4, in the same change as the event field appends, not before. **Four things move in that one change and none of them is optional** (established while fixing the PR #195 review): `crates/futarchy-primitives/src/lib.rs:12`; the assertion pinning it at `runtime/bleavit-runtime/src/tests.rs:2050`; the **64 legacy `ledger_sequence_scenarios` corpus rows**, which still emit the v16 event shape and flip to v17 by one `contract_version` default plus a regeneration; and the `Event::ScalarRedeemed`/`ScalarPairRedeemed`/`GateRedeemed`/`BaselineRedeemed` variants in `crates/conditional-ledger-core/tests/differential_vectors.rs`, which need the trailing `fee`. The corpus is now **explicitly versioned** rather than implicitly v16: the 11 `ledger_fee_scenarios` rows carry `params.contract_version: 17` because they exist to pin the target surface, the legacy rows stay v16 because that is what is in force, and a v16 model that charged a fee raises rather than silently dropping it. That is why the split is safe to hold — but it is exactly the kind of two-sided obligation that gets half-done, so it is written out here rather than left to the diff.

**Milestone-id convention (read this before citing a marker).** The spec layer landed as **one** amendment batch and its `(added/amended 2026-07-29, milestone E1)` markers date the batch, not the implementing milestone: **E1 owns the whole Track E spec layer** as well as its own code. E2, E3 and E4 implement parts of spec that E1 landed, so a marker reading `milestone E1` on redemption-fee text is correct and is *not* a claim that E1 implements it.

<!-- end source track narrative -->
