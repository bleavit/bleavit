---
id: S2
track: S
title: cargo-fuzz targets — SCALE payload decode, nested-wrapper filtering, LMSR trade paths
spec:
  - 15 §4.5
depends:
  - B1
status: done
---

**Done 2026-07-17 (worktree `milestone-S2`).** `fuzz/` = `bleavit-fuzz`, a **separate nightly-pinned cargo workspace** (root `exclude = ["keeper", "fuzz"]`; own `rust-toolchain.toml` = `nightly-2025-11-24`; committed `Cargo.lock`) so libFuzzer + nightly never perturb the runtime workspace's `=`-exact SDK pins (stable2603 at S2; **stable2606** since the D-19 line move) — the keeper precedent. Three invariant-asserting libFuzzer targets: **`payload_scale_decode`** (raw-bytes bounded decode of `execution_guard_core::Payload` + a structured `Arbitrary` generator reaching the guard admit/bound-rejection paths; asserts 16-call/64-KiB limits, checked length sums, committed-vs-outcome hash binding, verdict-independence from the queue `hash`, `try_state`); **`nested_wrapper_filter`** (I-8/I-10/I-11 — differential vs an ordered structural oracle **and** an independent property predicate `independent_admits`, over BOTH origin-less and `Some(class_origin)` paths, with a hand-computed `wrapper_admission_truth_table`; covers `proxy_announced`/`as_multi_threshold_1`/shared-batch-budget/dispatch-as/scheduler-capture/transitive-proxyish — discharges the A4-deferred fuzz obligation); **`lmsr_trade_paths`** (I-12 — drives real `market-core` trades over a **seed-selected book kind** (Decision either branch, Gate either branch/`GateType`, unbranched Baseline — so `buy_gate`/`sell_gate`/`buy_baseline`/`sell_baseline` are all fuzzed, incl. the historically solvency-buggy `sell_baseline`) through an atomic mock `LedgerOps`, asserting `try_state`, the exact per-kind inventory/conservation identity, the `drain ≤ ceil(b·ln2)` headroom ceiling, strict buy-monotonicity, fee/rounding-protected round trips, and the `48·b` fixed-domain edge). Curated 15-seed corpora; `tools/ci/fuzz-gates.sh` (CI job `fuzz`): fmt/clippy/10 oracle-unit-tests · `cargo fuzz build` ×3 · corpus regression (`-runs=0`) · random smoke (writes to a git-ignored dir so the committed corpus stays curated). **Fuzz-found + fixed:** an LMSR harness bug (a sell the depleted book can't collateralize fail-closes with `Error::Ledger` — the harness now treats any typed-`Err` trade as a rolled-back no-op; regression `depletion_sell_rejection` seed + unit test). **Blocker surfaced + fixed (both reviewers):** the production guard `decode_batch` decoded preimage-sourced batches with unbounded recursion (top-level count capped, depth not) — a G-1/R-7 latent wasm-trap/native-abort in audit-scope-A code; hardened with kernel `MAX_PAYLOAD_DECODE_DEPTH = 256` + `decode_all_with_depth_limit` + runtime regression `deep_preimage_batch_decode_fails_closed_at_the_depth_limit` (SQ-225). Reviews: spec-reviewer (0 S2-blockers; 1 major = the guard blocker, fixed) + adversarial (same blocker + oracle-independence/payload-generator majors, all addressed). Gates green (root workspace incl. runtime 60 + guard 42 · fuzz fmt/clippy/10 tests + 3 targets build + corpus regression + 90s smoke ×3, 0 crashes · reference-model 21 · doc links). SQ-225…SQ-227 logged. The adversarial review's Gate/Baseline coverage gap was **closed in-session** (all three book kinds now fuzzed; unit tests `lmsr_harness_covers_gate_and_baseline_books` + `kind_from_seed_defaults_committed_seeds_to_decision_accept`; `baseline_two_sided`/`gate_reject_security` seeds). **Follow-ups:** both closed in batch B6 (2026-07-20) — SQ-226 ratified core-type fuzzing as sufficient where the production boundary carries its own hardening + named regression (15 §4.5), and SQ-227 turned out not to be a coverage gap at all: I-13's property suite had already landed in S1, so 15 §1's I-13 Verification column was corrected away from "fuzz"
