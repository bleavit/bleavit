---
id: B4
track: B
title: XCM — USDC/DOT reserve transfers, Asset Hub channel, reserve-probe plumbing, coretime renewal (freeze-exempt)
spec: [09 §6, 07 §8, 01 §4]
depends: [B1a]
status: done
---

No `Transact` governance either direction. **Done 2026-07-16** — `runtime/bleavit-xcm`, a runtime-independent library crate the runtime wires (implemented concurrently with B1a; merged onto post-B1a/B2/B3 main in-session): 09 §6.1 rule-table components (default-deny barrier: `Transact`/`UnpaidExecution` refused at any nesting depth with fail-closed decode bounds, closed instruction allowlist, AH/relay/Coretime origins only, known query responses + version subscriptions, no unpaid path, no superuser conversion; exact USDC/DOT matchers + `{USDC←AH, DOT←AH/relay}` reserves; `IsTeleporter = ()`; governed-rate 2-D `WeightTrader` with refund ledger, payer-adverse rounding both directions); 07 §8 probe (builder + querier-authenticated `OnResponse` router, high-bit query-id partition vs `pallet-xcm`, appendix `SetFeesMode jit → RefundSurplus → DepositAsset → ReportError` so `ExecutionResult(None)` proves the full round-trip); 09 §4 coretime funding leg (verified relay-teleport route, V-19; separate fee budget so the renewal account receives ≥ the quote); 09 §5.2 caps (`usdc_mint_admissible` at the mint leg fail-fast + per-account transactional deposit gate); `classify_pallet_xcm_call` (exit pinned to Asset Hub dest; `claim_assets` self-scoped Signed per the amended 09 §6.1 row; everything else denied, exhaustive match); local-only `XcmHealthSink` + `HealthTrackingRouter`. **Two narrow xcm-free pallet seams added**: `pallet_oracle::ProbeDispatch` (crank fires the send; failures fail-static) and `pallet_futarchy_treasury::RenewalDispatch` (dispatch errors roll back accounting → keeper retry), with `bleavit-xcm` shipping the ready `XcmProbeDispatcher`/`XcmRenewalDispatcher` impls. Chain identity: `ASSET_HUB_PARA_ID`/`CORETIME_PARA_ID`/`USDC_PALLET_INSTANCE`/`USDC_ASSET_INDEX` single-homed in `futarchy-primitives` (13 §3.5 home; V-16…V-19). I-24 CI lint added to the gate script. Dual review (spec-reviewer + Codex adversarial ×2 at xhigh) — all 7 blockers/8 majors fixed in-session; focused re-pass: **all CLOSED, 0 blocker/0 major**, its 4 minors fixed in-session too. 33 doc-15 tests (end-to-end through the real seams) + oracle 75 + treasury 28. Follow-ups: ~~Params-backed `TraderRates` binding (SQ-112), caps meters (SQ-110/SQ-111), health→welfare binding (SQ-113)~~ — **all closed 2026-07-17 by the B4-residual session** (Current-focus paragraph); E21 test artifacts (B7/B8); SQ-101 re-key composes the ready `CappedInflows`/revenue/probe-dispatch paths.

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track B — Runtime, node and chain | -->

<!-- source row: | B4 | XCM — USDC/DOT reserve transfers, Asset Hub channel, reserve-probe plumbing, coretime renewal (freeze-exempt) | 09 §6; 07 §8; 01 §4 | B1a | ✅ | No `Transact` governance either direction. **Done 2026-07-16** — `runtime/bleavit-xcm`, a runtime-independent library crate the runtime wires (implemented concurrently with B1a; merged onto post-B1a/B2/B3 main in-session): 09 §6.1 rule-table components (default-deny barrier: `Transact`/`UnpaidExecution` refused at any nesting depth with fail-closed decode bounds, closed instruction allowlist, AH/relay/Coretime origins only, known query responses + version subscriptions, no unpaid path, no superuser conversion; exact USDC/DOT matchers + `{USDC←AH, DOT←AH/relay}` reserves; `IsTeleporter = ()`; governed-rate 2-D `WeightTrader` with refund ledger, payer-adverse rounding both directions); 07 §8 probe (builder + querier-authenticated `OnResponse` router, high-bit query-id partition vs `pallet-xcm`, appendix `SetFeesMode jit → RefundSurplus → DepositAsset → ReportError` so `ExecutionResult(None)` proves the full round-trip); 09 §4 coretime funding leg (verified relay-teleport route, V-19; separate fee budget so the renewal account receives ≥ the quote); 09 §5.2 caps (`usdc_mint_admissible` at the mint leg fail-fast + per-account transactional deposit gate); `classify_pallet_xcm_call` (exit pinned to Asset Hub dest; `claim_assets` self-scoped Signed per the amended 09 §6.1 row; everything else denied, exhaustive match); local-only `XcmHealthSink` + `HealthTrackingRouter`. **Two narrow xcm-free pallet seams added**: `pallet_oracle::ProbeDispatch` (crank fires the send; failures fail-static) and `pallet_futarchy_treasury::RenewalDispatch` (dispatch errors roll back accounting → keeper retry), with `bleavit-xcm` shipping the ready `XcmProbeDispatcher`/`XcmRenewalDispatcher` impls. Chain identity: `ASSET_HUB_PARA_ID`/`CORETIME_PARA_ID`/`USDC_PALLET_INSTANCE`/`USDC_ASSET_INDEX` single-homed in `futarchy-primitives` (13 §3.5 home; V-16…V-19). I-24 CI lint added to the gate script. Dual review (spec-reviewer + Codex adversarial ×2 at xhigh) — all 7 blockers/8 majors fixed in-session; focused re-pass: **all CLOSED, 0 blocker/0 major**, its 4 minors fixed in-session too. 33 doc-15 tests (end-to-end through the real seams) + oracle 75 + treasury 28. Follow-ups: ~~Params-backed `TraderRates` binding (SQ-112), caps meters (SQ-110/SQ-111), health→welfare binding (SQ-113)~~ — **all closed 2026-07-17 by the B4-residual session** (Current-focus paragraph); E21 test artifacts (B7/B8); SQ-101 re-key composes the ready `CappedInflows`/revenue/probe-dispatch paths. | -->

<!-- source track narrative -->

Doc-09 WBS delta rows map here and to Track A: E15→B2/B8 · E16→A2 · E17→A3 · E18→A6 ·
E19→A5 · E20→A10/B6 · E21→B4 · E22→A11/B1. (E1–E14 definitions live in git history —
superseded `BACKEND_PLAN.md` §26; their scope is covered by Tracks M/A/B.)

<!-- end source track narrative -->
