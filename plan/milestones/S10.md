---
id: S10
track: S
title: Phase-0 inference — exact Clopper–Pearson bounds on the published per-class false-pass rates, and the `required_n` figure the gate needs. **Evidence only:** the engine repair and any gate change are deliberately out of scope
spec:
  - 15 §4.9
  - 09 §7.1
depends:
  - S4
status: active
---

**Partial, done 2026-08-01; the remainder is deferred by design.** `simulation/src/bleavit_simulation/inference.py` (401 lines, 15 tests) — pure-stdlib exact one-sided Clopper–Pearson, reading the per-class numerators and denominators **out of the committed artifact** rather than hardcoding them. Reproduces all four published point estimates from the integer counts, then: **PARAM 0/168 → 1.767 %** and **META 4/711 → 1.283 %** upper bounds, both **above** the < 1 % gate (TREASURY 0.688 %, CODE 0.638 %). `required_n` gives PARAM 299 / TREASURY 473 / CODE 473 / META 1,568. Also pins that `calibration.py`'s `no_causal_wrong_pass_observed` status is factually wrong: 62 causal wrong-PASS flips were observed (PARAM 3, TREASURY 7, CODE 25, META 27), all dispositioned unprofitable — "nothing was probed" and "everything probed was unprofitable" are different claims. **Nothing is wired into `normative_violations`, `designation` or any eligibility field, and the artifact is untouched** — doing so is a normative 15 §4.9 change (SQ-549, SQ-550). **Deferred:** the attacker best-response allocator and the Baseline-anchor coupling, both of which change the artifact G0 consumes fail-closed
