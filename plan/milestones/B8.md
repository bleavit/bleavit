---
id: B8
track: B
title: Release-artifact publication pipeline — wasm+metadata+hashes, chain-specs, envs, chainHead fixtures, vector corpus (backend row E15)
spec:
  - 02 §11
  - 15 §5
depends:
  - B7
  - M3
status: done
---

Release-gating both directions (FE-R1). **Added 2026-07-15 (SQ-16/SQ-27):** owns the release-side supply-chain gates (15 §4.5 `--locked`+`cargo-audit`) and the ≥10⁷-point MPFR-256 corpus sweep + content-addressed publication per the 04 §4/§5 cadence. **Done 2026-07-17** (the pipeline; the B7-owned env artifacts and the B2/A8/A11 surface remain other milestones' obligations that this pipeline gates on): `.github/workflows/release.yml` (tag-triggered; dry-run dispatch mode publishes nothing; supply-chain + full workspace gates + tooling suites → artifact build → strict assembly → **atomic draft→upload→verify→publish**, prerelease until operator Arweave mirror evidence per SQ-136) + `tools/release/` — reproducible-recipe `build-runtime.sh`; `extract-metadata.py` (metadata/identity from a booted node, `state_getStorage(":code")`↔`--wasm` byte binding fail-closed); 156-entry `surface-manifest.json` over the full 02 §3/§6–§9/§12 critical surface (frozen layout expectations on wired surface only — blocked entries get theirs frozen from the real runtime when their milestone lands; metadata-independent raw 02 §12 `ReleaseChannel` fixture with offset-layout validation; exact-key 02 §8 USDC value assertions (decimals 6 / min_balance 10⁴); `system_properties` identity row); websocket chainHead recorder (deterministic normalized transcripts, per-operation + whole-run deadline budget covering both transports, real `System.Events` bytes required on both transports); `assemble-release.py` (artifact set 15 §5 (1)–(5) content-addressed `dist/<sha256>-<name>`, `bleavit.env-evidence.v1` run-evidence contract for B7 env dirs, `environments.json` inventory with live-only-for-bootnode-enforcing-profiles rule, wasm↔metadata↔chain-spec-`:code`↔commit binding as corruption (fails even dry-run), readiness report attributing every gap to its owning milestone). **Numerics:** ≥10⁷-point sweep `--sweep-out` mode in the single 04 §5 generator (deterministic per-shard seeding, 100 content-hashed shards, exp2/log2/ln/**cost** families) + dependency-free `#[ignore]`d `futarchy-fixed` checker (hash-verifies then parses the same bytes; `BLEAVIT_SWEEP_REQUIRE_FULL=1` release gate) + `sweep.yml` kernel/numerics-change CI path-trigger (04 §4 cadence); **verified full run: worst log2 0 ulp / ln 1 ulp / cost 2 ulp (bound 8), exp2 within 2⁻⁶³ relative** (V-26). **Supply chain (15 §4.5):** pinned cargo-audit 0.22.2 per-commit CI job + release-blocking leg + `--summary-out` disclosure embedded in the release manifest; `.cargo/audit.toml` with per-entry-annotated stable2603-pin-forced exceptions (24 findings; 16× wasmtime 35; `tracing-subscriber` 0.3.20 bump impossible — `sc-tracing 45.0.0` pins `=0.3.19`); keeper workspace audited clean-room (no inherited exceptions) — SQ-135. **A real tag release today fails strict assembly by design**, every gap attributed: B2 (FutarchyApi ×11 + metadata constants), A8 ×~38 / A11 ×~10 (runtime wiring), B7 (env evidence ×2), SQ-101 (Location-keyed USDC ×3). Orchestrated with **Codex subagents (gpt-5.6-sol/xhigh)**: 2 parallel authors → dual review (spec-reviewer 0 blocker/4 major/3 minor + adversarial Codex 11 findings) → 1 Codex fix round (16 items) → focused re-review (12 residuals) → **manual fix round by the orchestrator** (Codex usage limit hit; all 11 actionable residuals fixed + 1 documented non-fix: the sweep cost bound is deliberately stricter than the in-crate trade-path allowance). SQ-135…SQ-139. Gates green (full workspace ×3 runs · 53 release-tool + 6 sweep + 16 deploy unittests · full 10⁷ sweep ×2 · real-node e2e dry-run)
