---
id: A1
track: A
title: `pallet-constitution` — typed/bounded/rate-limited params, meters, capability tables, `PhaseFlags`, `ReleaseChannel` fixed-layout key, kernel re-export
spec:
  - 06
  - 13 §1/§4
  - 02 §7.3/§12
depends:
  - M1
status: done
---

I-6, I-7 (meter primitive; I-17 envelopes re-scoped to treasury/guard per amended 15 §1). **Done 2026-07-15** — production FRAME pallet over `constitution-core`: production `#[frame_support::pallet]` over `constitution-core` — frozen `Params`/`PhaseFlags`/`ReleaseChannel` (D-14 raw key + 168-byte layout test-pinned; runtime MUST instance the pallet as `Constitution`), `GovernanceOrigin: EnsureOrigin<_, Success = ConstitutionOrigin>` + per-call 06 §3.2 predicates, calls `set_param`/`set_capability`(FutarchyMeta)/`set_phase_flag`(Root, 09 §5.4)/`set_release_channel`(ConstitutionalValues, 02 §12), internal APIs `note_release_channel`/`note_phase_flag`/`charge_meter_internal`, genesis (code-owned registry; phase-flags/release-channel configurable), try-state (I-6/I-7 shape + D-14 key), mock, 32-test 15 §4.1 suite + shell-vs-core differential, v2 `#[benchmarks]`×4 + `WeightInfo` (B5 recalibrates). Spec-reviewer 2026-07-15: blocker (META+values origin) + 3 majors (values `set_capability`, un-spec'd `set_phase_flag`/`charge_meter` extrinsics) + `INTEGRATION_CONTRACT_VERSION` naming **fixed in-session**. Codex adversarial review (same day, "needs-attention") corroborated and added; fix round 2 applied: Root **removed** from param/capability/meter predicates (09 §5.4 exhaustive sudo scope), `set_phase_flag` bit-scoped to the armable mask 0–4 with one-bit internal setters for machinery bits 5–7, registry seeded to **32 keys** (every scalar/concrete/≤16-byte/non-[VERIFY] 13 §1 row), `ReleaseChannel` flags word validated (bits 3–31 reserved zero, 02 §12), FRAME crates `=`-exact-pinned, and a 600-step seeded randomized shell-vs-core differential added — which immediately caught and fixed a real core bug (failed `Meter::charge` persisted its lazy window reset; now strict no-op per G-1). **Closed 2026-07-15 (fix round 3, user-delegated decisions)**: SQ-4…SQ-13 resolved (Decision log), `amend_registry` shipped (kernel-bounded rows fully frozen per 13 rule 2; meta-bounds per rule 7), 13 §1 registry materialized at **87 keys** (only formula/TGE/uncalibrated rows absent), meters re-scoped (constitution = generic primitive, empty at genesis; envelopes with treasury/guard). Final spec-reviewer pass: 0 blockers / 1 major / 6 minors — all fixed in-session (full-tuple kernel freeze, capability-row swap to `(Code, AuthorizeUpgrade)`, uniform [VERIFY]-seeding criterion incl. `pol.b_baseline`/`collator.n_tgt`, uniform "—"→0 cooldown, comment sweep, 06 glosses). Follow-ups owned elsewhere: PoV-calibrated weights (B5), I-6 SMT/property artifact (Track S/S1), guard-side `code.spacing` metering (A11), full-runtime internal-writer negative suites (B1a/S5), reviewer spec-notes on 05 §1.3 gloss + rule-7 kernel-set upkeep (next 05/13 touch)
