---
id: F25
track: F
title: **FE-6a the local index's disclosure surface** — construct `LocalIndex` at boot, run `checkIndexAtBoot`, and render `IndexBootReport`
spec:
  - 10 §6.3, §7, §9.2, §9.4
  - 15 §2 INV-FE-15
depends:
  - F8
  - F7
status: done
---

**The obligation F8 built and could not discharge, owned here rather than absorbed into a neighbour's prose.** `packages/local-index` has **no production consumer**: nothing in `app/src` constructs a `LocalIndex`, so `checkIndexAtBoot` has no call site outside its own suite and every disclosure F8 wrote has a producer and no reader. That is the same defect class F8's own review rounds kept finding one layer down (a checker with no call site; an error code with no emitter; a record on a channel no query reads) and it cannot be closed inside F8, because what is missing is the *client*, not the package. **Explicit obligations, each a thing the user is owed and currently is not told:** `ChartDiscardRecord` — the chart rows a schema migration dropped over blocks `meta.coverage` still claims (this is named because it is the newest and the easiest to lose track of); `PendingRawEvictionRecord` — §6.5 blobs discarded under §9.1's bound; `dropped` and `invalidated` — coverage ranges that did not survive sanitization or the §6.3 edge checks, with their reasons; `unchecked` — ranges nothing could verify, **kept**, which must read as *cannot say* and never as verified; `pendingDecoder` — §6.5's *"N events pending decoder"*; and, on the read path rather than at boot, `CoveredHistory`'s `downsampled` and `chartDiscard` beside every history answer (10 §6.3 *"charts render holes as visible gaps with an explainer, tables state 'complete within [ranges]'"*). **Blocked on copy, not on code:** 10 §9.4 requires fixed user copy plus a documented recovery per error code, and `FE-IDX-002` has no definition (SQ-604) and now two candidate emitters (SQ-783); SQ-820 asks what a tier emptied outright may be *called* and SQ-821 where that fact belongs in a query's answer. Invent none of it here — ship the machine-readable records the package already produces, and bind the copy when those rule **Built 2026-08-07 on `track-f/f25-index-disclosure`; F25 stays 🔨 — GitHub Actions has been in a major outage all session, so no run has executed against this work and R-6's gate conjunct has no evidence.** The reader exists and it is wired: `src/features/analysis/src/index-boot.ts` is `checkIndexAtBoot`'s **first production call site**, `boot()` runs it before the mount, and `IndexBootDisclosure` renders what it returns as the shell's unconditional first child — outside the outlet, so it is on every route rather than on a screen. **What made the wiring possible without a chain** is that the boot check needs storage, not smoldot: 10 §3.1 puts `StorageOpen` before `WorkerSpawn`, and §6.3 already defines the answer for a chain that cannot be reached — *cannot say*, which **keeps** the range. So `cannotObserve` is the honest observer rather than a stub, and every range verdicts `unchecked` by construction, which is exactly the set the surface must not drop. **Three arms, because collapsing any two loses the distinction this row exists to make**: `checked`, `not-opened` and `unopenable`. An index nothing opened may not render as one that was opened and was fine — §6.3's asymmetry applied to the boot path itself — and today's build reaches `not-opened`, because `release-sources.json` pins `genesisHashes.para: null` and 10 §7 gives the index one database per chain identity with no default (V-260). That claim is **bound to the file**, so the day a genesis is pinned the suite fails and the wiring has to be finished. **The copy rule is a type, not a convention.** `DisclosureCopy` is `stated` (a sentence with the architecture section whose rule it discharges) or `awaiting` (the record's own fields rendered, the slot marked, the open rows named) and there is no third arm — so a sentence cannot be written here without either citing a rule or admitting the rule is unwritten. Four slots wait, all on `FE-IDX-002`: `ranges-dropped` (SQ-604, SQ-783, **SQ-921**), `ranges-invalidated` (SQ-604, SQ-783), `raw-blobs-evicted` (SQ-760, SQ-820) and `chart-rows-discarded` (SQ-604, SQ-783, SQ-820, SQ-821). Five state their sentence and cite it: the coverage summary and the holes (§6.3), `unchecked` (§6.3), *N events pending decoder* (§6.5, whose phrase the document supplies) and the downsampled label (§9.2). Both halves are **machine-checked**: every citation must resolve to a real section, and every waiting slot must name rows PLAN.md still lists as **open** — a mechanical expiry that fails the day SQ-604 is ruled. **`REPORT_DISCLOSURES` is total over `keyof IndexBootReport` and `HISTORY_DISCLOSURES` over `keyof CoveredHistory`**, and the suite re-derives both field sets by parsing the package's own interface declarations — so the next field added to the boot report cannot ship without a reader, which is this package's defect class made unrepeatable rather than remembered. The migration discard has **one** renderer across both paths (asserted item-for-item), so §9.4's *fixed copy per code* cannot become two sets of words that agree today. **Three findings the row did not name.** (1) `unchecked` has two causes with different futures and `CoverageVerification` carries no discriminator — an `unverifiable` edge can never be compared while an unreachable chain becomes comparable — so the surface says only what is true of both and **SQ-922** asks whether §6.3 obliges the distinction. (2) A coverage entry dropped by `sanitizeCoverage` is a **third** `FE-IDX-002` candidate beside SQ-604's disagreement and SQ-783's migration discard, with a third recovery (**SQ-921**). (3) The surface has **no 11 §11.2 inventory id** — the same gap F23 found in the provider UI — so none was minted and **SQ-920** asks where it belongs. **What this build honestly reaches:** the disclosure renders on every `routes.tsx` state (both the `PendingScreen` branch and the empty `implemented` map), and what a user sees today is the `not-opened` statement; `CoveredHistoryDisclosure` reaches **no** route, because every history screen is `built-unwired` on a live transport — its `chartDiscard` renderer is the one the boot path already reaches. **Named remainders:** once a genesis is pinned the storage open must move after the first paint (§3.1 renders the skeleton before `StorageOpen`) and this build has no re-render path; and `bootLocalIndex` returns the database handle that the ingest loop will need and nothing consumes yet. **Gates (local only):** `build` · `check:types` · `pnpm -C app test` exit 0 at **1,766 tests** across 20 suites (`test:analysis` 9 → 35) · all 27 `app/` gates green incl. `check:covered-history` (+ witness) · `depcruise` 0 errors, 2 pre-existing orphan warnings · PLAN tables · spec-question batches. **24 mutants, 0 survived** — three needed re-expression (one stale anchor, two refused by `tsc` rather than by a test) and all three were then killed. **✅ 2026-08-07.** R-6's two conjuncts are both satisfied: the R-6 review recorded above returned no blocker, and the gate evidence is PR #261 — all **23** checks `success` on `76477e3f`, squash-merged to `main` as `34cbf7a4`.
