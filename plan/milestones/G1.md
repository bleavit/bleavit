---
id: G1
track: G
title: Phase 1 exit — Zombienet 3 unattended epochs; collator/keeper-loss, dead-man, coretime-under-dead-man, PB-MIGRATION drills
spec: [09 §7.1]
depends: [G0, B7, B9]
status: done
---

**First real drill executions 2026-07-18** (drill ledger): 01 smoke **PASS 10/10** · 02 collator-loss **PASS 10/10** (V-24 native SIGSTOP/SIGCONT confirmed) · 03 keeper-loss **PASS 12/12** (B9 keeper topology wired: `zombienet/scripts/keeper-node.sh` exec wrapper, real live-mode daemon as node `keeper`, pre-pause + post-resume `bleavit_keeper_connected ≥ 1` machine-asserted; gate cleared) · 06 PB-MIGRATION **honestly re-gated on SQ-274** (no 09 §3.2 `migrations.force_failure`/`retry` control surface; the guardian 5-of-7 propose/approve workflow executes and the dispatching approval correctly refuses `TriggerInactive`) · 04/05 re-attributed to the **B10 dead-man detector** (freeze metadata surfaces wired; `note_dead_man_engaged` has no caller) · 07 re-attributed to **B10 AssetTransactor composition** (SQ-101 landed) · 08 parked (staging needs guard-queue staging + a live-triggerable freeze — effect substrate landed with B1b #104, trigger feed SQ-233 open; 3-day D-14 lane) · 09 **PASS 6/6** via the compressed `fast-timing` test runtime (SQ-128 implemented 2026-07-20; three real 84-block epochs in ~29 min, keeper cranking `Epoch.tick` unattended). Execution infrastructure repaired this session (V-36): Paseo-CSG `WASM_BUILD_WORKSPACE_HINT` release blocker, zndsl grammar conformance ×9, 32-bit timeout overflows, fast-runtime + `num_cores=3` relay scheduling (Decision log), drill-helper ss58/raw-membership/usable-balance defects. **2026-07-20 update (post-#105 G0 ✅/S4 ✅ merge — G0 dependency cleared):** SQ-274 **resolved** and **drill 06 now PASSES 3/3** — it stages `MigrationHalt = true` at genesis (new `pallet-execution-guard` `migration_halt` field; PLAIN spec so zombienet schedules it; no production fault-injection surface, R-7), `assert-halt` verifies the trigger, and the guardian 5-of-7 recovery's dispatching approval correctly **fails closed** with `DispatchError::Other` (the Migration playbook has no EmergencyPlaybook-safe call — distinct from the pre-SQ-274 `TriggerInactive`). **drill 07** — the `spec_version` sentinel is retired (capability probe on the genesis USDC) but its first real run surfaced the pinned Paseo `asset-hub-paseo-runtime` trapping on its parachain-system inherent (Paseo-CSG issue, re-gated). **drill 05** — staged-renewal exit-form code landed (genesis seeding of the `ops.coretime` line/custody + the ~8 h dead-man run remain). A drill-spec-regen blocker (the merged B1b seat-bond genesis assert panicked — `development_genesis` funds only 4 of the 7 seeded guardians) was fixed by genesis-funding all seven seat bonds within the 08 §2.1 1B/150M/200M invariants. **2026-07-20 (fast-timing extended to drills 04 & 08 — user "not 3 days and not 16 hours"):** compressed two more kernel constants behind the default-off `fast-timing` feature — `DEAD_MAN_RELAY_BLOCKS` 4,800→48 (drill 04) and `DESCRIPTOR_LEAD_TIME_BLOCKS` 43,200→12 (drill 08) — after an Explore coupling audit (both compress in isolation: one production consumer each, no invariant/property/try-state/TLA/limit-coverage magnitude dependence). Release arms byte-identical; `production_epoch_timing_floors_are_frozen` pins 4,800/43,200; `chain-spec-builder --verify` re-ran `EpochParams::validate` on the compressed wasm. **Drill 04's first-ever execution compressed its finality stall ~16 h→~8 min** (reached the 48-relay-block gap in ~8 min — timing proven) but `assert-dead-man engaged` **fails on a pre-existing detector/scenario mismatch (SQ-282), not timing**: `observe_dead_man` triggers on a relay-**parent** jump between consecutive parachain blocks (outage/catch-up), while a relay-**finality** stall keeps the parachain producing (liveness `pre-fault,2` passed) — the open 05 §4.8 `F`-signal `[VERIFY]` (`PersistedValidationData` exposes `relay_parent_number`, not the relay finalized head). Drill 04 re-gated on SQ-282. **Drill 08** now reads the live `descriptorLeadTime` (no 43,200 hardcode), so its ~3-day D-14 wait is gone; it stays gated only on freeze/expedited genesis staging (SQ-233). **2026-07-20 — Phase-1 exit reached (✅; SQ-282 + PB-MIGRATION docs, decide-yourself).** The 09 §7.1 line-282 Phase-1 exit drill set passes end-to-end: 01 smoke, **02** collator-loss, **03** keeper-loss, **06** PB-MIGRATION 3/3, compressed **09** 6/6 (SQ-128), **04 dead-man 18/18**, **05 coretime-under-dead-man 19/19**. **SQ-282 resolved:** SDK verification is definitive — the parachain runtime cannot observe the relay GRANDPA finalized head on stable2606 (`PersistedValidationData` = four fields, no `well_known_keys` finality key, `cumulus_pallet_parachain_system` surfaces only the relay best/anchor), so a finalized-head detector is impossible; the 05 §4.6 `F` `[VERIFY]` is closed and 05 §4.8/13 §2/14 TH-37 are re-scoped to the observable relay-parent-gap trigger (the detector code was already `relay_parent_gap`-based and correct), with the finality-stall residual assigned to an off-chain monitor (SQ-283, O5) + XCM-isolation bound. Drill 04 re-pointed to a real collator outage (pause all collators → relay best advances ≥ 48 → catch-up jump engages the dead-man; asserts the durable bit-6 + `incident_active` latch, the acute freeze staying unit-tested). Drill 05 proves the D-9 coretime-renewal freeze-exemption via a probe (`execute_coretime_renewal` → `RenewalWindowClosed`) under the engaged dead-man, on a fast-timing spec that genesis-seeds the coretime quote authority + renewal account (existing fields; **no treasury code change**). **SQ-279/280/281 reconciled** (PB-MIGRATION docs) + 5 stale section-refs — spec-reviewer 0 blocker. **No runtime-code change** (the detector was already correct); no 02 contract change; docs gates green. **Scope ruling (09 §7.1 phase table; decide-yourself, ratifiable):** drill **08** (expedited-CODE under a staged freeze) is a **Phase-2** exit criterion (line 283) and drill **07** (XCM funding-flow suite) is **Phase-3** (line 284) — neither is a Phase-1 gate; the `bleavit.env-evidence.v1` bundle is release-train (G2+, gated on SQ-203 chopsticks-cards + SQ-204 try-runtime-cli). Carried forward (non-Phase-1): drills 07/08 + the env-evidence bundle (G2/G3/release-train), SQ-283 (off-chain relay-finality monitor, O5), SQ-284 (raw-`TreasuryState` polkadot-js decode quirk, tooling), SQ-233 (cross-milestone trigger feeds). SQ-274 resolved, SQ-275/276/277 ruled, SQ-278 open, SQ-282/283/284 raised.

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track G — Rollout phase gates (evidence + META decision + values ratification each) | -->

<!-- source row: | G1 | Phase 1 exit — Zombienet 3 unattended epochs; collator/keeper-loss, dead-man, coretime-under-dead-man, PB-MIGRATION drills | 09 §7.1 | G0, B7, B9 | ✅ | **First real drill executions 2026-07-18** (drill ledger): 01 smoke **PASS 10/10** · 02 collator-loss **PASS 10/10** (V-24 native SIGSTOP/SIGCONT confirmed) · 03 keeper-loss **PASS 12/12** (B9 keeper topology wired: `zombienet/scripts/keeper-node.sh` exec wrapper, real live-mode daemon as node `keeper`, pre-pause + post-resume `bleavit_keeper_connected ≥ 1` machine-asserted; gate cleared) · 06 PB-MIGRATION **honestly re-gated on SQ-274** (no 09 §3.2 `migrations.force_failure`/`retry` control surface; the guardian 5-of-7 propose/approve workflow executes and the dispatching approval correctly refuses `TriggerInactive`) · 04/05 re-attributed to the **B10 dead-man detector** (freeze metadata surfaces wired; `note_dead_man_engaged` has no caller) · 07 re-attributed to **B10 AssetTransactor composition** (SQ-101 landed) · 08 parked (staging needs guard-queue staging + a live-triggerable freeze — effect substrate landed with B1b #104, trigger feed SQ-233 open; 3-day D-14 lane) · 09 **PASS 6/6** via the compressed `fast-timing` test runtime (SQ-128 implemented 2026-07-20; three real 84-block epochs in ~29 min, keeper cranking `Epoch.tick` unattended). Execution infrastructure repaired this session (V-36): Paseo-CSG `WASM_BUILD_WORKSPACE_HINT` release blocker, zndsl grammar conformance ×9, 32-bit timeout overflows, fast-runtime + `num_cores=3` relay scheduling (Decision log), drill-helper ss58/raw-membership/usable-balance defects. **2026-07-20 update (post-#105 G0 ✅/S4 ✅ merge — G0 dependency cleared):** SQ-274 **resolved** and **drill 06 now PASSES 3/3** — it stages `MigrationHalt = true` at genesis (new `pallet-execution-guard` `migration_halt` field; PLAIN spec so zombienet schedules it; no production fault-injection surface, R-7), `assert-halt` verifies the trigger, and the guardian 5-of-7 recovery's dispatching approval correctly **fails closed** with `DispatchError::Other` (the Migration playbook has no EmergencyPlaybook-safe call — distinct from the pre-SQ-274 `TriggerInactive`). **drill 07** — the `spec_version` sentinel is retired (capability probe on the genesis USDC) but its first real run surfaced the pinned Paseo `asset-hub-paseo-runtime` trapping on its parachain-system inherent (Paseo-CSG issue, re-gated). **drill 05** — staged-renewal exit-form code landed (genesis seeding of the `ops.coretime` line/custody + the ~8 h dead-man run remain). A drill-spec-regen blocker (the merged B1b seat-bond genesis assert panicked — `development_genesis` funds only 4 of the 7 seeded guardians) was fixed by genesis-funding all seven seat bonds within the 08 §2.1 1B/150M/200M invariants. **2026-07-20 (fast-timing extended to drills 04 & 08 — user "not 3 days and not 16 hours"):** compressed two more kernel constants behind the default-off `fast-timing` feature — `DEAD_MAN_RELAY_BLOCKS` 4,800→48 (drill 04) and `DESCRIPTOR_LEAD_TIME_BLOCKS` 43,200→12 (drill 08) — after an Explore coupling audit (both compress in isolation: one production consumer each, no invariant/property/try-state/TLA/limit-coverage magnitude dependence). Release arms byte-identical; `production_epoch_timing_floors_are_frozen` pins 4,800/43,200; `chain-spec-builder --verify` re-ran `EpochParams::validate` on the compressed wasm. **Drill 04's first-ever execution compressed its finality stall ~16 h→~8 min** (reached the 48-relay-block gap in ~8 min — timing proven) but `assert-dead-man engaged` **fails on a pre-existing detector/scenario mismatch (SQ-282), not timing**: `observe_dead_man` triggers on a relay-**parent** jump between consecutive parachain blocks (outage/catch-up), while a relay-**finality** stall keeps the parachain producing (liveness `pre-fault,2` passed) — the open 05 §4.8 `F`-signal `[VERIFY]` (`PersistedValidationData` exposes `relay_parent_number`, not the relay finalized head). Drill 04 re-gated on SQ-282. **Drill 08** now reads the live `descriptorLeadTime` (no 43,200 hardcode), so its ~3-day D-14 wait is gone; it stays gated only on freeze/expedited genesis staging (SQ-233). **2026-07-20 — Phase-1 exit reached (✅; SQ-282 + PB-MIGRATION docs, decide-yourself).** The 09 §7.1 line-282 Phase-1 exit drill set passes end-to-end: 01 smoke, **02** collator-loss, **03** keeper-loss, **06** PB-MIGRATION 3/3, compressed **09** 6/6 (SQ-128), **04 dead-man 18/18**, **05 coretime-under-dead-man 19/19**. **SQ-282 resolved:** SDK verification is definitive — the parachain runtime cannot observe the relay GRANDPA finalized head on stable2606 (`PersistedValidationData` = four fields, no `well_known_keys` finality key, `cumulus_pallet_parachain_system` surfaces only the relay best/anchor), so a finalized-head detector is impossible; the 05 §4.6 `F` `[VERIFY]` is closed and 05 §4.8/13 §2/14 TH-37 are re-scoped to the observable relay-parent-gap trigger (the detector code was already `relay_parent_gap`-based and correct), with the finality-stall residual assigned to an off-chain monitor (SQ-283, O5) + XCM-isolation bound. Drill 04 re-pointed to a real collator outage (pause all collators → relay best advances ≥ 48 → catch-up jump engages the dead-man; asserts the durable bit-6 + `incident_active` latch, the acute freeze staying unit-tested). Drill 05 proves the D-9 coretime-renewal freeze-exemption via a probe (`execute_coretime_renewal` → `RenewalWindowClosed`) under the engaged dead-man, on a fast-timing spec that genesis-seeds the coretime quote authority + renewal account (existing fields; **no treasury code change**). **SQ-279/280/281 reconciled** (PB-MIGRATION docs) + 5 stale section-refs — spec-reviewer 0 blocker. **No runtime-code change** (the detector was already correct); no 02 contract change; docs gates green. **Scope ruling (09 §7.1 phase table; decide-yourself, ratifiable):** drill **08** (expedited-CODE under a staged freeze) is a **Phase-2** exit criterion (line 283) and drill **07** (XCM funding-flow suite) is **Phase-3** (line 284) — neither is a Phase-1 gate; the `bleavit.env-evidence.v1` bundle is release-train (G2+, gated on SQ-203 chopsticks-cards + SQ-204 try-runtime-cli). Carried forward (non-Phase-1): drills 07/08 + the env-evidence bundle (G2/G3/release-train), SQ-283 (off-chain relay-finality monitor, O5), SQ-284 (raw-`TreasuryState` polkadot-js decode quirk, tooling), SQ-233 (cross-milestone trigger feeds). SQ-274 resolved, SQ-275/276/277 ruled, SQ-278 open, SQ-282/283/284 raised. | -->

<!-- source track narrative -->

Deferred (🅿, post-v1, do not implement): forecast trading / reopened books (N-8, D-8 — 04 §13);
order-book layer (04); Mode A binding; combinatorial futarchy (01 §2.4).

---

<!-- end source track narrative -->
