---
id: N2
track: N
title: "**Make the conditional-ledger shell instantiable** — `Config<I: 'static = ()>` / `Pallet<T, I>` while retaining only the existing default instance in the runtime; `ServiceLedger` wiring remains N7"
spec: [03 §1a, 15 I-4]
depends: [N1]
status: done
---

Completed 2026-08-01, alone and without sibling agents. Four mechanical Rust files changed; core, test sources, weights and runtime composition unchanged. Runtime metadata retains pallet index 52, storage prefix `ConditionalLedger`, all call/event/error indices and the identical module digest. Existing pallet 86/86, PT-1…PT-8 at 1,000,000 cases, differential corpus 5/5, TLA⁺ and changed-scope Rust gate green

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track N — External clients: futarchy as a service over XCM | -->

<!-- source row: | N2 | **Make the conditional-ledger shell instantiable** — `Config<I: 'static = ()>` / `Pallet<T, I>` while retaining only the existing default instance in the runtime; `ServiceLedger` wiring remains N7 | 03 §1a; 15 I-4 | N1 | ✅ | Completed 2026-08-01, alone and without sibling agents. Four mechanical Rust files changed; core, test sources, weights and runtime composition unchanged. Runtime metadata retains pallet index 52, storage prefix `ConditionalLedger`, all call/event/error indices and the identical module digest. Existing pallet 86/86, PT-1…PT-8 at 1,000,000 cases, differential corpus 5/5, TLA⁺ and changed-scope Rust gate green | -->

<!-- source track narrative -->

Opened 2026-08-01 on the "make the futarchy infrastructure accessible to other parachains,
smart contracts and services via XCM" work order. Bleavit hosts conditional decision markets
for external clients and **sells price discovery, not decisions**: it publishes the conditional
TWAPs with provenance plus a manipulation-cost bound, and the client's own on-chain rule decides
and executes locally. Bleavit never runs foreign code, and no external state reaches any Bleavit
decision, welfare or settlement input. v1 scope by user decision — the spec batch, contract bump
and threat rows land together. Full approved design: `.claude/plans/` (session plan, 2026-08-01).

<!-- end source track narrative -->
