---
id: F9
track: F
title: FE-7 `packages/providers` + `tools/snapshot` — optional acceleration, sampling, auto-disable
spec: ["10 §8.2–§8.4 (the mechanisms: format, screens, sampling, ladder, quotas)", "10 §8.1's in-release list. **Three exclusions, each named with an owner**, because a section cited whole with unnamed exclusions makes the narrowing unfalsifiable: the rendered half of §8 is **F23**", §8.2's first sentence names two artifacts — the *"against an archive node"* reader behind `tools/snapshot` (SQ-612) and the live-indexer HTTP interface with its *"reference implementation in `optional/indexer/`"* (SQ-613) — and those, together with §8.3's *"on enable + every 10 min"* probe driver, are **F24**]
depends: [F8]
status: done
---

INV-FE-3/15. Scaffold empty at F0 so the firewall is non-vacuous **Started 2026-08-05 with the health and sampling model.** The never-promote rule needed no code here and gets none: `Finalized<T>` is unnameable outside `chain-client`, so this package **cannot** produce one whatever it does — 10 §2.2 is enforced by a symbol it cannot reach. What F9 owns is what the client **says** about a provider, which is where the failures live. `defaultProviders()` returns empty **from a function taking no argument**, so there is nothing for a build or a previous session to inherit a default from — a configurable default is how *opt-in* quietly becomes *on for most people*, and it is the same device `defaultScope()` uses. **Any** mismatch auto-disables: §8.3 sets no threshold, and a threshold is what turns one caught lie into a tolerated error rate. **Unverifiable rows count neither way** — a row whose object is gone proves nothing, and counting it as a pass would let a provider evade sampling by serving only unverifiable rows, the cheapest available evasion; `effectiveCoverage` reports how much of a round was **actually comparable**, so a 2-of-100 check cannot read as a 100-row one. The auto-disable **reason is a required field**, because a source that vanishes unexplained reads as a broken app and gets re-enabled. And §8.4's guarantee is fixed copy **including its unflattering half** — it does *not* detect a self-consistent forgery at unreachable depth — since stating only the first half claims a guarantee the design declines to make. 5 mutations, 0 survived. **Import quotas landed 2026-08-05** (V-124) — four controls in one §8.4 sentence, three of them about *when*. **The snapshot format landed 2026-08-06**, as `bleavit.snapshot.v1` in **one module the producer and the consumer share** — they must agree on *which bytes are hashed* or the pin is decoration, and a second serializer fails every pin from that producer, which reads as a corrupt download. It reuses `handoff-envelope`'s canonical JSON under a distinct domain tag, because 10 §13.1 already gives this repository exactly one answer to *"which bytes"*. Screens are kept as **named classes** rather than one boolean so the forged corpus can be asserted per class (15 §4.8), and three of them invert the obvious implementation: the conservation replay checks **at every step** (a forger who drives an account negative and back is what a final-state check waves through); it deliberately does **not** assert I-1's cross-branch equality, which stops holding at the first `redeem` and would flag every settled vault; and `admitSnapshot` takes its hash function as a **required argument**, since an optional one is a content pin that defaults off. Amounts are decimal **strings** — `u128` base units run past 2⁵³ and `JSON.parse` would round them, after which the document fails its own conservation replay for reasons that look like forgery (V-74's shape). The `FE-PROV-001..004` family got its copy and recovery here, after 10 §8.4 was amended to bind 001 and 003 at all. **The forged corpus landed the same day** — 54 tests rejecting per **named class** (`malformed`, `canonical`, `pin`, `binding`, `coverage`, `conservation`, `derived-rows`), with **one document deliberately admitted**: a self-consistent deep forgery, which 10 §8.4 and 14 TH-50 both state is not detected, so a corpus made only of rejections would be evidence for a guarantee the mechanism declines to make. `admitSnapshot` takes the **file text** rather than a parsed object, which is the whole of the canonical-form check — §8.2's *"reproducible byte-identically by anyone"* is a claim about bytes, and a consumer that parsed first would accept two different files for one history. `check:types` carries the required-hash rule as a `@ts-expect-error` (an unused directive is itself an error, so it fails in both directions) after the negative-compilation corpus correctly refused it: declaring `@bleavit/providers` in `tests/firewall` turned `forbidden-package-edge.ts` from TS2307 to TS2305, since that fixture's proof is that the package is **unresolvable** there. **The sampling loop and the producer landed 2026-08-06**, and the second found a defect in the first. `sampling.ts` draws its sample from `random: () => number` — **a function of no argument**, so a selection seeded from the pages the provider served is unwritable rather than discouraged; that is the whole loop, because a sample the provider can predict is one it serves honest rows into for a cost of 1 row per 16 pages. It is **stratified** (one row per 16-page window, not N draws over the set, so a clustered forgery cannot hide in an unsampled region) and the count rounds **up** (rounding down lets any import under 16 pages verify nothing and report a clean round). `disabled` is **terminal in both arms** — §8.4 makes re-enabling an explicit user action, and the source that failed a sampling round is exactly the one whose next health probe succeeds — `Failing` counts **consecutive** failures, and `Slow` never disables. A check that **throws is `unverifiable` and the round continues**, because the reference is provider-supplied and a publisher who embeds one that reliably errors would otherwise discard the mismatches found earlier in the same round; a round where nothing was comparable is `inconclusive`, its own outcome rather than `clean`. **`app/tools/snapshot`** is the thin driver 10 §10.1 places under `app/`, and its one substantive decision is that `balances` are a **differential against chain state** rather than a restatement of the fold: a self-folded balance sheet agrees with itself by construction and makes §8.4's derived-row screen unable to fail on anything the tool emits, while the failure that actually happens is an **incomplete op set** — self-consistent and invisible to a self-fold. Coverage is what the reader **observed**, never the requested span. Every build ends by running the **client's own `admitSnapshot`** over the bytes it produced. Writing it exposed that the format's **arrays were not canonical** (see the Decision log): canonical JSON orders object keys and not array members, so `vaults`, `branches`, `balances` and `coverage` each had two legal spellings and two honest producers would have published two pins for one history. **The §8 wiring closed 2026-08-06** after an R-6 audit found 8 majors and 9 minors: every mechanism §8 mandates existed, and the *paths between them* did not. **`admitSnapshot` gave the wrong answer to the ordinary case** — it screened the chain binding with `equalBinding`, whose exact `specVersion`/`contractVersion` equality 10 §13.1 states for the three **handoff** formats, which describe one block. §8 mandates no chain binding at all and §6.4 assigns snapshots *"deep history beyond 30 days"*, which necessarily predates the current runtime, so the first upgrade refused every snapshot ever published — with `FE-PROV-003` copy telling the user to check a download that was never damaged. Narrowed to `genesisHash` (10 §7 gives one database per chain identity), and `FE-PROV-003` gained a **per-cause remedy** in the expert detail §10.4 already designates as the per-occurrence half: wrong chain, chain disagreement, or integrity. **§8.4's spot re-derivation is now implemented, injected and tested** — a named TH-50 mitigation that had no code, no injection point and no case. It walks **downward** from the newest covered block (the pinned window is at the head, §4.2), is bounded, and carries each block's **whole** movement list including when empty, because a checker asked only *"is this movement real"* catches fabrication and never deletion — the cheaper forgery, which replays and reconciles perfectly while overstating a holder's balance forever. `out-of-reach` is counted apart from `compared`, so a pass made entirely of unreachable blocks cannot read as evidence. **The import is streamed**: `importSnapshotStream` refuses at the chunk that crosses the byte bound *before* retaining it, counts rows once the document parses, runs the chain comparison, then shows the eviction preview as a **required** callback — so *"preview before import"* is a value a caller must supply rather than a step they can omit. The whole-text `admitSnapshot` stays for the CLI, and both admit the same documents. **The layer-3 mint is the only producer of a `provider` status**, takes a **branded** `AdmittedSnapshot` no other module can construct, and fixes `origin`/`providerId` at the mint site; `sampled` is true only when a block was actually **compared**. §8.1's in-release suggestions file ships typed, frozen and empty (no operator exists to curate — naming one would invent an address users send queries to), with the explicit accept returning provider **and** disclosure together. **The 15 §4.8 gate is now real**: `chainRowCheck` turns a served row into a verdict, and `tests/providers/lying-indexer.test.ts` drives it from the recorded chainHead transcripts through `@bleavit/mock-runtime` — before this, every case supplied a synthetic `RowCheck`, so what was certified was the ladder *given verdicts*, not that a lying indexer produces them. Minors closed: the 50 % `meaningful` floor is named in `LADDER`; the rate-taking sampling entry points moved behind `@bleavit/providers/testing` with a new `no-loosened-sampling-rate-in-production` rule **and** a barrel test, because the depcruise rule sees a subpath import and not a re-export (that mutation survived everything else); `diffSnapshots` gained a third `no-overlap` discriminant so vacuous agreement cannot read as agreement; `FE-PROV-001`'s unreachable arm is decided and documented (SQ-609); `tests/firewall/units.test.ts` compares each unit manifest against 10 §10.2's own parsed reference sets — which immediately found `handoff-envelope` missing from §10.2's handoff set while the unit imports it (spec amended, Decision log); and `ArchiveExport` is published as a generated JSON Schema. **22 mutations, 0 survived.** **Remaining:** the **archive-node adapter** stays unwritten, and its park is now stated with the operative reason — not 10 §4.2, which constrains smoldot *inside the browser* and does not reach a Node CLI, but that **no document names which archive read interface, endpoint, pagination or historical-metadata policy it binds to** (SQ-612). The **live-indexer HTTP interface and `optional/indexer/`** are likewise unwritten and out of F9's scope (SQ-613): no document specifies routes, page shape or row identity, so writing one means inventing it. Those two, plus **INV-FE-3's actionable-object chain fetch** (a screen-level obligation with no screen yet), **must move to a named follow-up milestone explicitly** rather than being carried in this row; SQ-614 asks which milestone owns them. #247 merged; what is left on this row is the R-6 sign-off on the **narrowed** Spec column, which is what the re-review named as the route to ✅. **The R-6 re-review's remaining findings closed 2026-08-06, and four of them were one defect wearing four faces: a brand that certified the wrong check.** `admitSnapshot`'s brand covers the *file screens*, so the mint accepted a hand-written `{ compared: 1, findings: [] }` beside it and badged the rows `sampled: true` — a claim this device compared a block against the chain, made by a caller that never did — and it ignored `findings` outright, so a report that had **caught** the document contradicting the chain minted exactly what a clean one did. Both arms now take evidence only the checking function can produce: a branded `SpotCheckReport` that must name **this** document by reference and carry no findings, and a branded `SampledRound` for the indexer arm, which had been taking the snapshot arm's report type where §8.4 gives indexers *sampling* and snapshots *screens*. The indexer arm also stopped minting a `snapshotsImported` record keyed by a string the call site invented — an indexer pins no file. **§8.4's spot re-derivation gained the ordering-and-comparison half of a chain-side producer — not the producer, and this row said otherwise until the R-6 re-review**: `chainSpotCheck(read)` derives a block's movement list from observed events, imposes §8.2's chain order (the half `bleavit.snapshot.v1` cannot express, SQ-840), refuses two events at one position and a movement from another block, and compares on length as well as content so **deletion** is caught and not only fabrication. It is driven from the recorded chainHead transcripts through `@bleavit/mock-runtime`, exactly as `lying-indexer.test.ts` drives the sampler — the recording answers for one pinned block whose events carry no ledger movement, which is the fabrication direction end to end and is stated rather than implied for the direction it cannot drive. **A publisher caught contradicting the chain now stops serving**: the refusal routes through the same `afterSampling` the sampler uses (`FE-PROV-002` beside the file's `FE-PROV-003`), while a damaged download, a wrong-chain document and an unfinished check leave it untouched — only a disagreement is evidence about the publisher (SQ-630 asks §8.4 to confirm the scope). **The barrel hole is closed in all four packages** by one shared helper that takes the whole `/testing` **namespace** and requires it disjoint from the barrel, with anti-vacuity on both sides: F9's own test had enumerated two names, so a third loosened export would have walked past it, and `chain-client`, `local-index` and `signing` were each defended by a comment. **Four `chatgpt-codex-connector` findings on #256 were triaged in the same pass and all four confirmed**: the mint binding above; `acceptSuggestion` returning a provider already `healthy` before anything probed it (now an `unprobed` state that **cannot serve reads**, INV-FE-12's fail-closed lattice rather than `probeDue(null, …)` answering *due* while nothing stops a caller reading first); the re-derivation stopping at an invented 128 blocks and **admitting**, where 10 §4.2 puts peer pruning at ~256 — it now walks until the checker says `out-of-reach` (and not before its first comparison, since a document ahead of this device's head answers unreachable at the top), with a ceiling derived as 2 × §4.2's figure whose only effect is a **refusal**; and the row quota, which was metered after the parse so *streamed* held for bytes and not rows — now metered per chunk by an upper bound (`{` count) that can over-count and never under-count. Minors: the content pin is taken over **the file's bytes** rather than a re-serialization (measured — a file with one space added hashed to the publisher's pin and the message named a hash the file does not have), the canonical-form comparison streams through `handoff-envelope`'s `canonicalJsonInto` instead of building a second 400 MB document, `meaningfulAtLeast: 0.5` is **deleted** rather than documented (an invented sufficiency line §8.3's release-constant licence does not reach — the report is three numbers and no verdict, SQ-633), `ProviderCoverageRow` is gone in favour of §6.3's own `CoverageRange` **imported as a type** (a value import would pull Dexie into this package's graph and falsify the claim beside it), `SAMPLING_GUARANTEE` is bound clause-by-clause to §8.4's own sentence extracted from the document, and the empty-provider-id refusal moved to the **first line** of the importer — it used to throw a bare `RangeError` from the mint, after the user had agreed to evict. The byte quota is now the **caller's** bound, capped by §8.4's 400 MB and required with no default, because 400 MB of input is not 400 MB of memory (SQ-632). **27 mutations, 0 survived.** **The R-6 spec-compliance review of the closed row returned 1 blocker and 4 majors on 2026-08-06; the blocker and two majors are fixed here and the rest are written down rather than quietly carried.** **The blocker was the deep-history case — the format's primary use — being *rejected*.** §8.4 scopes re-derivation to *"the covered blocks that fall inside light-client-reachable depth"* and names the rest a **disclosed** limit; §6.4 assigns snapshots *"deep history beyond 30 days … by design, not by omission"*. The walk had one undirected `out-of-reach` verdict and inferred the direction from whether anything had been compared yet, so a document entirely **below** the window — every covered block unreachable, which is the ordinary case — was read as one entirely **above** it: it spent all 512 asks on blocks whose answer was already known, hit the work ceiling, raised `spot-check-incomplete` and returned `FE-PROV-003`. Measured end to end on the shipped code: a valid 216,000-block snapshot **refused**, on a cause §8.4's own table does not list and against INV-FE-15's acceleration obligation. The fix gives the verdict a **direction** (`OutOfReachSide`, `above-window` | `below-window`) rather than a comment: `below-window` ends the walk as **finished** (the mandated set is empty and the document is admitted with `compared: 0` and no finding), `above-window` keeps descending, and the ceiling still refuses. The side comes from the checker, which is this device's own light client and never the publisher, and it is carried through `chainSpotCheck` rather than re-decided there. `SpotCheckReport` gained **`reach`** (`whole-document` | `window-floor` | `ceiling`) so the disclosure is a stated fact: `compared: 0` alone is produced both by the blind spot and by a pass that ran out of ceiling, and those are opposite outcomes. Two minors fell out and are fixed with it — the incomplete-check sentence claimed *"the chain was still answering"*, false in exactly the case that reached it, and the auto-disable denominator counted blocks it had not re-derived (`compared + outOfReach`), so one caught lie in the one readable block was reported as *1 of 2*. **The corpus was structurally blind to it**: the deep-forgery case used a 4-block document, so the ceiling was never reached, and the ceiling case answered `agrees`. **Major 2 — `canServeReads` was consulted by nothing**, in a package whose recurring defect is a check that measures nothing. It is now wired, and wiring it made its exact shape load-bearing, which exposed a narrowing the review did not name: it excluded **`failing`**, and §8.3's normative shape says *"only `Disabled` stops reads"* — the same ratchet the *consecutive* rule exists to prevent, one clause along. `failing` and `slow` now serve; `unprobed` does not, because it is **before** the ladder rather than a state on it (INV-FE-12's fail-closed lattice), and which of §8.3's two sentences governs that gap is SQ-771. The predicate is an exhaustive `switch`, so a sixth state cannot default to *serves*. `runSamplingRound` is gated on it — satisfiable, since a caller holding pages the source served performed the request that produced them and advances the ladder with `afterProbe` first, which makes §8.3's *"probe on enable"* structural rather than scheduled — and `ProviderDisabledError` became `ProviderCannotServeError`, since a class named *disabled* would misname the more common arm. The **import** is gated on a second, narrower predicate (`canSupplyPinnedImport`, `disabled` only): a pinned file the user already holds is admitted by its content hash and the §8.4 screens, none of which asks an endpoint anything, and gating it on a probe **nothing in this release drives** would refuse every import from a freshly accepted suggestion forever — a control that is always on is the sibling defect, not the fix (SQ-773). That closed a real hole in the same edit: an import from a source auto-disabled for contradicting the chain used to succeed and mint rows badged with its id, while `FE-PROV-002`'s recovery was on screen saying it *"has been switched off"*. `fleetState` now decides all-down **through** `allProvidersDown` instead of beside it; that predicate had no caller, and its `all-down`/`none-enabled` arms had **no test at all** — a second finding beyond the review. **§8.3's probe driver is owned by F24**, named rather than implied. **Written down, not fixed** (the review's majors 3 and 5 and its minors): `BlockMovementRead` has **no production producer** — nothing turns `System.Events` into `SnapshotOp`s, and every caller is a test closure — and the same holds for `ChainRead` behind `chainRowCheck`, so the row's earlier claim above is corrected and **F23** owns both readers; `SAMPLING_GUARANTEE` renders §8.4's *"which the import UI supports and recommends"* as *"supports and labels"* and the clause binding does not extract that clause, so the substitution cannot fail the gate; `selectSample` and `livenessRefusal` are barrel-exported with no caller; and §8.2's chain order is enforced only at **block** granularity because `bleavit.snapshot.v1` discards the extrinsic and event indices (SQ-840 owns it). Four spec questions raised: SQ-770 (is *"spot"* re-derivation exhaustive or a sample), SQ-771 (§8.3's two sentences and the enable gap), SQ-772 (§8.4's 400 MB and doc 10's §9-scoped 10⁶ convention — `import-quota.ts` keeps the conservative reading), SQ-773 (does §8.3's health gate an out-of-band pinned file at all). **11 mutations, 0 survived** (two first written in forms `tsc` rejected and re-run as forms that compile). **An adversarial R-6 re-review of that pass returned 0 blockers and 4 majors on 2026-08-06; all four are closed here.** **M-1 — the ceiling refused a valid snapshot on a cause §8.4 does not list, and it is the fixed blocker's own defect class with a narrower trigger.** Two live configurations reach the work ceiling with nothing wrong with the file: a device more than 512 blocks **behind** the document's newest covered block, and a document with more than 512 **reachable** covered blocks — and for the second the remedy copy (*"try again when this device has caught up"*) was **false**, so the refusal was permanent. The ceiling now **admits** with `reach: 'ceiling'`, exactly as `window-floor` already did, and the mint refuses to badge the rows `sampled` however many blocks agreed; the `spot-check-incomplete` finding class and the `incomplete-check` remedy are **deleted** rather than left unproduced, since every remaining `FE-PROV-003` cause is then a statement about the **document** and none about this device. **The ceiling's disposition is its own, not the blocker's** — SQ-811 asks §8.4 whether an unfinished re-derivation refuses or discloses, and a ruling toward refusal restores both. **M-2 — the guess moved instead of going away, and its failure direction flipped.** `OutOfReachSide`'s correctness is delegated to `BlockMovementRead`, which has **no production producer** (every caller is a test closure) and which F23 owns; nothing stated the two properties the walk depends on — that the reachable set is one contiguous interval at the head, and that `below-window` is returned only below the pinned floor. The old code guessed and failed toward **refusal**; the new code trusts and fails toward **admission**, and a reader answering `below-window` for an above-head block yields an admitted document with `compared: 0`, no finding, and the blind-spot disclosure. Fixed in two places: F23's row carries the reader's contract as an explicit obligation, and F9 ships **`checkBlockMovementRead`** behind `@bleavit/providers/testing` — an executable contract sweeping `floor-2 … head+2` that checks both properties plus head/floor anchoring, `chainSpotCheck`'s three throw conditions, determinism on re-ask, and an out-of-enum side, and that **refuses** a window it cannot exercise on both edges or one too wide to sweep rather than sampling it. Its suite is 16 cases, one positive control and fifteen deliberately-broken readers, so the harness is proven non-vacuous per violation class; the false-clean case is additionally measured end to end through `chainSpotCheck`. **M-3 — `SpotCheckReach` had three arms and four producing situations.** A document entirely **above** this device's head and smaller than the ceiling asked about every covered block, compared none, raised no finding and returned `whole-document`, whose own description says *"the walk ran out of document, not out of window"* — false in exactly that case, and a screen reading that arm as *fully re-derived* was wrong for a pass that verified nothing. A fourth arm **`above-window-only`** is added rather than folding it into `window-floor`, because the two disclose opposite things: `window-floor` is permanent (no sync brings that history back, and the second-producer diff is the only cross-check there will ever be) and this is transient (the device is behind). The corpus had no case for it; it has three now, including the empty-document boundary that keeps `above-window-only` from meaning merely *compared nothing*. **M-4 — the normative UI copy was substituted and the gate was scoped around it.** `SAMPLING_GUARANTEE` rendered §8.4's *"which the import UI supports and **recommends**"* as *"supports and labels"* — a third verb belonging to INV-FE-15's origin-labelling obligation — and the clause-by-clause binding extracted the four clauses around it and not this one, so the substitution could not fail. **The spec question under it was ruled first rather than picked silently**: doc 10 spelled the same clause two ways (§8.4 *recommends*, §2.3 *discloses*) and a client shipping one fixed string can satisfy only one, so §2.3 was amended to §8.4's verb under R-1 (Decision log). The clause is now rendered, bound, and a second test asserts **both** sections spell it the same way. Also fixed: `where` is validated against its union at both injected boundaries (an out-of-enum value read as `above-window` and ended in **admission**, while its two neighbours `extrinsicIndex`/`eventIndex` were already hardened); `FleetState.serving` is re-documented as a **permission** rather than *"sources that have answered a probe"* — false since `failing` began serving — and gains a `failing` count beside it, so a fleet answering nothing cannot render as *N serving*; `livenessRefusal`'s note is corrected, because `all-down` requires every source `disabled` and therefore no longer fires for a fleet that is merely unreachable, which makes §8.4's first `FE-PROV-001` arm **wider** than when SQ-609 recorded it; and `canServeReads`'s authority is re-cited from **INV-FE-12** (whose subject is an unknown *runtime* surface) to INV-FE-3 and INV-FE-15 plus SQ-771. Three spec questions raised: SQ-810, SQ-811, SQ-812. **21 mutants, 0 survived** — 18 behaviours, of which 3 were first written in forms `tsc` rejected and re-run as forms that compile. **Remaining:** the **archive-node adapter** is deliberately unwritten — R-2 forbids resolving it by assumption and the client's own `chain-client` offers no archive path (smoldot serves `chainHead` only, 10 §4.2) — so the driver is pure over an injected export and the CLI reads one from disk; plus the R-6 spec-compliance review and #247's merge. **✅ 2026-08-07.** R-6's two conjuncts are both satisfied: the R-6 review recorded above returned no blocker, and the gate evidence is PR #261 — all **23** checks `success` on `76477e3f`, squash-merged to `main` as `34cbf7a4`.

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track F — The canonical cross-platform client (`app/`) | -->

<!-- source row: | F9 | FE-7 `packages/providers` + `tools/snapshot` — optional acceleration, sampling, auto-disable | 10 §8.2–§8.4 (the mechanisms: format, screens, sampling, ladder, quotas); 10 §8.1's in-release list. **Three exclusions, each named with an owner**, because a section cited whole with unnamed exclusions makes the narrowing unfalsifiable: the rendered half of §8 is **F23**; §8.2's first sentence names two artifacts — the *"against an archive node"* reader behind `tools/snapshot` (SQ-612) and the live-indexer HTTP interface with its *"reference implementation in `optional/indexer/`"* (SQ-613) — and those, together with §8.3's *"on enable + every 10 min"* probe driver, are **F24** | F8 | ✅ | INV-FE-3/15. Scaffold empty at F0 so the firewall is non-vacuous **Started 2026-08-05 with the health and sampling model.** The never-promote rule needed no code here and gets none: `Finalized<T>` is unnameable outside `chain-client`, so this package **cannot** produce one whatever it does — 10 §2.2 is enforced by a symbol it cannot reach. What F9 owns is what the client **says** about a provider, which is where the failures live. `defaultProviders()` returns empty **from a function taking no argument**, so there is nothing for a build or a previous session to inherit a default from — a configurable default is how *opt-in* quietly becomes *on for most people*, and it is the same device `defaultScope()` uses. **Any** mismatch auto-disables: §8.3 sets no threshold, and a threshold is what turns one caught lie into a tolerated error rate. **Unverifiable rows count neither way** — a row whose object is gone proves nothing, and counting it as a pass would let a provider evade sampling by serving only unverifiable rows, the cheapest available evasion; `effectiveCoverage` reports how much of a round was **actually comparable**, so a 2-of-100 check cannot read as a 100-row one. The auto-disable **reason is a required field**, because a source that vanishes unexplained reads as a broken app and gets re-enabled. And §8.4's guarantee is fixed copy **including its unflattering half** — it does *not* detect a self-consistent forgery at unreachable depth — since stating only the first half claims a guarantee the design declines to make. 5 mutations, 0 survived. **Import quotas landed 2026-08-05** (V-124) — four controls in one §8.4 sentence, three of them about *when*. **The snapshot format landed 2026-08-06**, as `bleavit.snapshot.v1` in **one module the producer and the consumer share** — they must agree on *which bytes are hashed* or the pin is decoration, and a second serializer fails every pin from that producer, which reads as a corrupt download. It reuses `handoff-envelope`'s canonical JSON under a distinct domain tag, because 10 §13.1 already gives this repository exactly one answer to *"which bytes"*. Screens are kept as **named classes** rather than one boolean so the forged corpus can be asserted per class (15 §4.8), and three of them invert the obvious implementation: the conservation replay checks **at every step** (a forger who drives an account negative and back is what a final-state check waves through); it deliberately does **not** assert I-1's cross-branch equality, which stops holding at the first `redeem` and would flag every settled vault; and `admitSnapshot` takes its hash function as a **required argument**, since an optional one is a content pin that defaults off. Amounts are decimal **strings** — `u128` base units run past 2⁵³ and `JSON.parse` would round them, after which the document fails its own conservation replay for reasons that look like forgery (V-74's shape). The `FE-PROV-001..004` family got its copy and recovery here, after 10 §8.4 was amended to bind 001 and 003 at all. **The forged corpus landed the same day** — 54 tests rejecting per **named class** (`malformed`, `canonical`, `pin`, `binding`, `coverage`, `conservation`, `derived-rows`), with **one document deliberately admitted**: a self-consistent deep forgery, which 10 §8.4 and 14 TH-50 both state is not detected, so a corpus made only of rejections would be evidence for a guarantee the mechanism declines to make. `admitSnapshot` takes the **file text** rather than a parsed object, which is the whole of the canonical-form check — §8.2's *"reproducible byte-identically by anyone"* is a claim about bytes, and a consumer that parsed first would accept two different files for one history. `check:types` carries the required-hash rule as a `@ts-expect-error` (an unused directive is itself an error, so it fails in both directions) after the negative-compilation corpus correctly refused it: declaring `@bleavit/providers` in `tests/firewall` turned `forbidden-package-edge.ts` from TS2307 to TS2305, since that fixture's proof is that the package is **unresolvable** there. **The sampling loop and the producer landed 2026-08-06**, and the second found a defect in the first. `sampling.ts` draws its sample from `random: () => number` — **a function of no argument**, so a selection seeded from the pages the provider served is unwritable rather than discouraged; that is the whole loop, because a sample the provider can predict is one it serves honest rows into for a cost of 1 row per 16 pages. It is **stratified** (one row per 16-page window, not N draws over the set, so a clustered forgery cannot hide in an unsampled region) and the count rounds **up** (rounding down lets any import under 16 pages verify nothing and report a clean round). `disabled` is **terminal in both arms** — §8.4 makes re-enabling an explicit user action, and the source that failed a sampling round is exactly the one whose next health probe succeeds — `Failing` counts **consecutive** failures, and `Slow` never disables. A check that **throws is `unverifiable` and the round continues**, because the reference is provider-supplied and a publisher who embeds one that reliably errors would otherwise discard the mismatches found earlier in the same round; a round where nothing was comparable is `inconclusive`, its own outcome rather than `clean`. **`app/tools/snapshot`** is the thin driver 10 §10.1 places under `app/`, and its one substantive decision is that `balances` are a **differential against chain state** rather than a restatement of the fold: a self-folded balance sheet agrees with itself by construction and makes §8.4's derived-row screen unable to fail on anything the tool emits, while the failure that actually happens is an **incomplete op set** — self-consistent and invisible to a self-fold. Coverage is what the reader **observed**, never the requested span. Every build ends by running the **client's own `admitSnapshot`** over the bytes it produced. Writing it exposed that the format's **arrays were not canonical** (see the Decision log): canonical JSON orders object keys and not array members, so `vaults`, `branches`, `balances` and `coverage` each had two legal spellings and two honest producers would have published two pins for one history. **The §8 wiring closed 2026-08-06** after an R-6 audit found 8 majors and 9 minors: every mechanism §8 mandates existed, and the *paths between them* did not. **`admitSnapshot` gave the wrong answer to the ordinary case** — it screened the chain binding with `equalBinding`, whose exact `specVersion`/`contractVersion` equality 10 §13.1 states for the three **handoff** formats, which describe one block. §8 mandates no chain binding at all and §6.4 assigns snapshots *"deep history beyond 30 days"*, which necessarily predates the current runtime, so the first upgrade refused every snapshot ever published — with `FE-PROV-003` copy telling the user to check a download that was never damaged. Narrowed to `genesisHash` (10 §7 gives one database per chain identity), and `FE-PROV-003` gained a **per-cause remedy** in the expert detail §10.4 already designates as the per-occurrence half: wrong chain, chain disagreement, or integrity. **§8.4's spot re-derivation is now implemented, injected and tested** — a named TH-50 mitigation that had no code, no injection point and no case. It walks **downward** from the newest covered block (the pinned window is at the head, §4.2), is bounded, and carries each block's **whole** movement list including when empty, because a checker asked only *"is this movement real"* catches fabrication and never deletion — the cheaper forgery, which replays and reconciles perfectly while overstating a holder's balance forever. `out-of-reach` is counted apart from `compared`, so a pass made entirely of unreachable blocks cannot read as evidence. **The import is streamed**: `importSnapshotStream` refuses at the chunk that crosses the byte bound *before* retaining it, counts rows once the document parses, runs the chain comparison, then shows the eviction preview as a **required** callback — so *"preview before import"* is a value a caller must supply rather than a step they can omit. The whole-text `admitSnapshot` stays for the CLI, and both admit the same documents. **The layer-3 mint is the only producer of a `provider` status**, takes a **branded** `AdmittedSnapshot` no other module can construct, and fixes `origin`/`providerId` at the mint site; `sampled` is true only when a block was actually **compared**. §8.1's in-release suggestions file ships typed, frozen and empty (no operator exists to curate — naming one would invent an address users send queries to), with the explicit accept returning provider **and** disclosure together. **The 15 §4.8 gate is now real**: `chainRowCheck` turns a served row into a verdict, and `tests/providers/lying-indexer.test.ts` drives it from the recorded chainHead transcripts through `@bleavit/mock-runtime` — before this, every case supplied a synthetic `RowCheck`, so what was certified was the ladder *given verdicts*, not that a lying indexer produces them. Minors closed: the 50 % `meaningful` floor is named in `LADDER`; the rate-taking sampling entry points moved behind `@bleavit/providers/testing` with a new `no-loosened-sampling-rate-in-production` rule **and** a barrel test, because the depcruise rule sees a subpath import and not a re-export (that mutation survived everything else); `diffSnapshots` gained a third `no-overlap` discriminant so vacuous agreement cannot read as agreement; `FE-PROV-001`'s unreachable arm is decided and documented (SQ-609); `tests/firewall/units.test.ts` compares each unit manifest against 10 §10.2's own parsed reference sets — which immediately found `handoff-envelope` missing from §10.2's handoff set while the unit imports it (spec amended, Decision log); and `ArchiveExport` is published as a generated JSON Schema. **22 mutations, 0 survived.** **Remaining:** the **archive-node adapter** stays unwritten, and its park is now stated with the operative reason — not 10 §4.2, which constrains smoldot *inside the browser* and does not reach a Node CLI, but that **no document names which archive read interface, endpoint, pagination or historical-metadata policy it binds to** (SQ-612). The **live-indexer HTTP interface and `optional/indexer/`** are likewise unwritten and out of F9's scope (SQ-613): no document specifies routes, page shape or row identity, so writing one means inventing it. Those two, plus **INV-FE-3's actionable-object chain fetch** (a screen-level obligation with no screen yet), **must move to a named follow-up milestone explicitly** rather than being carried in this row; SQ-614 asks which milestone owns them. #247 merged; what is left on this row is the R-6 sign-off on the **narrowed** Spec column, which is what the re-review named as the route to ✅. **The R-6 re-review's remaining findings closed 2026-08-06, and four of them were one defect wearing four faces: a brand that certified the wrong check.** `admitSnapshot`'s brand covers the *file screens*, so the mint accepted a hand-written `{ compared: 1, findings: [] }` beside it and badged the rows `sampled: true` — a claim this device compared a block against the chain, made by a caller that never did — and it ignored `findings` outright, so a report that had **caught** the document contradicting the chain minted exactly what a clean one did. Both arms now take evidence only the checking function can produce: a branded `SpotCheckReport` that must name **this** document by reference and carry no findings, and a branded `SampledRound` for the indexer arm, which had been taking the snapshot arm's report type where §8.4 gives indexers *sampling* and snapshots *screens*. The indexer arm also stopped minting a `snapshotsImported` record keyed by a string the call site invented — an indexer pins no file. **§8.4's spot re-derivation gained the ordering-and-comparison half of a chain-side producer — not the producer, and this row said otherwise until the R-6 re-review**: `chainSpotCheck(read)` derives a block's movement list from observed events, imposes §8.2's chain order (the half `bleavit.snapshot.v1` cannot express, SQ-840), refuses two events at one position and a movement from another block, and compares on length as well as content so **deletion** is caught and not only fabrication. It is driven from the recorded chainHead transcripts through `@bleavit/mock-runtime`, exactly as `lying-indexer.test.ts` drives the sampler — the recording answers for one pinned block whose events carry no ledger movement, which is the fabrication direction end to end and is stated rather than implied for the direction it cannot drive. **A publisher caught contradicting the chain now stops serving**: the refusal routes through the same `afterSampling` the sampler uses (`FE-PROV-002` beside the file's `FE-PROV-003`), while a damaged download, a wrong-chain document and an unfinished check leave it untouched — only a disagreement is evidence about the publisher (SQ-630 asks §8.4 to confirm the scope). **The barrel hole is closed in all four packages** by one shared helper that takes the whole `/testing` **namespace** and requires it disjoint from the barrel, with anti-vacuity on both sides: F9's own test had enumerated two names, so a third loosened export would have walked past it, and `chain-client`, `local-index` and `signing` were each defended by a comment. **Four `chatgpt-codex-connector` findings on #256 were triaged in the same pass and all four confirmed**: the mint binding above; `acceptSuggestion` returning a provider already `healthy` before anything probed it (now an `unprobed` state that **cannot serve reads**, INV-FE-12's fail-closed lattice rather than `probeDue(null, …)` answering *due* while nothing stops a caller reading first); the re-derivation stopping at an invented 128 blocks and **admitting**, where 10 §4.2 puts peer pruning at ~256 — it now walks until the checker says `out-of-reach` (and not before its first comparison, since a document ahead of this device's head answers unreachable at the top), with a ceiling derived as 2 × §4.2's figure whose only effect is a **refusal**; and the row quota, which was metered after the parse so *streamed* held for bytes and not rows — now metered per chunk by an upper bound (`{` count) that can over-count and never under-count. Minors: the content pin is taken over **the file's bytes** rather than a re-serialization (measured — a file with one space added hashed to the publisher's pin and the message named a hash the file does not have), the canonical-form comparison streams through `handoff-envelope`'s `canonicalJsonInto` instead of building a second 400 MB document, `meaningfulAtLeast: 0.5` is **deleted** rather than documented (an invented sufficiency line §8.3's release-constant licence does not reach — the report is three numbers and no verdict, SQ-633), `ProviderCoverageRow` is gone in favour of §6.3's own `CoverageRange` **imported as a type** (a value import would pull Dexie into this package's graph and falsify the claim beside it), `SAMPLING_GUARANTEE` is bound clause-by-clause to §8.4's own sentence extracted from the document, and the empty-provider-id refusal moved to the **first line** of the importer — it used to throw a bare `RangeError` from the mint, after the user had agreed to evict. The byte quota is now the **caller's** bound, capped by §8.4's 400 MB and required with no default, because 400 MB of input is not 400 MB of memory (SQ-632). **27 mutations, 0 survived.** **The R-6 spec-compliance review of the closed row returned 1 blocker and 4 majors on 2026-08-06; the blocker and two majors are fixed here and the rest are written down rather than quietly carried.** **The blocker was the deep-history case — the format's primary use — being *rejected*.** §8.4 scopes re-derivation to *"the covered blocks that fall inside light-client-reachable depth"* and names the rest a **disclosed** limit; §6.4 assigns snapshots *"deep history beyond 30 days … by design, not by omission"*. The walk had one undirected `out-of-reach` verdict and inferred the direction from whether anything had been compared yet, so a document entirely **below** the window — every covered block unreachable, which is the ordinary case — was read as one entirely **above** it: it spent all 512 asks on blocks whose answer was already known, hit the work ceiling, raised `spot-check-incomplete` and returned `FE-PROV-003`. Measured end to end on the shipped code: a valid 216,000-block snapshot **refused**, on a cause §8.4's own table does not list and against INV-FE-15's acceleration obligation. The fix gives the verdict a **direction** (`OutOfReachSide`, `above-window` \| `below-window`) rather than a comment: `below-window` ends the walk as **finished** (the mandated set is empty and the document is admitted with `compared: 0` and no finding), `above-window` keeps descending, and the ceiling still refuses. The side comes from the checker, which is this device's own light client and never the publisher, and it is carried through `chainSpotCheck` rather than re-decided there. `SpotCheckReport` gained **`reach`** (`whole-document` \| `window-floor` \| `ceiling`) so the disclosure is a stated fact: `compared: 0` alone is produced both by the blind spot and by a pass that ran out of ceiling, and those are opposite outcomes. Two minors fell out and are fixed with it — the incomplete-check sentence claimed *"the chain was still answering"*, false in exactly the case that reached it, and the auto-disable denominator counted blocks it had not re-derived (`compared + outOfReach`), so one caught lie in the one readable block was reported as *1 of 2*. **The corpus was structurally blind to it**: the deep-forgery case used a 4-block document, so the ceiling was never reached, and the ceiling case answered `agrees`. **Major 2 — `canServeReads` was consulted by nothing**, in a package whose recurring defect is a check that measures nothing. It is now wired, and wiring it made its exact shape load-bearing, which exposed a narrowing the review did not name: it excluded **`failing`**, and §8.3's normative shape says *"only `Disabled` stops reads"* — the same ratchet the *consecutive* rule exists to prevent, one clause along. `failing` and `slow` now serve; `unprobed` does not, because it is **before** the ladder rather than a state on it (INV-FE-12's fail-closed lattice), and which of §8.3's two sentences governs that gap is SQ-771. The predicate is an exhaustive `switch`, so a sixth state cannot default to *serves*. `runSamplingRound` is gated on it — satisfiable, since a caller holding pages the source served performed the request that produced them and advances the ladder with `afterProbe` first, which makes §8.3's *"probe on enable"* structural rather than scheduled — and `ProviderDisabledError` became `ProviderCannotServeError`, since a class named *disabled* would misname the more common arm. The **import** is gated on a second, narrower predicate (`canSupplyPinnedImport`, `disabled` only): a pinned file the user already holds is admitted by its content hash and the §8.4 screens, none of which asks an endpoint anything, and gating it on a probe **nothing in this release drives** would refuse every import from a freshly accepted suggestion forever — a control that is always on is the sibling defect, not the fix (SQ-773). That closed a real hole in the same edit: an import from a source auto-disabled for contradicting the chain used to succeed and mint rows badged with its id, while `FE-PROV-002`'s recovery was on screen saying it *"has been switched off"*. `fleetState` now decides all-down **through** `allProvidersDown` instead of beside it; that predicate had no caller, and its `all-down`/`none-enabled` arms had **no test at all** — a second finding beyond the review. **§8.3's probe driver is owned by F24**, named rather than implied. **Written down, not fixed** (the review's majors 3 and 5 and its minors): `BlockMovementRead` has **no production producer** — nothing turns `System.Events` into `SnapshotOp`s, and every caller is a test closure — and the same holds for `ChainRead` behind `chainRowCheck`, so the row's earlier claim above is corrected and **F23** owns both readers; `SAMPLING_GUARANTEE` renders §8.4's *"which the import UI supports and recommends"* as *"supports and labels"* and the clause binding does not extract that clause, so the substitution cannot fail the gate; `selectSample` and `livenessRefusal` are barrel-exported with no caller; and §8.2's chain order is enforced only at **block** granularity because `bleavit.snapshot.v1` discards the extrinsic and event indices (SQ-840 owns it). Four spec questions raised: SQ-770 (is *"spot"* re-derivation exhaustive or a sample), SQ-771 (§8.3's two sentences and the enable gap), SQ-772 (§8.4's 400 MB and doc 10's §9-scoped 10⁶ convention — `import-quota.ts` keeps the conservative reading), SQ-773 (does §8.3's health gate an out-of-band pinned file at all). **11 mutations, 0 survived** (two first written in forms `tsc` rejected and re-run as forms that compile). **An adversarial R-6 re-review of that pass returned 0 blockers and 4 majors on 2026-08-06; all four are closed here.** **M-1 — the ceiling refused a valid snapshot on a cause §8.4 does not list, and it is the fixed blocker's own defect class with a narrower trigger.** Two live configurations reach the work ceiling with nothing wrong with the file: a device more than 512 blocks **behind** the document's newest covered block, and a document with more than 512 **reachable** covered blocks — and for the second the remedy copy (*"try again when this device has caught up"*) was **false**, so the refusal was permanent. The ceiling now **admits** with `reach: 'ceiling'`, exactly as `window-floor` already did, and the mint refuses to badge the rows `sampled` however many blocks agreed; the `spot-check-incomplete` finding class and the `incomplete-check` remedy are **deleted** rather than left unproduced, since every remaining `FE-PROV-003` cause is then a statement about the **document** and none about this device. **The ceiling's disposition is its own, not the blocker's** — SQ-811 asks §8.4 whether an unfinished re-derivation refuses or discloses, and a ruling toward refusal restores both. **M-2 — the guess moved instead of going away, and its failure direction flipped.** `OutOfReachSide`'s correctness is delegated to `BlockMovementRead`, which has **no production producer** (every caller is a test closure) and which F23 owns; nothing stated the two properties the walk depends on — that the reachable set is one contiguous interval at the head, and that `below-window` is returned only below the pinned floor. The old code guessed and failed toward **refusal**; the new code trusts and fails toward **admission**, and a reader answering `below-window` for an above-head block yields an admitted document with `compared: 0`, no finding, and the blind-spot disclosure. Fixed in two places: F23's row carries the reader's contract as an explicit obligation, and F9 ships **`checkBlockMovementRead`** behind `@bleavit/providers/testing` — an executable contract sweeping `floor-2 … head+2` that checks both properties plus head/floor anchoring, `chainSpotCheck`'s three throw conditions, determinism on re-ask, and an out-of-enum side, and that **refuses** a window it cannot exercise on both edges or one too wide to sweep rather than sampling it. Its suite is 16 cases, one positive control and fifteen deliberately-broken readers, so the harness is proven non-vacuous per violation class; the false-clean case is additionally measured end to end through `chainSpotCheck`. **M-3 — `SpotCheckReach` had three arms and four producing situations.** A document entirely **above** this device's head and smaller than the ceiling asked about every covered block, compared none, raised no finding and returned `whole-document`, whose own description says *"the walk ran out of document, not out of window"* — false in exactly that case, and a screen reading that arm as *fully re-derived* was wrong for a pass that verified nothing. A fourth arm **`above-window-only`** is added rather than folding it into `window-floor`, because the two disclose opposite things: `window-floor` is permanent (no sync brings that history back, and the second-producer diff is the only cross-check there will ever be) and this is transient (the device is behind). The corpus had no case for it; it has three now, including the empty-document boundary that keeps `above-window-only` from meaning merely *compared nothing*. **M-4 — the normative UI copy was substituted and the gate was scoped around it.** `SAMPLING_GUARANTEE` rendered §8.4's *"which the import UI supports and **recommends**"* as *"supports and labels"* — a third verb belonging to INV-FE-15's origin-labelling obligation — and the clause-by-clause binding extracted the four clauses around it and not this one, so the substitution could not fail. **The spec question under it was ruled first rather than picked silently**: doc 10 spelled the same clause two ways (§8.4 *recommends*, §2.3 *discloses*) and a client shipping one fixed string can satisfy only one, so §2.3 was amended to §8.4's verb under R-1 (Decision log). The clause is now rendered, bound, and a second test asserts **both** sections spell it the same way. Also fixed: `where` is validated against its union at both injected boundaries (an out-of-enum value read as `above-window` and ended in **admission**, while its two neighbours `extrinsicIndex`/`eventIndex` were already hardened); `FleetState.serving` is re-documented as a **permission** rather than *"sources that have answered a probe"* — false since `failing` began serving — and gains a `failing` count beside it, so a fleet answering nothing cannot render as *N serving*; `livenessRefusal`'s note is corrected, because `all-down` requires every source `disabled` and therefore no longer fires for a fleet that is merely unreachable, which makes §8.4's first `FE-PROV-001` arm **wider** than when SQ-609 recorded it; and `canServeReads`'s authority is re-cited from **INV-FE-12** (whose subject is an unknown *runtime* surface) to INV-FE-3 and INV-FE-15 plus SQ-771. Three spec questions raised: SQ-810, SQ-811, SQ-812. **21 mutants, 0 survived** — 18 behaviours, of which 3 were first written in forms `tsc` rejected and re-run as forms that compile. **Remaining:** the **archive-node adapter** is deliberately unwritten — R-2 forbids resolving it by assumption and the client's own `chain-client` offers no archive path (smoldot serves `chainHead` only, 10 §4.2) — so the driver is pure over an injected export and the CLI reads one from disk; plus the R-6 spec-compliance review and #247's merge. **✅ 2026-08-07.** R-6's two conjuncts are both satisfied: the R-6 review recorded above returned no blocker, and the gate evidence is PR #261 — all **23** checks `success` on `76477e3f`, squash-merged to `main` as `34cbf7a4`. | -->

<!-- source track narrative -->

Epic IDs from 11 §11.13. FE-P1…P11 prototype gates from 10 §12 (+FE-P10/FE-P11, 11 §11.13).
Rooted at **`app/`** (10 §10.1, amended 2026-08-03); the `frontend/` placeholder is retired.

**Phase grouping** (2026-08-03). **Phase 0** = F19, the spec batch and its two proofs. **Phase 1**
= F0, F2–F6, F20, F21, F7, F10, F11, F22 — web + PWA canonical plus a **direct-download** desktop
Tauri build; no app store, no iOS, no Android, no Product target, therefore **no INV-FE-8/-10
amendment needed**, since store re-signing is the only thing that makes those texts false.
**Phase 2** = F7b, F8, F9, F12, F14, F16–F18 + mobile shells. **Phase 3** = the store question
(scope INV-FE-8/-10 to the canonical channel, add INV-FE-16 channel honesty) + the Product target,
which ships fail-closed read-only until PROD-1/PROD-2 pass.

**Handoff-first navigation** (user ruling, 2026-08-03 — 11 §11.2). The client's two *primary*
surfaces are **prepare a capsule** (S21) and **review/confirm/sign** (§11.3–§11.4 + S22); the
analytical depth and the whole FE-14/FE-15 set sit behind **Advanced**. This reorders Phase 1 —
the handoff packages and the Skill set (F20/F21) now precede the screens (F7) instead of trailing
them — and it is a **presentation change only**: every screen in the 11 §11.2 inventory still
ships, still works with no external tool present, and the no-infrastructure certification run
still executes with the handoff surfaces disabled. Demoting a surface is permitted; removing one
would falsify INV-FE-4 and the 15 §2.1 reading that admits the handoff at all. **Two consequences
are not free and are tracked, not assumed:** SQ-552 (the missing 09 §1.2 ↔ 11 §11.5 precondition
diff) now blocks a *primary* surface rather than a deferrable one, and doc 14's TH-74 residual was
re-assessed for its enlarged exposure (§3.9 preamble).

<!-- end source track narrative -->
