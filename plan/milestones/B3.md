---
id: B3
track: B
title: Node, chain specs (ss58 7777, WSS bootnodes listed), genesis config incl. VIT allocation/vesting
spec:
  - 02 §8/§10
  - 08 §2
  - 12 §6.2
depends:
  - B1a
status: done
---

**Done 2026-07-16.** `node/bleavit-node` — a thin `polkadot-omni-node-lib =0.14.1` branding (runtime ships in the chain spec per 02 §11, not the node; dev manual-seal verified importing blocks end-to-end from a generated spec). Unblocked the previously-unbuildable node stack: `core2` vendored byte-identical behind `[patch.crates-io]` (every crates.io version yanked; V-15) + the `polkadot-stable2603-1` maintenance-pin adoption (`staging-xcm-executor =25.1.0`). **`pallet-vesting =46.0.0`** at frozen index 14 (V-8), D-13 nobody rows for `force_vested_transfer`/`force_remove_vesting_schedule`. **Genesis presets** carry the exact 08 §2.1 five-group 1B-VIT allocation (MAIN/community/incentives on `bl/trsry`-derived pots; founding-team Charlie/Dave on the conservative 0%-at-cliff single-schedule 4-year curve, test-pinned incl. the authorized ≤1-block floor tail; sum == `VIT_TOTAL_SUPPLY` const-asserted). **deploy/ + tools/deploy**: pinned `chain-spec-builder 17.0.0` generator; structural validator enforcing 02 §8 identity (ss58 7777/VIT/12), the 02 §10 bootnode gate (≥8 WSS with **distinct peer IDs** / ≥4 operators / ≥2 on :443, byte-level pot-account identity) and the full genesis-allocation gate (paseo/polkadot profiles REQUIRE a conforming patch); bootnode manifests ship phase-gated empty; ss58-registry submission artifact prepared (**submission itself before Phase 2** — O3). CI: rust-cache + wasm target + libclang + deploy-tests + `--locked` everywhere. **Dual review** (spec-reviewer 0 blocker/2 major/5 minor; Codex adversarial 3 high/2 medium) — every major/high fixed in-session; the reviewed-then-**removed** `VestingFeeAdapter` ruling + curve economics logged (Decision log; SQ-107…SQ-109). Gates green
