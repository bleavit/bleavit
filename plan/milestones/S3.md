---
id: S3
track: S
title: Generated limit-coverage suite — one dispatch-past-limit test per 13-registry key; unmatched keys fail CI
spec:
  - 15 §4.6
depends:
  - A1
  - B1
status: done
---

I-22 CI half. **Done 2026-07-17 (worktree `milestone-S3`).** `tools/limit-coverage/`: strict extractor over 13 §1/§2/§4 (rule-6 ParamKey encoding — `key:` overrides, per-class ×4 materialization, ops.* enumeration; **per-bound expansion of every multi-limit §2 row** per SQ-160; header/second-table/indented-row/malformed-row drift hard-fails) + exhaustive 175-key manifest, S3-historical — the current tree is 179 keys / 67 / 75 / 36 / 1 / **0 unwired** over 98 seeded records (59 dispatch-limit / 72 param-bounds / 36 value / 1 diagnostic / 7 unwired; every class needs reason, unwired needs a PLAN-validated owner whose milestone is still pending — exemptions expire mechanically when the owner flips ✅) + checker (23 unit tests; CI property mutation-verified: new 13 row / deleted marker / stale fixture / reclassified-marked key / unknown owner → red). Dispatch-limit binding: `// limit-coverage:` markers, each key lexically pinned to its Module error or specified `behavior` (clamp/no-op/latch/eviction/neutral-settlement surfaces per SQ-156), `#[ignore]` rejected, one test may cover several keys; new tests incl. 197th-book, 129th-round, 9th vesting schedule, κ one-interval clamp, TickBatch/settle_cohort admission bounds, ReapBatch bounded progress, real-runtime `UncheckedExtrinsic` admission for `orc.max_proof_bytes` (bound ok / bound+1 rejected). Generated suite: per-key amendment-bounds loop over all 95 seeded records (min−1/max+1/Δ+1/cooldown/`KernelBoundImmutable`; 35-record kernel-bounded set == 13 rule 7 exactly; `genesis-keys.json` byte-asserted vs `constitution_core::genesis_params()`; 18 doc-§1 rows unseeded = the `[VERIFY]`-gated set + SQ-158's two). Honesty: 7 unwired + 15 kernel-constant `consumer_binding` keys printed every run, all → **B10** (new milestone row), with the completed-owner expiry ratchet. CI: gate-script legs unconditional + `limit-coverage` workflow job. **Process (Codex gpt-5.6-sol/xhigh):** author → dual review (spec-reviewer 0/1/2 — extraction fidelity, kernel-bounded set, CI property all mutation-verified + adversarial 2/7/2) → remediation (11 dispositions; phase3-caps/TwapCheckpoints reclassified unwired after verification, consumer-binding gap found broader than claimed — 15 keys) → focused re-pass **0 blockers/1 major** (all dispositions verified; residual per-bound split of `MinTrade`/`MaxTrade`, `prop.max_*`, PB-LEDGER-FREEZE duration/renewal fixed by hand). Gates green. SQ-155…SQ-160. Follow-ups: B10 re-arms unwired/consumer-binding keys; TickBatch runtime-admission moves to the runtime when A8 enters `RuntimeCall`
