---
id: N4
track: N
title: `pallet-client-registry` (index 65) + core — governed bonded client registry keyed by `Location`, the `ExternalClient(ClientId)` origin, ingress meter, bond custody, try-state, benchmarks
spec: [16, 09 §6.5]
depends: [N3]
status: done
---

Done 2026-08-01. Exact-location forward/reverse registry, native VIT hold custody, drain-safe tombstoned removal, fail-closed optional bond provider, per-client ingress meter, strict custom origin, generated 50×20 weights, try-state, benchmarks and runtime index 65. Origin carries `ClientId: u32`, not `Location` (~306 B `MaxEncodedLen` would blow `pallet-scheduler`'s bounded `Agenda`)

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track N — External clients: futarchy as a service over XCM | -->

<!-- source row: | N4 | `pallet-client-registry` (index 65) + core — governed bonded client registry keyed by `Location`, the `ExternalClient(ClientId)` origin, ingress meter, bond custody, try-state, benchmarks | 16; 09 §6.5 | N3 | ✅ | Done 2026-08-01. Exact-location forward/reverse registry, native VIT hold custody, drain-safe tombstoned removal, fail-closed optional bond provider, per-client ingress meter, strict custom origin, generated 50×20 weights, try-state, benchmarks and runtime index 65. Origin carries `ClientId: u32`, not `Location` (~306 B `MaxEncodedLen` would blow `pallet-scheduler`'s bounded `Agenda`) | -->

<!-- source track narrative -->

Opened 2026-08-01 on the "make the futarchy infrastructure accessible to other parachains,
smart contracts and services via XCM" work order. Bleavit hosts conditional decision markets
for external clients and **sells price discovery, not decisions**: it publishes the conditional
TWAPs with provenance plus a manipulation-cost bound, and the client's own on-chain rule decides
and executes locally. Bleavit never runs foreign code, and no external state reaches any Bleavit
decision, welfare or settlement input. v1 scope by user decision — the spec batch, contract bump
and threat rows land together. Full approved design: `.claude/plans/` (session plan, 2026-08-01).

<!-- end source track narrative -->
