---
id: E5
track: E
title: **Reduce the recurring cost base** — E1–E4 worked the revenue side of the crossover; this works the cost side, which is where the leverage actually is. Derive and land a reduced operating point for the two lines that are 94.5 % of `C`, re-derive 08 §10's tables from it, and phase-gate the ops lines that Phase 4 does not need
spec:
  - 08 §6.1–§6.3, §10.1, §10.4–§10.6
  - 13 §1 (`mkt.obs_interval`, `keeper.rebate`, `keeper.budget_epoch`, `collator.comp_epoch`), §5 items 4/6 + the occupancy screen
  - 12 §6.1
depends:
  - E1
  - E2
  - E3
  - E4
status: active
---

**A values-layer milestone, not protocol code** — R-1/R-2 govern: every figure must be *derived* (from the now-benchmarked fee basis, or from 08 §10's own formulas) and none may be picked. **Where the money is:** of the 1,145,562 USDC/yr derivable base, keepers are 908,408 (79.3 %) and collators 173,929 (15.2 %). `ops.keepers` alone is 699,694 (61.1 %), and 08 §6.3 states plainly what it buys — out-of-window observation coverage whose gaps "only degrade chart density, never decisions". **Lever 1, `mkt.obs_interval` 10 → 20 blocks:** both keeper lines are `(epoch.slots·6 + 1) × window / mkt.obs_interval × keeper.rebate`, so halving the grid halves both — 908,408 → ≈ 454,200/yr, a 40 % cut to the whole base from one PARAM key. Raising it is the **admitted** direction under the 13 §5 occupancy screen (lowering is what is refused at the genesis registry); max-Δ +5 and cooldown 1 epoch make it two amendments. Costs, stated rather than waved: TWAP samples fall 4,320 → 2,160 per decision window, and the κ clamp `(1±κ)^k` admits half as many intervals per unit time — so the recorded series becomes *more* manipulation-resistant and *less* responsive to genuine news. The lever bottoms out near 20, where the required metered budget falls to ≈ 6,026/epoch against `keeper.budget_epoch`'s 6,000 kernel floor. **Lever 2, re-base `keeper.rebate`:** 0.09 is 3× an **assumed** 0.03 USDC crank fee and the 13 §1 row still carries `[VERIFY fee basis at benchmark time]` (SQ-117). Every pallet is now benchmarked, so the basis is measurable rather than assumed — this is an open `[VERIFY]` whose evidence finally exists, and R-2 requires resolving it against live sources rather than carrying it. Magnitude unknown until measured; both keeper lines scale linearly with it, so it compounds with lever 1. Dropping the multiple 3× → 1× is lawful (that is the row's min) and would cut both lines by two-thirds, but 08 §6.2 is explicit that the 3× *is* the keeper profitability margin and A-1 stops being economically reasonable at 1× — recorded so the floor is visible, not recommended. **Lever 3, `collator.comp_epoch` 2,000 → 500** (registry min; max-Δ ×2, two amendments): 173,929 → 43,482/yr. Cutting collator *count* 5 → 3 saves more but is a liveness posture call, not an economics one. **Lever 4:** fund `ops.keepers` against measured load — 08 §6.1's 580,320 is explicitly the **worst-case zero-organic-trade** figure, since on-trade updates advance the observation grid for free. **Lever 5:** phase-gate the eight unsized `ops.*` lines. At Phase 4 (PARAM only) there is no oracle, so `ORACLE` and `ops.oracle_evidence` are near zero; no attested metrics, so `ops.watchtowers` is near zero; and `ops.arweave`'s ArNS permabuy is capex, not opex. **Explicitly refused:** `REWARDS` (43,482 — already the PARAM-rate ceiling, paid only on `Executed`, and it buys the proposal flow that generates the revenue) and the realized POL divergence (18,830 — 08 §10.6 calls it the treasury buying price discovery). Together 5.4 % of `C`; cutting either attacks the mechanism to save nothing. **Not reducible and not in the table at all:** `ops.coretime`'s `broker.renew` price. A parachain cannot run without buying coretime and nothing in this repo prices it, so the one unavoidable cost is the one 08 §10.1 excludes. **Why this is worth a milestone rather than a parameter tweak:** applying 08 §10.4's own `H* = C / 0.00825` at the central τ = 3, levers 1+3 take `C` 1,145,562 → 560,907 and `V*` 138.9M → 68.0M — from **1.17× a saturated 5×TREASURY slate to 0.89× a saturated 5×PARAM one**, with runway to the 13.86M CODE seeding floor going 9.7 → 19.9 yr. That changes what the endowment has to bridge *to*, not merely how long it lasts, and it does so without touching a fee rate (which 08 §10.4's σ-band rule forbids chasing revenue with). Those two figures are derived here from 08's formulas and are **not** currently in the spec; landing them there is part of this milestone (R-4). **Two hard constraints:** the frozen 08 §4.1 class floors MUST NOT move — they are compile-time literals reachable only by CODE under the 13 §5 item-6 value screen — and the 08 §10 tables are normative there, so PLAN carries status only. Verify: the 13 §5 occupancy screen must **admit** each proposed value (`constitution.set_param` re-derives items 1–4 and refuses by value, so a rejected amendment is the gate working); a `// limit-coverage:` marked test per amended dispatch-limit key; a regression pinning `keeper.budget_epoch` above its 6,000 kernel floor at the chosen `mkt.obs_interval`; re-derived 08 §10.1/§10.4/§10.5/§10.6 tables landed in the spec with the amended values. Blocked on nothing; SQ-526 is adjacent, not a dependency. **Pass 1 landed 2026-07-30 and the target is met — but almost none of it was the lever this row anticipated.** The row assumed a values-layer sweep; three research passes (spec outflows, code outflows, weight-derived fee measurement) found instead that **79.3 % of the cost base rested on an unmeasured placeholder**. **SQ-531:** `keeper.rebate` = 0.09 USDC was seeded as 3x an *assumed* 0.03 USDC crank fee, tagged `[VERIFY]` and never checked. Priced from the committed generated weights the real fee is **0.000085 USDC**, so the seed was ~**1,058x** the fee it claimed to be 3x of. Kernel basis 30,000 -> **85 uUSDC**. **SQ-528, found underneath it and worse:** the measurement was impossible because `FeeMultiplierUpdate = ()` fell through to sp-runtime's blanket `Convert for ()` returning `FixedU128::default()` = **0**, so the fee multiplier collapsed at the first `on_finalize` and **transaction weight was not priced at all from block 2 onward** - a standing DoS subsidy on a PoV-bound parachain, invisible to a suite whose only fee test ran one block. **SQ-530, found in parallel:** four fail-soft treasury paths persisted `load()`-folded `PendingMainCredit` without discharging it, so NAV over-stated custody cumulatively - the over-permissive direction for every NAV-derived control. All three fixed with regressions that fail before and pass after. **Result: cost base 1,145,562 -> 239,728/yr; with `collator.comp_epoch` at its 500 registry minimum, 109,281/yr.** Runway to the 13.86M CODE seeding floor **9.7 -> 101.9 yr**, to the binding 21.26M arming floor **3.3 -> 34.3 yr**, both at ZERO revenue; crossover `V*` 138.9M -> **13.2M**, i.e. **below** a five-slot PARAM slate held at the bare `dec.v_min` floor, so revenue covers cost at minimum viable activity for tau >= 2. **The `Pays::No` restructuring this row implied was investigated and REFUSED** on an adversarial Codex review's objection: `keeper.rebate` is the shared payout for epoch/execution/cleanup/welfare/ORACLE cranks too, so blanket retirement needs a per-dispatch useful-success proof across several pallets - large, risky, and made unnecessary by the basis correction. **Remaining for pass 2:** the `mkt.obs_interval` lever is now worth ~1,300/yr and is not worth spending governance on; `collator.comp_epoch` -> 500 is the one lever still worth taking and needs two PARAM amendments; the eight unsized `ops.*` lines still need phase-gating; SQ-532 (the keeper meter's kernel floor can no longer bind) and SQ-529 (congestion pricing) need decisions **Pass 2 landed 2026-07-31 and closed the one decision pass 1 declined (SQ-536).** The refusal rested on R-2's escalation test — unsafe error direction **and** no evidence anchor — but that is a conjunction, and the second clause was a fact about this repository that I had generalised into a fact about the world. **Polkadot OpenGov referendum #1870 (passed, executed) funds 38 system-parachain collators at $250/collator/month, $307.24 fully loaded = 211.97 USDC/epoch**, so D-15's 2,000 seed was **9.44x** a governance-approved rate for the same job. The comparison is like-for-like because Bleavit collators earn nothing from fees or tips (`OnChargeTransaction`'s `()` drops the imbalance), which is exactly why Polkadot's system parachains are treasury-funded. **Reseeded to the registry minimum 500** (2.36x the anchor) — the bound, not a chosen intermediate, because R-2 says derive and *take the lowest lawful value then show it clears the anchor* is a derivation where 1,000 would be a pick. Safe because the min is unchanged (all 20x of headroom is upward), recovery is x2 per amendment on a 1-epoch cooldown, underfunding is fail-soft (08 §2.4), and the launch set is invulnerable. **Cost base 239,728 -> 109,281/yr; runway to the BINDING 21.26M arming floor 15.6 -> 34.3 yr at zero revenue; `V*` 29.1M -> 13.2M; quiet epoch 10,200 -> 2,700; consistent break-even 7 occupied slots (unreachable at `epoch.slots` = 5) -> 1.** The >= 25-year goal is now met against the binding floor rather than only the seeding floor. Anchor built into the reference model first so the number was machine-checked before anything depended on it (suite **132**); a genesis test now pins the seed, closing a gap the work exposed — **nothing in the workspace pinned the *value* of any genesis PARAM row**. Verified against live sources per R-2 and logged as **V-61**, including one figure found and rejected as an anchor. Gates: fmt/clippy/test over constitution-core + pallet-constitution + bleavit-runtime **515 tests exit 0**, reference-model 132, limit coverage `unwired = 0`, PLAN tables, batch index, doc links 84. **Remaining for E5:** SQ-529/532/533/534 decisions and phase-gated funding of the eight unsized `ops.*` lines — all governance/doc work
