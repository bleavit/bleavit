---
id: F28
track: F
title: FE-20 **the visual layer** — Tailwind v4, a token system, and the 92 class names the markup already carried
spec: [10 §2.1 (INV-FE-9 labelling), §9.4 (budgets), 12 §1.1 (reproducible build), §5.1 (CSP)]
depends: [F7, F7b, F17, F26]
status: done
---

**The application shipped with no stylesheet at all, and nothing said so.** 119 `className` occurrences across 25 `.tsx` files named 92 distinct BEM-shaped classes and five interpolated families, and not one of them had a rule — no `.css` in the repository, no Tailwind, no PostCSS, no CSS-in-JS, zero bytes of CSS in `dist/`. Every suite passed throughout, because an unstyled element still contains its text: `renderToStaticMarkup` sees the same string either way. **Tailwind v4.3.3 via `@tailwindcss/vite`** (peer range `^5.2.0 || ^6 || ^7 || ^8` admits the pinned Vite 8.1.4), exact-pinned like every other dependency here. **The markup is untouched** — the class vocabulary is styled where it stands, because the suites assert on `renderToStaticMarkup` output with quote-anchored `class="…"` regexes: converting to utilities breaks 19 assertions and silently voids 3 more, which are negative assertions that would go vacuous rather than red. The stylesheet is linked from `index.html` rather than imported from TypeScript, which avoids `noUncheckedSideEffectImports` and stops a `.css` specifier reaching a package's emitted `dist/*.js`, where Node would refuse it and take the whole screens suite with it. **The one design constraint that is not taste:** 10 §2.1 requires provenance labelling to be *"mandatory, non-suppressible"* and INV-FE-9 admits *"no unlabeled rendering path"*, so no rule may hide a badge, a `data-status` treatment, a refusal or a notice — not by `display:none`, not by `opacity:0`, not behind `:hover`. Provenance is quiet here and never absent: the marker always renders, the three risky statuses (`provider`, `stale-cache`, `external-proposal`) additionally mark the **value** rather than only the badge beside it, and it is the detail that expands on hover. **Three integration defects were found and fixed rather than shipped:** the CSS entry was missing from `RECIPE_INPUTS`, so two builds with different Tailwind configuration would have published the same `buildRecipeDigest`; the `@source` scan was wide enough to reach `dist` and the pnpm virtual store, making the emitted sheet a function of the install shape; and the dev server was unusable, since a `<meta>` CSP applies to the *source* document and both `style-src 'self'` and the unsubstituted `connect-src __CONNECT_SRC__` block it — a `serve`-only plugin now relaxes exactly those two, and `pipeline.test.ts` asserts the built tree still carries `style-src 'self'` with no `unsafe-inline` and no dev marker. **New gate:** `check:style-coverage` (+ witness), binding markup and stylesheet in both directions, with the five interpolated families **declared** rather than inferred — its first version reported 100 % coverage and zero dynamic families on a tree that had five, because its pattern could not parse `className={`…${…}`}`. **R-6 review ran on the first cut and returned 0 blockers, 5 majors, 12 minors; all five majors and five minors are fixed here.** Two of the majors falsified claims this row made. (1) **`@source` does not narrow — it *adds* to auto-detection**, so the scan I described as scoped was still reading every non-ignored file under `app/`: `.ordinal`, `.row-4`, `.start-3` and `.end-2` reached the published stylesheet from test *prose*, and the emitted CSS was a function of any scratch file present in one build environment and not the other. `source(none)` fixes it; the sheet lost 4,356 raw bytes and all four utilities. (2) **Five colour pairs sat under 3:1 in dark mode**, three of them provenance badges, because the `-600` state steps are tuned against white and were reused verbatim on a dark ground — the labelling obligation met nominally, which is the exact failure this row claims to avoid. State text now indirects through theme-flipping tokens like the surfaces already did: the five measured pairs move from 2.21–2.60 to **4.48–9.31**, and the light theme's faint labels from 3.16–3.50 to **4.78–5.29**. Also fixed: `.button:disabled` used `opacity`, which composites label and ground together and produced 2.41:1 on the primary — the fade its own comment disclaimed; `.shell`'s `grid-template-rows: auto 1fr` named two rows for a three-or-four-child shell, so with the §11.10 banner present the **header** absorbed the slack and the page body was pushed to the bottom edge (now a column flex with the growing child named on itself); `.disclosure`'s `overflow: hidden` clipped the focus ring off the control that opens a decoded payload; the dev-only CSP marker `/* --dev-only */` parsed as the host-source `*` with path `/`, widening the dev policy to every host rather than annotating it; and the witness leg asserted only that its collectors returned something, which is the weaker half of the very control this gate argues for — it now runs the same `analyse` over a fixture carrying one of each defect and requires all four to be reported. **Verification:** 43/43 App CI commands, stylesheet byte-identical across repeated builds, the gate mutation-tested three ways and the witness mutation-tested itself, all exit codes checked directly. **Open:** the system font stack is a placeholder for a self-hosted face, `check:render-budget` needs a Chrome this workstation lacks (CI has one), SQ-1040 records that no byte budget measures the stylesheet, SQ-1041 asks whether the client has a stated contrast target at all, and the badge detail is reachable on hover but not by keyboard

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track F — The canonical cross-platform client (`app/`) | -->

<!-- source row: | F28 | FE-20 **the visual layer** — Tailwind v4, a token system, and the 92 class names the markup already carried | 10 §2.1 (INV-FE-9 labelling), §9.4 (budgets), 12 §1.1 (reproducible build), §5.1 (CSP) | F7, F7b, F17, F26 | ✅ | **The application shipped with no stylesheet at all, and nothing said so.** 119 `className` occurrences across 25 `.tsx` files named 92 distinct BEM-shaped classes and five interpolated families, and not one of them had a rule — no `.css` in the repository, no Tailwind, no PostCSS, no CSS-in-JS, zero bytes of CSS in `dist/`. Every suite passed throughout, because an unstyled element still contains its text: `renderToStaticMarkup` sees the same string either way. **Tailwind v4.3.3 via `@tailwindcss/vite`** (peer range `^5.2.0 \|\| ^6 \|\| ^7 \|\| ^8` admits the pinned Vite 8.1.4), exact-pinned like every other dependency here. **The markup is untouched** — the class vocabulary is styled where it stands, because the suites assert on `renderToStaticMarkup` output with quote-anchored `class="…"` regexes: converting to utilities breaks 19 assertions and silently voids 3 more, which are negative assertions that would go vacuous rather than red. The stylesheet is linked from `index.html` rather than imported from TypeScript, which avoids `noUncheckedSideEffectImports` and stops a `.css` specifier reaching a package's emitted `dist/*.js`, where Node would refuse it and take the whole screens suite with it. **The one design constraint that is not taste:** 10 §2.1 requires provenance labelling to be *"mandatory, non-suppressible"* and INV-FE-9 admits *"no unlabeled rendering path"*, so no rule may hide a badge, a `data-status` treatment, a refusal or a notice — not by `display:none`, not by `opacity:0`, not behind `:hover`. Provenance is quiet here and never absent: the marker always renders, the three risky statuses (`provider`, `stale-cache`, `external-proposal`) additionally mark the **value** rather than only the badge beside it, and it is the detail that expands on hover. **Three integration defects were found and fixed rather than shipped:** the CSS entry was missing from `RECIPE_INPUTS`, so two builds with different Tailwind configuration would have published the same `buildRecipeDigest`; the `@source` scan was wide enough to reach `dist` and the pnpm virtual store, making the emitted sheet a function of the install shape; and the dev server was unusable, since a `<meta>` CSP applies to the *source* document and both `style-src 'self'` and the unsubstituted `connect-src __CONNECT_SRC__` block it — a `serve`-only plugin now relaxes exactly those two, and `pipeline.test.ts` asserts the built tree still carries `style-src 'self'` with no `unsafe-inline` and no dev marker. **New gate:** `check:style-coverage` (+ witness), binding markup and stylesheet in both directions, with the five interpolated families **declared** rather than inferred — its first version reported 100 % coverage and zero dynamic families on a tree that had five, because its pattern could not parse `className={`…${…}`}`. **R-6 review ran on the first cut and returned 0 blockers, 5 majors, 12 minors; all five majors and five minors are fixed here.** Two of the majors falsified claims this row made. (1) **`@source` does not narrow — it *adds* to auto-detection**, so the scan I described as scoped was still reading every non-ignored file under `app/`: `.ordinal`, `.row-4`, `.start-3` and `.end-2` reached the published stylesheet from test *prose*, and the emitted CSS was a function of any scratch file present in one build environment and not the other. `source(none)` fixes it; the sheet lost 4,356 raw bytes and all four utilities. (2) **Five colour pairs sat under 3:1 in dark mode**, three of them provenance badges, because the `-600` state steps are tuned against white and were reused verbatim on a dark ground — the labelling obligation met nominally, which is the exact failure this row claims to avoid. State text now indirects through theme-flipping tokens like the surfaces already did: the five measured pairs move from 2.21–2.60 to **4.48–9.31**, and the light theme's faint labels from 3.16–3.50 to **4.78–5.29**. Also fixed: `.button:disabled` used `opacity`, which composites label and ground together and produced 2.41:1 on the primary — the fade its own comment disclaimed; `.shell`'s `grid-template-rows: auto 1fr` named two rows for a three-or-four-child shell, so with the §11.10 banner present the **header** absorbed the slack and the page body was pushed to the bottom edge (now a column flex with the growing child named on itself); `.disclosure`'s `overflow: hidden` clipped the focus ring off the control that opens a decoded payload; the dev-only CSP marker `/* --dev-only */` parsed as the host-source `*` with path `/`, widening the dev policy to every host rather than annotating it; and the witness leg asserted only that its collectors returned something, which is the weaker half of the very control this gate argues for — it now runs the same `analyse` over a fixture carrying one of each defect and requires all four to be reported. **Verification:** 43/43 App CI commands, stylesheet byte-identical across repeated builds, the gate mutation-tested three ways and the witness mutation-tested itself, all exit codes checked directly. **Open:** the system font stack is a placeholder for a self-hosted face, `check:render-budget` needs a Chrome this workstation lacks (CI has one), SQ-1040 records that no byte budget measures the stylesheet, SQ-1041 asks whether the client has a stated contrast target at all, and the badge detail is reachable on hover but not by keyboard | -->

<!-- source track narrative -->

Epic IDs from 11 §11.13. FE-P1…P11 prototype gates from 10 §12 (+FE-P10/FE-P11, 11 §11.13).
Rooted at **`app/`** (10 §10.1, amended 2026-08-03); the `frontend/` placeholder is retired.

**Phase grouping** (2026-08-03). **Phase 0** = F19, the spec batch and its two proofs. **Phase 1**
= F0, F2–F6, F20, F21, F7, F10, F11, F22 — web + PWA canonical plus a **direct-download** desktop
Tauri build; no app store, no iOS, no Android, no Product target, therefore **no INV-FE-8/-10
amendment needed**, since store re-signing is the only thing that makes those texts false.
**Phase 2** = F7b, F8, F9, F12, F14, F16–F18 + mobile shells. **Phase 3** = the store question
(scope INV-FE-8/-10 to the canonical channel, add INV-FE-16 channel honesty) + the Product target,
which ships fail-closed read-only until PROD-1/PROD-2 pass.

**Handoff-first navigation** (user ruling, 2026-08-03 — 11 §11.2). The client's two *primary*
surfaces are **prepare a capsule** (S21) and **review/confirm/sign** (§11.3–§11.4 + S22); the
analytical depth and the whole FE-14/FE-15 set sit behind **Advanced**. This reorders Phase 1 —
the handoff packages and the Skill set (F20/F21) now precede the screens (F7) instead of trailing
them — and it is a **presentation change only**: every screen in the 11 §11.2 inventory still
ships, still works with no external tool present, and the no-infrastructure certification run
still executes with the handoff surfaces disabled. Demoting a surface is permitted; removing one
would falsify INV-FE-4 and the 15 §2.1 reading that admits the handoff at all. **Two consequences
are not free and are tracked, not assumed:** SQ-552 (the missing 09 §1.2 ↔ 11 §11.5 precondition
diff) now blocks a *primary* surface rather than a deferrable one, and doc 14's TH-74 residual was
re-assessed for its enlarged exposure (§3.9 preamble).

<!-- end source track narrative -->
