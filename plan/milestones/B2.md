---
id: B2
track: B
title: `FutarchyApi` runtime API — all 11 methods + view types
spec: [02 §3–§4]
depends: [B1a]
status: done
---

**Done 2026-07-16** — `runtime-api/` (`futarchy-runtime-api`): `sp_api::decl_runtime_apis!` declaring all 11 methods byte-for-byte per 02 §3 over the §4 view types single-homed in `futarchy-primitives` since M1; every bound from `bounds::*` (`MAX_QUEUED_EXECUTIONS = bounds::MAX_LIVE_PROPOSALS`, the guard-core derivation); `sp-api ==41.0.0` (stable2603). Tests: 11-method mock-impl callability, API ID/VERSION freeze, bound freeze vs 02 §3, per-view SCALE round-trips, FRAME-`BoundedVec` cross-encoding both directions. spec-reviewer **0/0/0**; Codex adversarial 2 high = pre-existing SQ-55/SQ-43 (frozen-contract defects riding the 02-amendment batch, user sign-off needed before B1a implements `nav()`/`recent_cohorts()`), 1 medium fixed (cross-encoding test) + metadata-shape residual → SQ-106. The runtime-side `impl_runtime_apis!` of all 11 methods is B1a's assembly work

<!-- source section: | ## Milestones | -->

<!-- source track heading: | ### Track B — Runtime, node and chain | -->

<!-- source row: | B2 | `FutarchyApi` runtime API — all 11 methods + view types | 02 §3–§4 | B1a | ✅ | **Done 2026-07-16** — `runtime-api/` (`futarchy-runtime-api`): `sp_api::decl_runtime_apis!` declaring all 11 methods byte-for-byte per 02 §3 over the §4 view types single-homed in `futarchy-primitives` since M1; every bound from `bounds::*` (`MAX_QUEUED_EXECUTIONS = bounds::MAX_LIVE_PROPOSALS`, the guard-core derivation); `sp-api ==41.0.0` (stable2603). Tests: 11-method mock-impl callability, API ID/VERSION freeze, bound freeze vs 02 §3, per-view SCALE round-trips, FRAME-`BoundedVec` cross-encoding both directions. spec-reviewer **0/0/0**; Codex adversarial 2 high = pre-existing SQ-55/SQ-43 (frozen-contract defects riding the 02-amendment batch, user sign-off needed before B1a implements `nav()`/`recent_cohorts()`), 1 medium fixed (cross-encoding test) + metadata-shape residual → SQ-106. The runtime-side `impl_runtime_apis!` of all 11 methods is B1a's assembly work | -->

<!-- source track narrative -->

Doc-09 WBS delta rows map here and to Track A: E15→B2/B8 · E16→A2 · E17→A3 · E18→A6 ·
E19→A5 · E20→A10/B6 · E21→B4 · E22→A11/B1. (E1–E14 definitions live in git history —
superseded `BACKEND_PLAN.md` §26; their scope is covered by Tracks M/A/B.)

<!-- end source track narrative -->
