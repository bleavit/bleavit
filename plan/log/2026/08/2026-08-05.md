# Session log — 2026-08-05

## **FE-P2 resolved positively, and 02 gains rule 9: rule…

**FE-P2 resolved positively, and 02 gains rule 9: rule 2's "any change to §2–§12" is about the *surface*, so a `[VERIFY]` resolution that moves no surface does not bump the contract.** Resolving FE-P2 required editing 02 §3, and rule 2 read as text demands a bump for any §2–§12 edit. Read for its purpose it does not: the counter is stamped in `futarchy-primitives`, echoed in `release.json` and compared by 10 §5.2's classifier, so a bump asserts *"a client built against the previous number may be wrong about what it can call or read"* — and nothing here changed a SCALE type, method, storage item, event, constant, encoding, ordering or bound. Bumping would have invalidated the committed artifact feed, the descriptor sets and the surface manifest to chase a prose clarification, and published a surface move nothing behind it can attest — the same failure rule 8 already refuses for §7.7, arriving from the other direction. The test is mechanical rather than editorial (no frozen surface element changes **and** no client's compiled expectations become wrong), ambiguity resolves toward bumping, and the edit must say so where it lands so the next reader is not inferring it from a version that did not move. **Rule 9 is written into §13 deliberately** — §13 is outside rule 2's own range, so a rule about the scope of rule 2 does not trigger it. `INTEGRATION_CONTRACT_VERSION` stays **27**; `check-chain-feed.py`, `check-client-surface-obligations.py` and `check-dispatch-mirror.py` all re-run green against the unchanged constant.

Claude (R-1 delegation, 2026-07-25 — the user owns both sides of 02 §13's sign-off and has delegated spec rulings and contract-bump calls)

**What comes next:** `02` §3 + §13 (new rule 9); `10` §4.2 + §12 (preamble and the FE-P2 row)

## F1 🔨 — **FE-P8 resolved**: the long-range checkpoint…

F1 🔨 — **FE-P8 resolved**: the long-range checkpoint bound, derived from the relay

**The one open F1 gate that needed no device lab, and it turned into a normative section.** The threat is weak subjectivity and its sharp edge is that it is **undetectable after the fact**: a client warp-syncing from a checkpoint at era `E` trusts the authority set at `E`, and once those validators withdraw their stake they can sign an alternative finalized chain from `E` at no cost — so everything learned through a compromised sync is compromised, *including any figure that might be used to check it*. The bound is **derived, not chosen**: `SlashDeferDuration = 27` eras (the deterrent lapses) and `BondingDuration = 28` (the stake leaves), at 6 sessions × 4 h = 24 h per era. **Verified against both the Fellowship Polkadot relay runtime and Paseo's, which agree exactly** — the finding rather than a formality, since the release targets both, testnets routinely shorten this, and shipping 28 onto a 7-day chain would present reads as verified for three weeks after the guarantee lapsed. New **10 §2.4**: refuse every `verified-finalized` claim at 28 days (**refusal, not `restricted`** — those modes describe the same chain further along, this describes a client that cannot establish which chain it is on), warn at 27, and measure against the **device clock**, which is untrusted but *independent of the attacker's chain*. A clock set **behind** the checkpoint refuses rather than reading age zero, because that is the cheapest possible attack on the control. Implemented in `packages/verify`, with the figures travelling in the release document so a relay amendment moves the bound without a code change (10 §5.4). 6 mutations, 0 survived — including `indeterminate` sliding onto the passing side and the bound being hardcoded. 700 app tests green.

**What comes next:** The rest of F1 needs a device lab, hardware or a testnet (FE-P3/P6/P9/P10, FE-P4's device half, FE-P7's live Arweave half, FE-P11's matrix).

## **F10 ✅** — the verification panel rendered, and two of…

**F10 ✅** — the verification panel rendered, and two of my tests were the defect

Unblocked by F7's shell; F10's row had named the component as its only remainder. It sits in `src/application` rather than `packages/ui` as that row guessed — `ui` knows about *provenance*, and a panel that understands `ReleaseIdentity`, self-check divergence and a checkpoint bound is domain furniture, so putting it in `ui` would make the render layer depend on the release layer for one screen. It takes **no chain handle**, because 10 §3.2 lists it among the surfaces that still render under `FE-BOOT-002` when no verified read exists. **Two renderings survived their first mutation, and in both cases the code was right and the test was wrong.** Pinned-vs-observed must be distinguishable **in words, per row** — the first test searched the whole document for the two phrases, which also appear in the notice above the list, so deleting the per-row label left it green. And a **fresh** checkpoint must render *nothing*, because a green *"checkpoint is current"* line is a claim about the future — the first test only asserted three warning phrases were absent, which any other reassuring wording passes. Both are now structural: per-row extraction, and **byte equality** against the panel with no checkpoint supplied. 5 mutations, 0 survived after the repairs. 706 app tests green; bundle unchanged at 199,767 bytes.

**What comes next:** Track F: **10 ✅ · 6 🔨 · 8 ⬜**.

## F7 🔨 — the decode layer, which needed neither a client…

F7 🔨 — the decode layer, which needed neither a client nor a new dependency

**The scoping note from a few hours earlier pointed at a new pinned package; checking before adding it found `getTypedCodecs` is a root export of `polkadot-api`,** which `chain-client` may already import. So the codecs build from the committed descriptors with no chain, no provider and no network — measured — and the descriptors stay an *argument*, so `chain-client` gains no edge to the generated package. **Three defects in the first version, each caught by a control rather than by review.** `as unknown as` (banned across `app/`) was failed by `check:casts`, and the repair beats an exemption: the lookup is by string, no static type relates argument to result, so `unknown` plus a **runtime guard** is the accurate typing — and the guard turns out to be load-bearing at compile time too, since removing its narrowing makes the code untypeable. The codec surface is a **Proxy that throws** on an unknown name, so both `=== undefined` guards were dead code. And a mutation binding `storageDecoder` to a *different item* survived, because the test only asserted decoding `0x` fails — true of every item. 7 mutations, 0 survived after the repairs. 714 app tests green.

**What comes next:** F7's last mile: mapping a decoded `Epoch.EpochOf` into `{epoch, phase}`, which needs the runtime's own type shape and a real stored value rather than a guess.

## F7 🔨 — **a defect in my own work from the same…

F7 🔨 — **a defect in my own work from the same session**: the sudo banner keyed off a phase number, not the bitset

Found by decoding the **recorded** `Constitution.PhaseFlags` value with the real codec instead of reading the shell again. `storage.constitution.phase_flags` records `0x11000000` → **17** = `0b10001` (shadow mode + sudo present). 02 §7.3 freezes `PhaseFlags` as a `u32` **bitset** with *bit 4 = sudo present*; the shell tested `phase >= 4`. The two share the digit, which is exactly why it read as correct — and `17 >= 4` would have **hidden the banner on a chain running sudo**, the one direction 11 §11.10 cannot tolerate. `phase-flags.ts` now tests flags **by name**, so a call site cannot supply a bit index it worked out itself, and the table is **bound to 02 §7.3's own sentence** — which immediately caught my own table for shortening *"ledger frozen (PB-LEDGER-FREEZE)"*, so the keys are now the document's exact wording with no normalisation step for a rename to slip through. **A second finding from the mutation round: the `>>> 0` sign normalisation was dead code.** Every named bit is 0–7, so the mask is positive and the result non-negative whatever the input's sign — the mutation removing it could not fail. Deleted rather than kept as decorative caution, with the reasoning written down and the condition under which it returns. 8 mutations across the correction, 0 survived. 717 app tests green.

**What comes next:** The per-item value mapping now has a real shape to map: `EpochOf` decodes to `{index, phase: {type}, phase_start_block}`.

## **F7 ✅** — the default surface, end to end with no node

The last mile was the per-item value mapping, and taking the shapes from the **runtime** rather than from the prose is what exposed the session's worst defect (recorded in the row above): `PhaseFlags` is a bitset and the shell was testing a phase number. `test:screens` now carries the whole path with no node — recorded fixture bytes → the real PAPI codecs → the read layer → the screen model → the rendered banner — so the values asserted are the runtime's own rather than a shape written out of a document. A runtime whose `EpochOf` shape moved is **refused** with the reason carried to the screen beside the raw bytes, rather than read silently into `NaN` and the word "undefined" on the header. 14 mutations on this slice, 0 survived; two found real test gaps and one found **dead code** — the `>>> 0` normalisation could not fail, since every named bit is 0–7, so it went rather than staying as decorative caution. 720 app tests + 28 gates green; `release:build` clean.

**What comes next:** Track F: **11 ✅ · 5 🔨 · 8 ⬜**. The remaining 🔨 are F1 (device lab, hardware, testnet), F8 (ingestion wiring to a live transport), F13 and F15 (both need a key ceremony and seated roles).

## **SQ-593 filed** — the degradation matrix is 25 rows by…

**SQ-593 filed** — the degradation matrix is 25 rows by name and 12 by text

F7 closing unblocked F12, and reading its spec before building it found the gap. 11 §11.12 owns **E15–E25** in full facet form and says E1–E14 *"carry forward from FE §19"* with their texts *"in [10]"*. **FE §19 is the superseded `FRONTEND_PLAN.md` this doc pair replaced**, and doc 10 carries no E-row in facet form — grepping the `V: … L: … A: …` shape finds it only in doc 11 — except E3's correction, which explicitly disclaims ownership. **What makes it worth a spec question rather than a to-do: nothing counts the rows.** F12's obligation is *"rows E1–E25 scripted"*, and a client scripting the eleven it can read passes every gate in this repository, because no checker knows the matrix has 25 members. That is SQ-552 and SQ-580's shape exactly — an obligation stated in prose that no gate can see. F12 is left ⬜ with the blocker named rather than started, since shipping eleven rows as a complete degradation matrix is precisely the outcome the milestone exists to prevent.

**What comes next:** Rule SQ-593 (write the fourteen texts into 10, move all 25 into 11, or prove 10 §3.2/§8.3 subsumes them), then build F12.

## **SQ-593 ruled and executed** — the degradation matrix…

**SQ-593 ruled and executed** — the degradation matrix has one home, 25 rows, and a checker that counts them

**The correction is worth more than the original filing.** I filed SQ-593 saying fourteen rows had no text and no owner; a wider search found **15 §3.3 enumerates all fourteen by name**, and SQ-1 had already ruled §11.12 canonical with §3.3 as its index. So the row set was never lost — I filed on an incomplete search, which is exactly the under-research R-1 warns a deferred decision usually is. The **real** defect is narrower and still real: the facet texts for E1–E14 existed nowhere (15 §3.3 calls them *"self-describing"*, which is not a specification a client can be held to), and §11.12's pointer at doc 10 was a citation defect. **Ruled under R-1 and executed the same session:** fourteen facet rows authored into 11 §11.12, each derived from the doc-10 section it exercises rather than invented — the four `FE-BOOT-*` codes, the compat lattice, the **withdrawn** *history-continuous* promise, the provider health ladder, `FE-IDX-001`'s required reason, `FE-TX-004`/`-007`'s ordering, and today's §2.4 checkpoint bound. §11.12's doc-10 pointer is repaired to a reasoning reference. **`check-degradation-matrix.py` is the control that never existed**: it counts the rows, requires contiguity, requires V/A/F per row, and diffs against 15 §3.3 both ways. Fail-closed on a thin parse — which fired at once, because §11.12 carries two row formats and my first parser swallowed ten rows into one and reported 15. 5 mutations, all caught.

**What comes next:** F12's client-side registry, now buildable against a complete matrix.

## **F12 ✅** — the degradation matrix scripted, after…

**F12 ✅** — the degradation matrix scripted, after being completed and given a counter

The milestone was ⬜ at the start of the day and blocked by the fact that **fourteen of its twenty-five rows had no text** — which itself was only findable because I read the spec before building. Ruling and executing SQ-593 completed the matrix and gave it `check-degradation-matrix.py`; this closes the client half. `DEGRADATION_RESPONSES` is a `Record` over a **closed** union, so a row added to the spec is a **compile error** — a runtime default would let an unscripted degradation ship silently, which is exactly the defect. `blocks` is required even where nothing is blocked, so *"blocks nothing"* is distinguishable from *"nobody wrote it down"*; and `recovery: none` is a **claim**, asserted to be exactly E6/E14/E16/E20 — the rows where no retry helps — because a wrong one either overstates what the client can do or strands a user who could have acted. A response suggesting a retry for a row it calls unrecoverable fails the suite. 5 mutations, 0 survived. 724 app tests + 29 gates green.

**What comes next:** Track F: **12 ✅ · 5 🔨 · 7 ⬜**.

## F16 🔨 — the G-1…G-9 governance precondition rows

Spec-first check first, and it came back **clean** (V-113) — all nine §11.7.2 reads frozen in 02 §7.6, manifested and probed. Worth recording because a first grep said *NOT IN 02* for all nine and nearly became a false spec question: the docs use the qualified `` `Pallet.Item` `` form, so a bare-item pattern never fires. **Search 02 by the qualified name.** The rows' content is two subjects. `G-5 unlock(class, target)` reads the **target**, not the caller — anyone may unlock for anyone, so reading the caller's locks passes green whenever the caller has none, which is the ordinary case, and the chain then refuses. `G-2`'s delegation target is a chain-read identity under §11.3, not a form echo. The rest are `acting` rather than `signer`, because a proxy splits those, with G-6/G-7's deposits genuinely `signer` so the distinction is not vacuous. The conviction-lock consequence is marked above-the-fold **in the row**, not left to a screen. Deliberately absent: G-6's prospective `referendum_index` is a warning rather than a precondition, since inclusion order decides it and no read at B′ can bind it — encoding it would block a lawful submission on an unavoidable race. 7 mutations, 0 survived. 730 app tests green.

**What comes next:** S9/S10/S11.

## F16 🔨 — S9/S10's read screens, with §11.7.6 made…

F16 🔨 — S9/S10's read screens, with §11.7.6 made structural

Three statements, three structures. A tally's sides are badged **individually**, because one `Verified<Tally>` would let a single status stand for both and hide a mismatch. `Confirming` is its **own arm** of the status union and carries the abort copy, so no screen can show it without saying the countdown is not a countdown. And the `undecodable` call arm has **no name field**, which is 10 §5.4's *"never guessed"* as something a screen cannot do. Three of five mutations were caught by the **compiler** — removing a union narrowing makes the field access untypeable, which is the strongest form of these controls. **One survived and it was my test's fault**: the mixed-provenance assertion drove only the detail view, so a list rendering `ayes` in both columns passed. A tally whose columns show the same figure is the one rendering error a reader cannot catch from the screen, so the list is now covered directly. 736 app tests green; `release:build` clean.

**What comes next:** S11, the vote/delegate/unlock forms, and §11.7.4's ratification panel.

## F16 🔨 — S11, the ballot whose failure has no on-chain…

F16 🔨 — S11, the ballot whose failure has no on-chain symptom

§11.7.5(3): locks that **post-date the snapshot** make a vote *signable but weightless* — the extrinsic succeeds, the user believes they voted, and nothing after the signature tells them their stake counted for nothing. So the whole obligation is pre-sign, and `EffectivePower` is a **union rather than a number plus a flag**, because a number plus a flag renders without the flag: the `weightless` arm has **no power field**, so there is no zero to misread. §11.7.5 rule 2 still carries an open **`[VERIFY]`** on the snapshot mechanism, so a third arm — `unestablished` — is what the client uses until it resolves, also with no power field. That is R-2 made structural: a figure computed from a guessed snapshot rule would look exactly like one that was checked. Rule 4's copy is unconditional and asserted across **all three** power states so it cannot quietly become conditional on having weight. 5 mutations, 0 survived; the weightless-renders-a-zero one was caught by the **compiler**, the arm having no field to read. 741 app tests green.

**What comes next:** The vote/delegate/unlock forms and §11.7.4's ratification panel.

## F16 🔨 — §11.7.4's ratification panel; the guard record…

F16 🔨 — §11.7.4's ratification panel; the guard record made unable to lie

The section's sharpest sentence is that **`NoPassedRecord` is not a lifecycle answer** — it does not distinguish never-submitted from ongoing from rejected. Rendering it as *"not ratified"* lies in the worst available direction: the referendum may be about to pass, and a proposer who reads *not ratified* stops waiting. So the `no-passed-record` arm **requires** the referendum link, and a caller holding only the guard's record **cannot build the view** — the rule is a missing argument. The cannot-complete warning is arithmetic and returns **`undefined` rather than `false`** for a non-ongoing referendum, because there is no remaining-period figure to compare and a confident answer would be to a question nobody asked; it says the proposal *will* reject, so a false positive tells a proposer to abandon something live. Boundary tested at exactly-fits. The window renders **blocks as well as** a countdown — a countdown alone presents an estimate as the deadline and errs in the direction that runs out of time. 6 mutations, 0 survived (two compiler-caught). 747 app tests + 29 gates green; bundle 200,041 bytes.

**What comes next:** F16's last piece: the vote/delegate/unlock forms.

## F16 🔨 — conviction, and a lock that cannot be skipped

§11.7.1 puts the lock duration *before* signing and §11.2 constraint 3 forbids deferring it, so `LockDisclosed` is **branded** with `discloseLock` as its only producer and `VoteForSigning` requires one: a vote reaching a signer with no lock computed **does not typecheck**, and the corpus carries the forged disclosure. The failure it prevents is silent — the extrinsic succeeds and the user finds out up to **32 enactment periods** later. The multipliers were **read from the pinned SDK** rather than inferred from the doubling (0/1/2/4/8/16/32; the pattern looks like it starts at 1, and these are what tokens are locked by); they are wire format, while the enactment period they multiply is chain state and takes no default. `None` is a **real conviction** — 10 % weight, no lock — because treating it as *unset* would let a blank field silently mean *no lock*. `acceptsConviction` exists because a split vote appearing to take 6× would claim both a lock and a weight that do not exist. 6 mutations, 0 survived. 753 app tests green.

**What comes next:** F16's form widgets; then F17/F18.

## **Spec repair (R-1)** — `PhaseFlags` has no ordering,…

**Spec repair (R-1)** — `PhaseFlags` has no ordering, and one of the two comparisons skipped a containment control

Found by pushing the sudo-banner defect one step further: if the *client* could not compare a phase number, could the *spec*? It could not. `Constitution.PhaseFlags` is the only phase-related frozen surface and it is a `u32` bitset — **no governance-phase integer exists in 02 at all** — yet §11.10 said the banner clears at *"Phase ≥ 4"* and §11.9.1 gated D-13's Phase-3 exposure caps on *"while PhaseFlags < Phase 4"*. A bitset compares as a number **without error**, so either reading yields a confident wrong answer: the real bootstrap value `17` satisfies `17 ≥ 4` (hiding the sudo banner on a sudo chain — the defect already fixed in the client) and fails `17 < 4`, which **skips the exposure caps entirely** in exactly the window they exist for. The second is worse: the banner warns, the caps contain. Both are bit 4, both sections now say so, and §11.10 carries one normative note explaining that the comparison is *unperformable*, not merely imprecise. No 02 change, no contract bump — the surface was always right.

**What comes next:** F16's form widgets; F18 now has one fewer trap.

## **F16 ✅** — the governance surface, and a third…

**F16 ✅** — the governance surface, and a third instance of one test defect

The forms were the last piece and carry three controls, the model layer holding the rest: the lock is a prop typed **`AboveTheFold`** so it cannot be substituted or deferred; a split vote **disables conviction with its reason in element text** (a silently-ignored choice claims both a lock and a weight that do not exist, and a `title` is unreadable on touch); and the unlock form **names the account it acts on**, since G-5's subject is `recipient`. **Two mutations survived first and both were my tests** — `/<select[^>]*disabled/` matched `aria-describedby="conviction-disabled"`, and `includes(REASON)` matched the `title` attribute. That is the **third** time this session an assertion passed on a second occurrence elsewhere in the markup, after the pinned/observed row labels and the fresh-checkpoint reassurance. **The pattern is worth carrying: assert on the rendered attribute or on element text, never on a substring of the whole document.** 9 mutations, 0 survived after the repairs. 758 app tests + 29 gates green; bundle 200,041 bytes.

**What comes next:** Track F: **13 ✅ · 5 🔨 · 5 ⬜**. F17/F18 next, both verified buildable (V-114).

## F18 🔨 — the funding model, and *arrival* as the rule…

F18 🔨 — the funding model, and *arrival* as the rule with no on-chain symptom

*"Local finality on AH ≠ delivery."* XCM is asynchronous, so the Asset Hub extrinsic finalising proves the message was **sent** — a tracker showing *done* there tells a user their money landed when it has not. `DepositProgress`'s `credited` arm **requires the futarchy-chain observation**, so it cannot be reached from the AH leg at all. Each leg keeps **its own `Verified<T>`**: two light clients, two finalized blocks, and one status covering both would assert a cross-chain consistency nothing established. D-13's caps read **the bit** (V-115), and an **unreadable** cap **blocks** rather than proceeding without its limit. Degraded XCM health **warns**, because I-24 is fail-static and blocking would refuse what the chain accepts. Dusting is a **band, not a floor** — a full withdrawal is fine. And **unknown** destination viability is distinct from bad *and* from good, since collapsing it is what *"silently skipped"* means. Both `[VERIFY]`s respected. 7 mutations, 0 survived. 764 app tests green.

**What comes next:** The S12/S13 screens and the second Asset Hub light-client connection.

## F17 🔨 — the upgrade crank, where the artifact cannot…

F17 🔨 — the upgrade crank, where the artifact cannot reach a wallet unhashed

A runtime upgrade is the most consequential signature this client produces, and §11.8.4 step 3 requires the hash check **before** submission with the artifact *"never reaching the wallet"*. `VerifiedArtifact` is **branded**, `verifyArtifact` is its only producer, and `UpgradeSubmission` requires one — so unhashed bytes cannot be assembled into a submission; the corpus carries the fixture. The mismatch **throws**, because a refusal a caller may ignore is not a hard block, and the copy rules out **retrying gateways until one passes** — the natural move that softens it. **`applicable_at` is read, never recomputed** (SQ-552's lesson), and a test asserts there is **no lead-time arithmetic in the module at all**, so it cannot creep back. `submissionOutlook` **states uncertainty**: FE-P10 is open, so it says the multi-MB question is unestablished rather than implying it will work — and that the verification survives a failed submission. 4 mutations, 0 survived. 769 app tests green.

**What comes next:** F17's remaining consoles.

## F9 🔨 — provider health, and the guarantee stated with…

F9 🔨 — provider health, and the guarantee stated with its unflattering half

The never-promote rule gets **no code here**: `Finalized<T>` is unnameable outside `chain-client`, so this package cannot produce one whatever it does. F9 owns what the client **says** about a provider. `defaultProviders()` is empty **and takes no argument**, so nothing can inherit a default into it — that is how *opt-in* becomes *on for most people*. **Any** mismatch disables (§8.3 sets no threshold, and a threshold tolerates the first caught lie). **Unverifiable rows count neither way**, and `effectiveCoverage` reports what was actually comparable — otherwise a provider evades sampling by serving only unverifiable rows and a 2-of-100 round reads as a 100-row check. The auto-disable reason is **required**, since a source vanishing unexplained reads as a bug and gets switched back on. And §8.4's normative copy keeps the half that is unflattering: sampling **does not** detect a self-consistent forgery at unreachable depth. 5 mutations, 0 survived. 775 app tests green.

**What comes next:** F9's snapshot tooling and sampling loop, which need F8's transport.

## The composition root, and three screens that were…

The composition root, and three screens that were hiding in plain sight

The outlet's `implemented` map had been an empty literal in `boot.tsx` since F7. Moving it to a real composition root and adding `unaccountedScreens()` — *every inventory screen is either implemented or declared pending, never neither* — immediately found **S2, S21 and S22 in neither map**. They are *built*, so they were not listed as pending; they are *unwired*, so they were not registered. The outlet therefore rendered them as **"not in this build" with no owning milestone named**, which reads as *coming soon* indefinitely and is exactly how a screen disappears. `PENDING_SCREENS` now means *"this build does not reach it"* and its entries **say which of the two reasons applies**, because *not built* and *built but unwired* need different work and a reader who cannot tell them apart does the wrong one. The composition root also stays deliberately **empty rather than registering screens against placeholder data** — an app that looks finished while showing invented numbers is what every provenance rule here exists to prevent. **A control-character bug, the second this session:** a `\b` written through a shell heredoc became a literal **backspace byte** inside a regex, so the pattern matched nothing and the assertion failed on a string that plainly contained the word. Found with `cat -A`, the same tool that found the NUL byte in `tests/receipts` during F20. The tree is now swept — **0 files carry a control character** outside tab/newline. 778 app tests green.

**What comes next:** Track F: 13 ✅ · 8 🔨 · 2 ⬜, and only F14 (device lab) and F22 (attested manifest) are untouched.

## A gate for the defect class this repo keeps…

A gate for the defect class this repo keeps rediscovering — **defined and unreachable**

`FE-HANDOFF-012` shipped with **zero emitting call sites**, `FE-HANDOFF-010` was the same shape a milestone earlier, the `>>> 0` normalisation could not fail, and three screens sat in neither the implemented nor the pending map. Same defect four times, and no green run distinguishes any of them from a control that simply never fires. 11 §11.2 constraint 3's five facts are the highest-consequence instance — each changes the meaning of a signature — so `check:above-fold` finds every `aboveTheFold(<fact>)` call site **off the AST** (a first regex pass missed the multi-line ones and reported three unemitted when only two were) and requires each fact to have an emitter **or** be declared with the milestone that will supply one. A **stale declaration fails**, so the promise cannot become a permanent excuse. **Its limit is measured, not assumed:** the gate asks whether a fact has *an* emitter, not the *right ones* — §11.10 needs the sudo banner on every route **and** on every confirm screen, so deleting the shell's emitter leaves the confirm one and the gate stays green. That is covered, and I checked rather than hoped: removing it fails **three** assertions in `tests/screens`. The division is deliberate — a call-site gate cannot know *where* a fact belongs, and a render test cannot know whether anything still constructs one. 3 mutations; the one that "survived" is the documented limit, not a hole.

**What comes next:** Track F: 13 ✅ · 8 🔨 · 2 ⬜.

## F17 🔨 — the guardian console, where every control…

F17 🔨 — the guardian console, where every control exists because the signature is hard to undo

E20 admits a playbook **only under a verified trigger**, so `TriggerState` carries an `unread` arm treated exactly as `inactive` — anything else puts *could not check* on the same side as *checked and fine*, on the one power whose justification is that the condition holds now. The two refusals are **worded differently** because the guardian's next step differs. **Approving twice is its own refusal**: a guardian who re-approves believes they moved the count, and a generic message leaves them unable to tell whether the action is stuck or they are. An **overrun allowance reads as zero**, because a negative is headroom to any arithmetic downstream. And the ratification consequence names **their own bond** — half slashed, recalled, *whether or not the action was correct* — for every power rather than only playbooks. 6 mutations, 0 survived. 784 app tests green; App (Track F) passed on `72888a95`.

**What comes next:** F17's reporter console (SQ-564 gates two of its preconditions), treasury streams, the snapshot crank, the incident registry.

## F17 🔨 — treasury streams, where the last unit is…

F17 🔨 — treasury streams, where the last unit is somebody's money

Vesting is integer `bigint` arithmetic that **floors against the claimant** (R-7): a float rounds a stream's last unit in or out of existence. **Not-started and fully-claimed are different zeros**, because a bare zero for both tells a recipient the wrong next step. A stream whose **end is not after its start** is refused rather than divided by — the *treat-it-as-instant* reading would credit the whole total from malformed chain state. And E1's ruling is structural: `INSURANCE` is a **classification against its target**, never a bare balance, because a rising balance is not protocol income and a component handed the raw field could plot it as earnings. 6 mutations, 0 survived. 790 app tests green.

**What comes next:** F17's reporter console (SQ-564), snapshot crank and incident registry.

## F17 🔨 — the snapshot crank and registry; F17's model…

F17 🔨 — the snapshot crank and registry; F17's model layer is complete but for SQ-564

P-15's *"never sign a guaranteed no-op"* made visible: the `no-op` arm is **distinct from `ready`** and its copy says the transaction *would pay a fee and change nothing*, which is the part a user cannot see because the button looks identical either way. §11.8.6's required sentence exists because **the natural reading is wrong** — an open challenge holds the **settlement**, not a governance decision, and the opposite belief credits an incident filing with leverage the design withholds. An **unread watchtower extension** makes the deadline `indeterminate` rather than defaulting to the base window: a countdown ignoring an extension tells a challenger they are out of time when they are not. 5 mutations, 0 survived. 794 app tests green.

**What comes next:** Only F17's reporter console remains, and **SQ-564** gates two of its preconditions.

## F17 🔨 — the reporter console; SQ-564 gates…

F17 🔨 — the reporter console; SQ-564 gates *completeness*, not the build

The dangerous response to an unreadable precondition is a console that checks what it can and shows a green *ready to sign*: a registration the chain will refuse then looks identical to one it will accept, and the user learns the difference by losing a fee and being told nothing. `RegistrationCheck` carries **`uncheckable` as a required field** — no shape of the result omits the two unreadable conditions, so no screen can claim a complete check. Each is named with its **dispatch error** and with *why* it cannot be read, because *"this might fail"* teaches nothing while `ReporterEjected` is recognisable when it arrives. The caveat does **not** say *ready to sign* and states the cost of failure: the stake is not taken, the fee is. 4 mutations, 0 survived. 798 app tests green. **F17's model layer is complete.**

**What comes next:** The rendered consoles for F16/F17/F18 over the models now in place.

## The render-provenance gate — a control the source…

The render-provenance gate — a control the source *claimed*, and the promotion hiding behind it

`datum.tsx` had documented `check-render-provenance.ts` as the half of INV-FE-9 that types cannot enforce. **The file did not exist**, and the hole it named is real: `<Panel title={`Referendum ${id.value}`}>` typechecks and puts a chain read on screen with no badge — worse for an *identifier* than for a quantity, because the failure is a user acting confidently on the wrong object while every badged figure below describes something else. Built **type-aware**, since a grep fires on `key=`, on `event.currentTarget.value` and on the verification panel's release-constant rows, and a gate that fires on correct code is deleted rather than fixed. It found 4 display sites — and its **second rule** found the thing worth building it for: `{ value: a.value - b.value, status: a.status }`, in two places. A derived figure wearing one input's badge **promotes provider data to verified by arithmetic**, which no type and no firewall rule can see. `combine`/`combine2` now takes the weakest input's status and **refuses outright when the reads span different blocks** — their difference is true of neither, and no status can say that — so a guardian power whose remaining allowance cannot be established is *not offered*, and the ratification panel says *unknown* rather than silently withholding a warning it shows only on `no`. The witness taught two more: `getPropertiesOfType` returns `[]` for an unresolved generic reference (rule A was off), and the fixture is not a workspace package so **every rule inspected an unresolved type while reporting only "did not fire"** — now an assertion, because that reads exactly like a rule that broke. 9 mutations, 0 survived. 815 app tests green.

**What comes next:** Track F: 13 ✅ · 8 🔨 · 2 ⬜.

## F17's screens — S14…S19 rendered, and a pending map…

F17's screens — S14…S19 rendered, and a pending map that can no longer lie

The consoles over the finished models: **both** approval numbers, never a percentage (at 4-of-5 versus 5-of-7 a bar looks the same and the decisions differ entirely); **every** blocking reason, since a guardian who fixes one and hits the next learns the screen is guessing; the ratification consequence **before** the propose button and on the clean path too, which is exactly where a screen is tempted to drop it; the reporter's two unreadable conditions shown **when nothing else blocks**, because that is the only path where a green *ready to sign* could form; a no-op crank **disabled** with the fee stated; and a hash mismatch rendered as a **refusal with both hashes, never a retry** — re-downloading cannot make different bytes into the authorized runtime, and offering *try again* miscasts an identity failure as a transport one. The new gate caught **my own code** minutes after I wrote it: `claimableNow` is derived from six reads and both call sites had borrowed `claimed`'s status, so it now returns `Combined<Claimable>` and a claim whose amount cannot be established is **not offered** — this is somebody's money. **`PENDING_SCREENS` had already gone stale within a day**: nine entries still read *"F17 — the reporter console"* for screens that were by then built. Prose could not hold it, so each entry names the **component** it waits on and the suite checks **both directions** — a `built-unwired` entry whose export is missing is a false promise, and a `not-built` entry whose export **exists** now fails the build the moment it lands. That second direction is the whole point: a declaration that cannot outlive the condition it describes. 19 mutations, 0 survived — one initially survived and exposed a real test defect (the reasons were matching inside the button's `title` attribute, so a screen rendering only the first passed). 833 app tests green.

**What comes next:** Track F: 13 ✅ · 8 🔨 · 2 ⬜. F17's screen layer is complete; F8's transport is what unblocks registering any of it.

## F17's §11.8 compliance read — which is why F17 is…

F17's §11.8 compliance read — which is why F17 is **not** ✅

Run before considering the milestone done, and it changed the answer. §11.8.2 requires the approve flow to render *"the **exact enumerated call batch** … decoded and displayed, never summarized away"*, and `ApproveAction` showed a justification hash and nothing else — a guardian could put the system's most privileged signature behind calls **nobody had seen**. `PendingAction.calls` is now required, an undecodable call has no name field to guess from, and **both** an undecodable call and an **empty** batch block the approval: on powers that cannot be undone, *could not decode* must not land where *decoded and fine* lands, and an empty batch reads as harmless while meaning the batch could not be read at all. §11.8.5's *"staleness shown prominently"* is built too, with **both thresholds as required arguments** — *"> 4 days"* is a chain tunable and baking it in would leave the client warning late after an amendment. **Five requirements remain and are now enumerated in F17's row** rather than summarised as *"FE-P10 and SQ-564"*, which is what the old cell said and which would have let the next session mistake a five-item gap for two external blockers. A mutation also taught something: adding `hidden` to the batch list left every *"the call names appear"* assertion green, so the container is checked for markup-level concealment — and the CSS-level limit is written down rather than implied. 9 mutations this round, 0 survived; one apparent survivor was my own harness quoting bug, re-run properly and killed. 838 app tests green.

**What comes next:** Track F: 13 ✅ · 8 🔨 · 2 ⬜. F17's five remaining §11.8 clauses; F8's transport still gates registering any screen.

## §11.8.1's evidence rules — a re-hash, two distinct…

§11.8.1's evidence rules — a re-hash, two distinct failures, and the XSS gate the client did not have

Evidence is adversary-chosen bytes rendered where the most privileged actors decide disputes, so §11.8.1 asks for three separate things and the client had none of them. **Re-hash before rendering**, with `computeHash` a *required* argument — an optional digest function is a check that defaults off, which is how `FE-HANDOFF-010` shipped unreachable. **Unavailable ≠ mismatched**: 07 adjudicates unobtainable evidence as *absent*, so a blank panel would let a reader conclude none was filed, and only the mismatch arm says another gateway may help — this one served something else. **Never HTML**, which no type can enforce, so `check:no-html-sinks` (+ `:witness`) fails on six forms across all app source. Its list is **closed rather than a pattern** (*"anything ending in HTML"* fires on `escapeHtml`, `parseHtml`, `htmlLang` and gets switched off) and it runs on the **AST**, so the sinks discussed in its own header are not findings. Proven by mutation rather than by a green run: putting `dangerouslySetInnerHTML` into `EvidencePanel` fires the gate **and** fails the escaping test. Also pinned — invalid UTF-8 shows replacement characters rather than blanking the document, because refusing on one bad byte hands a filer a way to make their own evidence unreadable while still hashing correctly. 8 mutations, 0 survived. 843 app tests green.

**What comes next:** F17's four remaining §11.8 clauses (recompute_proof, the pending list's target, the `nav()` view, the registry filing path).

## §11.8.3's `nav()` view — a section that reads as a…

§11.8.3's `nav()` view — a section that reads as a field list and is really a list of prohibitions

Three renderings you would naturally build are the forbidden ones, and each is now structural. The account balances are a **partial** view — nine `ops.*` lines have no field — so the obvious table with a sum at the bottom is *wrong*: every line carries a **required** partial-view note and the module exports **no function that sums them**, because a reader who sees a decomposition assumes it decomposes and then reads the residual as a chain error. *"Never renders full backing while the flag is set"* is one `<Amount datum={nav.total}>` away at all times, so the `haircut` arm has **no `headline` field** and adding one is a compile error — with the residual stated rather than hidden (a component also holding the raw view can still do it by hand, so the render test asserts no bare `NAV` label, and the healthy render is the anti-vacuity control). And an income figure **cannot exist unbounded**: `WindowedIncome` requires its block window, and `InsuranceSwept` — deliberately outside 02 §5's ingest set — has no slot at all. Class-floor distance is continuous and measured against **`spendable_nav`**, never `total`: under a haircut that is 0 and every floor is genuinely unmet, while `total` would report floors met on a spend the protocol refuses. 8 mutations, 0 survived; two were compile errors, which is the strongest form of the claim. 849 app tests green.

**What comes next:** F17's last three §11.8 clauses: `recompute_proof`'s local recomputation, the pending list's target, and the registry filing path with its pallet-bound ingest filter.

## The last two §11.8 clauses — a filter that must bind…

The last two §11.8 clauses — a filter that must bind the pallet, and a proof that cannot be submitted unreproduced

`pallet-registry` and `pallet-oracle` both publish `WindowAcknowledged`/`WindowExtended`, with **different fields**. A filter matching the variant name compiles, runs, and quietly ingests events about a different sub-game — the symptom is a registry countdown that moves when an *oracle* watchtower extends an *oracle* round, and a challenger who trusted it misses their window. Checked **both ways**, because one alone is weaker than it looks: the pallet must match **and** the body must not carry `component`/`round`, since a right-label wrong-body event is the same failure by another route and checking only the label trusts the labeller. Refusals are returned rather than dropped — an event this client will not read is information, and a countdown built on an unaudited stream is how the wrong deadline renders. Then §11.8.1's `recompute_proof`: *"never submit a proof the client's own recomputation contradicts"* is not a comparison a caller can be asked to remember, so `RecomputedProof` is branded with one mint site and the submission type requires one. The **evaluator is a required argument** — `FE-HANDOFF-010`'s shape for the third time this track — and a **non-deterministic component is refused before the evaluator runs**, because a disagreement there looks like fraud when it is really a component nobody promised would reproduce. 10 mutations, 0 survived; two exposed real test gaps (an empty evidence hash, and a dropped target column). **All six §11.8 gaps from V-117 are now closed.** 858 app tests green.

**What comes next:** F17's residue is external: FE-P10 is an empirical measurement, SQ-564 a contract addition. F8's transport still gates registering any screen.

## F8 🔨 — the ingestion loop, where the only risks are…

F8 🔨 — the ingestion loop, where the only risks are order and continuity

The decision layer was already complete and tested; what was missing is the loop, and its failures are ones no unit test of a pure function can see. **Coverage advances only after the write lands** — the tempting shape advances per scan and writes in the background, and a crash then leaves coverage claiming blocks whose rows were never stored, which is fatal precisely because coverage is *what the client consults to decide it need not re-fetch*. **A skipped block is a hole, never a span**: subscriptions drop, and the cheap `addRange(selfRange(first, latest))` on resume claims every block in between as self-ingested — 10 §6.3's forbidden promotion reached by a *reconnect* rather than a merge — so the loop adds one single-block range at a time and lets `addRange` join only genuine adjacency. **A failed body fetch fails the block**, because recording it as ingested while its attributed extrinsics are missing produces a filtered history, and a filtered history is indistinguishable from an empty one. Sequential by construction: ingesting concurrently breaks the first rule by parallelism with an identical symptom, and the suite asserts the interleaving. 9 mutations, 0 survived — **and the first attempt at rule 1's mutation was a no-op I wrote myself**, which reported *survived* and reads exactly like an untested control; worth recording, because that is the one way this method quietly lies. 867 app tests green.

**What comes next:** F9's snapshot import and sampling, over the loop now in place.

## F1 🔨 — FE-P6 answered without hardware, and the answer…

F1 🔨 — FE-P6 answered without hardware, and the answer is that the *chain* is the blocker

10 §12 files FE-P6 as a hardware test. Reading the pinned `frame-metadata-hash-extension` settles the load-bearing half instead: the digest is a **compile-time** `RUNTIME_METADATA_HASH` env var that `substrate-wasm-builder` sets only when metadata-hash generation is enabled, and mode `Enabled` without it returns `UnknownTransaction::CannotLookup`. This runtime declares `CheckMetadataHash` in its **frozen `TxExtension` stack** — so a reader concludes hardware signing is supported — while `build.rs` calls `build_using_defaults()` and no `metadata-hash` feature exists anywhere. **The chain rejects every mode-1 transaction today**, which means the hardware test would have returned a correct device and a refusing chain, and somebody would have spent a day on the wrong layer. The *declared-and-unreachable* defect class again, this time in the runtime. Ruled **SQ-594**, routed to new milestone **B21**, with both build values **derived** rather than picked: `enable_metadata_hash("VIT", 12)` from the 02 §8 chain-spec properties `validate-chain-spec.py` already asserts. Not executed in-session on purpose, and the row says why: the wasm bytes move, so the chain feed, descriptors and surface manifest all need regeneration plus the exhaustive Rust gate. **The client was already right** — `packages/signing` grants no `metadata-hash` capability and has no function that could — so what changed is that its refusal now cites a verified chain fact instead of an open prototype gate. F1's remaining gates are now **all** device-lab, hardware, testnet or credential work; none is answerable by analysis, and the row says so per gate.

**What comes next:** The next open Track F milestone — F1 cannot proceed further from here, but the track can.

## F8 🔨 — the loop's durable half, where the whole design…

F8 🔨 — the loop's durable half, where the whole design is one transaction

The in-memory invariant (coverage advances only after the write resolves) does not survive a crash on its own. Two separate persists have two orderings: *rows then coverage* is harmless — a re-ingest, free because ids are deterministic and replay is idempotent, which the suite asserts rather than assumes — while *coverage then rows* leaves `isVerifiedAt` answering `true` forever for a block with nothing behind it, in the very structure consulted to decide the client need not re-fetch. So `BlockWrite` carries `coverageAfter` and `storeWriter` commits events, rows and coverage in **one Dexie `rw` transaction**, which removes the choice rather than documenting it. Run against a real IndexedDB, because the property *is* atomicity and a stub grants that for free. **Mutation-proven in the way that matters**: rewriting the adapter as coverage-first-then-rows fails the rollback test, so the test tests atomicity rather than merely that a write happened. The row's `origin` is also pinned in both directions — a body fetched behind a layer-2 header stores as `operator`, a self-header one as `self` — because a store that quietly upgraded it would make a depth-fetched body indistinguishable from a verified one for every later reader. 4 mutations, 0 survived. 871 app tests green.

**What comes next:** F9's snapshot import and sampling, which now has a loop and a durable store to sit on.

## F9 🔨 — the import quotas, where three of the four…

F9 🔨 — the import quotas, where three of the four clauses are about *when*

10 §8.4's sentence reads like one limit and is four controls. **"Streamed"** is where the quota is enforced: a check after the file is read is enforced by the browser running out of memory, which is a crash with a moral rather than a refusal — so the meter refuses at the crossing chunk and has no after-the-fact variant. **Both bounds**, because 400 MB in 100 rows and 4 M rows totalling 10 MB each pass one and fail the other, and both break something real. **"Before import"** is the entire third control: §9.2's ladder means a large snapshot evicts what is already stored, and the user's decision is whether to trade what they have for what the snapshot offers — possible only *while they still have both* — so `planImport` writes nothing and the entry point requires its result. Oldest-first eviction, stopping as soon as it has enough; an infeasible import **reported rather than thrown**, because a plan that will not construct cannot explain itself and *"import failed"* is what makes somebody clear their data by hand. The meter is **pure**, so one import cannot inherit another's totals — a stateful one reads as a smaller snapshot than it is. 6 mutations, 0 survived. 879 app tests green.

**What comes next:** F9's live sampling loop, which is the last piece needing a real transport; `tools/snapshot` itself is the producer side.

## F18 🔨 — the funding screens, where the failure is…

F18 🔨 — the funding screens, where the failure is flattening a warning into a block

Every refusal was already decided in `funding.ts`; what a rendering can still get wrong is the **block/warning distinction**, and §11.9 is most explicit exactly there. Degraded XCM health and an unverifiable destination are *warnings*: I-24's fail-static property means the funds are held, not lost, and a client that refused what the chain accepts is the failure 15 §4.8's mirror rule forbids. Rendered in the same red box as a real block, the user reads *"there is a problem"* and does not send — a lawful deposit stopped by presentation. So the two arrive at different severities and the suite asserts a **warning-only state leaves the control enabled**, which is the assertion a happy-path suite never makes and a cautious developer breaks first; a real block disabling it is the anti-vacuity control. *"Sent"* is never rendered as *"arrived"* — the model already makes `credited` unreachable from the Asset Hub leg, and the screen does not undo it in copy: the block is labelled **as the Asset Hub's**, because a bare block number with no chain beside it is how a user concludes the transfer landed. The two chains' figures never share a badge. **The `PENDING_SCREENS` expiry check fired on its first real occasion** — building these two components turned it red immediately, which is what it was built for. 4 mutations, 0 survived. 883 app tests green.

**What comes next:** F18's second Asset Hub light-client connection; F9's live sampling loop. Both are transport work.

## F8 🔨 — the transport's finalized-block surface, and an…

F8 🔨 — the transport's finalized-block surface, and an array the head reading discards

Wiring the ingestion loop to `ChainHeadConnection` surfaced a defect in the shape of an API rather than in any line of code: `chainHead_v1_follow` reports finalization as `finalizedBlockHashes`, an **array**, and the connection keeps `.at(-1)`. That is exactly right for the finalized *head* and exactly wrong for a consumer that must **see each block** — an ingestion loop built on it skips every intermediate block of a multi-block finalization, and because the loop turns unseen blocks into holes rather than spanning them, the symptom is not corruption but a coverage display that is permanently gappy under normal operation, which is a gap indicator nobody can read. `onFinalized(listener)` now emits every hash in order, after the pin announcement so a listener can read at what it is handed. **One of my own comments was wrong and is corrected rather than softened**: it claimed emission must precede `#unpin` so a listener never sees a released hash — `#unpin` takes `prunedBlockHashes`, disjoint from the finalized set, so the ordering does no work. A reassurance with nothing behind it is what a later reader relies on. 4 mutations, 0 survived. 885 app tests green.

**What comes next:** The scan→ingest pipeline over this surface, then F9's live sampling.

## Reading 10 §6.5 *after* building against it — one…

Reading 10 §6.5 *after* building against it — one agreement and one gap

Two findings, and the order matters: implementing first and reading second tests whether an implementation and a spec agree **by accident**. (1) §6.5 already says *"cursor-range advance in the same IndexedDB transaction"* — the atomicity I derived from first principles hours earlier is **normative**, and `storeWriter` satisfies it rather than exceeding it. Recorded because independent convergence is the cheapest evidence that neither reading is wrong. (2) The same sentence continues *"single-writer via the leader's `fut-ingest` lock"*, and **no such lock exists**: `navigator.locks` appears in no package. Two tabs ingesting would double-write and race on coverage, which per-write atomicity does nothing about. A real F8 gap, now in its row — §4.4 specifies the mechanism, and FE-P3's open half is about Web Locks *on Safari*, not about whether to use them. Also filed **SQ-595**: `attributedExtrinsics` refuses an event index ≥ `scan.extrinsicCount` and §6.5 names no source for that count at scan time, since a block with no watched extrinsic never has its body fetched. Filed with a recommendation rather than decided, because resolving it *weakens an existing shipped control* and doing that silently is the opposite of what the control is for.

**What comes next:** The `fut-ingest` lock, then the scan adapter once SQ-595 is ruled.

## F8 🔨 — the `fut-ingest` lock, and a fake that was…

F8 🔨 — the `fut-ingest` lock, and a fake that was granting the property it tested

10 §6.5 requires single-writer ingestion via §4.4's lock and the client had none. What breaks without it is **not** the rows — ids are deterministic and `bulkPut` idempotent, which is why §6.5 pairs the lock *with* idempotence — but **coverage**: each tab holds its own in-memory copy and writes it wholesale, so the last writer wins and a stale tab erases a live one's advance. Both writes are individually atomic and mutually destructive, which is precisely what a per-write transaction cannot see. Absence of the Web Locks API **refuses** rather than falling back, because *"run unlocked just this once"* is the double-writer reached by the one path nobody tests; the refusal states its bounded cost, since the index is an accelerator and INV-FE-7 keeps it off the transaction path. Requested with `ifAvailable` rather than queued: a follower that waits becomes the ingester the moment the leader closes and resumes from whatever coverage it last read, which may be minutes stale — §4.4 gives ingestion to the `fut-leader` holder, so a tab that cannot take the lock immediately is not the leader. **The finding worth carrying is in the test, not the code**: the Web Locks fake tracked name occupancy only, so `shared` and `exclusive` behaved identically and the mode mutation survived while every test passed. A stand-in built from what the caller needs rather than from what the real API guarantees tests the caller against itself, and it looks *simpler* in review. 7 mutations, 0 survived after the fake was fixed. 892 app tests green.

**What comes next:** The scan→ingest adapter, once SQ-595 is ruled.

## SQ-595 ruled — and the ruling shipped a latent bug that…

SQ-595 ruled — and the ruling shipped a latent bug that only mutation could see

§6.5's loop fetches a body **only** for blocks with a watched extrinsic, so at scan time there is usually no extrinsic count and the section names no source for one. Of the three ways out, two are worse than they look: **deriving** the count from the events makes the index check *vacuous by construction* — a check that cannot fail, the defect class this client keeps finding — and **always fetching** a body breaks the cost claim outright. So the count is optional and the guard **moves** to the fetched body, which is both the authoritative count and the place decoding actually happens. **The interesting part is what the change broke on the way in**: with the count optional, `bodies.length !== scan.extrinsicCount` compares against `undefined` and is therefore always true — and every existing fixture supplied a count, so no test evaluated that branch. Caught by mutation, split into two checks, and pinned with a **boundary** case (index N against N extrinsics) after an off-by-one survived: a comfortably-out-of-range fixture cannot distinguish `>` from `>=`, and the second mutation showed the bound was wrong in the *other* direction too if nudged. 3 mutations after the repair, 0 survived. 895 app tests green.

**What comes next:** The scan→ingest adapter is now unblocked; F18's second Asset Hub connection is the other buildable item.

## Review remediation — PR #234's three Codex findings and…

Review remediation — PR #234's three Codex findings and two CodeQL alerts

All five closed, and the two P2s were confirmed **against pinned source rather than against my reading of it**. **P1 (V-129)**: the served `release.json` always carried `releaseTxid: null` while its own parser required the field, so every real deployment was refused as `unpublished` — and the field is *unfillable*, since it names a manifest containing the file that would have to name it. 12 §1.2 already resolved the circularity; the consumer had drifted from it. Two mechanisms hid it: a **previous review round fixed it from the wrong side** by adding the field to the producer, and the test asserted on the driver's returned object rather than the uploaded bytes. **P2 (V-130)**: `pallet-multisig` 49.0.0's `operate` reaches its execution branch *before* the `AlreadyApproved` arm, so a 2-of-2 whose members both used `approve_as_multi` could be completed by either — and the client told both they could not. **P2 (V-131)**: `selfRange` minted verified provenance from three plain numbers and was in the package barrel, so `providers` could launder backfilled data; now behind a `/testing` subpath with a witnessed firewall rule. The **CodeQL** alerts were test-only regex uses, rewritten as matches rather than suppressed — and the `screens` one turned out to be **dead code** whose replacement is strictly stronger (it catches `signPayload(`/`signRaw(`, which the original could not). One defect was my own and caught by my own test (V-132): `new URL()` lowercases hostnames, so reading a TXID from a subdomain returned a well-formed wrong address. 12 mutations across the four fixes, 0 survived. **904 app tests green**, full suite exit 0.

**What comes next:** Migrate `app/`'s 79 hand-written JS files (51 `.js` tests, 25 `.mjs` tools, 3 `.cjs` configs) to TypeScript with a `tsc --noEmit` project over `tests/`+`tools/` — user instruction 2026-08-05; Node 22.18 runs `.ts` tests directly and `erasableSyntaxOnly` is already set. Then F8's scan adapter.

## The TypeScript migration's first slice, and a hole it…

The TypeScript migration's first slice, and a hole it exposed in every suite

**Measured before migrating** (1,974 errors, 1,499 after installing `@types/node`, which was absent), then converted four suites and wired `check:types` into `pnpm test`, with `tsconfig.tests.json`'s `include` growing per clean slice so the gate is green throughout rather than red for the whole migration. It paid immediately — two malformed fixtures in the first five files, including a `planImport` call missing a required field in a test written hours earlier. **And it exposed V-133**: `node --test` exits 0 on a glob matching nothing, so renaming four suites silently emptied one and the run still passed. `tools/run-suite.ts` now fails a suite that executed zero tests — a hole that predated the migration and applied to **every** suite in the repository, which is why it is worth more than the slice that found it. 904 app tests green, full suite exit 0.

**What comes next:** 18 migration slices remain (largest: screens 343, transaction-builder 195, tools 180).

## Generated-artifact normalization, landed on `main`…

Generated-artifact normalization, landed on `main` ahead of the feature work

PRs were running 66–73k added lines and the cause was measured rather than guessed: **PR #228 was 96 % machine-generated** (63,817 of 66,345; 2,528 hand-written). Two artifacts carry almost all of it, and both are now normalized **at their generator** so the tree and the producer cannot disagree: chainHead transcripts 29,151 → 259 lines (lossless, round-trip asserted per file, re-serialized through the recorder's own `deterministic_json`), and PAPI's `.d.ts` 34,876 → 30,332 via a prettier step inside the drift checker, with `descriptors:generate` rerouted through `--update` so there is exactly one normalization site. Three measurements corrected a confident wrong answer along the way: `.gitattributes` `-diff` collapses GitHub's *display* but **not** its `+/-` counters; the TypeScript printer makes `.d.ts` **larger**; and prettier's `--print-width` alone does nothing because it preserves an object's original line breaks — `--object-wrap collapse` is the flag that works. Doc comments are kept (77 % of the remaining lines): stripping them reaches −80 % and costs hover documentation on every chain surface, which is the wrong trade in solvency-critical code. Deterministic across three regenerations; `descriptors:check`, `check-chain-feed.py`, `tsc -b`, 186 app tests and 89 release-tooling tests all green.

**What comes next:** Rebase `track-f/feed-surface-gate` on this, then split it into the five approved stacked PRs.

## PR #234 rebased onto post-#235 `main` and split into a…

PR #234 rebased onto post-#235 `main` and split into a five-PR stack (#236–#240)

**The rebase found two defects and the split found a third, none of which a gate would have reported.** (1) `git rebase` drops merge commits, so the merge that renumbered this branch's spec questions after #233 took SQ-583/584 was lost and two files silently reverted to pre-merge ids. (2) **The same renumbering was already incomplete before the rebase** — five code sites and one log row still cited SQ-586 for the P-9 reserved-destination predicate, which is SQ-588; SQ-586 is now a *different* question, so each citation pointed a reader at the wrong finding while looking valid. That is the failure `FE-HANDOFF-009` stays retired to avoid, met from the other side. (3) Building the slices, probing *"does `tests/ui/` exist"* reported suites with **no test files at all**: pnpm leaves a `node_modules` symlink tree inside every workspace package directory — V-133's shape a third time, checking the container instead of the content. **The split is verifiably lossless**: #240's tree byte-compares equal to the branch, and each PR builds and tests green on its own base (188 / 600 / 810 / 904 / 904 tests, no empty suite). Generated bulk is isolated in #236, which leaves ~37,900 hand-written lines across four PRs of 1k–20k. Also corrected: taking PLAN.md's incoming version at each replayed commit is right for every intermediate state but would have **deleted** #235's two rows from an append-only log; both restored, and the branch's two near-duplicates trimmed to what is genuinely this branch's.

**What comes next:** Merge #236→#240 in order as CI greens; then Track F's 6 🔨 / 5 ⬜ milestones.

## Merged #236; rebased the stack; fixed the four Codex…

Merged #236; rebased the stack; fixed the four Codex findings on #237

**#236 merged to main** (`73666ff7`, 22/22 green) and the four branches behind it rebased with their deltas verified unchanged by patch-id. Then the review round on #237: all four findings real, and checking them against the runtime instead of the report changed two answers. A P1 promotion (`selfRange` minted regardless of `headerOrigin`, so operator backfill read as light-client verified) and a P1 capability claim (`decoded-payload` granted for a channel `sign()` never uses — the app has no extrinsic encoder, so it cannot be used at all) are fixed structurally rather than by comment. The P-12 finding was fail-**closed** and its suggested fix fail-**open**; the runtime's two `ensure!`s settled it and exposed a spec defect in 11 §11.5 row 12, now split (Decision log). And fourteen gates — 296 assertions, including the 02 §7.7 `foreign:check` — had never executed in CI, because the wiring gate was one-directional in the direction that fails silently. Two of my own errors, both found by running rather than reading: a parser that read one `pnpm run` per line reported three wired witness legs as unwired, and a loose `grep '^OK'` matched a checker's stdout so I called `tools/ci/tests` green while four of its tests were red. 10 mutations, 0 survived.

**What comes next:** Push the rebased stack (**blocked: force-push denied by the sandbox — needs a permission rule or the user's `!` prefix**), then merge #237→#240. F1's remaining gates need hardware, ar.io credentials, seated people or a chain to measure against.

## TypeScript migration of the suites (task #31); V-140 /…

TypeScript migration of the suites (task #31); V-140 / V-141 found on the way

**The migration's first two slices found two defects, and the more serious one was found *because* the types refused to lie.** `tests/receipts` and `tests/contexts` were already TypeScript and had never been type-checked — `check:types` covered four slices, not six. Adding them surfaced 56 errors, 36 of them one shape: the suites could not construct a `Finalized<T>`. That is the brand working, so the question became how production builds one — and the answer was that `chain-client`'s barrel simply re-exported `finalize`, while `provenance.ts`'s own comment claimed it did not. **V-140**: a probe in `packages/transaction-builder/src/` minting a `Finalized<T>` from an invented literal compiled clean; two controls confirmed the probe was really compiled and that the brand still rejects a literal. Barrel closed, `@bleavit/chain-client/testing` added on the `signing/testing` precedent, `no-finalized-minting-outside-chain-client` written with its own witness line, corpus fixture pinned at the measured TS2724. Four controls, all mutation-proven. **V-141**: every suite's own `test` script globbed `*.test.js`, so the six already-migrated directories reported `# tests 0` with exit 0 — a green pass having run nothing, measured rather than assumed. Fixed to bare `node --test` and gated in both directions. The fixtures now call the real `finalize`, so fixture and production agree by construction rather than by care; `scopeFromConsent` was widened to `readonly string[]` because a validator whose signature asserts what it validates has an unreachable refusal. **910 app tests green across 16 suites, `check:types` clean on six slices, `tools/ci/tests` 206 OK.**

**What comes next:** Migrate the remaining ten test slices (`verify`, `screens`, `ui`, `intents`, `descriptors`, `protocol`, `chain-client`, `transaction-builder`, `local-index`, `release`) plus the 27 `tools/*.mjs`. **Still blocked on the force-push** — the four rebased branches cannot reach origin, so #237 keeps reporting `CONFLICTING` and the #237→#240 merge train cannot start.

## F1 (FE-P10) — and the runtime defect reading it found

With #235 merged, **PR #234 was split into five stacked PRs (#236–#240)**, verified lossless by byte-comparing the last slice's tree against the branch it was carved from. Then F1, rather than stopping at the milestone boundary. The Track F stack's F1 cell says every remaining prototype gate *needs a device lab, hardware, a testnet run or Arweave credentials, not analysis*; FE-P10's own text disagrees, because *transaction-pool/gossip size limits, peer banning on oversized transactions* are **constants in the pinned SDK**, not device behaviour. Reading them found something better than a number. The chain's acceptance ceiling is chosen by the call's `DispatchClass`, and the guard's permissionless `apply_authorized_upgrade` declared none — so it was `Normal`, capped **262,150 B below** an image of `MaxRuntimeCodeBytes`, the frozen 02 §9 constant its own pallet publishes, while the `frame_system` call it forwards to is `Operational` and fits. Fixed to `Operational` and pinned by a test that derives both sides and was watched failing first. 450 runtime + 74 guard tests green; `cargo fmt --all --check` and `clippy --all-targets -D warnings` clean on both crates. V-136, SQ-596 and the ruling logged.

**What comes next:** Merge #236→#240 in order as CI greens. Then FE-P2's execution half — the spec's own experiment is a *wire-level test with a lying mock peer*, and `tests/chain-client` already drives the production transport at the wire level from recorded transcripts. Then F17's remaining consoles.

## Stack rebased onto post-#241 `main`; V-142

**#241 merged, which orphaned both lines of work at once — the five-branch split stack *and* #242 — and re-running the rebase is where V-142 came from.** All six branches rebased and proven lossless by `git patch-id --stable` over each branch's own delta: split/2, split/3 and split/4 **byte-identical**, split/5 and the migration branch identical **excluding PLAN.md**, #242 identical excluding the two files that legitimately merged. Every conflict was an append collision in PLAN.md, resolved by keeping both sides — and a resolver written for it **refused its first hunk correctly-but-wrongly**, because it keyed Session log rows by date and two sessions on one day are ordinary, not an edit; fixed to use whole-line identity there. #242's `app/package.json` conflict was resolved as a **union** rather than by taking either side, since taking the incoming line would have silently reverted every gate `main` gained since that branch forked. **One failure was real and investigated rather than assumed to be rebase noise**: `health.test.ts` could not find `createPeerWatch`, which was a stale `dist/` left by the branch switch — `pnpm test` does not build first, so switching branches and testing without `pnpm run build` reads the previous branch's artifacts. 910 app tests green on the migration branch, 199 on #242, `tools/ci/tests` 206 OK.

**What comes next:** **Push all six branches** — still the only blocker, and now larger: nothing has reached origin, so #237 and #242 both report `CONFLICTING` against a `main` they are already rebased onto. Then merge #237→#240, then #242. Then the remaining ten TypeScript slices.

## Push unblocked; #237 CI repaired; `tests/ui` migrated

**The force-push is unblocked** — an explicit `Bash(git push*)` allow rule, added by the user's instruction, because the auto-mode classifier gates force-pushes *independently* of the `allow`/`ask` lists and had refused three times while nothing in the config denied it. All six branches are on origin. CLAUDE.md now records why the rule is needed and why a force-push is routine here rather than exceptional: `main` takes squash merges, so every merge orphans whatever is stacked behind it. **#237 then went red on the Reference model job — V-143**, a second consumer of 11 §11.5 that my row split had missed; fixed on `split/2-core-packages` where the doc change lives and propagated by rebase, all four deltas patch-id **identical**. **I broke the stack once doing that** and should record how: I passed each branch's own tip as the rebase *upstream* instead of its base, so `--onto` replayed an empty range and collapsed split/3, /4 and /5 onto split/2. Recovered from the loose SHAs — and the diagnosis was delayed by checking `.git/rebase-merge`, which does not exist in a **worktree**: the stale state was under `.git/worktrees/<name>/rebase-merge`, so the check reported *no rebase in progress* while one was jammed. `tests/ui` is migrated and in `check:types` (33 tests), which found a fixture building `coverage: {from, to}` against a `CoverageRef` that has neither field. **910 app tests, 700 reference-model, 206 tooling — all green.**

**What comes next:** Merge #237→#240, then #242, then open a PR for `track-f/ts-migration`. Nine test slices remain (~1,090 errors): verify, screens, intents, descriptors, protocol, chain-client, transaction-builder, local-index, release — plus the 27 `tools/*.mjs`.

## #237's CI failure was a *split* defect, not a code one…

#237's CI failure was a *split* defect, not a code one — V-144/145/146; `tests/protocol` migrated

**The red job was `Reference model`, and the reference model was never the problem**: its 700 tests pass on every branch and on `main`. The failure is the `tools/ci/tests` leg inside that job, and its cause is that **the PR split put halves of one change on different branches**. V-143's repair shifted 11 §11.5's rows, and the *mutation suite* that quotes that numbering — `test_check_dispatch_mirror.py` — was updated four commits later in `split/5`, so `split/2`, `/3` and `/4` each carried a doc its own anchors no longer matched. **Nothing local could see it**: my working tree is the top of the stack, where both halves are present. So every branch tip was re-verified in a detached worktree with the job's own legs, which found **two more instances** (V-146): seven `app` gate scripts defined on `split/3`/`split/4` whose `ci.yml` steps were in `split/5`, and — after moving them — the line-anchored `^\s*pnpm run` matcher on those branches, which cannot see the second half of `a && b` and so reported a correctly wired witness leg as unwired; that regex fix was *also* sitting downstream of the first `&&` step it must parse. Separately **V-145**: `app/release-out/` was never gitignored despite AGENTS.md saying so, and my `git add -A` had committed 3,712 lines of generated `release.json` + CycloneDX SBOM — while a later branch's `.gitignore` rule had already *looked* like the fix and untracked nothing. All five branches re-verified independently and the rebase proven lossless by `git patch-id --stable`, every delta differing only in the files deliberately moved. **`tests/protocol` is migrated** (task #31): `corpus.js` was the last hand-written JavaScript module in the test tree and is now typed TypeScript — `CorpusInteger = number | string` is *measured*, since the same field carries both shapes, and `TranscendentalRow.f` stays `string` so the differential's "widen this suite" guard cannot become dead code. Two guards were repaired rather than annotated: a corpus `side` string now checked (the kernel treats anything but `'long'` as SHORT, so a respelled corpus would have quoted the opposite side and V6 would still have passed) and `assert.notEqual(x, undefined)` replaced, which narrows nothing and let `undefined >= low` run as `false`. 45 protocol tests, 7 anti-vacuity checks, 2 mutations run and reverted; `check:types` now covers **eight** slices.

**What comes next:** Push all five branches, then merge #237→#240 and #242. Eight test slices remain: verify, screens, intents, descriptors, chain-client, transaction-builder, local-index, release — plus the 27 `tools/*.mjs`.

## #31 (TypeScript migration) · V-142 · V-143

**The `app/` JavaScript→TypeScript migration is complete.** Every test slice and every tool is TypeScript, and `pnpm run check:types` covers all of it: 19 suites plus `tools/*.ts`, `tools/release/**` and `tools/verify-release/**`. Closed this session: the release suite (the last of 19), the 10 release/verify-release tools, the 14 `tools/check-*` / `tools/generate-*` checkers, and the four dependency-cruiser `.cjs` files. **The four `.cjs` were the open decision, and it resolved better than the two options I had framed** — dependency-cruiser reads its config with `import()` for `.js`/`.cjs`/`.mjs` and JSON5-parses anything else, so a `.ts` config cannot exist; but an `.mjs` config CAN `await import()` a `.ts` helper that Node 22.18 type-strips (measured against depcruise 17.0.1). So the two *logic* modules became `tools/depcruise-external.ts` and `tools/handoff-packages.ts` — the files that have shipped two vacuous-matcher defects (V-86, V-92) — and only the rule data stayed JavaScript. **No generated JavaScript was committed**, which was the point of the user's rule. Both depcruise legs are byte-identical before and after: 159 modules / 291 dependencies / 3 pre-existing warnings, witness 14 errors. `tools/verify-release/` became a workspace member so its one `@bleavit/verify` type import is declared where it is used — a root devDependency would have been reachable from every package by the parent-directory walk and quietly weakened pnpm's isolated-`node_modules` firewall. **Typing found four real defects the JavaScript accepted** — see V-142/V-143 and the two below. Full chain green: 910 tests across 16 suites, every checker gate, every witness leg.

**What comes next:** F17/F18. Three JS files remain and each is structurally required to be one (see Current focus); the stack still needs its force-push.

## #31 shipped as #243 · #238 merged · the stack rebased ·…

#31 shipped as #243 · #238 merged · the stack rebased · V-150/V-151

**The migration's last slice is committed and pushed, and #31 now has a PR (#243).** Two commits: the `app/tools/` half (every checker, generator and release-pipeline module `.mjs` → `.ts`; `check:types` covers 19 suites plus `tools/*.ts`, `tools/release/**`, `tools/verify-release/**`) and the spec half. Gate re-run on the staged tree before committing: **910 tests across 16 suites, 0 failures**, plus `build`, `depcruise` and `depcruise:witness`. **No generated JavaScript is committed** — Node 22.18 type-strips these directly, so the migration removes JavaScript rather than adding a build step that re-creates it. **The user merged #238 mid-session**, which orphaned everything stacked behind it, so the four remaining branches were rebased onto `7248d798`: the three split rebases replayed clean and **patch-id-verified lossless** (1 / 3 / 14 patches identical), and `track-f/f1-gate-sweep` (#242) needed real resolution in `app/package.json` + `.github/workflows/ci.yml`. **Both sides were additive in both files, and the trap was the `test` chain**: `git` offers two whole `"test":` lines, and taking both makes a *duplicate JSON key* whose last occurrence silently wins — that would have dropped 14 gates from every run while `pnpm test` still exited 0. Caught by a duplicate-key scan, which is now part of the drill. Resolution verified on the rebased branch (`tsc -b` clean, `check:smoldot-surface` green over the pinned PAPI/smoldot pair, its witness fires on an invented method, `check:above-fold` survives). All four pushed with **pinned** `--force-with-lease=<branch>:<sha>`; #242's `CONFLICTING` state is cleared and all four PRs are `MERGEABLE`.

**What comes next:** **Merge train: #239 → #240 → #243**, each needing the same rebase drill after the one below it squashes; #242 is independent of the stack and can land any time. CI is running on all four. Then F18 — one item: a second smoldot chain in the boot reducer with **its own** `Verified<T>` lineage (`packages/chain-client/src/boot.ts`, `topology.ts`). F1 stays PARKED on the device lab / testnet / ar.io credentials.

## F1 PARKED (properly) · V-152 · F18 started — the…

F1 PARKED (properly) · V-152 · F18 started — the two-chain provenance rule made structural

**V-152 first, because it invalidates a claim I made earlier in the session.** The Stop-hook escape for F1 never engaged: `guard-track-goal.sh` matches `^>\s*\*\*PARKED:\*\*` and the line written was `> **PARKED: …**`, opening the bold at the colon and closing it two lines later. The hook kept blocking and **F1 was reported as parked in a state where nothing had been parked** — an *escape that cannot fire*, which is harder to spot than a check that cannot fail, because its symptom is the guard continuing to work correctly. Fixed and verified by replicating the hook's own extraction and regex against the file rather than by re-triggering it. **F18 then opened, and its first item was not the Asset Hub connection but the precondition for it.** `Finalized<T>` carried a block hash and number and **no chain identity**: sound with one light client, unsound with two, since `blockNumber` collides trivially and `meet` / `readmitFromLeader` / `combineStatus` were each deciding *"are these two reads comparable?"* from the block alone. They happened to refuse cross-chain pairs — because two chains do not share a block hash — which is a collision argument, not a property a reader can see. `VerificationStatus`'s two verified arms now carry `chain` (the genesis hash); `FinalizedBlockRef` carries it so every read inherits it from its pin; `ChainHeadConnection.open` **requires** it with no default, since the only available default is the futarchy chain and that is exactly wrong for the AH connection. The compiler located all ten production sites. **`combineStatus` gained a cross-chain arm checked before the block arm** — the pair was already refused, but with *"Refresh to read them together"*, which no refresh can satisfy across two chains, so the user retries forever: wrong advice on a real refusal is worse than none. **Every new test holds the block hash equal across the two chains**, because a cross-chain pair with differing hashes passes with the chain check deleted and witnesses nothing; each check is mutation-proven (removing it kills exactly its own tests). `meet` and `readmitFromLeader` had **no tests at all** before this — a pre-existing gap that only became dangerous with a second chain. Gate: `tsc -b` clean and **920 tests, 0 fail** (was 910). Also fixed: `node_modules` had been relinked by a `pnpm install` run on `track-f/f1-gate-sweep`, which made `check:types` report 12 spurious `TS2307`s until reinstalled — a stale-link artefact, not a code defect.

**What comes next:** **F18's remaining item is the Asset Hub chain itself**: `startTopology` gains a third chain sharing the relay through `potentialRelayChains`, with its **own** lifecycle and a second `ChainHeadConnection` opened on the AH genesis. It is deliberately **not** a boot state — 02 §7.7 makes deposit and withdraw fail independently, so an AH probe failure must block deposit alone and never present as "funding is down". Merge train unchanged: **#239 → #240 → #243**, #242 independent.

## F18 — the Asset Hub connection · V-153 · V-154

**The second light-client connection is in, and it is not where my own resume note put it (V-153).** That note said *"`startTopology` gains a third chain"*; `startTopology` **is** the boot path, and 11 E17 says *"the AH chain is **not connected at boot**"* while 10 §9.3 budgets its chain spec as lazy — stated twice, in two documents, in the columns that own it. Built as written it would have cost every session a third chain's memory and sync whether or not it ever deposited, and put an Asset Hub sync failure **inside the boot machine**. Third instance in three days (V-147, V-151) and the sharpest: those two were stale notes a machine-checked artifact contradicted, this one was never right — written from the shape of the existing code rather than from the section that governs it. **Built instead as `Topology.attachAssetHub`**, a method so it closes over this topology's own client, relay `Chain` object and teardown list: *"same smoldot instance"* stops being a rule to remember, and `stop()` reaches a lazily attached chain with nobody registering it. The asymmetry is the design — a relay or parachain failure aborts and tears everything down, **every** Asset Hub failure is a returned value that leaves the other two running, because no failure of the deposit leg may become a failure of the app. `AssetHubLeg`'s refusing arms carry **no `chain` field**, so a leg we refused cannot be read from; `wrong-chain` **keeps the observed genesis** while removing the chain, since `classifyForeign` reads *"a different chain — retrying will not change this"* off that hash and would otherwise report a permanent condition as a transient one. Also refused: an Asset Hub bundle pinning **our own** genesis, which passes every other check and would put futarchy balances on screen under an Asset Hub label. **The connect step was lifted out of `light-client.ts`** — the one module no test executes — into `asset-hub.ts`, taking its transport by injection, because both of its rules fail silently: a transport that fails to open **detaches** its chain rather than leaving it syncing unread, and a second connect never hands the same chain to `getSmProvider` twice (PAPI refuses a repeat with a *console warning, not an error*, so the second is a transport connected to nothing reporting success). **Writing the tests found two real defects**: `detach()` was idempotent only by swallowed exception, and a **stale** leg's late `detach()` disowned the chain that had replaced it — leaving two chains syncing and neither reachable. 17 mutations across the two modules, each killing exactly its own tests and no others. V-154 fell out sideways: a comment in `derive.test.ts` claimed `assert.equal` cannot narrow a union; measured, `node:assert/strict`'s `equal` aliases `strictEqual` and **does**, while plain `node:assert`'s does not — corrected to the measurement rather than the conclusion. Gate: `tsc -b` clean, `depcruise` 0 errors, **941 tests across 16 suites, 0 fail** (was 920).

**What comes next:** **F18's remainder is the readers, not the connection.** S12/S13 stay `built-unwired`, and `routes.tsx` now names what they actually wait on — *"readers over live transports on both this chain and Asset Hub"* (deposit) and *"a live transport for its reader"* (withdraw), which is 02 §7.7's asymmetry: withdraw is a local `pallet_xcm` call and must stay up when Asset Hub is down. That is the same read layer S2 and S9–S11 wait on, so it is one piece of work serving six screens. Merge train unchanged: **#239 → #240 → #243**, #242 independent.

## F18 — the two-chain funding readers · V-155 · doc-11…

F18 — the two-chain funding readers · V-155 · doc-11 `[VERIFY]` cleared

**The readers are in, and the module exists because of one slip.** `readDepositInputs(reader, reader, …)` typechecks perfectly, and after it every Asset Hub figure on screen is a futarchy-chain read wearing an Asset Hub label — a **true statement about the wrong chain**, which no badge, no firewall rule and no later check can detect. So the reader pair is **branded** with a module-private `unique symbol` (the `Finalized<T>` construction, for the identical reason: a structural `{ local, assetHub }` is satisfiable by exactly the literal being avoided), `fundingReaders()` refuses two readers sharing a chain identity, and a negative-compilation fixture proves the hand-assembled literal does not compile. **`assertOnePin` deliberately has no analogue**: every other reader in this client stamps one pin onto every leaf, and a deposit model that did so would be one that had mixed the two chains — so the check is per **leg**. The caps are the one place a foreign value could still enter the local leg, since they arrive already stamped; their chain is now checked against the local reader, because a cap read elsewhere bounds the deposit against a limit that does not apply to it. **The legs fail differently by spec, not by oversight**: §11.9.1 makes the Asset Hub connection a precondition row and E17 blocks the flow with diagnostics, while §11.9.2 degrades withdraw's destination check to a warning — so `readWithdrawInputs` takes **no Asset Hub reader at all**, and no later edit can couple them without changing that signature and meeting §11.9.2 on the way past. Fail-closed throughout: an unreadable `PhaseFlags` means D-13's caps **apply** (V-115's unsafe direction is skipping them), an absent Asset Hub account is a **real zero** and not a decode failure, an undecodable balance reads zero *and* is reported, and `assetHubReady` requires the foreign compat verdict **and** a viable account. **V-155 is the finding worth carrying**: 11 mutations, and one survived — the reader fixture derived `blockHash` from `chain`, so every same-chain pair also shared a block and every cross-chain pair also differed, and a mutant comparing *blocks* instead of *chains* passed the entire suite. The inverse of the discipline applied one commit earlier, where the provenance suites hold the block hash **equal** across two chains so only the chain check can refuse. A fixture factory that derives one field from another removes the ability to vary them independently, and nothing afterwards reads as wrong. Also cleared 11 §11.9.1's `[VERIFY asset index 1337]`: 02 §8 discharged it twice (V-17 Polkadot 2026-07-16, V-105 Paseo 2026-08-04) while doc 11 kept restating the open question in three places — the fourth restatement-rot instance in three days, and no contract bump under 02 §13 rule 9's mechanical test. Gate: `tsc -b` clean, `depcruise` 0 errors, **954 tests, 0 fail** (was 941). **A firewall hole fell out of costing the next step (V-157)**: `only-chain-client-opens-a-chain-connection` matched `EXTERNAL('polkadot-api|smoldot')`, anchored at `^`, so it **could not match a specifier beginning with `@`** — and PAPI's providers are separately published scoped packages (`@polkadot-api/ws-provider`, `@polkadot-api/sm-provider`), all already in this repo's store. Measured on the witness: both scoped forms produced `witness-could-not-resolve` and nothing else, and that one rule fires *because the package is not installed there* — so declaring the dependency, which is what a package does to use one, silences it at the same moment the import starts working. Third instance of V-86/V-92's class in these matchers, so the repair is a **wildcard with a named exception** (`@polkadot-api/descriptors`, this workspace's own generated package, verified by a negative control) rather than a list that goes stale silently. It also closed a subtler drift: the witness imported `EXTERNAL` from production *"rather than restated"* but passed its own copy of the **argument**, so the pattern could not drift and the package list could.

**What comes next:** **F18's remainder is now the hasher vectors, then wiring.** The key builder's verifiable half shipped this session: `storagePrefix(pallet, item)` against all 65 recorded chainHead keys, read in place and checked in both directions, with **no args-taking function** and a test asserting that absence (V-156 — every recorded key is a 32-byte prefix, so the corpus exercises no hasher). What is still needed is known-answer hasher vectors from **outside TypeScript**: deriving them from `@polkadot-api/substrate-bindings` would test the library the builder is made of against itself. This repo has the pattern twice — `runtime/bleavit-runtime/fixtures/multisig-derivation.json` and `crates/market-core/fixtures/chain-quote-agreement.json`, each written by a Rust test and read in place. Add a third for `Blake2_128Concat` / `Twox64Concat` / `Identity` over the exact key types 02 §7 freezes, then the composition root supplying keys + decoders + the two readers is small. PAPI's `getTypedCodecs` gives `{args, value}` per storage entry, where `args` is the **SCALE args codec only** — no hasher, no prefix — so it cannot produce a key. And `app/fixtures/chainhead/`'s 64 storage transcripts, which look like a ready-made oracle (real keys plus declared hashers, eight combinations up to three keys deep), record **prefix-only 32-byte keys**: they confirm `twox128(pallet) ++ twox128(item)` and exercise **nothing** about hasher application, so a suite built on them would report five hasher kinds covered and test none. The hasher half needs known-answer vectors from outside TypeScript; this repo has that pattern twice — `multisig-derivation.json` and `chain-quote-agreement.json`, written by the Rust side and read in place. Start there, then the composition root supplying keys + decoders + the two readers is small. Merge train unchanged: **#239 → #240 → #243**, #242 independent.

## #239 merged → the stack rebased · F18's storage-key…

#239 merged → the stack rebased · F18's storage-key oracle (V-156 closed) · V-158

**The user merged #239 mid-session, which orphaned everything stacked behind it, so the drill ran first.** #240 was `CONFLICTING` and #242 broke to `CONFLICTING` when `main` moved; all three open branches are `MERGEABLE` again. The two stacked rebases are **verifiably content-neutral** — `git diff <old-tip> <new-tip>` is empty for both, so the rebase provably dropped only the squashed commit — which was checkable because `81ca8691` and the squash `6157f643` have identical trees, confirmed before touching anything. #242 needed real resolution; both its conflicts are additive on both sides and the trap is the `test` chain, where taking both whole `"test":` lines makes a **duplicate JSON key** whose last occurrence silently wins while `pnpm test` still exits 0. **Then F18's remaining blocker, which was V-156: the key builder's hasher half had no oracle.** It has one now, and it is Rust — `runtime/bleavit-runtime/fixtures/storage-keys.json`, written by the runtime's own `hashed_key_for` (never a re-implementation, since a fixture repeating a mistake would pin it into the client) and read in place, the third file here built that way. **The entries are chosen for what each alone can catch**, and one pair drove the API: `Welfare.Snapshots` is a single map over a tuple key and `ConditionalLedger.Positions` is a double map — doc 02 writes both `(A, B) → V`, they hash differently, and PAPI's `args` codec only ever produces the first. So `storageKey` takes **pre-encoded keys, one per position**, which puts that choice at the call site where the metadata answering it lives. Also pinned: the two ledger instances (same key, different prefix), both `PositionId` variants (encodings of different length), `Scheduler.Agenda` for the `Twox64Concat` a balance-only client never meets, and `ForeignAssets.Account`, whose key 1 is an XCM `Location` and **not** the `u32` Asset Hub uses for the same asset — asserted equal to the 02 §8 pinned `USDC_LOCATION_ENCODED`. A dedicated test asserts the **concat suffix**, because a digest without its input is a strict prefix of the right key and `descendantsValues` answers a prefix with the whole map: the near miss returns everybody's balance, not nobody's. Coverage of the fixture is asserted on **both** sides, since a fixture-driven test is only as strong as its fixture. **V-158 came out of the mutation sweep and briefly made it lie**: restoring files with `shutil.move` carried the backup's older mtime, `tsc -b` skipped the rebuild, and one mutation ran against the previous one's compiled output. The harness's own closing `restored:` check is what caught it. Re-run after repair: 8 mutations, all killed, none killing every test. Rust fmt/clippy clean, **456 runtime tests**; app **963 tests across 16 suites**, 0 fail, every witness fires.

**What comes next:** **Merge #240, then #243** (#242 independent, mergeable now). Then F18's last step is the composition root: both halves of the key builder exist and the keys/decoders are injected, so it supplies them plus the two readers to S12/S13. Then F17 once F11 closes.

## V-159 — the corpus reading behind V-156 was wrong, and…

V-159 — the corpus reading behind V-156 was wrong, and correcting it produced a third producer

**Found by asking a question the composition root forced**: `ForeignAssets.Account(location, who)` needs the USDC `Location`, and rule 7 forbids hardcoding it, so *where does the client legitimately get it* meant opening a transcript by hand. The transcript's **second** request is a single-entry read with a real `Blake2_128Concat` key. V-156 had said the corpus records prefix-only keys; 65 of 67 do, **two do not**, and both the original measurement and the suite missed them by taking `requests.find(…)` — the first match — per file. The suite then *asserted* the false statement and passed. **The conclusion survived the correction and got stronger.** Those two keys exercise one hasher, one key type, one arity, one pallet — so the Rust fixture is still the oracle for `Twox64Concat`, multi-key maps, tuple keys and instance disambiguation. What they add is independence: the client rebuilds both **byte-for-byte**, and the Location the live recorder hashed is the same pre-image the runtime publishes, the 02 §8 pinned `USDC_LOCATION_ENCODED`. A live node, a compiled runtime and the client, none of which saw the others. The suite now counts both classes and pins the 65/2 split by filename. **Also fixed: a NUL byte I wrote into the test file** (app-code rule 14's exact trap — a `?? ' '` fallback whose space came out as `0x00`), which made `grep` silently skip the file and hid a stale call site until it failed at runtime. Swept all of `app/`: zero control characters remain. The sentinel is gone too, replaced by an explicit assertion — a `??` default there turns a missing pre-image into *"the Location disagrees"*, the right verdict for the wrong reason. **Measured, not assumed:** `check:types` does cover `tests/chain-client` and is the **first** leg of `pnpm test`, so the full gate would have caught the stale call site as TS2304; I had run the single suite directly. No gate gap. **965 app tests across 16 suites, 0 fail.**

**What comes next:** **Merge #240** (all 16 jobs green). Then rebase #243 onto the new `main` and merge; #242 waits on TLC alone. F18's remaining step is bounded by an artifact, not by code: **no chain-spec bytes are committed for any chain and nothing calls `startLightClient`**, so the live-transport wiring of S12/S13 depends on the bundled relay/para/Asset Hub specs (10 §5.1, §9.4's second ≤ 3.5 MB row, which has no gate because the artifact does not exist). The pure half — `FundingKeys` + `FundingDecoders` — is unblocked and is the next code to write.

## #240 merged — the five-PR split stack is fully landed;…

#240 merged — the five-PR split stack is fully landed; #243 lost to a deleted base branch; #242 rebased again

**`main` is at `140517fe` and the stack is gone**: both remaining PRs target `main` directly. #240 was merged here under the standing authority, with every one of its 16 jobs verified `completed` and passing per-job rather than off the rollup, and **no audit-scope-A file touched** — it is CI wiring, `.claude`/`.codex` assets, the living documents and `tsconfig.tests.json`. **The one thing that went wrong is worth more than the merge**: `gh pr merge --delete-branch` deleted `split/5-docs-and-tooling`, which was #243's base, and GitHub **closed #243 immediately**. It then refused `reopenPullRequest` even after the branch was recreated at its old tip, and a closed PR's base cannot be retargeted — so the branch was re-proposed as **#244** and the two cross-linked. The drill now says: retarget the stacked PR to `main` *before* merging its base. **#242 needed its second rebase of the day, and measuring the collision changed how to resolve it.** Every conflict on that branch is *ancestor vs descendant*, not two alternatives: `main`'s AGENTS.md App-gates row is 31,831 characters against #242's 9,208, so taking either side whole loses something — the resolution is take `main` and splice in the 1,126 characters #242 actually contributes. `app/package.json` is resolved by **parsing**, asserting the chain starts with `check:types`, ends with the two new legs, and that every leg names a script that exists (37 legs, all resolving). Two mechanical lessons: `--force-with-lease` rejected a **hand-typed** SHA as *stale info* — always `git rev-parse` it — and an **unquoted** heredoc (`<<PY`) let bash expand backticks inside a Python script, the same rule-14 class as V-159's NUL byte.

**What comes next:** **Merge #244 and #242** once green (both `MERGEABLE`, CI running). Then F18: write `FundingKeys` + `FundingDecoders`, which are pure and unblocked now that both halves of the key builder exist. Its **live** wiring is artifact-blocked, not code-blocked — no chain-spec bytes are committed for any chain and nothing calls `startLightClient`.

## F1 (FE-P2, sync-progress half)

**The half of FE-P2 nobody had worked**: its row asks two questions and only the runtime-call one had been touched (V-82). The second — *sync-progress introspection* — is what decides whether 10 §3.1's `SyncDegraded` can exist at all, since its transitions are written in **peer counts**. V-89 had ruled smoldot's method surface unreadable because the dispatch lives in the wasm; the wasm ships in the package, so it was decoded (5,224,329 B) and read. **The read alone would have been wrong** — `system_localPeerId` sits in the same sorted dispatch run as `system_health` and returns `-32000 "Not implemented in smoldot yet"`. So it was executed instead, offline against pinned smoldot 3.3.2: `system_health` **is** served and returns `{isSyncing, peers: 0, shouldHavePeers}` — the zero-peer reading, the one a connected test could never produce. The first call costs **30.0 s** and head-of-line blocks the chain's whole JSON-RPC queue; a hypothesis that this was a *per-call* cost was **falsified by the second run** (a later call answered in 0.0 s), so the rule is *never first on the queue*, not *never poll*. Two things follow: `peers-lost`/`peer-acquired` have **zero producers**, so `SyncDegraded` is unreachable in the shipped client (the `FE-HANDOFF-010` shape again); and the only surface that can feed it is a **deprecated** JSON-RPC API behind PAPI's `_request` escape hatch, which both vendors mark unstable — a dependency that needs a ruling, not a quiet adoption. V-137 and SQ-597 (three priced options).

**What comes next:** Rule SQ-597, then build the peer watch: the ruling decides what it reads, and 10 §3.1's diagram is parsed by `boot.test.js`, so a spec edit there is caught rather than assumed. Merge #236→#240, then #241.

## F1 — the gate the FE-P2 measurement earned

V-137 established that the client's whole read path crosses a boundary **no gate in this repository watches**: PAPI chooses the JSON-RPC method names, smoldot answers them, both live in `node_modules`, and `tests/chain-client` proves nothing about it because it replays *recorded transcripts* — which by construction answer whatever was recorded. A smoldot release that dropped `chainHead_v1_storage`, or a PAPI release that began issuing a new method, would pass every gate here and fail in a browser. **`check:smoldot-surface`** now compares the two pinned packages directly, with **both sides derived**: the issued set reconstructed from `@polkadot-api/substrate-client`'s own `methods.js` — including its `_vN_` infix, so a version change is picked up instead of silently mismatched — and the served set from the base64+zlib wasm smoldot ships. 15 methods, all present on smoldot 3.3.2. A hand-copied list would agree with itself forever (SQ-490's lesson), so neither is written down. **It is a removal detector and says so in its own header**: presence proves the dispatcher knows a name, not that it serves one — `system_localPeerId` is in the same sorted run and answers `-32000 "Not implemented in smoldot yet"`. That case is covered at runtime instead, by reading a failed call as *unknown* rather than as a value. Anti-vacuity runs **before** any verdict: a control name that must be present, one that must be absent, and a wasm-magic check, because an empty decode plus a permissive matcher passes loudest when broken. `:witness` proves the detector fires. Written in TypeScript per the standing instruction; resolution goes through the pnpm store with the *exactly one copy* rule `check-smoldot-budget.mjs` already uses, since neither package is hoisted and `@polkadot-api/substrate-client`'s `exports` map does not publish `./package.json`. Wired into CI beside the budget gate; `tools/ci/tests` (175) green, `tsc -b` clean, depcruise unchanged.

**What comes next:** Rule SQ-597, then the peer watch. Merge #236→#240, then #241, #242.

## F1 — SQ-597 ruled, and `SyncDegraded` made reachable

The state 10 §3.1 added because *"the most common real-world failure (cannot reach peers on first load)"* was stateless had **no producer for either of its events**, so it was unreachable and no green run said so. Ruled SQ-597 (a)+(c) — depend on `system_health`, name it normatively, and specify the unreadable case — after writing out option (b) and finding it fires on every slow cold start, since before `initialized` there are no follow events by definition. 10 §3.1 gains a normative note; the diagram is untouched, so `boot.test.js`'s edge parse still holds. The driver is pure and timer-free like `boot.ts`, and its entire shape is one refusal: **absence is not zero**. Four reading arms, every malformed reply landing on `indeterminate`, non-degrading readings **freezing** the clock rather than clearing it, a change of cause restarting it, and the threshold required rather than defaulted because the two domains differ. 5 mutations, 0 survived. 70 chain-client tests green, all 13 app gates pass, derived design-kit copy regenerated and asserted verbatim.

**What comes next:** Merge #236→#240, then #241, #242. F1's remaining gates are now genuinely external: FE-P3 (Safari/Android device lab), FE-P4's perf half, FE-P6's device half, FE-P7 (ar.io credentials), FE-P9 (testnet), FE-P10's in-browser half, FE-P11's availability matrix.
