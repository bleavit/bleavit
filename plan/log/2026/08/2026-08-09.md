# Session log — 2026-08-09

## **F26 (Codex re-review of #285)** · V-371

**The FE-COMPAT-003 arm added in this PR was not reachable from the foreign chain, so the same defect stood one function away.** `classifyAssetHub` short-circuited on `deps.runtime === undefined` into `classifyForeign`, which maps a missing `specVersion` to `unsupported` — telling the user to *load a newer release* when nothing about the chain had been established. Doc 10 §5.2, written in this same PR, is decisive rather than inferred: a client that could not read `spec_version` *\"has established nothing about the chain in front of it\"* and enters **neither the three-mode union nor the foreign arms**. It is reachable, not theoretical — `ChainHeadConnection.finalizedRuntime()` returns `undefined` for an answering chain on an uninitialised follow, an `invalid` runtime, or after dropped announcements. One shared `identitySettles` now decides the three outcomes identity alone owns, and an unread runtime returns `unestablished`/`FE-COMPAT-003`. `foreign.ts` is unchanged: its `specVersion === undefined` branch stays as the fail-closed floor for a caller with no layer above it. Both directions are asserted in one test — an unread runtime must **not** carry the *newer release* recovery, and an **observed** out-of-set version still must. Three mutants, all killed.

**What comes next:** Land the remaining second-round findings, then merge in dependency order

## **F26 (Codex re-review round 3 of #285)** · 10 §3.1…

**F26 (Codex re-review round 3 of #285)** · 10 §3.1 amended · V-375

**The state SQ-1011 added rendered nothing, and the test that should have caught it structurally cannot.** `CompatUnavailable` was absent from `RENDERING_STATES`, so a user whose probe never completes saw a blank screen — the exact person the state was added for. `SyncDegraded` was absent for the same reason, though 10 §3.1 grants it `Degraded`'s peer-diagnostics panel and `Degraded` is in the set. **The masking is the finding.** `RENDERING_STATES` has one consumer: a breadth-first test asserting every non-terminal state can reach a rendering state. It consumes the set as a **goal set**, so it fails only where no goal is reachable — it cannot fail on a wrongly *excluded* state, and cannot fail on a wrongly *included* one either, since adding members only makes goals easier to reach. It would pass for **any superset of the correct set, including the set of all states**. Measured rather than argued: that test printed `ok` on both sides of the defect. The replacement drives the **reducer** instead — it proves the session cannot leave the state without `peer-acquired`, firing every other event the machine has (the list exhaustive by the compiler, since a forgotten event would quietly weaken the claim into one that passes), then asserts renderability where it proved the session rests. **`CompatCheck` stays out of the goal set deliberately:** including it would have weakened the very test that hid the defect. Continuity is carried by a session-keyed predicate instead, so a retried probe keeps its diagnostics and a first probe shows the boot skeleton. 10 §3.1 now states that the retry MUST NOT take the surface down, and that `FE-COMPAT-003` survives the retry edge and is retired only by a completed classification.

**What comes next:** Land F17's and F18's round-three findings, then merge in dependency order

## **F18 (Codex re-review of #289, and the audit it…

**F18 (Codex re-review of #289, and the audit it prompted)** · V-372

**Three more assertions in the drill harness checked a shape where the spec fixes a value, and one of them was the third instance in a single function.** `assertFundingReport` accepted any nonempty `foreignMode`, admitting all six `ForeignMode` values where 15 §4.8 says only `wrong-chain` is reachable in any Zombienet topology — and the regression is real, not hypothetical: a label naming no pin returns `unreachable`, which is exactly the development-label bug an earlier commit fixed, and the old rule would have accepted its return. An audit of the whole harness then found `requireReads` asserting that each key starts with `0x` and never **which** surfaces were read, so a producer dropping both Asset Hub reads and keeping `PhaseFlags` passed. **Ruled superset, not exact set:** a dropped read falsifies the drill's own claim and is the only unsafe direction, while an added read falsifies nothing and 02's frozen set grows by design — an exact-set rule would go red on correct changes and get loosened by whoever was unblocking themselves. `finalizedHash` gained its two value checks (66 hex characters, and never equal to genesis). The boot leg's Asset Hub verdict became its own `assetHubVerdict` field rather than a concatenated sentence, asserted **conditionally** — a flat rule would fail the drill on relay sync timing, and a measured run has that leg reporting `unavailable` at one minute of network age while funding read the same chain at block 49. Eight red failures first, each for its own reason, and the compiler found the one other construction site. `assertWrongChainReport` was left alone deliberately. **One hazard named rather than papered over:** `classifyForeign` answers `unreachable` for two different facts — a label matching no pin, and an unobserved genesis — and the second is reachable live, because the compat provider attaches a second time beside the reader's handle. The refusal stands, and both messages now name both causes so a red drill does not send the next reader after the wrong one.

**What comes next:** Land F13's and F17's rounds, then merge in dependency order

## **F18 (Codex re-review round 3 of #289)** · 15 §4.8…

**F18 (Codex re-review round 3 of #289)** · 15 §4.8 amended · V-377

**The release drill could certify none of its four claims and still report a pass — and the reason was a ruling I made one round earlier.** `openDepositLeg` returns `asset-hub-unavailable` and `asset-hub-bundle-pin-mismatch` **before** it opens the Asset Hub reader, pairs the two readers, or classifies. Proven by execution, not by reading: the producer was driven with a classifier fake that throws if reached, both runs completed, and the old rule accepted both. The round-one fix had treated those refusals as *the documented refusal the topology forces* — but 15 §4.8 rules only that `ForeignMode` can never reach `full` locally, and the \"absent or unpinned\" language is 02 §7.7's rule about **client behaviour**, not about what satisfies a test row. The harness docstring asserted a spec ruling the spec does not make. **Split per the ruling:** `fundingCertification` reports the three funding-leg claims of §4.8, the tier is required and defaults to `release`, an environmental refusal now fails at the release tier, and a lower-tier run prints `NOT CERTIFYING` in its own output rather than only in an exit code. `run-evidence.py` pins the tier so an ambient variable cannot downgrade an evidence run. The two `wrong-chain` facts are separated: the bundle identity check compares against the development pin built minutes earlier, the foreign verdict against the real Paseo pin — the client cause is renamed `asset-hub-bundle-pin-mismatch` while the verdict keeps `wrong-chain`, which 10 §5.2 owns. **Residual, stated rather than hidden:** the drill script performs I/O and cannot be executed per commit, so its wiring is bound by a deliberately lexical test — it would not catch an edit passing a different correct-looking variable.

**What comes next:** Merge in dependency order once the reviews settle

## **F17 (v30 wiring + Codex re-review of #287)** ·…

**F17 (v30 wiring + Codex re-review of #287)** · SQ-1030 resolved · V-374

**The `AllowanceBook` had no producer, and building one turned up why the chain's four counters behave differently.** v30 published the limits, so `allowanceBook(reads)` now pairs them with `Guardian.Allowances` — applying the pallet's own window test, same `saturating_sub` and same `>=`, before subtracting. Otherwise a stored `pause_used_in_window = 1` against a limit of 1 reads as an **exhausted** meter for an **available** emergency power. The asymmetry has a cause worth keeping: `sync_epoch()` runs in `on_initialize` and zeroes the two per-epoch counters, so those are current at any finalized block, while **nothing** resets the pause pair outside `check_and_consume` — pinned by a source assertion so a change either way fails and says which direction the client must move. O-3's two `blocking` obligations became ordinary clauses on both `O-3` and `O-4`, and **S15's approve control opens again**. Codex's re-review caught the half-fix: the gate filtered which obligations *blocked* and still returned the unfiltered list for *display*, so a `pause_intake` approval opened and still warned about playbook conditions — one predicate now serves both. The propose side was closed in the same round on its own precedent, since `proposalBlocks` already blocks `PB-MIGRATION` because the fifth approval reverts. **The scoping constraint was made unrepresentable rather than checked:** the two playbook reads live inside the `activate_playbook` arm of the union the client builds, so the four other arms have no field to carry one, proven by a firewall fixture whose declared error was verified by flipping the expectation. 21 mutants across the two rounds, 0 survivors — two first-pass survivors were both real test weaknesses and are recorded, including one where the fix and the defect are behaviourally identical because every obligation is now unscoped, killed by asserting on the source with the reason written in.

**What comes next:** Merge in dependency order once the re-reviews land

## **F17 (Codex re-review round 3 of #287)** · V-376

**Two controls offered a transaction the chain was always going to reject, and the diagnosis of the first one was wrong in a way worth recording.** Codex read the hold-horizon off-by-one as a ceiling problem, because `check_and_consume` spells **both** bound failures `DurationTooLong`. It is the **lower** bound: `until >= now`, where `now` is the height executing the dispatching fifth approval, and B′ is finalized by 11 §11.4's own definition — so a hold ending at B′ is already spent when the chain reads it. The ceiling needed no matching change and deliberately got none: the client's `B′ + max` is the stricter of the two, and widening it would require a height the client does not have. **A margin was considered and refused under R-2** — anything beyond *\"B′ is spent\"* would be a claim about inclusion latency or council signing speed, and neither has an anchor; a mutant adding `+ 1` is killed, so the refusal is asserted rather than documented. **A third instance the review did not name** was fixed with it: `approvalBlocks` blocked only on `now > expiresAt` while the core requires `now <= expires_at`, so an action whose last admissible block is B′ could still be approved. Two neighbouring sites were checked and left alone with reasons — they are deadline comparisons where only `deadline − 1` is at risk, which is not a guaranteed revert and would need the margin R-2 refuses. **The bonded-filing arm was closed by type, not by check:** `EpochClosure.open` carried no datum and no pin while `closed` and `unread` both carried evidence and both refused — the only permitting arm was the only unproven one, on a **bonded** action. It now carries the finalized read plus a module-private `unique symbol`, and `check:casts` discovers the brand as 18 of 18, so `as EpochClosure` is refused outside its module. 13 mutants, 0 survivors, after two first-pass survivors that were both real: the hold **ceiling** boundary had never been asserted, and the brand was **not load-bearing** — the required `read` field alone already refused the empty literal, so the fixture proved less than it appeared to. Both closed.

**What comes next:** Land F18's round three, then merge in dependency order

## **F17 (Codex re-review round 4 of #287, and the sweep…

**F17 (Codex re-review round 4 of #287, and the sweep it prompted)** · SQ-1037 filed · V-379

**Yesterday's repair made these arms prove a read *happened*; none of them said **which** read.** A finalized absence read for a sibling `spec_version`, another epoch, or the **other registry instance** produced the same branded `open` and permitted a bonded filing the runtime answers `AlreadyFinal`; a `PlaybookRegistered` read for one playbook admitted an activation of another. Fixed by shape where a shape can hold it — the instance is a type parameter, and `FilingInputsBase.specVersion` was **deleted** so there is no second value to disagree with, because a comparison is forgettable and nothing-to-compare is stronger. Propose derives, approve compares: on propose the client builds the action, on approve the id is decoded from foreign bytes, so two values genuinely exist and the comparison lives inside the one helper both flows call. **The sweep found four more and closed them all:** `RerunState` carried no pid (a `delay_once` of p7 admitted by a read of p9), `FrozenSpecVersions` no epoch, `FilingOccupancy` neither — all bonded or signature-costing — and `AllowanceMeter` is now branded at **both** levels, with a fixture per level proving neither brand alone suffices. **A fixture defect made a whole class of tests vacuous:** `finalized(v, n)` minted every read at `0xdead` whatever `n` was, while `combineStatus` keys on the hash — so every test that *varied the block* varied nothing, and the one place that genuinely needed it had quietly hand-written its own status object. 25 mutants, 3 genuine survivors, **all the same shape: the control correct and the sentence beside it naming the wrong subject** — a guardian proposing a pause told no `delay_once` allowance remained, a stale occupancy reported as a full registry, an expiry refusal dropping the one actionable half of its reason. The corpus also caught three fixtures failing for the wrong reason, including a real vacuity: the new type error shares `TS2322` with three incumbent fixtures, which would have passed while never reaching their own defect.

**What comes next:** Merge in dependency order once the reviews settle

## **F17 (Codex re-review round 5 of #287)** · 10 §2.1…

**F17 (Codex re-review round 5 of #287)** · 10 §2.1 corrected · SQ-1038 filed · V-380

**The brand technique did not do what the specification said it did, and the false sentence lived in the file that defines the control.** Object spread copies symbol-keyed properties, so `{ ...genuine, field: substituted }` keeps the brand and typechecks with no assertion — which is why `check:casts` could never see it. Proven twice over: `tsc` exit 0 on the spreads, then a runtime probe showing a re-keyed closure **permitting** a bonded filing against `(41, 9)` on a read that proved absence for `(40, 2)`. **The repair cost was the surprise.** A phantom non-exported `declare class` with one `#private` member, intersected with the existing shape: TypeScript drops private members from a spread type and keeps everything else, so five brands were hardened with **zero** consumer changes, zero fixture changes and zero runtime representation. `#private` over TypeScript `private` deliberately — the latter is an ordinary own enumerable property at runtime, so the type story and the runtime story would disagree, which is the defect shape this whole exercise is about. **The sweep found two more of the same severity than the review named:** `GatePassed` (`{...passed, at: elsewhere}` re-pins the proof, and `at` is compared nowhere while three consumers read it permittingly) and `FunderReads` (`{...genuine, freeBalance: someone else's}` keeps `account`, so `WrongSubjectError` never fires on a bonded control). **The mutation then found the symbols do no type work at all** — all five could be deleted with the suite green — so `check:casts` discovery was extended to the marker device; the **first version of that extension silently dropped `GatePassed`**, 21 brands to 20, which is the exact failure the gate exists to prevent reintroduced by the change closing it, caught by measuring rather than assuming. A 21st brand, `VerifiedArtifact`, had never been in the governed set at all. **`Finalized<T>` is exempted with reasons**: `derive` grants the same power by design, so the spread is not an escalation, and hardening it would break 10 §2.2. Eight brands remain open and ranked as SQ-1038.

**What comes next:** Merge in dependency order once the reviews settle

## **F13 (Codex review of #286)** · SQ-1035 filed · 12…

**F13 (Codex review of #286)** · SQ-1035 filed · 12 §1.4 amended · V-370

**Four checks in the release verifier could not fail, and the fix for the first one had to be refused.** `compare` counted `release_signatures` and `attestations` out of the served document; `buildReleaseJson` emits neither, so every genuine release verified with zero of each. Codex proposed adding the fields to the producer — **wrong, and it is the circularity 12 §2.1 creates**: release keys sign `release.json`'s hash and both verifiers check the served bytes, so writing a signature's own transaction id back into the document invalidates the signature over it, and the field would ship empty forever. Gate 4 now publishes both id sets and the CLI takes `--signature`/`--attestation`, with *named none* reported distinctly from *published none*. The other three: `ReleaseChannel` was read as **one `u64` at 152** where 02 §12 freezes `keyring_generation: u32` at 152 and `revoked_key_bits: u64` at 156 — generation 4 read as revocation bit 2, and the mask's top four bytes truncated, so the verifier could revoke an innocent key or miss a revoked one; `--arweave` was never compared against the document's own `arweaveManifestTxId`, so an impostor manifest serving the signed bytes exited **0** printing `MATCH`; and the served tree was enumerated from the signed map alone, making `runSelfCheck`'s `unexpected` branch unreachable, so a gateway serving an extra payload exited **0** printing `MATCH`. Each defect was re-broken in isolation with the others fixed, so none hid behind another. **Also fixed here:** 12 §1.3's own published command passes `--release-json ar://<txid>` and the value went into the gateway URL verbatim as `ar%3A//`, so the spec's literal copy-pasted line could not resolve — and the failure read as a missing transaction rather than a malformed request.

**What comes next:** Request the four Codex re-reviews, then merge in dependency order from #288

## **F13 (Codex re-review of #286)** · SQ-1036 filed · 12…

**F13 (Codex re-review of #286)** · SQ-1036 filed · 12 §1.3/§2.1 amended · V-373

**The verifier checked one of the two Arweave addresses 12 §1.2 names, and the published command still could not run.** `twoPassDeploy` returns genuinely distinct ids — `assetManifestTxId` is the pass-1 tree `release.json` pins, `manifestTxId` is the pass-2 manifest containing the `release.json` sibling, and it is the one ArNS repoints to and a browser loads. The earlier fix bound the **right** id for the check it performs (the pin comparison is only meaningful against `M`) and was **half a fix**: the second address was never requested at all, so a poisoned final manifest exited 0 printing `MATCH`. `--final-manifest` is now required, refused when equal to `--arweave`, fetched through every gateway and byte-compared, with its `release.json` entry bound to the transaction the signatures were verified over. `finalTrees` and `manifestFindings` are **required** fields of `CompareInputs`, because an omitted second address is indistinguishable from a checked one — which is how the first repair left it. **One of my own premises was refuted with spec evidence:** I told the agent the keyring could not have a default, and §2.1 says *\"Keyring published in-repo, in-app, and on Arweave\"*, so it now defaults to an empty phase-gated in-repo file on the `signers.json` precedent. The gateway default was **declined** on two citations rather than preference — gate 4 publishes the URL set per release, and §5.1 leaves naming operators to the operator (SQ-994) — so writing hostnames here would pick operators no spec anchors. Deliberate non-check: the two manifests' per-path ids are not compared, since whether pass 2 mints new data items for identical bytes sits inside FE-P7's open `[VERIFY]`; the fixture gives them different ids so a future checker cannot pass by comparing them.

**What comes next:** Land F17's O-4 close, then merge in dependency order

## **F13 (Codex re-review round 4 of #286)** · 12…

**F13 (Codex re-review round 4 of #286)** · 12 §1.3/§1.5 amended · V-378

**The gate that lets a release skip the 72-hour soak had no runnable implementation, and the multi-gateway floor could be met by one operator answering twice.** `diff-scope --against <incumbent-txid>` was parsed positionally and died on `ENOENT: … open '--against'` — the expedited lane's only mechanical check, with no CI job running it and coverage only of the `diffScope` *function*. It now fetches the incumbent through **every** gateway and refuses on disagreement, since a lane with no soak may not rest on one gateway's account. Separately the gateway parser accepted the same endpoint under two names, so §1.3's ≥2 floor was satisfied by **23 responses from one loopback server** and `crossGatewayFindings` compared an operator with itself. Rows must now be distinct operators at distinct endpoints — both halves, since either alone leaves the hole — with hosts compared across `rawUrl` and `txUrl` so a crosswise share is caught, ports and paths ignored, and a placeholder subdomain label dropped. **What it cannot catch is stated in the code:** one organization holding two domains, a CDN fronting several names, two names resolving to one address. None is decidable from configuration text, and §5.1 leaves *which* operators are independent to whoever names them. **The audit I asked for found more than the finding:** of seven published command lines, one was broken (fixed), one named a repository that does not exist (fixed — `futarchy-app` appears exactly once in the whole specification, the remote is `bleavit/bleavit`, and 10 §10.1 roots the client at `app/`), and three are O1's parked shape gaps — the `./tools/verify-release` launcher is a directory, and neither `build.sh` nor `repoint.sh` exists.

**What comes next:** Land F17's round four, then merge in dependency order

## **Track F integration (#290)** · F26 merged · F18, F17,…

**Track F integration (#290)** · F26 merged · F18, F17, F13 integrated · 12 §1.5 amended · SQ-1039 filed · V-381

**Four pull requests became one branch, and the union held a defect neither branch could see.** #285 (F26) merged to `main` on 23/23 checks verified per check. The other three each edit `PLAN.md`, so merging them in sequence would have cost three conflict resolutions and three CI cycles; one integration branch costs one of each and validates the union. **That was not a convenience — the union failed to typecheck.** F26's SQ-1011/SQ-1012 ruling made `code: 'FE-COMPAT-003'` a required literal on `ForeignVerdict`'s `unestablished` arm, stating in its own comment that a site omitting it must not compile. F18's drill test constructs that arm and was written before the field existed. Neither branch's CI could fail on it, because the field and the site never met until here. **Two more resolutions would have reverted work silently**: `client-boot.js` moved its inline assertions into `assertBootReport`, which covers all four and strengthens three but named only `unestablished` where the base also refused `not-attempted`; and the milestone table held the F17 and F18 rows on **both** sides, each carrying the other's older text, so either whole-side choice would have dropped a milestone's narrative with nothing reported. `rerere` was disabled for every merge for that reason. The batch index needed the same care twice: F17's and F13's branches both predate F26, so both still named rows it closed — pruned by reading each row's own status rather than by hand. **The one P1 fixed** is F13's: `EXPEDITED_SCOPE` allowlisted `assets/descriptors/`, a path in the tool's fixtures and in no build, so 12 §1.5's expedited lane was authorized on an imaginary output. Ruled under R-1 and refused instead, with the structural reason; SQ-1039 owns what restores it. **The other open P1 needed no fix** — #289's was already closed by two commits landing after the reviewed base, confirmed in the code. The six P2s are recorded and deliberately unfixed, by user instruction.

**What comes next:** Merge #290 once its checks are green, then close #286, #287 and #289 as superseded

## **PR #291 — the Supply-chain CI failure and the five…

**PR #291 — the Supply-chain CI failure and the five Codex review findings**

**The red job and the reviewer's P1 were one defect, and it is the one this repository has already been bitten by twice.** `explainer/package-lock.json` is a committed lockfile that `tools/ci/audited-workspaces.toml` did not classify, so `check-audited-workspaces.py` failed the whole gate — which is the check working: the manifest is exhaustive by design precisely because `app/Cargo.lock`, `fuzz/Cargo.lock` and `app/pnpm-lock.yaml` each shipped unaudited behind a green job (SQ-985). Classifying it **found two real advisories** the tree had been carrying: `brace-expansion` 5.0.8 (GHSA-rgw5-rvv9-x895) under `eslint -> minimatch`, and `nanoid` 3.3.16 (GHSA-2v37-7h3g-55p8) under `vite -> postcss`. Both were **updated, not waived** — 5.0.9 and 3.3.18, in range, no `overrides` needed — which is what the npm leg demands of a lockfile a browser executes. Verified with the pinned `osv-scanner v2.4.0` (digest checked against `tools/env/pins.env`) over **both** npm lockfiles: 1 finding, the pre-existing `@opentelemetry/core` waiver, still matching. Two tests in `tools/ci/tests` restated the manifest's row counts as literals (`5`, and `1` npm); they now derive both from the manifest, and a third was added asserting the per-ecosystem work lists **partition** it — a row whose ecosystem no filter selects would pass the coverage check and still reach no leg. **The P2 the spec owed:** doc 13 §5's proof shares were divided by the block-*length* ceiling; corrected in the owning doc, in `pov_budgets.rs`'s doc comment and in the explainer, with the reasoning in the Decision log above. Chasing *every* reference, as the review asked, found a **fourth** copy that is not prose: `reference-model/.../frontend_budget.py` had `NORMAL_PROOF_BUDGET = 3_932_160` taken from that same sentence, and it **divides a proof size by it** — so the model published 36 trades per block where the runtime pins 70. It now derives both budgets from `MAX_POV_SIZE` and names the length ceiling separately so the two cannot be confused again, and a new non-parametric test pins the derivation. Every other test in that module was parametric in the budget, which is exactly why none of the 703 noticed. **The other three P2s:** `npm run verify` never checked the three fixtures it copies in from `reference-model/`, `tools/limit-coverage/` and `crates/market-core/`, so a moved source would have left every suite replaying a stale copy and passing — `build-fixtures.mjs --check` re-derives in memory and compares byte-for-byte, and it leads `verify` (negative-tested by perturbing a copy). `SourcePanel`'s `aria-modal` was a promise with no mechanism: the page behind it kept its tab order, so a keyboard user tabbed into controls the overlay covered, and closing dropped focus at the top of the document — siblings on the path to `<body>` are now `inert` and the opener gets focus back, with seven tests including one that fails the naive `document.body.children` sweep. And `explainer/` was absent from both living maps, so `README.md`, `AGENTS.md` (layout + gates) and a new `.claude/rules/explainer.md` now carry it.

**What comes next:** **The first push went red, and the reason is the same defect one layer down.** `tools/release/tests`'s stub osv-scanner routed its answer on the literal `endswith('pnpm-lock.yaml')`, so the newly classified `explainer/package-lock.json` was answered as a **crates.io** package and `check-npm-advisories.py` refused it — a fixture restating what `audited-workspaces.toml` owns, failing a gate that was working correctly. The stub now takes the npm filenames from the checker's own `LOCKFILE_NAMES`, which its docstring already names as the single place a manager is admitted. **My own error, and a known one:** I ran `tools/ci/tests` and not the suite list `ci.yml` actually loops over, which includes `tools/release/tests`. Re-run derived from the workflow rather than from memory: reference-model 703, simulation 110, deploy 142, tools/reference-model 12, release 101, phase-gates 74, monitoring 111, ci 378, env 85, limit-coverage 23 — all OK. **Gates run, in full: `npm run verify` in `explainer/` — 691 tests / 31 files, lint, `tsc`, fixture check, math check, production build green; shell 248.50 kB gz and the renderer still a strictly lazy 248.83 kB. `tools/ci/tests` 378 tests OK. `check-audited-workspaces.py`, `check-npm-advisories.py` (both lockfiles, pinned scanner), `check-doc-links.py`, `check-plan-tables.py` green. `cargo fmt -p bleavit-runtime --check` green.** **Not run, and why:** the exhaustive Rust gate — the only runtime change is a `///` doc comment, and CI runs the workspace job on the push. **Still not run, unchanged from the previous row:** the no-text-overlap sweep over the six new scenes, which is a manual browser harness. Follow-ups that remain open: the 06 §6.1 client-ingress reading `the-border` flagged, and the three doc-02 §3 bounds still defined scene-locally. **No milestone advanced; `app/` (Track F) untouched.**

## **F28 (the visual layer)** · SQ-1040 filed · V-382

**The app had no CSS at all, and 2,396 passing tests could not see it.** A user running the application noticed it rendered unstyled; the repository confirmed it — zero `.css` files, zero CSS bytes in `dist/`, no Tailwind, no PostCSS, no CSS-in-JS, and 119 `className` occurrences naming 92 classes that nothing defined. The spec is deliberately silent on visual design and `docs/design/claude-design-kit/PROMPT.md` says so outright (*"the visual identity does not exist yet"*), so this establishes the convention rather than following one. Tailwind v4.3.3 on the pinned Vite 8.1.4, markup untouched, stylesheet linked from `index.html`. **Three defects found in my own work before it shipped**, each by measuring rather than assuming: the CSS entry missing from `RECIPE_INPUTS` (two builds, different Tailwind config, identical published `buildRecipeDigest`); an `@source` scan wide enough to reach `dist` and the pnpm store, which would make the sheet a function of install shape and break the two-environment tree hash; and a dev server that could not render at all, because a `<meta>` CSP binds the source document and both `style-src 'self'` and the unsubstituted `connect-src` block it. **Two of my own checkers lied in the familiar direction.** The first coverage scan reported 100 % and *zero* dynamic families on a tree with five — its pattern could not parse `className={`…${…}`}`, so the reassuring number was computed over the subset it could read. The second read `.5rem` and `.95` as class selectors and reported 84 dead rules. Both are fixed and the gate is mutation-tested three ways with exit codes verified. **And a CSS comment broke the build:** `packages/*/dist` inside a comment closes it at the `*/`, which the compiler caught and the style gate did not, because its comment-stripping regex stops at the same early terminator.

**What comes next:** Merge #290, then push F28 for review

## **F28 (R-6 review remediation)** · SQ-1041 filed · V-383

**The review falsified two claims the milestone row made, and both were the kind that reads as done.** `@source` **adds** to Tailwind's source detection rather than replacing it, so the scan I had described as narrowed was still reading every non-ignored file under `app/` — `.ordinal`, `.row-4`, `.start-3` and `.end-2` reached the published stylesheet as utility candidates read out of *test prose*, which made the emitted CSS a function of whatever files a build environment happened to hold. `source(none)` closed it and the sheet lost 4,356 raw bytes. And five colour pairs sat under 3:1 in dark mode, three of them provenance badges: the `-600` state steps are tuned against white and were reused verbatim on a dark ground, so the labelling 10 §2.1 calls non-suppressible was met nominally — measured 2.21–2.60, now **4.48–9.31** through theme-flipping state-text tokens. Three more majors were self-contradiction of the same shape: `.button:disabled` used `opacity` and produced the 2.41:1 fade its own comment disclaimed, `.shell` named two grid rows for a four-child shell so the header absorbed the slack whenever the §11.10 banner rendered, and the style gate's witness asserted only that its collectors returned something — the weaker half of the control the gate exists to argue for. All five majors and five minors fixed; the witness now runs the same analysis over a fixture carrying one of each defect, and was itself mutation-tested.

**What comes next:** Merge #290, rebase F28 onto main, open its PR

## **F18/F26 (the D-13 caps reader, and the recorder…

**F18/F26 (the D-13 caps reader, and the recorder run)** · SQ-1034 resolved · V-384

**The blocker was a missing manifest entry, and it had hidden behind a green gate for weeks.** `InflowCaps.CumulativeDeposits` is frozen in 02 §7.4 and was in neither `surface-manifest.json` nor `CRITICAL_SURFACE`, so 10 §5.2's compat lattice could not fail on the Phase-3 deposit-cap read and §11.9.1's D-13 row was structurally unpassable. The entry's `layout` is **derived** from the committed runtime metadata — the same `surface_layout()` derivation `check-chain-feed.py` applies to the other 301 — and its chainHead transcript is **recorded** against a booted node. **My first attempt was wrong in a way the existing gates could not see**: I wrote `query: "map"`, the only such value among 302 entries, and the recorder maps anything but `prefix` to a `value`-typed read — which on a map prefix returns nothing and records a fixture certifying nothing. `check-chain-feed.py` verifies layouts against metadata, not query types, so it read green. I also claimed no recorder run was needed; the layout is derivable offline, the **transcript is not**, and three assertions were red until the run happened. Re-recording moved all 302 fixtures, and the diff had to be understood rather than accepted: normalising the pinned block left 222 byte-identical and 80 differing only in an embedded block header (aura slot, seal), so the corpus is semantically unchanged. `storage-keys.test.ts` then refused the new fixture until it was independently key-verified — the right refusal, and the prefix count is re-pinned 80 → 81 with the caveat its three earlier bumps carry: the hasher is still never exercised, so the corpus grew and the suite's reach did not. **Verification:** 43/43 App CI commands, `test:chain-client` 277/277, `test:descriptors` 44/44 and `test:mock-runtime` 13/13 — the last two red before the run. 16 mutants, 16 killed, one of which found a real defect: a headroom guard that could not fail, because `bigint` is signed.

**What comes next:** Drive F26/F18 to ✅ on merged evidence; park what waits on the physical world

## **F18, F26, F28, F17 ✅** · SQ-1042…SQ-1047 filed · V-385

**The R-6 review returned NO BLOCKER for F26, F18 and F17, and the union of the three open PRs was not green.** #292/#293/#294 became **#295**: one branch, one CI cycle, and one fix neither could carry — F28's style gate is bidirectional, so markup F26 wrote after F28's stylesheet fails only in the union, and the rule cannot ride on F28 because the dead-rule half would then fail it. The review's sharpest major is the class this track keeps finding: `readDepositCaps` fetched the FE-P2 witness and never compared it. Fixed, mutation-proven, and the anti-vacuity test caught the same shape **inside the fix**. F17's row claimed *nine* open spec questions — fourteen of its seventeen ids are resolved, the three that remain each cost one transaction fee and no bond (all refuse through `ensure!`, so FRAME reverts the reserve), and its cross-milestone transport blocker is closed here. **Then `main` moved under the pull request, and the waiter watching it could not see that.** #291 landed, #295 went `CONFLICTING`, and GitHub runs no workflows on a conflicted pull request — so all 15 checks the waiter read as *pending* were stale placeholders that would never resolve. It reported `bad=0` seven times and had no way to report anything else. Resolved by keeping **all six** Session-log rows rather than a side, since a whole-side choice silently deletes a milestone's narrative — the failure already recorded against #290. The union was re-validated rather than assumed, because main added `explainer/` at the repository root and F28's Tailwind scan is exactly the kind of thing that reaches past its own directory: 54/54, and the style gate passed, so `source(none)` holds. **Both review majors then landed on #295 rather than in a second pull request**, so one CI cycle carries them and F17 flips on the same evidence. The challenge row names its filing on both reads — a bond read from a cheaper filing in the same epoch had opened a challenge the chain refuses **after** the signature — and `read-only-incompatible` now reads `Constitution.ReleaseChannel`, which nothing in `app/` had read at all, and renders the pointer its own sentence promises. Nine mutants, nine killed. The agent also hit a circular import `depcruise` catches and CI would have failed on.

**What comes next:** Merge #295, then close #292, #293 and #294 as superseded — all three are contained in it by ancestry, so none is merged. Then delete the four merged head branches and the scratch worktrees. F1, F11, F13 and F14 are parked on a ruling, credentials, a ceremony and a device lab, and nothing in this repository unblocks them.

## PARKED: 2026-08-09 — Track F's code is done. What…



> **PARKED: 2026-08-09 — Track F's code is done. What remains needs a device lab, a ceremony,
> credentials, or a ruling, and none of it is unblocked by more work in this repository.**
>
> **#295** (`integration/track-f-v3`) supersedes #292, #293 and #294. It carries F28's visual
> layer, F26's compat verdict reaching the shell, and F18's D-13 caps reader — plus two fixes
> neither branch could carry alone.
>
> **The union defect, again, and it is the third time this pattern has paid.** F28 added a
> **bidirectional** style-coverage gate, and F26 added markup after that stylesheet was written.
> Each branch is green alone. In one tree the gate reports `compat__disabled` and
> `datum--unavailable` as unstyled. The fix cannot ride on F28 either, because the dead-rule half
> would then fail it. `datum--unavailable` is **not** a misspelling of the existing
> `datum__unavailable`: that one is the child span inside `ui`'s `Datum`, this one is a whole
> datum standing in for one.
>
> **The R-6 review returned NO BLOCKER for F26, F18 and F17**, with three majors. The sharpest is
> the one this repository keeps finding: `readDepositCaps` called `crossCheckedCall` and **never
> compared the witness**, so 10 §11's FE-P2 pairing was fetched and discarded while the module's
> own note said it was not optional. Fixed, with the comparison mutation-proven — and the
> anti-vacuity test caught a second instance **in the fix itself**, where an *undecodable* witness
> skipped the comparison and let the caps through. An unreadable second view is not agreement.
>
> **F17 is ✅ here.** The review's two remaining majors landed on #295 rather than in a second pull
> request, so one CI cycle carries both and F17 flips on the same evidence. The challenge row now
> names its filing on both of its reads, and the `read-only-incompatible` arm reads
> `Constitution.ReleaseChannel` and renders the newer-release pointer its own sentence promises —
> nothing in `app/` had read that item at all. Nine mutants across the two, all killed.
>
> **One finding about this session's own tooling, because it is the class this track keeps hitting.**
> `main` advanced under #295 while a bounded waiter polled it, and the pull request went `DIRTY`.
> GitHub runs no workflows on a conflicted pull request, so every check the waiter read as
> *pending* was a stale placeholder that would never resolve. It reported `bad=0` seven times and
> could not have reported anything else. The replacement reads `mergeStateStatus`, refuses to call
> an empty rollup green, and stops on head drift. A waiter that only counts checks is a check that
> cannot fail.
>
> **Four rows stay 🔨, and each names the external thing it waits on:**
>
> - **F1** — **SQ-940**, which only the user can rule. `ar-io/ar-io-ant-process` was archived
>   2026-06-09 and the AR.IO contracts moved from AO to Solana. An ANT authorizes by membership
>   and returns on the first matching controller, so a controller list is **1-of-m**: adding
>   controllers widens custody rather than splitting it. The remaining prototype gates need a
>   device lab, a live chain, hardware, or ar.io credentials.
> - **F11** — the ten readiness blockers `release.json` names, each of which `--production`
>   already refuses on: no seated bootnode operator, no gateway set chosen, no production genesis
>   or chain-spec hash, no paraId, no keyring, and a version still `0.0.0`. The live Arweave
>   adapter's Turbo-SDK half is prototype gate **FE-P7**, whose `[VERIFY]` R-2 forbids resolving
>   by assumption.
> - **F13** — the key ceremony itself, the real signer names and keys, and `liveGateway`. The
>   tooling is finished and mutation-proven (38 mutants, 0 survivors).
> - **F14** — the physical device lab 10 §9.4 names, and a Playwright probe that is not
>   installed. *Memory desktop*, *memory mobile* and *mobile CPU* cannot be measured without it.
>   The chain-spec row is measurable-when-declarable rather than blocked.
>
> Everything that was unblocked is merged. **#295 landed on `main` as `acf9c1ae` with 23/23
> checks green, verified per check**, and #292, #293 and #294 are closed as superseded rather
> than merged — all three are contained in it by ancestry, so merging one would apply the same
> work twice. Track F is **26 of 30**.
>
> **The branch and worktree cleanup ran with it.** 43 of 48 worktrees removed, and 27 remote
> branches deleted: the four this work produced, plus 23 whose pull request is `MERGED`, which
> is the only test that proves the work survives the deletion. The five worktrees left are the
> primary checkout, this session's own, the **locked** `vit-issuance` (open #296), Codex's, and
> another session's scratch tree. **Seventeen branches were left deliberately**, and a next
> session should not read that as an oversight: thirteen have a `CLOSED` pull request and four
> have none at all, so neither group can be proven contained in `main` by any mechanical test —
> a squash merge leaves no ancestry to follow. They need a person's judgement, not a script's.

> **Ten pull requests merged 2026-08-07/08. The load-bearing find is that one missing decision
> held four milestones, and nobody owned it.**
>
> `probeCriticalSurface` was defined once and called **zero** times; `createClient` was
> constructed nowhere. So 10 §5.2's compatibility classifier had never run on either chain — and
> `check-client-surface-obligations.py` exists precisely because *"an unfrozen read is one the
> compat lattice cannot fail on"*. The gate enforced the freeze faithfully while the consumer the
> freeze exists for did not exist. Behind that one gap sat F14's four unmeasurable budget rows,
> F17's screen registration, F18's deposit leg, and `implementedScreens()`, which still returns `{}`.
>
> **Why nobody built it (SQ-1010):** `probe.ts` waits on F6, whose row never mentions it; the
> 2026-08-04 log assigned the placement to F7, whose row never mentions it; PLAN called it *an
> F3/F7 boot-sequence decision*. **F3, F4, F6 and F7 are all ✅.** Handed forward three times, and
> every recipient closed without taking it. **F26** now carries it.
>
> **The sharpest single defect is in the same family.** `CompatSurface` declared four of PAPI's six
> groups, and `FOREIGN_SURFACE`'s frozen 02 §7.7 entry is a **call**, which lives in `compat.tx` —
> so `PolkadotXcm.limited_reserve_transfer_assets`, the deposit leg itself, was the one Asset Hub
> surface the compatibility check structurally could not examine.
>
> **Every review this session found a check that had quietly stopped checking.** An anti-vacuity
> test anchored on renamed text. A quota manager with no caller. A `TRIGGER_READS` table nothing
> derived from. `app/pnpm-lock.yaml` audited by nothing while doc 14's TH-44 named the control.
> Retiring a `blocking` obligation opening a 5-of-7 playbook activation whose trigger was never
> evaluated. A runtime-cache eviction whose own new test read it as boundedness. A mutation test
> asserting through a **local copy** of the function under test.
>
> **Three were mine, and the third was destructive.** A union resolution keyed on a substring that
> appeared inside another line's prose deleted a gate's documentation while its assertions passed —
> they checked one side of a two-sided union. A merge guard counting non-SUCCESS checks would have
> merged a PR whose run had not registered, because an empty rollup satisfies "zero failures". And
> a scripted edit of this very section deleted **3,485 lines** of PLAN.md, because `## Current
> focus` is followed by a 3,375-line blockquote and the script took everything to the next `##`.
> Caught by the line count, restored from git, redone with exact-match edits. **Edit this section
> by matching its opening lines, never by heading-to-heading range.**
>
> **2026-08-08 — the "externally blocked" reading was too wide, and the user's own constraint is
> what falsified it.** Asked which milestones could still close given that this project has
> **Zombienet and Chopsticks and no public testnet**, the honest answer turned out to be *more*
> than the parks claimed, not fewer. Twenty-two screens sit `built-unwired` and twenty of them
> wait on one sentence — *a live transport*, or *a pinned chain to boot against*. That reads as
> unreachable without a testnet. It is not: `zombienet/networks/bleavit-xcm.toml` already spawns a
> four-validator relay, Bleavit at para 4242, **Asset Hub at para 1000** and Coretime at 1005, and
> `tools/env/generate-relay-specs.sh` writes every one of their chain specs. That is the exact
> two-chain topology the client needs, Asset Hub half included. So the artifact is **generated
> rather than committed**, and the dev-pin ruling of 2026-08-07 already covers the difference: a
> development pin may exist and may never enter `release-sources.json`. What is genuinely missing
> is the harness that boots the app against it, and that is code this track can write.
>
> **F27 is ✅ as of 2026-08-08.** Both R-6 conjuncts are met: PR #283 at `5d49b69a` reports
> **23 of 23 checks SUCCESS**, and the spec review of the final shape returned **0 blockers**.
> Drill 14 passes end to end — `pin`, `boot` at `compatMode: "full"` and `specVersion: 2` with
> a finalized head, and `wrong-chain` refused as `FE-BOOT-003` — through the shipped
> `client-boot.js`, which until today had never executed at all.
>
> **The milestone's own thesis held against itself.** Four drill runs were needed, and three of
> the four failures were the harness rather than the client: a teardown that left three
> collators of the previous network alive, a helper that reached zombienet's embedded Node 18
> instead of the system one, and a leg parsing a shared stdout as JSON. Each looked like a
> defect somewhere else. So did every finding this milestone started from.
>
> A park is a claim and it decays — three notes in two days were falsified by one command each,
> and this was the fourth.
>
> **Track F's remaining milestones after F27 are externally blocked, and that list is shorter than
> it was.** F1 waits on SQ-940 and a device lab; F11 on FE-P7 and the rollout blockers; F13 on the
> key ceremony and live gateways **only** — its two-environment byte-identical run has been a
> required check named exactly that since #269, and this file went on calling it external for four
> days. F14 keeps three device-lab rows and loses five to F27. F17, F18 and F26 wait on F27 alone.
>
> **Two questions are the user's alone.** **SQ-940** — `ar-io/ar-io-ant-process` was archived
> 2026-06-09 and the contracts moved from AO to Solana, and an ANT authorizes by membership, so a
> controller list is **1-of-m**. It blocks launch. **SQ-994's membership** — the floor of three
> gateways is ruled and enforced, and naming the operators grants permanent allowlisted egress for
> every user, which is a values judgement rather than an implementation choice.
>
> **15 §4.8's nightly cadence is declined, by the user, 2026-08-08.** The reason given was the
> Actions time it costs every night, and that was the half of the question only the user owned.
> So the Zombienet suites keep the `release` and `g1` tiers `suites.json` already admits, no
> scheduled workflow is added, and **the drills run on a release and on demand**. Recorded here
> rather than as a spec edit: 15 §4.8 states a cadence for a project that is running, and this one
> has no nightly budget to spend yet. Revisit it when a release cadence exists — the gap is a
> deliberate, owned divergence and not an oversight, which is exactly what would otherwise decay
> into one.
