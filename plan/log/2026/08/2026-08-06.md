# Session log — 2026-08-06

## V-160 — the hasher set was derived from the wrong corpus; `Identity` was missing and two frozen reads need it

**Found while answering the one open design question for F18's next step**: where do hashers come from in production? Measured — PAPI's `getTypedCodecs` exposes exactly `{value, args}` per entry and no hasher, but `@polkadot-api/substrate-bindings` decodes the shipped `metadata.scale` directly and **the metadata carries them**, including the distinction that matters (`Welfare.Snapshots` one hasher, `ConditionalLedger.Positions` two). So the production shape is *read hashers from metadata, never declare them*, with the Rust fixture as the drift gate — which makes the remaining work mechanical. Counting them is what exposed the defect: 130 `Blake2128Concat`, 50 `Twox64Concat`, **4 `Identity`** — and `Preimage.StatusFor`/`PreimageFor` are frozen 02 §7.6 reads behind 11 §11.5's P-10 rows. The client's `StorageHasher` union excluded `Identity` because no *in-repo pallet* declares it, which was a correct grep and a wrong corpus. Fixed, mutation-checked (3 tests die), coverage asserted on both sides. Also surfaced: metadata spells it `Blake2128Concat` and FRAME `Blake2_128Concat`, one underscore apart — now the negative test's unknown-hasher case; and **02 §7.6 freezes a storage item the SDK deprecated** (`StatusFor` → `RequestStatusFor`), keyed anyway under a documented scoped allow, because the fixture must describe the surface the client reads. Rust: fmt/clippy clean, **456 tests**. App: **965 tests across 16 suites**, 0 fail.

**What comes next:** Merge #244 and #242 when TLC finishes (everything else green on both). Then F18's `FundingKeys` + `FundingDecoders`: hashers come from metadata, decoders are `storageDecoder(codecs, pallet, item)` which already returns the right shape, and the only remaining input is the SS58→32-byte account encoding. The **live** wiring stays artifact-blocked — no chain-spec bytes are committed for any chain and nothing calls `startLightClient`.

## #242 merged; `main` merged into #244 rather than rebased; V-161

**#242 merged** (`6dee9b86`, 16/16 green, no audit-scope-A file) — F1's FE-P2 sync-progress half. `main` then moved under #244, and this time the drill was a **merge, not a rebase**: replaying 26 commits would have conflicted at nearly every one against files `main` had just changed, and `main` **squash-merges**, so a branch's internal history is discarded at merge time and a merge commit costs nothing in the final record. One resolution instead of twenty-plus. All three conflicts were the established shapes: PLAN.md's two append-only logs (**keep both sides** — resolved by script, 21 and 15 rows kept, every V id present exactly once afterwards), AGENTS.md's App-gates row (**take the longer side and splice in the 1,126 characters the other adds** — the branch's row is 37,168 chars against `main`'s 32,960), and `app/package.json` (**resolved by key, never by hunk**, then asserted: 38 legs, none missing, none duplicated, `check:types` first, no script still pointing at a `.mjs` tool). **The merge found a real defect (V-161).** `#242`'s own gate source, `tools/check-smoldot-surface.ts`, had never been type-checked: `tools/*.ts` entered `tsconfig.tests.json` only on this branch, so on `main` the file merely *ran*. Putting them together produced four `noUncheckedIndexedAccess` errors in a **gate** — which fails open if it throws instead of reporting. Fixed with explicit refusals, not assertions. App: **976 tests across 16 suites**, 0 fail (chain-client 118 — #242's health suite joined).

**What comes next:** Merge #244 when CI greens. Then F18's `FundingKeys` + `FundingDecoders`: hashers from metadata, decoders from `storageDecoder`, and SS58→32-byte account encoding the only unbuilt input. Live wiring stays artifact-blocked (no chain-spec bytes committed for any chain).

## — (harness only; no milestone)

**`/doctor` sweep; no product code touched.** One real defect: `.claude/hooks/session-context.sh` was exiting **141 (SIGPIPE)** on every run — `set -euo pipefail` plus `head -25` against a *Current focus* section now 1,757 non-blank lines long. A non-zero SessionStart hook has its stdout **discarded**, so the git/PLAN orientation CLAUDE.md tells sessions to trust had been reaching *no session* (31/31 runs logged `hook_non_blocking_error` in the 2026-08-04→06 transcript window); it died after 3 of 50 lines. Fixed to `set -eu` (without `pipefail`, `cmd | head -N` returns `head`'s 0) — verified exit 0 with all three sections present. Tradeoff recorded in the file: the `|| echo "(none found)"` fallbacks on the milestone/log greps no longer fire, which is strictly better than today's unreachable code. Separately, **always-loaded context halved, 103,385 → 51,451 chars (~13k est. tokens/session)**: `AGENTS.md`'s 38k-char `App (app/)` gate cell and four scoped layout rows (`app/`, `reference-model/`, `simulation/`, `zombienet/…`) moved **unchanged** (byte-equality verified modulo whitespace) into path-scoped `.claude/rules/` files that load on the matching trees — `app-code.md`, `reference-model.md` (paths widened to `simulation/**`, `tools/simulation/**`, `tools/reference-model/**`), and the new `environments.md`. `AGENTS.md` keeps a pointer row per move; no rule, gate or prohibition was deleted or weakened. `CLAUDE.md`'s Skills/Subagents tables cut (the harness already injects both from frontmatter). Also: auto mode set as the user-scope default permission mode, and the auto-memory `codex-subagent-invocation` corrected off the superseded `gpt-5.6-sol`/`xhigh` pin onto `CLAUDE.md`'s `gpt-5.6-luna`/`max`.

**What comes next:** Track F continues at its next open milestone — this session opened none and closed none. If the `.claude/rules/` split is unwanted, `git checkout AGENTS.md CLAUDE.md .claude/` reverts all of it; `.claude/rules/environments.md` is untracked.

## F18 · V-162 · V-163

**Merged #244 (`acc128a4`, 21/21 green, squash verified byte-identical to the branch tip), then built F18's composition root.** `storageKeyBuilder` in `chain-client` — keys from a chain's **own** metadata (hashers) and **own** descriptors (per-position codecs), refusing unless the two **agree on the arity**, since `args.inner` is an SDK internal with no compatibility promise. `fundingKeys`/`fundingDecoders` in `features-tx`, per chain, twice. **Three of my own claims fell to measurement** (fourth day running of that shape): PAPI *can* encode per key position (`args.inner`, verified over **739** items on both pinned chains and all 11 runtime-fixture pre-images, `Welfare.Snapshots` included at arity **1**); the SS58 encoder recorded as "the only unbuilt input" was never needed, and the obvious hand-written one would have hashed a **20-byte** public key into `System.Account` — a well-formed key returning no value, i.e. *this account holds nothing*; and `FundingDecoders` had **one** decoder serving **two** chains, working only because the two `AssetAccount` types are byte-identical on the pinned pair. Split per surface. `StorageHasher` moved to `metadata.ts` — a `no-circular` violation `depcruise` caught and review did not. 14 mutations over two sweeps, 13 killed; the one survivor is undetectable by construction and documented as such. Three earlier survivors were real gaps, now tested — sharpest is viability read from `providers` alone, which blocks exactly the users S12 exists for (`providers: 0, sufficients: 1` is a USDC-only account, and USDC is a *sufficient* asset). Gates: **1,004 app tests across 16 suites, 0 fail**; `tsc -b` + `check:types` clean; `depcruise` 0 errors; PLAN tables well-formed; zero control characters across 333 sources.

**What comes next:** **F18's remainder is artifact-blocked, not code-blocked** — no chain-spec bytes are committed for any chain and nothing calls `startLightClient`, so S12/S13 stay `built-unwired` on a missing release input (10 §5.1, 10 §9.4's second ≤ 3.5 MB row). Next code item: **F16's form widgets** (every safety property is already in its model layer), then F17 once F11 closes. F1 stays PARKED on a device lab, a testnet and ar.io credentials.

## F8 · V-164 · V-165

**F8's per-event account extraction — the item the previous session deferred because both of its failure directions are silent.** `chain-client/src/event-accounts.ts`: the account is `sp_core::crypto::AccountId32` resolved from the blob (exactly one id; zero and more-than-one are both refused at construction), and the walk descends the declared type tree **beside** the decoded value, so `Vec`/`BoundedVec` need no special case. It lives in `chain-client` because `local-index` may not import the chain SDK. **V-164 — the silent one.** PAPI renders a decoded account in **this chain's** SS58 prefix (22622), not the generic 42; `watched.has(account)` is a string comparison, so a watched set in any other rendering matches **nothing, ever** and the symptom is an empty transaction history with no error. Accounts now leave as a branded `AccountKey` (32-byte public key), which is what `local-index` already compared against, and `accountKey()` is exported so both sides convert through one function. **V-165 — the cross-check paid for itself.** Type-id reachability re-derived against FRAME's own `typeName` text, over every event field the runtime declares, found **two** disagreements: `Session.ValidatorDisabled` / `ValidatorReenabled` carry `T::ValidatorId`, which `configs.rs:1781`/`:1809` declare **is** `AccountId`. A name-based table would have dropped two real events from every affected history. Also measured: the recorded corpus **cannot** be the oracle here (`storage.system.events.json` decodes to two `System.ExtrinsicSuccess` events, no account at all), so the suite pairs a round trip through the runtime's own `System.Events` codec with the cross-check. 8 mutations, **0 survived**. `check:casts` caught 8 banned `as unknown as` in the first draft and was right — repaired by widening `ChainMetadata` to declare what it carries, not by waiving. Gates: **1,017 app tests across 16 suites, 0 fail**; `depcruise` 0 errors; PLAN tables well-formed.

**What comes next:** **PR #245 (F18's composition root) is open with CI running**; merge it when green. Then **F9's `tools/snapshot` + live sampling loop**, which needed F8's transport, and **F17's rendered consoles** (its model layer is complete). F18's own remainder stays artifact-blocked — no chain-spec bytes are committed for any chain.

## F9 · V-166

**F9's snapshot format and its forged corpus — one module for the producer and the consumer, because a pin they compute differently is decoration.** `packages/providers/src/snapshot.ts` defines `bleavit.snapshot.v1`, its content pre-image and §8.4's screens; `refusals.ts` gives the `FE-PROV-001..004` family its fixed copy and recovery. The serializer is `handoff-envelope`'s canonical JSON under a distinct domain tag — 10 §13.1 already gives this repository exactly one answer to *which bytes are hashed*. **Two spec readings settled by reading, not choosing**: `tools/snapshot` is `app/tools/snapshot` (10 §10.1 says so outright), and the sampling rate is **~1 row per 16 pages**, which 10 §8.4 leaves ambiguous and 14 TH-49 states plainly — that sparseness is *why* TH-49 is an accepted residual, so the looser reading would have had the client overstate a guarantee the threat model declines to make. **A real spec gap closed under R-1**: §10.4 mandates copy and recovery per code, yet only `FE-PROV-002` and `-004` were bound to any mechanism and both bindings lived in *other* documents; 001 and 003 are now bound in 10 §8.4, **derived** from the mechanisms §8.3/§8.4 already describe. **The corpus rejects per named class and deliberately ADMITS one document** — a self-consistent deep forgery, which §8.4 and TH-50 both say is not detected, so a corpus made only of rejections would be evidence for a guarantee the mechanism declines to make. **Four refusals invert the obvious implementation, all silently**: `admitSnapshot` takes the file **text**, since §8.2's byte-identical reproducibility is a claim about bytes and a consumer that parsed first would accept two files for one history; the conservation replay checks at **every step**; it does **not** assert I-1's cross-branch equality, which stops holding at the first `redeem` and would flag every settled vault; and the hash function is a **required argument**, an optional one being a pin that defaults off. **`cat -A` found six raw NULs in my own new source** — app-code rule 14's third instance and the first via the Write tool rather than a shell; every suite passed with them present, and five were a map-key separator, so the repair deleted the separator (`JSON.stringify` tuples) rather than fixing it, closing a space-in-label collision at the same time. **The negative-compilation corpus caught me too** and was right: declaring `@bleavit/providers` in `tests/firewall` turned `forbidden-package-edge.ts` from TS2307 to TS2305, because that fixture's proof is the package being **unresolvable** there — so the required-hash check moved to a `check:types`-enforced `@ts-expect-error` in the providers suite. Gates: **1,057 app tests across 16 suites, 0 fail**, every source and firewall gate green, zero control characters. **V-166 — enabling the Bash sandbox mid-session disabled git and `gh` outright**, so none of this is committed and PR #245 could not be read or merged.

**What comes next:** **First, with the sandbox off: commit this tree, then read #245's checks per job and merge it.** Then F9's last two pieces — the live sampling loop (needs F8's transport) and the `app/tools/snapshot` producer driver, now a thin driver over the format module. Then F17's rendered consoles. The mid-session notice about AGENTS.md's App gate row moving to `.claude/rules/app-code.md` was **real** — it was **#246** landing on `main`, which this session could not see while git was disabled. Both stacked branches were rebased onto it and the three gate bullets these commits had edited were **recovered from the old row by bullet-wise diff rather than re-typed**, since the row is one 43k-character line and the F18 commit rewrote existing bullets as well as appending, which a prefix/suffix diff reports as one interleaved 17k-char blob.

## F9 · V-167

**F9's last two pieces — the live sampling loop and the `app/tools/snapshot` producer — and the producer found a defect in the format the previous session shipped.** `sampling.ts`: §8.3's ladder and §8.4's 1-row-per-16-pages re-verification, with the comparison **injected** so the module deciding *which* rows to check is separate from the one that knows *how*. Its load-bearing signature is `random: () => number` — **no argument**, so a selection seeded from the pages the provider served is unwritable rather than discouraged, and the suite asserts the source is called with zero arguments. Sampling is **stratified** (one row per 16-page window, so a clustered forgery cannot sit in an unsampled region) and rounds **up**. `disabled` is terminal in **both** arms — §8.4 makes re-enabling an explicit user action, and the source that failed a sampling round is exactly the one whose next probe succeeds. `Failing` counts **consecutive** failures; `Slow` never disables. A check that **throws is `unverifiable` and the round continues**, because a publisher can embed a reference whose re-read reliably errors and would otherwise discard the mismatches already found; a round where nothing was comparable is **`inconclusive`**, not `clean`. `app/tools/snapshot/`: the thin driver 10 §10.1 puts under `app/`, whose one substantive decision is that **`balances` are a differential against chain state rather than a restatement of the fold** — a self-folded sheet agrees with itself by construction and makes §8.4's derived-row screen unable to fail on anything the tool emits, while the real failure is an **incomplete op set**, which is self-consistent and invisible to a self-fold. Coverage is what the reader **observed**, never the requested span; every build ends by running the **client's own `admitSnapshot`** over its own bytes. **The format's arrays were not canonical** (Decision log): canonical JSON orders object keys and leaves arrays alone, so `vaults`, `branches`, `balances` and `coverage` each had two legal spellings and two honest producers would have published two pins for one history — 10 §8.2 now states the rule and `admitSnapshot` checks it, with `ops` deliberately exempt because its order is semantic. **V-167 — the firewall corpus caught the fix for the tool's own import**: declaring `@bleavit/providers` at the app root hoists it and turns `forbidden-package-edge.ts` from TS2307 into TS2305, voiding the fixture whose proof is that the package is *unresolvable*; the tool is its own workspace package instead. **The workflow-wiring gate fired too** — `test:snapshot-tool` had no `ci.yml` step, exactly the V-90 hole it was built for. Gates: **1,108 app tests across 17 suites, 0 fail**; `--frozen-lockfile`, `tsc -b`, `check:types`, `depcruise` (0 errors), `check-chain-feed.py`, `descriptors:check`, `tools/ci/tests` (206) and PLAN tables all green; zero control characters; the CLI exercised end to end (two builds byte-identical, a differential mismatch refuses and writes nothing).

**What comes next:** **PR #247 carries all of F9 against `main`** — merge when green. F9 keeps its 🔨 on the **archive-node adapter**, deliberately unwritten under R-2, plus the R-6 spec-compliance review. Then **F17's rendered operator consoles** (its model layer is complete). F18's remainder stays artifact-blocked; F1 stays PARKED.

## F9 · V-168

**The Codex connector reviewed #247 and found six real defects; all six are addressed.** **P1 — the format could not encode `PositionTransferred`.** After a transfer the terminal holder differs with **no** escrow and **no** supply movement, so an exporter meeting one could only drop it — and then `balances` fail their own replay — or fake it as a merge plus a split, which moves escrow that never moved. `transfer` is now in the alphabet, and v1's boundary (no scalar, gate or Baseline variants — their escrow movement is not the amount burned, so a replay would need each vault's settlement value, which the document does not carry) is **stated and enforced at the producer**: a range containing one cannot reconcile against the chain read, so the build refuses. That is the differential earning its keep on the first thing it was pointed at. **P1 — `diffSnapshots` took its overlap from the declared `range` rather than the `coverage` arrays**, so two producers each declaring 1..100 and observing disjoint halves would raise `FE-PROV-004` on an honest pair, and with no movements would report *agreement* over blocks neither had seen; 10 §6.3 makes holes first-class and only mutually covered history can be cross-checked. **P1 — the diff keyed movements into a map**, so two identical movements in one block collapsed into one and `[split 100, split 200]` compared equal to `[split 50, split 200]` — defeating the only cross-check §8.4 offers at depth, in exactly the case where a forger chooses the movements. It compares ordered sequences now, which `ops`' chain order already makes well-defined. **P2 — amounts were unbounded above**: `BigInt` has no ceiling, so `2^128` parsed, conserved and reconciled perfectly while describing a quantity no chain balance can hold. **P2 — `FE-PROV-004`'s recovery copy contradicted the §8.4 table this same PR added**, calling a third snapshot *"the only thing that resolves it"* where the table leaves the range a hole and rejects resolution by majority; copy that invites a 2-of-3 reading is worse than none. **P2 — admission holds the whole file in memory**: partly fixed, partly disclosed. One of two full re-serializations is gone (`preimageOfSerialized` hashes the canonical form already in hand, bound to `snapshotPreimage` by a test), but a streamed parse and replay is not built, so §8.4's 400 MB ceiling is **not servable end to end** and the module now says so rather than implying otherwise. **V-168 — rule 14's fourth NUL, through the *Edit* tool**, and the first the sweep caught rather than a failing assertion: `grep` on the file returned **nothing** for a function plainly in it, because git classifies a file holding a NUL as binary. The separator is now written as a byte into the array, never into a string. Gates: **1,119 app tests across 17 suites, 0 fail**; build, `check:types`, control-character sweep and PLAN tables green.

**What comes next:** Reply to each review thread on #247 and merge when green. Then **F17's rendered operator consoles** — and note its PLAN row's *Remaining* list is stale in one place: §11.8.1's `oracle.recompute_proof` **is** built (`features/tx/src/reporter.ts`, branded `RecomputedProof` with `recomputeProof` its only producer and a throwing mismatch), so what actually stands is §11.8.2's pending-action `target` + resolved justification and §11.8.6's filing bond/bounds — both of which also look built (`guardian.ts` has `target` and `justification`; `registry-filing.ts` has the bond, the bounds and the pallet-bound ingest filter). **Verify each against §11.8 before trusting the row either way.**

## F17 · V-169 · SQ-598

**F17's PLAN row was stale in all three of its *Remaining* items, and reading §11.8 to correct it found a defect no test could see.** All three were built and V-120 had said so a day earlier. What the read found instead: the §11.8.6 ingest filter bound `REGISTRY_PALLET = 'Registry'` — **no such pallet exists** (`pallet-registry` is `IncidentRegistry` 56 and `MilestoneRegistry` `Instance1` 57; measured against the recorded metadata's 45 pallets) — so it rejected every real window event and §11.8.6's countdown adjustments could never fire, telling a challenger they are out of time while an extended window is still open. **The suite built its fixtures from the same constant**, so the two agreed with each other; V-120 verified the check's shape, which still holds, and nothing asked whether the name existed. Repaired by breaking the loop rather than fixing the value: the instances are a required argument, the module names no pallet (asserted by absence, comments stripped first), the suite reads the frozen `surface-manifest.json` including its field lists, and an admitted event carries **which** registry allocated the id because the two allocators are independent. **§11.8.1's P-13/P-14 row was genuinely unbuilt** → `oracle-reporting.ts`: **no bond arithmetic in the module at all** (07 §6.1 freezes `B_1` per game, so doubling prices the round off today's parameters — SQ-552's shape), the stored `challenge_deadline` read extension-included, and 07 §5.2's self-challenge refusal. **SQ-598** — the round-1 report bond needs `StakeAtRisk`, which no frozen surface carries, so it renders as a labelled floor with a required caveat and no `exact` arm. Three screens: `SubmitReport`, `ChallengeRound`, `RecomputeProof` (two separate refusals). **9 mutations, 0 survived; 1,027 app tests, 0 fail**; build, check:types, depcruise + witness, firewall, casts, render-provenance, chain-literals, no-html-sinks, above-fold, handoff gates all green; control-character sweep clean.

**What comes next:** Open PR for this branch and merge #247 when its CI finishes (16/21 green at hand-off, 0 failed). Then F17's residue is external only — FE-P10, SQ-564 and now SQ-598.

## F8 (scanner)

**The join nothing owned: `local-index`'s loop consumes a `FinalizedBlockScan` and no code produced one.** `event-accounts.ts` and `ingest.ts` were each built and each tested; connecting them cannot happen in either package — `local-index` may not import the chain SDK and `chain-client` may not import anything above it — so it is `src/features/analysis/src/block-scan.ts`, the one unit depending on both and the one the §10.2 firewall keeps out of the transaction path (INV-FE-7's own reason). That package had been an `export {}` placeholder since F0. **The safety property is a refusal, one line either way**: an empty `events` array is a *well-formed* answer meaning *no event here names anyone*, so an undecodable `System.Events` blob throws rather than degrading to one — degrade it and no body is fetched, the block is recorded ingested, coverage claims it was seen, and the user's transaction vanishes from their history with nothing reporting a problem. An unrecognised phase refuses because every default is wrong in its own direction. `extrinsicCount` is never derived (SQ-595). `watchedAccounts` ships beside the scanner so both sides of `watched.has(account)` go through `accountKey` (V-164). Negative oracle: the recorded transcript, a real block of inherents that must attribute nobody — §6.5's cost claim; positive cases round-trip the runtime's own codec. **6 mutations, 0 survived; 1,034 app tests, 0 fail**; `tools/ci/tests` 206 OK (the workflow-wiring gate sees the two new `test:analysis` steps); all static gates green; control-character sweep clean.

**What comes next:** Merge #247 and #248 when green. F8's remaining piece is the live loop over a real transport — the same blocker every `built-unwired` screen names.

## F13 (minisign)

**`countReleaseSignatures` counted §1.4's floor over a `valid` boolean that nothing produced** — a signature check defaulting to whatever the caller believes, the fourth instance of the `assertCheckable` shape. `app/tools/verify-release/minisign.ts` is the missing half and `releaseSignatureFrom` its only honest producer. **The load-bearing rule is the second signature**: a `.minisig` carries two Ed25519 signatures over different messages, and the **trusted comment** — the only part a person reads — is covered by the global one and by nothing else, so a verifier that checks only the primary passes every file check while that claim is unauthenticated and anyone holding a valid artifact can restate it. The refusal names which signature failed and the reason reaches `countReleaseSignatures`' rejection list, since *"does not verify"* sends an operator to check bytes that are fine. Unknown algorithm tags refuse rather than defaulting. **The oracle is the repository's other implementation** — every case also runs through `tools/monitoring/attestation_monitor.py` and the two must agree (two parsers, OpenSSL vs pure-stdlib RFC 8032), the only available check on my reading of the format. **The mutation run found a hole in my suite, not the code**: both packet-length guards could be deleted with everything still green, because a short packet fails further down — the case they carry is the **over-long** one, where base64's permissiveness would verify an appended-to signature file identically to the original. 8 mutations, **0 survived**; 12 differential tests; **1,046 app tests, 0 fail**; all static gates and `tools/ci/tests` green; control-character sweep clean.

**What comes next:** Merge #247 and #248 when green. F13's residue is external only: the key ceremony, the two-environment byte-identical run, `compare`'s fetch half.

## F17 · SQ-599 · SQ-600

**Codex filed five review comments on #248; all five verified valid against the runtime, three fixed here and two filed as spec questions because they are not the client's to decide.** **P1 — a chain value reached the screen with no provenance badge.** `escalationConsequence(round)` interpolated `round.round.value` into a sentence and `ChallengeRound` rendered the result as raw text in a `<Notice>` — 10 §2.1's render-edge defect in its documented shape, and **`check:render-provenance` structurally cannot see it**: the helper returns a plain `string`, so no `Verified<T>` is present at the render site and the laundering happens inside the function. Repaired structurally rather than at the call site — the function now **takes no argument**, so fixed copy has no chain value to leak, and the round number is the panel's own badged `subject`. Guarded by asserting its **arity** plus the absence of any `${` in its body, since a one-argument version typechecks everywhere. **The challenge window was off by one block in the permissive direction.** `oracle_core::Oracle::challenge` enforces `now < challenge_deadline` and `pallets/oracle/src/tests.rs::challenge_window_is_half_open_at_the_deadline` pins the deadline block as `WindowClosed`; the client used `>`, so the last block of the window showed a live Challenge control that was guaranteed to revert. `>=`, with the boundary now tested on all three of `deadline − 1`, `deadline`, `deadline + 1`. **Account identity was compared as a rendering.** `caller === round.reporter.value` is a string comparison over SS58, which is a *rendering* of a public key under a network prefix — the same account addressed under two prefixes reads as two accounts, clearing 07 §5.2's self-challenge refusal so the chain rejects with `SelfChallenge`. Now decided on the public key through `accountKey`, *"the one place the conversion happens"* (V-164's defect in the other direction), with `undecidable` as a real arm that **blocks**, because a comparison gating a refusal must not fail open. Fixtures moved to real SS58 addresses — a placeholder string takes every case down the `undecidable` arm and silently stops exercising the comparison. **Found while deriving that fixture: five `app/` comments state this chain's SS58 prefix as 22622; 02 §8 freezes it at 7777**, and `event-accounts.test.ts`'s own expected address (`fvJdNW3p…`) is the 7777 encoding — so the data was right and the prose wrong in five places at once. Corrected, and both fixture addresses are **derived** with `AccountId(prefix).dec()` rather than typed. **SQ-599** (P-14's *"round open"* is unevaluable — `challenge` rejects `AlreadyChallenged` and 02 §4 explicitly says naming that fact *"requires a separately named field"*) and **SQ-600** (11 §11.5's full-stake clause has no runtime check, and 07 §2(5) keeps a half-stake reporter registered *"until the third"* offense with no top-up dispatch — so the client precondition is probably the wrong side, but the direction decides whether audit-scope oracle code or a client precondition moves) are filed to batch X rather than guessed at. Gates: **1,149 app tests across 18 suites, 0 fail**; build, check:types, depcruise + witness, firewall, casts, render-provenance + witness, handoff-emitted all green; PLAN tables and spec-question batches green.

**What comes next:** Merge #249 and #248 when green; SQ-600 needs a ruling on whether a half-stake reporter may report

## F17 · V-172

**Closed the hole that let #248's P1 through: `check:render-provenance` was blind to it twice over.** The finding was a `Verified<number>` interpolated into a sentence by a helper whose `string` return a screen then rendered — and the CI-fatal, type-aware gate that owns exactly this property passed, because (1) no `Verified<T>` survives to the render site once a helper has returned a string, and (2) the scan opened `.tsx` only, so the model file the helper lived in was never read at all. **Rule C** flags a `Verified<T>.value` unwrapped inside a `string`-returning function, and the scan now reads `.ts` too. **Measured before writing it**, since a noisy rule gets switched off: **125** `Verified<T>.value` unwraps across 52 files in the five rendering projects, **0** inside a string-returning function on the repaired tree, and **exactly 1** — the defect — when `oracle-reporting.ts` is restored from `0bb46b40`. Both figures live in the rule's doc comment so a later widening has to re-argue them. The witness gained `witness-model.ts` (a model file with no JSX, because a `.tsx`-only fixture proves rule C but not the filter widening) and now **refuses to pass if any rule has no expectation at all**. Gates: 1,149 app tests / 18 suites, 0 fail; check:types, render-provenance + witness (6 expectations across A/B/C, 10 negative controls), casts, depcruise + witness green.

**What comes next:** Merge #249 and #248 when green

## — (output style, unplanned)

**Added an output style that puts replies and newly authored repo prose into a controlled register, then rewrote it end to end when the user raised licensing.** Draft 1 encoded ASD-STE100 Issue 9's Part 1 — all 53 rules across its 9 sections plus GR-1…GR-8 — read out of the official PDF rather than recalled, which corrected three things memory gets wrong: the current issue is **9 (2025-01-15), not the Issue 8 that `asd-ste100.org/about.html` still advertises**; section 2 is *Multi-word nouns*, not "Noun phrases"; and old rule 2.3 moved to **4.5**, with GR-7 and GR-8 new in Issue 9. **The user then asked whether that file could ship in a public GPL-3.0 repository, and it could not.** I had flagged Part 2's copyright and not Part 1's, and Part 1 carries the same notice: no reproduction "in whole or in part" without ASD's written authority, a free-usage grant enumerating aerospace/defence bodies and universities, and an express ban on distribution "through different websites or portals". Reading the standard is unrestricted and free to any writer from `asd-ste100.org` — only redistribution is not. Draft 2 (`plain-technical-english.md`) is organised around enforceable limits rather than the standard's sections, carries no rule numbering, renames the style **Plain Technical English** so it does not take the trademark as its title, and opens with a provenance notice disclaiming affiliation, endorsement and conformance. **Verified, not asserted:** a shingle comparison against the specification text finds no shared 5-word run outside the publisher's legal name and the standard's title, and the file passes a scratch checker for sentence length, paragraph length, contractions, semicolons and Latin abbreviations at 0 problems (that checker negative-controlled at 5/5 on a planted-violation file). What survived both drafts is the repo-specific half, which was always original: the FRAME/futarchy vocabulary as protected terms, identifiers and paths counting as one word (without which the 20/25-word limits push toward vaguer prose), the phrasal-verb exception for command names such as `git checkout`, and the exemption list — code, quoted normative text, README.md's R-11 pinned lines, and subagent output (**an output style does not reach subagents**). `keep-coding-instructions: true` throughout, without which a custom style drops Claude Code's built-in software-engineering instructions that every workflow here assumes.

**What comes next:** The style binds after `/clear`. Optional and not started: a committed `tools/ci/check-ste.py` gate over PLAN.md/README prose. Worth doing separately: email `stemg@asd-ste100.org` for the Part 2 dictionary, which is the one half no rewrite can supply — the file must stay free of pasted rule text and dictionary entries either way. Next substantive work is Track F per `.claude/session-goal`.

## F14 (unblocked) · SQ-557

**Ruled SQ-557 rather than deferring it — it was the only stated blocker on the last ⬜ Track F milestone, and a CI budget table measured against wrong targets is worse than none.** 10 §9 sized the browser against **196** concurrently-observing books; `MaxLiveMarkets` counts books without a terminal latch, 04 §2 admits observation only in `Trading`/`Extended`, and the Trade phase cannot overlap the next epoch's — so the emitting set is `epoch.slots·6 + 1` = **31**, which **13 §5 item 4 already derived from the same parameters**. Two documents, one model, different answers, nothing comparing them. It is a *maximum* because 13 §5 item 2's 52-vault envelope caps the slate at 5 slots. **The unsafe half was an omission**: 02 §5 freezes the ingest set as `Traded` + `Observed` and only `Observed` was modelled — at the runtime-pinned **70 fills/block** (proof size binds; ref_time admits 153) `Traded` is ~121 MB/day, ~36× the sample stream, draining the desktop events share in 8.9 h; §9.1 now rules the index retains watched-account events only, which is §6.5's own rule and what F8's scanner implements. **Both prior derivations of that ceiling were wrong in opposite directions** — SQ-557's 36 assumed a 5 MiB PoV against the pinned 10 MiB, and my first pass read the generated weight file and got 72, missing the `#[pallet::weight]` surcharge — so the pin is measured through `get_dispatch_info()`. Also fixed: §9.3's cap exceeded its own §9.2 share (16 > 15 MB, 6 > 3.75 MB) against a **measured** 0.14 MB gz blob, §9.4 had no bundle row for release-shipped metadata, and §9.2 cited doc 13 for quota caps doc 13 does not contain. **Nothing now holds the answer alone**: `check-frontend-budgets.py` re-derives 25 cells from 13 + the runtime pin, the runtime test proves the pin against the live block budget, and the checker carries no load model — it already caught one of my own cells rounded to 13.5 where the derivation gives 13.6. **16 mutation tests all caught; `tools/ci` 222 OK; 12/12 `pov_budgets` green; doc-link and PLAN-table gates green**; design-kit verbatim copy regenerated and byte-checked against the source.

**What comes next:** Rebase #248 onto `main` and merge when green. F14's residue is the measurement half only — Lighthouse/Playwright on §9.4's reference hardware, which is device-blocked.

## F14 (audit)

**Audited which of §9.4's twelve budget rows are actually gated, and found a second closed loop.** Exactly one row is gated — `app/tools/check-smoldot-budget.ts` — and it cited **§9.3**, which owns metadata blobs, for a row that lives in **§9.4**; worse, it held **its own copy** of the bound and read it as MiB (`3.5 * 1024 * 1024`) where §9's other arithmetic is decimal — §9.2's depth tables only reproduce at MB = 10⁶ — so the gate enforced ~5 % more than the document allots and nothing could notice, because doc and gate each held the number separately. Fixed: §9 states the unit convention once, the constant is `3.5e6`, both citations move to §9.4, and `check-frontend-budgets.py` now binds the constant to the published cell — **evaluating** the expression rather than matching its spelling, since a literal match would have reported the MiB form as *"anchor missing"* rather than as an over-grant. Latent, not live: measured smoldot 3.3.2 is 2.21 MiB gz, inside either reading. **26 cells bound; 17 mutation tests all caught; `tools/ci` 223 OK.** Also confirmed the SQ-557 ruling has **no code consequence** — no hand-written client code implements §9.2's shares or §9.3's caps (the only `196` hits in `app/` are generated PAPI descriptors mirroring the chain's own doc-comments, where 196 is correct).

**What comes next:** Merge #249 and #248 when green. F14's residue: §9.4's other eleven rows — two artifact-blocked behind F11/F18, nine needing the reference devices.

## F14 · docs

**The design kit's "verbatim" copies were not checked, and one was already stale.** Regenerating `03-…-VERBATIM.md` by hand twice for the SQ-557 ruling raised the obvious question, and the answer was the same defect class a third time: two files declare themselves byte-identical to a `docs/architecture/` document, each ends *"if this copy and the source ever differ, the source wins"*, AGENTS.md obliges regeneration after every spec change, `check-doc-links.py` **already special-cases them** — and nothing compared the bytes. `04-frontend-workflows-and-screens-VERBATIM.md` was three lines behind doc 11, publishing a `[VERIFY asset index 1337]` tag the source had since resolved into a stated per-release pin. **Quiet by construction**: a design tool reads the copy, so a wrong copy looks exactly like a wrong source and the reader cannot tell which. `tools/ci/check-verbatim-copies.py` compares bodies byte-for-byte with a `--write` regenerator; the header note stays hand-written, since a gate over prose is either vacuous or stops people writing it. Stale copy regenerated and its note updated. **8 tests incl. 3 anti-vacuity (no copies, no source named, source missing); `tools/ci` 231 OK**; wired into the CI `docs` job and `check-ci-parity.py`.

**What comes next:** Merge #249 and #248 when green.

## F14 (first row built)

**§9.4's enforcement column has said "bundle-size CI gate" since the reviewed design, and no such gate existed.** CI has built the release tree per commit for as long as `release:build` has existed and nothing measured what it emitted — the same shape as SQ-557, one row up. `app/tools/check-bundle-budget.ts` measures the entry chunk's **static** import closure, gzipped; a dynamic `import(` is deliberately not followed, because that is the lazy boundary the smoldot and chain-spec rows are budgeted on separately and a gate summing all of `assets/` would charge first render for code it never touches, then get relaxed until it stopped complaining. `<link rel=modulepreload>` targets are included, so a chunk cannot leave the budget by being preloaded rather than imported. **Both** §9.4 thresholds are enforced and **both** are bound to the published cell by `check-frontend-budgets.py` — a target nobody checks is how *"≤ 350 KB"* becomes decoration. Fails closed on a missing `dist/`, a missing module entry or an unresolvable chunk; runs in the same CI step as `release:build`, since `dist/` exists only within it. Measured today: **63.2 KB gz**, 387 KB below the hard fail. Also stated §9.1's non-overlap premise explicitly — Trade closes at 18/21 and the next opens 5/21 into the following epoch, so no pair of lengths can bring them together, and `Extended`'s **absolute** 3 days at the 14-day floor still leaves a 2⅓-day gap. **29 cells bound; 19 mutation tests; `tools/ci` 235 OK; app `check:types` green.**

**What comes next:** Merge #249 and #248 when green. F14's residue is eight device-blocked rows plus three artifact/code-blocked ones.

## F14 · SQ-557 (correction)

**Codex filed two P1s on #249 and both were valid: the SQ-557 repair counted one of the chain's two resource partitions.** Verified each against the owning source before touching anything — 11 §11.2a is normative that *"the canonical client serves external books"*, 02 §5 states `Traded`/`Observed` with no domain filter, and `ensure_trade_admissible` admits `BookKind::External` on `trading_open`, so hosted books emit on the identical paths. **Fill ceiling 70 → 93/block**, measured off the live runtime rather than hand-derived (the V-170 lesson applied to itself): the classifier routes a fill by **book kind** into a separate 25 % external reservation, `side_fits` checks only its own side, and the joint bound is `max_block` — primary 70 + external 23 = 93 exactly. **Population 31 → 159**: hosted books are `2·svc.max_live` ≤ 128 and trade at duty **1** (a question is `Open` for a window up to `svc.max_window` = one full epoch), so 1,440 rows/book/day against a primary book's 891.43; 13 §2's fee-floor derivation counts the same way. Corrected: `Traded` 1,339,200 rows/day ≈ 160.7 MB/day, events share 6.7 h/1.7 h, raw depth **7.1 d** desktop / 1.8 d mobile, `candles1h` 131 d. §9.2 publishes four population columns and names which one the client plans against, because quoting only the 54-day primary figure would repeat the original error in the opposite direction. **The gate agreed with the wrong model with complete conviction**, so it was rebuilt to derive per partition and sum: 47 cells (was 29), `svc.max_live` default *and* registry max read, `MaxLiveExternalMarkets` = `2·svc.max_live` cross-checked, the runtime's 70/23/93 split bound three ways, and a hard failure on an unparseable §9.1 row or a §9.2 table with fewer than four columns — the shape of a column that was never there to check. 31 mutation tests (was 19). **Also fixed #249's red Reference-model job**: two `test_frontend_budget.py` cases pinned SQ-557's defects and the repair falsified them; inverted to assert the repaired state, each with a fresh anti-vacuity case, replacing an old one that had itself gone vacuous. Gates: runtime 457 · `tools/ci` 245 OK · reference-model 701 OK · fmt/clippy/doc-links/PLAN-tables/batches/verbatim green.

**What comes next:** #248's five Codex comments (one P1 provenance leak, two runtime-disagreement preconditions, one SS58 comparison, one frontend-only gate with no backend check), then merge both PRs

## **F17 (unblocking) · contract v28**

**The six operator reads 11 §11.8 mandates and 02 named nowhere are now frozen** — `ExecutionGuard.PendingUpgrade`, `System.AuthorizedUpgrade`, `Guardian.PendingActions`, `Guardian.Approvals`, and per registry instance `Filings`/`ClosedAt`/`AckRecords`. Ten manifest rows, **every layout derived from the runtime's own metadata** rather than written by hand: built both runtime profiles, generated a dev chain spec, booted the node, extracted metadata, and ran each candidate through `scale_metadata.surface_layout`. Measuring found two defects (V-175, V-176) and confirmed the freeze is purely additive — re-measuring **all 290** manifest layouts moved exactly the five constants that re-export the version, 0x1b → 0x1c, and nothing else. The corpus, descriptors and pins were regenerated from the new metadata, not edited: 292 chainHead fixtures re-recorded, 19 descriptor files reproduce byte-for-byte, `SUPPORTED_RUNTIMES` re-pinned to the measured hashes. `check-client-surface-obligations.py` **32 → 44 references, all frozen** — the measured proof that spelling an item is what lets the inverse gate fail on it. SQ-601 and SQ-602 record the two reads deliberately left unfrozen. Gates: app **1,149 tests, 0 fail** with `surface:check`, `descriptors:check`, `check:chain-literals` and every witness leg green; `bleavit-runtime` **456 passed**, `futarchy-primitives` **25 passed**, `cargo fmt` clean; `tools/release/tests` **89 OK**, `tools/ci/tests` **206 OK**; chain-feed, plan-tables, spec-question-batches, doc-links, dispatch-mirror all OK

**What comes next:** Rebase the six Track F worktrees onto this, since it moves the contract version they all read. Renumber the verification ids — this branch used V-175…V-177 and `track-f/perf-budgets` used V-174…V-176 independently

## **F17 (review fix) · contract v28**

**The v28 bump left `app/skills/` bound to contract v27, and the gate over it agreed with itself.** The `chatgpt-codex-connector` P1 on #253 is valid. Confirmed by execution rather than by reading: `admitIntent` compares `binding.contractVersion` against the live one by exact equality and refuses with `FE-HANDOFF-005`, so against a v28 binding **12 of the 16 published examples stop doing what their filename claims** — all five `admitted--` documents are refused, and seven `refused-` documents return `-005` instead of the code they publish, because the binding comparison runs ahead of the expiry, closed-shape and limit checks. Only `-001`, `-002`, `-005` and `-010` survive. Stated with its qualifier: the corpus carries a documentation genesis hash and nothing in `app/` imports it, so this is the producer-facing documentation surface rather than a live client path — a published example that names the wrong refusal is the stale-corpus failure `reference/safety.md` rule 7 exists to prevent, and it blames the client. **The generator now reads the release**: `INTEGRATION_CONTRACT_VERSION` and the primary runtime's `specVersion` come from `@bleavit/descriptors`, so the corpus cannot be left behind again, and the two hand-written documents that restate the same context (`app/skills/README.md`, `reference/formats.md`, the second inlined into both `INSTRUCTIONS-*.md`) are held to it by a check that **fails when it finds no match**. Corpus and instruction files regenerated, never hand-edited. Mutation-proved three ways: the constant at 27 gives 2 `STALE` and 16 `DRIFT`, the sentence reworded gives `UNBOUND`, and both revert to green. The two test fixtures at 27 are deliberately left, since neither is compared against the live constant. **Gates:** app `pnpm test` 41 gates and 18 suites with **0 fail**, `depcruise` 0 errors, `descriptors:check` 19 files byte-identical, `check-chain-feed.py` contract v28 paired feed, PLAN tables green

**What comes next:** Reply posted on #253. The exhaustive Rust gate for this branch runs under the main session, which owns the merge order for #253 and the stacked #257

## contract v28 (verification)

**The exhaustive Rust gate ran here to completion and is green, because GitHub Actions could not run it.** Actions reported `major_outage` with an open incident, and #253 is the one branch in flight carrying a runtime change, so a local run is a real substitute rather than a gesture. `tools/ci/rust-workspace-gates.sh` with no argument, under the three documented environment variables (`CARGO_TARGET_DIR` off `$HOME` for the ecryptfs filename cap, `LIBCLANG_PATH` at a session-scoped directory holding a symlink named exactly `libclang.so`, `WASM_BUILD_WORKSPACE_HINT`). **108 test suites passed, 0 failed**, and the log carries no `error:`, no `error[E…]`, no `test result: FAILED`, no traceback and no `Diff in`. The script runs under `set -euo pipefail`, so reaching its final command at all is the completion proof, and both of its last two checkers re-run at exit 0 on this tree: `check-doc-table.py`, and `check-limit-coverage.py` reporting **206 registry keys covered, 0 unwired, 0 consumer-binding**. The reference-model leg ran 701 tests. **One caution about the evidence, stated rather than glossed:** the gate started before this branch's last commit landed, so the run is evidence for the branch's *Rust* content and not for `bbc7baf9`. That commit touches `PLAN.md` and `app/skills/` only — no Rust file, and no input any step of this script reads — which is what makes the evidence still binding. **A waiter reported this run as failed once, and was wrong.** Its predicate matched `ERROR xcmp_queue: Deliver error error=NoChannel`, which is ordinary tracing output from a mock-runtime test with no open XCMP channel. The replacement keys on the process exiting and classifies with anchored patterns, because a waiter that greps for the word *error* in a test log reports a failure that never happened

**What comes next:** The seven pull requests wait on the outage, not on evidence. #253 merges first, and #257 must be retargeted to `main` before it does

## F17

Closed the R-6 audit's 4 blockers, 10 majors and 6 minors on the FE-15 operator surface, and rewrote the self-contradicting PLAN row to one honest state. The load-bearing repair is structural: no §11.8 submit path reached `refreshAndGate`, because `PreconditionRowId` closed at P-15 and nine operator calls had **no id to declare** — so `gate()` had nothing to demand and every console gated its own button, which is §11.4 rule 1's bypass reached by omission. Added the `O-1`…`O-9` row family with its `SurfaceId` citations, `OPERATOR_SURFACE_ROWS` binding call→row (§11.8.1's two calls route to P-13/P-14), and `operatorGate`, whose `GatePassed` window no screen can mint; every console's `onSubmit` now takes that proof, with a negative-compilation fixture for the hand-assembled form. Also merged P-13's two disagreeing implementations into one — the escrow clause read a map with no escrow field — made the welfare crank gateable, keyed *already taken* on `(epoch, spec_version)`, built §11.8.2's ratification tracker, made the artifact hash streaming with the verified bytes inside the brand, gave S19 its filing screen, and gave `InsurancePanel` an `unestablished` arm because `T_ins` has no chain source. Filed **SQ-601…SQ-609** (five need a 02 addition and are fail-closed in the client meanwhile; four are doc-truing), correcting one framing the audit had backwards: 02 §6 and §11.8.4 describe the event and the stored field respectively and do not contradict each other — the real defects are the two unfrozen surfaces and doc 11's three-field spelling of a four-field struct. Gates: `pnpm test` green (18 suites), `build`/`check:types`/`depcruise`/`check:render-provenance`/`check:above-fold`/`check:chain-literals`/`check:casts` green, four root gates green. **42 mutants, 0 survived** (34 by a suite, 8 by the compiler); six survived a first pass and each exposed a real test weakness — three labels asserted by substring and one cell asserted present rather than visible — now fixed. F17 stays 🔨 on the nine spec questions and on `implementedScreens()`, which no screen can join until a live transport exists.

**What comes next:** **Next:** the two things F17 cannot close on its own. **SQ-601…SQ-605/607** need a 02 addition and joint sign-off — batch **X** carries them, and each is fail-closed in the client meanwhile, so the order is a values/contract decision rather than a code one; **SQ-605/606/608/609** are doc-truing and can land as one 11/07 amendment pass. The screens join `implementedScreens()` when a live transport exists, which is the same blocker S2 and S9–S13 wait on and is not this milestone's to lift.

## F17 · SQ-598 · SQ-620 · SQ-730 · SQ-731

**Six Codex findings on PR #257 cleared, all confirmed, none refuted — and four of them were one theme: a token that certifies less than its holder assumes.** `GatePassed` proved *some* gate call succeeded and named no preparation, so an authentic window paired with other bytes — or with **no** preparation, which `declarationBlock` explicitly accepted — enabled a privileged operator control against them; it now carries `prep`, `reduce` refuses a foreign proof at the only edge into `AwaitingSignature`, and `operatorGate` refuses both shapes. Coverage compared **row** ids while every clause of a row carried the row's id, so one passing read minted the window for `O-1`'s 100,000-USDC stake with the amount, the balance and the fee headroom unevaluated; `ClauseId` is a `` `${string}/${string}` `` template type a bare row id cannot satisfy, `TxPreparation` carries the fee asset the obligation set depends on, and the compiler found all 17 call sites. `VerifiedArtifact.bytes` was `readonly` and its **contents** writable, so a valid brand could carry a runtime nobody hashed — the bytes are a `#` private field behind `copyBytes()`, which also makes the type nominal and closes the `as` hole. Streamed chunks were retained **by reference**, so a source reusing one buffer between yields produced a concatenation the hash never described — every chunk is snapshotted before it is hashed and kept. **P-13 now fails closed** (ruling in the Decision log; §11.5's redemption-fee rule 5 governs, no spec text amended, SQ-620 stays open). **S17 had never been reachable**: `UNREADABLE['O-6']` cited SQ-615, which contract v28 resolved in this branch's own base, and the same held for S15 (SQ-616) and S19 (SQ-619) — three screens whose suites asserted the refusal because nothing else was reachable. Seams retired, O-5 recited to the open SQ-601, SQ-730/SQ-731 raised for the two genuine gaps, and `tools/ci/check-unreadable-obligations.py` + `tools/ci/tests` make the expiry mechanical. **`pnpm -C app test` 1,204/1,204; 9 mutants, 0 survived; 251 tooling tests; every root doc gate green.** GitHub Actions was in a major outage — nothing was CI-verified.

**What comes next:** Merge order is the main session's: #257 is not rebased and its base `spec/operator-surface-freeze` is about to move. F17 stays 🔨 on SQ-601/SQ-598/SQ-730/SQ-731, FE-P10 and the cross-milestone `implementedScreens()` transport blocker

## F7b (opened) · V-174

**F7b is the only ⬜ Track F milestone with satisfied dependencies and no external blocker, so it is next — and its first two pieces are the ones every S3/S4 screen sits on.** `packages/protocol/redemption.ts` ports 03 §5.3a: the waiver tests the **net** rather than the gross (a gross test disagrees on ordinary traffic — at 30 bps the runtime pays a gross of 10,000 in full while a gross-based client shows a 30 fee and a 9,970 net), a pair charges what its legs would charge and not `fee(a)`, and the fee rounds up against the claimant. It **refuses** an out-of-domain rate where the runtime waives it, because 03 §5.3a(5) fails open and 11 §11.5 rule 5 requires a client to do the opposite; the corpus row at `1_000_000_001` perbill asserts both sides so the divergence is known rather than latent. Certified against `ledger_fee_scenarios` read in place (04 §5), with **doc 03 §5.3's own fee column parsed** for the charged/exempt verdict — a third artifact, so no two of the three can agree by construction — and the protocol-account exemption proven non-vacuous. **V-174: the corpus refuted my own first assertion.** I asserted `fee(a)` always overstates, which is what §5.3a(2a)'s worked example shows; it *understates* at `a = 1,000,000` (3,000 against 3,001), because two independent per-leg ceilings can exceed one — and that is the direction a client must fear, since a fee below the chain's shows a net above what the account receives. `src/features/tx/ledger-domain.ts` is §11.2a rules 1/2/5: the domain bit test against a **required** `ServiceIdBase` argument, the service call subset derived from 16 §7.6's rule and compared against doc 11's S4 list both ways, and rule 2 made a type (`NoInfer`) so a cross-domain total does not compile — proven by a `tests/firewall` fixture, not by a green run. Gates: **1,173 app tests across 18 suites, 0 fail**; build, types, depcruise 0 errors + witness, firewall 24, casts, chain-literals, render-provenance + witness, surface/foreign/smoldot all green; control-character sweep clean.

**What comes next:** Merge #249 when its five pending jobs settle, then F7b's VOID decomposition (§11.6), the S3 ticket, S4's two-domain reads, S20 and S2's dashboard

## F7b (opened) · V-174

**F7b is the only ⬜ Track F milestone with satisfied dependencies and no external blocker, so it is next — and its first two pieces are the ones every S3/S4 screen sits on.** `packages/protocol/redemption.ts` ports 03 §5.3a: the waiver tests the **net** rather than the gross (a gross test disagrees on ordinary traffic — at 30 bps the runtime pays a gross of 10,000 in full while a gross-based client shows a 30 fee and a 9,970 net), a pair charges what its legs would charge and not `fee(a)`, and the fee rounds up against the claimant. It **refuses** an out-of-domain rate where the runtime waives it, because 03 §5.3a(5) fails open and 11 §11.5 rule 5 requires a client to do the opposite; the corpus row at `1_000_000_001` perbill asserts both sides so the divergence is known rather than latent. Certified against `ledger_fee_scenarios` read in place (04 §5), with **doc 03 §5.3's own fee column parsed** for the charged/exempt verdict — a third artifact, so no two of the three can agree by construction — and the protocol-account exemption proven non-vacuous. **V-174: the corpus refuted my own first assertion.** I asserted `fee(a)` always overstates, which is what §5.3a(2a)'s worked example shows; it *understates* at `a = 1,000,000` (3,000 against 3,001), because two independent per-leg ceilings can exceed one — and that is the direction a client must fear, since a fee below the chain's shows a net above what the account receives. `src/features/tx/ledger-domain.ts` is §11.2a rules 1/2/5: the domain bit test against a **required** `ServiceIdBase` argument, the service call subset derived from 16 §7.6's rule and compared against doc 11's S4 list both ways, and rule 2 made a type (`NoInfer`) so a cross-domain total does not compile — proven by a `tests/firewall` fixture, not by a green run. **§11.6's VOID decomposition landed in the same session**: consolidate → pair → redeem, in the only order that reaches the maximum (both inequalities asserted, not argued), with SQ-171 split into the *action* and the *copy* — `parCopyPermitted` requires **no residue**, since a portfolio of one unit of pair and ninety-nine of residue recovers far under par. No fee on that path, asserted by absence. Gates: **1,186 app tests across 18 suites, 0 fail**; build, types, depcruise 0 errors + witness, firewall 24, casts, chain-literals, render-provenance + witness, surface/foreign/smoldot all green; control-character sweep clean.

**What comes next:** Merge #249 when its five pending jobs settle, then F7b's VOID decomposition (§11.6), the S3 ticket, S4's two-domain reads, S20 and S2's dashboard

## F7b (opened) · V-174

**F7b is the only ⬜ Track F milestone with satisfied dependencies and no external blocker, so it is next — and its first two pieces are the ones every S3/S4 screen sits on.** `packages/protocol/redemption.ts` ports 03 §5.3a: the waiver tests the **net** rather than the gross (a gross test disagrees on ordinary traffic — at 30 bps the runtime pays a gross of 10,000 in full while a gross-based client shows a 30 fee and a 9,970 net), a pair charges what its legs would charge and not `fee(a)`, and the fee rounds up against the claimant. It **refuses** an out-of-domain rate where the runtime waives it, because 03 §5.3a(5) fails open and 11 §11.5 rule 5 requires a client to do the opposite; the corpus row at `1_000_000_001` perbill asserts both sides so the divergence is known rather than latent. Certified against `ledger_fee_scenarios` read in place (04 §5), with **doc 03 §5.3's own fee column parsed** for the charged/exempt verdict — a third artifact, so no two of the three can agree by construction — and the protocol-account exemption proven non-vacuous. **V-174: the corpus refuted my own first assertion.** I asserted `fee(a)` always overstates, which is what §5.3a(2a)'s worked example shows; it *understates* at `a = 1,000,000` (3,000 against 3,001), because two independent per-leg ceilings can exceed one — and that is the direction a client must fear, since a fee below the chain's shows a net above what the account receives. `src/features/tx/ledger-domain.ts` is §11.2a rules 1/2/5: the domain bit test against a **required** `ServiceIdBase` argument, the service call subset derived from 16 §7.6's rule and compared against doc 11's S4 list both ways, and rule 2 made a type (`NoInfer`) so a cross-domain total does not compile — proven by a `tests/firewall` fixture, not by a green run. **§11.6's VOID decomposition landed in the same session**: consolidate → pair → redeem, in the only order that reaches the maximum (both inequalities asserted, not argued), with SQ-171 split into the *action* and the *copy* — `parCopyPermitted` requires **no residue**, since a portfolio of one unit of pair and ninety-nine of residue recovers far under par. No fee on that path, asserted by absence. **§11.5's P-1/P-2 rows landed with them** (`trade-ticket.ts`): the fee cross-check over 02 §9 rule 4's two published forms, FE-CHAIN-005 as an **exact** quote agreement rather than a tolerance, a balance row against the **charge** rather than the amount, and P-2's Baseline rows failing closed when unread. Gates: **1,202 app tests across 18 suites, 0 fail**; build, types, depcruise 0 errors + witness, firewall 24, casts, chain-literals, render-provenance + witness, surface/foreign/smoldot all green; control-character sweep clean.

**What comes next:** Merge #249 when its five pending jobs settle, then F7b's VOID decomposition (§11.6), the S3 ticket, S4's two-domain reads, S20 and S2's dashboard

## V-173 (SS58)

**#248 corrected five `app/` comments stating this chain's SS58 prefix as 22622; two survivors outranked all five.** `.claude/rules/app-code.md` is loaded into every session that opens `app/**`, and PLAN.md's **V-164 row** — the findings table a later session trusts for measured facts — stated 22622 twice while quoting an address that is the 7777 encoding. Measured before correcting rather than trusted: `AccountId(7777).dec(aliceKey)` is exactly the address V-164 quotes, and 22622 renders the same key as a string sharing nothing with it. Both corrections say inline that the number moved, because a log that quietly rewrites its own measurements is worth less than one that shows the correction; the session-log rows and the superseded Current-focus block keep their original wording as history. **A CI gate was scoped and rejected on measurement**: over every `.md`/`.ts`/`.rs`/`.py` in the repository, "a number stated near ss58/prefix" is 42 real hits against 200+ false positives, because `prefix` here almost always means a storage, key, byte or hash prefix — the noise level that gets a gate switched off rather than fixed. The residual is named instead: a frozen 02 §8 value restated in prose has no mechanical reader.

**What comes next:** Rebased #249 onto post-#248 `main` in the same session

## F7b (review fixes)

**Three P1 review findings on #250, all confirmed, all the same defect class — a precondition row that passes while measuring nothing.** All three sat in `trade-ticket.ts`, 11 §11.5's P-1/P-2 rows. **(1) The inputs were `Verified<T>`**, which admits `provider`, `verified-best`, `stale-cache` and `external-proposal`, so a ticket assembled from an operator snapshot returned an **empty** block list — read by `mayPrepareTrade` as *every precondition passed*. Fixed at the type level, not with a guard: the leaves are `Finalized<T>`, the control `transaction-builder`'s `evaluate` already used, with a `tests/firewall` fixture proving a provider read does not compile. A type carries a block but cannot compare two, so **one pin across every leaf is a row** (§11.4 pins one B′; rows from two blocks are each true about a state that never existed). **(2) P-1's slippage recheck had nothing to check against** — `TradeInputs` carried neither the direction nor the encoded `max_cost`/`min_proceeds`, so a buy drafted at 1,002,000 against a refreshed charge of 1,003,000 passed every row and came back `SlippageExceeded` (04 §6.1 step 4). `TradeOrder` is now a union, so a ticket with no bound at all does not typecheck. **(3) The balance row was a buy's**, applied to both directions: P-1 reads *"user USDC balance (buy) / position balance (sell)"*, and comparing a seller's positions against USDC proceeds passed a user holding 900,000 units of a 1,000,000 sale whenever the sale paid less than they held. Two consequences beyond the findings: `QuoteView`'s `cost` and `fee` are carried apart and compared field by field (one sum hides two offsetting differences, and which one matters depends on the direction), and `orderTotal` is the single derivation of `cost + fee` / `cost − fee`. **No spec change and no new spec question**: P-1 already mandates both missing checks, so SQ-700/701/702 were reserved and are unused — the reading is in the Decision log. **7 mutations, 0 survived**; the first two were caught by `tsc` rather than by a suite and were re-run as compiling mutants, because a compiler catch does not prove a new test measures anything. **GitHub Actions was in a major outage, so every gate was run locally:** `pnpm test` exit 0 — **1,210 tests across 18 suites, 0 fail** (firewall 25, screens 257, tx 140), `depcruise` 0 errors, PLAN tables green, control-character sweep clean.

**What comes next:** Reply on the three PR threads, then F7b's render layer and its reads — S4's position reads across both domains, S20 balances, S2's finalized decision dashboard

## F7b (S5–S8) · V-185

**F7b's four core screens plus the S2 read that was declared and never performed.** `decision_stats` was named in `PROPOSAL_READS`, decoded by `ProposalDecoders` and ruled on by `viewFor` — and no call site invoked it, so `ProposalView`'s `decided` arm was constructible only by hand and `ProposalDetail`'s dashboard was a panel no read could fill. Every consequence was silent, because the tests exercising those paths built their own fixtures. `readProposals` now fetches it, and **which** proposals it asks about is the load-bearing choice: asking only about sealed ones is the obvious implementation and it makes `viewFor`'s anomaly branch unreachable from a real read, since a `Some` on a `Trading` proposal is exactly the contradiction that branch reports. The open-trading states are therefore asked too; the pre-market ones are not, because there the question is meaningless rather than merely `None`. **S5 (P-10/P-11)** keys the rate limit and the bond to the **funder** through a branded `FunderReads` whose subject is re-checked — an author-keyed count passes a submission the runtime refuses — and P-10's per-funder count is **unreadable through any frozen surface** (`Epoch.IntakeQueue` holds ids only per 05 §7(6), `IntakeProposals` is internal, `proposal_summaries()` is the post-qualification set), so it is stated as an uncheckable condition on the clean path rather than passed or blocked. P-11 admits proposer **or** funder, tested from both sides. **S6** is fourteen rows with `DeadManInputs` carrying no `expedited` field at all, so the D-9 exemption is out of scope for row 12 rather than ignored by it; row 13's waiver is over the conjunction; a pre-grace `NotRatified` blocks and is **not** terminal. **S7**'s `ActiveSpec` union has no version field on its unavailable arm, and the gate sampling fact comes from `GateBreachFlags` **entry presence**, since an unsampled epoch and a clean one carry identical bytes (05 §4.7). **S8**'s voided `CohortRow` arm has no `s1e9` while keeping its members' outcomes. **V-185: `CoreKeys` shipped three storage-key functions nobody called** — the prefixes paired with a runtime-API result are named to `crossCheckedCall` and resolved by the reader, so an injected port for them could disagree with the prefix the cross-check used and no test could see it. They are removed, and a counting `CoreKeys` now asserts every remaining member is reached. **58 mutations, 0 survivors** (57 killed by the suite, 1 by the type system — the `ActiveSpec` union will not compile a version render on the unavailable arm); **one hole was found in my own suite rather than in the code**: an undecodable `epoch_status` substituting `Intake` for the phase survived, because nothing distinguished an unread phase from a read one, and a substituted `Intake` turns a failed read into a passing precondition. Gates: **1,255 app tests across 18 suites, 0 fail**, plus build, `check:types`, depcruise (0 errors), firewall 24, casts, chain-literals, render-provenance, above-fold, no-html-sinks; `check-plan-tables.py`, `check-dispatch-mirror.py` (11 backend ↔ 14 frontend rows) and `check-client-surface-obligations.py` green; control-character sweep clean.

**What comes next:** S3, S4 and S20 remain with their own agents; `implementedScreens()` stays unwired until a live transport exists

## F7b (#258 review) · V-186…V-189 · SQ-790/791

**Five review findings on #258, all confirmed, and two of them decided by the pallet rather than by reading.** **(1)+(2) `SubmitInputs` and `ExecuteInputs` accepted `Verified<T>`** — #250's defect at S5 and then at the execution gate, which R-7 puts in the strictest class. Every leaf is `Finalized<T>`, **including the nested capability, suspension, lock, retry and bounds structures**, with two `tests/firewall` fixtures (one nesting the provider leaf deliberately) and §11.4's single-B′ rule as a row in both gates — `P-10 read pin`, `P-11 read pin`, and S6's row **0**, outside the fourteen because it describes this client's reads rather than a dispatch check. **(3) The execution deadline is not `grace_end` once a mandate has failed** (`lib.rs:1956`): `failed_at + RETRY_WINDOW` **replaces** it, non-monotonically, so the client was wrong in both directions — the reported lock-out, and the unreported one where an expired retry read as live. 09 §1.2(1) said neither and contradicted 05 §2.1's T18/T23, so it is corrected under R-1 with 11 §11.5 row 2 and a first 13 §2 home for the value. `failed_at` is readable; `RETRY_WINDOW` is not (**SQ-790**), so that state blocks and explicitly does not declare the mandate over. **(4) Four reason codes were names the runtime has never returned, and three more were real names on the wrong check** — `check-execute-error-codes.py` binds the union to the error enum, a suite test binds every emitted code to `do_execute`'s body, and a per-arm table binds the mapping. **(5) S7 read raw `Constitution.Params` for three fields the stored record does not hold**; it reads `params()` cross-checked against that prefix now. **Found here and not reported: the `stamp()` producer mint in the same file** — invisible precisely because the consumers were weak, and a type error now (V-189). `derive` is ported byte-identical from `track-f/f7b-render`. **15 compiling mutants, 0 survived**, plus 5 in the new gate's unit tests; 2 mutations rejected as non-compiling, one of which is itself the finding. GitHub Actions in a major outage, so all evidence is local: app **1,273 tests / 18 suites / 0 fail**, `pnpm test` exit 0; `tools/ci` **251 tests, 0 fail**; plan-tables, batches, dispatch-mirror, surface-obligations, verbatim-copies and the new code gate all green.

**What comes next:** Reply to the five #258 threads, push, and hand `track-f/f7b-render` back for its rebase (it must reconcile the duplicate `derive` to one definition). **SQ-790/791 stay open** — both are 02 additions that must not ride inside a review fix.

## F7b (S3 · S4 · S20)

**The three Advanced screens and their read layers landed, and the interesting work was making doc 11's prohibitions unreachable rather than remembered.** A reaped Baseline book answers every read with nothing, and nothing formats as `0.00` — which on a trading screen reads as *worth zero* rather than *gone* — so the `reaped` arm carries no book, no quote and no inputs at all. Every other mapping-and-book combination is **corrupt**, a hard block, and labelling it reaped would hide one. §11.2's in-Trade preview ban is a type with no arm for a statistic, §11.6's *no winning branch* is two arms with no `winner` field, §11.5 rule 1's exempt payout is an arm with no `fee` and no `net`, and §11.2a rule 2's merged total has nowhere to live. The FE-P2 cross-check pairs each view with **its own domain's prefix**, derived from the domain so no call site can cross them. **Writing the tests found two defects in the draft.** An empty-but-read book reported *not available* instead of a verified zero, because `combine(x, [])` refuses — the honest arm where nothing was read, and a false one where the domain answered with no rows. And S20 gave the native and USDC figures the same label, in the one screen where the two units differ by six orders of magnitude. Both fixed and both mutation-proven. Bindings are parsed, never restated: 02 §7.3's bit table by **name**, 02 §7.4's `NOT Assets.Account(1337, who)` prohibition, 11 §11.2's S4 and S20 rows, §11.2a rules 2 and 3, §11.5 rule 3, §11.6 steps 2 and 3, E6 and E16 — plus every chain constant from `fixtures/chainhead/`. Three suites, 74 tests, **28 mutations and 0 survivors**. `test:screens` 340; full app suite **1,292 tests across 18 suites, 0 fail**; build, types, firewall 24, depcruise 0 errors, render-provenance, above-fold, chain-literals, casts, no-html-sinks all green; control-character sweep clean. Not wired to a transport, and no screen is registered in `implementedScreens()`: no chain-spec bytes are committed for any chain, so every built screen in this client stays `built-unwired`.

**What comes next:** S2's finalized decision dashboard, then the composition roots that build keys and decoders for the S3, S4 and S20 readers

## F7b (S3 render) · V-182

**Repaired `track-f/f7b-render` after its rebase onto #250's fixed tip, and the ten type errors were the symptom rather than the defect.** Git reported no conflict because the two sides changed different files; `tsc` caught what it could not. Under the errors: `market-reads.ts` held a local `finalized` helper wrapping any value in a hand-written finalized status object. The brand is a non-exported `unique symbol` in `packages/chain-client`, so the helper produced a plain `Verified<T>` and the render layer had been **asserting** finality for values it never proved final — **nine** of them. Two were caller-supplied inputs the chain was never asked about (the book id, the epoch), and two took the **payload** of a caller-supplied `Verified<TradeBounds>` while discarding its status, so a `provider` read went in and a finalized-looking `MinTrade` came out on a value 11 §11.5 P-1 reads. No existing gate could see it: `check:casts` wants an assertion, the render gate's rule B wants a borrowed `.status` access, and neither shape was present. **The root cause was a missing spelling.** 10 §2.2 admits values computed purely from finalized reads, and the barrel exported that for two reads (`meet`) and none for one — so a reader needing a `Finalized<boolean>` out of a `Finalized<readonly StorageItem[]>` wrote its own status. `derive` closes it and widens nothing (`meet(a, a, …)` is the same function, asserted). Every leaf is now taken from the transport, decoded **inside** the derivation rather than stamped after it; `maxTrade` goes through `meet` as the one genuine two-read leaf; caller-supplied readings are `Finalized<T>` and are pin-checked up front (`MixedPinError` **throws** — a client defect, not a chain state); and the two rendered inputs pass through under the caller's own provenance. **The screen changed too.** 02 §4's `cost` and `fee` render **apart on both sides** with 04 §6.1's per-direction combination beneath them, because a chain `(100, 3)` and a client `(101, 2)` agree on a buy's total and disagree on a sell's net — one figure per side would show agreement for a pair that blocks under `FE-CHAIN-005`. Labels are keyed by direction, since *"total debited"* over a payout reverses the sign of the trade. **6 mutations, 0 survived, each made to compile first**: the mint re-added behind an `as Finalized<T>` (caught by `check:casts` and by a new source assertion in `tests/screens`), the book id re-stamped, the epoch re-stamped, the quote normalised to one figure (7 tests died), the direction defaulted in two places, and `derive` shifting the pin it carries. Gates all local — GitHub Actions is in a major outage: `pnpm test` **exit 0, 1,363 tests across 18 suites, 0 fail**; `depcruise` 0 errors with 2 pre-existing `no-orphans` warnings and its witness firing on 20 edges; `check-plan-tables.py` green; control-character sweep clean. No spec question raised — SQ-740…SQ-744 unused.

**What comes next:** The branch is committed and **not pushed**; its integration is the caller's. Then S2's finalized decision dashboard and the composition roots for the S3/S4/S20 readers

## F7b (S4 · S20 provenance) · V-183 · V-184 · SQ-830

**The two files `audit/provenance-mints` could not reach, and one of them was lying to the user.** #260 swept `main` for helpers that manufacture a `verified-finalized` status rather than obtain one, fixed six sites and added a gate; three files were out of its reach on this stack, and #258 had already closed the third. `position-reads.ts` (V-184) was the benign half: seven leaves that really were part of what `crossCheckedCall` returned, badged from `reader.at` rather than from the answer, with the stamping helper passed **into** `projectVault` as an argument — right in substance, wrong in the direction of the dependency, and structurally indistinguishable from `market-reads.ts`, which was wrong in substance too. `balance-reads.ts` (V-183) was not benign. It badged `who` — the **key** its two frozen maps are read at — `verified-finalized` at the reader's pin, and S20 renders that string through `<Identifier>` in the panel subject, so a finalized badge sat beside a value the chain was never asked for. 10 §5 forecloses it by name: *"a selected account"* is a chosen value and *"can never be represented as verified"*. It is now the caller's `Verified<string>`, passed through. Its second defect was the one #260 had already ruled on elsewhere: an undecodable balance contributed a badged `0n`, defended by a comment arguing the `undecodable` row said not to believe it — but INV-FE-9 attaches the label to the datum, so an explanation in a sibling array never reaches the field. The figure is absent now and the screen renders a refusal; the suite's own test was already called *"no substituted figure"* and asserted only the raw bytes. **9 mutations, 0 survived**, one re-run because `noUnusedLocals` caught the first form. #260's gate fetched and run unmodified: **7 findings before, 5 after**, both of this branch's gone and the five remaining all `main`'s, each fixed by #260. Gates local (GitHub Actions outage): full `pnpm test` exit 0 — **1,378 tests across 18 suites**, `test:screens` 420 — plus depcruise 0 errors, `check-plan-tables`, `check-spec-question-batches`, and a control-character sweep clean.

**What comes next:** F7b — S2's finalized decision dashboard, then the composition roots for the three Advanced readers; SQ-830 needs a ruling before any of them renders a chosen account

## F22

**The desktop shell, and a gate that was green while measuring nothing.** F22's whole point is that a downloaded build embeds the *published* tree, so the assertion is made **twice, in two languages** — `app/tools/desktop/` on the build machine, `app/crates/embedded-tree/` inside the binary before it creates a window — and the two are bound by a **corpus read in place** (`crates/embedded-tree/fixtures/self-check-cases.json`), which publishes file **content** rather than digests so neither side's hash function goes unexercised. The comparison is `packages/verify`'s own `runSelfCheck`, reused rather than reimplemented; the only change it needed was a narrower parameter, because a per-commit build has no Arweave address and `parseReleaseDocument` correctly refuses that for reasons unrelated to whether the map describes the tree. **The defect worth carrying forward is my own.** The first composition ran 12 §1.2's `assertPublishable` *before* the self-check — and that function compares digests too and **throws on the first disagreement**, so `runSelfCheck` was unreachable code whose findings array was empty in every run that reached it. The witness passed anyway, on all three mutations. It was found by deleting the comparison and re-running: green. The order is now self-check first, the publishability gate runs only over a tree that already matched, and the witness requires an **enumerated finding of the right kind** rather than "it threw or it reported something" — a throw is now a witness *failure*, because it means the wrong leg decided. Re-measured with the self-check neutered: red, naming the leg. **`packages/platform` imports no host SDK at all**, which is a decision: the permission 10 §10.1 grants would force host code into the web bundle or a second build path, and the second is what the milestone forbids. The consequence is that `only-platform-touches-host-sdks` has no real edge in the tree and the depcruise witness is its entire liveness proof — both spellings, since a typo in either arm of the alternation leaves half the rule dead. The cruise path gained `src-tauri` and `tools/desktop`, since a rule that cannot see the one directory whose job is talking to a host is a rule about nothing. **Verified against the real binary, not asserted:** happy path prints `release verified: 30/30`; a file added, a file altered and a file deleted each exit **70** before any window exists; and removing `release-out/` fails the *build* with the message naming `release:build`, which is the CI negative control for the claim that the shell cannot be built without the attested tree. Tauri 2.11.5 builds on this workstation and in CI (webkit2gtk-4.1 present locally; the new `desktop-shell` job installs it). Gates: 1,179 app tests green including the 30 new ones, `check:embedded-tree` + witness, root workspace `cargo metadata --locked` unchanged. **`check:casts` caught an `as unknown as` in my own new test** and the repair was better than the cast — the hand-written lattice needs no assertion to typecheck, which is precisely the hole the test exists to demonstrate.

**What comes next:** Phase 1's last open row is **F11** (🔨 on this base — F22 depended on it and consumed its output, `release-out/release.json`, without needing it closed). Track F on this base: 14 ✅, 7 🔨, 2 ⬜

## F22 (spec review)

**`spec-reviewer` returned 0 blockers and 3 majors, and two of the majors were fail-open defects I had written.** The binary's `None`-asset branch substituted **empty bytes**, and `sha256("")` is a real digest — so a legitimately zero-byte pinned file on an unserveable key would have *verified*. It is now a refusal, and it moved into the pure crate, which is the more important half: `Context<Wry>` cannot be constructed in a unit test, so anything decided in the shell is decided in code no suite executes, and `hash_assets` takes `Option<&[u8]>` precisely so the trap is testable. `lattice`/`meet` **defaulted an unrecognised capability state to `proven`** — the one direction a fail-closed lattice must never default to, and those functions exist because the type is not enough, so they cannot then trust the type. The third major is real and **cannot be closed today**: `build.rs` compiles in whatever `release-out/release.json` sits on the build machine, unverified, so the desktop check proves internal consistency rather than membership of the published release — and 12 §1.4's control against exactly this is that CI holds no minisign keys. The keyring is empty pre-ceremony (0 against §1.4's floor of two), so there are no signatures to verify; it becomes blocking at the first download link and F13 owns the verifier. Also fixed: the refusal was **free text with no code**, which 10 §9 forbids in as many words and for which 14 TH-38 already assigns **`FE-REL-002`** — this shell is that family's first consumer anywhere in the repository; `unknownPlatform` claimed `channel: 'web'`, which tells a possibly-native host that the service worker is checking its bytes; Tauri moved to `=`-exact pins because `tauri-codegen` decides what gets embedded; and INV-FE-12 was miscredited as the lattice's owning invariant when its subject is the *runtime* surface and app-code rule 10 is the actual rule. Gates after the fixes: **1,180 app tests**, app-workspace fmt/clippy/test/build, `check:embedded-tree` + witness, `tools/ci/tests` 206, root `cargo metadata --locked` unchanged.

**What comes next:** **Six spec questions the review surfaced, none resolved and none opened as SQ rows here** — each asks what the doc set should say about a distribution channel it does not currently describe, and opening them touches the batch index six concurrent branches also edit: (i) `docs/architecture/` specifies **no native or downloadable channel at all** — nothing states what a downloadable artifact must carry (signature, SBOM, reproducibility gate, per-artifact hash publication) or which INV-FE obligations bind it; (ii) what INV-FE-8's *"surfaced to the user"* means when detection precedes any UI, given 14 TH-38 prescribes a red banner a non-rendering shell cannot draw; (iii) whether 15 §4.5's supply-chain gates, scoped to *"every backend CI run"*, extend to a client workspace that now produces a distributed binary, and whether 12 §1.1's SBOM covers it; (iv) whether the Tauri stack is inside 01 §9's exact-pin regime, which names no Tauri version; (v) INV-FE-11's panel source on a channel whose `release.json` is a sibling transaction by construction; (vi) whether 10 §10.1's host-SDK clause is a permission or presupposes a JavaScript host binding

## F22 (bot review, PR #252)

**One P2 from `chatgpt-codex-connector`, confirmed: the desktop adapter claimed `proven` for the capability the branch beside it had just disproved.** `desktopPlatform` returns an adapter on a *divergent* embedded tree rather than throwing, and that is right — INV-FE-8 surfaces divergence and never repairs it, and a thrown adapter is a divergence nobody can render. What was wrong is what the returned adapter claimed: `'embedded-tree-attestation': proven()` sat three lines under the divergence check, so every surface guarded by `requireCapabilities(..., ['embedded-tree-attestation'])` was enabled in exactly the state the host had reported as failed. App-code rule 10 says the opposite in as many words: an unproven capability is **absent**, and absence disables the dependent surface with a named reason. **The repair is a derivation rather than a corrected literal**, because a literal beside a check can fall out of step with it and a value computed from the check cannot: `embeddedTreeCapability` is a total `switch` over `AttestationState`, all three constructors take their value from it, and a fourth arm now fails to compile rather than inheriting whichever branch was written first. The absent reason names the divergence, keeps the count exact and caps the list at three paths, since the full list belongs on `PlatformAdapter.attestation` where a panel renders it. **Two siblings of the same shape were in the same function.** `external-navigation` was `proven()` on the desktop channel — the web adapter's argument copied to a channel it does not cover, since that argument is about *browsers* while a downloaded application opens an external URL only where its host grants it, and this shell registers no host plugin and ships an empty `capabilities.json`. It offered a control that would do nothing when clicked, and it is a reported `HostReport` field now. And a `reported-verified` attestation over **zero pinned files** was believed, the exact mirror of the empty-findings refusal beside it: no honest release reaches it (`readPerFileHashes` and the Rust `attest` both refuse an empty manifest), so it means the report is wrong, and it is refused. **Mutation-proven one revert at a time, rebuilding first because a stale `dist/` has already made a suite pass for the wrong reason**: the literal fails 3 of the new tests, the deleted zero-pin refusal 1, the copied `external-navigation` 1. **All local, because GitHub Actions is in a major outage**: `build`, `depcruise`, `depcruise:witness`, `descriptors:check`, `check-chain-feed.py`, the aggregate `pnpm test` at **1,185 tests and 0 failures**, `release:build`, `release:check`, `check:bundle-budget`, `check:embedded-tree` 30/30, its witness firing on every mutation, and `signers:audit`.

**What comes next:** No spec question opened, and the reason is checked rather than assumed: the capability set is F22's own with no `docs/architecture/` citation, app-code rule 10 owns the fail-closed posture, and 10 §13.4 already routes external-link availability to prototype gate FE-P11. F22's five named residuals are untouched — the largest stays `build.rs` compiling in an unverified `release.json`, which cannot close until the signing ceremony exists

## F14 (measurement half)

**§9.4's twelve unmeasured budget rows were triaged into three buckets and every measurable one is now gated.** Already enforced: initial JS and smoldot — re-verified as genuinely wired into CI *and* bound to their published cells, not rebuilt. Newly measurable and now built: the release-shipped fallback-metadata size (over the committed per-`spec_version` blobs an FE-P5 fallback would carry, against **both** §9.4's 1.5 MB and §9.3's 8-blob count), the chain-spec size (no spec is declared, so the gate instead requires the readiness blocker to stand and **fails the moment a hash is pinned with nothing to weigh** — an exemption that expires by itself), and **first meaningful render** under Lighthouse 12.8.2 on §9.4's own reference profiles. Three findings drove the design rather than decorating it: the audit §9.4's row is *named* after produces no numeric value at all, Lighthouse's desktop preset is 1× where §9.4 asks for 4×, and the gate leaked a Chrome process on every refusal (V-179, V-181). Genuinely blocked and **not faked**: eight rows, four needing a chain the client cannot boot against (no chain-spec hash, no genesis, no para id; the recorded transcripts are one pinned block, so there is no second head), one needing the §9.2 quota manager that has no module, three needing the physical device lab. Gates: `pnpm test` green, `tsc -b` green, both new gates and both witnesses green, 262 tooling tests, 59 §9 cells re-derived.

**What comes next:** F14 stays 🔨 — five of thirteen §9.4 rows enforced; the remaining eight resume when a testnet, the quota manager or a device lab exists

## F14 (#254 review round)

**Both Codex P1s on #254 are valid, and both are this milestone's own defect class: a gate measuring something other than what it claims.** Neither was reachable by a threshold binding — `check-frontend-budgets.py` proved both gates carried their published cells, which they did. **Finding A (render budget):** §9.4 publishes p50 and p95 and the gate compared a three-run **median** against both, so samples of 2 s, 2 s and 20 s pass a 6 s p95 threshold. Fixed by gating the sample's **nearest-rank p95 tail** against p95 and its median against p50, in a Chrome-free `statistic.ts` so the comparison is testable on samples a real run cannot produce. The tail is the slowest run for every sample of **nineteen or fewer** — not twenty, which the new test caught, since ⌈0.95·n⌉ = n only while 0.05·n < 1. **Finding B (artifact budget):** the chain-spec row budgets what a browser lazily fetches and the gate gzipped **source paths** under `REPO_ROOT`; `connect-src.ts` opens those for bootnode multiaddrs and `build.ts` copied only `schemas/` and `skills/`, so **the release genuinely emits no chain-spec bytes and no client path fetches one**. Both halves fixed: `build.ts` copies declared specs into `dist/chain-specs/` **before hashing** (so the service worker pins them, the `copyProducerAssets` argument), and the gate measures the emitted tree — declaration ⇔ emission in both directions, every pinned role matched by **SHA-256** (the client's own boot check), and the pre-genesis exemption now requires the emitted tree to be empty as well as the blocker to stand. **Raised SQ-720** while binding the roles: §9.4 budgets relay + para + Asset Hub and `chainSpecHashes` pins two, though 11 §11.9.1 boots a light client against Asset Hub and verifies its bundled bytes — filed in X with a mechanically expiring readiness blocker. New `app/tests/budgets` (**18 tests**), **mutation-proved**: five separate reverts, each failing exactly the case written for it. Artifact witness 5 → **8** cases plus a positive control. `check:artifact-budget` moved into the release-build CI step, since it now needs `dist/`. **Gates, all local — GitHub Actions is in a major outage:** `tsc -b` green, app suites **1,167 tests / 0 failures**, artifact budget + 8-case witness green, render budget measured **0.40 s desktop / 1.80 s mobile** FCP at the tail with its witness firing, `tools/ci` **264 OK**, frontend-budgets **60 cells**, doc links / PLAN tables / batches / verbatim / dispatch mirror / surface obligations green. Also dropped AGENTS.md's *"26 gates"* count for `app/` — the catalogue was already 28 bullets when that figure was written the same day, and it is a number nothing checks in the one file every session loads.

**What comes next:** Reply to both #254 comments; F14 stays 🔨 on the same eight device-, chain- and quota-manager-blocked rows

## F8 · SQ-760…SQ-765

**The second R-6 re-review of F8 (0 blockers, 6 majors, 9 minors) closed, and its main finding was a ruling implemented halfway.** 10 §9.1 says chain-wide `Traded` *"is consumed into the candle aggregates as it is scanned and never stored row-by-row; a chain-wide trade tape is a bounded windowed read, never a retained table"*. The branch had built the retention filter and neither the aggregation nor the windowed read, so **nothing anywhere wrote a candle row** — §9.2's candle-depth tables described a tier with no producer, and the largest stream on the chain (1,339,200 rows/day at the pinned ceiling, ~6.3× the observation stream) was scanned, filtered out and forgotten. Both halves now exist and both land in the ingest transaction that advances coverage, so a bar can never summarise a block the index does not claim. Three of the section's four unspecified dimensions were **derived rather than chosen** (the table follows from §9.2's ladder order, the provenance from §7's every-row rule, the resolution from the table) and the fourth — which price series — is SQ-762, shipped folding `p_after` alone because merging it with the κ-slew `o_t` would publish a bar describing neither. The four correctness majors shared one shape: a label whose property was absent. The `${block}:raw` row was never retired, so a decoded block was stored twice and the pending count could only rise; the same blob was chain-wide retention with no rung to reach it; `bulkPut` replaced a coarse bucket rather than merging it, so whole-bucket folding held within a pass and not across one; and the quota manager re-read the whole table per bucket while the suite's row sizes made a handful of rows exceed a 300 MB share, which is why nothing saw it. **Codex's six findings on #255 were triaged in the same run** — five confirmed and fixed (committed conflict markers in the derived design kit, an unversioned primary-key migration that would have made an existing database fail to *open* rather than drift, a progress guard that abandoned a rung on a bucket that legitimately freed nothing, compaction that could summarise two provenances under one badge, and the unretired raw row); the sixth was half-refuted, since the repair path retains foreign ranges but cannot mint a `self` one. **All local: GitHub Actions was in a major outage, so every gate was run here.** `pnpm test` exit 0 (1,237 tests), `check-plan-tables.py` / `check-spec-question-batches.py` / `check-verbatim-copies.py` green, and **32 of 32 mutants killed** across the aggregation, the tape bound, the raw-blob bound, the ladder's cursor, the compaction refusal, the genesis identity and the schema migration.

**What comes next:** F8 stays 🔨 pending the ✅ call: the four scoped Phase-1 rows (no production consumer calls `runIngest`/`applyQuota`/`checkIndexAtBoot`/`coveredQuery`) are unchanged and deliberate. Next: merge #255, then the F8 ✅ decision

## F8 · SQ-603…SQ-608

**F8's R-6 audit findings closed — 2 blockers, 10 majors, 4 minors — and then the review of *that* returned 18 more, of which 3 were blockers.** Both blocker rounds were the same defect class and it is the one this package exists to prevent: **a label that survives while the property under it is absent.** Round one: stored rows collapsed four `RangeOrigin` values into two and guessed the origin back as `operator`, so an opt-in indexer's row was persisted and would be badged as 10 §6.2's **protocol-funded** layer-2 data; and `priceSamples` and the three candle tables carried no origin at all, though 10 §2.3 names them as the surface whose *only* mitigation is mandatory labelling. Fixing the second exposed a third the audit had not seen — `foldCandles` refuses to merge two sources in memory and a table keyed `[bookId+openAt]` stores one on top of the other, so the label survives and the number under it is whichever source wrote last. The source is now part of the key of all four chart tables. **Round two's real blocker is the sharpest finding of the session**: candle→candle degradation added the **target** bucket width to a **source** row, so of the four hours in one 4-hour bucket only the first qualified; that bar was written from one hour and the next pass replaced it with one built from the other three — an hour of history destroyed, coverage still claiming the blocks, and the *downsampled* label still saying nothing is missing. Its companion came out of an accident worth recording: **my own mutation harness was running against the worktree while the reviewer read it**, which left one schema line mutated and produced two false blockers — but under that mutation the delete matched nothing, the share never fell, and `applyQuota` folded one bucket **forever**. §9.2 says running out of room is *\"a reported outcome\"*, so an unbounded loop is not an admissible failure mode for it; there is now a progress guard and a test that reaches it through a Dexie middleware that swallows deletes. The lesson generalises past this session: CLAUDE.md documents the concurrent-edit hazard for `workspace-write` Codex jobs, and a local mutation harness is the same hazard with no label on it — **never run one while anything else reads the tree**. Also closed: the §9.2 **quota manager**, which did not exist; `mergeDownsampled`, which tested adjacency and dropped whole finer ranges, deleting the label from blocks it never covered; `compactSettledEvents`, which deleted **every** event in a proposal's block span because `events` carries no proposal reference; `exhausted`, computed from two chart shares and blind to the events share and the hard cap; `addRange`, which accepted a foreign-chain range and left the *next* mutation to throw; coverage validation on the **read** path; §9.3's blob-count cap and pinned set; §6.3's per-range edge fields with `verifyRange`/`verifyRanges` and real callers, so `invalidateRange` stops being an export nobody calls; the `derived-local` badge, which counted gaps where §6.3 asks for the boundary set; and the four missing 15 §4.8 tests, of which the load-bearing one is the assertion that `no-range-minting-outside-ingest` **is reported** — driven over the **production** config, because `depcruise:witness` restates the rule under a `witness-` name and never exercises the real clause. **Doc 10 §2.1, §6.3, §7 and §9.2 amended under R-1** (Decision log; no 02 change, so no contract bump), the derived design kit regenerated, and **six spec questions filed** (SQ-603…SQ-608): an *\"ingested event subset\"* naming no set, `FE-IDX-002` declared and defined nowhere, three integrity checks the light client cannot perform at the depths they describe, a **persisted** database name with two readings, eight table names with no keys, and a downsampled label with no provenance. All 26 app gates green (`pnpm test` exit 0), 5 doc checkers green, **42 mutations run and 0 survived** — two of them killing test defects rather than code defects, which is the run paying for itself.

**What comes next:** **F8 is NOT ✅.** R-6 asks for a spec-compliance review that finds no blockers and the only review of this work found three; a re-review of the repaired state is owed, plus the wiring the last minor names — nothing in `app/src` yet calls `runIngest`, `applyQuota`, `checkIndexAtBoot` or `coveredQuery`, which is Phase 1's stated scope rather than an omission.

## F8 · SQ-557 fallout

**A rebase moved the spec under a finished branch, and nothing failed — which is the whole point of recording it.** F8's four commits were rebased onto post-#249 main, where **SQ-557 had just been ruled**. No file conflicted, every gate was green before and after, and three things this branch encodes had silently become false. **The one with teeth**: 10 §9.1 now rules the index retains *\"only events attributing to the user's watched accounts\"*, and `storeWriter` stored every event of every block — which §9.2 measures as spending the whole 15 % events share in **~6.7 h desktop / ~1.7 h mobile** at the chain-permitted `Traded` ceiling, after which the ladder would evict the user's own history in order to keep storing strangers' trades. Fixed in the loop, where the watched set already lives, with the row id keeping the event's index **in the scan** rather than in the retained list — number the retained list and adding a watched account renumbers every earlier row, so the replay writes a second copy of history beside the first. **The one worth generalising**: §9.3's caps were cut to §9.2's metadata share, so `METADATA_BOUND` held the superseded 16 / 6 MB and was right only because `platformBudget` takes the `min` — and the *test asserted the contradiction still existed*. A test that pins a defect passes for exactly as long as the defect lives, and this repository keeps finding that pattern in its own suites; here the defect was fixed by somebody else, on another branch, and the suite went on asserting it. **The third is prose rot**: §9.2 withdrew *\"not achievable within the caps\"* as false and deleted its unresolvable 13-parameters citation, and three code comments went on quoting the withdrawn sentence while a fourth called a closed question open. The lesson is narrow and worth keeping: **a rebase is a semantic merge of code against prose, and git can only see the prose.** Nothing here would have been caught by conflicts, by CI, or by re-running the suite — only by re-reading the owning sections after the base moved. SQ-603 is now half-answered by the same ruling and says so. `pnpm test` exit 0, **1,209 tests**, 0 fail; 3 new mutants for the retention rule, the retained-event numbering and the §9.3 bound, all killed.

**What comes next:** **F8 still 🔨** — unchanged: the re-review R-6 asks for is still owed, and it should now be run against the post-#249 text rather than the text this work was written against.

## F8 · SQ-780…SQ-783

**F8's third R-6 review returned 2 blockers, 5 majors and 5 minors; both blockers, the first two majors and one minor are fixed, and F8 stays 🔨 by instruction.** The blockers share a shape worth naming: **each was a defect the suite could not reach, in code the suite covers thoroughly at every size it uses.** (1) `measureDepth` computed each tier's extremes by spreading every row of the tier into `Math.min`/`Math.max`. V8 refuses a spread above ~125,390 arguments — measured here on the pinned `node v22.18.0`: 125,000 fine, 130,000 `RangeError: Maximum call stack size exceeded` — and `applyQuota` calls `measureDepth` on every pass, so the entire retention pass threw once any tier passed that size. At the 159-book registry maximum `candles1h` grows 3,816 rows/day and arrives in **33 days**, about a quarter of §9.2's own published 131-day desktop candle depth and well before the 500,000-row candle share triggers any relief; on mobile the 125,000-row share and the crash arrive together. The suite was blind for the same reason it had been blind to the whole-table read the previous round fixed — the DBCore seam test resets its counter before `applyQuota`, and `measureDepth` runs against a nearly-empty fixture. The extremes now come off the `at`/`openAt` indexes (`first()`/`last()`): 1 ms against 4.25 s at 130,000 rows, which also closes the unbounded read the function's own doc-comment argued against. The second site — the pending-raw eviction record's block envelope, reachable at ~225,000 blobs inside the 45 MB events share — folds instead. (2) The v1 → v3 migration emptied the four re-keyed chart tables while `meta.coverage` carried through unchanged, so afterwards `coveredSamples` answered a covered span with an empty array and a table stated *\"complete within [ranges]\"* over nothing, with no record, no error code and nothing in the boot report. **The loss is permitted and stays** — INV-FE-7 makes this storage disposable and the alternative is a database that fails to open, the one outcome INV-FE-7 says the client must survive — but INV-FE-15 requires the unverified to be *\"either absent with an explanation or present and labeled\"*, so the untrue claim is separable from the loss and one `meta.put` separates them. Dexie restores a version's deleted tables into its own upgrade transaction and queues the deletion after the content upgrade, so `version(2).upgrade` counts the rows **and** writes the record in the transaction that drops them. Majors 1 and 2 were both about a pass that reported nothing: a version-1 raw row carries no `pendingBlock`, so `pendingRawRows` refused **permanently** and `measureUsage` — the first call in `applyQuota` — threw on line one (now backfilled in `version(3).upgrade`); and three ordinary-data refusals threw straight out of the pass, so a pinned metadata set larger than its budget skipped the whole chart ladder and one mixed-provenance proposal skipped the §6.5 raw-blob bound (now caught per rung and reported as a `refused` step, with measurement moved off the sparse index so only the eviction can refuse). Three majors and four minors are **recorded, not repaired** — the largest is that §9.2's raw tier, which takes 60 % of the cap and is the ladder's first rung, **has no producer at all**: `Observed` is scanned and discarded, so the tier is permanently empty in production (SQ-782). `pnpm -C app test` exit 0 at **1,243 tests** (local-index 175 → 181); **10 mutants, 0 survived**, one of them only after its test was strengthened — the zero-row migration guard survived a fresh-database assertion because Dexie does not run upgraders when it creates a database from nothing, so the case is now exercised through a real version-1 upgrade with empty chart tables. GitHub Actions was in a major outage, so every gate here is local.

**What comes next:** F8 is **not** ✅: the parent agent owns that call. Next is the three recorded majors (SQ-781/SQ-782 need rulings; the `proposalsArchive` key is a chosen limitation) and the four standing minors.

## F8 · SQ-820, SQ-821 · F25

**F8's fourth R-6 review returned 1 blocker, 4 majors and 5 minors; the blocker and all four majors are closed, and F8 stays 🔨 by instruction.** **The blocker was the previous round's blocker surviving one layer down, and its shape is the one this package keeps producing: a true record on a channel nothing reads.** The migration writes a `ChartDiscardRecord` inside its own upgrade transaction and `checkIndexAtBoot` surfaces it — but `coveredQuery` reads `CoverageRef` and nothing else, so the history read still answered a covered span with an empty array, no hole and no explanation. Measured: `coveredSamples(db, 'book-1', 1..9)` → `{ data: [], ranges: [1..9 self], holes: [] }`. That is *complete over an emptied tier*, which no ruling can make true, so it was repaired rather than parked on SQ-780/SQ-783. **The repair is `CoveredHistory<T>`**: the store's query path returns §6.3's own `CoveredResult` **inside** a container that also carries the two channels recording rows removed from a covered span — `downsampled` and `chartDiscard` — each bounded by the span asked about, with a discard whose envelope could not be named reported for every span (overlap cannot be ruled out, and dropping the explanation on *cannot say* turns an announced loss back into a silent one). The published §6.3 interface is untouched, because **which** repair is right is the spec question the blocker turns on (SQ-821), and the old bare-rows shape no longer typechecks, so the sibling fields cannot be forgotten. **Major 1 was introduced by the previous round's own repair** — the per-rung `catch` that stopped one refusal ending the pass also let a later rung persist a phantom label, because `degradeOldestBucket` merged into the shared accumulator before opening its transaction. Now merged and written inside the `rw` callback and returned from it. **Major 2:** the 130,000-row regression fixture exercised `priceSamples`, the tier SQ-782 records as having **no producer**, while the crash arithmetic was about `candles1h` at a separate call site; both are seeded now, and the comment claiming the ladder ran is corrected with a measured reason — one 3,600-key `bulkDelete` against a 130,000-row table had not returned after **nine minutes** under `fake-indexeddb`. **Major 3** is a new milestone row, **F25**, owning the render of `IndexBootReport` with `ChartDiscardRecord` named as an explicit obligation: `local-index` has no production consumer, so no work inside F8 can give the disclosure a reader. **Major 4:** the withdrawn §7 citation was still in the code and in a thrown message, 600 lines from a comment stating the truth — both now cite `SCHEMA_V3` and SQ-607. Five minors closed: the producerless `'measure'` rung, the mixed-population row count, the hardcoded schema pair, and the two unbounded report lists. **Mutation found a gap the review did not:** the existing transaction test proves `writeDownsampled` joins an *ambient* transaction and never that the ladder's own write is in the *fold's*, so moving it after the `rw` block survived all 185 tests — closed by a test that refuses the label write and asserts the delete rolled back with it. **12 mutants, 0 survived** (three were rewritten because `tsc` caught the first form, and the harness itself had to be fixed: `shutil.move` restores the backup's mtime, so `tsc -b` skipped the rebuild and left a mutant compiled into `dist/`). `pnpm -C app test` exit 0 at **1,248 tests** (`test:local-index` 186). Two spec questions filed (SQ-820, SQ-821).

**What comes next:** **F25** — give the disclosure a reader; the `FE-IDX-002` copy binds when SQ-604/SQ-783 rule

## F9 · SQ-609…SQ-614

**An R-6 audit of F9 found 8 majors and 9 minors, and the common shape was that every §8 mechanism existed while the paths between them did not.** Four controls in §8.4's one quota sentence were implemented and none was wired: `admitChunk` had no production caller, `planImport` was bound to nothing, and `admitSnapshot` took a 400 MB file as one string, so *streamed* was a word rather than a property. `importSnapshotStream` now drives all four in the only order that makes each mean something — refuse at the chunk that crosses the byte bound **before** retaining it, count rows once the document parses, compare against the chain, then show the eviction preview as a **required** callback — and the mutation that moves `parts.push` one line earlier is caught. **The screen that was actively wrong was the chain binding.** `equalBinding`'s exact `specVersion`/`contractVersion` equality is 10 §13.1's rule for the three handoff formats, which describe one block; §8 mandates no binding and §6.4 assigns snapshots deep history that necessarily predates the current runtime, so the first upgrade refused every snapshot ever published — and `FE-PROV-003` told the user to check a download that was never damaged. Narrowed to `genesisHash`, with a per-cause remedy in the expert detail §10.4 already designates as the per-occurrence half (SQ-610). **§8.4's spot re-derivation existed only as a sentence** — a named TH-50 mitigation with no code, no injection point and no test. It is now injected in `RowCheck`'s shape, walks downward from the newest covered block, is bounded, and carries each block's **whole** movement list including when empty, because a checker asked only *is this movement real* catches fabrication and never deletion. **The 15 §4.8 lying-indexer row was the sharpest finding**: it is a PR gate, and every existing case supplied a synthetic `RowCheck`, so what was certified was the ladder *given verdicts*. `chainRowCheck` plus a suite driven from the recorded chainHead transcripts through `@bleavit/mock-runtime` closes it with no node and no network — and its own anti-vacuity assertion immediately caught a first draft that harvested ten rows, one stratum, one comparison. Also landed: the layer-3 origin mint (branded, so an unadmitted document is untypeable there, and `sampled` true only when a block was compared), §8.1's in-bundle suggestions file with its disclosure, the `no-overlap` diff discriminant, the named 50 % floor, the sampling rate behind `@bleavit/providers/testing`, and `ArchiveExport` as a generated schema. **Two findings came from the new tests rather than the audit:** the unit-manifest test caught 10 §10.2's handoff set missing `handoff-envelope` while the unit imports it (spec amended, Decision log), and the mutation run caught that `no-loosened-sampling-rate-in-production` sees a subpath import and **not** a barrel re-export — one `export {} from './testing.js'` restored the loosened rate to every consumer with the rule green, so a barrel test is the other half. 22 mutations, 0 survived; full `pnpm test` green.

**What comes next:** F9's remaining work is named rather than carried: the archive adapter (SQ-612), the live-indexer interface (SQ-613) and INV-FE-3's actionable-object fetch must move to a milestone SQ-614 asks 10 to name. Then the R-6 review and #247's merge

## F9 · F23 · SQ-630…SQ-633

**The R-6 re-review's remaining findings and four `chatgpt-codex-connector` P2s on #256, closed together — and six of the eight were one defect: a control that certified the check beside the one it was trusted for.** `admitSnapshot`'s brand covers the file screens, so the mint took a hand-written spot-check report next to it and badged rows `sampled: true` for a comparison nobody ran — and ignored `findings`, so a report that had **caught** the document contradicting the chain minted what a clean one did. Both mint arms now take evidence only the checking function produces (a branded `SpotCheckReport` that must name **this** document by reference and carry none, and a branded `SampledRound` for the indexer arm, which had been handed the *snapshot* arm's type where §8.4 gives indexers sampling and snapshots screens). §8.4's spot re-derivation gained the chain-side producer it never had — `chainSpotCheck` imposes §8.2's chain order, refuses two events at one position, and compares length as well as content so **deletion** is caught — driven from the recorded chainHead transcripts through `@bleavit/mock-runtime` exactly as the sampler suite is. A publisher caught contradicting the chain now **stops serving** (`FE-PROV-002` through the same `afterSampling`, beside the file's `FE-PROV-003`), while a damaged download, a wrong-chain file and an unfinished check leave it untouched. Codex's four, all confirmed: the mint binding; `acceptSuggestion` returning `healthy` before anything probed it (now `unprobed`, which **cannot serve reads** — `probeDue(null, …)` says a probe is *due* and stops nobody reading first); the re-derivation stopping at an invented 128 blocks and **admitting**, where §4.2 puts peer pruning at ~256 — it now walks until the checker says `out-of-reach`, not before its first comparison (a document ahead of this device's head answers unreachable at the **top**), under a ceiling derived as 2 × §4.2's figure whose only effect is a refusal; and the row quota metered after the parse, so *streamed* held for bytes and not rows — now per chunk, by a `{` count that can over-count and never under-count. Minors: the pin is over **the file's bytes** (measured: a file with one space added hashed to the publisher's pin and the message named a hash the file does not have), the canonical comparison streams through `canonicalJsonInto` instead of building a second 400 MB document, `meaningfulAtLeast` is deleted rather than documented, §6.3's `CoverageRange` is imported as a **type** (a value import would pull Dexie into `providers`), `SAMPLING_GUARANTEE` is bound clause-by-clause to §8.4's own extracted sentence, and the empty-id refusal moved to the importer's first line from behind the eviction preview. **The barrel hole is closed in all four packages** by one shared helper over the whole `/testing` namespace — F9's own test listed two names, and `chain-client`, `local-index` and `signing` were defended by comments. **27 mutations, 0 survived; `pnpm test` 1,245 tests green locally** (GitHub Actions was in a major outage, so nothing was verified by CI).

**What comes next:** **F23 opened** and F9's *Spec* narrowed to the mechanism half, which is the review's stated route to ✅: SQ-614 is answered in that row rather than left open. Next: F9's R-6 sign-off on the narrowed column, then F23 behind F7b/F8

## F9 · F23 · F24 · SQ-770…SQ-773

**F9's R-6 re-review returned 1 blocker and 4 majors; the blocker and two majors are fixed, the rest are written down.** The blocker was measured on the shipped code first: a valid **216,000-block snapshot was rejected**. §8.4 scopes re-derivation to the covered blocks *inside* light-client-reachable depth and calls the rest a **disclosed** limit, and §6.4 assigns deep history to snapshots by design — but the walk had one undirected `out-of-reach` verdict and guessed its direction from whether anything had been compared, so the ordinary case (everything below the window) was read as the rare one (everything above it): 512 asks with a known answer, then `FE-PROV-003` on a cause §8.4's table does not list. `SpotVerdict` now carries **which side** and `SpotCheckReport` a **`reach`**, so the blind spot and an unfinished pass stop being the same `compared: 0`. Two minors fell out: the incomplete-check sentence claimed the chain was still answering (false in exactly the case that reached it) and the auto-disable denominator counted blocks never re-derived. **Major 2**: `canServeReads` had no caller anywhere; wiring it into `runSamplingRound` exposed that it excluded `failing`, against §8.3's own *"only `Disabled` stops reads"*, and the import is gated on a deliberately narrower predicate instead (a pinned file asks no endpoint anything, and nothing drives §8.3's probe). `fleetState` now decides all-down **through** `allProvidersDown`, whose arms had no test at all. **Written down, not fixed**: `BlockMovementRead`/`ChainRead` have no production producer — nothing turns `System.Events` into `SnapshotOp`s — so F9's *"gained its chain-side producer"* is corrected and **F23** owns both readers; `SAMPLING_GUARANTEE`'s *supports and recommends* clause is unbound by the clause gate; `selectSample`/`livenessRefusal` are exported with no caller; §8.2's chain order holds only at block granularity (SQ-840). New **F24** owns §8.2's archive reader, `optional/indexer/` and §8.3's probe driver, so F9's Spec column names every exclusion with an owner. **Gates: all 26 `app/` gates green** (`build`, `check:types`, `test` — providers 185/185, and every other suite and witness), `depcruise` 0 errors, `check-plan-tables`, `check-spec-question-batches` and `check-doc-links` green. **11 mutations, 0 survived** (two re-run as forms that compile after `tsc` caught the first attempt). GitHub Actions is in a major outage, so there is **no CI evidence** for this commit — local only.

**What comes next:** **F9 is deliberately still 🔨** — the R-6 sign-off is the user's call. Then F23's two chain readers, and F24 once SQ-612/SQ-613/SQ-771 are ruled

## F9 · F23 · SQ-810…SQ-812

**An adversarial R-6 re-review of F9 returned 0 blockers and 4 majors; all four are closed, and the sharpest is that the fixed blocker's guess did not go away — it moved, and its failure direction flipped.** **M-1:** the work ceiling still refused a valid snapshot on a cause §8.4's table does not list, and **two** live configurations reach it — a device more than 512 blocks behind the document's newest covered block, and a document with more than 512 *reachable* covered blocks, where the remedy copy (*"try again when this device has caught up"*) was **false** and the refusal permanent. The ceiling now **admits** with `reach: 'ceiling'` and the mint declines to badge those rows `sampled` however many blocks agreed; `spot-check-incomplete` and its `incomplete-check` remedy are deleted, leaving every `FE-PROV-003` cause a statement about the **document**. **M-2:** `OutOfReachSide`'s correctness is delegated to `BlockMovementRead`, which has no production producer and which F23 owns, and nothing anywhere stated the two properties the walk rests on — contiguity at the head, and `below-window` only below the pinned floor. The old code guessed and failed toward **refusal**; the new code trusts and fails toward **admission**, since a reader answering `below-window` above its own head yields an admitted document with `compared: 0`, no finding and the blind-spot disclosure. Fixed in two places: F23's row carries the contract as four explicit obligations, and F9 ships **`checkBlockMovementRead`** behind `@bleavit/providers/testing` — an executable contract that sweeps the declared window with margin at both edges, checks both properties plus head/floor anchoring, `chainSpotCheck`'s three throw conditions, determinism on re-ask and an out-of-enum side, and **refuses** a window it cannot exercise on both edges rather than sampling it. Sixteen cases: one positive control and fifteen deliberately broken readers, so no violation class is asserted by a harness that might not detect it, and the false-clean case is measured end to end through `chainSpotCheck`. **M-3:** `SpotCheckReach` had three arms and four producing situations — a document entirely above this device's head and smaller than the ceiling returned `whole-document`, the one arm a screen may read as *fully re-derived*, having verified nothing. Fourth arm `above-window-only`, kept apart from `window-floor` because that one is permanent and this one is transient; three new cases including the empty-document boundary. **M-4:** the normative UI copy shipped *"supports and labels"* for §8.4's *"supports and **recommends**"*, and the clause-by-clause gate extracted the four clauses around it, so the substitution could not fail. **The spec question under it was ruled before the code was touched**: doc 10 spelled the clause two ways (§8.4 *recommends*, §2.3 *discloses*) and a client shipping one fixed string can satisfy only one, so §2.3 is amended to §8.4's verb under R-1 — owning section, the weaker verb being redundant where it stands, a diff being a **falsifier** so recommending it can never certify a forgery away, and the stronger obligation subsuming the weaker (Decision log). A second test asserts both sections agree. Also fixed: `where` validated against its union at both injected boundaries (an out-of-enum value read as `above-window` and ended in **admission**, while its two neighbours were already hardened); `FleetState.serving` re-documented as a **permission** and given a `failing` count beside it, since a fleet answering nothing reported *N serving*; `livenessRefusal`'s note corrected, because `all-down` needs every source `disabled` and so no longer fires for a merely unreachable fleet — §8.4's first `FE-PROV-001` arm is **wider** than when SQ-609 recorded it; and `canServeReads` re-cited from INV-FE-12 (whose subject is an unknown *runtime* surface) to INV-FE-3, INV-FE-15 and SQ-771. **Gates: all 26 `app/` gates green** (`pnpm -C app test` exit 0, 1,279 assertions, providers 210/210), `depcruise` 0 errors, witness fired, `check-plan-tables` · `check-spec-question-batches` · `check-doc-links` · `check-verbatim-copies` green. **21 mutants, 0 survived — and the battery's first run reported 17 survivors, which was my harness's defect and not the tests': the suites import `dist/`, so a mutant in `src/` that is not rebuilt is never executed.** Three of the 18 behaviours were first written in forms `tsc` rejected and re-run as forms that compile. GitHub Actions is in a major outage, so there is **no CI evidence** for this commit.

**What comes next:** **F9 stays 🔨 — the R-6 sign-off is the user's call.** Then F23's two chain readers, wiring the production `BlockMovementRead` to `checkBlockMovementRead` and giving `ChainRead` the same treatment; F24 once SQ-612/SQ-613/SQ-771 are ruled

## F7 · V-200 (audit, unplanned)

**A helper named `finalized` was asserting finality rather than establishing it, in four modules, and all three provenance gates were green on it.** Swept `app/src/**` and `app/packages/**` by the status literal, by the longhand shape, and for any local helper manufacturing a provenance status of any kind: **8 construction sites** outside the modules that own provenance, of which **6 are lies** — a `0`/`'unknown'` fallback badged `verified-finalized` when the epoch decode failed (`chain-reads.ts`), the same for `0n` on both funding legs, a pre-read sentinel badging `0` `verified-best` at block 0 (`boot.tsx`), and a caller-supplied number stamped with a caller-supplied pin (`review.tsx`). `chain-reads.ts` contradicted itself in one object literal: `epoch`/`phaseLabel` took a badged fallback while `phaseFlags`, three lines down, chose `undefined` for the identical failure. **Ruled against the comment defending it, on 10 §2.2's unconditional *"only to values read through smoldot with storage proofs checked"* plus INV-FE-12's no-guessing rule and INV-FE-9's label-on-the-datum rule** — the sibling `undecodable` array is not the datum being labelled. No spec change, no contract bump, no new status, **no spec question**: the texts settle it. Repair makes the honest thing expressible rather than adding one more check — `chain-client` now exports `derive(read, compute)` (grants nothing: no read in, nothing out) beside the withheld `finalize`, every reader threads its own read's pin, `ShellChainState`'s three leaves admit absence so the pre-read state can be itself, `Clamped` reaches the review screen paired with its chain input's own `Verified<T>` (`ReviewedClamp`, with a mismatch refusal), and consumers fail closed on absence. **The gate is the more valuable half**: `check:provenance-mints` (+`:witness`) bans constructing a `VerificationStatus` outside three named modules, matching the **discriminant position of a closed union** — complete, and silent on comparisons, `switch` arms and type declarations, so it needs no exemption. Constant-folds named discriminants and shorthand; scans `packages`/`src`/`tools` and deliberately not `tests`. **Mutation evidence: 6 mutants, 0 survived** — every one compiles under `tsc -b` and `check:casts`, `check:render-provenance` and `depcruise` are green on all six. Gates: `build`, `check:types`, the full `pnpm -C app test` chain (all 34 legs, 1,156 tests, 0 failures), `depcruise` (0 errors), `check-plan-tables.py`, `check-spec-question-batches.py` — all green.

**What comes next:** The same pattern exists on branches I did not touch: `balance-reads.ts`, `position-reads.ts` and `core-screen-reads.ts` (`track-f/f7b-render`; `core-screen-reads.ts` also on `track-f/f7b-core-screens`). `market-reads.ts` is already repaired on `track-f/f7b-render` and carries its own `derive`, so that branch and this one both add it — merge them as one. Once those land, the gate catches any recurrence
