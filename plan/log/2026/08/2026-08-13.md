# Session log — 2026-08-13

## Security-remediation artifact-flow continuation

Continued draft PR #304's remediation after the date boundary without rewriting the August 12
history. The exact-OCI chain-spec handoff review found that release-tier hosted-service drills
10–13 declare the separately built `bleavit-client-local.json`, but tag CI did not generate it. It
also found that the evidence runner's broad `bleavit-*` classification would have compared that
client runtime to the shipped primary and refused the run even after generation was added.

Release CI now generates the client-parachain harness before evidence. The runner derives every
chain-spec prerequisite from each selected topology, binds only exact ordinary/migration primary
names, treats the client and fast-timing runtimes as explicit separate classes, requires every
release-tier suite to retain a primary binding, and rejects an unclassified Bleavit spec. A full
mocked relay-generator execution proves the supplied OCI primary survives the second handoff into
ordinary, raw and migration specs while the fast variants keep their distinct test runtime.

Focused evidence is green: 91 environment tests outside the localhost-socket-restricted sandbox,
175 deploy tests, 129 release tests, the workflow contract/pin checks, Bash syntax, Python compile,
environment validation, all documentation integrity gates, 1,170 local links, verbatim mirrors and
`git diff --check`. No Cargo, network or OCI artifact build was launched for this source-only fix.
The next step is a clean commit followed by genuine same-SHA OCI/artifact regeneration.

## Security-remediation artifact closure

Committed the exact-OCI/client-spec source closure at `a4983e85`, then regenerated the paired
runtime artifacts from that clean source in the digest-pinned OCI environment. The primary and
recovery Wasm SHA-256 values are respectively `1955a75d…c05` and `93090dd3…952`; booted metadata,
the embedded and independently recomputed RFC-78 digests, and every extracted `:code` binding
agree. Canonical ordinary, raw and migration specs embed the exact primary; fast-timing and N10
client specs retain their separately classified runtimes.

The current v31 chain feed, 302-primary/300-recovery chainHead transcript, PAPI descriptors,
critical surface, skills and exact mock-runtime capacity expectations were regenerated. The
artifact assembly contains 315 content-addressed entries, reports no corruption, and retains ten
declared pre-launch/evidence gaps rather than masking them. Focused descriptor, app release,
chain-client, mock-runtime, explainer and release-tooling gates are green; the final stale-consumer
scan and `git diff --check` are also green. The remaining handoff is the generated-artifact/status
commit, branch push and draft-PR CI. The descriptor and mock-runtime wrappers initially could not
spawn their nested read-only subprocesses inside the restricted sandbox; the identical official
commands passed when rerun with nested subprocess permission.

## Current-main reconciliation before PR handoff

Fetched and merged current `origin/main` after GitHub reported that draft PR #304's base had advanced
by one commit. Preserved main's strict shared `plan/questions/<ID>.md` frontmatter reader in the
transaction-builder expiry test and retained both independent August 12 session records. The only
textual conflicts were that test and the additive log file.

The merged state passes app build and test typecheck, all 179 transaction-builder tests, all 88
analysis tests, unreadable-obligation validation, plan rendering, question batches, Markdown tables,
all 1,170 local links and `git diff --check`. The merge is ready to commit and push to the existing
draft PR; exhaustive GitHub CI remains the publication gate.

## Draft-PR CI repair

Inspected every failed job in draft PR #304's first exhaustive run and traced four independent
causes. The app container rejected the mounted checkout as dubiously owned before deriving
`SOURCE_DATE_EPOCH`; both reproducibility jobs now export the exact checked-out commit time and
provide exact-checkout Git trust to all later release subprocesses through job-scoped environment
config. A benchmark-only market mock import is feature-gated. The public XCM
router simplification removed welfare accounting reads and writes, so `pallet_xcm` was re-measured
at the release-blocking 50-step/20-repeat fidelity instead of editing its generated weight file.
Finally, the newly published `extract-zip` high advisory has no fixed release and is reachable only
through Lighthouse's isolated render-budget tool; the version-keyed waiver records why browser
archive extraction is not executed and why the package cannot enter the app bundle.

Focused workflow/advisory tests, action-pin validation, Rust formatting, and market Clippy with and
without `runtime-benchmarks` are green. The benchmark runtime build and committed-fidelity XCM
measurement are green. Runtime-derived integration artifacts must be refreshed from the resulting
clean source commit before the single replacement push because the weight change alters the shipped
Wasm.

The first full XCM suite then reached a frozen-source tripwire that still pinned the barrier before
the August 12 closed-allowlist remediation. The 69 behavioral tests were green; only the checksum
failed. The guard now names and freezes the current non-client legacy branch, the owning service
verification text explicitly permits decision-recorded hardening of shared deny components, and the
new digest acknowledges that exact authorized source amendment rather than deleting the tripwire.
The full market suite then exposed a similarly masked fixture issue: its reserved-funder case used
an unaligned service question and therefore correctly failed the earlier ID-band guard. Moving that
case to the next aligned family makes the test reach and prove `FunderMismatch`; production logic is
unchanged.

## Draft-PR CI artifact refresh

Committed the reviewed CI/source repair at `13eaabc6`, then rebuilt the paired runtimes from that
exact clean source in the digest-pinned OCI recipe. Primary/recovery Wasm hashes are respectively
`2fa46f46…5d3b` and `c70a3c54…eadd`; their booted `:code`, metadata and independently recomputed
RFC-78 bindings agree. Canonical and release-drill specs carry the new primary bytes, strict
chainHead recording captured 302 primary and 300 recovery surfaces with zero missing, and the four
feed provenance records plus 303 transcripts were refreshed.

All focused gates are green: affected Rust suites, committed-fidelity weight drift, supply chain,
workflow/tooling tests, feed/surface/descriptors, app release, chain client, mock runtime, skills,
explainer, release/deploy/environment tooling and documentation integrity. Assembly contains 315
content-addressed artifacts with zero corruption; its ten pre-existing readiness gaps remain
explicit. The final artifact/status commit is ready for the single replacement push to PR #304.

## Watched-CI metadata measurement repair

Watched the replacement PR #304 run through completion and found one remaining red job: App's
release block correctly refused because the regenerated v31 `metadata.scale` grew from 492,363 B
to 493,122 B while 10 §9.3 and its gate still published the earlier measurement. Re-measurement
with the gate's own Node zlib path gives 155,848 B for the larger gzip blob, which still rounds to
0.16 MB. Updated the owning spec, gate constant, mutation anchor and derived verbatim copy without
changing the eight-blob, 1.5 MB combined, or cache budgets. The exact failing release sequence is
green, including release build/check/manifest, bundle budget, artifact budget and all eight witness
cases; the 18 focused budget tests and 61 spec-binding mutation tests are also green. The
replacement CI run will be monitored at the repository's five-minute cadence.
