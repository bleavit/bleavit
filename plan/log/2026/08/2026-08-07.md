# Session log — 2026-08-07

## Track F integration · V-201 · SQ-605/SQ-610 (cited)

**Finished the ten-branch `integration/track-f-v2` merge and implemented the three rulings that unblocked it.** The four conflicted files were resolved by reading both sides with `rerere` off, and each side's distinctive content was then verified to survive by comparing the merged blob against both parents and the base: `PLAN.md` keeps both sides' appended rows (832 + 3 lines, all present verbatim); `chain-client`'s `index.ts`/`provenance.ts` reconcile to **one** `derive` definition, whose code was byte-identical on both sides, under a doc comment carrying both arguments; `proposal-reads.ts` keeps the F7b structure with the audit branch's `derive` replacing the banned local helper, and **each statistic now descends from the `decision_stats` call's own pin** rather than the summary read's — proved by a double that answers the two calls at different blocks, since the two pins are equal for any honest reader and the slip would otherwise be invisible (V-184's shape). **Four more files had lost a side in earlier merges of this branch and no suite could see it** (V-201): the lockfile refused `--frozen-lockfile`, `tsconfig.tests.json` was invalid JSONC, and `ci.yml` was invalid YAML with both §9.4 budget gates orphaned. **Ruling 1** made 10 §6.3's `RangeEdge` a discriminated union so a provider range discloses that its edge is not checkable and why, instead of refusing, defaulting, or comparing the chain against itself. **Ruling 2** put a sanctioned `provider` status constructor in `shared-types` rather than widening the mint gate's allowlist, and the gate was re-proved to bite. **Ruling 3** re-measured §9.3's metadata figure after v28 (470,546 B raw; gz re-derived and still 0.15 MB, so §9.4 is unchanged). Every `app/` gate green, the aggregate `pnpm test` green, all root checkers green, 276 `tools/ci` tests green; 11 mutants, 0 survived. No milestone status changed.

**What comes next:** Two reds this session did not take a decision on, both predating the merge and both in V-201: PLAN.md's duplicate milestone rows (F22 holds ✅ and ⬜ at once — `integration/track-f`'s `e880e72d` is the user's own remedy), and `EXECUTION_RETRY_WINDOW` unclassified in the limit-coverage registry. Then the exhaustive Rust gate and the PR

## Track F integration · V-210 · SQ-790 (cited)

**Closed the limit-coverage red that V-201 had recorded and left, and the classification turned out to sit on top of a real coverage gap.** `EXECUTION_RETRY_WINDOW` reached 13 §2 under SQ-790, when a client defect proved to be a spec contradiction: 09 §1.2(1) stated the execute window as `maturity <= now <= grace_end` unqualified, while 05 §2.1's T18/T23 transitions and the guard's own two-armed check admit a failed mandate through `failed_at + EXECUTION_RETRY_WINDOW` **instead of** `grace_end`. Classified `dispatch-limit` with `ExecutionGuard::GraceExpired` in `tools/limit-coverage/registry.toml`, against `param-bounds` (kernel constant, not an amendable key, not genesis-seeded) and against `value` (it refuses a real user dispatch with a Module error, and `value` buys an exemption from the marked test this gate exists to demand). **The gap behind it was real and verified before any code was written**: every existing `execute` plus `GraceExpired` test was retry-unaware, and the only retry-window boundary assertions in the pallet belonged to `expire_failed_execution`. New test `t23_execute_window_follows_failed_at_and_not_grace_end` pins the substitution in both directions at the exact boundary, including the non-monotone case where the retry window closes **before** `grace_end` and the refusal arrives while the grace window is still open. Gates green and read in full: `cargo fmt --all -- --check`, `cargo clippy -p pallet-execution-guard --all-targets -- -D warnings`, `cargo test -p pallet-execution-guard` (75 passed, 0 failed), `check-limit-coverage.py` (207 keys, 83 dispatch-limit), and the 23 limit-coverage tooling tests. 3 mutants, 0 survived; the marker proved load-bearing by deletion. No milestone status changed.

**What comes next:** The other red V-201 recorded is untouched and still open: PLAN.md's duplicate milestone rows, where the two F22 copies disagree on status. Then the exhaustive Rust gate and the PR

## F8 · F9 (local-index half) · SQ-900/SQ-901

**F8's fifth R-6 review: four majors, all closed, and the finding worth carrying forward is that three consecutive repairs each landed where the finding pointed rather than where the harm was.** Round three had repaired `coveredQuery` to return a `CoveredHistory` — and pointed it at `priceSamples`, the tier SQ-782 records as having **no producer in production**, while `candles1h`, which `storeWriter` fills from 10 §9.1's scan-time aggregate on every block carrying a fill, had no covered read at all. So the disclosure machinery answered over a permanently empty table and the only way to draw a real chart was `db.candles1h.toArray()`: the bare rows §6.3 exists to forbid, arriving by the one route left open. **Major 1** adds `coveredCandles` over the same wrapper and, because the rule keeps being violated precisely where nothing checks it, a **new structural gate** — `app/tools/check-covered-history.ts` (+ `:witness`, CI step, 27th `app/` gate) fails on any chart-tier reference outside a `coveredQuery(…)`, on the TypeScript AST, with two named internals (`quota.ts`'s ladder, `loop-store.ts`'s aggregate) and deliberately **no exemption for `store.ts`**. **Major 2**: the repaired query resolved three `meta` reads through `Promise.all` and awaited the row read after — four transactions, so a fold committing between them returns rows already deleted beside labels that predate the deletion, which is §9.2 obligation 1's silent splice reassembled on the read side. Now one `db.transaction('r', …)` with rows read first (labels only grow, so that order can only over-explain), asserted as **one transaction identity** through Dexie's DBCore seam rather than by racing a scheduler. **Major 3**: `readChartDiscard` returned `row.record` unchecked while `readCoverage` sanitizes and `readDownsampled` checks `Array.isArray`, and `discardOver` dereferences it — so after round three a record corrupted to `null` threw a `TypeError` out of the **render** path, where before it could only spoil the boot report. Both it and `readPendingRawEvicted` now validate and return `undefined`, which is INV-FE-7's own disposal. **Major 4**: `readTradeTape` skipped a block the stream never delivered with the same `continue` that skips an out-of-window scan, so a reconnect gap inside a window returned fewer fills and rendered as a quiet market — the module refuses an *inverted* window for exactly that reason and had not applied it to a gap. The conservative reading ships (observed and missing spans beside the fills, in the module's own vocabulary rather than §6.3's) and the question is **filed, not settled**: **SQ-900** asks whether §6.3's *"every history query"* binds a read §9.1 makes deliberately not layer 3, **SQ-901** whether the obligation is per query, per tier or per screen. **Also fixed, from F9's review of the same package:** `runIngest` dropped `verifyRanges`' `unchecked` set, and §6.3's last bullet makes placement in neither list mean *"one that genuinely passed"* — so with the `unverifiable` edge arm live, **every** provider range was reported to the per-session path as chain-agreed, while `checkIndexAtBoot` told the truth about the same coverage. **Minors:** `ChartDiscardRecord`'s `undefined` envelope meant both *nothing was covered* and *the coverage row would not parse* — an ordinary client and a corruption event — and is now one `ChartDiscardSpan` with three arms; `downsample`'s rendered `reason` derives its rung from `DEGRADATION_LADDER` instead of claiming *"raw samples"* on all three; `isGenesisHash` left the barrel (no consumer) and `mint.ts` keeps the half of its justification that is true; `writeDownsampled` moved to `/testing`, since §9.2 obligation 1 binds its label to the delete and the barrel is how the phantom label is reached from outside; the stale **0.14 MB** metadata figure is **0.15 MB** in five places. **17 mutants, 4 survived their first run** — three were refused by `tsc` (`noUnusedLocals` on the validator the mutant orphaned) rather than by a test, one carried a stale anchor, and all four were re-expressed and killed. **Gates local only: GitHub Actions has been in a major outage all session**, so F8 stays 🔨 and nothing here is CI-evidenced.

**What comes next:** F25 renders the disclosure (`IndexBootReport`, `ChartDiscardRecord`, the tape's gap spans); SQ-820/821/900/901 are rulings, not code; CI evidence for these commits once Actions recovers

## F23 (🔨, **not** ✅) · V-240 · V-241 · V-242 · SQ-880

**`@bleavit/providers` had never been imported by anything in `app/src`, and F23 is its first consumer.** Measured before writing a line (V-242): the package was declared in the `analysis` unit's reference set and no module in any of the three compilation units named a symbol from it, so every 10 §8 mechanism existed, was tested, and was called by nobody. Seven modules landed on `track-f/f23-provider-surface` in `app/src/features/analysis/src/`, plus a 36-test suite. **The centrepiece is `SpotCheckReport.reach`, and the reason it is the centrepiece is measured rather than argued (V-240):** `badgeCopyFor`'s `provider` arm reads only `providerId` and `sampled`, so an honest deep-history snapshot, a document ahead of this device's head, and a pass that spent its ceiling above that head all badge **byte-identically** as *unverified — X*. `SpotCheckReach` was split into four arms so those would stop collapsing and it collapsed again one layer up — which made F9's *admit rather than refuse* choice circular, since its justification is that the limit is **disclosed**. The disclosure now has a consumer, and the test asserts both halves: the badge set has size 1 (the collapse is real) and the three renderings are distinct (the screen separates them). **A fifth situation the type cannot express (V-241):** `spotCheckSnapshot` rewrites `whole-document` only when `outOfReach > 0` and `checkCoverage` admits an empty `coverage` array, so a document covering nothing ends at `whole-document` with `compared: 0` — the arm documented as the only one a screen may read as *fully re-derived*. Contained in the renderer: `reachReading` is a total function of the arm **and** the counts, five readings over four arms, and every arm renders `compared`/`outOfReach` so `compared: 0` is never shown bare. `wouldBadgeSampled` is bound to `mintSnapshotRows`' own expression by parsing both files, so the surface cannot claim a badge the mint refused. Also landed: §8.3's ladder with its required reasons and `failing` rendered **beside** `serving`; `fleetState`'s three arms with `none-enabled` carrying no `FE-PROV-*` code; §8.1's suggestion list and an accept interstitial that **derives** its disclosure and renders it above the control; §8.4's eviction preview with **no confirm control at all** on the infeasible arm; every `ImportOutcome` arm including both refusals when the chain disagreed; the two-snapshot diff with `no-overlap` as its own non-agreement and no control that picks a side; §6.3's holes/downsampled/discard with **distinct sources named** rather than gaps counted; and INV-FE-3's actionable-object fetch made structural by `onAct` taking `Finalized<T>`. **Gates read in full, none piped:** all 26 `app/` gates green (`pnpm -C app test` exit 0, 1,755 assertions; analysis 36/36, providers 212/212, screens 467/467), `depcruise` 0 errors, `depcruise:witness` fired, `check-plan-tables.py` and `check-spec-question-batches.py` green. **27 mutants, 0 survived** — 7 first written in forms `tsc` rejected (every one an orphaned import or parameter under `noUnusedLocals`/`noUnusedParameters`) and re-run as forms that compile. GitHub Actions has been in a major outage all session, so there is **no CI evidence** for this commit and F23 stays 🔨.

**What comes next:** Most of the F23 row is still open and named in it: the **two §8.4 chain readers** (nothing turns `System.Events` into `SnapshotOp`s, so `checkBlockMovementRead` still has nothing to be wired to), the **layer-3 write** of the minted rows, and a **history query** that calls the coverage renderer. Then SQ-880 — the provider UI has no 11 §11.2 inventory id, so none of these seven screens is reachable from `routes.tsx` and none can even be declared pending

## F7b · V-220 · V-221 · SQ-860

**Built F7b's named remainder — S2's finalized decision dashboard and the composition roots for S2/S3/S4/S20 — and writing the roots is what falsified three shipped models.** `screen-composition.ts` turns this chain's metadata and descriptors into the keys, decoders and call arguments four readers had been declaring and nobody building; `chain-client` gained the runtime-API half of its codec surface (`apiDecoder`/`apiArgs`/`decodeApiResult`, `concatDigestBytes`). The dashboard renders 02 §4's `DecisionStatsView` whole, on the 1e9 grid, with the four gate TWAPs named rather than indexed and **no** trailing uplift — step 7's floor needs a trailing Baseline TWAP the view does not publish, so the obvious subtraction is right only while the Baseline floor does not bind. The model it replaced had `outcome` and `upliftPpm`, **neither a field of that view**. Three defects of one class fell out: `ProposalSummary.title` had no chain source at all (SQ-860, now the `payload_hash`), `STATS_REQUIRE_SEALED` named three non-variants and missed four real post-decision states, and `trade-ticket.ts`'s `ProposalState` union named five more that do not exist (V-221). Two measured traps closed at the root (V-220): an SS58 address handed to the `[u8; 32]` position-view argument encodes to **25 silent bytes**, and a `(PositionId, AccountId)` key must be split by re-encoding the id rather than at a constant offset. `PositionDecoders` is per domain now, per 02 §7.4. **18 mutants, 0 survived**; app suite **1,758 tests, 0 fail**, every `app/` gate green plus `check-chain-feed.py`, `check-plan-tables.py` and `check-spec-question-batches.py`.

**What comes next:** **F7b is NOT ✅** and must not be marked so: GitHub Actions has been out all session, so no run has executed against any of this and R-6's gate conjunct has no evidence. When CI returns, run it on `track-f/f7b-close` and close F7b if green. S2/S3/S4/S20 remain `built-unwired` on purpose — the roots exist, the transport does not, and `chainSpecs` is still `[]` with both `chainSpecHashes` null (F18's artifact remainder). SQ-860 needs a ruling only the user can give: derive a description from the payload preimage, add a bounded label to 02's `Proposal`, or state that a proposal is identified by its commitment (what the client implements today).

## F25 · SQ-920 · SQ-921 · SQ-922 · V-260…V-262

**F25: the local index's disclosures reached a user for the first time.** `checkIndexAtBoot` gained its production call site (`src/features/analysis/src/index-boot.ts`), `boot()` runs it, and `IndexBootDisclosure` renders the result as the shell's unconditional first child — every route, not a screen, because 11 §11.2's inventory has no row for it (SQ-920, the gap F23 found in the provider UI, not papered over with an invented S-number). All seven `IndexBootReport` fields and all three `CoveredHistory` fields are bound to a disclosure by a record **total over the interface**, and the suite re-derives both field sets from the package's own declarations, so the next field cannot ship without a reader. Copy is a type: `stated` carries the section whose rule it discharges, `awaiting` carries the record's fields plus the open rows, and there is no third arm — four slots wait on `FE-IDX-002` (SQ-604/SQ-783/SQ-820/SQ-821/SQ-760/SQ-921) and every one is checked against PLAN.md's **open** status, which fails the day a ruling lands. `unchecked` is rendered and worded as *kept, not agreed* (§6.3's own *cannot say*), since every provider range verdicts `unchecked` by construction. Two findings the row did not name: `unchecked`'s two causes are indistinguishable in the returned value (V-261, SQ-922), and a coverage entry dropped by `sanitizeCoverage` is a **third** `FE-IDX-002` candidate with a third recovery (SQ-921). Today's user-visible state is `not-opened`: the release pins no parachain genesis (V-260) and 10 §7 admits no default, and the claim is bound to `release-sources.json` so a pin fails the suite. **Local gates only — GitHub Actions has been in a major outage all session:** `build`, `check:types`, `pnpm -C app test` exit 0 at 1,766 tests (`test:analysis` 9 → 35), all 27 `app/` gates, `depcruise` 0 errors, PLAN tables, spec-question batches. **24 mutants, 0 survived** (three re-expressed: one stale anchor, two `tsc` refusals).

**What comes next:** F25 stays 🔨 pending CI and an R-6 review. Then: the four blocked copy slots need SQ-604/SQ-783/SQ-820/SQ-821 ruled, and `CoveredHistoryDisclosure` needs a history screen (F7b/F23) before it reaches a route

## F23 · F7b · F25 · F8 (integration fold on `integration/track-f-v2`, **no milestone status changed**) · V-280

**All four branches compiled together for the first time, and the one conflict left was semantic rather than mechanical.** F8's three-arm `ChartDiscardSpan` met F23's `coverage-view.tsx`, which read the old two-state shape and had written 10 §9.4's missing `FE-IDX-002` copy in its own voice — a second renderer of the record F25 had just given exactly one. Resolved by delegation, not by a field rename: the coverage view now renders `CoveredHistoryDisclosure` for holes, downsampled ranges and the migration discard, and keeps only §6.3's complete-within summary, the distinct sources and the per-range edge facts. F25's *one renderer, asserted item-for-item* test held on its own terms and could never have seen the duplicate, because both sides of its `deepEqual` come from `chartDiscardItem`; it is now joined by a source-level rule that the record is named in one module of `app/src`, whose anti-vacuity leg immediately found that the owner never named `ChartDiscardSpan` and drove the three arms into a total `switch`. The two rewritten tests replace one that had pinned the two-state reading as correct. **Gates, all green on the folded tree:** `pnpm install --frozen-lockfile` (the merged lockfile needed the three workspace links `src/features/analysis/package.json` declares, and that edit is committed here), `build`, `check:types`, the 47-script `pnpm test` aggregate (20 suites, 1,823 tests, 0 failures, exit 0), `depcruise` (0 errors, 2 pre-existing `no-orphans` warnings), `descriptors:check`, `release:build` (14 standing readiness blockers, none new), `check:artifact-budget` + witness, `check:bundle-budget`, `check:embedded-tree` + witness, `check:render-budget` + witness, seven repo doc gates, `check-limit-coverage.py` (207 keys, 0 unwired), `check-chain-feed.py`, and 276 `tools/ci` tests. **Two environmental notes, neither a finding about this merge:** `check:render-budget` fails without `CHROME_PATH` on this workstation and passes against the cached Chromium 1228 (V-180 records the same), and one `tools/ci` test skips because this box runs Python 3.10 and `tomllib` needs 3.11. A control-character sweep over `app/src`, `app/packages`, `app/tests` and `app/tools` returned zero findings.

**What comes next:** **CI has not run against this tree.** GitHub Actions is only just back from a multi-hour outage, so nothing here is marked ✅ and no milestone status moved. Next: let CI run `integration/track-f-v2`, then review the two duplications this fold exposed but did not resolve — `distinctSources` (F23, origin plus provider id) beside `boundarySet` (F25, origin only) answer §6.3's *name the distinct sources* sentence at two different granularities, and the same screen now prints a block span two ways, `#a–#b` from the coverage summary and `a..b` from the disclosure below it. Both are cosmetic today and both are the shape that becomes a defect when a third surface copies one of them.

## F1 (FE-P1…FE-P11) · V-300…V-306 · SQ-940 · SQ-941

**Eight of the eleven prototype gates moved, and none of them needed the experiment its own row named.** That is the session's finding, not a side effect. Every *Experiment* column is a plan, and PLAN's parked note had repeated those plans as blockers — *"no further analysis moves it"*. **FE-P1 was recorded resolved on two of its six halves.** The four open ones are all in the pinned tree. `PullOptions.at` defaults to finalized. `watchEntries` types only `{at: "best"}`. `bestBlocks$` may skip blocks, so it cannot drive ingest. `getEstimatedFees` runs `TransactionPaymentApi_query_info` at the **finalized** block. PAPI signs `CheckMetadataHash` as **mode 0 unconditionally**. **The code assumed the weaker answer.** 10 §2.3 names the fee estimate explicitly as a value that must be `Finalized<T>`, then softened itself *because FE-P1 was open*. `estimateFee` took a bare `bigint` while `nonceFor`, two functions below it, took `Finalized<bigint>` and compared block hashes. It now takes two reads and returns through `meet`, so a fee priced at one block against a rate read at another yields nothing. **9 mutants, 0 survived** — and the first cut of two was discarded, because they died to TS6133 on an unused import, which is a compiler kill that proves nothing. Re-cut to compile, the block-agreement tests killed them. **FE-P5 corrected the spec instead of confirming it.** 10 §4.2 claimed the `chainHead` group only. smoldot implements the legacy group too, and `state_getMetadata` reaches any hash at unbounded depth. The real wall is `chain_getBlockHash(height)`, refused by design. So §9.3's blobs are **required**, not a fallback. **FE-P7 is the most consequential.** An ANT authorizes by membership, so its controller list is 1-of-m and more controllers widen custody. 12 §4.2's primary option is withdrawn across 00, 10, 12 and 14. The AO implementation was archived on 2026-06-09 and ArNS now runs on Solana, so doc 12's name-control machinery describes a system that no longer runs — **SQ-940**, launch-blocking, needs the user. **FE-P9 was never blocked on a testnet**: its triggers live only in a superseded document this repository does not contain — **SQ-941**. FE-P3's SharedWorker half is negative and structural. FE-P10's two size halves are positive with 64 % of the Operational length budget unused. FE-P4's gz figure moved 12,955 B on a zlib change while raw and brotli held, so that budget must never become an equality check. Gates run: `build`, `check:types`, full `test` (27 legs), `check:plan-tables`, `check-spec-question-batches`, `check-dispatch-mirror`, `check-client-surface-obligations`.

**What comes next:** **F1 stays 🔨.** SQ-940 is open and launch-blocking, and CI has not run this branch — R-6 needs both. The user owns SQ-940 and SQ-941. Everything else in F1 needs a device lab, a live chain, hardware or ar.io credentials. Next milestone by dependency order is F18.

## F7b (R-6 review) · V-320…V-323 · SQ-960…SQ-965

**Cleared the two blockers and the first major of F7b's R-6 review, on `integration/track-f-v2`. F7b stays 🔨.** Both blockers were classes this milestone had already repaired once, on the surfaces the earlier rounds did not reach. **(1)** `redemption-ticket.ts` typed every leaf `Verified<T>`, so a provider-sourced rate and `MinSplit` produced a `charged` quote and `mayPrepareRedemption() === true` — 11 §11.4 rule 4 and §11.5 rule 5 both forbid it, and the net payout is `AlwaysVisible` above the fold. It survived because `trade-ticket.ts` had a `tests/firewall` fixture and this module did not; every leaf is now `Finalized<T> | undefined` and the fixture exists (V-320, V-321). **(2)** `POSITION_READS` declared `ConditionalLedger.Vaults`, `BaselineVaults` and `ServiceLedger.Vaults` and nothing read them, so `vault_state` — the field selecting the redemption call — reached the VOID layout with no FE-P2 cross-check, while the test written to prevent that asserted `'baselineVaults' in POSITION_READS.primary`. All three are read per domain now, keys and decoders and a per-vault memo, and the suite asserts the reader port's recorded `storage` calls (V-322). **(3)** A failed `quote()` decode substituted a zero pair and rendered `0.000000 USDC` under a `verified-finalized` badge — V-183's defect one module over. The chain-side quote is absent now, a `BalanceUnreadable`-shaped refusal renders in its place, and the block is kept as `P-1 quote()` carrying no `FE-CHAIN-005`, because E6's code asserts a disagreement (V-323, Decision log). **Six spec questions filed and none settled** — the five the review raised (SQ-960…SQ-964) plus one of mine (SQ-965: a fail-closed `PhaseFlags` makes the screen state a freeze the chain never stated). **Gates:** `install --frozen-lockfile`, `build`, `check:types`, `pnpm -C app test` exit 0 at **1,834 tests** across 20 suites (the whole gate aggregate), `test:firewall` 28 fixtures (27 forbidden + the positive control), `depcruise` 0 errors, `check:embedded-tree` (+ witness), `check-plan-tables.py`, `check-spec-question-batches.py`, `check-client-surface-obligations.py`, `check-dispatch-mirror.py`, `check-chain-feed.py`. **16 mutants, 0 survived** — M3 survived the first pass and killed the assertion that let it.

**What comes next:** CI on `integration/track-f-v2`; then the review's second and third majors. F7b must not be marked ✅ until those close and CI is green

## F23 (⬜ → 🔨) · F25 (R-6 review) · V-340…V-342 · SQ-980 · SQ-981

**Cleared the blocker, the first major and two named minors of the F23/F25 R-6 review, on `integration/track-f-v2`. Neither milestone is marked ✅.** The blocker was one range answered two ways: `coverage-view.tsx` rendered a `checked` edge as *"both are checked against the chain"* while F25's boot surface listed the same range under *"Ranges this client could not check"* — 10 §6.3 defines that arm as *all three checks **can** run*, the verdict lives in `CoverageVerification`, and `CoveredHistory` carries none (V-340); with no genesis pinned (V-260) every range in fact verdicts `unchecked`. The column now states what the edge **records**, `EDGE_IS_NOT_A_VERDICT` says the answer carries no verdict, and the stronger reading — that it should say *was* compared — is filed as **SQ-980**, not settled. The `checked` branch had **no fixture**: every coverage fixture used an `unverifiable` edge including on a `self` range, a combination §6.3 does not describe; `self` ranges now carry a `checked` edge and the note is asserted both ways. Major 1: `SAMPLING_GUARANTEE` said *"two independent sources"* where §8.4/§2.3 say *"snapshot producers"*, naming a cross-check `FE-PROV-004` does not implement — and the clause table paired the section's clause with the **substituted** rendering (V-342), the second time it was scoped around its own drift. Copy fixed, pair re-pointed, F23's check gained the **constant → document** direction; whether normative UI copy may be rendered rather than quoted is **SQ-981**, filed and not settled. Minor A: the badge assertion could not fail — three byte-identical arguments to a pure switch — and now derives its statuses from `mintSnapshotRows`' own output, proven by a control in which the old form survives the mutant the new one kills (V-341). Minor B: `distinctSources` delegates to `boundarySet`, so §6.3's boundary set has one implementation and its wording one home. Also corrected the F23 row itself: status ⬜ → 🔨 over seven landed modules, and **36** suite tests → **28** (27 before this round). Gates: `install --frozen-lockfile`, `build`, `check:types`, `pnpm -C app test` **1,835 tests / 20 suites / 0 failures**, `depcruise` (0 errors, 2 pre-existing orphan warnings) + witness, `check:casts`, `check:chain-literals`, `check:render-provenance`, `check:provenance-mints`, `check:covered-history`, `check:no-html-sinks` (each + witness), `check:above-fold`, `check-plan-tables.py`, `check-spec-question-batches.py`; control-character sweep clean. **10 mutants, 0 survived** plus the M7 control.

**What comes next:** CI on `integration/track-f-v2`; then the review's second major and its nine remaining minors

## **F24 ✅** · SQ-982/983/984 ruled · SQ-986…SQ-993 filed · V-344…V-349

**F24 done: 10 §8.5's three artifacts ship, 22/22 CI SUCCESS on `e1949d9e`, R-6 re-review 0 blockers.** Four review rounds in one day and **every one found the same shape — individually correct functions whose defect existed only in their composition, under a suite that covered each half separately.** (1) `readRange` and the reference server ran the full snapshot screen set, so §8.4's conservation replay refused **every span not reaching genesis** and the server answered `500` to conforming requests: `from`/`to` existed and were unusable. §8.5.2 had already ruled a page does not owe the replay; the code never caught up. Being precise changed the answer twice — monotone coverage **stays** (internal, and what makes the coverage arithmetic sound), and the derived-row check does not drop for the replay's reason, which forced the missed ruling: **a page's `balances` are read from state**, because §8.5.2 assigns this route to §8.4's sampling and sampling re-verifies against the chain. (2) That inversion exposed a live defect: `samplingPages` feeds `balances` into a chain comparison, so a fold is a mid-history subtotal held against a current holding — disagreeing on every honest page and **auto-disabling the operator**. Unreachable only because defect (1) masked it. (3) The R-6 review then found the sharpest instance: `samplingPages` emitted §8.2's canonical **decimal** while `chainRowCheck` compares `ChainReadResult.hex` and correctly declines to decode — **every honest row mismatched**, so §8.4's one live control over an indexer only ever disabled honest operators. (4) The re-review found that fix **narrowed and not closed**: `ProviderRow.claimed`'s contract still read *"canonically rendered"* and **no test composed the two halves**, so the defect could be rewritten from its own documentation. Also fixed: the `disqualified` arm carried the snapshot-file remedy for a live index; `livenessRefusal` labelled every auto-disable *"is not responding"* including two that answered promptly (typed `AutoDisableCause` now); the §8.1 disclosure written that morning **asserted a ten-minute heartbeat the shipped app does not perform** (required `Heartbeat`, no default); `providerUrl` appended the route **inside** a query so `?a=1` requested `/`; two spellings of §8.5.3's answer condition left a source permanently `Healthy` and permanently unusable; and the `/range` walk had **no byte or row bound at all** against an untrusted operator. `check-finalized-casts.ts` governed 1 brand while the workspace had 15 — it now **discovers** them and covers 16, finding one real assertion site. Also two CI reds, both mine and both from running the gates I remembered rather than the gates `ci.yml` lists: a reachable **ReDoS** (`js/polynomial-redos`, high, measured 20k→181 ms / 80k→3,139 ms, now 0.03 ms) and a broken doc link. **Gates: `pnpm test` 1,926 pass / 0 fail across 20 suites, 13 static gates, 16 doc gates, and CI 22/22 SUCCESS verified per check.**

**What comes next:** Merge #265. Track F then has **no buildable item left**: seven rows are parked on a device lab, a testnet, the key ceremony, seated roles, chain-spec bytes or **SQ-940**, which needs a user ruling and blocks launch. Eight F24 questions ship open — **SQ-986** is the one to read first: this milestone's own `balances` ruling is **not expressible** with the current sampler types, since `ChainRead` takes no block

## **F15 ✅** · **F26 🔨 (new)** · F1/F11/F13/F14/F17/F18 · contract **v29** · SQ-598/601/602/617/618/620/621/730/731 resolved · SQ-627…629, SQ-1003…1017 filed
span: 2026-08-07/08

**Ten PRs merged. The load-bearing find: one missing decision held four milestones and nobody owned it.** `probeCriticalSurface` was defined once and called **zero** times and `createClient` was constructed nowhere, so 10 §5.2's classifier had never run on either chain — while `check-client-surface-obligations.py` enforced the freeze that classifier exists to consume. `probe.ts` waited on F6, the 2026-08-04 log assigned it to F7, PLAN called it an F3/F7 decision, and **F3/F4/F6/F7 are all ✅** (SQ-1010). **F26** carries it now, and its sharpest find is that `CompatSurface` declared four of PAPI's six groups while `FOREIGN_SURFACE`'s frozen 02 §7.7 entry is a **call** — so the deposit leg's own dispatch was the one AH surface the check could not examine. **Contract v29** resolved nine questions as one package: `bond_quote` publishes the **bond** not the exposure (R-7 rounds against the claimant who posts it), 02 §7.6's text does not move because a published projection is not raw storage, and §11.8.2 gains one row per `PlaybookTrigger` variant — the prose had named five of eight. Its own repair then opened a **5-of-7 playbook activation whose trigger was never evaluated**, because retiring a `blocking` obligation removed the only thing closing that control. **Every review found a check that had quietly stopped checking**: an anti-vacuity test on renamed text, a quota manager with no caller, `TRIGGER_READS` with no consumer, `app/pnpm-lock.yaml` audited by nothing while TH-44 named the control, a runtime-cache eviction its own test read as boundedness, and a mutation test asserting through a local copy of the function under test. **Three were mine**: a union keyed on a substring inside another line's prose deleted a gate's docs while one-sided assertions passed; a merge guard counting non-SUCCESS checks would have merged an unregistered run; and a scripted edit of *Current focus* deleted **3,485 lines** of this file, caught by a line count and restored from git. Also closed SQ-985's npm half (TH-44's control had no implementation), ruled SQ-1009 and implemented it (#278), and learned that a **`DIRTY` PR triggers no CI at all** — #273 sat an hour showing a stale `5/5 SUCCESS`.

**What comes next:** Merge #278 and #279; then Track F is externally blocked throughout — chain-spec bytes, a device lab, the key ceremony, seated roles. **SQ-940 and SQ-994's membership need the user**
