# Session log — 2026-08-14

## Dependabot remediation

Inspected the merged security-remediation state and traced the remaining npm alert to the isolated
Lighthouse render-budget workspace. Upgraded Lighthouse 12.8.2 to 13.4.1, which replaces Puppeteer
with `puppeteer-core` 25.7.0 / `@puppeteer/browsers` 3.2.0 and removes `extract-zip` 2.0.1 plus the old
`@opentelemetry/core` path. Re-verified that Lighthouse still has no default FMP audit and retains
the §9.4 preset inputs, then moved the Node floor and both immutable CI images to 22.19.0. Updated the
owning spec and derived mirror, resolved SQ-1006, and deleted both obsolete npm waivers.

The full supply-chain scan then found a separate high advisory in `nanoid` 3.3.17 through Vite's
PostCSS dependency. Added an exact 3.3.18 workspace override and refreshed the lock. The final
six-workspace supply-chain gate is green: zero npm advisories, with every existing RustSec,
GHSA-only and unsound-advisory waiver still valid. On this arm64 macOS workstation the gate used an
official OSV Scanner 2.4.0 Darwin binary via `BLEAVIT_OSV_SCANNER`; its default bootstrap artifact is
Linux-only.

Frozen install, app build/typecheck, release build, the real Chrome render-budget gate and its
witness are green. The render run measured desktop FCP at 0.71 s median/tail and mobile at 3.68 s
median / 3.69 s tail; the mobile p50 warning remains visible and its tail is below the 6 s hard
limit. All 401 CI-tooling unit tests pass with the repository-pinned PyYAML 6.0.2, as do the 57
focused advisory/reproducibility tests, 18 budget tests, 215 release tests, chain-client suite,
workflow action-pin check, frontend-budget derivation, plan rendering/tables/question-batches and
`git diff --check`. V-390 records the dependency graph and verification.

The user subsequently authorized a branch and publication handoff. A blanket commit created during
that handoff captured the untracked 380 MB root `.pnpm-store/` (18,347 derived content-addressed
files), and the same accidental tip appeared on the remote branch. The local branch was repaired
before handoff: `.pnpm-store/` is now root-ignored, removed from Git's index without deleting the
local cache, and the commit rewritten to contain only the intended remediation plus that guard. The
remote correction therefore requires a force-with-lease push; Codex did not perform that push.
