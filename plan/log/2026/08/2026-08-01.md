# Session log — 2026-08-01

## S7, S8, S9, S10 (partial), S11

**The S6 grounding method run across every area of the spec that had no executable form — 11 new reference-model modules, 1 new simulation module, 3 extended, 1 monitoring co-simulation; `reference-model` 290 → 611, `simulation` 53 → 68, `tools/monitoring` 107 → 108. No Rust and no spec text touched.** Pipeline: a 21-agent audit measured which spec sections the Python trees cite (docs **01, 06, 09, 10, 11, 14 had zero citations anywhere**), hunted claims *about behaviour over time or over a space of inputs*, and adversarially verified 51 candidates; then **15 Codex authors** (`gpt-5.6-sol`, `xhigh`) implemented them from the documents alone — one isolated git worktree each, because R-13's snapshot/restore warning means parallel `workspace-write` jobs in one tree silently revert each other. Sandbox mode read back from every job log. **18 findings raised as SQ-543…SQ-560 (batch B7), none decided in passing.** Seven are wedges or unsafe-direction errors and three of those are live in shipped Rust: the chain cannot fund a CODE upgrade at the shipped `pol.budget_epoch` (SQ-543), cannot leave Phase 3 (SQ-544), and the expedited CODE lane force-rejects the proposal it exists to ship while its published latency is unreachable at every lawful configuration (SQ-545). **Three lessons worth carrying.** (1) **The audit was wrong often enough to matter** — Codex corrected a load-bearing fact in 9 of 15 briefs, including four that changed a conclusion (PARAM is not an upgrade-payload class; the cheapest blanking attack is 500 USDC/epoch via five PARAMs, not 5,000 via one META; the expedited worst case is 82 d, not 46, once every lawful `epoch.length` is swept; and the Phase-0 `AttackCost̂` leg did **not** "measure nothing" — it measured 62 causal flips and dispositioned them, so the proposed violation would have failed a spec-compliant artifact). Delegating the *implementation* is also delegating a second reading of the finding, and that is where most of the value came from. (2) **A green isolated worktree is not an integrated green tree.** `registry.py` passed alone and failed on merge: its kernel-hygiene scan walks the repo for Rust consumers, and this repo has worktrees checked out inside it at `.claude/worktrees/`, so a duplicate `futarchy-primitives/src/lib.rs` made the `QUOTE_CLAMP_*` orphans read as consumed. Fixed by excluding `worktrees`/`.git` from the scan; **CI would never have caught it** because a clean checkout has no nested worktrees. (3) **Green is not the bar — mutation is.** Perturbing a real module constant turns the owning suite red in every case tried (`POL_BUDGET_DEFAULT` → 39 failures, `GATE_P_MAX_DEFAULT` → 13, `BYTES_PER_MB` → 14, `GUARDIAN_SEATS` → 2), and the orphan-scan fix is itself mutation-proven. Falsified claims use the SQ-527 pattern (assert the derived truth, name the SQ in the docstring, expose the defect through a `check_*` findings accessor), so the suites are green rather than red-by-design. Zero float/random/wall-clock across all 11 modules; `git diff --check` clean; vector freshness, LMSR doc-table, limit coverage (194 keys, unwired 0), PLAN tables and spec-question batches all green.

**What comes next:** **Take the rulings, starting with SQ-543 and SQ-544** — both make a documented path unreachable and both are values-layer calls the user owns; SQ-546/547/548 are one family and should be ruled together. Then the **deliberate omissions**, which are omissions and not oversights: the S10 engine repair (attacker best-response allocation, Baseline-anchor coupling) changes the committed `bleavit.sim-calibration.v1` artifact that G0 consumes fail-closed and needs SQ-549/SQ-550 ruled first; and `market.py`'s V5 integer-conformance residue touches the generated normative vector corpus, the one change in this set with real blast radius. Nothing is uncommitted-and-unexplained: the tree is 30 files, all additive Python, on `main` and not yet committed

## S7–S11 (PR #206 review round)

**Two review rounds on #206; the reviews were worth more than the PR's first pass and one of my published findings in three needed correcting.** **(1) The Codex connector bot's single P2 was real but pre-existing**: `coretime_annual_cost_path` (E7, `778ff42`, byte-identical to `main` — my diff only moved its lines) carried a comment promising to carry the fractional renewal and then scaled the spend instead, so the `do_renew` recurrence ran one advance short per ~23 years. Verified before fixing: at the shipped 3 % cap the ceiling binds at advance 157 (year 12) while the dropped advance first appears at year 23, so it always landed in the saturated region and **every published 08 §10 figure moves by < 0.01 years** — the bot's "every subsequent ratchet state is priced incorrectly" is right as a mechanism and overstated here. It bites the *slower* ratchets: at a 1 % cap saturation is at advance 464 and a 25-year line is understated **0.70 %**, and understating cost overstates runway (R-7 unsafe). Fixed with the discrete carry + 5 regression tests; two pre-existing anchors corrected to the discrete truth (99,964.41 not 100,000; 398,631.07 not 400,000) rather than widened. **(2) Three commissioned adversarial reviews found more than the bot did, and the theme was uncomfortable: my own briefs' independence rule was violated by the modules those briefs produced.** `threat_costs` and `frontend_budget` took generated Rust weights as their *oracle*, so a legitimate benchmark regeneration would break the "specification" model or make the runtime the spec; both are now parameterised by a documented fee/weight with the measured value demoted to clearly-labelled `observed_*` implementation evidence. Several falsification tests pinned the **stale published value** (`10,900`, `196`, `BadPreimage`), so a *correct* spec edit would have turned them red — the exact trap the briefs warned against; they now assert the derived side and let `check_*` carry the discrepancy. **(3) Four published findings were over-claimed and are corrected in their SQ rows before any ruling was taken** — I re-read the owning spec text myself rather than take the reviewer's word, and the reviewer was right on all four. **SQ-547**: rule 7 says verbatim those couplings || "bind at the consuming engine", so this is *implementation non-conformance*, not a spec gap; `welfare.wP + wA = 1` is compliant; and 3 of my 8 couplings were never stated in any document (08 §1.3 makes the treasury caps *concurrent*, never ordered). **SQ-548**: 04/05 define trailing-window *containment*, so `trailing == window` is not a breach. **SQ-558**: 06 §5.4 says "50 % of bond", which on the live-bond reading never reaches zero. **SQ-560**: the spec establishes concentration and custody, not coordination, and A-3 is about capture. **(4) Four reproduced bugs fixed and re-verified**: `registry`'s `rglob` descended into `target/`/`.git/`/`.claude/worktrees/` before its exclusions could act (a reviewer reproduced `FileNotFoundError` on a transient build file) — now a pruned top-down `os.walk`, 1.65 s; `slate`'s two `read_text()` calls lacked `encoding=` and raised `UnicodeDecodeError` under `LC_ALL=C` — the whole suite now passes under the repro conditions; `threat_costs`' weight parser used `re.search` and silently ignored extra terms, so a synthetic function with a bogus 999 M ref-time term still returned the committed tuple; and an exact gzip byte assertion was compressor-dependent (Python zlib 106,875 vs GNU gzip 106,893 on identical input) and is now a spec-relevant bound. Added `spec_values.py` + a cross-module consistency test with a mutation control, because the same normative value was duplicated across modules with nothing binding them — the shared constants are deliberately **not** imported everywhere, since a shared import would make that test compare an object with itself. **(5) One more found by checking the fix itself, which is the reason to re-verify a merge rather than trust five green worktrees.** The new cross-module consistency test was **partly vacuous on arrival**: routing NAV floors and `epoch.length` through `spec_values` made every listed consumer hold the *same object*, so those groups compared an object with itself and could never fail — and the shipped "mutation control" perturbed the **returned dict** rather than a module, proving only that the comparator compares. Independent mutation confirmed it: drifting a module's floor left the suite green. Replaced with three guards that do fire — groups are classified single-homed vs duplicated, a duplicated group must hold ≥ 2 independent statements (so routing one more module through `spec_values` reds the suite instead of silently retiring a check), single-homed values are grounded against the **owning document** by parsing 08 §4.1, and the mutation control now patches a module attribute and re-collects. Both mutations verified red. Suites: `reference-model` 611 → **630**, `simulation` 68 → 69, `tools/monitoring` 108.

**What comes next:** Await CI on the pushed head; the earlier run was green 15/15 with 4 in flight at the time of the second push. Then the rulings, unchanged in priority: **SQ-543 and SQ-544** first (each makes a documented path unreachable and both are values-layer calls), then the SQ-546/547/548 family together — noting SQ-547's disposition is now code rather than doc

## SQ-561 (security fix, unplanned)

**The 07 §3 permanent reporter ban was defeated by arithmetic, and the fix belongs at entry rather than at ejection.** Found while refuting the Track N design's claim that external settlement cannot run in `pallet-oracle` — one of that argument's three reasons was reporter-registry contamination, and checking it surfaced a live defect in shipped code instead. `MAX_REPORTER_RECORDS = MAX_REPORTERS = 64`; the eviction rule correctly never drops an ejection, so the store reaches a state where `upsert_reporter_record` returns `false`. `record_reporter_offense` then emits `ReporterRecordsFull` and **releases the seat anyway** — correctly, because G-1 forbids a full table from aborting a values-track verdict — but with no retained row `register_reporter`'s `carried` lookup reads `None`, so the account re-enters at **full** stake with `offenses = 0`, skipping §2(5)'s half-stake re-entry too. §3's "MAY NEVER re-register" is unconditional; the code was not. The structural tension underneath is real and now stated normatively: permanent bans accumulate without bound, I-20 says state does not, and the spec had never said which side gives. **It gives at entry.** The verdict path keeps failing open; `register_reporter` refuses any account with no retained record once nothing is evictable (`ReporterRecordsSaturated`), because an unrecorded account is then indistinguishable from a dropped ban. Seated reporters are untouched so the live set keeps serving, and the only escape is a CODE change enlarging the store — the right venue, since the size is a consensus constant whose growth is a PoV cost. Gate and drop share one `evictable_record` helper so they cannot drift. Regression test drives the **real** ejection path and initially failed *during setup* — the gate refused to seat the victim — which was itself the proof the gate is preventive: once saturated nobody new is seated, so only an already-seated account can take the 65th ejection. Reordered to that sequence. Gates: `rust-workspace-gates.sh --changed oracle-core pallet-oracle` exit 0 (fmt, clippy `-D warnings`, 53 + 126 tests); plan-tables, spec-question-batches green.

**What comes next:** Open as its own PR against `main`; Track N continues on a separate branch

## SQ-562 / SQ-563 (spec + model fix, unplanned)

**A published security figure was 1.928× too high, in the direction that makes the chain look safer than it is.** Found while verifying the Track N plan's claim that `ManipFloor̂` could be resold to external clients — the bound turned out to need fixing before it could be reused. 05 §5.6's `C_disp` expands to `b·(logit(p̄+δc) − logit(p̄))`, and 04 §3 says in one line that this is the **displacement** in *shares*, while the **cost** is `b·ln((1−p)/(1−p′))` in *USDC*. 04 §3's own V3 vector prints both for the same move — 4,054.65 shares against 2,231.44 USDC — so they are neither equal nor proportional, and `C_hold` is USDC. **Why it matters beyond dimensional tidiness:** the quantity is a *lower* bound on attacker cost, and 05 §5.6's escape clause fires when the published floor reads **below** `3·InCapPrize`. Overstating the floor therefore delays the chain's own security trigger. At Bleavit's PARAM pair it read 3,005.64 against a true 1,559.23. **Two implementations existed and disagreed by 1.928× with nothing comparing them** — the simulation was already right (`_signed_manip_floor` → `lmsr.displacement_cost`), the reference model faithfully reproduced the spec's error. The durable half of the repair is the cross-check test, not the formula edit: it pins the corrected value, asserts the superseded expression really was >1.9× high, and re-derives the simulation's two-sided legs to prove agreement. **A second finding fell out and was deliberately not fixed:** §5.6 calls this a mandatory diagnostic the engine "computes and emits, per decision", and no Rust implements it — there is no `DecisionDiagnostics` type anywhere. So a MUST-obligation on the values layer names an input with no producer. Filed as SQ-545 (batch X) rather than folded in: emitting it is 02 contract surface plus an unbudgeted `ln` on the decide path, and the correction here must stay reviewable on its own. Gates: reference-model 291 tests, simulation 53, `generate-vectors.py --check` clean (no corpus row consumes this function), `check-doc-table.py` green, plan-tables + spec-question-batches green.

**What comes next:** Write N1 — the Track N spec batch — now that its §5 rests on a corrected bound

## N1 (Track N opened — futarchy as a service over XCM)

**New track opened on the "make the futarchy infrastructure accessible to other parachains, smart contracts and services via XCM" work order; design approved, spec batch not yet written.** Scope settled with the user across six questions: hosted decision markets; **price discovery is the product, not decisions** (client's own rule decides and executes locally, so 05's `W` stays Bleavit-scoped and I-24 stays intact); v1 scope, so the contract bump and threat rows land in the same change set; the client reports the realized value under a bonded challenge game with a neutral failure edge; both delivery legs (pull surface **and** XCM push); a governed bonded client registry; a fee on every market created for a client; and integration ease plus human client documentation as first-class deliverables. Design ran as three read-only exploration agents then two independent architecture passes, and **the second pass corrected two things in my draft, both adopted**: the ledger becomes a second FRAME *instance* rather than a third vault family (I-4/L-2 is stated against *the* sovereign account, singular, so shared custody masks an external-domain deficit until Bleavit's own traders are already unbacked), and settlement runs in its own pallet rather than inside `pallet-oracle`. Verified directly against the pinned SDK, not taken on report: `Transact` is filtered **twice** (`staging-xcm-executor-28.0.0/src/lib.rs:1105` `SafeCallFilter`, then `call.dispatch` through `BaseCallFilter`); XCM v5's `Response` (`staging-xcm-24.0.0/src/v5/mod.rs:268-281`) carries no arbitrary data, so `QueryResponse` is structurally not a data channel and push must be an outbound `Transact`; the ledger already carries two disjoint vault families (`Vaults` by `ProposalId`, `BaselineVaults` by `EpochId`, folded separately by try-state); `market::create_market`/`seed` already take the subsidy source as an argument, so client-funded books need no seeding-logic change; `PhaseFlags` bit 8 is inside `RESERVED_MASK`. **No spec text written yet, deliberately** — four read-only `codex exec` jobs (`gpt-5.6-sol`, `max` effort, `--sandbox read-only` verified from each job log per R-13) are refuting the load-bearing numbers first, because R-2 forbids writing normative text on unverified figures. **One independent finding already, ahead of those verdicts:** the design pass's revenue table rests on `H_q ≈ 3S/ε` (60·S at ε = 0.05), which can only come from setting `C_hold ≥ 3S` — but certification deliberately counts **only** client-funded `C_disp` and refuses to count measured depth, so `H_q ≈ 60·S` is an assumption about organic trading depth, not a consequence of certification. If held capital instead tracks what the client posts, revenue per question falls by roughly an order of magnitude and instrument D is **more** load-bearing than the "≈ 2 % of what hosting earns" line claims. Tree state: `CLAUDE.md` (Codex model/effort → `max` + the parallel-worktree rule) and this PLAN.md entry; **no code, no spec files touched**; docs gates baselined green (doc-links, plan-tables, spec-question-batches, runbooks).

**What comes next:** Read the four refutation verdicts and reconcile them against my own `H_q` derivation; fix any figure that does not survive; then write N1 — doc 16 + 09 §6.5 + the amendment batch + D-20 + contract v20 + TH-66…73 + I-34…38 — as one coherent change set, followed by the R-3 sweep (AGENTS.md's three "00–15" mentions, README repo map, `docs/design/` regeneration). N2 (ledger instancing) lands alone after it

## N1 ✅ (Track N spec batch)

**The verification round changed the design before a line of it was written, which was the point of running it.** Four read-only `codex exec` jobs (`gpt-5.6-sol`, `max`, `--sandbox read-only` per R-13) were asked to *refute* the approved plan. Three of four verdicts refuted load-bearing claims, and two `main` defects fell out that shipped as their own PRs (#207 SQ-543, #208 SQ-544) before N1 was written — SQ-544 because plan §5 proposed reselling `ManipFloor̂`, and a bound with a unit error cannot be resold. **What the refutations changed.** (i) The "three independent reasons" external settlement cannot run in `pallet-oracle` became **two**: the coverage rule *is* satisfiable (`ORC_ROUNDS_MAX = 4` ⇒ 667 ≤ 1,000 bps) and the `MAX_ROUNDS` wedge was simply wrong. The surviving argument is stronger — the discipline keys are chain-wide, so hosting is possible **only by degrading Bleavit's own oracle economics**. The three false arguments are recorded in 16 §6.2 rather than deleted, because a boundary defended by a wrong argument is one correction away from being reopened for the wrong reason. (ii) `b_min` is **1.928× larger** than drafted (36.75/14.24/6.73·S at ε = 0.02/0.05/0.10, not 18.74/7.48/3.70) — the same unit error propagating, and rounding is up because a `b` rounded down is a certificate that does not hold. (iii) The revenue case **inverted**: `H_q ≈ 60·S` rested on `AttackCost = εH`, which this repo never states; the derivable quantity is the client's own forced escrow, `2·b_min = 28.5·S`, making instrument **D ≈ 20 %** of evidenced per-question revenue rather than 2 %. So the per-market fee is far more load-bearing than the first draft made it look, and "the external order flow is the revenue" is now stated as a hypothesis 15 §4.9 **cannot test**, never as a forecast. `keeper.rebate` is 0.000255 (0.000085 is the fee *basis*), so marginal cost is 15.42 not 5.14 USDC. (iv) `ε_max` was **deleted from the product**, not caveated — it is not an upper bound under the real backward-crediting accumulator, and a false security claim is worse than none. (v) Three money-movement corrections landed: the three-way `ProtocolAccounts` split, `FundingDomain` (not `Option`), and the refund blocker — `withdraw_book` requires both parties be protocol accounts, so a correctly non-protocol client cannot be refunded at all. (vi) The `H` channel is closed by a **resource partition**; naive weight subtraction is unsafe because external calls could then fill blocks while `H` reads healthy. **Also fixed in passing:** 02 carried *three* different stale version numbers (18/18/17 against a true 19). v20 is authored **not-in-force** per the v17/v18 precedent, since N1 ships no code. **Scope delivered:** doc 16 (13 sections) + 09 §6.5 + amendments to 00/01/02/03/04/05/06/07/08/09/12/13/14/15 + RB-SERVICE + three alert rows with pinned Prometheus expressions and N9-owned seams + the R-3 sweep. Gates: plan-tables, doc-links, spec-question-batches, runbooks (14/24 bound), alert-coverage (24 rows, 51 metrics, 9 seams) and all seven tooling suites green. **No Rust touched** — verified by an empty `git diff -- '*.rs'`.

**What comes next:** **N2 — instance the conditional ledger.** It lands **alone**: highest-risk refactor in audit scope A, instance `()` must stay byte-identical, and the gate is every existing ledger test + PT-1…PT-8 + TLA⁺ + the full differential corpus green with no diffs

## N1 review pass (self-conducted after the Codex job was refused)

**Working the adversarial-review axes by hand found two real defects in my own N1, and the Codex provider refused the job midway.** The read-only review agent (`gpt-5.6-sol`, `max`, `--sandbox read-only` verified) was killed by the provider's content filter — *"flagged for possible cybersecurity risk"* — while reading `staging-xcm` weight/trader source under a brief asking it to *attack* the ingress template. A false positive on defensive review of this repo's own posture, but the job is lost. CLAUDE.md's fallback rule is written for capacity walls and applies in spirit here: **losing the provider must not lose the pattern**, so the axes were worked directly rather than dropped. Disclosed here as that rule requires. **Two defects found, both mine.** (i) Doc 16 §3.1 cited *"06 §1's eight-origin closure clause"* — **the clause is in 01 §6**, not 06. A citation that does not resolve is worse than no citation, because it reads as verified. (ii) The clause states that none of the eight is obtainable from *"a signed extrinsic, **XCM origin conversion**, or wrapper call"* — and N1 introduces exactly an XCM origin conversion. It stays **literally true** (the new origin is a *ninth*, in a different pallet, with a different `OriginCaller` variant), but the plan promised an appended scoping paragraph and I had put a row in 06 §3.2 instead and never written it. Now appended at 01 §6, and it carries the load-bearing half: `EnsureFutarchyOrigin` succeeds for *any* `pallet_origins::Origin`, so a client origin inside that enum would have made the closure depend on a filter, whereas a separate pallet makes `try_origin` fail **structurally**. **Axes verified clean:** no "exactly two" XCM claim survives; I-34…38 each defined exactly once; TH-66…73 each exactly one row; and the three arguments 16 §6.2 records as *false* really are false — `ORC_ROUNDS_MAX = 4` gives 666.7 ≤ 1,000 bps (satisfiable), and `oracle-core:786` `ensure!(rounds.len() < MAX_ROUNDS, RoundLimit)` confirms 128 bounds *concurrent* games rather than wedging settlement. **Economics re-derived independently:** `b_min` 14.2368·S, escrow 28.47·S, `R_B` 0.04271·S, D's share 19.0 % at 100 bps, escrow 2,847,366 > `phase3.tvl_cap` 2,000,000 — all five match doc 16 §8.

**What comes next:** Retry the ingress/origin/segregation axes on Codex under a defensive framing; N4 and N5 are in flight

## N1 review (continued) — the nesting enumeration was 3 of 9

**The doc named three inner-program XCM instructions; there are nine.** Checked directly against the pinned `staging-xcm 24.0.0` `v5/mod.rs` rather than from recollection: `TransferReserveAsset` (488), `DepositReserveAsset` (626), `InitiateReserveWithdraw` (659), `InitiateTeleport` (675), `SetErrorHandler` (726), `SetAppendix` (742), `ExportMessage` (943), `InitiateTransfer.remote_xcm` (1111), `ExecuteWithOrigin` (1131). **The security claim survives intact** — the template is positional and exhaustive, so none of the nine is at an admitted position and all nine fail the match by construction. What was wrong was the *reasoning*, which enumerated three and thereby implied a deny-list of three would do. That is precisely the incompleteness the positional design exists to make irrelevant, so leaving it stated as a three-item list undersold the argument and could mislead an implementer into building the weaker thing. **Two omissions were worth their own mention and now have it.** `SetErrorHandler`/`SetAppendix` carry `Xcm<Call>` — with `Call`, so they can carry a `Transact` — and execute **locally** on error or completion. And `ExecuteWithOrigin { descendant_origin, xcm }` executes locally **under a descended origin**, reaching the sub-identity that keeping `DescendOrigin` unadmitted is supposed to refuse, without `DescendOrigin` appearing anywhere in the program. Both 16 §3 and 09 §6.5 now carry the complete set as a table, with the point made explicitly: the positional match enumerates none of them, which is the argument for matching shape rather than membership.

**What comes next:** Codex re-review running under a defensive framing after the first job was refused

## N1 — six Codex-connector findings on #209, all valid, all fixed

**The most consequential one is mine and it is the same class of error as SQ-544, one layer up: I conflated the LMSR liquidity parameter `b` with the cash that funds it.** [04](../../../../docs/architecture/04-markets-and-pricing.md) §2 mints "per-book headroom `b·ln 2`" and §3 sizes `b = SubsidyBudget / ln 2`, so a client's posted subsidy is `b·ln 2` per book, not `b`. Three published figures move and **one caveat reverses**: escrow **28.47·S → 19.736·S**, `R_B` **0.0427·S → 0.0296·S**, instrument D's share **19.0 % → 25.2 %** (the correction *raises* D, strengthening the conclusion), and at `S` = 100 k the escrow is **1,973,644 — under** `phase3.tvl_cap` = 2,000,000, where the superseded figure said no consequential question was reachable in Phase 3 at all. The corrected reading is thinner than it sounds and is stated that way: the cap is shared with every other inflow, so one such question consumes essentially all of it and `register` must meter against the **live** remaining cap rather than the constant. **A P1 security contradiction:** §5.2 defined `Certified` over `ManipFloor̂ = C_disp + C_hold`, but `C_hold` is a **measured** contest-capital term — so with `V_win > 0` an underfunded client could buy the published certificate out of *organic trader depth*, which is exactly the cannibalization §8.4 refuses. The predicate is now over **`C_disp` alone**; the published floor and the admission gate are different quantities with different jobs. **A P1 self-contradiction:** §6.4 listed "client deregistered" as a universal VOID edge while §2 promised removal preserves live questions. §2 wins — VOIDing on a values-track vote would change trader payouts and could destroy an unsealed report the client already paid for, on a vote the traders had no part in. The guardian pause stays the immediate lever, because it is bounded and is *meant* to VOID. **Three more:** instrument D said "per market created" and a question carries two books, so the literal rule charged **2×** what §8.2 justifies — now once per question; `location = Location::here()` cannot authenticate a local signer (it is identical for every one), so `ClientRecord` gains `local_signer: Option<AccountId>` with exactly one identity field set and an exact-match signed conversion; and the v20 §13 entry described §4/§6/§7/§9 additions that **were never written** — a changelog is not a surface, and two clients reading only it would encode v20 differently, so 02 now carries a **§4a** with the full `ReportView`/`SettlementTrust`/`QuestionPhase` layouts, the four event shapes, the three storage rows, the §7.1 instance-`()` scoping and the §9 constants, with the version still at 19. Added `svc.max_clients` (13 §1 + limit-coverage) because §7's `Clients` row needs a bound to cite.

**What comes next:** N4 in flight; then N6

## N1 — the re-framed XCM review landed 13 more findings, all valid

**Three of them were claims I had made that are simply false, and the worst was a requirement no implementation could satisfy.** (1) **09 §6.5 demanded one whole-program matcher shared by the barrier, the origin converter and `SafeCallFilter`. The SDK cannot provide it**: `ConvertOrigin::convert_origin` receives `(Location, OriginKind)` and `SafeCallFilter` is a `Contains<RuntimeCall>` receiving a decoded call — only the barrier ever sees the instruction slice. Stating it as "one matcher" would have been implemented as a lie or not at all. Replaced with the honest decomposition (three checks, three inputs, admission requires all three) and the test obligation moved to their **intersection**. (2) **I-34's "type-level fact" is not type-level.** The executor requires `ConvertOrigin<RuntimeOrigin>`, and `RuntimeOrigin` admits every constructor in the runtime, so no signature forbids `Signed`. It is a review promise **with a mechanical check attached** — a single `Ok(...)` expression plus the negative-origin matrix — and now says so. (3) **"A new SDK variant fails compilation" is not guaranteed** and appeared in four places (16 §3, 09 §6.5, I-35, TH-68). Retracted in all four: the real guarantee is the runtime refusal (not at an admitted position), backed by the `=`-exact pin and a test asserting the admitted set has exactly six positions. **Two more of mine that overclaimed:** 16 §1 said *"No client bytes are ever dispatched"* while the design admits a `Transact` — false, and replaced with the narrower true guarantee (only this service's own dispatchables are reachable; no client payload, predicate or decision rule is ever evaluated). And the SQ-545 gate had the wrong rationale after the `C_disp` fix: certification no longer derives from `ManipFloor̂`, so what SQ-545 blocks is the **`manip_floor` report field**, not the `certified` flag. **Consistency sweep, all fixed:** doc 08 §10 still carried the superseded escrow arithmetic *and* "per market created"; **02 had five more frozen-constant rows hardcoding `= 17 in force`** — the exact anti-pattern the v17 entry says it eliminated, leaking back through table cells; 02 §4a lacked `QuestionId`/`ClientId`/`VoidReason` definitions and a real `hosted_report` signature, so it was not byte-implementable as claimed; 03's normative `Config` listing was still non-generic with one `ProtocolAccounts`; 01 said "Fifteen component documents" and omitted the two new pallets; 00 D-20 and 09 §6.1 still named three inner-program instructions; TH-70 still defined certification over `ManipFloor̂`.

**What comes next:** N4 still in flight

## N4 ✅ + two reconciliations + SQ-547

**The agent found my brief was wrong, and said so rather than implementing it.** I had written "every dispatchable in this pallet classifies to `CallDomain::ExternalClient`". That is unimplementable for the two **administrative** calls: `admit_client`/`remove_client` need `ConstitutionalValues` authority (16 §2), and classifying them `ExternalClient` would both make that authority unreachable and violate I-35, which says the domain is reachable by no governance origin. They classify `ConstitutionalValues`; `ExternalClient` stays the sole **client-service** origin. **I-34 got a stronger proof than the one I retracted this morning.** The pallet's own `Origin` enum is matched exhaustively with a single arm, which *does* fail compilation on a new variant — unlike the `ConvertOrigin<RuntimeOrigin>` conversion, whose type-level claim I had to withdraw. Plus `Origin::max_encoded_len() <= 5`, guarding the `pallet-scheduler` `Agenda` concern that made `ClientId` a `u32`. **Two reconciliations against my own N1 edits.** (a) I had added `svc.max_clients` to 13 §1; the agent added `MaxClients = 64` to 13 §4 as the structural map bound. Mine is **redundant** — R-2 step 1 calls a key whose job an existing key does a defect — so it is removed and 02 §4a repointed at `MaxClients`. (b) I wrote the bond as **USDC** in 13 §1 while the implementation holds **native VIT** per the B19 precedent my brief pointed at; 13 §1 now says VIT. **SQ-547 filed on what that second one exposes**, which neither of us thought through: I-36 makes the bond the source of **prepaid XCM delivery fees**, and delivery is paid in an asset the router accepts — DOT or USDC — never VIT. "Prepaid from the client's bond" therefore names a conversion that does not exist at a price nothing publishes. The precedent is genuinely mixed (`att.bond` VIT, `orc.reporter_stake` 100,000 USDC), so it is a values judgement; the honest default is a separate small USDC delivery float, because it is the only option that introduces **no price**. Blocks N9, not N7. Gates re-run by me from a second worktree: `--changed client-registry-core pallet-client-registry bleavit-runtime` exit 0 — **419 + 5 + 14**; limit coverage 200 keys, 4 unwired; no ledger/market/epoch/oracle/XCM file touched. The agent's first two gate runs were **red** (clippy `too_many_arguments`; a mis-scoped `runtime-benchmarks` feature) and it reported both verbatim rather than only the green one.

**What comes next:** N6 (`pallet-market` external books) — its dependencies N2 and N5 are both ✅

## SQ-565 resolved — the bond and the delivery float are two balances

**Ruled rather than carried into N9, because a deferred decision is unfinished work (R-1) and this one was blocking a milestone.** N4 implemented the client bond as native VIT, correctly following the B19 (`pallet-attestor`) precedent my own brief pointed it at — and I-36 then spends that bond on **XCM delivery fees**, which are paid in DOT or USDC under 09 §6.1's trader and never in VIT. "Prepaid from the client's bond" named a conversion that does not exist at a price nothing publishes. **The ruling is (b): keep the VIT bond, add a separate USDC `delivery_float`.** Two balances are more to reason about than one and that cost is accepted deliberately, because it is the **only option that introduces no price**. (a) making the whole bond USDC would match `orc.reporter_stake` but discards the B19 hold discipline for a deposit that is governance-adjacent by nature. (c) charging VIT at a governed rate puts a VIT/USDC rate **on a fee path**, and 08 §9's placeholder rate is explicitly not fit for that — the same trap SQ-531 already sprang once on `keeper.rebate`, where a rate assumed rather than measured was wrong by 353×. **The failure mode is bounded and lands where failure was already permitted:** an exhausted float stops **pushes only**, and 16 §9 already makes the pull surface authoritative and push best-effort. So the new balance cannot create a new way to lose a report. Landed in 16 §2 (the `delivery_float` row plus the reasoning), 16 §9's I-36 clause and 15's I-36 row.

**What comes next:** N9 is unblocked; N7 is in flight

## N3 ✅ (`CallDomain::ExternalClient`)

**One enum arm, landed alone on the most sensitive crate, and the compiler found its own consumer.** `origins-core` gains a twelfth `CallDomain`, appended last. Two tests pin the two properties that define it and that pull opposite ways: `allowed_for` is **false for all eight governance origins** (I-35 — the governance surface and the client surface are disjoint by construction, which is what keeps 06 §1's eight-origin closure clause literally true) and **true for `None`**, the deliberate widening 16 §3.1 records, because off-chain services cannot send XCM and use the identical calls from a local signed account. The judgement call this milestone actually contained: **`ExternalClient` is left OUT of `is_privileged`'s exempt set**, so a client-domain call inside a proxy-ish wrapper is refused `PrivilegedWrapper` (I-10/I-11). The cost is real — an off-chain service cannot batch or proxy these calls — and it buys that no wrapper path can carry one under another account's origin. R-7 picks the refusal, and the trade is recorded on the variant rather than in a commit message. The runtime's S5 `matching_origin` went **non-exhaustive at compile time**, which is the behaviour the plan wanted from landing this alone; it now names the variant explicitly instead of folding it into a wildcard, so a contributor who later gives the client domain a governance origin has to delete a comment saying not to. Also carried: the local exhaustive-gate prerequisites in CLAUDE.md are real and were needed here — `CARGO_TARGET_DIR` off the ecryptfs `$HOME` (the wasm build hit the ~143-char filename cap verbatim), `LIBCLANG_PATH` pointing at a **real** `libclang-14.so.1`, and `WASM_BUILD_WORKSPACE_HINT`. Gates: `--changed origins-core bleavit-runtime` exit 0 — 10 + 418 tests.

**What comes next:** N4 (`pallet-client-registry`) — its dependency N3 is now ✅. N2 is in flight in its own worktree

## N5 ✅ (question-service functional core + independent model)

**Delivered the FRAME-free hosted-question kernel and its independently derived Python executable form.** The Rust crate is `no_std`, depends only on the fixed-point/primitives/codec layer, makes every lifecycle failure terminate in VOID, retains the sealed report on failure, assembles the full report, uses the corrected LMSR cash-cost expression with claimant-adverse rounding, derives `b_min` from the existing security factor, and validates identity-bound attestor quorums. The Python model independently re-derives the lifecycle, security figures, settlement ladder and report binding. Read-only compliance review found one cross-question sealing blocker; the API now returns an explicit refusal carrying the still-open question and report so the caller can void, and the re-review found no blockers. Required gates all green: `cargo fmt --all` (silent), changed Rust gate (18 tests), full reference-model suite (309 tests), vector freshness (silent). No pallet, runtime or XCM file changed. **Findings retained rather than hidden:** an even-quorum half-grid median is `0.500000000` in Rust and exact `0.5000000005` in Python because doc 16 does not select tie rounding; the tolerance source/type is not yet committed; and doc 16 binds report provenance semantically but does not freeze the preimage encoding/domain/hash.

**What comes next:** **N2 — instance the conditional ledger, alone.** N7 must freeze the settlement tolerance and median tie rule before FRAME wiring; N9 must freeze the provenance encoding/domain/hash before publishing the contract surface

## N5 ✅ + four spec rulings it forced

**The implementation pass found four places doc 16 left an implementer free to choose, and every reasonable choice differed from the others' — which is the state that produces a divergence nobody notices.** Ruled under R-1 rather than deferred: (i) an **even** quorum settles on the arithmetic mean of the two central values **floored to the 05 §4.4 `1e9` grid** — dispositive reason first, that an unfloored mean is not a representable settlement value at all, and only then the R-7 direction; (ii) the median is over **every** in-window submission from a named attestor, never "the first ⌈n/2⌉", because a first-`q` rule is **order-dependent** and transaction ordering is controlled by neither the client nor Bleavit — a collator could select which attestors count; ⌈n/2⌉ is the threshold to settle *at all*; (iii) the deviation **tolerance is a per-question field frozen at registration**, kernel-bounded, not a `Params` key — a values majority able to widen it after trading opened could retroactively excuse an attestor the client had priced (the `SECURITY_FACTOR` argument, TH-73); (iv) `provenance_hash` is **`blake2_256` over a domain-separated SCALE preimage** (`b"bleavit/hosted-report/v1"`) and is 02 contract surface, frozen with v20, because clients verify reports by storage proof cross-chain. **The divergence was already live and the agent reported it rather than smoothing it**: reports `0.4` and `0.600000001` gave `0.500000000` in Rust and `0.5000000005` in Python. Ruling (i) makes Rust right. **Delivered:** `crates/question-service-core/` (lifecycle · math · report · settlement · errors, 18 tests) + the independent `reference-model/.../service.py` and its suite. `b_min` reproduces the published table to full precision — 36.7448973924 / 14.2368323715 / 6.7221301765 → **36.75 / 14.24 / 6.73 ×S**, rounded **up** — and `ManipFloor̂(b_min) = 3.000000` exactly, so the certification relation binds at equality rather than approximately. **`ε_max` correctly absent** (16 §5.1). Gates re-run by me from a second worktree: `--changed question-service-core` exit 0, reference-model **309**, `generate-vectors.py --check` clean, no pallet/runtime/XCM file touched.

**What comes next:** N4 still in flight; N6 unblocks on N2 + N5

## N2 ✅ (instance `pallet-conditional-ledger`)

**Completed the audit-scope-A generic-parameter refactor without adding a second runtime instance or changing behavior.** Mirrored `pallet-registry`: `Config<I: 'static = ()>`, `Pallet<T, I>`, instance-keyed storage/event/error/hooks/calls/migration and `#[instance_benchmarks]`; runtime and ledger mock bind `Config<()>`. Only `pallets/conditional-ledger/src/{lib.rs,benchmarking.rs,mock.rs}` and `runtime/bleavit-runtime/src/configs.rs` changed; `conditional-ledger-core`, all ledger test sources, generated weights, market, epoch, XCM and `construct_runtime!` are untouched. Before/after pallet count is exactly 86/86 (68 `tests.rs`, 15 `property_tests.rs`, one differential sweep, two mock tests); core JSON differential corpus is 5/5 both times. The full 1,000,000-case PT-1…PT-8 shard passed 15/15, vector freshness is clean, and the TLA⁺ gate passed all models and falsifiability witnesses. `cargo fmt --all` and `rust-workspace-gates.sh --changed pallet-conditional-ledger conditional-ledger-core bleavit-runtime` are green. The release runtime still emits `ConditionalLedger` at index 52 with storage prefix `ConditionalLedger`; its full module digest is byte-identical before/after (`925450f8c256bbb67b47bf4196bfe4ba97236b7ba33b579dda48dbf0ff181056`).

**What comes next:** N4 (`pallet-client-registry`). N3 reached `main` first (#210), so this branch was rebased onto it rather than landing before it — both prerequisites are now in the tree

## Rebase onto N3, and two things `main` should not be carrying

**Rebasing #211 onto `main` after #210 landed surfaced a leaked scratch file and a duplicated log entry, both mine, both cleaned up here because this is the next PR to merge.** (1) **`resolve.py` — a 1,051-byte throwaway PLAN.md conflict-resolution helper — is in `main`**, committed by accident on #210. It is not a tool, has no tests, no CI binding and no reference anywhere in the repository; it was a one-session convenience. Deleted, and the replacement kept in the session scratchpad instead, which is where it should have been the first time. (2) **The session log carried two entries twice**, once under the ids they were written with and once under the ids they were renumbered to when #206 minted SQ-543…SQ-560 ahead of them. The stale copies were not merely redundant: `SQ-543` and `SQ-544`/`SQ-545` now name #206's own questions (`pol.budget_epoch` funding, `phase3.tvl_cap` reachability, the expedited CODE lane), so a log row describing the oracle ban-store fix under `SQ-543` **misattributes work to an unrelated question**, which is worse than a duplicate and survives every checker — the batch gate validates the question *table*, not the log. Removed the two stale copies; the correct rows (`SQ-561`, `SQ-562`/`SQ-563`) stay. Also dropped a note in the milestone preamble that had become false in the opposite direction: it told a future session that `origins-core` here has no `CallDomain::ExternalClient`, which was true only while N2 and N3 were unmerged siblings. **Nothing in the N2 refactor itself changed** — the rebase replayed it and the conflicts were confined to PLAN.md.

**What comes next:** Rebase #212 (N5) onto the same `main`; then the N6/N7/N11 cascade

## N4 ✅ (bonded client registry)

**Built the external-client capability boundary as a frame-free core plus a FRAME custody shell, and implementation review corrected the spec where its authority rows conflicted.** The core carries pure bounded record/counter transitions; the pallet owns exact-equality `Location`↔`ClientId` indexes, monotone ids, native VIT holds, per-client ingress counters, tombstoned drain-safe removal, `try-state`, benchmarks and generated weights. Runtime index **65** binds strict guardian-track `ConstitutionalValues` administration, and the optional live `svc.client_bond` read returns `ClientBondUnset` before any hold or write while the `[VERIFY]` row is absent. I-34 is reviewable in one test: the custom origin has only `ExternalClient(ClientId)`, every signed/root/none/eight-governance alternative fails, and exact-location reverse lookup is the only conversion seam. The N1 authority table had grouped roster administration into the client call domain even though 16 §2 requires values governance and I-35 gives no governance origin access to that domain; split and recorded deliberately. Added `MaxClients = 64`, derived from the hard `svc.max_live` ceiling. Canonical 50×20 weights generated. **Gate history is intentionally not polished away:** first required gate was red on core `too_many_arguments` and was repaired with typed `AdmissionContext`; second was red because a dev dependency leaked `runtime-benchmarks` into stock FRAME pallets and was repaired by feature-scoping it; the final exact gate exited 0 with runtime **419**, core **5**, pallet **14**. Limit coverage is green at **200** keys (four N7-owned service keys remain unwired). Independent test and spec reviews found no blocker. R-3 sync updated architecture, living documents and the derived design pack.

**What comes next:** N5 is next in this work line; N2 continues in its isolated worktree

## N6 ✅ (`pallet-market` external books)

Implemented `BookKind::External` as an atomic Accept/Reject pair over the service ledger route; disjoint 128-book live/retained counters and 2,368-row physical bound; typed `FundingDomain`; exact-funder-only subsidy return; MAIN-owned trading/redemption fees; pair-aware dust/reap; `SERVICE_ID_BASE` try-state; contract v20 + release manifest; generated weights and reference/limit registries. R-7 review found and fixed three additional money paths before closure: pair seeding now locks both same-branch halves instead of leaving client-mergeable inventory; the FRAME rollback test now reaches a successful first split before making the second fail by one unit; and `buy`/`sell`/`crank_observe`/Sweep select their owning instance's I-4 freeze through `LedgerRoute`, with failed service Sweep proven to leave MAIN, recognition, funder, escrow and marker unchanged. Shared PB-DEPEG creation freeze remains global. Independent final compliance review: 0 blocker/0 major/0 minor. Exact required gates green: fmt silent; changed Rust gate exit 0 (runtime 419, conditional ledger 87, market 106, cores/differential/property/vectors); limit coverage 204 keys with the four N7-owned `svc.*` rows explicitly unwired; reference model 649. No forbidden pallet/XCM file changed.

**What comes next:** N7: bind the real `Instance1`, implement question lifecycle/settlement atomicity and activate service capacity; N9 owns remaining hosted-report v21 surface

## N7 implementation complete / resource partition residual

Built `pallet-question-service` at index 66 and the first real `pallet-conditional-ledger::<Instance1>` as `ServiceLedger` at 67. Registration atomically funds two external books and service escrow, freezes the client terms/attestor set/deadlines, charges instrument D once and earns it only at Sealed; unset `svc.fee_bps` is the arming refusal. Settlement uses the N5 four-ruling core, named bonded submissions and terminal VOID on failure; clock-driven VOID is permissionless; registry removal does not void; pause does. The pallet hashes the exact domain-separated SCALE provenance preimage with `blake2_256`, and refuses seal when no real manipulation diagnostic exists. Contract v21 adds the exact §4a `hosted_report` surface without shape changes. I-37 is enforced by a union reserved predicate, per-instance local predicates, separate inflow exemption, disjoint id/account bands and instance-selected freeze/custody routes; the client is asserted outside all three. Try-state, benchmarks, canonical weights, PT-9, Instance1 differential, TLA⁺ and fuzz coverage landed. Final requested gates are green: fmt silent; changed Rust runtime **422**, ledger **87**, Instance1 **8**, PT-9 **7**, question pallet **26**, core **18**; limit coverage **205/0 unwired**; reference model **652**. The registry-count change is exactly three seeded keys: `svc.max_live`, `svc.max_window`, `svc.epsilon_min`; `MaxServiceAttestors` is a new structural classified row, not a seeded ParamKey. The final static scan contains no added `unwrap`/`expect`/`panic!`/`unsafe`; runtime-benchmark tests are **118/118**, and the four-target fuzz corpus/smoke gate is green outside the ptrace sandbox. A final post-cleanup Rust rerun first failed at the documented ecryptfs Wasm path limit (`File name too long`) because the short-target environment was omitted; the prescribed `/tmp` target, real `libclang.so` and workspace-hint rerun passed with the counts above. Independent test/spec reviewers found no blocker inside the authorized scope. No oracle, epoch, welfare or XCM file changed.

**What comes next:** Keep N7 🔨 and the service inert; explicit authority is required to implement 16 §8.4–§8.5/PT-10's welfare-owned resource partition, then re-run PT-10 before arming. N8/N9 remain next only after that dependency is honestly resolved.

## N7 rebase onto `main` found two defects the differential could not see

**Cascading N6/N7 onto `main` after N2/N3/N5 landed surfaced a dropped contract field and a red freeze test — neither caused by the rebase, both live in the branch.** (1) **`tolerance_1e9` was missing from the Rust `ReportView` and from the provenance preimage on *both* sides.** 02 §4a declares it between `epsilon_1e9` and `certified` and states exactly why it is contract surface — settlement takes tolerance as an *argument*, so without it in the hash a widened value could excuse an otherwise-slashable submission undetectably — and 16 §5/§6.3 agree. `question-service-core::Report` carried it; `futarchy_primitives::ReportView`, the shared `report_view_provenance_preimage`, the pallet's `ReportDraft`/view construction and the **entire Python model** did not. **The differential test could not catch this**: both implementations had the same gap, so they agreed with each other and disagreed only with the spec — the exact blind spot an independent model exists to avoid and does not cover when the omission is symmetric. N7's refactor to one shared preimage function was kept (it is the right structure: pallet and core cannot drift), with the field restored in the spec's declared position. The vector was re-derived **from the Python model**, not by re-running Rust, so it stays an oracle rather than a tautology: preimage 193 → 201 bytes with `8096980000000000` (10,000,000 LE) between epsilon and `certified`, hash `38bf7974…` → `2d2e978e…`, and both suites pin the new value. (2) **N7 raised `TelemetryApi` to `api_version(3)` for `service_collateral()` but left its own freeze test asserting 2**, so `runtime_api_id_and_version_are_frozen` was red on the branch. Fixed to 3 with the reason recorded at the assert: `TelemetryApi` is deliberately outside the 02 contract (12 §6.3 owns it, the ops exporters are its only consumer), so the bump needs no `INTEGRATION_CONTRACT_VERSION` change — but it stays frozen so silent drift fails rather than passes. `FutarchyApi` `ID`/`VERSION` are unchanged.

**What comes next:** N11 rebase, then N8 once its Codex job lands

## N8 ✅ (exact XCM client ingress)

Implemented the corrected three-input admission intersection: an exhaustive six-position whole-program barrier, exact registered `Location` plus `OriginKind::Xcm` converter with the single I-34 `Ok(...)` expression, and classifier-derived `CallDomain::ExternalClient` safe-call filter. The existing three-deny closed barrier is preserved as `LegacyBleavitBarrier`; a BLAKE2 source tripwire freezes the three reused deny implementations, and its 2,048-case recursively generated no-`Transact` differential is byte-identical before/after across result and both mutated inputs. Runtime executor drills prove acceptance only when all three checks pass, no USDC mint/TLV-cap movement, exact-location ingress metering, every registry/service call against Signed/Root/None/all eight governance origins/ExternalClient, the full call-domain inventory, and rejection of the seven named privileged/wrapper calls. The six-position/52-variant tripwire and all-nine-inner-carrier test close SDK drift and nested execution; a dedicated independent-oracle fuzz target covers the same boundary. Final exact gates are green: fmt silent; changed Rust runtime **425**, XCM **67**, origins **10**, registry **15**; five-target fuzz corpus plus 30-second smokes; reference model **652**. The compliance reviewer found and we fixed one real test defect (the supposed negative Signed account had been registered), then returned no blocker. The spec consistency sweep removed stale impossible shared-matcher, type-level-I-34 and future-variant-compilation claims from 00/15/16 and the derived design; no 02 surface changed.

**What comes next:** N9 is next, but its VIT-bond-to-DOT/USDC delivery-fee mismatch remains the existing SQ-547 values blocker; do not invent a conversion or touch the egress path in N8.

## N11 docs 🔨 (written before N10, on purpose)

**The client documentation is the user's own third requirement, and writing it before the client kit is a test of the kit rather than a shortcut.** If the quickstart is hard to write, the API is wrong — so `docs/integration/` was drafted against the settled design and N10 now has a target to hit rather than a surface to describe afterwards. What fell out: the client `Config` reduces to **four decisions** (what you measure, what would change your mind, how sure you need to be, what to do with the answer), and the client never touches XCM, a fee calculation or a call encoding. That is the shape N10 must deliver. Nine files: README (what it is, in one example), quickstart, three transport guides (parachain / contract / off-chain service), `reading-the-report`, `settlement`, `costs`, `errors`. **Three things the docs say plainly because burying them would be worse.** (i) **`manip_floor` is a floor, not a ceiling** — a large value means faking was expensive, not that nobody tried; this is repeated in three files because it is the single most misreadable field, and the project already shipped a 1.928× overstatement of it once (SQ-562). (ii) **The escrow is ~19.7 × declared stake** at ε = 0.05, and `costs.md` gives the `b` vs `b·ln 2` distinction explicitly because conflating them understates the bill by 44 % — the same error the spec itself made. (iii) **A client can capture its own question's settlement**; `settlement.md` states it, bounds it to that question's escrow, and points at the `settlement_trust` field a counterparty should read. Also carried into the docs: registry removal does **not** VOID live questions; push is best-effort and **pull is authoritative** (with the reason — a push that mattered would let any client degrade Bleavit's health metrics by doing nothing); and the local-signer binding that replaced the unworkable `Location::here()` identity. R-3 sweep done (AGENTS.md + README rows).

**What comes next:** N8 in flight; N10 must bind the quickstart to the drill
