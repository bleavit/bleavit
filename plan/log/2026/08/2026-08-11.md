# Session log — 2026-08-11

## **TR7 — runtime wiring at slot 68, plus the merge of…

**TR7 — runtime wiring at slot 68, plus the merge of `main` into #296**

**Seven of nine tasks land, and the fix round closed three paths that were live and unguarded.** Each was proved by mutation rather than by reading: the gate arm's two position reads could be **swapped** with 485/485 green, the Baseline arm had **no test at all** and accepted a hardcoded `[par, 0]`, and the benchmark-fixture guard — written because a helper that silently declines regenerates a *cheaper* weight no gate can see — was itself `#[cfg(feature = "runtime-benchmarks")]` and **ran in no gate anywhere**. CI built that feature and tested it nowhere. The new leg is scoped to `tests_trading_rewards` on measured evidence, not convenience: unscoped, 71 pre-existing tests fail deterministically, none of them a trading-rewards test (**SQ-1053**). **The scoped re-review then caught the controller overstating why**, and caught it the right way — by neutralizing the cause and re-measuring rather than by re-reading. A welfare-component fabrication predating this work explains **8** of the 71, not all of them; the remaining 63 have no identified cause. The claim had already been copied into the gate comment, AGENTS.md and two PLAN.md rows, and all four are corrected here. The reward sovereign also joined `ReservedProtocolAccounts`, and `budget_headroom_usdc` was deleted after the inline into `settle_epoch` left it with zero callers. **The fix-round agent was killed by a watchdog with its work uncommitted**; the transcript's byte offsets show it finished every mutation and was stopped afterwards, so the controller re-verified the whole tree independently rather than trusting it — one `cargo fmt` violation was the only real gap. **The merge cost far less than expected.** Only `PLAN.md` conflicted, in the two append-only logs; `configs.rs` and `tests.rs` auto-merged. The chain feed needed **no** re-record: #299's runtime work is metadata-neutral (no dispatchables, pallet errors, events or `#[pallet::constant]`), checked by reading it rather than assumed from the file list, and `check-chain-feed.py` passes on the merged tree. **The merge ran in a detached scratch worktree** because the re-review agent held `configs.rs` mutated — merging under it would have committed someone else's mutation.

**What comes next:** TR8, the property suite and reference model; then TR9, the coupling screen and its five-location spec correction

## **PR #296 CI repair — three red jobs, all this…

**PR #296 CI repair — three red jobs, all this branch's, plus the first per-function weight-noise revert**

**Every failure was real, and the one that mattered was invisible to the verification I had actually run.** `runtime/bleavit-xcm/src/mock.rs` implements `pallet_futarchy_treasury::Config`, TR6 added four associated types to that trait, and only the treasury's own mock and the runtime were updated — so `cargo clippy --workspace --all-targets` failed on `E0046` where every targeted `cargo test -p <crate>` I ran had passed. The lesson is the memory note's, again: derive the gate set from `ci.yml`, never from the subset that felt relevant. Fixed with `EnsureNever<AccountId>` (the harness dispatches nothing here, and the call moves VIT out of the incentive pot), the existing unit funding seam, and two `parameter_types`. **Reference model, two independent causes.** `test_registry` failed 212 ≠ 208: TR1 correctly moved the *genesis* counter 113 → 114 for `rwd.rate`, but TR3/TR4/TR5 then added four 13 §4 structural rows, so the *entry* counter had to move by five. This is the first time the two counters move by different amounts, and the comment's claim that `rwd.rate` was "the ONLY row that program adds" was true of the seeded keys and false of the registry — the counter catching its own comment is exactly what it is for. The four `test_sustainability` failures were **not** fixed by updating their pins; see the Decision log for the per-function weight-noise revert, after which the suite passed **703/703** with no pin edited at all. **App:** `check:artifact-budget` refused on 10 §9.3's stale 473,749 B metadata figure; re-measured to 489,180 B raw / 154,051 B gz, doc and derived copy updated, rounded 0.15 MB unchanged. **Gates re-run green here:** `cargo fmt --all -- --check` (exit 0, status captured rather than chained off a pipe), `cargo clippy --workspace --all-targets --locked -- -D warnings`, all three weight gates, limit coverage (212 keys, 0 unwired), reference model 703, plan tables, doc links, verbatim copies, spec-question batches, chain feed, dispatch mirror, client-surface obligations, unreadable obligations, release-blocker citations, execute error codes. The exhaustive local gate needed all three documented environment exports — `LIBCLANG_PATH` at a symlink to the *resolved* `libclang-14.so.14.0.0`, and an out-of-tree `CARGO_TARGET_DIR` because ecryptfs caps filenames and the wasm builds overrun it

**What comes next:** TR8 (property suite + independent reference model), then TR9 — the `99 × rwd.rate_ppb ≤ 200 × mkt.fee_ppb` screen with its spec correction in the same commit, which is the launch blocker. A fresh grep for the false anti-farm claim finds it in **08 (three places), 13 §1, 14 TH-78 and 15 §4.1** — six, not the five the plan names, exactly as that task's own brief warns

## **TR8 ✅ · CI round 2 · the TR9 spec correction…

**TR8 ✅ · CI round 2 · the TR9 spec correction (uncommitted, awaiting its screen)**

**CI round 2 went from three red jobs to two, and the survivor taught the branch's recurring lesson a third time.** App turned green on the artifact-budget repair. The reference-model red was my own incomplete edit — `tools/ci/tests` anchors a mutation on the literal `MEASURED_BLOB_RAW_BYTES` inside the app gate file, and I had moved that constant in the gate and in doc 10 §9.3 but not in the anchor; the pair caught the third edit, which is what it is for. The Rust red was new: `tests_migration_guard::composed_runtime_upgrade_…` asserted the treasury lands at `StorageVersion(3)` and TR6 legitimately took it to 4 with the `IncentiveRemaining` migration. **That test is `#[cfg(feature = "try-runtime")]`, so neither `cargo test --workspace` nor `cargo clippy --workspace --all-targets` runs it** — clippy compiles a feature-gated test and executes nothing, so "clippy green, workspace green" was not evidence about it. Three feature flags on one branch now: `runtime-benchmarks` at `bleavit-runtime` (SQ-1053), `runtime-benchmarks` at `pallet-oracle` (#299) and `try-runtime` here. Fixed by asserting 4 **and** asserting the migration's effect — nothing at runtime level checked that `IncentiveRemaining` was seeded, so a bump-and-do-nothing migration would have passed. Verified 494 passed / 0 failed under the feature. **TR8 landed and its most valuable output was finding the plan wrong.** The plan's own anti-farm property was false: `reward ≤ debit + 2f·net` needs `r ≤ 2f` while the coupling gives only `r ≤ 200f/99`, so a pair may sit 1.01 % above the asserted bound. Proptest produced an in-coupling, in-range counterexample at 2×10⁷ cases; it is pinned as its own test asserting the wrong form fails and the corrected one is tight. Eleven properties plus two witnesses, a `rewards` shard in `property-gates.sh` and the CI matrix, and `trading_rewards.py` as the independent model. Shard verified honored rather than assumed — 0.00 s at 8 cases against 0.61 s at 10⁶. **Its review returned 1 Critical + 3 Important**, and the Critical is a reporting defect rather than an arithmetic one: the report claimed a 30,000-vector differential with zero mismatches, the reviewer's own differential found `book_acquired` after settlement disagrees in 59 % of vectors, the harness had been deleted, and the claim had been promoted into `.claude/rules/reference-model.md`, which loads into every future session. Every *scored* quantity did match. Ruled under R-1 before dispatching the fix: the kernel's decrement is right and 08 §2.6 rule 3 now states it, because without it a second settlement credits `received` twice and an over-credit is the direction R-7 forbids — so the model changes, not the kernel

**What comes next:** **TR9, and it is the only chain task left.** Its spec half is already written and uncommitted (see Current focus); its code half — `screen_rwd_rate_coupling` in `constitution-core`, wired into the amendment path and try-state, plus 13 rule 7's third coupling — is not started, and both must land in one commit. Then: run the exhaustive gate once for the whole state, push, and write the client plan (contract v30 → v31, docs 10/11, the app panel)

## TR9 · SQ-1052 · #296 CI

**TR9 landed, SQ-1052 was ruled, and the third red CI job was in a pallet this branch never edited.** TR9 shipped spec and code in one commit (`00a5b562`): `screen_rwd_rate_coupling` enforces `99 × rwd.rate_ppb ≤ 200 × mkt.fee_ppb` at the amendment boundary and in try-state, at exactly the three call sites the sibling `screen_redeem_fee_coupling` occupies — verified by diffing call sites rather than trusting the report. The implementer's headline finding was **against its own brief** and was right: the brief's negative test used a pair *inside* the break-even, so the screen correctly admitted it. That exposed a real property — at the seeded `mkt.fee`, `rwd.rate`'s registry maximum is already inside the break-even, so the rate arm is unreachable until the fee moves. **The exhaustive gate ran green over the whole state**, exit 0 in 9 h 17 m. **CI round 3 found a third red job**: `benchmark-smoke`, one hard-gate line out of 35 pallets — `pallet_xcm::claim_assets` worst-case proof size 5,502 → 5,540, because adding a pallet to `construct_runtime!` changed the trie. Regenerated at the committed 50×20 fidelity and reduced to that one function per the 2026-08-02 rule; the local measured proof `2037 → 2075` reproduces CI's `+38` exactly, and the re-check now PASSES. **Two process gaps were the owner's find, not mine**: TR9 had no task review and the SDD ledger stopped at Task 7, so a resuming session would have re-dispatched TR8. Both closed. The review then caught a seventh location of the retracted anti-farm claim, inside a row I had already edited (14 TH-78's attack-cost column), and a second Rust spelling of the screened predicate, now single-homed to `constitution_core::rwd_rate_coupled` with an inverse-binding test.

**What comes next:** **Push, then the final whole-branch review** on the most capable model, carrying the deferred-minor roll-up from all nine tasks. Then `finishing-a-development-branch`, then the client plan (contract v30 → v31, docs 10/11, the app panel), which is still unwritten. **Two items are owed and not done:** the chain feed is one pallet-error variant stale, because `check-chain-feed.py` compares storage items and the pallet set but not errors, events or calls — the SQ-582 shape one surface over, and it needs a spec question raised rather than a silent re-record

## Final review · C1 · I5 · SQ-1054

**The whole-branch review returned NOT merge-ready — 2 Critical, 12 Important — and every finding is now fixed or ruled.** **C1 is the one that mattered:** the settlement adapter read the trader's ledger position at *crank* time while `redeem_scalar_impl` burns it, so a trader who redeemed before the permissionless fold was folded with `position = [0,0]`, collected nothing into `received`, kept a real `spent`, and **was debited for an accurate forecast**. Third arrival of one failure mode after SQ-1051 and the `settled_value` type, reached this time through *when* a quantity is read. Ruled by deleting the position read and crediting `book_acquired`; the first shape considered was rejected as **worse than the bug**, because dropping the entry on a missing position is a debit escape a trader can time. The ruling also makes the crank cheaper — `settle_market_score` 7 reads → 5, proof 6,124 → 3,697. **I5** narrowed `settle_epoch`'s permissionless justification, which was false: a third party could finalize a victim into a zero-headroom moment for a transaction fee. **SQ-1054 raised** — `pallet-trading-rewards` did not *compile* under `runtime-benchmarks`, so its `impl_benchmark_test_suite!` had never run; six mocks share the cause, this branch's own pallet is fixed (91 → 97 tests) and the other five are that row's work. **Two CI failures this round were mine**: the Phase-0 corpus-family inventory never learned TR8's new family, and my own `pallet_xcm` weight fix broke a PoV pin because `clippy --all-targets` compiles a test without running it — the lesson I had written into AGENTS.md hours earlier. Weights regenerated and reduced per the 2026-08-02 rule; `pallet_market` produced no stale storage dimension and reverted whole.

**What comes next:** **The chain feed, then the branch close.** The feed is stale by two pallet-error variants and `check-chain-feed.py` cannot see errors, events or calls — raise that as a spec question before re-recording, since a silent re-record hides the gate hole. Then `finishing-a-development-branch`, then the client plan (contract v30 → v31, docs 10/11, the app panel), still unwritten. **13 Minor findings and 5 promoted minors remain triaged but unfixed** — see `final-review.md` and the ledger roll-up

## Chain feed re-record · SQ-1055

**The feed was stale by two pallet-error variants, and the gate that exists to catch a stale feed cannot see an error at all.** `Constitution::RewardRateAboveWashBreakeven` (TR9, `00a5b562`) and `TradingRewards::ThirdPartyWouldClampReward` (the final review's I5, `d887b3e5`) both landed after the correct re-record at `6eb62b51`, and `check-chain-feed.py` stayed green through both. **Re-recorded by the documented procedure** in `app/fixtures/chain-feed/README.md` — chain specs, a release collator build, both runtime profiles, and `extract-metadata.py` against each from a booted node. No `spec_version` bump: directories `2/` and `3/` are the primary and paired terminal-recovery profiles, not a version history. Both now carry metadata v15, contract v30 and 46 pallets, and the recovery half re-proved its zero-multi-block-migration property as part of its build. `spec-versions.ts` is a *checked input* rather than a generated output, so it was repinned by hand — from a fresh `sha256sum` of each blob **cross-checked against its own `runtime-info.json`**, never copied from the checker's error message. **SQ-1055 raised rather than the drift silently closed.** The gate compares which pallets exist and what storage they publish, and compares **no** error, event or call variant: measured at **426 errors and 238 events** across the 18 in-repo pallets, plus every call. It is [SQ-582](#spec-questions)'s own argument one surface over — that row added the storage leg because the pallet-set check *"compares which pallets exist and never what they publish"*, and an error is also something a pallet publishes. The row prices the three surfaces separately, because only errors fail safe: a missing **call** is a workflow `packages/papi-descriptors` cannot construct, and a missing **event** is V-164's shape, since `event-accounts.ts` resolves accounts by metadata type id and an unlisted event presents as an empty transaction history. **One caveat is measured rather than warned about:** a `#[pallet::call_index]` → `pub fn` scan over a 400-character window finds 4 of `pallet-market`'s 7, missing `buy` (+490 chars), `sell` (+817) and `sweep_revenue` (+433) — one-directional, so it would report nothing and read as coverage. The extension is **not built here**, being a new gate leg with its own tests and witness. Gates: chain feed (both directories + the 10 §5.1 pair), `surface:check` (300 probed, 2 unprobed), `descriptors:check` (19 files byte-for-byte), `build`, `test:mock-runtime` 13, `test:chain-client` 277, `test:descriptors` 44, `foreign:check`, `test:screens` 726, `test:tx` 172, PLAN tables, spec-question batches 591, doc links, release-blocker citations, unreadable obligations, control-character sweep over all 12 changed text files.

**What comes next:** CI on the re-record, then the branch's remaining minors and `superpowers:finishing-a-development-branch`

## Chain feed re-record — CI repair · 10 §9.3

**The re-record turned the App job red at a step I never ran, and the gate was right.** `check-artifact-budget.ts` refused `release:build`: the new `metadata.scale` is 492,363 B and gzips to 155,548 B, which is **0.16 MB** at the two decimals 10 §9.3 publishes to, against the 0.15 the document still carried. **This is the first of five re-measurements where the rounded figure actually moves**, and §9.3's text asserted the opposite in as many words, so the edit is a correction rather than a refresh. §9.4's combined bound stays at 1.5 MB — eight blobs at the new size are 1.28 MB, and the row already carried the headroom this growth consumed — so only its derivation prose repoints. The checker's two constants move with the document, which is what makes the pair checkable in either direction. **The measurement is the checker's own:** local `node:zlib` `gzipSync({level: 9})` reproduces CI's 155,548 B exactly, while Python's `gzip` at level 9 gives 155,171 B here, a 377-byte zlib-build difference — so the published figure is what the tool measures, never what a convenient utility reports. **The process error is the part worth keeping.** I built the feed gate list from `app/fixtures/chain-feed/README.md` and `.claude/rules/app-code.md` rather than from `ci.yml`, so ten feed-reading gates passed locally and the eleventh step in the job was one I had never run. The repair was to extract every `run:` from `ci.yml`'s `app` job programmatically and execute all **68** in order: **66 green**, and the only two failures are `check:render-budget` and its witness, which need Chrome — absent on this workstation and present in CI, where that step passed on `d887b3e5`. It is the one app gate that cannot run here, and it refuses to degrade to skipping by design. Also green: `release:build`, `release:check`, `release:manifest`, `check:bundle-budget`, `check:artifact-budget` and its witness, `check:embedded-tree`, `test:release`, `signers:audit`, `check:smoldot-surface`, plus `check-verbatim-copies.py` (regenerated, byte-identical), `check-frontend-budgets.py` (73 published §9 cells re-derived), doc links and PLAN tables.

**What comes next:** CI on the fix, then the branch's remaining minors and `superpowers:finishing-a-development-branch`

## Chain feed re-record — CI repair 2 · mutation anchors

**Running a gate is not running the gate's tests, and that is what went red next.** `check-frontend-budgets.py` passed on the corrected document, so the previous row's claim was true — but `tools/ci/tests/test_check_frontend_budgets.py` carries the published figures as **mutation anchors**, quoted verbatim out of `check-artifact-budget.ts`, and two of them named the values I had just moved. `assert_mutation_caught` asserts the anchor is present *before* mutating, so a stale anchor fails loudly rather than making the mutation vacuous — which is the design working, and the test's own comment had predicted this exact failure from the previous time it happened. Both anchors repinned to `0.16` and `492_363`, both expected messages with them, and the raw-size comment gained the `489_180 → 492_363` step. **The three blob tests were then run by name and verbatim rather than counted**: each asserts the checker exits non-zero *and* prints the expected sentence, so passing is a liveness proof and not a green line. Suites: `tools/ci/tests` **378** (2 skipped), `deploy` 172, `reference-model` 12, `release` 101, `phase-gates` 74, `env` 86, `monitoring` 111, `limit-coverage` 35, plus `validate-environments.py`, `generate-vectors.py --check` and `check-doc-table.py`. **A second habit is recorded because it nearly hid this:** the suite runner piped each suite through `tail -3`, and unittest writes its summary to unbuffered stderr while the tests write to block-buffered stdout — so the tail showed test chatter and the `OK` line had already scrolled past. The suite was re-run with its whole output captured. Truncating verification output discards exactly the line being relied on.

**What comes next:** CI on the fix, then the branch's remaining minors and `superpowers:finishing-a-development-branch`

## — (security, unplanned)

**Both open Dependabot alerts triaged; one real fix taken that neither alert named, and the gate hole that hid it closed.** **Alert #27 — `glib` 0.18.5, RUSTSEC-2024-0429 / GHSA-wrw7-89jp-8q8g (medium).** Not fixable by any bump: the 0.18 line ends at 0.18.5, the fix is 0.20.0, and `gtk 0.18.2` requires `glib "^0.18"` — the **archived** GTK3 binding whose last release is 2024-12-09, still what current `tauri 2.11.5` resolves. Proven **unreachable**: `VariantStrIter::new` is `pub(crate)`, so `array_iter_str` is the only producer, and a grep across every crate source in the resolved closure of all four workspaces matches `glib-0.18.5` itself and nothing else — inside glib, only its own `#[cfg(test)]` module and one doc comment. **Alert #28 — `@opentelemetry/core`.** Already waived 2026-08-07 with a proven `reaches_bundle = "no"`; the user chose to keep the waiver rather than take its `clears_when` (a Lighthouse 12.8.2 → 13.4.1 major that re-baselines 10 §9.4's published render budget and cannot be measured here — no Chrome on this workstation). **The finding that mattered more than either alert:** nothing in this repository failed on #27, and nothing ever would have. `cargo-audit` prints `unsound` as an allowed warning (15 §4.5 *required* that) and `check-ghsa-only.py` skips every RustSec-carried finding by contract, so the gate was green with the advisory printed in its own log — `warning: 17 allowed warnings found`. Triaging the whole class found **five** `unsound` findings across the four cargo lockfiles, and one was a plain fix nobody had looked at: **`event-listener` 5.4.1 → 5.4.2** (RUSTSEC-2026-0221, `!Send` tags crossing threads via `StackSlot`), a patch release reachable by `cargo update --precise` in **both** the root and keeper workspaces. The remaining three are waived with proof — `lru` (no `LruCache::iter_mut()` call site exists; `libp2p-identify` declares the dependency and never uses it) and `memmap2` (`parity-db` **does** call `flush_range`, and the range is the mapping's own tail: `set_len(file_size(..))` precedes every `map_mut` and `file_size = total_entries*8 + META_SIZE`, so the subtraction cannot underflow). **Shipped:** leg 5 of `supply-chain-gates.sh` (`check-unsound-advisories.py` + `unsound-waivers.toml`), waivers keyed on `(id, package, version, workspace)` with a required `exposure` whose `triggerable` value is refused at load time, a stale-waiver leg, summary schema v4 → v5 with `waived_unsound`, 12 new tests, and the R-1 amendment to 15 §4.5 (SQ-1048) that the change required. The gate now runs `cargo audit --json` once and shares it, instead of twice. **Verified:** `supply-chain-gates.sh` exit 0 with all three waivers matched and `event-listener` gone from the findings; witness run against the real `app` report with an empty waiver file exits 1 naming RUSTSEC-2024-0429; `tools/ci/tests` 390 pass, `tools/release/tests` 101 pass; both TOML parsers agree on the committed waiver file, checked under 3.11.

**What comes next:** Dismiss both alerts on GitHub with their triage text (user-approved), then open the PR and let CI run.

## — (security, unplanned)

**Codex connector bot P1 on #300, confirmed and fixed: leg 5 could be switched off by one line in `.cargo/audit.toml`.** The bot's claim was exactly right and my own docstring had the reasoning backwards. `cargo-audit` reads `.cargo/audit.toml` from its working directory and drops an ignored advisory from `warnings` **entirely** — measured, not argued: ignoring RUSTSEC-2024-0429 takes `app`'s report from 16 unmaintained + 1 unsound to 16 + 0. Leg 5 read leg 2's reports, so an entry in that file left **no unwaived finding and no stale waiver**, and the run reported a clean scan it never performed. I had claimed the stale-waiver leg covered this. It does not: staleness fires only where something **already waived here** is suppressed, and the dangerous case is an advisory nobody triaged. Worse, the bypass is what this change made attractive — before leg 5 an `unsound` advisory was silent anyway, and now that one turns CI red, adding its id to `.cargo/audit.toml` is the first thing a person under time pressure reaches for. **Fix:** leg 5 takes its reports from a directory holding **no** `.cargo/audit.toml`, naming each lockfile with `--file`, so it evaluates the true advisory set whatever a workspace chose to suppress. The checker re-proves that instead of trusting it and **refuses any report whose `settings.ignore` is non-empty**, so a stray config cannot quietly re-suppress the one leg whose job is to not be suppressible. It also fails closed on an empty report, because the shell must now tolerate cargo-audit's exit status (an unsuppressed root run legitimately exits non-zero on the vulnerabilities leg 2 waives). Clause 4 isolation is untouched — leg 2 still audits from each workspace root, and the summary still discloses each workspace's own ignore list from its own with-config run, which is why those two runs stay separate. **Witness, against the real toolchain and not a stub:** with RUSTSEC-2026-0002 added to `.cargo/audit.toml` and its waiver removed, leg 2 passes and the gate exits **1** naming `[UNWAIVED] root: lru 0.12.5`. That scenario was green before this fix. 15 §4.5 gained the normative sentence (the leg MUST evaluate an unsuppressed set and MUST refuse a suppressed one). **Verified:** gate exit 0 with all three waivers matched and the summary still disclosing root's seven ignores; `tools/ci/tests` 394 pass, `tools/release/tests` 102 pass, including a new end-to-end test that an auditor suppressing even on `--file` fails the whole gate.

**What comes next:** Await CI on #300.

## #296 ← `main` merge · SQ-1048 collision

**`main` moved to `4732e3d4` (#300) and #296 went `DIRTY`, which matters more than it reads: a conflicted PR runs no CI, so a rollup can sit green on a merge that was never built.** Only `PLAN.md` conflicted, in the three usual places, and `.github/workflows/ci.yml`, `AGENTS.md`, `Cargo.lock`, `keeper/Cargo.lock` and 15 §4.5 auto-merged. **Inside the conflict was a real one: both branches minted `SQ-1048` independently, for different questions.** That is the exact collision `check-spec-question-batches.py` was written to catch after `SQ-286` survived a merge, and it caught it. **#300's row keeps the id** — it is cited in normative spec text, [15](../../../../docs/architecture/15-invariants-and-testing.md) §4.5 twice, and in AGENTS.md, and it merged first — so this branch's gross-versus-net proceeds row becomes **SQ-1056**, its single citation in 08 §2.6 moved with it, and the row itself records why rather than leaving a reader to wonder. Both rows are resolved, so no batch entry moved. **The Current focus resolution dropped exactly one block, deliberately:** #299's in-flight row, which `main` had already retired in the same edit that added #300's — verified by reading main's own replacement text, *"#299 merged as `57d31ca9` … its former in-flight row is retired here"*, rather than by assuming the newer side was complete. **Nothing was taken on trust.** `rerere` is off in this worktree with an empty cache, so no cached resolution could replay silently, and a completeness script asserted the union directly: every spec-question id, session-log row and decision-log row from **both** sides is present exactly once — 591 + 584 → **592** questions, 864 + 849 → **866** log rows, 273 + 262 → **274** decisions. Gates on the merged tree: 18 document and surface checkers green, `tools/ci/tests` **394** (main's new `test_check_unsound_advisories.py` takes it from 378), `tools/release/tests` **102**.

**What comes next:** the Rust legs over main's `event-listener` bump, then CI on the merge

## RUSTSEC-2026-0253 · `lru` × 3 (unplanned, main-wide)

**A new advisory turned `main` red at its own tip, and #296 with it — the failure was inherited, not caused.** `RUSTSEC-2026-0253` (`LruCache::pop()` is not panic-safe) was published after #300 merged, so leg 5 reported three unwaived findings on **both** branches identically: `root: lru 0.7.8`, `root: lru 0.12.5`, `keeper: lru 0.16.4`. Attributed by reading main's own run rather than by assuming, since a red job on a feature branch usually is the branch. **The owner ruled it lands on #296 directly**, accepting that every other open PR stays red until this merges. **The fix is unavailable everywhere:** the patch is `0.18.2` and all three are pin-forced — `tracing-log 0.2.0` at `^0.7.7`, `libp2p-identify 0.45.0` at `^0.12.3`, `smoldot-light 1.3.1` at `^0.16.3` — each confirmed by a verbatim `cargo update --dry-run --precise 0.18.2` refusal. So three waivers, and the exposures differ because the evidence differs. **The precondition is narrow and it is what each entry is argued against**: a *stored key's* `Drop` must panic inside `pop()`. `root/0.7.8` is **unreachable** — `tracing-log` declares one cache and the crate contains no `.pop(` at all. `keeper/0.16.4` is **unreachable** — `smoldot-light` declares six caches and its only two `.pop(` lines are `Multiaddr::pop`, an unrelated method of the same name. `root/0.12.5` is **constrained**, and deliberately not `unreachable`: `pop` **is** called, once, at `libp2p-swarm 0.45.1 peer_addresses.rs:81`, keyed on `Multiaddr` — which the *existing* RUSTSEC-2026-0002 waiver had already recorded as used, so reusing that row's `unreachable` claim would have been false. The precondition is unsatisfiable there because `multiaddr`, `libp2p-identity` and `multihash` define **zero** `impl Drop` across every vendored version, checked rather than asserted. **Verified by running the leg, not by reasoning about it:** local `cargo-audit 0.22.0` (the gate pins 0.22.2) over all four lockfiles from a directory holding no `.cargo/audit.toml`, fed to `check-unsound-advisories.py` — 6 findings, all triaged, no stale waiver, exit 0. `tools/ci/tests` 394. **`main` stays red until this merges**, which is the accepted cost of the placement ruling.

**What comes next:** CI on the fix, then the branch's remaining minors and `superpowers:finishing-a-development-branch`

## In flight, 2026-08-11 — #296, the VIT trading accuracy…



> **In flight, 2026-08-11 — #296, the VIT trading accuracy rewards program. All nine chain tasks
> are done. What remains is the push, the final whole-branch review, and the client plan.**
>
> The program pays VIT for realized forecast accuracy, funded from the Phase 3–4 incentive
> allocation that has been funded and undeliverable since genesis. Design and plan live in
> [docs/proposals/](../../../../docs/proposals/) — this row does not restate either.
>
> **TR9 shipped the anti-farm correction, spec and code in one commit** (`00a5b562`). The design
> claimed the per-account earning cap delivered the invariant. It does not: the cap is proportional
> to each account's *own* bond, and one operator funds both sides, so an asymmetric pair directs
> the profit. With equal bonds the pair nets exactly zero, which is why the claim survived three
> revisions. The rate coupling is the real defense, and `99 × rwd.rate_ppb ≤ 200 × mkt.fee_ppb` is
> now screened at the amendment boundary and in try-state, at exactly the three call sites the
> sibling `screen_redeem_fee_coupling` occupies.
>
> **SQ-1052 is RULED, by the owner, 2026-08-11: the program MAY authorize the whole 100,000,000
> VIT pot.** The runtime already bound exactly that, so the ruling confirms the code rather than
> changing it, and no code cites the question. Three spec edits make the doc set agree — see the
> Decision log. The consequence worth carrying: 08 §2.5's reporter-loan backstop is now stated as
> **subordinate** to §2.6, so sequencing the reporter line before a large authorization is an
> operational requirement of Phase-3 arming.
>
> **The exhaustive gate ran green on this state** — `tools/ci/rust-workspace-gates.sh`, exit 0,
> 9 h 17 m, over `00a5b562`. That duration is a cold-cache local artifact and not the CI figure.
> CI runs the identical script in about an hour because `ci.yml` restores a warm cargo cache and
> sets `CARGO_INCREMENTAL=0`, which this workstation did not. Set that variable locally, and
> prefer the two feature-gated legs over the whole script — see the Decision log.
>
> **The final whole-branch review found 2 Critical and 12 Important, and both Criticals were
> real.** Report at `.superpowers/sdd/…/final-review.md`. It earned its cost by seeing what no
> per-task review could, because none of them owned two subsystems at once.
>
> **C1 — redeeming before the fold debited an accurate forecaster.** The settlement adapter read
> the trader's ledger position at *crank* time and `redeem_scalar_impl` burns it. Settlement and
> redemption open at the same instant and the fold is permissionless, so the ordinary act of
> redeeming produced `received = 0` against a real `spent`. Third arrival of one failure mode,
> after SQ-1051 and the `settled_value` type. **Ruled: credit `book_acquired` and delete the
> position read entirely.** The first shape considered was worse than the bug — dropping the entry
> when the position is missing is a debit escape, since traders know their own outcome and would
> redeem-before-fold exactly when losing. The ruling also makes the crank cheaper:
> `settle_market_score` goes **7 reads → 5**, proof size 6,124 → 3,697.
>
> **I5 — `settle_epoch` is permissionless only while it costs the participant nothing.** A third
> party could finalize a victim into a zero-headroom moment for a transaction fee, which is the
> harm the spec already forbids for a permissionless sweep. A caller other than the participant is
> now refused when the live headroom would clamp the reward.
>
> **SQ-1054 raised — the defect class a third time.** `cargo test -p pallet-trading-rewards
> --features runtime-benchmarks` did not *compile*, so that pallet's `impl_benchmark_test_suite!`
> had never run a benchmark. Not branch-specific: six mocks share the cause, and
> `pallet-client-registry` was reproduced. Fixed for this branch's own pallet only (91 → **97**
> tests, the six new ones being the six benchmarks) with a counted gate leg.
>
> **Two CI failures this session were the controller's own**, and both are fixed: the Phase-0
> gate's corpus-family inventory never learned TR8's `trading_reward_scenarios`, and
> `pov_budgets::xcm_claim_assets_fits_the_normal_class` broke on the `pallet_xcm` weight fix
> because `clippy --all-targets` compiles a test without running it.
>
> **Weights regenerated at the committed 50×20 fidelity, then reduced under the 2026-08-02
> per-function rule.** `pallet_trading_rewards` keeps two functions: `claim_rewards` (reads 4 → 7,
> writes 3 → 5 — the old fixture measured the non-closing arm, because `&&` short-circuits on a
> live bond) and `settle_market_score` (the C1 saving). The other four are ref_time noise and are
> reverted verbatim. `pallet_market` produced **no** stale storage dimension at all and reverts
> whole, which is the expected result: the I3 first-fill surcharge lives in the weight attribute,
> not in the generated file. **No `weight-regression-acks.toml` entry is needed** — the gate
> reports `pallet_trading_rewards.rs` as a NEW weight file against the merge base, so there is no
> baseline for `claim_rewards` to regress against.
>
> **Gates green for this state:** runtime 491, `pallet-trading-rewards` 95 (101 under
> `runtime-benchmarks`, all six `bench_*`), kernel 29, differential 2, property 20, reference model
> 768, corpus `--check` fresh, limit coverage 212/0 with its 35 tooling tests, phase-gates 74,
> `tools/ci/tests` 378, weight purity 387, weight storage bounds, weight regression, doc links,
> PLAN tables, spec-question batches 590, `pallet-futarchy-treasury --features try-runtime` 120,
> `pallet-origins --features try-runtime` 20, fmt. The `benchmark-smoke` drift check over the two
> regenerated pallets is the last one running.
>
> **The chain feed is re-recorded, and the gate hole it hid is now a question rather than a
> footnote.** The feed had gone stale by two pallet-error variants — TR9's
> `Constitution::RewardRateAboveWashBreakeven` and I5's `TradingRewards::ThirdPartyWouldClampReward`,
> both added after the correct re-record at `6eb62b51`. Re-recorded by the documented procedure in
> `app/fixtures/chain-feed/README.md`, from a booted node over both runtime profiles. Both
> directories now carry contract v30 at 46 pallets, and `spec-versions.ts` is repinned from a fresh
> `sha256sum` of each blob cross-checked against its own `runtime-info.json`, never from the
> checker's error message.
>
> **SQ-1055 raised, because a silent re-record would have hidden the reason it was needed.**
> `check-chain-feed.py` compares which pallets exist and what storage they publish, and compares
> **no** error, event or call variant — 426 errors and 238 events across the 18 in-repo pallets,
> plus every call. The row carries the measurement, the three surfaces' unequal consequences, and
> one caveat any repair must price in: a naive `#[pallet::call_index]` scan misses `buy`, `sell`
> and `sweep_revenue` on `pallet-market`, so it would read as coverage while blind on the two calls
> the client trades through. **The extension itself is not built here** — it is a new gate leg with
> its own tests and witness, and this branch is at final-review-complete waiting on CI.

> **In flight, 2026-08-11 — the two open Dependabot alerts, and the gate hole that let one of
> them reach GitHub unseen.** Unplanned, user-requested, outside every track.
>
> **Neither alert was fixable by a version bump, and both are settled.** `glib` 0.18.5 (#27,
> medium) is pin-forced by the **archived** `gtk 0.18.2` and proven unreachable — no crate in the
> resolved closure calls the affected iterator. `@opentelemetry/core` (#28) keeps its 2026-08-07
> waiver by the user's ruling, rather than taking its `clears_when`: a Lighthouse major that
> re-baselines [10](../../../../docs/architecture/10-frontend-architecture.md) §9.4's published render budget
> and cannot be measured on this workstation.
>
> **The work is the third thing, and it is the point.** Nothing in this repository failed on #27
> and nothing ever would have — §4.5 *required* `cargo-audit` to treat `unsound` as
> informational, and the GHSA-only leg skips every RustSec-carried finding by contract. Triaging
> the whole class across all four cargo lockfiles found five `unsound` findings, and one was a
> plain fix nobody had looked at: **`event-listener` 5.4.1 → 5.4.2** in the root and keeper
> workspaces. Leg 5 of `supply-chain-gates.sh` now gates the class, behind SQ-1048's R-1
> amendment to [15](../../../../docs/architecture/15-invariants-and-testing.md) §4.5.
>
> What each fix is and why is in the *Decision log*; the triage and the evidence are in
> *Unplanned changes* and the *Session log*. This row does not restate them.
>
> **Status.** Open as **#300**. The Supply chain job passed on the first push, and a Codex
> connector bot **P1** on that push was confirmed and fixed: leg 5 read leg 2's reports, so one
> line in `.cargo/audit.toml` could switch it off silently. It now evaluates an unsuppressed
> advisory set and refuses a suppressed report. Witnessed against the real toolchain — an ignored,
> unwaived `unsound` id makes the gate exit 1, where it was green before. `tools/ci/tests` (394)
> and `tools/release/tests` (102) are green. Two spec edits, both to 15 §4.5, no 02 change and no
> `INTEGRATION_CONTRACT_VERSION` bump.
>
> **#299 merged** as `57d31ca9` and is what this branch builds on; its former in-flight row is
> retired here.

> **PARKED: 2026-08-09 — Track F's code is done. What remains needs a device lab, a ceremony,
> credentials, or a ruling, and none of it is unblocked by more work in this repository.**
>
> **#295** (`integration/track-f-v3`) supersedes #292, #293 and #294. It carries F28's visual
> layer, F26's compat verdict reaching the shell, and F18's D-13 caps reader — plus two fixes
> neither branch could carry alone.
>
> **The union defect, again, and it is the third time this pattern has paid.** F28 added a
> **bidirectional** style-coverage gate, and F26 added markup after that stylesheet was written.
> Each branch is green alone. In one tree the gate reports `compat__disabled` and
> `datum--unavailable` as unstyled. The fix cannot ride on F28 either, because the dead-rule half
> would then fail it. `datum--unavailable` is **not** a misspelling of the existing
> `datum__unavailable`: that one is the child span inside `ui`'s `Datum`, this one is a whole
> datum standing in for one.
>
> **The R-6 review returned NO BLOCKER for F26, F18 and F17**, with three majors. The sharpest is
> the one this repository keeps finding: `readDepositCaps` called `crossCheckedCall` and **never
> compared the witness**, so 10 §11's FE-P2 pairing was fetched and discarded while the module's
> own note said it was not optional. Fixed, with the comparison mutation-proven — and the
> anti-vacuity test caught a second instance **in the fix itself**, where an *undecodable* witness
> skipped the comparison and let the caps through. An unreadable second view is not agreement.
>
> **F17 is ✅ here.** The review's two remaining majors landed on #295 rather than in a second pull
> request, so one CI cycle carries both and F17 flips on the same evidence. The challenge row now
> names its filing on both of its reads, and the `read-only-incompatible` arm reads
> `Constitution.ReleaseChannel` and renders the newer-release pointer its own sentence promises —
> nothing in `app/` had read that item at all. Nine mutants across the two, all killed.
>
> **One finding about this session's own tooling, because it is the class this track keeps hitting.**
> `main` advanced under #295 while a bounded waiter polled it, and the pull request went `DIRTY`.
> GitHub runs no workflows on a conflicted pull request, so every check the waiter read as
> *pending* was a stale placeholder that would never resolve. It reported `bad=0` seven times and
> could not have reported anything else. The replacement reads `mergeStateStatus`, refuses to call
> an empty rollup green, and stops on head drift. A waiter that only counts checks is a check that
> cannot fail.
>
> **Four rows stay 🔨, and each names the external thing it waits on:**
>
> - **F1** — **SQ-940**, which only the user can rule. `ar-io/ar-io-ant-process` was archived
>   2026-06-09 and the AR.IO contracts moved from AO to Solana. An ANT authorizes by membership
>   and returns on the first matching controller, so a controller list is **1-of-m**: adding
>   controllers widens custody rather than splitting it. The remaining prototype gates need a
>   device lab, a live chain, hardware, or ar.io credentials.
> - **F11** — the ten readiness blockers `release.json` names, each of which `--production`
>   already refuses on: no seated bootnode operator, no gateway set chosen, no production genesis
>   or chain-spec hash, no paraId, no keyring, and a version still `0.0.0`. The live Arweave
>   adapter's Turbo-SDK half is prototype gate **FE-P7**, whose `[VERIFY]` R-2 forbids resolving
>   by assumption.
> - **F13** — the key ceremony itself, the real signer names and keys, and `liveGateway`. The
>   tooling is finished and mutation-proven (38 mutants, 0 survivors).
> - **F14** — the physical device lab 10 §9.4 names, and a Playwright probe that is not
>   installed. *Memory desktop*, *memory mobile* and *mobile CPU* cannot be measured without it.
>   The chain-spec row is measurable-when-declarable rather than blocked.
>
> Everything that was unblocked is merged. **#295 landed on `main` as `acf9c1ae` with 23/23
> checks green, verified per check**, and #292, #293 and #294 are closed as superseded rather
> than merged — all three are contained in it by ancestry, so merging one would apply the same
> work twice. Track F is **26 of 30**.
>
> **The branch and worktree cleanup ran with it.** 43 of 48 worktrees removed, and 27 remote
> branches deleted: the four this work produced, plus 23 whose pull request is `MERGED`, which
> is the only test that proves the work survives the deletion. The five worktrees left are the
> primary checkout, this session's own, the **locked** `vit-issuance` (open #296), Codex's, and
> another session's scratch tree. **Seventeen branches were left deliberately**, and a next
> session should not read that as an oversight: thirteen have a `CLOSED` pull request and four
> have none at all, so neither group can be proven contained in `main` by any mechanical test —
> a squash merge leaves no ancestry to follow. They need a person's judgement, not a script's.

> **Ten pull requests merged 2026-08-07/08. The load-bearing find is that one missing decision
> held four milestones, and nobody owned it.**
>
> `probeCriticalSurface` was defined once and called **zero** times; `createClient` was
> constructed nowhere. So 10 §5.2's compatibility classifier had never run on either chain — and
> `check-client-surface-obligations.py` exists precisely because *"an unfrozen read is one the
> compat lattice cannot fail on"*. The gate enforced the freeze faithfully while the consumer the
> freeze exists for did not exist. Behind that one gap sat F14's four unmeasurable budget rows,
> F17's screen registration, F18's deposit leg, and `implementedScreens()`, which still returns `{}`.
>
> **Why nobody built it (SQ-1010):** `probe.ts` waits on F6, whose row never mentions it; the
> 2026-08-04 log assigned the placement to F7, whose row never mentions it; PLAN called it *an
> F3/F7 boot-sequence decision*. **F3, F4, F6 and F7 are all ✅.** Handed forward three times, and
> every recipient closed without taking it. **F26** now carries it.
>
> **The sharpest single defect is in the same family.** `CompatSurface` declared four of PAPI's six
> groups, and `FOREIGN_SURFACE`'s frozen 02 §7.7 entry is a **call**, which lives in `compat.tx` —
> so `PolkadotXcm.limited_reserve_transfer_assets`, the deposit leg itself, was the one Asset Hub
> surface the compatibility check structurally could not examine.
>
> **Every review this session found a check that had quietly stopped checking.** An anti-vacuity
> test anchored on renamed text. A quota manager with no caller. A `TRIGGER_READS` table nothing
> derived from. `app/pnpm-lock.yaml` audited by nothing while doc 14's TH-44 named the control.
> Retiring a `blocking` obligation opening a 5-of-7 playbook activation whose trigger was never
> evaluated. A runtime-cache eviction whose own new test read it as boundedness. A mutation test
> asserting through a **local copy** of the function under test.
>
> **Three were mine, and the third was destructive.** A union resolution keyed on a substring that
> appeared inside another line's prose deleted a gate's documentation while its assertions passed —
> they checked one side of a two-sided union. A merge guard counting non-SUCCESS checks would have
> merged a PR whose run had not registered, because an empty rollup satisfies "zero failures". And
> a scripted edit of this very section deleted **3,485 lines** of PLAN.md, because `## Current
> focus` is followed by a 3,375-line blockquote and the script took everything to the next `##`.
> Caught by the line count, restored from git, redone with exact-match edits. **Edit this section
> by matching its opening lines, never by heading-to-heading range.**
>
> **2026-08-08 — the "externally blocked" reading was too wide, and the user's own constraint is
> what falsified it.** Asked which milestones could still close given that this project has
> **Zombienet and Chopsticks and no public testnet**, the honest answer turned out to be *more*
> than the parks claimed, not fewer. Twenty-two screens sit `built-unwired` and twenty of them
> wait on one sentence — *a live transport*, or *a pinned chain to boot against*. That reads as
> unreachable without a testnet. It is not: `zombienet/networks/bleavit-xcm.toml` already spawns a
> four-validator relay, Bleavit at para 4242, **Asset Hub at para 1000** and Coretime at 1005, and
> `tools/env/generate-relay-specs.sh` writes every one of their chain specs. That is the exact
> two-chain topology the client needs, Asset Hub half included. So the artifact is **generated
> rather than committed**, and the dev-pin ruling of 2026-08-07 already covers the difference: a
> development pin may exist and may never enter `release-sources.json`. What is genuinely missing
> is the harness that boots the app against it, and that is code this track can write.
>
> **F27 is ✅ as of 2026-08-08.** Both R-6 conjuncts are met: PR #283 at `5d49b69a` reports
> **23 of 23 checks SUCCESS**, and the spec review of the final shape returned **0 blockers**.
> Drill 14 passes end to end — `pin`, `boot` at `compatMode: "full"` and `specVersion: 2` with
> a finalized head, and `wrong-chain` refused as `FE-BOOT-003` — through the shipped
> `client-boot.js`, which until today had never executed at all.
>
> **The milestone's own thesis held against itself.** Four drill runs were needed, and three of
> the four failures were the harness rather than the client: a teardown that left three
> collators of the previous network alive, a helper that reached zombienet's embedded Node 18
> instead of the system one, and a leg parsing a shared stdout as JSON. Each looked like a
> defect somewhere else. So did every finding this milestone started from.
>
> A park is a claim and it decays — three notes in two days were falsified by one command each,
> and this was the fourth.
>
> **Track F's remaining milestones after F27 are externally blocked, and that list is shorter than
> it was.** F1 waits on SQ-940 and a device lab; F11 on FE-P7 and the rollout blockers; F13 on the
> key ceremony and live gateways **only** — its two-environment byte-identical run has been a
> required check named exactly that since #269, and this file went on calling it external for four
> days. F14 keeps three device-lab rows and loses five to F27. F17, F18 and F26 wait on F27 alone.
>
> **Two questions are the user's alone.** **SQ-940** — `ar-io/ar-io-ant-process` was archived
> 2026-06-09 and the contracts moved from AO to Solana, and an ANT authorizes by membership, so a
> controller list is **1-of-m**. It blocks launch. **SQ-994's membership** — the floor of three
> gateways is ruled and enforced, and naming the operators grants permanent allowlisted egress for
> every user, which is a values judgement rather than an implementation choice.
>
> **15 §4.8's nightly cadence is declined, by the user, 2026-08-08.** The reason given was the
> Actions time it costs every night, and that was the half of the question only the user owned.
> So the Zombienet suites keep the `release` and `g1` tiers `suites.json` already admits, no
> scheduled workflow is added, and **the drills run on a release and on demand**. Recorded here
> rather than as a spec edit: 15 §4.8 states a cadence for a project that is running, and this one
> has no nightly budget to spend yet. Revisit it when a release cadence exists — the gap is a
> deliberate, owned divergence and not an oversight, which is exactly what would otherwise decay
> into one.
