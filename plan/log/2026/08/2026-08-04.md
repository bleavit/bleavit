# Session log — 2026-08-04

## **Track N landed** · Codex re-review discharged

**The re-review's third finding was refuted by measurement, and the measurement was only decisive because of an accident worth writing down.** Codex re-reviewed `1c30254` and raised one new P2: flipping `prime_register_scan` to `sealed: false` supposedly *activates* the ratio arithmetic, leaving `register`'s ref_time stale. **The premise does not hold** — at the parent commit `48ea2cb` there was no seal check at all, and the fixture's `contest_valid: true` / `accrued = MAX` meant the division and multiplication **already executed for all 64 records** in the run that produced the committed weight. The state it compares against (new guard *with* `sealed: true`) was never a committed measurement, because both changed in one commit precisely so the measured path would stay the worst case; the guard only ever adds an *earlier* return, and the fixture defeats every one. **Its general principle was right regardless** — unchanged storage dimensions establish reads and proof, never execution time — and my own report had leaned on exactly that, so it was re-measured rather than argued. **The accident: this workstation *is* `msi-pro-b650-s` / Ryzen 9 7950X3D, the host and CPU in the committed file's own header**, so a 50×20 run is same-machine *and* same-fidelity and the delta is signal rather than the 127 % cross-machine spread AGENTS.md warns about — which also retired the earlier 2×1 `+4.5 %` as a pure fidelity artifact. Result: `register` **1,205,304,379 → 1,179,416,198 (−2.1 %)**, i.e. the committed value **over**-declares; untouched functions moved up to −9.5 % (`settle`) and +2.2 % (`set_paused`) in the same run, bounding the noise floor above register's delta either way. **Deliberately not regenerated:** doing so would *lower* the declared weight, moving it toward under-declaration — the unsafe direction under R-7 — so the conservative committed value stands. Two existing mechanisms cover it independently: `register` carries an unused component (`_a`) so it is constant-weight, whose storage totals are a hard gate at any fidelity (passed), and the pallet's other fitted slopes already put it in the release-blocking *Component weights at committed fidelity* set. **CI on `1c30254`: all 22 checks SUCCESS**, verified on `conclusion == success` per check rather than on a waiter's exit code — the previous waiter had reported exit 0 while its own log read `CI|completed|cancelled`, because a push cancels the in-flight run on a non-`main` ref (R-12) and `completed` covers `cancelled`. **#227 merged; Track N complete, N1–N15 ✅.**

**What comes next:** Track N closed. Values-layer adoption of `svc.client_bond` / `svc.price_cap` / `svc.max_live` remains a user decision; SQ-574/575/576 stay open and none blocks a milestone

## PR #229 blocked on a main merge — **not** on its own gates

**`main` moved and the branch cannot merge; the first symptom was a CI run that never appeared.** After pushing four PLAN-only commits, `gh` showed no `ci.yml` run for `befc943` — only CodeQL. The cause is `mergeStateStatus: DIRTY`: `pull_request` workflows run against the **merge ref**, GitHub cannot build it while the merge conflicts, so no run is created at all. **A missing run is a conflict symptom, not a CI outage** — worth knowing, because "no run" reads as an infrastructure glitch and invites a re-push that changes nothing. `main` is at `45340d7` (Track N complete, N1–N15); the branch is 1 behind. `ci.yml` and `AGENTS.md` auto-merge; **PLAN.md is the only conflict**, in the shape R-9 predicts (Current focus / Milestones / Session log), and rerere recorded a preimage. **The blocking detail is an SQ id collision.** Track N took **SQ-577, SQ-578 and SQ-579** on main for its own questions while this branch used the same three for unrelated ones. A colliding id is the one thing an append-only index cannot absorb. **Renumber this branch's five, +3 each: 574→577, 575→578, 576→579, 577→580, 578→581** — main's highest is 576. This branch's ids are cited in `app/packages/descriptors/src/compat.ts`, `app/tests/descriptors/compat.test.js`, `app/tools/generate-critical-surface.ts`, `app/packages/descriptors/src/critical-surface.ts` (regenerate, do not edit), `app/packages/signing/src/raw.ts` and `app/packages/transaction-builder/src/rows.ts`. **Main's citations must not be touched**: `reference-model/.../slot_pricing.py`, `reference-model/tests/test_slot_pricing.py`, `docs/architecture/13-parameters.md`, `docs/architecture/16-hosted-question-service.md`. **Do not resolve the conflict hunks by hand** — my Current-focus rewrite de-aligned the diff so main's side of hunk 1 spans 1,207 lines. The deterministic route, already measured: take **main's PLAN.md as the base**, then re-apply this branch's additions as sets — 15 session-log rows (identified by set difference; main contributes 14 of its own), the resolved SQ-552 row (main still carries the open version), the five renumbered SQ rows inserted after main's SQ-579, batch **X** reconciled (main 15, this branch adds 3 genuinely new), batch **B7** with SQ-552 removed as resolved, and one merged Current focus.

**What comes next:** **Merge `origin/main` and renumber, with fresh context.** I stopped deliberately rather than push it through: PLAN.md is the canonical status record, a botched resolution silently loses either Track N's or Track F's history, and I had accumulated several avoidable errors by this point. **Nothing is lost by waiting** — the branch is clean at `befc943`, the exhaustive gate is **green 15/15 on `a31463f`** (Rust workspace, Model checking and Benchmark smoke all completed), and the only diff since that verified sha is PLAN.md. Re-run CI after the merge, since none has run against the merged tree

## main merged · V-83…V-97 restored · a V-id collision found

**The merge landed, and re-applying my rows surfaced two things worth more than the merge itself.** Resolved deterministically (main's PLAN.md as base, this branch's additions re-applied as sets) rather than by splicing a 1,207-line hunk. This branch's five spec questions renumbered **+3 to SQ-577…SQ-581**, since Track N had taken 574/576 on main for unrelated questions. **`ci.yml` now runs again**: the PR was `DIRTY`, and `pull_request` workflows build against the merge ref that GitHub cannot create while a merge conflicts — the missing run *was* the conflict. **Taking main's PLAN.md silently dropped 14 verification-log rows (V-83…V-96) and four milestone status flips**, and neither the table checker nor the batch checker can see that: both verify *shape*, not *presence*. Caught by diffing the two files rather than trusting the resolution. Restored, with V-97 added. **`V-72` is used twice on main** — the pnpm-reproducibility item and N10's client-integration-kit item — and it predates this branch. Spec questions get a uniqueness gate (`check-spec-question-batches.py`); **the verification log gets none**, which is exactly why this survived. The fix is a few lines beside the existing checker. **Method note, since it cost real time twice:** `cmd-A && cmd-B` reports success when A fails and B passes. It printed "V-97 recorded" over a `ValueError`, and earlier committed a broken table over a failing checker. Check exit codes explicitly when the second command is the verification.

**What comes next:** CI is running on the merged head — the first exhaustive run against this tree. Then rule SQ-580 (Multisig has no frozen surface) to unblock F6's multisig/proxy, and reconcile the manifest against 02 §9 (SQ-581). **Scope note for planning:** Track F is **5 of 22 milestones ✅**, 3 in progress, **14 untouched** (app shell, local index, providers, verify, distribution, degradation UX, reproducible build, performance, ops handbook, governance and operator surfaces, funding flow, handoff, skills, desktop). It is a multi-session track, not a session's work

## SQ-581 taken end-to-end, then reverted

**Merged #229 to main as `c3b65fb` (21/21 checks, CI 16/16 on `e92c608`), then took SQ-581 as far as it goes without a runtime build.** All **36** declared-but-absent constants were resolved against the committed metadata: **every one exists**, so all 36 are real omissions and my earlier "some are naming artifacts" caveat was wrong — the per-pallet `INTEGRATION_CONTRACT_VERSION` constants are genuinely separate entries. Generated entries with derived layouts; **`check-chain-feed.py` accepted all 36** against the real metadata (256 verified), `CRITICAL_SURFACE` regenerated to 256, and P-1's fee cross-check came back with both halves. **No contract bump needed.** **Then reverted, because the recorded feed blocks it**: two suites assert bidirectionally that every frozen surface entry has a chainHead fixture, and the new constants have none. Hand-writing them is exactly what app-code rule 12 forbids. Tree left green rather than shipped half-done. **The valuable part is that the fix is now fully specified** — add the entries, regenerate the feed from a booted runtime, regenerate the surface, restore the clause — with two traps named — and one of my own claims corrected: a constant's fixture is **not** content-free (all 39 differ; each records the expected-vs-actual layout), so the completeness gate is sound, and re-recording moves the pinned block so **all 222 fixtures change**, making this a regeneration cycle rather than an incremental add; and a naive snake_case pass mangles `INTEGRATION_CONTRACT_VERSION` per character.

**What comes next:** Regenerate the feed and land SQ-581 — it needs a runtime build, so it belongs with a backend session rather than at the end of a long frontend one. Then SQ-580 (Multisig, which *does* need a 02 change and therefore a contract bump). **Track F remains 5/22 ✅, 3 🔨, 14 untouched**; it is a multi-session track

## SQ-582 — the drift gate's own artifact has drifted

**Stopped deferring the runtime build, ran it, and it found something the deferral was hiding.** The node binary was already cached; the runtime wasm took ~47 s. Extracting metadata at HEAD gives **468,432 bytes against the committed 466,703**, and decoding both shows the runtime publishing two storage items the feed does not — `QuestionService.LiveExternalDepth` and `QuestionService.ScarcityMultiplier`, from Track N's N15. **Same spec_name, same `spec_version` 2, same contract v23, same 45 pallets**, so `check-chain-feed.py` sees nothing: it compares the *pallet set*, never the *surface set*. Two different runtimes both claim spec 2. Confirmed on **both** halves of the 10 §5.1 pair. **This is the defect F2 exists to prevent, recurring in F2's own artifact** — F2 was built because a keeper metadata blob sat wrong for eleven days under green CI, and the feed it produced is now stale under the gate it shipped with. **Not regenerated, deliberately**: the stale feed is the evidence, and erasing it before the extended gate exists would leave the next session unable to prove the gate fires. Ruling first, regeneration second.

**What comes next:** Extend `check-chain-feed.py` to diff the surface set, then regenerate. Note the cheap per-commit leg cannot do it from source alone — it would need the built runtime — so the surface diff may have to live on the release-blocking leg while the per-commit leg compares the committed blob against the committed manifest. Then SQ-581 lands on the same regeneration

## SQ-581 ✅ · SQ-582 ✅ — the feed regenerated and the gate that catches it going stale

**Both landed, and the second only existed because I stopped deferring the first.** I had written that SQ-581 "needs a runtime build, which belongs with a backend session". The node binary was cached and the runtime wasm took **47 seconds**; the reason for deferring was wrong. Running it found **SQ-582**: the committed feed described an older runtime (missing `QuestionService.LiveExternalDepth` and `ScarcityMultiplier` from Track N's N15) while `check-chain-feed.py` passed, because it compares *which pallets exist* and never *what they publish*. **Order mattered and was chosen deliberately**: write the gate, watch it fire on the live stale feed (both halves, exactly the two items), *then* regenerate. Regenerating first would have erased the evidence before the gate existed. The gate is **source-parsed** — `#[pallet::storage]` out of in-repo pallet sources — rather than diffed against a fresh build, which is what lets it run on every commit instead of once a release; one-directional and in-repo only, since feed entries with no in-repo declaration are SDK pallets, not drift. **SQ-581** then landed on the same regeneration: 36 constants 02 §9 declared frozen and the manifest never listed, layouts derived from metadata, **256** surfaces verified on both halves, no contract bump. P-1's fee cross-check came back with both halves — the clause that had cited `constant.market.min_trade` because the right constant was uncitable. **Two of my own defects caught by mutation**: the first tests covered the *parser* and not the *comparison*, so deleting the check broke nothing (fixed by extracting `stale_storage()` and testing it directly); and the recording silently degraded to classic-RPC because `websockets` was 9.1 where the recorder imports `websockets.sync.client`, added in 12 — the pinned `15.0.1` was in the README and I had logged the mismatch earlier and walked past it.

**What comes next:** Four mutations on the gate, all caught. Full local gate green: app 188 across six suites, tools/ci 175, tools/release 89, chain-feed, descriptors byte-identical, surface, depcruise + witness. **SQ-580 (Multisig) is next** and is the one that genuinely needs a 02 change plus a contract bump. Track F: F4/F6 unblocked on the SQ-581 side

## **`svc.client_bond` + `svc.price_cap` adopted — the hosted service is open**

**The user took the last two values-layer decisions Track N had left open, and the interesting part is what the tripwires caught on the way.** Adopted: `svc.client_bond` = **100,000 VIT** (4× `att.bond`, the only in-system VIT anchor) and `svc.price_cap` = **4×**; `svc.max_live` left at 16. Full reasoning and authority in the Decision log. Route was **genesis seeding, not a migration** — the chain is pre-genesis (no tag, no release), so 13 §1's migration paragraph describes the live-chain case and now says so. **Five gates fired, each one correctly, and none was a false alarm:** the genesis-key fixture (111 → 113), the doc-13 row-encoding count, `registry.toml`'s `genesis = false` claim on both keys, the reference model's independently-derived `REGISTRY` (which needed the rows *and* their derivations, not a count bump), and its doc-13 parser — which had never seen `svc.price_cap`'s unit string, because the extractor only reads rows carrying a concrete value, so **a `[VERIFY]` row's unit spelling is unverified until the day it is seeded**. That last one is a small instance of SQ-576's shape in the doc layer rather than the weight layer. **The one runtime test that failed was the one that should have**: `n4_client_bond_and_guardian_track_are_live_only_when_explicitly_seated` asserted the bond row was absent. It was **not** repaired by deleting the assertion — the consumer still reads the live row with no genesis fallback, so the test now proves fail-closed by *removing* the row and restoring it, keeping the only coverage of `ClientBondUnset`. Deleting it because the default changed would have quietly retired a safety path at the exact moment the service opened. **SQ-576's class was the live risk here and it held**: both `register` and `admit_client` prime their own benchmark state (`prime_params` seeds `svc.price_cap`, `prime_client_bond` seeds the bond), so the measured paths were already the armed ones — re-measured to confirm rather than assumed, and `register` moved −0.3 % with storage dimensions unchanged. Rust **1,959 tests green** across 87 binaries; reference-model **700 green**; weights, limit-coverage (206 keys, 0 unwired), doc links, PLAN tables and batch index all green; fmt/clippy clean.

**What comes next:** Open the PR; SQ-574 stays open — adoption chose a value, it did not derive one

## PR #232 · Codex P1 verified and fixed · 02 §11 feed regenerated

**The automated Codex review of #232 was right, and the reason generalizes: `ClientRegistry::ClientBond` is the only frozen metadata constant in this runtime whose value *is* chain state.** It is a `#[pallet::extra_constants]` fn over `RuntimeClientBond` → `live_balance_param(b"svc.client_bond")`, and unlike `u32_param_or`/`perbill_param_or` it has **no `default_param` fallback** — the absence that makes the row's presence the arming act is also what makes the published constant move when genesis seats it. `frame-support-procedural` computes a constant's metadata `value` by *calling* the function at `pallet_constants_metadata()` time, and `extract-metadata.py` reads metadata by `state_call` against a **booted node**, i.e. against genesis state. **Measured, not argued:** a chain booted from this commit publishes `Some(100,000,000,000,000,000)` where the manifest froze `0x00`, and diffing the *whole* manifest against the fresh metadata gives **exactly one drift in 220 layout-checked surfaces** — this constant and nothing else. **Two corrections to the finding as filed.** No app test reads that value (`critical-surface.ts` carries ids, not layouts; client_bond is not in the FE-R1 bounds table), so "tells frontend tests admission is unarmed" overstated it. And the sharper consequence it did not name: `check-chain-feed.py` compares the *committed* blob against the *committed* manifest, so **the stale pair agreed with itself and CI stayed green** — SQ-582's shape one layer down, and the reason this could not have been caught by any existing gate. **Fixed by regenerating, never by patching** (app-code rule 12): node + both runtime profiles rebuilt, both metadata blobs re-extracted from booted nodes, all **222 chainHead transcripts re-recorded** (`strict_ready: true`, 0 missing on either side of the pair), PAPI descriptors regenerated, and the manifest layout **derived** by `scale_metadata.surface_layout` rather than hand-written. `SUPPORTED_RUNTIMES` needed its two `metadataSha256` entries re-pointed — `surface:check` caught that itself, which is the F4 gate doing exactly its job. **The regeneration also closed SQ-582's artifact half, unplanned:** the committed feed was recorded at `3442e5c` and was missing `QuestionService.LiveExternalDepth` and `ScarcityMultiplier` from N15, so the new blob is 1,745 bytes larger and the surface-set diff against the old one is *exactly* those two items. That is the staleness #231 found; this branch fixes the artifact, #231 still owns the durable part — the set-diff **gate** — and nothing here duplicates it. **The gate that would have caught it now exists**: `frozen_manifest_constant_values_match_what_this_runtime_publishes` binds every frozen constant value to `Runtime::metadata_at_version()` under `development_ext()` — in process, no node, every commit — and it is **witnessed, not merely green**: reverting the manifest to `0x00` fails it with the exact drift line, so the rule is proven live. Two build traps worth carrying: the ecryptfs filename cap kills the wasm build in a `$HOME` target dir (AGENTS.md already says so), and a *failed* wasm build leaves `bleavit_runtime.compact.wasm` behind, which makes `substrate-wasm-builder` skip regeneration so the **compressed** blob is never written and `chain-spec-builder` then fails with a bare "wasm blob shall be readable". Green: runtime **449**, app **186** across six suites + build/depcruise/descriptors:check/surface:check/casts/budget/witness, `tools/release` **89**, `tools/ci` **167**, `check-chain-feed` 220×2 + pairing, fmt.

**What comes next:** Commit and push. **Coordination:** #231 (`track-f/feed-surface-gate`) is regenerating the same feed from a base without this adoption, so whichever merges second re-runs `app/fixtures/chain-feed/README.md` — the artifacts conflict by construction, not by mistake

## SQ-583 — CodeQL alert 19 (unplanned, user-requested)

**The repository's one open code-scanning alert is a false positive, and the evidence that settles it also retires the "just fix the code" option.** `rust/cleartext-logging`, **high**, on `pallets/conditional-ledger/tests/instance1_differential.rs:121`. Read the **SARIF code flow** off the analyses API instead of the summary line, which is what made the disposition provable rather than arguable: source `program.params.protocol_accounts.iter()` (:936–939) → `account(name)` (:178) → its `other =>` fallthrough (:183) → `fixture_failure` (:120) → `panic_fmt` (:121). The value that would be logged is **the label of a holder the corpus does not define**; that field holds one string, `carol`, in 1 of 11 fee rows and `[]` in the rest. **13 structurally identical fallthrough helpers sit in the same file and only the one fed through a `.*acc(ou)?nt.*`-matching field is flagged**, and alert 18's flow in the frame-free twin has the same two endpoints — one finding, reported once per differential suite. **The part worth carrying forward: the reported source moves under a fixed sink.** Alert 18 was raised citing line 273 and its SARIF today cites line 1028 with no change to that helper, because the alert anchors on the shared `fixture_failure` sink that many name-matching fixture fields reach — so a fix aimed at the named source re-anchors rather than closes, and the reliable code fix would de-interpolate all 13 helpers and degrade a normative differential's diagnostics (15 §4.4; I-37). Put three priced options to the user rather than assuming which one "fix it" meant; they chose the narrow one. **Alert 19 dismissed as `used in tests`** with a 276-character comment pointing at SQ-583; no code changed; the API now reports **0 open alerts** across the repository.

**What comes next:** Nothing outstanding on this alert. `used in tests` was chosen over `false positive` because it self-invalidates: if either differential's holder helper leaves `tests/`, the stated ground fails and alert 19 should be reopened. A third twin file would raise a third identical alert — at that point revisit the in-tree waiver surface (advanced setup with a narrow query filter), which was priced and declined here for branch-protection blast radius

## `Benchmark smoke` ✅ · two pre-existing weight defects · SQ-584 · main merged (#231, #233)

**The red gate was one line and this branch's; the sweep that bounded it found two more that were not, behind a gate that has never run.** PR #232's `Benchmark smoke` failed on `pallet_xcm::claim_assets`, estimated PoV **5,469 → 5,502**. Cause: seating `svc.client_bond`/`svc.price_cap` adds two leaves under the `Constitution::Params` prefix, which lengthens the trie path for the key `claim_assets` reads through the inflow-cap adapter (`Measured` 2,004 → 2,037; reads/writes unchanged at 6/4). CI and this workstation produced the *same* 5,502, so layout rather than jitter. **Causation was measured, not inferred** — a differential build of the identical tree with only the two `row(...)` calls removed reproduces 5,469 — and that same build is what ruled the adoption *out* as the cause of what came next. **The first bounding argument was wrong and had to be checked.** "Only `Constitution::Params` readers can move" holds for this branch (107 such functions; the 97 constant-weight ones are hard-gated at 2×1 and CI named exactly one; the other 10 re-checked at 50×20, clean) but it does not explain `pallet_execution_guard::qualify_recovery_image`, which reads no `Params` key and drifted anyway. Sweeping **all 17** pallets carrying a fitted component at 50×20 — the release job's own scope — found two stale weights, both reproduced by the differential build and therefore pre-existing: `qualify_recovery_image` **understates** worst-case PoV by 34 B and `pallet_collator_selection::add_invulnerable` **overstates** by 99 B. Both files predate N7 (2026-08-02), which added the `QuestionService`/`ServiceLedger` prefixes to `construct_runtime!` and restructured the global trie. **Neither was catchable:** both are component-bearing, so `benchmark-smoke` at 2×1 demotes them to advisory *by design*, and the gate that hard-gates them is release-blocking with `git tag` still empty. Both regenerated on the user's instruction; the durable fix is a trigger rather than a regeneration, raised as **SQ-584**. Keep one number: the 2×1 advisory figure is a *different* number, not a noisier one — `add_invulnerable` reads 12,043 there against 12,119 canonical — so promoting the 2×1 verdict to hard would manufacture false failures instead of coverage. **The one runtime test that failed was the one that should have:** `pov_budgets::xcm_claim_assets_fits_the_normal_class` pins that proof bound as a literal, so it is a tripwire on the *state layout* and not only on pallet code; repinned to 5,502 with the derivation recorded beside it, and `assert_fits` still passes. Nothing else in the tree pinned any of the three bounds. **`main` moved twice mid-session** — #231, then #233, which took **SQ-583** for its own row, so this branch's renumbered to **SQ-584**: the SQ-574/575/576 collision shape, caught before landing this time. #231's feed regeneration conflicts with this branch's by construction; resolved by taking main's side for the generated artifacts and then **regenerating the whole feed from the merged tree**, which is required rather than tidy — #231 added 36 manifest entries and the recorder takes its work list from the manifest, so picking either side whole would ship a feed missing surfaces. Both runtime profiles rebuilt, both metadata blobs re-extracted from booted nodes, **258** surfaces re-recorded (`strict_ready: true` on both halves), descriptors regenerated, `critical-surface.ts` re-derived, and `SUPPORTED_RUNTIMES`' two `metadataSha256` entries re-pointed — `surface:check` caught that itself, again. **One self-inflicted scare worth recording:** `git stash --include-untracked` run while a merge is in flight silently discards `MERGE_HEAD`. Every file was recoverable from the stash; the merge relationship was not, and had to be re-established by hand. A stash is not a pause button during a merge. Gates: app **13/13** (build, depcruise, depcruise:witness, firewall, protocol, casts, descriptors:check, mock-runtime, chain-client, surface:check, descriptors, tx, budget); `check-chain-feed` 256 surfaces × 2 + pairing; weight purity, storage bounds and regression; the drift gate at CI's own 2×1 on all three regenerated pallets; `rust-workspace-gates.sh --changed bleavit-runtime` exit 0 with **449** runtime tests; PLAN tables, batch index, dispatch mirror, integration ABI, quickstart drill, runbooks, limit-coverage, environments, alert coverage; tooling suites release 89 / ci 175 / deploy 98 / env 84 / monitoring 111 / reference-model 12 / phase-gates 74.

**What comes next:** Push the merge — CI is the first run against the merged tree. **SQ-584 is the open thread:** until the component-fidelity check has a per-commit trigger, the next `construct_runtime!` addition can understate a weight again and only a release would find out.

## SQ-580 ✅ — contract **v24**, and the gate that asks the inverse question

**SQ-580 was filed as one missing surface and is twelve.** Building the inverse probe — *which client obligations in docs 10/11 name a surface 02 does not freeze* — turned `Multisig.Multisigs` into a class: the four `Referenda` items, both `Preimage` items, both `ConvictionVoting` items, `Scheduler.Agenda`, `System.Events`, and **`Epoch.ResourceLocks` — pallet-epoch's own storage, missing from its own §7.1 table** while 09 §1.2(8) makes it a dispatch-time check. All twelve exist in the runtime and always did; what was missing was the contract obligation, and with it any probe. **The ruling worth recording is why upstream SDK storage belongs in a Bleavit contract**: it belongs *more* than Bleavit's own. Bleavit storage moves only when Bleavit changes it, and §13 rule 2 makes that bump this counter; an SDK release-line bump moves an upstream layout with **no Bleavit source change and no counter to notice**. §7.4 had already taken that judgement for `System.Account` and `ForeignAssets.Account` — v24 completes the category rather than opening one. **One thing had to be given up, and it is recorded as a weakening rather than a detail:** `System.Events` expands to **2,265,785 characters** because `Vec<EventRecord<RuntimeEvent>>` transitively contains every event of every pallet — 12× the whole manifest, and a drift signal that fires when any unrelated pallet gains an event. It is frozen as its container with `RuntimeEvent` elided (236 chars), which is sound **only** because §6 freezes that subtree event-by-event; so the rule adopted is *elide only what another frozen entry covers*, and `OriginCaller` — frozen by nothing else — keeps `Referenda.ReferendumInfoFor` and `Scheduler.Agenda` expanded at ~29 KB each. **Full regeneration cycle, not an incremental add**: node build (7m13s), both runtime profiles, both metadata blobs, all **270** chainHead fixtures re-recorded at a new pinned block (`strict_ready`, chainHead-v1, nothing missing on either half), descriptors, `CRITICAL_SURFACE` (268 probed), and `SUPPORTED_RUNTIMES` — which `surface:check` caught stale, doing exactly its job. **A second record defect surfaced on the way out:** SQ-581 and SQ-582 had status cells reading `open` **and** `**RESOLVED 2026-08-04.**` at once; the batch checker tests the status *prefix* (rightly — an open row's prose legitimately contains "resolved"), so it believed the wrong half and only spoke when closing SQ-580 made it look. Both flipped, all three dropped from batch X.

**What comes next:** Three adversarial Codex reviews (`gpt-5.6-luna`, `max`, read-only) were running against this change at hand-off — spec ruling, elision soundness, gate vacuity — plus the exhaustive Rust gate. **Then F6's multisig/proxy, which SQ-580 existed to unblock**: the sharp part is that a wrapper splits identity in two — the call acts as the multisig/proxied account while the *signer* pays the fee — so a precondition table that checks one account for both is checking the wrong one

## Three adversarial Codex reviews (`gpt-5.6-luna`, `max`, read-only) on the v24 batch — **two blockers in my own work**, both fixed

**Running the reviews was worth more than the change they reviewed.** (1) **Blocker — the new CI gate was untracked.** `ci.yml` invoked `check-client-surface-obligations.py` while `git status` still showed it `??`, so a clean CI checkout would have failed on a missing script. Caught only because a reviewer read the *diff* rather than the working tree. (2) **Blocker — the `::` half of the extractor was untested.** All four positive controls were dotted, so narrowing `(?:\.|::)` to `\.` dropped `Market::Fee`, `ConditionalLedger::RedemptionFee` and `ServiceIdBase` — the two rate constants 11 §11.5 calls compulsory — while every control still matched and the count floor still cleared. Three `::`-form controls added. (3) **Major — my own §7.6 table had five wrong hashers**: §7 defaults maps to `Blake2_128Concat` and `TrackQueue`/`DecidingCount`/`VotingFor`/`ClassLocksFor`/`Agenda` are `Twox64Concat`, so a client following the table derives wrong storage-proof keys. (4) **Major — the tests read the floors back from the module they were testing**, so `MIN_REFERENCES = 0` passed; floors are now literals. (5) **Major — `load_waivers()` was never exercised**: every test monkeypatched it, so `return {}` survived the suite; the real TOML path is now tested. (6) **Major — my elision coverage claim was false.** I wrote that §6's entries cover the elided subtree; the review counted **125 manifest event rows against 427 runtime event variants**. The honest claim — everything in the subtree that this contract freezes is frozen by §6, and the rest is not contract surface — is now what 02, the manifest and this log say. (7) 02's header still declared **v22 and 12 API methods**, stale since v23.

**What comes next:** **Three new spec questions the reviews earned:** SQ-583 (terminal referendum variants carry no tally, and two documents promise one — verified: `tally` occurs exactly once, inside `Ongoing`), SQ-584 (nothing derives 02 §6's event obligations from runtime source, and event entries discard the SCALE `index`, so inserting an event shifts discriminants invisibly — the event-side twin of SQ-582), SQ-585 (the Asset Hub `Assets.Account(1337, who)` read 11 §11.6 mandates is frozen nowhere; invisible to the inverse gate **by construction**, since it only extracts Bleavit `construct_runtime!` prefixes — the family recurring across a *chain* boundary). **NOT DONE: the exhaustive `rust-workspace-gates.sh` did not finish** — it was still compiling `bleavit-runtime` when the session closed for a reboot, and the scratchpad it built into is `/tmp` and gone. The Rust evidence that exists is the **changed-scope** run (fmt/clippy/25 tests on `futarchy-primitives`); every Python and app gate is green on the committed tree. **Re-run the exhaustive gate first thing** — a workspace-wide constant moved, so it is the one piece of verification this commit does not carry

## F6 — the wrapper identity split wired into P-1…P-15; **SQ-588 raised, and it blocks F6 alone**

**Wiring multisig/proxy into the precondition tables found that the tables had no notion of *whose* account a clause reads.** Every clause implicitly meant "the signer", which is correct only for an unwrapped extrinsic. Under `Proxy.proxy` the inner call executes as `real`, under `as_multi` as the derived multisig account, and the signer still pays the fee — so an implicit subject checks the wrong account and **fails green**: the signer's healthy balance turns every row green while the runtime rejects the inner call. All 67 clauses now carry a `subject`, and **11 §11.5's own P-3 text is split** — "USDC balance ≥ amount + fee headroom" is one clause only while signer and origin coincide, so the two reads are separated where they happen rather than corrected in the document, whose sentence stays true of the case it describes. `recipient` is a third subject, not a variant: P-9's bounds are the *destination's*, and an absent recipient **throws** rather than falling back to the signer, because substituting a healthy sender would pass a transfer the runtime refuses. Two mutations run against the new assertions (re-conflate P-3; demote every `acting` clause to `chain`) — both caught, including the anti-vacuity one that would otherwise let the whole mechanism be inert.

**What comes next:** **SQ-588, found by reading 11 §11.5's P-9 row against the table rather than trusting it:** the row mandates "recipient is not a protocol account", the runtime tests `MarketProtocolAccounts::contains_key`, and 02 freezes that item nowhere — so the clause has no `SurfaceId` and **was silently missing**. Sixth instance of the family. **The part that matters more than the instance:** the inverse gate I shipped this session *cannot see it*. It extracts `Pallet.Item` references, and P-9 states its obligation in prose naming no surface — so a gate built for exactly this family is blind to any obligation phrased as a requirement rather than a citation, the same blindness as SQ-585 reached by a different route. That bounds what the gate may be credited with, and it is why the reading was done by hand. F6 is otherwise complete and stays 🔨 on this one ruling

## F10 🔨 — `packages/verify`: identity pins, release self-check, verification panel

**Started the first Track F milestone that needed no unblocking, and its three properties are all ones that fail silently.** (1) **A genesis mismatch is terminal, and it is not INV-FE-12's `restricted`.** Those reduced modes exist for a runtime whose *surface* is partly unknown — the same chain, further along than the client. A wrong genesis means a **different chain**: no subset of functionality is safe, and every balance rendered belongs to someone else's account. So the verdict type carries **no severity and no override** — there is nothing for a caller to weigh, because the only correct response is to stop. (2) **Chain-spec hash and genesis hash are separate obligations, and the order matters**: the spec check runs first, because if the bytes handed to smoldot were not the release's own then the genesis they yield is a fact about an input the release did not choose, and reporting it as a genesis mismatch names the wrong failure — the chain looks wrong when the file describing it was. (3) **Divergence is surfaced, never repaired** — INV-FE-8's own closing sentence — enforced structurally rather than by convention: the package exports no repair or refetch path and a test asserts that it does not, because a refetch asks the channel that just served wrong bytes for better ones and the user gets a flicker instead of a warning.

**What comes next:** **Two shapes worth carrying.** The self-check reports an **`unexpected` served file** as its own finding, not as a variant of `changed`: it is the direction a manifest-driven loop structurally cannot see — nothing the signed manifest lists has changed, so iterating the manifest reports everything in order while an extra file rides along. And `buildPanel` is **synchronous and takes no chain handle**, because 10 §3.2 names the verification panel among what still renders under `FE-BOOT-002` when smoldot never started; a panel that awaited a read would be unavailable in exactly the incident it exists for, and its absence would look like part of the same outage. 17 tests; two mutations run (drop the `unexpected` direction; let a genesis mismatch keep operating) and both caught. `tests/verify` added to the pnpm workspace — the list is explicit by design, so the frozen-lockfile path CI uses was re-verified rather than assumed. **F10 stays 🔨**: the panel component, the O1-provisional `release.json` binding and platform hashing remain, the last deliberately the caller's since `SubtleCrypto`, node and Tauri differ while the comparison that decides whether a user is warned does not

## F8 🔨 — `packages/local-index` gap-tolerant coverage

**The dangerous operation in an index is the one you get for free.** The natural implementation extends a cursor and joins whatever is adjacent; 10 §6.3 forbids exactly that across provenances, and the reason is worth stating because the merge *looks* like better coverage: a joined range must claim **one** origin, so claiming `self` tells the user that provider blocks were light-client verified — a silent promotion §2.2 says has no path at all — while claiming `operator` merely discards verification. Neither is available, so the boundary is kept and rendered. Same reasoning as `Finalized<T>` not living in `shared-types`, one layer down. Ranges join only on **identical** provenance (two operators are two sources); overlapping provenances keep **both** ranges, because those blocks genuinely were ingested twice and "is block N verified?" must still find the `self` range.

**What comes next:** **Two consequences pinned by test rather than left to be found later.** The **1,200-block gap** 10 §6.3 says cannot be closed with verified data stays open — that section withdrew the old "local-index catch-up; history continuous" promise as impossible, and code that quietly closed such a gap would be re-implementing it. And **merging coarsens invalidation**: once same-provenance ranges join, the joined range is the unit, so corruption anywhere in it drops the whole span and a stale reference to a sub-range matches nothing. That surfaced as a failing test whose *premise* was wrong, not whose code was — worth the correction, since the alternative was shipping an expectation the model does not hold. Two mutations run; the first had to be **rewritten because `tsc` caught it before the test did** (unused parameters), which proves nothing about the suite — redone so it compiles, and the assertion fired

## F20 🔨 — `packages/intents`: the inbound format, its refusals, and the clamp

**Built the part of the handoff that an attacker actually touches, so the suite is a corpus of refusals rather than a happy path.** The rule worth carrying is the one that reads like an inconsistency: an unknown key at the **top level** is tolerated and an unknown key inside **`action`** is refused. 10 §13.2 gives the reason — a top-level extra is a producer annotation no consumer reads, while one inside `action` is a *proposed semantic* and "precisely where an encoded call would be placed", so tolerating it there would be tolerating the attack. Both halves are asserted **on the same document**, because testing them separately lets the first pass for the wrong reason. Two more that are easy to invert and are now pinned: a **newer** runtime is refused while an **older** one is admitted and rebuilt (an intent's version never selects an encoding), and **`FE-HANDOFF-009` stays retired** — the family is `001..013` *with the gap*, because a reused code makes two different failures indistinguishable in every log and support thread written on either side of the reuse.

**What comes next:** **The clamp is where the client asserts authority**, and the shape matters as much as the arithmetic: a ceiling takes the minimum of asked/chain/policy and a floor the maximum, so the worst a hostile document achieves is a transaction the user was already permitted to make on terms *worse for the attacker*. `Clamped` retains **asked and encoded together** with which bound won — collapsing them into one number would destroy exactly the distinction (`external-proposal` vs chain-derived) that lets a user notice the tool asked for something the client refused. Amounts survive past 2⁵³ as decimal strings, which is the V-74 trap one layer over. Two mutations run — tolerate foreign `action` keys; let the tool's ceiling win — and both caught. **F20 stays 🔨**: the two export formats, the transport layer, `schemas/`, the canonical digest and `FE-HANDOFF-010..013` remain, the last needing the exporter and a refreshed block

## F20 — the export side: scope, consent, canonical JSON and the digest

**Two of these are honesty properties rather than correctness ones, and both are the kind a reasonable implementation gets subtly wrong.** (1) **Consent is per export and never inherited.** Opt-in alone is satisfied by a checkbox that remembers — and a remembered scope is exactly how a user who shared holdings once, for one question, ships them to a third party every time afterwards having agreed once. So `defaultScope()` **takes no argument**: a `defaultScope(previous)` signature is all it would take to make consent sticky, and the test asserts the arity rather than the intent. (2) **Pseudonymization is labelled for what it does not do.** 11 §11.14.4 requires the offer *and* the label; the label says it replaces the address and leaves the holdings, which remain a fingerprint — what leaks is **linkage rather than content**, since the amounts are already public but a capsule pasted into a hosted service ties them to an identity in that service's logs and cannot be un-sent. Offering it when no account scope is in play is **refused**, because that would imply the capsule otherwise identifies the user.

**What comes next:** **The digest deliberately does not authenticate**, and the module is shaped so it cannot be mistaken for something that does: 10 §13.1 says capsules are unsigned because signing one with the user's chain key would reuse a signing key for a non-chain purpose and manufacture an artifact that *looks* authoritative — so the package exports nothing matching `/sign|authenticate|attest/`, asserted by test. The pre-image is `tag ++ NUL ++ canonical(core)`; the NUL is what stops tag `ab`+payload `c` colliding with tag `a`+payload `bc`, which across formats would let a receipt's digest validate a context. **A defect of my own, caught while writing it:** the NUL bytes went into the source **literally**, which is invisible in an editor and silently mangled by anything that assumes text — replaced with unicode escapes in both the module and the suite. Canonical JSON refuses an unsafe-integer `number` rather than rounding it (V-74 one layer over: a lossy amount yields a document whose digest is stable and whose contents are wrong). 18 tests

## **Exhaustive Rust gate GREEN on v24** · two adversarial Codex reviews found **2 blockers and ~20 majors in this session's own new code** — partially fixed, rest in `git stash@{0}`

**The gate first, since it was the outstanding debt:** `rust-workspace-gates.sh` completed end-to-end on the contract-v24 tree — **3,484 tests, 0 failures**, every leg including the runtime release/benchmarks/try-runtime builds, `no_std`, weights and limit-coverage (206 keys, 0 unwired). That closes the one piece of verification the v24 commit shipped without. **Then the reviews, and they were worth more than the code they reviewed.** `gpt-5.6-luna` at `max`, read-only, two in parallel over `intents`/`contexts` and `verify`/`local-index`/`wrappers`. **Blocker A (`intents`): digest admission is simply absent** — 10 §13.1 gates every inbound document on its digest and `parseIntent` never reads the field. **Blocker B (`verify`): the panel was fail-open** — `mayOperate` returned `true` when the chain identity had *never been checked*, so skipping verification was indistinguishable from passing it. **A concrete correctness bug: P-9's per-entry deposit was classified `acting`**, but 03 §4 says the deposit is taken from the entry owner, "the *recipient* on `transfer`" — so a healthy sender passed the row while the runtime rejected the transfer for the *recipient's* headroom. Verified against the spec before accepting the finding.

**What comes next:** **The rest, all real:** prototype-chain bypasses in **both** `runSelfCheck` and the intent parser (a lookup consults the prototype while `Object.entries` does not, so `Object.create({'app.js': good})` is neither compared nor reported); an empty manifest yielding a *verified* panel because `assertCheckable` was a separate call nobody was forced to make; **`self` provenance being caller-forgeable** in `coverage.ts` — a string union is not authority, so an operator payload relabelled `origin: 'self'` manufactures verified coverage through the front door; a getter that throws escaping the whole-document refusal contract; amounts unbounded against `u128`; refusal *details* interpolating attacker-controlled field names while the module promised they never would; expiry throwing an exception instead of emitting `FE-HANDOFF-008`; `clampLimits` producing **no ceiling and no refusal** when the caller omits the chain-derived cap; and several of my own tests shown vacuous by exact named mutations. **Partially remediated then stashed for the reboot** (`git stash@{0}`): the panel fail-open (renamed `chainIdentityVerified`, defaults false), own-key/null-prototype reads, `assertCheckable` moved *inside* `runSelfCheck`, the P-9 subject, and a `SelfIngested` brand so `origin: 'self'` is mintable only by `selfRange()` — the `Finalized<T>` device one layer down. **Those changes do not compile against the existing suites yet** (three test files construct the old shapes), which is why they are stashed rather than committed: R-9 forbids a red commit. **F6/F8/F10/F20 all stay 🔨**; none may be credited until this list is discharged

## Review remediation, round 1: the `verify` blocker, both prototype bypasses, the forgeable `self` origin and P-9's wrong subject

**Discharged the fail-open blocker and the three findings that were exploitable rather than merely untidy.** (1) **The panel no longer fails open.** `mayOperate` returned `true` when the chain identity had *never been checked*, so an app that skipped verification was indistinguishable from one that passed it. The flag is renamed **`chainIdentityVerified`** and defaults **false** — the rename is the fix, because the old name asked "may we proceed?", which has a plausible `true` answer before any check, while the new one asks "was it verified?", which does not. The panel still **renders** with nothing verified; those were two questions in one boolean and separating them is what resolves the tension with 10 §3.2. (2) **Own-key, null-prototype reads** in `runSelfCheck`: a plain lookup consults the prototype chain and `Object.entries` does not, so `Object.create({'app.js': good})` made the comparison see a good hash the enumeration never listed — a tampered file neither compared nor reported. Both directions now tested. (3) **`assertCheckable` moved *inside* `runSelfCheck`**, since as a separate call it protected only callers who remembered it, and an empty manifest otherwise produced a **verified** panel. (4) **`origin: 'self'` is branded** (`SelfIngested`, mintable only by `selfRange()`) — a string union is not authority, and relabelling an operator payload manufactured verified coverage through the front door.

**What comes next:** **The correctness bug, verified against the spec before accepting it:** P-9's per-entry deposit clause was `acting`, and 03 §4 takes the deposit from the entry owner — *"the recipient on `transfer`"* — so a healthy sender passed the row while the runtime rejected for the **recipient's** headroom. Subject and wording both moved; "you" was the wrong person. **Two honesty corrections to my own tests.** TypeScript proved the `providerId` runtime check unreachable once the union was branded — kept anyway, through a widened view, because the callers that matter are **untyped**: the suites are JavaScript and so is any decoded storage record, and a type that cannot be violated in TS is still violated by JSON. And the "no repair" test **overclaimed**: the review showed `globalThis.location?.reload()` inside `runSelfCheck` survives a name scan. The comment now says what the scan is (a review aid, not a proof) and a **purity test** covers the half it cannot reach. 15 app gates green, 18 verify tests. **Still open from the reviews:** the `intents` digest blocker, getter-throw escape, `binding` closedness, u128 bounds, `id` validation, the fail-open `clampLimits`, detail echoing and `FE-HANDOFF-008`

## Review remediation, round 2: the `intents` **digest blocker**, and the documented network gate that did not exist

**The blocker was an admission check that was defined and never run.** 11 §11.14.1 lists seven admission checks — "schema equality, **digest**, chain binding, expiry, closed-object shape, limit presence and internal consistency" — and the parser performed four of them. `FE-HANDOFF-010` existed as a constant and was **unreachable**, so an altered or truncated document was admitted exactly like an intact one, and no test that parses a valid document could ever see it. The fix is not just to add the check but to make it **unskippable**: the module now exports **`admitIntent`** and no digest-free parse entry point at all, and the hash function is a **required** field of the admission context — an optional one is a digest check that defaults to *off*, the same defect `assertCheckable` had in `packages/verify`. Hashing stays the caller's (SubtleCrypto is async, so admission is), while the pre-image moved to a new zero-dependency **`@bleavit/handoff-envelope`**: 10 §13.1 states the envelope conventions once for all three formats, and two copies are two answers to the one question that must have exactly one — *which bytes are hashed*. It is a separate package rather than a module in `contexts` for a firewall reason: `contexts` depends on `chain-client` (export's input is `Finalized<T>`), and the **inbound** parser must not be able to reach a chain connection even transitively. **The other seven `intents` findings**: `binding` is closed like `action`/`limits` (10 §13.1 tolerates extras "at the top level only"); every amount is bounded against `u128` and every version against `u32`; the **id is a canonical u64 decimal** — one rule that removes URLs, control characters, bidi overrides and homoglyphs at once, and without which 11 §11.14.4's id-substitution defence has nothing to resolve; the input is **text**, which kills the getter-throw escape and the read-twice hazard at the source rather than one field at a time; the parser bounds are now **computed** from 02-frozen type widths (~1.1 KB, not 64 KiB) and measured in **UTF-8 bytes**, since `.length` admitted four times the cap in astral characters; expiry is **`FE-HANDOFF-008` on arrival and `FE-HANDOFF-011` at the refresh**, never a thrown error escaping the taxonomy; and **no refusal echoes a document-supplied string** (10 §13.4) — the details were composing attacker-chosen field names into user-facing text, asserted now by a marker in every string slot.

**What comes next:** **The `clampLimits` fail-open, which looked like nothing:** with no client-side ceiling recomputed at `B′` the whole `maxCost` entry was *omitted*, so what reached the encoder was a trade with **no cost bound at all** — the widest possible limit, from the function whose entire contract is that limits only narrow. Now `FE-HANDOFF-011`. `narrowMaxAge(-1, …)` had the same shape and returned the client **maximum**. **`local-index`:** an inverted span made `holesIn` return `[]`, which reads as *complete coverage* — in the one module whose purpose is to make missing data visible; and `addRange` never checked its own input, so two same-provenance ranges that already overlapped (a rehydrated IndexedDB blob, exactly the corruption INV-FE-7 assumes) survived every later add. Both refuse now, and the invariant is closed under the API. **The gate AGENTS.md described and the repo never had:** the `no-network-primitive-in-handoff` rule matches **imports**, and every primitive rule 6 names — `fetch`, `XMLHttpRequest`, `WebSocket`, `EventSource`, `sendBeacon`, dynamic `import()` — is a **global**, so a handoff package could have called `fetch()` under fully green CI. Added `check:handoff-network` with a witness fixture, and proved it fires on a real package by injection. **Non-vacuity proven by mutation, not by a green run**: deleting the digest comparison kills 3 tests, the canonicity guard 1, the span guard 1. 16 app gates green (318 tests). **Still open:** the multisig account is caller-supplied rather than **derived** — that one needs a Rust-produced fixture and belongs to F6

## Review remediation, round 3 — three adversarial Codex reviews on round 2 found **17 findings in my own fixes**, incl. 6 majors

**Ran the reviews the user standing-instructed (`gpt-5.6-luna`, `max`, `--sandbox read-only`) against the round-2 commit, and they were worth more than the commit.** The three that matter most, all on the inbound parser. **(1) A missing monetary limit was admitted and then *defaulted* from chain state** — 10 §13.2 says in as many words that a missing limit is "refused, never defaulted; there is no safe default for money", and letting the clamp step supply the client's own value is defaulting by another name: the tool stated no bound and the transaction acquired one, so nothing the user saw as *asked* had ever been asked. The limit is now required **and direction-matched** — a prepare buys and carries a ceiling, a close sells and carries a floor; the wrong one is not a tighter bound in other units, it is a limit on a quantity the trade does not produce, sitting on the confirm screen looking like protection and binding nothing. **(2) Duplicate JSON keys were last-wins.** A document can say two things at once: a tool renders the first `action` while Bleavit admits, digests and confirms the second — nothing is malformed to either side, they simply read different documents, and the digest cannot see it because it is computed over the collapsed object. Refused now, comparing keys **after unescaping** (`\\u006f` is an `o`). **(3) The digest field accepted any even-length hex**, so the algorithm was whatever the caller's function happened to be — and a two-character "digest" against a real SHA-256 was reported as **tampering** rather than as a malformed field, blaming the channel for a document that was never a `bleavit.intent.v1` document.

**What comes next:** Also fixed: fields are read as **own properties** (an ambient `Object.prototype` pollution let `{"schema":"…"}` alone be admitted on inherited containers the closed-shape scan never enumerates); `genesisHash` is shape-checked **before** comparison, since `null` is a malformed field and reporting it as "a different chain" tells the user something false about what the file was for; the byte cap gained a **6× escaping allowance**, because the ceiling table counts decoded widths while the cap measures received bytes — a conforming document written in `\\uXXXX` escapes was being refused for how its producer spelled it; and `clampLimits` now validates **its own inputs**, since a `chainDeadlineBlock` behind `B′` encodes a transaction born expired and `NaN` propagates through `Math.min` into the encoded deadline. **`canonical JSON` was not canonical across languages** — JS sorts keys by UTF-16 code unit (astral keys land before `U+E000`; Python's `sort_keys` disagrees), `1.0`/`1e-7` render differently in every language, `Date`/`Map`/`Set`/`RegExp` all serialize as `{}` so three unrelated documents share one digest, a sparse array emits `[,]`, and `TextEncoder` folds every lone surrogate to `U+FFFD` so two domain tags collide. All six closed. **The network gate I had just added was itself weak**: it matched `fetch(` and missed `const f = globalThis.fetch`, `fetch?.(`, a call split across lines, workers, resource elements and `eval` — and its witness proved only that eight regexes matched eight spellings, never that the scan **reached** the packages. Now 15 patterns over whole files, a shared path list with dependency-cruiser so the two gates cannot disagree, and a witness that fails on a mis-declared path. dependency-cruiser's handoff rule was a **named-library denylist** (`import ky` defeated it); replaced by "nothing on a handoff path imports anything external, node built-ins included", in both recorded forms — and the `dependencyTypes` half **could not fire** until a resolvable import was added to the witness, since every existing witness import is unresolvable. **`local-index`**: block heights are u32-bounded (`2**53 + 1 === 2**53` made two ranges one apart join and claim a block nobody ingested), `holesIn` validates its **ranges** and not only its span, unknown origins and empty provider ids are refused, `holes` is checked as derived rather than supplied, canonicity is O(n log n), and `invalidateRange` validates like every other mutation. **Two of my own claims were wrong and are corrected rather than defended**: a code comment said `holesIn` "double-counts the overlap" (it computes the union), and AGENTS.md said the 1,200-block gap "stays open" as though the module enforced it — the `self` brand is compile-time only, an untyped caller can forge it, and the real control is INV-FE-7 plus the firewall. Spec (R-1): 10 §10.1 gains `handoff-envelope`; 10 §13.3's `FE-HANDOFF-004` class widens to "a closed core object (`binding`, `action`, `limits`)", which 13.1's extras-at-the-top-level-only rule already implied. 16 app gates green, 355 tests

## F6 — the multisig account is **derived** from the runtime's own rule, not taken from the caller

**`as_multi` executes the inner call as an account nobody chooses, and the client was letting a caller name it.** That account is the one every 11 §11.5 precondition row reads — the balances, positions, bonds and locks that decide whether the inner call succeeds belong to it and not to the signer — so a wrong value is silent in the dangerous direction: the client reads some other account's healthy balance, reports every row green, and the runtime rejects. The user signed something the client had told them would work. `MultisigAccount` is now a branded string only `deriveMultisigAccount` can mint, in the same way and for the same reason `Finalized<T>` is constructible only inside `chain-client`: a value that decides what gets read must not be writable as a literal. `otherSignatories` refuses a signer outside the set, because `ensure_sorted_and_insert` inserts the caller *before* deriving — so a non-member produces a different account on chain, and every precondition just evaluated belongs to an address the transaction never acts as.

**What comes next:** **The derivation is checked against the runtime, not against my reading of it.** New `runtime/bleavit-runtime/src/tests_multisig_derivation.rs` emits `pallet_multisig`'s own answers to `fixtures/multisig-derivation.json`, read **in place** by `app/tests/transaction-builder/multisig.test.js` — the quote-agreement pattern, where whichever side moved is the side that goes red. It publishes the SCALE **pre-image as well as** the account, because a wrong hash, a missing compact length prefix and a byte-order slip otherwise all present as "the accounts differ"; and it asserts its own pre-image hashes to its own account, so a wrong domain prefix cannot produce a fixture that only the client appears to get wrong. Nine cases, chosen for what they distinguish: same set / different threshold, adjacent-byte neighbours, and **63 vs 64 signatories** — SCALE's compact length widens at 64, so a hardcoded one-byte prefix passes every small committee and derives a wrong, perfectly well-formed address for a large one. Proven non-vacuous by mutation: encoding the prefix as a `Vec` kills 7 of 14 tests, a single-byte compact kills 2. The hash is **injected** (`blake2b-256` is not a platform primitive anywhere this app runs — `SubtleCrypto` lacks it and node exposes `blake2b512`), so what lives in the package is the pre-image construction, which must not vary. 81 tx tests green; runtime fmt and clippy clean

## **Contract v25 — PARKED on PR #232, one red gate, stated exactly.**

**Rebased onto `main` at `d5a997e9`** (PR #231 merged): 13 commits replay clean, `rerere` had nothing to do. **Complete and green:** the fourteenth `FutarchyApi` method `is_reserved_protocol_destination` (`runtime-api`, `#[api_version(4)]`) implemented against the pallet's own `ReservedProtocolDestinations` `Contains` rather than a restatement of it; `INTEGRATION_CONTRACT_VERSION = 25` with its pin rationale; the frozen `sp_api` assertion 3 → 4; 02 §3 with the paragraph on why it is a method and not published constants, §1/§8 at v25, the §13 v25 entry, v24 marked superseded; `surface-manifest.json` at v25 with the new `api.` entry and its five contract-version constant layouts re-derived; the two frozen-method-set tests moved 13 → 14; `CRITICAL_SURFACE` regenerated (269 probed entries). **And the clause the bump exists for:** P-9 now carries *"the recipient is not a protocol account"* against `api.is_reserved_protocol_destination`, source **`runtime-api`** and subject **`recipient`**. Both are asserted and both are mutation-proven — flipping the source to `constant` kills 2 tests (§11.4 rule 2 wants an exact chain read, not a local namespace computation) and flipping the subject to `acting` kills 1 (the address under test is the one the user just typed). Green: `cargo build`/`test -p bleavit-runtime` (450), `-p futarchy-primitives` (25), `-p futarchy-runtime-api` (5), `tools/release/tests` (89), `test:tx` (83), and every other app suite.

**What comes next:** **Three red gates, all one fact, and why they must stay red for now.** `pnpm run test:mock-runtime` fails 2 of 11 (*every frozen surface entry has a fixture*; *the paired terminal-recovery runtime serves the same metadata surface*); `pnpm run surface:check` fails (*the manifest contract version and `SUPPORTED_RUNTIMES` disagree*); `pnpm run test:descriptors` fails 1 of 15 (*every probed surface is reachable in the recorded transcripts*). One cause — `app/fixtures/chain-feed/{2,3}/` and the 222 `chainhead/` transcripts still describe the v24 runtime, which has no fourteenth method. `check-chain-feed.py` fails for the matching reason. **Regenerating now would be wrong, not merely early.** PR #232 (`feat/adopt-svc-values`) freezes a new `ClientBond` surface and regenerates the same artifacts; `metadata.scale` is binary, so there is no merging those, only regenerating. Whichever of #232 and this branch lands second must regenerate against the *other's* runtime — and a regeneration done from this tree today would omit #232's surface while `check-chain-feed.py` passed locally, which is the silent direction. #232 is being handled in a separate session. **Resume:** once #232 is on `main`, rebase, then run the `app/fixtures/chain-feed/README.md` sequence once (chain specs · release node · both runtime profiles · `extract-metadata.py`, recovery with `--embed-wasm` · copy into `2/` and `3/` · re-record `app/fixtures/chainhead/` · `descriptors:generate` + `descriptors:check` · `surface:check`), then the exhaustive Rust gate. `spec_version` does not move. Env: `CARGO_TARGET_DIR`, `LIBCLANG_PATH` (session-scoped dir with a `libclang.so` symlink to the **resolved** object, currently `/usr/lib/x86_64-linux-gnu/libclang-14.so.14.0.0`), `WASM_BUILD_WORKSPACE_HINT`

## **F11 🔨 — the release train: built, gated per commit, and taken apart by three adversarial reviews**

Took the largest **unblocked** Track F milestone while v25 stays parked on PR #232. F11 owns the *document and the release train*, not the payload: `index.html` with the 12 §5.1 meta-CSP, `vite.config.ts` at the 01 §9 pin **Vite 8.1.4** (`base: './'` — an absolute base makes every asset request leave the release directory and resolve to another transaction's file on a path gateway), a shell package carrying the service-worker entry, the PWA manifest, and `app/tools/release/`: the seven-step pipeline, its committed `sources/`, and a README. **The order of the steps is the design** — the CSP substitution and SRI injection precede the worker's baked hash map (which pins `index.html`), and `release.json` is written last (it pins `sw.js`, which the map substitution rewrote); both orderings are asserted end to end, because getting either backwards ships a worker that refuses its own entry document — fail-closed, *at the user*. The **`connect-src` allowlist is derived** from declared sources and diffed against a committed incumbent (15 §4.8): nothing mechanical can tell an ar.io gateway from a vendor endpoint dressed as one, so D-21's control is that an addition costs a second reviewable file, and the README says exactly that. Also landed here: **the 10 §5.4 no-literal gate** and the **`MockSigner` release-exclusion assertion** over emitted chunks — the one 10 §10.1 rule dependency-cruiser cannot answer, because it is about the bundle rather than the import graph. One ruling worth reading (Decision log): 12 §5.2's cache name **cannot** be baked (§1.2 learns the TXID only after uploading the tree the worker is in) and **must not** be derived from `location` (gateway URL shapes are FE-P7's `[VERIFY]`), so it is fetched from `release.json` at install — safe because the field *names a cache and does not authorize a byte*, and fail-closed rather than defaulting to a constant two releases would share.

**What comes next:** **Three Codex reviews (`gpt-5.6-luna`, `max`, `--sandbox read-only`) found 25 defects in my own work; all fixed.** The worker returned **non-2xx bodies unhashed** — a `404` carrying attacker HTML for `index.html` is a response the browser *renders* — and returned **cached responses without re-verifying them**, though `caches` is same-origin storage any script can write; both are now refusals, mutation-proven (1 test each). `{ type: ACTIVATE_MESSAGE }` with **no `pinned` field activated a pinned release**, because a missing boolean defaulted to *not pinned*. The literal gate was a line scanner and every hole was a **tokenizer** hole (`const open = \"/*\"` swallowed the next lines; `/[//]/` read as a comment; template interpolation was blanked; `1n << 63n` and `0x10000` were invisible — and app-code rule 8 names the first outright); rewritten on the **TypeScript AST** with constant folding, and its witness now asserts **per declared line** with two negative controls. `release.json` emitted `arweaveManifestTxId` while the consumer requires **`releaseTxid`**, and the test that should have caught it compared the producer to a list beside the producer — the required fields are now parsed out of `ReleaseIdentity`. Plus: the percent-decoded wildcard (`https://%2A.example` → `https://*.example`); the **missing `'self'`**, which would have silently disabled INV-FE-8's self-check; SRI parsing only double-quoted attributes and comparing `rel` as one string; the two-pass driver publishing any object and never binding the sibling TXID into the final manifest; the SBOM skipping tarball/git dependencies; the determinism probe skipping binaries; a duplicated key id satisfying 12 §1.4's *distinct*-key floor; a malformed-but-present genesis pin counting as pinned; a dirty worktree recorded as a clean `HEAD`. **Two findings were answered by making the test real:** `worker-runtime.test.js` now loads the **built** `dist/sw.js` into a `vm` and drives it (the pure-policy suite stayed green with the worker's digest check deleted), and the allowlist enforcement is its own exported call (asserting the *diff function* stays green with the `throw` removed). 20 app gates green, 66 release tests. **Unchanged:** the three v25 reds blocked on #232. **Then closed F10's `release.json` binding in the same session** (`parseReleaseDocument`, 9 tests): its own module, because the join is exactly where the halves disagreed, and bound to the document the build actually emits rather than to a hand-written sample. It **refuses** rather than repairs — an unpublished record (`releaseTxid: null`) has no content address to compare bytes against, and a record with unresolved readiness blockers is refused outright, which is the state today's build is genuinely in and is asserted as such. Renaming the producer's field turns a test red. The **assertion gate caught my own `as unknown as`** on the way in and the fix was better than the cast: descriptor keys are now *converted* to spec versions, so a key that is not one is refused instead of sitting in the record as a hash for a runtime nobody can name. F10's own package surface is complete; only the rendered component remains, and it belongs to `packages/ui` behind F7's shell. **F11 also unblocks O1** (release-train tooling), F13, F15, F17 and F22. **Then, in the same session:** F8's **ingest + backfill + candles** (60 tests, 7 mutations caught, and the `check:chain-literals` gate built hours earlier caught two of my own constants — one a real defect, one a unit coincidence derived away rather than waived); F13's **`verify-release` verdict and signer registry** (18 tests, wired into CI as `signers:audit`); and **FE-P11's load-bearing half resolved** against the primary specifications (V-104). **A defect worth carrying forward:** `candles.ts` shipped a **NUL byte** as a composite-key separator — a Write-tool artefact that makes a file invisible to `grep` and made one mutation run report a false *0 kills*. The whole `app/` tree was swept clean; the lesson is that a mutation run reporting **zero** kills deserves the same suspicion as one reporting a failure. **Next:** F8's Dexie store, F15's ops handbook, and F6/F4 when #232 lands

## F6 ✅ — all 20 adversarial-review findings fixed

**The review is why this row closes, not why it slipped.** A read-only Codex round (gpt-5.6-luna, max effort) found 20 defects in `packages/transaction-builder` + `packages/signing`; the suite was green before and after, and **twice asserted the defect** — `rowsFor('P-12').length < 13`, and a test requiring `decoded-payload` on a descriptor with no decode channel. **Verify against the enforcing pallet, then code** was the order, and it paid three ways: #8 was *refuted* for the `execute` path (`Constitution.PhaseFlags` genuinely is what the guard reads — it is the ledger rows that were wrong); four findings needed **no contract bump** because the surfaces existed and the rows cited the wrong ones (`EpochStatusView.ledger_frozen`, `account_positions().len()` where `PositionTotals` is per-*position* supply, `ProposalSummaryView.funder` since v18); and **two were defects in 11 §11.5 itself**, repaired under R-1 — P-10 keyed the intake cap to the caller where `epoch-core:774` counts the **funder** and hardcoded `4` where `intake.max_acct` is META-amendable within [2,8], and P-11 admitted only the proposer where `epoch-core:823` admits the funder too. A client written to that text refuses lawful withdrawals, and no test of the client against its own table can see it. **Structural additions rather than patches:** `anyOf` clause groups (a disjunction read as a conjunction blocks the *lawful* case); `FeeAsset` threaded as a required argument with a runtime guard, since an omitted one silently returned a row minus its headroom clause; per-crank `CRANK_STALENESS` with the two cranks 02 §7 cannot answer carrying a **named refusal**; keyed `MultisigRead` (a stale *absence* is the dangerous half); `as_multi_threshold_1` for 1-of-N, which `as_multi` refuses outright; branded `AdmittedRate`; and `CapabilityGrant`, which replaces a self-declared `Set` the registry trusted — `metadata-hash` is now *unreachable* rather than undeclared. **18 mutations, all caught; four failed to compile first and were redone rather than counted** — and one *survived* (`distinct >= 3` on the crank map), so P-15 now pins correspondence rather than variety. **SQ-590 parked fail-closed:** `Proxy.Proxies` is compulsory under 11 §11.3 and 02 §7 freezes it nowhere — the eighth instance of SQ-580's class and the first `check-client-surface-obligations.py` structurally could not catch, since §11.3 names the obligation only in prose. Gates: **26 app gates + 5 doc checkers green**; exhaustive Rust gate **exit 0 at v26** (108 suites, limit coverage 206/206, reference model 700). test:tx 93 → 120, test:firewall 12 → 14.

**What comes next:** **F4's Asset Hub artifacts** (the pinned AH runtime wasm through `tools/release/` extraction + two `[VERIFY]` tags), then **F7**. SQ-590's bump is a natural pair with F4's, since both touch 02 §7 and both need one feed regeneration rather than two.

## SQ-590 executed — contract v27 freezes `Proxy.Proxies`

**The row is the small half; the evidence type is the change.** 11 §11.3 admits proxies as call wrappers and §11.4 rule 2 makes every precondition an exact chain read, but §11.3 named no storage — so the delegation check was outside the v24 sweep, outside `check-client-surface-obligations.py`, and outside 10 §5.2's classifier, which probes exactly the frozen set. 02 §7.6 gains the row, `INTEGRATION_CONTRACT_VERSION` moves 26 → 27, and `DelegationEvidence`'s `read` branch now carries **`Finalized<ProxyRead>` keyed on `real`** — an 11 §11.4 rule 4 obligation that was not deferred but *unreachable*, since with no frozen surface the one wrapper check deciding whether a signature is lawful was the single place a provider-served answer would have been believed. Keyed because `Proxy.Proxies` is a map: a read taken for another account is not stale evidence but evidence about someone else, and its **emptiness** is the dangerous half — the same shape as `Multisig.Multisigs` in the last round. Two normative notes ride the row: the **stored** `proxy_type`/`delay` govern, and an unrecognised `ProxyType` is **unproven, never permissive** (this runtime declares only `Any` — read out of the metadata as `ProxyTypeenum[Any=0]`, so it is a *value*, not a promise). **Four mutations, all caught**, one of them the compile-time binding: delete the manifest entry and `PROXY_DELEGATION_SURFACE` fails `TS2322` at its declaration instead of decaying into a comment. **SQ-591 filed rather than absorbed** — naming the item in §11.3 fixes this instance; the gate is *sound but not complete*, and quietly amending the doc would have retired the symptom and kept the cause. **Two prose copies of the version number were two bumps stale** (02's Ownership line, and the §7 crate note that had already been "corrected" once in E4) — that sentence no longer names a number at all, since only §9's table is parsed by anything. Feed regenerated end to end: both runtimes rebuilt, metadata re-extracted, 222 chainHead transcripts re-recorded, 16 descriptor files byte-reproduced, `CRITICAL_SURFACE` 277 → 278. Gates: **exhaustive Rust gate exit 0** (limit coverage 206/206, reference model 700, LMSR doc tables green — note the log was piped through `tail`, so per-suite counts were lost; the exit code under `set -e` is what the pass rests on), 26 app gates + 6 doc checkers green, `test:tx` 120 → 122.

**What comes next:** **F4**, already scoped and part-verified in the same session — see the row below.

## F4 groundwork — the Asset Hub pin, both `[VERIFY]` tags discharged, and a PAPI footgun found

**Both of 02 §7.7's `[VERIFY]` tags are now verified against live sources (R-2), and the artifact chain is closed.** *Asset index:* `Assets.Metadata(1337)` on **Paseo** Asset Hub decodes to name `USD Coin`, symbol `USDC`, 6 decimals, not frozen — byte-identical from two independent operators. This is the half V-17 did not cover: V-17 verified 1337 on **Polkadot** AH, and 02 §7.7 pins *the Asset Hub of the relay the release targets*, which at Phase 2 is Paseo. *Exact extrinsic:* `PolkadotXcm` is pallet **31**, `limited_reserve_transfer_assets` is call **8**, params `(dest, beneficiary, assets, fee_asset_item, weight_limit)` — present and not removed, though `transfer_assets` (11) carries an identical signature. *Artifact:* `paseo-network/runtimes` **v2.4.2** publishes `asset-hub-paseo_runtime.compressed.wasm`, whose srtool digest's `sha256` matches the download exactly and whose `core_version` (`asset-hub-paseo`, spec 2004002, tx 16, state 1) matches the live chain — so the pinned artifact and the chain the surfaces were verified on are the same runtime. Genesis `0xd6eec261…1ef2`, agreed by both operators. **The footgun, found by reading the generator and then proven by running it:** `.papi/whitelist.ts` is a single **global** file, and an object-keyed whitelist naming only the foreign chain silently guts every chain it does not name — `globalWhitelist = whitelist["*"] ?? []`, and `[]` is truthy, so the other chain gets `applyWhitelist(metadata, [])`. Measured: `bleavit.ts` **458,662 → 43,258 bytes** and `descriptors.ts` **60,457 → 1,667**, with `papi generate` reporting nothing wrong. The `.papi/README.md` design ("the Asset Hub set does use a whitelist") is therefore not implementable as written; the AH set must be generated from its **own** config root so the whitelist is a plain array over the only chain present, and cross-chain leakage is structural rather than a `"*"` key someone must remember. `descriptors:check` would have caught the drift, but as "the whitelist broke generation" — and the tempting fix, regenerate and commit, ships the gutted set.

**What comes next:** Land the foreign feed (`app/fixtures/foreign-chain-feed/`), generate the AH descriptor set from its own config root, fill `FOREIGN_CHAIN_PINS`, and add the TS-side surface-presence gate — which cannot be Python, because PAPI reads **v16** from the wasm and `scale_metadata.py` stops at v15.

## F4's Asset Hub artifacts landed; **F4 stays 🔨 on one named remainder**

**The plan in the row above survived contact except for the config root, and the exception is the finding.** Generating the AH set "from its own config root" is impossible: PAPI writes `"name": "@polkadot-api/descriptors"` as a **string literal**, so two generated descriptor packages cannot coexist in one pnpm workspace — one workspace, one config, one global `.papi/whitelist.ts`. And a per-chain whitelist guts every chain it does not name, because `globalWhitelist = whitelist["*"] ?? []` and `[]` is truthy: measured, `bleavit.ts` **458,662 → 43,258 bytes** with `papi generate` reporting nothing wrong (V-107). So `.papi/README.md`'s own stated design is withdrawn and replaced by the measurement; all three chains share one un-whitelisted package, at a cost of ~1.3 MB of committed TypeScript. **What landed:** `tools/release/pin-foreign-runtime.py`, `app/fixtures/foreign-chain-feed/asset-hub-paseo/2004002/` (v2.4.2, sha256 srtool-confirmed, `core_version` matching two live operators, genesis cross-checked), the AH descriptor set (19 files byte-reproduce), `FOREIGN_CHAIN_PINS` filled, and `pnpm -C app run foreign:check` wired into `test` and CI. **Eight mutations, all caught** — and one of them is a *warning* path rather than a failure: pointing the row at `teleport_assets` fires the upstream-deprecation notice and still passes, which is both the anti-vacuity control and the evidence that `limited_reserve_transfer_assets` is not itself deprecated. **F4 does not close**, and the reason is worth stating rather than rounding off: the probe's assignability binding was recorded as waiting on F6, F6 closed without wiring `createClient`, and `light-client.ts` still excludes it on purpose — putting metadata compatibility underneath the read layer would make the app fail to construct a client instead of booting into `ReadOnlyIncompatible` and saying why. Descriptors were the other half of that deferral and are now done; the placement of `createClient` is an F3/F7 boot-sequence decision. Gates: 27 app gates + 6 doc checkers + `tools/release` (89) and `tools/ci` suites green.

**What comes next:** **F7** — what renders inside the F11 shell — carrying `createClient`'s placement with it, which closes F4's last item as a side effect.

## **F20 ✅** — the context exporter, the refusal family's one home, and the published `schemas/`

**Three defects, and each was invisible to a green run for a different reason.** **(1) `FE-HANDOFF-012` was defined and unreachable** — zero emitting call sites, which no suite distinguishes from a code that simply never fires; the same shape as the digest gate. It now fires from `buildCapsule`, whose rule is that **scope is checked in both directions**: data supplied outside the consented scope is *refused rather than dropped* (a silent drop leaves the export screen and the exporter disagreeing with nothing to surface it), and a **consented scope with no read supplied is refused too**, because a capsule announcing `positions` and carrying none reads to every tool and every human as *this user holds no positions* — a false claim about the chain from the document whose only job is true ones. Empty is data, absent is not. Books carry a `kind` **checked against the id band, never derived** (deriving exports a label the user never saw); positions are keyed **by domain**, so 11 §11.2a rule 2's merged total has nowhere to live rather than being a rule to remember. **(2) The `FE-HANDOFF-*` family had two homes and their copy for 013 already disagreed.** `receipts` cannot import the inbound parser across the §10.1 firewall, so it declared its own union and its own sentence — and because no call site takes both, the compiler could never see the drift. The family moved to `handoff-envelope` (the argument that put `ChainBinding` there) and gained the **`recovery` 10 §13.3 requires per code**, which nothing carried: a refusal without one is a dead end dressed as an explanation, and unlike copy and detail it cannot be composed at the call site. **(3) `app/schemas/` publishes the three formats, generated from the parser's own declarations and byte-compared.** The way a hand-written schema breaks is not a missing field — JSON Schema's `additionalProperties` defaults to **true**, so omitting it on `action` publishes the *opposite* of 10 §13.2's asymmetry to the one audience writing producers. `schemas:check` is only determinism, so the **differential suite** is what makes it true, and it found a real defect on its first run: the schema had `limits` all-optional while the parser **requires** a direction-matching monetary limit (prepare ⇒ `maxCost`, close ⇒ `minProceeds`, each refusing the other). *"There is no safe default for money"* is a property of the **file**, so the schema owed it; a producer following the published version would have shipped `"limits": {}` and been refused at the user, and a proceeds floor on a purchase is worse — it sits on the confirm screen looking like protection while binding nothing. The validator **throws on an unimplemented keyword**, with a negative control per keyword. **Two of my own tests passed for the wrong reason and only mutation testing said so:** one asserted bigint id parsing through `bookId`, which is echoed verbatim, so nothing depended on the value the bit test saw (the case with teeth is `2^63 - 1`, the largest primary id, which rounds *up to* the boundary and is classified `service`); the other asserted a JSON number is refused, which `type: 'string'` already does, leaving the canonical-decimal **pattern** — leading zeros, signs, exponents — completely uncovered. 15 mutations run, 12 caught first time, 3 rewrites. **One unrelated find, worth carrying because of what it disables:** `app/tests/receipts/receipts.test.js` was committed containing a **literal NUL byte** (in a NUL-terminator assertion), so git classified it as *binary* — meaning every diff of that file since it landed has shown `Bin 10215 -> …` and no lines at all, and `grep` skipped it silently. Both are invisible failures of *review*, not of tests. Replaced with the `\0` escape; a sweep found no other source file in `app/` carrying one. Gates: full `pnpm -C app test` exit 0 (**616 tests across 14 suites** + 8 gates), `tools/ci/tests` 202, PLAN tables green.

**What comes next:** F21 — `skills/`, the portable Agent Skill, which now has a published schema to bind to; then F7

## **F21 ✅** — `app/skills/`, the producer side of the handoff

**Under handoff-first this is a product surface, and it is the one part of the system with no mechanical control over its quality** — the client can refuse a malformed document and cannot refuse a bad argument (10 §13.5's accepted residual, doc 14 TH-49). So `reference/safety.md` gives the *reason* behind every rule rather than listing them: a producer that understands why call bytes are banned in **both** directions does not need the rule, while one following a checklist finds the gap in the checklist. **Two artifacts are generated, for one reason.** The identical safety rules must reach a directory-shaped Agent Skill, a ChatGPT instruction **text box with no filesystem**, and a plain prompt. Hand-written that is three copies which agree on day one and diverge at the first amendment — *invisibly*, because nobody diffs a ChatGPT instruction box against a repository, and a stale copy is a producer operating under the previous version of the safety argument. The generator also **fails on a surviving pointer** to a file the inlined reader cannot open, since an instruction that cannot be followed teaches the reader that instructions here are optional. The **16 examples** carry their expected verdict in the *filename* and every one is executed against the **real parser**: a published example that no longer does what it says is worse than none, because somebody debugging against a stale corpus concludes the client is wrong and then loosens the document until it passes — precisely what rule 7 forbids. The label lives in the filename rather than a manifest because a filename cannot drift from itself. **Coverage is over the nine codes an inbound document can cause, with the exclusions named** — `-011` needs live chain state, `-012`/`-013` are export-side — so *"we cover the family"* cannot quietly become *"we cover the easy ones"*. **One scope call made rather than deferred:** `skills/` and `schemas/` are copied into the release tree, **before anything is hashed**, so the service worker pins them. A skill nobody can obtain from the client they are running is not delivered, and the documented route would otherwise depend on fetching from a code host — the shape of dependency D-21 exists to remove, though 10 §13.5's convenience-only status means no invariant was being falsified. The **ordering** is what a bare copy step gets wrong and both directions are mutation-proven: copied in after the map is baked, they are same-origin paths the worker refuses, fail-closed at the user for no reason. 8 mutations, all caught. Gates: full `pnpm -C app test` exit 0 (**617 tests across 13 suites** + 10 gates), `tools/ci/tests` 202, PLAN tables green.

**What comes next:** F7 — the default surface (app shell S1, proposal list/detail S2, confirm & sign, and S21/S22), whose dependencies F6 and F20 are now both ✅

## **F7 🔨** — the default surface, React 19, and a firewall hole both gates were blind to

**The entry note I wrote for this milestone was wrong, and probing it instead of trusting it is the session's finding.** V-109 said the handoff screens *"cannot compile"* with `jsxImportSource: react`. They compile: `--traceResolution` shows `react/jsx-runtime` resolving to the workspace-root `@types/react`, so a hoisted types package satisfies the 10 §10.2 compile gate for **every** project regardless of the isolated layout — and dependency-cruiser cruises *source*, so the compiler-injected import is absent from its graph entirely. Both the primary gate and the redundant one were blind, and the first failure would have been `vite build`. New gate `check:handoff-emitted` (+ 6-case witness with two negative controls) reads the **emitted** trees; V-109 corrected, V-110 records the general form. **`packages/ui` makes INV-FE-9 structural off a fact about React**: `Verified<T>` is an object and `ReactNode` refuses one, so a chain value reaches the screen only through a component that derives the badge from the status — the residual (`{x.value}` by hand) is written down rather than papered over. 11 §11.2 constraint 3 is enforced twice (type *and* a render-time throw for the indirect form), with the fact list bound to the section citations in doc 11's own paragraph. S2's union has **no field** for in-Trade decision statistics — verified, since TS narrows by discriminant before excess-property checking — and the confirm summary is a branded `DecodedCall` that also refuses a decode of other bytes. **Two defects in already-green work:** F11's `main.ts` ran `document.getElementById` at import while `index.ts` re-exported it, so the package was unimportable in Node (split into `boot.tsx` + a three-line entry); and S22 badged the **encoded** limit `external-proposal` even when the client's own chain read produced it, which surfaced **SQ-592** — a policy-bound value has no status in the six-member lattice, so it fails closed. **20 mutations, 0 survived**; 3 were re-run because the first attempt was caught by `noUnusedLocals` rather than by a suite, which is not the same thing. `@vitejs/plugin-react` dropped after a byte-identical rebuild (V-111). 665 app tests + 28 gates green.

**What comes next:** F7's read layer: `Epoch.EpochOf`, `Constitution.PhaseFlags`, `Epoch.Proposals`/`proposal_summaries()` through `chain-client` into the screen models, plus a hash-route outlet. Then the next Track F item.

## Workflow: the one-milestone rule removed, and a Stop hook that keeps a track goal

**User instruction, twice.** First: remove the rule saying one milestone per session. It was already gone from AGENTS.md R-5 and the `/implement` skill (July), but **`.codex/prompts/implement-next.md` still said "IMPLEMENT. One milestone only."** — the Codex playbook, which explains that agent's behaviour and not mine. Removed there, and the leftover "there is no such limit" negations dropped from R-5 and the skill, since a rule that does not exist needs no denial. CLAUDE.md and `.codex/README.md` re-worded to match. Second: implement a Stop hook that blocks ending while the goal is unmet. `.claude/hooks/guard-track-goal.sh` reads a one-line `.claude/session-goal` (`track: F`), counts that track's milestone rows in PLAN.md, and blocks with the next one named. **Opt-in** (no file, no behaviour), **escapable** only by a `> **PARKED:**` line in Current focus — a sentence the next session reads, not a flag nobody sees — and it honours `stop_hook_active` so a wrong goal file cannot make a session unendable. Eight branches tested, including the two where a guard *silently stops firing*: a complete track and a PARKED escape must go quiet, and a PARKED line written in the wrong section must **not**.

**What comes next:** The hook is live for track F.

## F7 🔨 — S1's read layer, the outlet, and a declared record of what is not built

**The read layer's two properties are enforced rather than assumed.** *One screen, one block*: `assertOnePin` re-checks the finished model against the reader's pin, because a header showing the epoch at one block and the phase at another is not a stale display but a **view that never existed** — and it is exported so a test can reach it, since no input to `readShellState` can produce a mixed model and a test driving the public API could only assert two hashes it wrote itself were different. That was the first version of the test, and it was replaced. *Undecodable is an outcome, not an exception*: the decoder returns a result, the raw bytes reach the screen through `ui`'s `Undecodable` — deliberately **not** a seventh `VerificationStatus`, since the bytes are authentic and it is the interpretation that failed, and one label answering two questions is how "verified" stops meaning one thing — and an unreadable `Constitution.PhaseFlags` yields `undefined` rather than a substituted 4, keeping the sudo banner up. **The outlet declares what is missing instead of hiding it.** `PENDING_SCREENS` names the owning milestone per screen and is checked against the inventory **in both directions**, so a screen dropped from the client cannot pass as one that is merely pending. `screenForHash` was briefly a second implementation of `screenFor` — collapsed, with a test over every path plus the unknown-hash case, because the disagreement would have highlighted one screen while rendering another and each half would have looked right alone. 10 mutations across the two slices, 0 survived. 679 app tests + 28 gates green; `release:build` clean at 199,767 bytes.

**What comes next:** S2's reads, then the `chain-client` typed-decode binding that supplies the decoders.

## F7 🔨 — S2's reads and the confirm gate wiring; **one mutation survived and the test was the defect**

**The statistics rule is one a plain implementation gets wrong.** *"Render them if the API returned some"* is one anomaly away from 11 §11.2's prohibition — and since both reads come from **one pinned block** it would be a contradiction rather than a race, with two readings and only one safe to act on. `viewFor` returns the pre-decision arm and reports an anomaly: wrong that way costs a wait, wrong the other way puts a projected outcome on an open market. The state check is an **allowlist**, so a lifecycle state from a newer runtime renders nothing (INV-FE-12) — a denylist admits it. A prefix key with **no value** is reported rather than filtered, because dropping it shortens the list while every remaining entry decodes perfectly. **The confirm controller: `undefined` before the chain has been re-read** (nothing beats remembered rows), failures-only on `Blocked`, and `mayOfferSigning` on the *state* rather than on "every row passes" — `FE-TX-007` blocks with an **empty** `failed`, so that predicate is vacuously true exactly where the bytes are stale. **M34 survived**: my `Blocked` test had no `signingWindow`, so a controller preferring the window fell back and passed. The dangerous session is `Blocked` *after* a gate once passed; the test now builds it, and the fix was to the test. 10 mutations across both slices, 0 survived after that repair. 691 app tests + 28 gates green.

**What comes next:** The `chain-client` typed-decode binding — the one remaining F7 item, and the only piece that needs PAPI.

## **F4 ✅** — the probe's shapes bound to PAPI's real ones, and the binding was vacuous first

**The blocker was a false premise, held for two milestones.** The item was recorded as waiting on a `createClient` that nothing wires — but **assignability is a compile-time relation**: `TypedApi<typeof bleavit>` is a type, `getStaticApis()`'s return is a type, and `compat` is a property of it. No client, no node, nothing constructed. `light-client.ts`'s ruling that `createClient` must stay *above* the read layer (so a metadata incompatibility becomes 10 §5.2's `ReadOnlyIncompatible` rather than a constructor throw) stands untouched — it was simply never this item's dependency. Home: `app/tests/descriptors/types/`, because `descriptors` names no PAPI type by design and `chain-client` is the only package the firewall lets import the SDK, while a **test** may import both. **It shipped vacuous first and the witness caught it.** `G[keyof G][keyof G[keyof G]]` takes its inner `keyof` across the *union* of pallet objects, yielding only their common keys — `never`, since pallets share no member names — and **`never` is assignable to everything**, so the positive control compiled having compared nothing. Then the witness failed to notice the repair because it held **its own copy** of the broken definition, which is V-86's shape one layer up; it now imports both types from the file it witnesses. Also pinned: `isCompatible` is declared with **method syntax**, and its bivariant parameters are what make PAPI's `(from?: CompatibilityLevel) => boolean` property assignable — rewriting it as a property "for consistency" would break the binding. 693 app tests green.

**What comes next:** Track F: **9 ✅ · 7 🔨 · 8 ⬜**. Next: F7's last item (the `chain-client` typed-decode binding), or the next open row.
