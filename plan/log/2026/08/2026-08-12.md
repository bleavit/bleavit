# Session log — 2026-08-12

## Plan split completed

Finished Tasks 8, 10 and 11 of the approved plan split. Converted all 223 verification findings, disambiguated four legacy id collisions as V-386–V-389, archived the Current-focus history without inferring dates, shrank `PLAN.md` to its focus and generated index, moved batch declarations into `plan/batches/`, and updated every living workflow document and hook that named the former tables.

**What comes next:** review the final diff, run the complete tooling/docs gate set and CI-parity check, then hand off the branch without committing or pushing unless the user asks.

## Verification closure

The complete tooling and documentation gate set is green. The environment suite's first sandboxed run could not bind localhost sockets and reported `PermissionError`; the same 86 tests passed outside the filesystem sandbox, confirming an execution-environment restriction rather than a repository defect. CI parity passed for all 17 environment-sensitive gates in a CI-shaped shallow clone.

The exhaustive `tools/ci/rust-workspace-gates.sh` run passed from a clean scratch target with the documented `CARGO_TARGET_DIR`, `LIBCLANG_PATH` and `WASM_BUILD_WORKSPACE_HINT` setup. It covered formatting, Clippy, the workspace and keeper tests, release/runtime-benchmark/try-runtime profiles, runtime-profile recovery matrices, no-std builds, reference-model checks, generated weights/storage bounds, and all 207 limit-coverage keys.

## Publication

After explicit user authorization under R-9, committed the completed split as `dd702c6b`, pushed `plan/split-tree`, and opened draft [PR #301](https://github.com/bleavit/bleavit/pull/301). The connected GitHub app created the PR because the local `gh` token for `Chralt98` remains invalid; the repository's SSH remote authenticated the push successfully.

**What comes next:** let PR #301's CI run against the published branch, then mark it ready when green. The exhaustive local gate already passed for the code state; this status-only closure does not require a redundant local rerun under R-12.

## PR #301 conflict reconciliation

Merged `origin/main` at `d55f3673` into `plan/split-tree` after GitHub reported PR #301 as conflicted. The textual conflicts were `.gitignore`, `AGENTS.md` and the legacy `PLAN.md`: the first two were reconciled additively, while the latest monolithic plan was run through the split converter so main's newer records were not discarded. The resulting tree carries 117 milestones, 592 questions and 223 verifications, adds the 2026-08-10/11 dated records, and updates B7 from 89 to 92 open questions. Main's new gate rationale lives in `.claude/rules/quality-gates.md`, preserving the split's short always-loaded manual.

The newer Current-focus text exposed one converter defect: rebasing the directory link `docs/proposals/` dropped its terminal slash and made the losslessness proof fail. `rewrite_repo_links` now preserves that marker, with a regression test. A clean migration of latest main in a temporary tree passed every per-kind losslessness and round-trip proof. The combined checkout then passed all 118 plan-tool tests, generated-index freshness, Markdown tables, spec-question batches, all 1,154 local links, all 212 limit-coverage keys and 36 limit tests, every plan-consuming obligation/coverage gate, the remaining documentation gates, and `cargo fmt --all -- --check`.

**What comes next:** PR #301 is ready for review; GitHub CI on the reconciled head is the remaining merge gate. The earlier exhaustive Rust gate covers the split implementation, and main's merged commits carry their own CI evidence, so R-12 does not call for another duplicate exhaustive local run.

## README rewrite based on merged PR #301

Rewrote `README.md` into a conventional GitHub landing page with a deployment
warning, concise protocol overview, live track summary, four-path quick start,
documentation routing, compact repository map, development guide, contributing
guidance and security route. After PR #301 merged, merged `origin/main` at
`434407d1` into `worktree-update-readme` so PR #302 is based on the shipped plan
tree rather than on PR #301's former feature branch.

The integration kept main's short generated `PLAN.md`, preserved the README
rewrite, updated its status links and workflow language to the new `PLAN.md` +
`plan/` contract, and migrated the rewrite's former monolithic-PLAN record into
today's change and session files.

**What comes next:** run the plan renderer, plan/docs gates and README guard over
the merged tree, commit the conflict resolution, and push the updated PR #302.

The full plan/docs gate catalogue passed on the merged tree: generated plan
indexes, Markdown tables, all 1,154 local links, question batches, dispatch
mirror, execution errors, unreadable obligations, release blockers, client
surface, runbooks, signers, integration ABI and the README pin guard. The branch
delta against `origin/main` passes `git diff --check`. A first-parent staged
check also printed nine whitespace findings already present in merge base
`434407d1`; `git diff d55f3673..origin/main --check` reproduces the same nine,
so none belongs to PR #302 and this integration does not rewrite PR #301's
merged artifact opportunistically.

PR #301's obsolete “ready for review” Current-focus handoff was removed after
the user confirmed it had merged. Track F remains the current parked focus.

## Whole-repository security review

Completed a read-only security review of commit `08f77ce1` across the protocol/runtime, XCM,
frontend/signing, hosted service, release/deploy/monitoring, keeper/node, dependencies and assurance
tooling. The review confirms 0 Critical, 5 High, 8 Medium and 2 Low findings. Ten novel findings are
recorded as SQ-1057…SQ-1066; existing SQ-504, SQ-1032, SQ-1035, B21 and the accepted mutable-Action
hardening finding were revalidated rather than duplicated. The full evidence and limitations are in
`docs/reviews/codex-repository-security-review-2026-08-12.md` and today's audit record.

The online supply-chain gate and focused Python, reference-model, simulation, million-case rewards
property, runtime-benchmark and repository control gates passed. No live environment/device lab,
long fuzz/model-checking campaign or redundant exhaustive Rust gate was run. At the user's explicit
request, the review is being published first as a draft PR; fixes follow only after that review state
is committed and the PR exists.

## Security remediation for draft PR #304

Published the audit-only checkpoint first as requested, then implemented all 15 findings. The pass
changes the hosted-service retention model and contract to v31; makes signing authorization own its
finalized reads and exact target; isolates XCM health to runtime-authored protocol sends and denies all
nine inner-program carriers; hardens release monitoring, reproducibility and workflow provenance;
adds bounded client-report pruning; enables and artifact-binds RFC 78 metadata hashes; and makes
recovery-image release failures sticky and repairable through the halt bitmap.

Independent read-only re-review stayed active throughout. It found and drove closure of sealed-pause,
benchmark-shape, client-prune-weight, surface-encoding, storage-monitor partition, clean-CI dependency
and documentation/runbook drift. Its final pass also found that the release monitor compared explicit
bundle paths but not the browser-visible immutable/named root routes; all raw path manifests and both
root routes are now independently compared against the verified manifest index, with hostile-route
fixtures. The production market/question-service weights were regenerated at
50×20 only after the reviewer cleared the complete reachable benchmark fixture; unrelated timing-only
noise was reverted per the established per-function rule. The source tree is being committed before
OCI/feed generation so build provenance names the actual clean source SHA.

The source-gate checkpoint is green: `cargo fmt`; the full 497-test `try-runtime` leg; the required
22-test runtime-benchmarks leg; generated-weight, storage-bound, regression and 212-key limit-coverage
controls; 126 release-tool tests; workflow pin/wiring checks; 139 monitoring tests plus alert/runbook
coverage; frontend build/dependency/signing/provenance checks; eight transaction suites; four exact
negative TypeScript fixtures; and the explainer's 691-test verification/build. The sandbox prevents a
Node test process from spawning the firewall's nested `tsc`, so those four new fixtures were compiled
directly and CI retains the ordinary anti-vacuity corpus run. While doing so, the suite wrapper's false
claim that Node 22's test-runner glob expands `{js,ts}` was corrected: it now resolves patterns with
`fs.globSync` before invoking the runner, so a local or CI suite cannot silently execute zero files.

The first clean-SHA OCI attempt pulled and verified the exact image digest, then stopped before
compilation because rustup's default post-install self-update probe tried to create
`/usr/local/cargo/bin/updtest*` inside the deliberately read-only image. The wrapper now passes
rustup's supported `--no-self-update` flag, with a source-level regression assertion and operator
documentation. A new clean source commit is required before retrying; reusing the prior SHA after a
tooling change would make build-info v3's provenance claim false.

With self-update disabled, the next clean-SHA attempt built the primary runtime successfully in the
confined image, then correctly failed artifact emission because this checkout is a linked worktree:
its `.git` file points to parent metadata intentionally absent from the container. The wrapper now
requires a clean checkout, resolves and validates the 40-hex source commit plus default epoch on the
host, passes the commit as an explicit OCI evidence input, and rechecks both HEAD and cleanliness
before accepting outputs. The worker never invokes Git and the parent repository remains unmounted.
Clean/dirty fake-Docker fixtures, build-profile validation and the full release suite cover the
boundary. This likewise closes the earlier dirty-worktree provenance concern mechanically.

**What comes next:** run the scoped source gates, commit the clean remediation source/weights, generate
the primary/recovery runtime and v31 chain-feed/descriptors/skills from that exact SHA in the pinned OCI
environment, run the artifact/app gates, push both commits to draft PR #304 and hand GitHub CI the one
coherent remediation state.

## SR-13 mandatory-weight follow-up

The final clean-source review found that SR-13's newly sticky recovery-image cleanup was still
undercharged: `on_initialize` and the migration-completion callback invoked the production preimage
unpin without including that trait-backed work, and completion omitted its anchor deletion. Added an
explicit bounded `RecoveryImages` unpin-weight seam, using generated `pallet-preimage`
`ensure_updated(1) + unrequest_preimage()` in production, and made the helper account its own
`RecoveryImage` read/delete. Completion now also charges the anchor deletion before registering the
whole callback as Mandatory. Exact mock/runtime assertions cover the failed release and repaired retry;
the runtime case still proves an independent migration-stall source survives repair.

Focused verification is green: `cargo fmt --all -- --check`; both pallet release-failure regressions
(2 passed, 75 filtered); and the production-runtime SR-13 regression (1 passed, 490 filtered). The
runtime test's first in-tree-target attempt stopped before assertions on the workstation's documented
eCryptfs filename limit; the identical test passed with the required short `/tmp` `CARGO_TARGET_DIR`
and `WASM_BUILD_WORKSPACE_HINT`.

## Final source-boundary closure

The post-SR-13 independent pass found two release blockers in claims that had been marked closed.
First, `refreshAndGate` still accepted caller executable callbacks; a caller could launder arbitrary
runtime, compatibility and precondition values through `derive` from one irrelevant finalized read.
The public boundary now takes only `(prep, connection)`, opens its own fresh pin, observes the
finalized runtime and returns named `FE-TX-004` until a complete closed evaluator owns every key,
decoder and predicate. The explicit-value evaluator and transition enumerator moved behind the
transaction-builder testing subpath, and production dependency rules reject both the subpath and a
relative deep import of the underlying module.

Second, the unattended monitor consumed a producerless provisional snake-case release schema and
conflated the app document's asset manifest M with `ReleaseChannel`'s final manifest M′. It now parses
the real `bleavit.app-release.v1` camelCase contract, requires explicit M′ when producing the
credential index, proves M and M′ are distinct, verifies `paths(M)` against `perFileHashes` and
`paths(M′) = paths(M) ∪ {release.json}`, and binds index/resolver/channel only to M′. A real
app-produced fixture crosses the producer→index→monitor boundary.

Focused evidence is green: app build and test typecheck; eight transaction suites; cast and
provenance gates plus witnesses; dependency-cruiser plus witnesses; four exact negative TypeScript
fixtures; 145 monitoring tests; alert coverage and all runbooks. Direct production-path probes report
both new dependency rules by name. This filesystem sandbox prevents Node tests from spawning nested
checker processes; the pre-existing local-index anti-vacuity suite fails with the identical empty
subprocess output, while direct invocations pass/fail with the expected diagnostics. Final read-only
adversarial review found no exploitable blocker in either patch. It did catch one normative omission
before commit: §11.4's future-evaluator pseudocode produced an unnamed proof. The corrected contract
binds the exact preparation and frozen `{scaleHex, account}` target, matching the already-fixed signer
boundary and preventing the future evaluator from reintroducing SR-03 replay/substitution.

## Exact OCI runtime handoff into chain specs

The first genuine artifact build refused at primary metadata extraction because the chain spec's
genesis `:code` did not match the digest-pinned OCI `runtime.wasm`. The refusal was correct; tracing
the producer found two substitutions before it. `generate-chain-specs.sh` rebuilt the selected
profile on the host after the OCI pair existed, and `generate-relay-specs.sh` later invoked that
generator again and selected its host output for all ordinary drill specs. Passing the OCI path to
only the first call would therefore still have left canonical specs overwritten and B7 evidence
bound to the wrong bytes.

Both generators now accept `--runtime-wasm PATH`. The deploy generator rejects a missing/empty file
and skips only the runtime build while retaining the pinned chain-spec-builder check; the environment
generator canonicalizes and passes the same file through, then uses it for ordinary and migration
drill presets. The release workflow supplies `release-work/runtime/runtime.wasm` at both call sites.
Primary extraction intentionally keeps booting the resulting canonical dev spec, so its existing
`:code`↔file check proves the handoff; recovery retains its temporary exact-Wasm embed. The fast-timing
runtime remains a separate test-only artifact. Focused tests and documentation are updated before a
new clean-source commit and OCI retry; no previous artifact is relabeled or reused.

Focused verification is green: Bash syntax for both generators; 175 deploy tests; 129 release tests;
86 environment tests; 30 workflow-wiring/action-pin/release-workflow tests; environment-definition
validation; workflow YAML parsing and immutable-Action checking; plan rendering, Markdown tables,
spec-question batches, 1,167-file local-link coverage and verbatim mirrors; Python compilation; and
`git diff --check`. The first sandboxed environment-suite run failed because the sandbox forbids its
localhost port probes (`PermissionError: [Errno 1] Operation not permitted`); the identical suite
passed outside that network restriction. Per instruction, no Cargo or OCI build was launched in this
source-fix pass.

## Client-parachain release prerequisite and exact runtime classes

The exact-Wasm handoff's adversarial review followed release-tier suite declarations through their
topologies and found that drills 10–13 also boot `bleavit-client-local.json`. A clean checkout carries
only the generated-directory `.gitignore`, and neither the release workflow nor the relay generator
called `generate-client-chain-spec.sh`, so every tag run would stop before those suites spawned. The
existing broad `bleavit-*` evidence classification introduced the opposite failure once that file
existed: it would hash the separately built client runtime as though it were the shipped OCI primary.

Release CI now generates the client harness after relay/drill specs and before evidence. The evidence
runner derives prerequisites from every selected topology, binds only the exact ordinary/migration
primary names, recognizes client and fast-timing files as separate runtimes, and rejects an unknown
Bleavit name. The relay second-handoff regression now executes the whole generator with fake pinned
Git/Cargo/builder programs, proving ordinary, raw and migration specs preserve the supplied OCI bytes
while the two fast specs preserve their distinct test runtime. No real Cargo, network or OCI work ran
inside that test.

Focused implementation evidence is green: 90 environment tests (outside the filesystem sandbox's
localhost-socket restriction), 175 deploy tests and 129 release tests. Workflow action pins, Bash
syntax, Python compilation, environment validation, all documentation structure checks, 1,167-file
local-link coverage, verbatim mirrors and `git diff --check` are green. Artifacts still require a new
clean commit and genuine same-SHA OCI regeneration.
