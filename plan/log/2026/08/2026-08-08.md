# Session log — 2026-08-08

## **F17 (the guardian class)** · SQ-1018…SQ-1022

**A re-review found that the trigger fix had closed one input shape and left three, plus the approval side.** That is the session's own lesson turned on itself: I fixed the reported instance and called the class closed. The four that remained — an approve control evaluating neither trigger nor allowance though the runtime enforces both at the **fifth** approval, a trigger unbound to the one the call names, a requirement keyed on `meter.power` rather than the call's, and a `TriggerState` with no cohort field so `OracleDeadlock`'s mandated `contains_key(target)` was unexpressible — share one root: three descriptions of one action travelling separately, each validated by a function that saw only one. `GuardianProposal` makes the call the single source, `guardianCall` derives the encoded power, `proposeFormBlocks` is **deleted**. **The enumeration was worth more than the fixes**: writing out 7 propose and 16 approve shapes found the class still open beyond the trigger, because `check_and_consume` runs inside `dispatch` and so the allowance, hold window and rerun ledger also fall on the fifth guardian. One shape stays open and is named — `TooManyPending`, count readable and bound not (SQ-1022); guessing 64 from Rust is what R-2 forbids and a `blocking` obligation would close the control outright. Repros 0/5 before and 5/5 after; **17/17 mutants killed** with a first negative control that *survived* and is reported, since that is how a mutation run lies. Exposure was zero throughout — `implementedScreens()` is `{}`.

**What comes next:** R-6 re-review of the closed class before any F17 ✅; then Track F is externally blocked throughout. **SQ-940 and SQ-994's membership need the user**

## **F27 ✅** · F13 park corrected · V-350…V-354 · SQ-1023…SQ-1025 filed

**The user's own constraint — Zombienet and Chopsticks, no public testnet — falsified the parks rather than confirming them.** Twenty of the twenty-two `built-unwired` screens wait on *a live transport*, and `bleavit-xcm.toml` already spawns the exact topology that sentence describes: a four-validator relay, Bleavit at 4242, **Asset Hub at 1000**, Coretime at 1005. The chain was never the blocker. **Five defects stood between the repository and a client that had ever booted, and every one is the session's own class — a documented, tested, gated path nobody had executed.** (1) **V-350: `run-evidence.py` could not run a single Zombienet suite at any tier**, proven by executing the function rather than reading it — `--tier release`, `--tier g1` and the full set each raised on port 19944. The ports and the checker landed in the same commit (`5688da12`, 2026-07-20), so B7's evidence producer has never completed a Zombienet run, and `release.yml` swallows it into a `::warning::` that reads as *missing evidence* rather than as a checker refusing its own inputs. The cause is a **contradiction between two checkers**: `validate-environments.py` pins `KEEPER_RPC_PORT = 19944` and fails a keeper topology pinning anything else, while the runner refused any port appearing twice. Networks spawn sequentially, so duplicate scope is now per file; both pre-existing tests passed a single suite, so the cross-file scope was never covered. (2) **V-351: three topologies pinned no `rpc_port` at all** — `bleavit-xcm` plus **both** client topologies — so drills 07, 10, 11, 12 and 13 could not resolve a try-state endpoint either. (3) **V-352: every generated spec is plain and smoldot takes raw only**; the generator now emits both forms from one generation, since the pinned Zombienet cannot schedule a raw spec and `chain-spec.ts` refuses a plain one. (4) **V-353: `light-client.ts` is structurally unreachable from Node** — the browser worker pair fails *silently* there, not loudly, so the split is a seam rather than a check; `startLightClientWith` is the shared body and `node-light-client.ts` the Node host, proven by starting smoldot 3.3.2 under `worker_threads`. (5) **V-354: `dev-chain-pin.ts`'s usage block named files nothing produces**, and the two that directory does produce are plain, so its own raw check refused them — the documented command could not work. **Drill `14-client-boot`** joins them: `pin`, `boot`, `wrong-chain`, registered in both bound registries, with genesis hashes read from the **nodes** and never from smoldot's `chainSpec_v1_genesisHash`, which is the value the identity check compares against. Nine chain-free tests gate the harness per commit, including the anti-vacuity one: with the boot stubbed to **succeed**, `wrong-chain` must still fail. **The drill ran, and its boot leg passed on the first real execution (V-355).** The light client warp-synced the relay, linked and synced the parachain (*"Got parachain head from relay chain"*), downloaded and compiled the parachain runtime, delivered a finalized head, and 10 §5.2's classifier returned `classified` at **`spec_version` 2** — the value `SUPPORTED_RUNTIMES` commits descriptors for. That is the sentence `light-client.ts` has carried since F3 being retired: *"everything below the structural seam … is not verified by any test in this repository"*. Two corrections the run forced, both of which a desk reading would have got wrong: **the specs must be the ones zombienet spawned**, not the ones the generator wrote — zombienet rewrites the relay spec at spawn, so the pristine file has a different genesis and the client correctly refused it (which is the identity check working, on the first try) — and **Asset Hub does produce blocks** (#130, finalizing), so its `unavailable` verdict is about a 79 MB / ~189k-entry genesis smoldot cannot initialise inside the bound, not about drill 07's inherent trap as an earlier draft of the README row claimed. **SQ-1026 — found by the wrong-chain leg, fixed, and witnessed on a live chain (V-356).** `WrongChainError` was raised inside `getSmProvider`'s chain factory, and PAPI **retries that factory indefinitely** — 265 raises in one run, none propagated — so 10 §3.1's terminal `FE-BOOT-003` never reached a caller and `chain-session.ts`'s `instanceof WrongChainError` branch was **unreachable code**. Every layer believed the control was wired: the class exists, the branch exists, the comment says *"terminal and is re-thrown"*, and the suites pass by injecting a `start` that throws. Nothing had ever run it. The repair is a **latch**, and it needs both halves — the factory fails fast on every later call, which stops a client re-dialling a chain it has already proved it must refuse (two chains re-added per attempt), and it rejects a promise the opener races, which is what carries the error across a boundary PAPI will not let a throw cross. **Re-run against the live topology with one nibble of the parachain pin flipped: exit 0, `refused: true`, `code: FE-BOOT-003`, the two hashes differing by exactly that nibble, and raises down from 265 to 5.** The boot leg re-ran unchanged. **The R-6 review returned 2 blockers, 8 majors and 14 minors, and the first blocker is the milestone's own thesis turned on it (V-357).** The committed `client-boot.js` read `zombienet/specs/out/*-raw.json` while the passing runs had been driven by hand from the **spawned** specs — and this row already recorded that those are different chains. So the evidence was real and the code that produced it was not the code that shipped. The second blocker is the same shape: `startTopology` asserts the **relay** identity first, so a stale relay pin raises `WrongChainError` too, and the wrong-chain leg would have reported `FE-BOOT-003` witnessed without ever reaching the byte it flipped. Both fixed and re-run on the live topology: boot returns `compatMode: "full"` (the §5.2 **lattice**, not the `classified | unestablished` wrapper the leg was asserting — a `read-only-incompatible` runtime is `classified`, so that assertion could not fail), and wrong-chain returns `role: "para"` with `expected` equal to the corrupted pin and `observed` equal to the real chain. Also fixed: Asset Hub bootnodes were collected, passed, parsed and **dropped**; the `node-light-client` subpath had **no** dependency-cruiser bar, and the claim that workspace membership confined it was false — membership confines a root dependency, not a subpath export of a package ten members declare, so the rule now exists and was proven by an in-scope probe (its `from` is `^(src|packages)/`, which the witness suite cannot reach — recorded in the rule). Two tests were inert and are now real. **Asset Hub still reports `unavailable` with its bootnodes supplied**, so the dropped-peers hypothesis is retired and the genesis-size reading survives without being proven. **All six of the review's remaining majors are now closed (V-358).** `FE-BOOT-003` has a terminal state: `wrong-chain.ts` replaces the mounted tree rather than adding a banner, because §3.1 gives that state no outgoing edge and every other surface would be describing a chain the client cannot identify — and `main.ts`'s `.catch` re-throws anything that is not a wrong chain, so it stays one declared state's screen rather than where boot failures go quiet. The PAPI provider is **disconnected** on the terminal path, which stopping the topologies did not do: `ChainHeadConnection.open` never settled there, so its own `disconnect()` never ran and a state the spec calls terminal was leaving an unbounded retry loop behind it. `connectAssetHub` is bounded **in the client**, where 11 E17's *"blocked with diagnostics"* and 02 §7.7 put the obligation — a UI timeout on `COMPAT_PULL_DEADLINE_MS`'s stated grounds, longer because it covers a cold chain add rather than a metadata read, injectable, and proven by a negative control: with the bound removed the suite **hangs** at 615 of 617 rather than failing. `release.yml` gained the client install and build a release-tier drill over `app/` output needs, which it had never had. The drill's registration assertions moved to `relay-alice` — `paraIsRegistered` reads `paras.*`, which exists only on a relay runtime — and the timeout budgets now nest (3,300 s of steps under a 3,600 s suite, leaving 300 s for the spawn). **A second review, of those six fixes, returned a blocker and it was mine (V-359).** The provider disconnect was a **no-op**, and the reviewer proved it by executing the pinned packages rather than reading them: `getSmProvider` returns `getSyncProvider(...)`, and every call to it builds a fresh closure with its own proxy, token and retry chain — so `provider(() => {})` disconnected a second, unrelated connection and left the real loop running. 131 factory calls in 300 ms, 129 more in the 300 ms *after* the fix ran, against 0 more after disconnecting the connection the opener actually built. There is no backoff to wait out either: `consecutiveHalts` only increments on the `onHalt` path, so the loop runs at ~430 iterations a second. Now fixed by capturing that connection through a wrapping factory. **I had written a confident comment on top of code that did nothing** — the same shape as the blocker the first review caught, one commit later. **Three findings from that review are open and F27 does not close until they are:** `renderTerminalChainMismatch` calls `replaceChildren()` on a container React still owns, because `boot()` discards `mountTree`'s unmount handle, so the terminal state cannot actually tear the app down; `withDeadline` cancels nothing, and E17's own recovery action is *retry AH sync*, which re-enters `addChain` because neither the connector nor the topology dedupes an **in-flight** attach (exposure is zero today — S12 is `built-unwired`); and **the drill was changed and not re-executed**, which is V-357 repeated — the registration assertions moved node, three budgets moved, and the only post-change evidence is that the topology respawns. The inner deadline is back at 300 s, the one value ever observed to pass. **§4.8's nightly half is recorded, not implemented, and deliberately so:** `suites.json` admits only `release` and `g1`, there is no scheduled workflow in the repository at all, and this is not one drill's gap but every Zombienet suite's. Implementing it means spending hours of Actions time every night on binary fetches and Rust builds — an external commitment rather than a spec ruling, so it is **the user's call** and is listed here with that owner rather than taken unilaterally. **Owed:** a per-commit test of the latch needs a fake smoldot client that `startLightClientWith` will accept, and `RealSmoldotClient` is the pinned type — so either that signature becomes generic the way `startChainSession<C>` already is, or the drill stays its only executor. Recorded rather than skipped. `WrongChainError` is raised inside `getSmProvider`'s chain factory, and PAPI **retries that factory indefinitely** — 265 raises in one run, none propagated — so 10 §3.1's terminal `FE-BOOT-003` never reaches a caller and `chain-session.ts`'s `instanceof WrongChainError` branch is **unreachable code**. Every layer believed this control was wired: the class exists, the branch exists, the comment says *"terminal and is re-thrown"*, and the suites pass by injecting a `start` that throws. Nothing had ever run it. Three further defects the run exposed and this branch fixes: a Node worker error that **kills the process** on a benign removed-chain race (survivable in a browser, fatal here, and it fires on every compat probe because §3.2 re-runs the classifier on every `CodeUpdated`); an **unbounded** `connectAssetHub`, which 11 E17's *"blocked with diagnostics"* cannot mean; and a teardown that cannot interrupt a worker inside a long synchronous WASM computation. **F13's row was corrected**: its two-environment byte-identical run has been a required check named exactly that since #269, and the row called it external for four days. **One mistake worth recording**: `app/tools/dev-chain-pin.ts` was overwritten because a source comment described it in the present tense and `ls` was never run — F18 had shipped it. Restored from git; the row that claimed it did not exist is corrected. **The three open findings are closed (V-360).** The terminal state now tears the tree down before it replaces it: `boot` returns the unmount handle `mount()` hands it instead of dropping it, and `wrong-chain.ts` calls it first — `replaceChildren()` under a live React root deletes DOM the root still owns, and React does not fail at the deletion but at its next render against that container, which is somewhere else entirely and in a state whose whole purpose is that nothing further happens. The parameter narrowed from `Element` to a structural `TerminalHost`, because this workspace ships no DOM implementation and `check:casts` bans `as unknown as` outright, so a wide type would have left the ordering permanently untestable; a real `Element` satisfying it is proved by the compiler at `main.ts` rather than asserted in prose. `assetHubConnector.connect` deduped only **completed** attaches, so E17's *"retry AH sync"* — which the deposit leg's own deadline makes the expected path rather than the unlucky one — started a second attach, a second genesis probe and a second `chainHead_follow`, and the last to finish overwrote the cache so the rest could never be closed. Concurrent callers now join one attempt. Both mutants die: removing the join fails 3 chain-client tests, moving the unmount after `replaceChildren()` fails 3 screens tests. **The R-6 review of those fixes returned 0 blockers, 4 majors, 8 minors, and the two sharpest majors were regressions I had just introduced (V-361).** `close()` was a **total no-op against an in-flight attach** — it returns at `open === undefined`, the attempt then completes, installs a live transport nobody asked for, and serves it to every later `connect`; the join widened that to every joiner. Worse, a shared attempt that never settles **pinned the slot for the life of the connector**, so the retry control the join was protecting could never start anything again — and that is the ordinary case, not the pathological one, since `probeGenesisHash` loops with no timer and an Asset Hub genesis probe has been observed pending past five minutes. A control that reports a failure it cannot recover from is worse than the unbounded wait it replaced. So the bound moved **into** the connector: `connect` takes `deadlineMs`, an abandoned attempt detaches its chain and closes any transport it goes on to open, `close()` abandons too, and `withDeadline` is gone from `funding-session.ts` — a timer wrapped around the call could only ever abandon the wait. The two funding-session tests now compose over the **real** connector, which is what the review's *"tests that would pass on a defective implementation"* finding asked for. The other two majors are closed the same way: the plumbing that carries `FE-BOOT-003` to a screen lived in `main.ts`, which reads `document` at module scope and therefore **cannot be imported by any test**, so all eight new terminal-state cases passed on the original defect. `startShell` moves it into `boot.tsx` with every collaborator injected, `main.ts` is back to its own stated rule of two calls and no logic, and `boot` now unmounts on its own post-mount failure so *"a boot that rejects never mounted"* is structural rather than an accident of `registerReleaseWorker` catching everything. **The drill's own helper had never executed, and neither its cause nor its symptom is in this repository (V-362).** `spawnSync("node", …)` from inside zombienet does not reach the system Node: zombienet ships as a `pkg` single-file executable carrying **Node 18.5.0**, and the child came back as that — every frame under the error named `pkg/prelude/bootstrap.js`, and the binary carries pkg's `PKG_EXECPATH` and `PKG_INVOKE_NODEJS` markers. Node 18 neither strips TypeScript types (22.6 and later do) nor reads these files as ESM, so the leg died on `SyntaxError: Cannot use import statement outside a module` pointing at a correct `import` in `dev-chain-pin.ts` — a message that reads as a source defect and is nothing of the kind. The helper now resolves an **absolute** Node off PATH, requires the major band `app/.nvmrc` pins (the same file `ci.yml` feeds to `actions/setup-node`, so the drill cannot ask for a Node CI does not install), and refuses any candidate reporting `process.pkg`, which is defined only inside a pkg binary and so names what is being excluded rather than approximating it. Two weaker forms were written and thrown away first, **both of which rejected the correct answer**: comparing the child's `execPath` to the candidate (defeated by symlinks) and to `process.execPath` (defeated whenever the parent is legitimately that same Node). Found by executing the resolver against the real PATH before spending another six-minute run on it. **Then the boot leg failed on a second coupling of mine**: `client-boot.js` parsed the harness's whole stdout as JSON, which worked for exactly as long as nothing else wrote a byte to it — smoldot logs from a worker thread whose `console` is that stdout, so a run that had taken 348 seconds and **succeeded** was reported as `Unexpected token s in JSON at position 1`. The report now has a channel nothing else can write to (`--report <path>`), the previous one is deleted first so a leg producing none fails as missing rather than passing on the last run's answer, and stdout goes back to being where a person reads what happened. **Drill 14 then passed end to end — `exit=0`, all three legs, through the shipped `client-boot.js` for the first time (V-364).** `pin` in 2.1 s, `boot` in 345 s returning `compatMode: "full"` at `specVersion: 2` with a finalized head, and `wrong-chain` in 2.1 s returning `expected` and `observed` differing by exactly the nibble the harness flipped. Asset Hub is still `unavailable: no genesis answer within 300s`, which the leg tolerates and this row has recorded since the first run: a 79 MB, ~189k-entry genesis, not a dropped-peer or bootnode fault. Four runs were needed and three of the four failures were the harness rather than the client, which is the milestone's own thesis holding. **One failed run was my own teardown rather than the drill (V-363):** the kill list omitted `bleavit-node`, the only nodes not launched from `zombienet/bin/polkadot*`, so three collators of the previous network survived. They share the parachain genesis, nothing errors, every *is up* and *is registered* assertion passes, and the fresh collators sat at `best: #0` with **0 peers** until the 300 s block-height budget expired. After a complete teardown the same assertion passed in **30 s**, and the teardown now fails closed on a survivor. **15 §4.8's nightly cadence is declined by the user** on the Actions time it costs — the half of that question only they owned — so the Zombienet suites keep the `release` and `g1` tiers and no scheduled workflow is added. **Owed:** `client-boot.js` has now shipped two defects that only a live run could find and nothing tests it per commit; it is CommonJS outside the `app` workspace and takes its root from `process.cwd()`, so a per-commit test needs either an export or a root that does not depend on the invocation. Recorded rather than half-done. **Five of the review's eight minors are closed**: the `BootedShell.worker` miscitation (10 §3.2 is the compat-mode table and holds no worker state — the release worker is 12 §5.2), the `openDepositLeg` docstring that had drifted above the deadline constant's own and attached to neither symbol, the drill comment still claiming the unbounded Asset Hub connect was filed rather than fixed, the two unexported terminal types, and the *"no way to continue"* test — which listed four tag names against a tree built only from `section`, `h1` and `p`, so it could not fail without an unrelated rewrite and passed on a clickable `div`, a `role="button"` section and an `onclick` that reloads the page. It now looks for interactive tags, interactive ARIA roles and handler attributes, with a witness case proving the check fires. **A sixth minor is closed, and it was the one with teeth**: `await client.terminate()` sat **between the wrong-chain failure and its `throw`**, unbounded, so a worker inside a long synchronous WASM computation — the documented reason `boot.ts` already bounds the same call — would keep 10 §3.1's terminal state from ever reaching a screen. Both teardown sites now bound it at the drill's own 15 s, and `terminateWithin` resolves on rejection as well, because `await client.terminate()` would otherwise throw the **teardown's** error in place of the `WrongChainError`, turning a diagnosis that names two genesis hashes into whatever smoldot said while shutting down. **Two minors stay open**: the IndexedDB handle the terminal path never closes (benign — it writes nothing after the budget pass and is keyed on the pinned chain), and the connector's cache not being keyed on its `BundledChain` argument (unreachable while one Asset Hub is release-pinned, and pre-existing). **The review's three spec questions are filed as SQ-1027, SQ-1028 and SQ-1029, in batch B7** — whether 10 §3.1's terminal state may destroy INV-FE-11's *"always-available"* verification panel (recommended reading: *terminal* bars a path forward into the app and does not license the client to stop identifying itself, so the panel's content belongs on that screen), which INV-FE-9 status an observed genesis hash carries when rendered outside the typed component system, and whether E17's retry may be a renewed wait rather than an independent attempt. The third is the one F27 has already answered in code and the row asks the spec to ratify: a renewed wait while the attempt is live, a fresh attempt once it has been abandoned at its bound

**What comes next:** Get CI green on #283, which is R-6's other conjunct and the only thing left before F27 flips ✅ — the review conjunct is met (0 blockers) and drill 14 passes end to end. Then work SQ-1027 and SQ-1028, and the three review minors that stay open. A per-commit test for `client-boot.js` is owed and needs a decision first: export its internals, or take its root from the file rather than from `process.cwd()`

## — (dependency batch, unplanned)

**Merged both open `chore(deps)` PRs and found a main commit with no CI record.** #276 (`actions/setup-java` 5.6.0 → 5.7.0) and #277 (`clap` 4.6.4 → **4.6.6** in `/keeper`) were each 18/18 SUCCESS with CodeQL NEUTRAL, but both branched from `6bd20937` — four merges behind main — so neither run had seen today's tree, and main's own `ci.yml` had moved in that range while #276 edits that same file. Verified before merging rather than after: both commits cherry-picked onto `36de7ac2`, then the 377-test `tools/ci/tests` suite (the one that reads the workflow's wiring) green over the result. The keeper leg was not rebuilt, because main never touched `keeper/` in that range and #277 had already run it on that exact lockfile (R-12). New main tip `dba34593` diffs **empty** against the verified branch. Two corrections landed with the merges: Dependabot announced #277 as 4.6.5 while its lockfile resolves **4.6.6** (published between metadata generation and lock update, `clap_builder` 4.6.2 → 4.6.6 alongside), so the squash subject states the resolved versions. **The finding is CI-shaped:** `ba7a2d8b`, the intermediate main commit from #276, reports `cancelled` with `total_jobs = 0` — it never started a job. `cancel-in-progress: false` protects a **running** main run (`36de7ac2` survived both merges, still in progress) and cannot protect a **queued** one, since GitHub keeps at most one pending run per concurrency group. It sat pending 32 s and was displaced 3 s after #277 merged. A full run here exceeds two hours, so any two main commits inside one cycle leave the earlier untested. CLAUDE.md's "main never cancels" note corrected.

**What comes next:** Watch `dba34593`'s CI run, which is queued behind `36de7ac2`'s still-running one — that older run is the outstanding record for F27's #283 merge and had not finished when these landed. Then back to Track F: F27 stays open on SQ-1026, with SQ-1027/SQ-1028 and the two open review minors behind it

## **Contract v30** — guardian allowance constants + two playbook maps frozen

**A `Verified<T>` with no producer was deciding a privileged control, and the specification had already called the gap unresolved rather than a defect.** `AllowanceMeter.limit` could only be fabricated: `Guardian.Allowances` stores used-counters, nothing published a bound, and the suite shipped `finalized(3)` for all five powers where the kernel's values are 1, 2, 1 and none. 13 §1's *"exposure is unresolved"* was itself wrong — 06 §7 had already mandated it, so the note contradicted its own owning doc first. Four `#[pallet::extra_constants]` accessors, `Guardian.PlaybookRegistered` and `Guardian.ActivePlaybooks` frozen in 02 §7.4, contract 29 → 30. **`spec_version` deliberately stays at 2**, with the reasoning written into 02 §13 rather than assumed: no runtime is deployed, `git tag` is empty, v13/v21/v23 each added constants at 2, and a bump forces the paired recovery image too. Regenerated: both chain-feeds, all 302 chainhead fixtures, the surface manifest (layouts derived through `surface_layout()`, never typed), descriptors, skills examples and both verbatim copies. Rust 465 + 25 + 57 tests, `pnpm -C app test` 20 suites all green, 4 mutations all red — asserted against **two independent anchors**, the kernel constant and 06 §5.2's literals, because an accessor compared with itself proves nothing.

**What comes next:** Wire `AllowanceBook`'s limit to the frozen constants and replace O-3's two `blocking` obligations with real clauses, which closes SQ-1030 and reopens S15's approve control. **The `pause_intake` meter is windowed** — remaining is the limit minus the counter only inside `PauseIntakeAllowanceWindowEpochs`, and outside it the stored counter is stale, so a naive comparison shows an exhausted meter for an available power

## **F26** · SQ-1011/1012/1013 ruled · V-365/V-366

**The three rulings ratified what was built, and the review of them found the defect the newest rule describes.** F27 retired F26's stated blocker — the classifier now runs against a real chain — so the session went to the three open questions. All three came back the same way: the implementation was right and the *specification* was silent. 10 §5.2 keeps three modes and 10 §3.1 gains a state (`CompatUnavailable`, `FE-COMPAT-003`), because each mode is a claim about the runtime and this is a claim about the client; `ForeignMode` keeps five arms because 02 §7.7 had already frozen *"unavailable **or unprobed**"* before 10 had a name for the second. **SQ-1013 inverted its own question**: read off the pinned `smoldot@3.3.2` contract, a second `Chain` handle over an added spec is a lookup rather than a sync, the vendor *"strongly encourages"* relying on that rather than de-duplicating by hand, and the real cost is CPU share — so §9.4's memory rows were right and its **mobile CPU** row was the silent one. **Then the R-6 review found that §4.1's brand-new obligation 2 was violated by the function it was written about**: `compatProvider().release()` stopped only the newest of the topologies `getSmProvider` had made it create, leaking two chains per retry. Also closed: a red `docs` job (both VERBATIM copies drifted at exactly the two edited lines), `FE-COMPAT-003` stamped on a boot that never reached `CompatCheck`, and the drill recording the Asset Hub *wrapper* where the local leg records the lattice — the same defect, one chain over, standing beside its own written explanation. 15 §4.8's per-PR full-surface probe existed nowhere and now runs against both committed runtimes; `bleavit_recovery` had never been probed against any metadata. **Two mutants survived and are reported rather than hidden**: crossing the runtime pair still classifies `full`, because the two are surface-identical over `CRITICAL_SURFACE`.

**What comes next:** F26 stays 🔨 on three majors — the verdict has no session-scoped consumer, `InflowCaps.CumulativeDeposits` is frozen and unprobed (ruled a defect; needs a recorder run, not a hand-written layout), and INV-FE-12's signing rule is unwired. Two are the transport blocker. Integrate the four sibling branches: F13's `SIGNERS.md`, F17's client remediation, the guardian allowance freeze, and F18's live funding leg

## **F26 (Codex review of #285)** · V-369

**The leak F26 closed was reachable one race later, on the exact path the fix was written for.** `transientTopologies().track()` appended unconditionally and `releaseAll()` drained only what was already there. The probe that leaks is the **slow** one, and a slow probe is the one still inside `newTopology()` when `COMPAT_PULL_DEADLINE_MS` fires — `classifyChain`'s `finally` releases at once, and the pinned `sm-provider` cancels nothing, its whole teardown being `() => { isRunning = false; }`. So the in-flight `getChain()` still resolved, still called `track()`, and `sm-provider` then removed the **parachain alone**, leaving the relay chain and the `Topology` syncing until `LightClient.stop()`. `track()` now latches and stops a late arrival immediately. The regression test lands **three** late topologies, because a latch handling only the newest passes at two, and all three measured **zero** stops against the unfixed helper.

**What comes next:** Land F13's four findings, request the Codex re-reviews, then merge in dependency order

## **F18 (the live funding leg)** · SQ-1033, SQ-1034 ruled

**The funding read path ran against two real chains for the first time, and the Asset Hub timeout's stated cause was wrong.** Measured on the pinned client: `addChain` takes 23,644 ms on the 79.4 MB `genesis.raw` Asset Hub spec and **3 ms** on a 374-byte `genesis.stateRootHash` one, for the **identical** genesis hash — so 23.6 s was never the 300 s failure. What the connector waits on is Asset Hub's first **finalized** block, which cannot arrive before the relay has synced, and the old message named genesis and would have sent the next reader back to spec size. The support was verified by inflating the shipped WASM and reading smoldot's **own** diagnostic recommending that replacement, and the state root came from `export-genesis-head` over the spec bytes, offline — so the identity check still compares two independently sourced values. Spec: 79.4 MB → **544 bytes**. Five defects: a genesis form the client refused and smoldot accepts (and which the published Asset Hub light spec uses); the **spec id passed as `chainLabel`**, which classified every development Asset Hub `unreachable` — retryable — where the truth is terminal `wrong-chain`; an uncancellable promise raced around `connectAssetHub`, breaking E17's retry; **D-13's Phase-3 caps having no reader at all**, so every bootstrap-era deposit blocks; and SQ-1026 no longer reproducing, which made `zombienet/README.md`'s "the wrong-chain leg is red" row stale. **Drill 14 is green end to end for the first time** — `pin` 6.0 s, `boot` 342.6 s, `funding` 17.4 s, `wrong-chain` 1.9 s — and the harness's decisions moved out of the shell script into per-commit tests. 2,224 app tests, 0 failures.

**What comes next:** The two remainders converge: **SQ-1034**'s D-13 reader needs `InflowCaps.CumulativeDeposits` in the surface manifest, which is the identical gap F26's review found from the other side, so **one recorder run closes both** — and it must be a recorder run, because manifest layouts are recorded bytes. The deposit leg's positive case is certified by Chopsticks rather than Zombienet, now normative in 15 §4.8: §7.7 pins the Asset Hub of the relay a release targets, so a locally generated one can only ever be `wrong-chain`

## **F18 (Codex review of #289)** · V-368

**Drill 14 could pass having read one chain, and a ready leg could pass having decoded nothing.** `openDepositLeg` reports six blocked arms as `{ kind: 'blocked', reason }`, and the harness accepted any of them on a nonempty reason — so a *second* local `FinalizedReader.open` that failed left `withdraw: ready` beside `deposit: blocked`, indistinguishable from the documented Asset Hub refusal that 15 §4.8 says the topology genuinely forces. The blocked arm now carries a `DepositBlockCause` beside the sentence, mapped through a record keyed on the connector's own refusal arms so a seventh arm fails to compile rather than widening what the drill excuses, and the harness accepts a **closed list** of two causes rather than an `asset-hub-` prefix — `asset-hub-unreadable` and `classification-failed` are defects wearing an Asset Hub name. Separately, `requireReads` checked only that a key was built, never that the answer decoded, so a release-tier run stayed green with `ForeignAssets.Account`, `Assets.Account`, `System.Account` or `PhaseFlags` undecodable. A ready leg must now carry an empty `undecodable` list, and an absent list is not an empty one. Six tests, each run red first. **Not observed live:** Zombienet is a shared exclusive resource and the behavior is argued from source. **Residual, and it is the fix working:** if Asset Hub's `System.Account` bytes do not decode under the pinned Paseo metadata, drill 14 now goes red where it was green — that account is likely absent today, which decodes as a real zero, so the Asset Hub decoders may never have run on real bytes at all.

**What comes next:** Land F13's four findings, request the Codex re-reviews, then merge in dependency order

## **F17 (the owed re-review)** · SQ-1030, SQ-1031 filed

**The re-review found the class open again, and the sharpest blocker was one the source code asserted was safe.** `PB-MIGRATION` reached `ready` on propose and approve while 06 §6.2 guarantees the fifth approval reverts and records nothing — the client's comment said *"the runtime accepts the activation"*, which is true of `check_and_consume` and false of `dispatch` one frame up, and **a test pinned the wrong claim**. The pattern is the milestone's own: each round fixes the shapes it is shown and leaves the conditions one layer above where it looked. Three blockers, four majors, four minors; ten fixed here. **The one that is not is the one worth naming**: `AllowanceMeter.limit` was a `Verified<number>` with **no producer**, and the suite shipped `finalized(3)` for all five powers where the kernel's values are 1, 2, 1 and none — a fabricated provenance stamp deciding a privileged control, on the precondition §11.8.2 names by name. Ruled: **freeze the four kernel constants rather than close S15**, since the pallet already carries a `#[pallet::constant_name]` block and closing a guardian's core control over a compile-time number is the wrong R-7 trade. 14 mutants, 0 survivors, with the six bad first-pass mutants reported rather than counted.

**What comes next:** Integrate `track-f/guardian-allowance-freeze` next: it closes SQ-1030 and blocker 3 together, and closing SQ-1030 is what replaces O-3's two `blocking` obligations with real clauses and reopens S15's approve control. Until then that control is shut — correct under INV-FE-12, and the reason the suite now asserts `approvalBlocks(clean) === []` independently of the gate

## **F17 (Codex review of #287)** · V-367

**A blocking obligation attached to a row closed a control for four powers whose dispatch never reads it.** `guardian.approve_action` is one row, `O-3`, for all five guardian powers, and `operatorGate` spliced in `blockingObligationsFor(row)` unconditionally. The pallet guards both of `O-3`'s blocking obligations behind `if let GuardianPower::ActivatePlaybook` — and `guardian_core::dispatch` narrows the second to `PlaybookId::LedgerFreeze` alone — so approving a `pause_intake`, `delay_once`, `force_rerun` or `suspend_on_gate` was refused for a condition its own dispatch never evaluates. That is the client refusing what the chain would accept, which is the direction 09 §1.2's mirror exists to prevent, arriving through the one door `check-dispatch-mirror.py` does not watch. `UnreadableObligation` gained an optional `ObligationScope`, `blockingObligationsFor` takes the pending action and drops an obligation **only** on a positive mismatch, and an undecodable power keeps every obligation — scoping narrows on evidence, never on its absence. `check-unreadable-obligations.py` tolerates the new argument and was mutation-tested both ways. The screens suite had been asserting the over-blocking; it now renders a `pause_intake` approval with zero unreadable notices and a `PB-LEDGER-FREEZE` activation with two.

**What comes next:** Land the four remaining Codex findings, then merge in dependency order

## **F13 (the buildable half)** · SQ-1032 ruled

**The park said both remainders were external and one of them was a document that did not exist.** `SIGNERS.md` was absent from the repository entirely, and only the *names* in it were ever external — so the document, its schema and a fail-closed bidirectional checker all shipped, with **zero entries**, reporting four unseated populations rather than a clean separation. `compare` was the other half: the live gateway call is genuinely external and is four lines, while everything that decides — the fetch loop, the byte comparison against the signed map, cross-gateway divergence, minisign verification, the counting, the verdict — now runs per commit against a constructed transcript. **The defect this turned up is the one worth carrying forward**: `countAttestations` took no keyring, so a **revoked** key could satisfy §1.4's floor, while `attestation_monitor.py` already excluded them — two implementations of one control, disagreeing, with only one right. That led to **SQ-1032**, ruled here: §5.2's *"≥ 2 attestations"* inherits §1.4 gate 2's **by organization** reading rather than being a looser restatement, because independence is the whole claim a reproduction makes and §5.2's job is to detect the case where one organization is the adversary. 38 mutations, 0 survived, after two first-round survivors that were both real gaps in the suite.

**What comes next:** The monitor's keyring format needs an `organization` field before its `attestations_valid` series means what §1.4 means — named, and left to O5 to sequence, since changing a published format is not this milestone's call. F13's remainder is now the ceremony, the real names and keys, and `liveGateway`

## **`explainer/` re-trued against main and widened to the whole runtime (user-requested; no milestone advanced)**
span: 2026-08-08/09

**Session in progress at the time of writing — the tree does not compile, deliberately, and the state is recorded here rather than left to be rediscovered.** The explainer branch was cut at `8a95ef4c`; main has since advanced 69 commits, so its mirrored facts were re-checked against the current tree and it was asked to cover the whole parachain rather than the proposal flow alone. **Drift found and fixed (each verified against source before applying, not taken from the audit):** `INTEGRATION_CONTRACT_VERSION` 16 → **29** (user-visible, 13 bumps stale); the runtime API described as "eleven-method" with 11 of its **16** methods listed; `keeper.rebate` **353× wrong** (0.09 USDC vs the re-derived 0.000255, E5/SQ-531); `collator.comp` 2,000 → **500** (SQ-536 re-anchor); the doc-13 registry 106 → **113** keys (`ledger.rdm_fee` + six `svc.*`), which the app's own genesis-key gate had already been failing on; doc **16** absent from the citation type; doc titles for 06/07/09 wrong; the 03 §5.3a **redemption fee** entirely absent, so five calls reported gross payouts as what a claimant receives; the `ledger.min_split` floor missing from Signed `split_scalar`/`split_gate`; doc 05 §3.3's **corrected** k=3 failure mode (a 1-in-5 permanent forfeit, not the halt the app asserted twice); E6's author/funder split on T1/T2; contract-v19 oracle rulings (a reporter may not challenge its own round; a default decides bonds and **never** the value; a round-1 default pays the challenger nothing); metric 20's E1 label. **Two finds are structural.** (1) The guard scene was built on a **deleted** spec row: check 14 was `DescriptorLeadTime`, which 11 §11.5 removed (SQ-552) because that wait starts *after* a successful `execute` — executing is what authorizes the upgrade — so it was never a precondition `execute` could re-read; rows 12–14 are now dead-man / triggering freeze / batch bounds, and the scene's "fourteen conditions **the chain** reads" is corrected to eleven runtime checks shown as fourteen client rows. (2) `lmsr.ts` **priced a quote the chain refuses**: replaying `crates/market-core/fixtures/chain-quote-agreement.json` (added since the branch point) matched 31/32 rows, and the 32nd returned a real cost with `evaluable: true` where the runtime maps the fixed-point domain error to `PriceBoundExceeded` and returns the G-1 zero sentinel. The audit's proposed fix would have introduced a second defect — it ordered the domain check ahead of the trade bounds, but `core_market::quote` never checks bounds at all while the §6.1 extrinsic checks them first — so both paths are modelled separately and all 32 rows now replay as `chain-quotes.test.ts`. The corpus cannot express a refusal, which is why it never caught this. **Also landed:** the redemption fee with its per-leg pair rule (reproducing 13 §5.3a(2a)'s own witness exactly — 19,957, not the 19,940 a combined base gives) and its net-based waiver; `src/ui/glossary.ts` + `<Jargon>`, wiring up the `Term` component that had been **dead code since the first build**; a three-chapter grouped nav for 14 scenes. Gates on the correction pass alone: **530 tests** (was 521), lint · typecheck · math-check · build green, shell 184 kB gz, renderer still lazy. **IN FLIGHT, and this is what the next session must know:** six new scenes (`the-chain`, `the-upgrade`, `the-border`, `the-service`, `the-referees`, `the-window`) are being written by six background agents. `registry.ts`, `store.ts` and `App.tsx` already reference all six. **`npx tsc --noEmit` therefore fails with exactly five `TS2307 Cannot find module` errors** and nothing else; `the-upgrade/` has landed, the other five had not at the time of writing. `src/protocol` and `src/ui` pass in isolation (351 tests). No scene suite, no lint and no build has been run against the new scenes.

**What comes next:** **Session complete; the in-flight state described in the Done cell is resolved and every gate is green.** All six scenes landed: **684 tests / 30 files** (was 521 / 22), `tsc` 0 errors, `eslint` clean, generated-math check current, build green. Shell **248 kB gz** (was 182 — six substantial scenes), renderer unchanged at 248.83 kB gz and still strictly lazy: `dist/index.html` preloads only the app chunk and the stylesheet, and the ESLint three-import fence passes. Five glossary terms the scene agents had defined locally were promoted to `ui/glossary.ts` so no word carries two definitions (Aura, session, finalized, attestor, teleport); six genuinely scene-local ones stay as `<Term>`. **One correction to this session's own work:** the `pallet` glossary entry said Bleavit has thirty-nine pallets; `construct_runtime!` has **45** (44 once bootstrap `Sudo` is compiled out), so the count was dropped rather than corrected, since it legitimately changes at the Phase 3→4 boundary. **A spec defect surfaced and is recorded in the Decision log rather than fixed** (13 §5's proof budget is derived from a superseded 5 MiB PoV ceiling; every share it prints is double the true value). Follow-ups, none blocking: the doc 13 §5 correction above; the 06 §6.1 client-ingress reading `the-border` flagged; three doc-02 §3 bounds (`MAX_PARAM_KEYS`, `MAX_OPEN_ORACLE_ROUNDS`, `MAX_TREASURY_STREAMS`) still defined scene-locally rather than in `protocol/constants.ts`; and the no-text-overlap sweep has **not** been re-run over the six new scenes — it is a manual browser harness, not part of `npm run verify`, and it is the one pre-existing guarantee this session did not re-establish. **No spec, no runtime code and no milestone status was touched; `app/` (Track F) is untouched and F0 remains ⬜.**
