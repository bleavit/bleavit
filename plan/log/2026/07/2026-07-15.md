# Session log — 2026-07-15

## A1 (FRAME pallet)

Implemented `pallets/constitution` as a production `#[frame_support::pallet]` over `constitution-core`. **Dependency wiring:** the `polkadot-sdk` 2603.0.0 umbrella proved unusable as a direct dep (its lockfile resolution drags the node stack in: `sc-service → litep2p → multihash → core2 0.4.0` which is yanked), so the workspace now pins the FRAME crates directly at the versions copied from the umbrella's own manifest (`frame-support`/`frame-system`/`frame-benchmarking` 46.0.0, `sp-runtime` 46.0.0, `sp-io` 45.0.0, `sp-core` 40.0.0 = the stable2603 train; umbrella line kept as the documented anchor). stable2603 requires `DecodeWithMemTracking` on call args — added the derive to `futarchy-primitives::{FixedU64, ProposalClass}` and `constitution-core::{ParamValue, Capability, CapabilityRecord}` (additive, SCALE-identical; remaining Track-A pallets will need the same on their call-arg types). **Pallet:** frozen 02 §7.3 storage (`Params` CountedStorageMap ≤64 genesis-fixed, `PhaseFlags` u32, `ReleaseChannel` 168-byte value whose raw key + no-length-prefix layout are test-pinned; runtime must instance as `Constitution`), `Meters`/`Capabilities` BoundedVecs, Config{GovernanceOrigin→`ConstitutionOrigin`, CurrentEpoch, WeightInfo, BenchmarkHelper(bench-only)}, new-style `frame_system::Config<RuntimeEvent: From<Event<Self>>>` supertrait (stable2603 deprecates `type RuntimeEvent`), events/errors 1:1 with the core, genesis (code-owned 13 §1 registry seeding; only phase-flags/release-channel chain-spec-configurable; genesis-time asserts), try-state rebuilding the core aggregate + D-14 key identity, `#[pallet::extra_constants]` incl. `INTEGRATION_CONTRACT_VERSION`, storage-info strict (no `without_storage_info`), v2 benchmarks + placeholder `WeightInfo` (B5). **Spec-reviewer pass (R-6) found 1 blocker + 6 majors + 5 minors; fixed in-session:** (blocker) META+values keys were values-dispatchable and not META-dispatchable — inverted per 06 §1/§2.2 (`(FutarchyMeta, MetaAndValues)`; values half = execute-time ratification; `epoch.horizon_k` seeded + matrix tests); (major) `set_capability` no longer admits ConstitutionalValues (06 §3.2 row 4); (major) `set_phase_flag` narrowed to Root-only (09 §5.4) with machinery bits via new internal `note_phase_flag`; (major) un-spec'd `charge_meter` extrinsic removed — runtime-internal `charge_meter_internal` only (06 §3.2 closed matrix), call enum test-pinned to exactly 4 spec-named calls; (minor) metadata constant renamed to canonical `INTEGRATION_CONTRACT_VERSION`; also `constitution-core::can_set_release_channel` fixed earlier in-session to ConstitutionalValues-only per 02 §12's exhaustive writer list. **Remaining majors are user-gated and A1 stays 🔨:** I-17 meter content (SQ-12), full 13 §1 registry seeding — 7 of ~50 keys (SQ-8/SQ-10), `amend_registry` (SQ-7); minors: Root arms (SQ-11), `key16` truncation (SQ-8), 13 §4 bound rows (SQ-9), I-6 SMT (Track S). Ten spec questions SQ-4…SQ-13 logged from the review. **Gates (all green):** fmt · clippy -D warnings · workspace tests 186/186 (pallet 32 + benchmarks-feature 36 incl. 4 executed benches; core 13) · try-runtime check · no_std check (native no_std+benchmarks is upstream-unsupported — needs the wasm `substrate_runtime` cfg, exercised at B1a) · corpus/reference-model/vector freshness · docs link lint. Genesis-time `assert!` in `genesis_build` is the one deliberate panic surface (FRAME chain-spec convention; no dispatch-path panics). **Codex note:** user asked for Codex subagents, but the Codex CLI was unauthenticated all session (`loggedIn:false`); review ran with the native spec-reviewer instead — run `codex login`, then `/codex:adversarial-review` on this diff as a follow-up.

**What comes next:** Resolve SQ-4…SQ-13 with the user, then close A1's three re-scoped gaps (meters, registry seeding, amend_registry) and mark A1 ✅; then A2 (`pallet-conditional-ledger`) by the same shell-over-core pattern

## A1 (Codex adversarial review + fix round 2)

Codex CLI made operational for the session (three-layer failure unwound: CLI 0.45.0 → 0.144.4 upgrade for the plugin's app-server protocol; stale `model = "gpt-5.1-codex-max"` pin commented out of `~/.codex/config.toml`, backup kept). `/codex`-style adversarial review of the A1 diff returned "needs-attention" (5 high / 4 medium); triage: 4 highs corroborated the spec-reviewer's SQ-gated gaps, the rest were valid and fixed: **(1)** Root removed from `can_set_param`/`can_set_capability`/`can_charge_meter` — 09 §5.4's sudo scope is exhaustive and excludes parameter administration; matrix tests now pin Root refusal everywhere except phase-flag arming (SQ-11 stays open for deliberate widening). **(2)** `set_phase_flag` bit-scoped to `SUDO_ARMABLE_MASK` (bits 0–4, new `FlagNotArmable` error in core+shell); generic `note_phase_flag` replaced by one-bit internal setters (`note_ledger_frozen`/`note_dead_man_engaged`/`note_reserve_health`) so no internal caller can reach arming bits or another pallet's signal; full caller-auth story remains a B1a/S5 negative-suite obligation (Rust cannot authenticate calling pallets). **(3)** Registry seeded 7 → **32 keys**: every 13 §1 row that is scalar, concrete-valued, ≤16-byte-keyed and non-[VERIFY] (mkt.kappa/fee, dec.window/trailing/delta_max/coverage, gate.p_max/nb_coverage/nb_conv, exec.grace, trs.cap_proposal/30d/180d, code.spacing, orc.bond_floor/bond_bps/rounds/n_min, wt.quorum/stake, res.probe_amount, collator.n_min, att.bond, ledger.min_split, pol.b_gate) with kernel-constant floors where they exist and encodings pinned by a new spot-check test; remaining ~18 rows blocked on SQ-8/SQ-10/[VERIFY]. **(4)** `ReleaseChannel::new` now enforces 02 §12's "flags bits 3–31 reserved zero" (rejection + all-defined-bits vectors, extrinsic + core + genesis paths). **(5)** Workspace FRAME deps `=`-exact-pinned (frame-support =46.0.1, others =46.0.0/=45.0.0/=40.0.0; dead polkadot-sdk anchor entry removed, comment keeps the umbrella as version source) so `cargo update` cannot drift the SDK mix before B1a. **(6)** New 600-step seeded (xorshift, deterministic) randomized shell-vs-core differential across all five ops × nine authorities × shifting epochs with exact `Result` equality — it immediately caught a real core defect: a refused `Meter::charge` persisted its lazy window reset while FRAME's transactional storage rolled it back; core fixed to strict no-op-on-failure (G-1), differential green. Codex's `--all-features` test failure not reproducible (passes here; likely its sandbox). Placeholder weights confirmed B5-owned (Track-A DoD). Gates: fmt · clippy -D warnings · workspace 188/188 · benchmarks-feature 38 (5 benches execute) · --all-features · no_std check · corpus/reference-model/vectors · docs link lint — all green.

**What comes next:** Resolve SQ-4…SQ-13, close A1's three user-gated gaps, mark A1 ✅; then A2

## A1 (SQ resolutions + completion + ✅)

User delegated the open spec decisions ("Decide on your own how to fix the remaining issues"). Resolved SQ-4…SQ-13 as an R-1 amendment batch across 06/13/15 (02 deliberately untouched — no contract bump; see Decision log ×2): release-channel track/matrix rows, META+values `set_param` added to the rule-altering list, TREASURY-class `set_param` row, `PhaseFlags` writer map, canonical `ParamKey` encoding + per-class suffix convention + view projection, Percent→Perbill type fixes, 13 §4 constitution bound rows (Params 128/Caps 64/Meters 16), I-7/I-17 metering re-scoped to treasury/guard. Implemented: `constitution.amend_registry` (5th extrinsic; kernel-bounded rows refuse all amendment per 13 rule 2, non-kernel rows meta-bounded: min ≤ value ≤ max, kind-preserving, cooldown ≤ 8; origins FutarchyMeta | ConstitutionalValues; bench + weights + origin/error/adversarial tests), `ParamRecord.kernel_bounded` field, strict `key16` (>16-byte names rejected; explicit short keys), the materialized **87-key** genesis registry (all scalar concrete non-formula 13 §1 rows; per-class suffixes; kernel flags per the new rule-7 enumeration), empty constitution `genesis_meters()`, randomized differential extended with an `amend_registry` arm. Final spec-reviewer pass (after fixes): **0 blockers**; its 1 major (kernel rows allowed Δ/cooldown amendment vs rule 2) and 6 minors (over-conservative kb flags → enumerated normatively; [VERIFY] seeding inconsistency; "—" cooldown drift; `(Code, SetReleaseChannel)` capability row vs 02 §12; stale comments; 06 gloss) all fixed in-session; its spec-questions A–G resolved inside the same delegation (conservative directions) — two residual notes left for future doc touches (05 §1.3 "constitutional meters" gloss; keep rule-7 enumeration in sync). Gates: fmt · clippy -D warnings · workspace tests green (constitution 38 + core 13; 43 with benchmarks incl. 6 executed benches) · --all-features · no_std · corpus/reference-model/vectors · docs link lint. **A1 ✅.**

**What comes next:** Create the A1 PR (user-requested); then A2 — `pallet-conditional-ledger`

## — (Track M audit, report-only)

Ran `/spec-audit` over Track M (M0–M3): four parallel spec-reviewer agents (M0 vs 01 §9; M1 vs 02 §2/13 §2/01 §5.2; M2 vs 04 §4–§5; M3 vs 04 §5/15 §4.4/08/05 §4.4). Merged verdict **0 blocker / 13 major / 22 minor** (deduped; one corpus/differential major shared M2+M3) + 2 majors routed outside Track M — see Audit log. Load-bearing: `futarchy-fixed::exp2` violates 04 §4's normative ≤2 ulp bound (10.11 ulp worst, 36.9 % of 60k sampled fracs > 2 ulp, confirmed against the compiled crate; the 17-point committed corpus samples only 1–3-set-bit fracs, so the existing gate cannot detect it); the reference model is deep only for LMSR — its TWAP weights observations forward where market-core weights backward, `decide()` lacks 5 of 12 reject reasons and the Extend path, welfare pipeline / ledger op-set (≈7 of 22) / treasury worked arithmetic largely absent, sizing check rounds in the claimant-favoring direction; the 04 §5 three-way CI rule is unimplemented (futarchy-fixed certifies against the parallel Decimal-80 generator in `tools/fixed`, which already diverges from `vectors.json` on V3; `vectors.json` has zero Rust consumers); ≈half of 13's K-marked kernel constants are absent from `futarchy-primitives` with downstream cores hardcoding the values (`wt.quorum` floor exists as two independent copies); frozen `Proposal`/`ExecutionRecord` layouts live outside primitives; CI never compiles runtime-benchmarks/try-runtime code, installs a floating stable toolchain, and runs cargo unlocked. Cheap gates re-run green (futarchy-fixed 10 + primitives 6 tests, reference-model 8/8, both corpora fresh, doc links OK; the `--no-default-features` test run is vacuous — 0 tests execute). Report-only: no code changed, no statuses flipped (M2/M3 ✅ invalidation + M1 decision need user consent). **Mid-session the working tree was switched externally** from `feat/a1-constitution-frame-pallet` (1c0bb76, where the audit began and whose M0/M1/Cargo artifacts the reviewers cite) to new branch `fix/m3-reference-model-spec-compliance` (base 1db94f2, pre-A1) — audit records live on this branch; futarchy-fixed/reference-model/tools/CI files are identical in both states; SQ-14…SQ-28 numbered above the A1 branch's SQ-4…SQ-13 to avoid merge collisions (reconciled onto post-#44 main by the PR carrying these records).

**What comes next:** User: decide M2/M3 (and M1) status flips + remediation order (this branch's name suggests M3 first); answer SQ-14/SQ-16/SQ-17/SQ-18 before the M2/M3 fixes — they determine the fix shapes

## — (SQ resolution batch + audit consequences,…

— (SQ resolution batch + audit consequences, user-delegated)

Same session, on user instruction ("resolve the spec questions yourself", "don't wait for my consent"): resolved **SQ-14…SQ-28** as one R-1 batch across 00/01/04/05/09/13/14/15 (full inventory in the Decision log row this date) — highlights: 04 §4 ulp semantics + guarded-precision mandate + two-tier corpus cadence, 04 §5 corpus schema ownership + single-generator rule, 04 §7 backward TWAP weighting, 05 §1.2 frozen 20-field `Proposal<AccountId>` layout, 09 §1.2(3) `RuntimeVersionConstraint` layout, 15 §4.4 conforming-implementation duty + §4.5 supply-chain gates, 13 rule-1 primitives boundary / θ floors 0.90-0.85 / phase-offset representation, 01 §5.1 attestor row + §5.2 single-home rule + §9 pinning regime, SQ-23 queued as a 02 §9 erratum into the B2 amendment with SQ-2 (02 untouched, no bump). **Applied the audit consequences: M1, M2, M3 flipped ✅→🔨** with remediation deltas in their row notes; M0 kept ✅ with CI follow-ups noted; B8 row gains the release-side supply-chain + ≥10⁷-sweep obligations; Current focus rewritten (M3 remediation owned by this branch). All spec edits internally cross-referenced; docs link lint green; design kit checked for stale restatements (none — only a doc-11 VERBATIM copy mentions `RuntimeVersionConstraint`, semantics unchanged). No code changed **by this session**; while these records were being written, a concurrent external process began landing M3-remediation code in this branch's working tree (`reference-model/` + generator + tests, ~2.6k uncommitted lines appearing live) — whoever lands it must re-verify against the **amended** spec texts of this batch (esp. 04 §7 backward TWAP weighting, 05 §4.4 grid-exactness, 04 §5 single-generator/replayable-rows rules) and update M3's status per R-6.

**What comes next:** Next session: `/implement M3` on this branch (fix TWAP direction/staleness, decide() truth table, grid-exact welfare, 22-op ledger, 08 arithmetic + claimant-adverse rounding, single-generator corpus with Rust consumers, prec-context fix, replayable V6); then M2, then M1; fold the M0 CI follow-ups into the first session that touches CI

## A10 (`pallet-guardian` + `pallet-attestor`) — **in…

A10 (`pallet-guardian` + `pallet-attestor`) — **in progress**

`pallet-guardian` shipped as a production `#[frame_support::pallet]` over `guardian-core`: decomposed bounded storage named per 06 §8 (`Members`/`MemberBonds`/`PendingActions`/`Approvals`/`ReviewDeadlines`/`ActivePlaybooks`/`Allowances` + `RerunUsed`/`NextActionId`), five origin-checked extrinsics (`set_members`/`ratify_action`/`renew_playbook` = `ConstitutionalValues`; `propose_action`/`approve_action` = Signed member), the 02 §6 frozen event schema incl. `ReviewScheduled{action,referendum}` via an injected `ReviewScheduler` seam, cross-pallet reads via injected `ProposalStatusProvider`/`TriggerProvider` seams, a bounded `on_initialize` crank (playbook expiry + overdue-review slashing), `try_state`, genesis, mock, v2 `#[benchmarks]`×6 + `WeightInfo`. `DecodeWithMemTracking` added to the `guardian-core` call/event SCALE types (same wiring A1 needed); `T::AccountId↔[u8;32]` bridge via a supertrait assoc-type bound. Guardian gates green (lib + 9 unit + 17 bench-suite tests, `clippy -D warnings`, `fmt --check`, `runtime-benchmarks` + `try-runtime` feature builds). **In flight (subagents):** `pallet-attestor` authored by a Codex subagent mirroring guardian (storage per 06 §8, calls `set_members`/`attest`/`challenge_attestation`/`resolve_challenge`, `ValuesOrigin`/`RatifyOrigin` seams, 2-of-N quorum view); guardian 15 §4.1 suite + seeded shell-vs-core differential by `test-engineer`. Bonds mirror core arithmetic (no `Currency` — A10 is not audit-scope-A; real `fungible` holds are B-track, like PoV weights → B5 and origin wiring → B1a).

**What comes next:** Verify attestor + guardian differential; add the I-23 "every dispatched action reviewed" `try_state` check; `spec-reviewer` (guardian + attestor) + Codex adversarial review; full workspace gates; flip A10 ✅; open the PR

## A10 (checkpoint)

`pallet-attestor` completed by the Codex subagent (mirroring guardian) and **independently re-verified green** by this session: `lib.rs` 536 / `tests.rs` 487 / mock 140 / benchmarking 119 / weights 70; fmt · `clippy -D warnings` · 16 unit tests · 20 with `runtime-benchmarks` · `try-runtime` build · `attestor-core` 5 tests all pass. Storage per 06 §8 (`Members`/`Attestations` + `attestations_for`/`open_challenges` projections), calls `set_members`(ConstitutionalValues)/`attest`/`challenge_attestation`(Signed)/`resolve_challenge`(RatifyOrigin), 2-of-N quorum view for the guard (09 §1.2), try_state, genesis, v2 benchmarks; `att.min_members`/`att.quorum` sourced from `primitives::kernel` (single-home). Scope check: Codex touched only `pallets/attestor/**` + `crates/attestor-core/src/lib.rs` (DecodeWithMemTracking derives). `spec-reviewer` on attestor + `test-engineer` on the guardian 15 §4.1 suite/differential are in flight.

**What comes next:** Same "Next" as the row above

## **A10 (`pallet-guardian` + `pallet-attestor`) ✅**

Both pallets shipped as production `#[frame_support::pallet]`s over their frame-free cores (shell-over-core, orchestrated with Codex + `spec-reviewer` + `test-engineer` subagents). **Guardian**: 5 origin-checked calls, 02 §6 frozen events via a `ReviewScheduler` seam + cross-pallet `ProposalStatusProvider`/`TriggerProvider` seams, bounded `on_initialize` crank with terminal reaping, I-23 `try_state`; 35 unit + 11 core + 41 bench-suite tests incl. a **500-step × 20-seed shell-vs-core differential**. **Attestor** (Codex-authored, mirroring guardian): 4 calls, 2-of-N quorum view for the guard, 17 unit + 5 core tests. **Reviews (R-6):** two `spec-reviewer` passes (guardian 0 blocker/5 major/6 minor; attestor 0/1/4) + a Codex adversarial pass — **every major fixed in-session with tests**: guardian dispatched/expired + terminal reaping (lifetime→concurrent caps), re-election clears `pending`+`approvals` (stale-approval 5-of-7 bypass), `RecallScheduler` seam (§5.4 recall referendum), I-23 `try_state`, `saturating_add`; attestor `try_state` recall relax + `NoOpenChallenge`, `kernel` single-home. **Gates green:** fmt · `clippy --workspace -D warnings` · `cargo test --workspace` (54 suites) · `runtime-benchmarks` + `try-runtime` feature builds. Milestone-row note + Decision-log entry record the spec-silent design decisions; **SQ-46** (attestor `resolve_challenge`/event-naming freeze) + **SQ-45** (guardian per-playbook expiry / renewal / deadline edges) raised for ratification. Deferrals (B-track): `fungible` bond holds, seam wiring, per-playbook expiry, K/META single-home.

**What comes next:** Open the A10 PR to `main`; then **A2** (`pallet-conditional-ledger`, audit-scope-A) and A3–A9/A11 by the same shell-over-core pattern; then B1a runtime assembly

## — (M3 fix-pass reconciliation + SQ-29/SQ-30, autonomous…

— (M3 fix-pass reconciliation + SQ-29/SQ-30, autonomous continuation)

The M3 "external process" identified: the audit's own M3 spec-reviewer subagent, user-redirected mid-session to fix its findings via Codex-authored patches on `fix/m3-reference-model-spec-compliance`. Verified its commit boundaries (code commit 4214662 stages zero `docs/architecture/` files; records commit bd05727 is PLAN-only; uncommitted tree = exactly the SQ-14…28 spec batch) and re-ran the fast gates on the combined state: model 21/21, both corpora fresh, new doc-table gate green, futarchy-fixed 12/12 incl. the two new `vectors.json` differential consumers, links green. Resolved its two new spec questions under the standing delegation: SQ-29 (08 §5.4 PARAM doubling slip → 113,863/170,794/56,931 family; model workaround dropped in fix-branch commit 41531dd) and SQ-30 (05 §4.6 percentile family = inclusive linear/type-7 on the 1e9 grid, round-down). Docs-PR branch `docs/track-m-audit-and-sq-resolutions` gained commit #2 with the 08/05 §4.6 edits + these records; the fix branch is now 3 commits. Both branches remain push-blocked — no GitHub credentials in this environment.

**What comes next:** User: `! gh auth login` (or push manually), then both PRs go up; merge the docs PR first so the fix PR sits on the amended spec

## M3 (remediation implementation)

The M3 audit's own fix pass (read-only Codex authors, reviewer applied + gated) landed the reference-model remediation: backward/staleness TWAP (04 §7); full `RejectReason` surface + strict `decide()` order + Extend (05 §5.4/§5.5); complete ledger families + B-5 vector + VOID/conservation (03 §5/§6); grid-exact welfare + cold start + full-pipeline vectors (05 §4.4–§4.6); treasury worked arithmetic + claimant-adverse sizing (08, 05 §5.6); single-generator `bleavit.reference-model.v3` fixtures (`tools/fixed` derives from the model, replayable V6, all values model-computed, honest precision metadata); `futarchy-fixed` now consumes `vectors.json` (V1–V6 + 8-sample corpus); `check-doc-table.py` + CI/local-gate wiring; AGENTS.md gate row corrected (unittest, not pytest). SQ-29's PARAM-depth slip and SQ-30's percentile family surfaced here and were resolved in the docs PR (spec side) + reconciled in the model. Gates green: model 21/21, both corpora fresh, doc-table, links; `futarchy-fixed` 12/12 incl. the new differential consumers. **Delivered as a stacked PR on `docs/track-m-audit-and-sq-resolutions`** so the model's 56,931 `P_ref` matches the amended 08 §5.4 (a same-branch merge to main would have failed `check-doc-table.py`). M3 stays 🔨: ✅ gated on M2's guarded-precision exp2 + a fresh spec-reviewer pass (R-6).

**What comes next:** M2 remediation (guarded-precision exp2/log2 + per-commit ≥10³ dense-bit corpus gate, 04 §4–§5), then a spec-reviewer pass to close M3

## — (#46 Codex review triage, autonomous)

Reviewed the three `chatgpt-codex-connector` findings on PR #46 (all verified real). **Fixed inline (spec-internal, no 02 bump):** P1 — the frozen 05 §1.2 `Proposal` layout was missing `payload_len` although 09 §1.1 lists it as a queued field, 09 §1.2(2) requires the (hash, len) match, and 05 §5.6's `decide()` reads `p.payload_len`; added `payload_len: u32` after `payload_hash` (safe — the struct is not yet shipped in `futarchy-primitives`; separately, epoch-core's `Proposal` also lacks the field → A8 code follow-up). **Reconciled wording + deferred the 02 edits to the B2 amendment (avoids a mid-batch contract bump):** P2a — 13 §3.1 no longer claims 02 §9 is already consistent; it notes 02 §9's combined row still lists the phase-offset fractions under `params()` and that the split is queued for B2. P2b — 01 §5.1 and 06 §7 softened from "storage listed in 02" (a dangling ref — 02 has only `AttestationMissing`) to "to be frozen in 02 §7 at B2". The B2-pending list (SQ-2 row) now enumerates all three 02 items. Docs-fix committed to the `docs/track-m-audit-and-sq-resolutions` branch → updates #46. Link lint + doc-table + reference-model gates green.

**What comes next:** Merge #46 (carries spec + M3 fix); then M2 remediation; the three 02 items land as the B2 contract amendment (one bump, joint FE+BE sign-off)

## A4 (`pallet-origins` + `SafetyFilter`, FRAME shell)

Turned the `pallet-origins` scaffold into a production `#[frame_support::pallet]` **stateless shim** (no storage/calls/events/genesis, à la OpenGov `pallet-custom-origins`): real `#[pallet::origin]` 8-variant enum (06 §3.1; `DecodeWithMemTracking` for stable2603 `construct_runtime!`; byte-for-byte SCALE-index differential vs `origins-core`), the `EnsureOrigin` set (`decl_unit_ensure!` ×8 + `EnsureFutarchyOrigin`), `SafetyFilter` as a classifier-parameterized `Contains` (`SafetyClassifier`/`ModelClassifier`; concrete `RuntimeCall` projection = the one explicit B1a extension point, so the wrapper set stays *closed*; logic shared with the reviewed core), stateless `try_state`, real `construct_runtime!` mock, 19-test 15 §4.1 suite (I-8 scope split, I-10/I-11 closed-wrapper negatives incl. `proxy_announced`/`as_multi_threshold_1`/laundering/scheduler/budgets, G-5 no-escalation) + v2 worst-case-filter benchmark + `WeightInfo`; B1 frame-free model repointed `pallet_origins`→`origins_core`. Dual review (user-requested **Codex** + **spec-reviewer**): spec-reviewer 0 blockers / 1 major / 2 minor — the **major** (`origins-core` re-declaring the `MAX_NESTED` kernel constant — the Track-M audit's routed "tunables baked into cores → A4") **fixed in-session** by importing `futarchy_primitives::kernel::MAX_NESTED_LEVELS/_CALLS` (01 §5.2 single-home / SQ-25), which also cleared the naming minor; the nested-wrapper fuzz minor maps to **S2**, the concrete-`RuntimeCall` filter-exhaustiveness test to **B1a**; **SQ-31** (≤16-calls count semantics) logged. Gates green: workspace fmt/clippy/test + `runtime-benchmarks`/`try-runtime` combos + reference-model 21/21 + corpora + doc-table. Codex adversarial pass still running at write time; A4 held **🔨 pending its triage**.

**What comes next:** Triage the Codex findings; if no blocker, flip A4 ✅ and suggest the commit; then the next Track-A pallet (A2)

## A4 (Codex triage → ✅)

Codex adversarial review landed (1 "blocker" + 3 majors + 1 minor) and was triaged with evidence. The **"blocker"** (origin-less base filter denies bare governance leaves) is **not an A4 defect**: `execution-guard-core:538` already applies the origin-aware `SafetyFilter::validate(Some(class_origin))` (I-11), so the two-check split — base filter = signed-extrinsic guard; origin-aware re-check + `EnsureOrigin` at the guard/scheduler dispatch boundary (06 §3.4) — is the intended, guard-core-confirmed design; captured as the B1a dispatch protocol in the `SafetyFilter` docs + **SQ-32** (06 §3.3 prose-vs-code clarification). **Majors 2/3** (scheduler-origin authentication; borrow-don't-clone fail-closed visitor over the concrete `RuntimeCall`) → documented **B1a classifier constraints** on `SafetyClassifier`. **Major 4** (real-`RuntimeCall` `BaseCallFilter` dispatch / producer-path / XCM / exhaustiveness / fuzz) → structurally **B1a / S5 / S2** per the PLAN's decomposition (A1 precedent), except the one cheap gap — nobody row under `proxy_announced`/`as_multi`/`as_multi_threshold_1`/scheduler — **fixed in the suite**. **Minor** → **SQ-31**. Codex's own build/test couldn't run (its sandbox was read-only); local gates authoritative + green (workspace fmt/clippy/test + `runtime-benchmarks`/`try-runtime` + reference-model 21/21 + corpora + doc-table). A4 → **✅** (shim complete, R-6 spec-reviewer clear, deferrals tracked). No commit made (R-9 — awaiting user go-ahead).

**What comes next:** User go-ahead to commit A4 (suggested: `feat(origins): production pallet-origins — custom origins, EnsureOrigin set, SafetyFilter (A4)`); user ruling on SQ-31/SQ-32; then A2. B1a MUST honor the documented `SafetyFilter` dispatch protocol + `SafetyClassifier` constraints

## A2

Implemented `pallet-conditional-ledger` as the repo's second production `#[frame_support::pallet]` (after A1) over the relocated frame-free `conditional-ledger-core`. Adopted A1's exact-pin `=` SDK wiring (frame-support 46.0.1 / frame-system 46.0.0 / frame-benchmarking 46.0.0 / sp-runtime / sp-io / sp-core) and extended it with dev-only `pallet-assets 49.0.1` / `pallet-balances 47.0.0` for the mock's real USDC collateral; added the `DecodeWithMemTracking` derives FRAME storage requires to `futarchy-primitives`/`conditional-ledger-core` SCALE types. lib.rs: Config (03 §3), bounded storage (02 §7.4 / 03 §4 names, `Positions` key `(PositionId,AccountId)`), events (02 §6 frozen subset + gate/Baseline), errors, Signed + Resolve/Settle/Market-authority calls (03 §5), the §5.5 `MarketAuthority` internal `do_*`/`create_*` API, the new §5.4 `sweep_dust` reaping (absent from the core), real USDC custody via a bounded scoped-state adapter over the core (escrow + single-sourced `T::PositionDeposit` moves from state deltas, L-2/L-6), `try_state`, no block hooks. mock over `pallet-assets` ForeignAssets + mock authorities; 28 unit tests (per-extrinsic/error/origin-misuse/cap + §6.3 s=0.70005 and §6.4 split-100→void named vectors + differential-vs-core + review regressions), `#[benchmarks]` for all 23 calls, weights. Per the user's ask, ran two independent reviews: a Codex correctness pass (1 critical/2 high/3 medium/1 low) and a spec-reviewer compliance pass (1 blocker/2 major/7 minor), convergent on the top defects (partial-reap `PositionTotals` removal; deposit dual-source; `DepositFailed`). Fixed all: reap decrements `PositionTotals` per entry + propagates refund failures (G-1 rollback); deposit single-sourced from `T::PositionDeposit`; `transfer` creation-floor at live `T::MinSplit`; checked L-2 + explicit L-6 in try_state; self-transfer dedup; `DepositFailed` error; saturating subs; mock USDC min_balance 10⁴. R-1 doc edit: 03 §5.4 `VaultReaped { pid, residue }` + `BaselineVaultReaped`. A focused spec-reviewer re-verification confirmed all three RESOLVED with no new blocker/major. Full workspace gates (fmt/clippy -D warnings/test), runtime-benchmarks (51) and try-runtime green. Raised six spec questions, renumbered **SQ-33…SQ-38** (originally drafted as SQ-31…SQ-36, but PR #47/A4 claimed SQ-31/SQ-32 for its own questions first — merging main into this branch surfaced the ID collision, so A2's colliding two were renumbered SQ-37/SQ-38 and their cross-references fixed); A2 ✅. **Not claimed:** PT-1…PT-7 ≥10⁶ property suites (owed by S1) and the ledger↔Python differential (reference-model track). Rebased from a stale base (pre-A1/pre-M3-merge) onto post-#46 `main` before opening the PR — cherry-picked the single A2 commit, reconciled the `Cargo.toml` SDK-pin conflict onto A1's `=`-exact style, and re-merged this file's concurrent Track-M/A1 edits by hand; merged post-#47 `main` a second time for the same reason, resolving the SQ-ID collision with A4.

**What comes next:** A5–A11 next per Track-A DoD, then B1a (A3 still needs M2/M3, both reopened 🔨)

## A2 (#48 Codex review triage, autonomous)

Triaged the three `chatgpt-codex-connector` findings on PR #48 (`pallet-conditional-ledger`). **P1 (transactional wrapping) — FALSE POSITIVE, no fix:** Codex claimed FRAME dispatchables aren't rolled back on error unless `#[transactional]`. Not true for this SDK — frame-support 46.0.1's `#[pallet::call]` macro doc (`frame/support/src/lib.rs`) states every extrinsic is wrapped in `with_storage_layer`, so returning any error (incl. a late `T::Collateral::transfer` failure after `persist_*`) rolls the storage back; the pallet's module-doc + `reap_one` comment already rely on this (G-1). Strengthened the module-doc to name the mechanism so it isn't re-flagged. **P2 (transfer remainder sweep at the stale kernel floor) — TRUE, fixed:** 03 §7 R-2 binds the sweep to the LIVE `ledger.min_split` (a META tunable raisable to 1 USDC, 13 §1) but the core sweeps at the compile-time `kernel::MIN_TRANSFER_USDC` (0.01) and the pallet didn't override it, so once governance raises the tunable a Signed transfer could strand a sub-floor dust remainder. Added a pallet-level live-floor pre-sweep (bump `amount → balance` when the remainder would fall in `(0, MinSplit)`) before delegating; the core's K-floor sweep is then a no-op. Protocol senders exempt (exact by construction), matching the core. **P3 (`split_scalar`/`split_gate` skip the live creation floor) — TRUE, fixed:** R-2 "positions cannot be created below it" binds every Signed position-creating op, but only `split`/`split_baseline`/`transfer` enforced the live floor — the scalar/gate splits (which mint new LONG/SHORT/gate entries) delegated straight to the core, which guards no floor at all for them. Added `ensure!(amount >= T::MinSplit::get())` to both Signed extrinsics (the §5.1 rows omit it, but R-2 is the global rule; the `MarketAuthority` `do_*` paths stay exempt). Since `ledger.min_split`'s min IS the kernel floor (13 §1), the pallet check is always ≥ the core's, so no core/reference-model change needed. **Verification:** two regression tests (`scalar_and_gate_splits_enforce_live_min_split`, `transfer_remainder_sweep_uses_live_min_split`) drive the tunable to its 1-USDC ceiling via a now-`pub static MinSplit` mock param; confirmed both FAIL without the fixes (sweep left 0.5-USDC dust; sub-floor scalar split returned `Ok`) and pass with them. `differential_matches_frame_free_core` still green (pallet ≡ core at the default floor). Gates: fmt + clippy `-D warnings` + 30 pallet tests green. A2 stays ✅.

**What comes next:** Push the fix commit to update #48; then A5–A11 per Track-A DoD

## M1 + M2 + M3 (Track M finish)

Finished all three reopened Track M milestones in one PR (`feat/track-m-finish-m1-m2-m3`) on user direction ("Finish M3, M2 and M1 in this order… use codex subagents… create a PR"). **Discovered** the M3 reference-model remediation already sits on post-#47 main (byte-identical to the `fix/m3` branch), so M3 was code-present; **M2** was the real engineering. **M2:** reworked `exp2`/`log2`/`ln` to guarded **Q96** precision (32 guard bits, single final rounding; negative `exp2` via the complement identity — no reciprocal); an independent 10⁶-point Python re-derivation vs the 100-digit oracle measured **0 ulp** worst case (audit's pre-fix `exp2` = 10.11); folded the transcendental corpus into the single 04 §5 generator (≥10³ dense-bit, mean popcount 32, exp2 whole 1–63 + log2/ln top octave + edges), **retired `tools/fixed` + both CSVs**; exact-u256 `48·b` domain compare; `checked_mul` exp2 shift (no silent wrap); f64 (`from/to_f64`) gated behind `std`; imported error/domain bounds from `futarchy-primitives::kernel`; real `cargo build --no-default-features` CI gate. **M3:** ran a fresh spec-reviewer (R-6) and fixed its **blocker** (`percentile` now floors the type-7 interpolation to the 1e9 grid, 05 §4.6/SQ-30), **major** (decision vector rows carry the full effective input set, 04 §5), and per-gate-veto-order minor (`decide()` interleaves per-gate validity→veto, 05 §5.4, + proving vector); deleted the stale independence-inverting SPEC-NOTE. **M1:** moved `Proposal`(21-field, now `MaxEncodedLen`)/`ExecutionRecord`/`MarketSet` + created `MarketKind`/`phase_offsets` in `futarchy-primitives`; collapsed the `wt.quorum` double-copy + ~14 core constant duplications onto the kernel single home (incl. wiring epoch-core's entire phase schedule to `phase_offsets`); added codec-index/golden-field-order/view-size/exact-phase-numerator regression tests. Ran **3 spec-reviewers** (M2 0/0/5, M3 1/1/3, M1 0/1/8) + a **Codex** adversarial pass on the M2 kernel (0 defects) — every actionable finding applied. Clarified 15 §4.4 MPFR wording + re-trued `.claude/rules/reference-model.md` (R-1/R-3). Full workspace gate green (fmt · clippy `-D warnings` · workspace test · no_std build · reference-model 21/21 · vector freshness · doc-table · doc links). Logged SQ-39…SQ-44 (renumbered from a draft SQ-33…SQ-38 to clear the ID collision with A2/#48's ledger SQ-33…SQ-38).

**What comes next:** Commit + open the PR; then Track A FRAME shells (A2 next) / B1a; a future 02/05/08 touch resolves the open SQ-39…SQ-43; the ≥10⁷ MPFR sweep + supply-chain CI legs → B8

## M1/M2/M3 (PR #49 — rebase onto A2/#48 + Codex-comment…

M1/M2/M3 (PR #49 — rebase onto A2/#48 + Codex-comment triage)

Resolved the PR #49 merge conflict and the `chatgpt-codex-connector` review. **Conflict:** A2/#48 (landed on `main` after PR #49 opened) added `DecodeWithMemTracking` (FRAME PoV memory accounting, codec ≥3.7) to every `futarchy-primitives` SCALE type — the same file M1 rewrote. Rebased `feat/track-m-finish-m1-m2-m3` onto post-#48 `main`; took #48's `DecodeWithMemTracking` derives on the pre-existing types **and** kept M1's new types, adding the derive to `MarketKind`/`MarketSet`/`Proposal`/`ExecutionRecord` too (they live in on-chain storage under the A-track shells). PLAN.md: renumbered M-track **SQ-33…SQ-38 → SQ-39…SQ-44** (both branches had independently allocated 33–38; #48's ledger ones are authoritative on `main`), kept both branches' Decision-/Session-log rows. **Codex P2 (`MarketKind` variant names diverge from the contract):** correct — 02 line 5 makes 02 canonical for any name on the contract surface and the frontend decodes `MarketCreated.kind` by TypeInfo variant **name**, so under R-1 the *code* was conformed to 02 §5's frozen `GateS_Adopt`/`GateS_Reject`/`GateC_Adopt`/`GateC_Reject` (`#[allow(non_camel_case_types)]` + a byte-for-byte TypeInfo names-lock test); SCALE indices unchanged, **02 untouched → no contract bump** (resolves SQ-44). Full workspace gate re-run green (fmt · clippy `-D warnings` · workspace test incl. the new names-lock test · no_std build · reference-model 21/21 · vector freshness · doc-table · doc links).

**What comes next:** Force-push the rebased branch; confirm PR #49 mergeable; then Track A FRAME shells / B1a

## A3 (`pallet-market` FRAME pallet — IN PROGRESS, not yet…

A3 (`pallet-market` FRAME pallet — IN PROGRESS, not yet ✅)

User asked to implement A3 "by using codex subagents" and open a PR. On branch `feat/a3-market-frame-pallet`. Read the owning spec (04 all; 02 §2/§5/§7.4; 13 mkt.*/pol.b*; 15 §1 I-12/I-13) and the A2 pallet as the template, then locked the A3 architecture (decided autonomously per the ask): **(1)** live tunables `mkt.fee`/`mkt.obs_interval`/`mkt.kappa` injected via Config `Get`s (rule 4), with `market-core` gaining a `MarketParams` defaulting to today's literals so the reference-model differential stays bit-identical; `MinTrade` stays kernel. **(2)** the pallet *is* the ledger's `MarketAuthority` and drives `pallet-conditional-ledger`'s `do_*` API via a new `LedgerOps` trait shim; the frame-free core stays the differential oracle over in-memory `LedgerState`; atomicity via FRAME `with_storage_layer`; guarded by a shell-vs-core differential test. **(3)** frozen surface: `Markets: map MarketId → MarketBook` + `BaselineMarketOf: map EpochId → MarketId` (02 §7.4), events = the frozen 02 §5 five + an appended `Seeded`. **(4)** origins (06 §3.2): `buy`/`sell`/`crank_observe`/`reap` Signed; `create_market`/`seed`/`close` internal `MarketAdmin`. Wrote a full 6-phase implementation brief and delegated it to a Codex background task (`task-mrmekzm2-b4ln4f`, running in this worktree). At log time Codex is mid-implementation (Phase 1 core refactor started; `crates/market-core/src/lib.rs` modified). **Gates NOT yet run by the orchestrator; A3 stays 🔨.** New spec observation to log as an SQ next: 02 §7.4 names the `Markets` value type `MarketState` but 02 §4 defines no such struct and 13 §226 flags its size `[VERIFY]`, so the pallet stores the core `MarketBook` (value shape unfrozen) — cosmetic name queued for the B2 02-amendment batch (parallels SQ-37).

**What comes next:** Monitor the Codex task to completion; run the full gate suite (`rust-workspace-gates.sh` + `pallet-market` tests + `runtime-benchmarks` + `try-runtime` build + `market-core` parity); spec-reviewer + adversarial Codex review; fix findings; then flip A3 ✅, update Current focus, commit, open the PR

## A3 (`pallet-market` — completed ✅)

Finished A3 per "implement by using codex subagents + open a PR". A **Codex background task** wrote the whole production pallet + `market-core` refactor to a 6-phase brief; the orchestrator then ran the gates and a **dual review** (adversarial `codex exec` + the `spec-reviewer` agent) + a **`test-engineer`** hardening pass, triaged every finding, fixed the real ones, and re-gated. **Two blockers fixed, each with a regression test:** (1) `create_market` created a ledger vault *per book*, but a proposal's ≤ 6 books share ONE vault — it failed on the 2nd book of every proposal (even a bare PARAM decision pair) → idempotent `create_vault` guarded on `Vaults::contains_key`; (2) **Baseline sells charged the seller** — `sell_baseline`'s `do_split_baseline(who, net)` made the seller *fund their own payout*, debiting them ~net USDC against the real ledger (the in-memory oracle models no custody and hid it; the shell≡core differential can't see USDC attribution) → the **book** now funds the net payout set (`do_split_baseline(book, net)` + transfer both legs to the seller). Confirmed red-then-green (seller was overcharged 511 USDC pre-fix). **Majors fixed:** `crank_observe` phase-guarded (no post-Close TWAP pollution, 04 §2); `Markets` → `CountedStorageMap` with the 196-cap enforced at dispatch (I-21, `TooManyMarkets`); `seed` idempotency (`SeededMarkets`/`AlreadySeeded`); `try_state` strengthened to real I-12 structural collateralization (book↔ledger-vault existence; the escrow≥obligations solvency identity stays the ledger's L-1/L-2) + I-13 accumulator sanity; `STALE_GAP_BLOCKS` single-homed to `futarchy-primitives::kernel::MKT_STALE_GAP_BLOCKS` (13 §3.2). **Deferred by design** (documented, both reviewers concurred): **SQ-64** (`MarketCreated.epoch`/spec → A8/B1a wiring), **SQ-65** (POL one-split vs per-book, 04 §10 vs 13 §5.5 — code matches 13), **SQ-66** (reap anchor + `BaselineMarketOf` prune, 04 §8.3 vs 02 §7.4 → A8), **SQ-67** (`Markets` value type name); TWAP 8-checkpoint ring + window staleness + `last_observed_block` init → A8; deterministic book/fee sub-accounts + the `Params`-vs-`#[pallet::constant]` binding → B1a (consistent with A2's `MinSplit`). Core refactor (`LedgerOps` + `MarketParams` + book-level `pub fn`s) is behavior-identical at defaults (core parity 10/10, differential green). Gates all green: workspace fmt · clippy `-D warnings` · test · `runtime-benchmarks` (34) · `try-runtime` build · reference-model 21/21 · doc-table · doc links. 26 pallet tests. A3 ✅.

**What comes next:** Commit + open the PR; then A5–A11 per Track-A DoD, then B1a (wires `create_market`'s epoch/spec params, the `Params` bindings, deterministic book/fee sub-accounts; honors the reap/TWAP-window items A8 owns). PT-style I-12 loss-bound property suite + ledger↔Python differential → **S1**

## A3 (PR #53 — merge main + Codex-bot P1 fixes)

Rebased/merged post-#52 `main` (A5/#52 contract-v3, A9/#51, A10/#50 had landed) into the A3 branch — only PLAN.md conflicted (Current focus + Spec questions + Session log); `futarchy-primitives` auto-merged (my `MKT_STALE_GAP_BLOCKS` + main's contract v3 both applied). Renumbered the A3 spec questions **SQ-45…48 → SQ-64…67** to clear the collision with main's A5/A9/A10 SQ-45…63. **Fixed both `chatgpt-codex-connector` P1 comments on PR #53:** (P1a — `market-core::seed_book` Baseline) the Baseline seed split with the **book** account as payer, but the FRAME ledger charges that account's USDC and the book is not the POL_BASELINE treasury, so production seeding would fail (book unfunded) or drain the wrong account; now it splits from **`treasury`** and transfers both Baseline legs to the book, mirroring the decision/gate paths (positions identical → the shell≡core differential still holds). (P1b — `pallet-market::seed`/`create_market`) the internal (non-`#[pallet::call]`) admin paths get no FRAME storage layer, so a mid-sequence ledger `Err` after a scoped-adapter persist could strand partial state (and `seed` exited before writing `SeededMarkets`, so a retry could double-seed); both now run their ledger sequence inside `frame_support::storage::with_storage_layer`, rolling back atomically on any partial failure regardless of caller. Regression tests added (baseline seed funded from treasury via book/treasury USDC deltas; a forced-fail seed leaves no `SeededMarkets`/partial ledger state). Gates green (workspace fmt/clippy `-D warnings`/test · `runtime-benchmarks` · `try-runtime` · reference-model 21/21 · doc links). A3 stays ✅.

**What comes next:** Force-push #53; then A6–A8/A11 per Track-A DoD, then B1a

## A10 (#50 Codex review triage)

The `chatgpt-codex-connector` bot flagged one **P1** on PR #50: `attestor_core::has_quorum` gated CODE/META only on the 2-of-N attestation count, not on the 06 §7 "≥ 3 members required" **active-registry floor** — so after an ejection drops the active membership to 2, two attestations from the surviving pair still passed quorum (I-19). **TRUE, fixed:** `has_quorum` now returns `false` when `active_member_count() < MIN_MEMBERS` (new pub helper) before the quorum tally; regression test `quorum_fails_when_active_registry_drops_below_minimum` ejects one of three attestors, then two closed-window attestations from the remaining active pair are correctly rejected. Gates: fmt + clippy `-D warnings` + attestor-core 7 / pallet-attestor 17 + full `cargo test --workspace` green. A10 stays ✅.

**What comes next:** Merge #50; then A5–A9/A11 per Track-A DoD, then B1a

## A9 (`pallet-futarchy-treasury`, FRAME pallet + ✅, PR #51)

Implemented A9 as a production `#[frame_support::pallet]` over `futarchy-treasury-core`, using a Codex-plugin subagent (`codex:codex-rescue`) for the adversarial review alongside the native `spec-reviewer` (user asked to use Codex subagents). **Architecture:** a single bounded `StorageValue<TreasuryState>` aggregate (a `BoundedVec` mirror of the core `Treasury`; the hot NAV path reads the whole aggregate so decomposition buys nothing — B5 may split for PoV), a thin `load → core-op → persist` adapter so shell ≡ core is near-tautological; a `T::AccountId: From/Into<[u8;32]>` Config bound bridges the core's `[u8;32]` accounts (mock uses `AccountId32`). **Rule 4 (first Params *consumer*):** the treasury caps read from `pallet-constitution::Params` via a `TreasuryParams` provider and threaded into behavior-preserving core seam fields (defaults = 13 defaults, differential holds); a mock param-flip test proves the pallet reads Params, not a hardcode. **8 extrinsics** (one `FutarchyTreasury` `EnsureOrigin` for outflows + Signed-permissionless `claim_stream`/`execute_coretime_renewal`, renewal freeze-exempt D-9) + internal APIs. Genesis fixes the 1B VIT supply; try-state = core validator + I-7 issuance envelope. 24-test suite + 600-step differential + `#[benchmarks]`×8. **Spec edits (R-1):** 13 §4 treasury bounds; 08 §1.4 signatures; `VIT_TOTAL_SUPPLY` → `futarchy-primitives`. **Triple review — 0 blockers.** Fixed all 4 spec-reviewer majors (stream reaping → concurrent; NAV open-stream double-count → escrow nets once; POL/queued entry points; try_state envelope) + all Codex findings (zero-price quote; genesis supply fixed 1B; `fund_budget_line` near-`u128::MAX` atomicity; quote pruning). The differential + Codex forced real G-1 atomicity fixes in the core. SQ-47…SQ-57 logged. **Gates green** (fmt · clippy · workspace core 9 + pallet 24; 32 with benchmarks · no_std · try-runtime · corpus/reference-model/vectors · doc-links). Rebased onto post-#48 → post-#49 → post-#50 main as the Track-A/M burst landed (treasury pallet/core untouched by #46–#50; only PLAN.md hard-conflicted; SQs renumbered to clear each audit, finally 47–57). **A9 ✅.**

**What comes next:** Push A9 PR; then A5–A8, A11, then B1a

## A9 (PR #51 Codex-bot review round)

Addressed both `chatgpt-codex-connector` P2 comments on PR #51. **(1) Rolling issuance meter (resolves SQ-50 + SQ-54):** replaced the fixed-window `IssuanceMeter` (which reset `minted` at the 365-day boundary, letting a governance-timed `issue_vit` mint ≈2× `iss.inflation_cap` across the seam) with a bucketed **trailing 365-day `RollingMeter<366>`** — the same primitive as the 30d/180d outflow meters. `issue_vit` rolls a COPY (G-1 atomic), caps against `bps(vit_supply − trailing_minted, limit_bps)` (supply at window start) and commits on success; the trailing sum is never reset, so seam-straddling mints are summed and cannot exceed the cap. `try_state` now checks the trailing sum against the **fixed kernel ceiling** `ISS_INFLATION_CAP_BPS` (2%, down-only) rather than the live cap, so a mid-window down-amendment can't trigger a release-blocking failure (SQ-54). Pallet `load()` refreshes `issuance.limit_bps` from Params like the other meters; core + shell tests assert the seam is CLOSED. **(2) Loud `NavFloorUnmet` (bot #1):** the old `ensure_nav_floor` deposited the loud event AND returned `Err`, so a caller propagating the error rolled the event back (breaking the 08 §4.2 loud gate). Split into a **hard** `ensure_nav_floor` (returns `Err`, no event) + a **durable-loud** `flag_nav_floor(class) -> bool` that deposits `NavFloorUnmet` and returns below-floor — A8's arming crank calls the latter on its `Ok`-returning path (08 §4.4 "reject as deferred") so the event survives. Gates all green; the 600-step differential still holds (pallet ≡ core with the rolling meter). A9 stays ✅.

**What comes next:** Force-push to update #51; then A5–A8, A11, then B1a

## A5 (`pallet-oracle` FRAME shell + dual review + Codex…

A5 (`pallet-oracle` FRAME shell + dual review + Codex PR-review P1)

Built `pallets/oracle` as a production `#[frame_support::pallet]` over `oracle-core` (shell-over-core: hydrate whole `Oracle` aggregate → core state-transition → persist diff → deposit events). 02 §7.2 storage (Reporters/Watchtowers `CountedStorageMap`; Rounds/ComponentValues `StorageNMap` on the `(component,epoch,version)` triple; ReserveHealth; internal AckRecords/Recomputable/WatchtowerActive), 21 frozen events 1:1, 10 §13 extrinsics (`adjudicate` = OracleResolution via `EnsureOrigin`, rest Signed), a `ReportingContext` provider for `report`'s window/version/StakeAtRisk **plus the new `expected_components`**, no-XCM reserve probe (I-24), try-state (physical NMap key == embedded triple), genesis, mock (AccountId32), v2 benchmarks ×10, `Config::MaxRoundCloseBatch`; implemented the 07 §4 watchtower liveness discipline so all 21 events are reachable. **Dual review (Codex adversarial ×24 + `spec-reviewer` ×13):** all in-scope blockers/majors fixed with tests — F8 per-version ack corruption, F15 off-grid values, F2/F3 reserve fail-static, F7 per-version report provider, F24 half-open windows, F5 liveness streak, F19 reporter slash, F14 deterministic carry, F1 try-state key, F10 `adjudicate` terminal-gating, F11/F12 money deadline, F17 ejected-reporter no-op. SQ-59/60/61/62 resolved by user-delegated R-1 amendments (07/13; 02 untouched). **This session:** rebased onto post-#51 `main` (A2/A4/A9/A10/M-track landed) — twice, as `main` advanced mid-work (#50→#51); resolved the `oracle-core` constant-block conflict by adopting main's `kernel::WT_MAX/WT_QUORUM/…` single-home while keeping SQ-59's `MAX_ROUNDS`/`MAX_COMPONENT_VALUES = 128`; took main's PLAN.md and re-applied the A5 deltas with **SQ 39–43 → 58–63 renumbered** (A9/A10/M-track had claimed 39–57). **Fixed the Codex PR-review P1** (`force_neutralize_expired` left no-report components with no `ComponentValues` entry, breaking 07 §11(1)'s "the oracle guarantees the entry exists by the deadline"): added the `(component, version)` expected-set param + a round-free `neutral_no_report` core path + the `ReportingContext::expected_components` seam; amended 07 §11(1) (SQ-63); +1 core test (`force_neutralize_expired_neutralizes_no_report_components`) +1 pallet test (`note_settle_deadline_neutralizes_no_report_components`). Gates green: fmt · clippy `-D warnings` · workspace test · pallet-oracle 72 (+ `--all-features` = 82) · oracle-core 16 · try-runtime · no_std · reference-model 21 · doc links. **A5 stays 🔨** — the one open blocker is SQ-58 (the 02 §7.2 contract reconciliation, B2).

**What comes next:** Force-push #52 + reply to the Codex comment. B2: reconcile 02 §7.2 (triple key + full `RoundState`/`ReserveHealth` shapes) + bump `INTEGRATION_CONTRACT_VERSION` → A5 ✅. B1a: wire the oracle seams (F4/F6/F18/F23/SafetyFilter + `note_epoch_boundary`/`note_settle_deadline`/`reserve_probe_result`/`note_recomputable`/`expected_components` triggers). Then A3/A6–A8/A11

## A5 — contract reconciliation → **A5 ✅**

User flagged the circular dependency (A5's "defer to B2", but B2 → B1a → A1–A11) — deferral could never let A5 finish — and directed me to implement whatever's needed to complete A5 ("You decide on your own for the optimal choice"). **Amended the frozen contract 02 (v2 → v3) under the user's joint backend+frontend sign-off (R-1):** 02 §7.2 `Rounds`/`ComponentValues` → the per-version **triple key** `(MetricId, EpochId, MetricSpecVersion)` + full `RoundState`/`ReserveHealth` shapes; 02 §4 `OracleRoundView` gains `spec_version`; 02 §13 version history; 07 §13 synced; 00 D-2 records the bump; `INTEGRATION_CONTRACT_VERSION` 2 → 3 in `futarchy-primitives` (+ the constitution/primitives version tests). The v2 pair key was self-contradictory (its own bound note described per-version `RoundState`s a one-value-per-key map cannot hold) → an R-1 **correction**, pre-genesis (no deployed runtime, so no migration; B2/Track-F conform to the corrected contract). Added a `round_state_fields_match_contract_02_section_7_2` drift-lock test so it can't re-diverge. Resolves **SQ-58** and the **oracle portion of SQ-2** (its intake/phase-offset/attestor items ride a later bump). Gates green: fmt · clippy `-D warnings` · workspace test · oracle-core 17 · pallet-oracle 72 (+`--all-features` 82) · try-runtime · no_std · reference-model 21 · doc links; `spec-reviewer` re-checked the reconciliation (no blockers). **A5 → ✅.**

**What comes next:** Force-push #52. Then A3 (needs M2/M3), A6/A7/A8/A11 by the same shell-over-core pattern; then B1a — which now consumes a settled contract-v3 oracle surface

## A6 (`pallet-registry`, FRAME pallet + dual review → ✅,…

A6 (`pallet-registry`, FRAME pallet + dual review → ✅, PR #54)

Implemented A6 as a production **instantiable** `#[frame_support::pallet]<T, I>` over `registry-core` (user: "Implement A6 by using codex subagents … open a PR"): I authored the fidelity-critical code + core refactor; a Codex `codex:codex-rescue` adversarial review ran alongside the native `spec-reviewer`. Two instances (Incident=default `()`, Milestone=`Instance1` in the mock) prove "one pallet, two instances" (07 §7). Combines the ledger's `fungibles`-bond custody + scoped `load→core→persist→drain` adapter with the guardian's injected seams; **real USDC bond escrow/refund/40-60-slash** single-sourced from the core (R-7); 5 B1a-wired seams keep A6 independent of A5/A7/A8. Self-caught + fixed a vacuous `SpecVersionMismatch` (`file` took a caller `expected_spec` → now the trusted `Config::Epoch` seam). **Dual review** (spec-reviewer 1 blocker/4 major/5 minor + Codex 2 high/6 med/1 info) — both cleared the custody math, rule-4 wiring and scoped persist diffing; **every real finding fixed with regression tests**, re-confirmed by a focused re-pass (0 residual blockers): dropped the two oracle-namespace events (blocker — no contract bump); reap→re-close replay (archive-delay gate + `FilingCount`-present close); milestone aggregate >1.0 (target seam + clamp); persist O(epoch)→O(changed); inclusive-window race; ack cap; fallible welfare sink (G-1); custody-solvency `try_state`. Core refactor: `reg.bond_*`+milestone-target → `Params`/MetricSpec seams, `kernel::ORC_WINDOW_BLOCKS` single-home, exact insurance complement. Deferred B-track: permissionless `recompute_proof` (07 §9), SafetyFilter matrix rows (06 §3). SQ-58…SQ-66 logged, gates green, PR #54 opened.

**What comes next:** Rebase onto post-#52/#53 main; address the Codex-bot review; then A7/A8/A11, then B1a

## A6 (PR #54 — rebase onto post-#52/#53 main + Codex-bot…

A6 (PR #54 — rebase onto post-#52/#53 main + Codex-bot P2)

Rebased `feat/a6-registry-frame-pallet` onto post-#52 (A5/oracle + contract **v3**) / post-#53 (A3/market) `main`. `futarchy-primitives` auto-merged (my `kernel::ORC_WINDOW_BLOCKS` + main's contract-v3 both applied, no duplicate); only PLAN.md conflicted (Current focus + milestone table + Spec questions + Session log). **Renumbered the A6 spec questions SQ-58…SQ-66 → SQ-68…SQ-76** to clear the collision with main's A5 (58–63) and A3 (64–67); A5/#52's 02 §7.2 reconciliation independently **confirms** the A6 decision that `WindowAcknowledged`/`WindowExtended` are oracle (§7.2) events, so the registry correctly emits neither. **Fixed the `chatgpt-codex-connector` P2 on PR #54:** `ack_observed` on an already-`Challenged` filing returned `Ok` and inserted an `AckRecord` **without** the `WT_QUORUM` cap (the cap was only on the `Filed` path), so >`WT_QUORUM` watchtowers acking a challenged filing could breach the per-filing ack bound that `do_try_state` and `reap_epoch`'s bounded `clear_prefix` assume (try-runtime failure + stale keys after reap). **TRUE, fixed:** the core's `ack_observed` now **rejects** a challenged filing (07 §4 — "a posted challenge supersedes the quorum requirement", so acknowledgments are moot once challenged); regression test `ack_on_a_challenged_filing_is_rejected`. Gates re-run green (fmt · clippy `-D warnings` · workspace test — registry 28 / 35 w/ benches · runtime-benchmarks · try-runtime · no_std · reference-model 21 + vectors + doc-table · doc-links). A6 stays ✅.

**What comes next:** Force-push to update #54; then A7/A8/A11 per Track-A DoD, then B1a

## A7 (`pallet-welfare`, FRAME pallet + ✅)
span: 2026-07-15–16

Implemented A7: production `#[frame_support::pallet]` over `welfare-core`, orchestrated with **Codex subagents** (per the user's "use codex subagents" direction): a Codex authoring pass, a dual review (native `spec-reviewer` + a Codex `codex exec` adversarial pass), a Codex fix round, and a Codex regression pass — against a detailed spec-first brief. **Design (per 05 §4/§6/§7, 02 §4/§7.4, 13 §1/§4, 06 §3.2):** three FE-named bounded storage items `Snapshots`/`MetricSpecs`/`GateBreachFlags` (02 §7.4); rule-4 Params consumer — `welfare.thS/thC lo/hi` + `wP/wA` read from `pallet-constitution::Params` and threaded into `welfare-core` via a behavior-preserving `WelfareParams` (defaults = 13 defaults so shell ≡ core holds; the SQ-34 pattern); calls `register_spec` (ConstitutionalValues/metric track, 06 §3.2), `record_snapshot`/`record_daily_gate` (Signed keeper cranks, components from an injected `MetricInputs` seam); `compute_settlement` as the internal SettleAuthority endpoint (05 §6 one-path) dispatching `settle_scalar`/`settle_gate`/`settle_baseline` via an injected ledger seam; try_state + genesis + `#[benchmarks]` + mock + tests (incl. shell-vs-core differential + param-flip). **Coupling guard:** `welfare-core`'s `compute_settlement`/`gate_breach`/`new` + public types kept signature-stable (epoch-core/A8 depends on them); the `WelfareParams` threading is additive. **Storage finalized as three 02 §7.4 `StorageMap`s** (`GateBreachFlags: map EpochId→flags`, 05 §4.7; scoped `load`(sorted)→core→`checked` `persist` adapter). **`compute_settlement`** is `with_storage_layer`-atomic + class-gated (`has_gate_books`); **`prune`** adds the rolling-20 reap. **Reviews found a blocker + 6 majors** (storage-shape→maps, no-pruning→reap, MetricSpec validation gaps `epsilon==0.01K`/pillar↔source, settlement atomicity, inactive-spec acceptance, versionless seam) **+ a regression minor** (activation-lead `saturating_add`→`checked_add`) — **all fixed in-session with regression tests; final Codex review 0 blocker/0 major**. Behavior-preservation + no-XCM + epoch-core signature-stability confirmed. **R-1 doc edit:** 06 §3.2 gained the `welfare.record_snapshot/record_daily_gate` keeper-crank rows (Decision log; resolves the rule-6 gap). SQ-77…80 logged. **Gates green:** fmt·clippy -D warnings·`cargo test --workspace` (363; welfare 29 pallet+17 core; 32 w/ benchmarks)·try-runtime·no_std·reference-model 21/21·vectors·doc-table·doc-links. **A7 ✅ 2026-07-16.**

**What comes next:** Commit + open the A7 PR; then A8/A11 per Track-A DoD, then B1a (wires the welfare MetricInputs/Ledger seams + epoch-driven snapshot/prune/settlement triggers + the I-16 cohort→spec_version binding)
