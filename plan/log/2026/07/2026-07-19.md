# Session log — 2026-07-19

## G0+S4 (SQ-271 — gate-attacking re-calibration DONE:…

G0+S4 (SQ-271 — gate-attacking re-calibration DONE: META gate defense breaks sub-3P)

The gate-attacking `--full` re-calibration completed (76 min, 10,000 proposals). **Decisive result — gates are NOT a universal fix:** under a faithful gate-suppressing attacker, **META regressed 0.000 % → 1.19 %** (10/837; wrong-PASS flips at ~0.82–0.90×3P, sub-3P) while **CODE (0.000 %) and TREASURY > 1 % (0.000 %) held even under attack**. The class-differentiated outcome (only the highest-prize class breaks) confirms the model is realistic, not a strawman: META's large prize (≤ `trs.cap_proposal`·NAV for upgrade payloads) funds both gate suppression and the welfare hurdle within 3P. Implications: (a) the CODE/META 0.000 % that motivated the gate-book direction was partly an artifact of the unattacked control — META's gates do not actually resist; (b) gates for TREASURY ≤ 1 % would likely still work (small prizes, like TREASURY > 1 % which holds), but PARAM gates (already marginal) and META's existing gates are not the answer; (c) the **at-risk depth-measure fix** (exclude balanced settlement-riskless inventory — the belief-capture finding) is now the root-fix candidate across ALL classes, closing the common self-certification seam gates cannot. Regenerated artifact committed (`--check` structure green, 24-row subsample byte-exact); violations now name three classes (PARAM 3.81 %/TREASURY 1.64 %/META 1.19 %). G0/S4 stay 🔨.

**What comes next:** **User decision on mechanism direction:** (A) at-risk depth-measure as the root fix for all classes (recommended — addresses META + PARAM + the seam) vs (B) gates for TREASURY ≤ 1 % + separate META hardening + a PARAM measure. Then implement → re-calibrate → publish (V-12) → flip ✅

## G0+S4 (SQ-271 — META break VERIFIED as unprofitable…

G0+S4 (SQ-271 — META break VERIFIED as unprofitable margin-erosion, not exploitation)

Per the user's *verify META break first* ruling, computed the profitability of every causal wrong-PASS flip in the gate-attacking artifact (realized attacker loss ÷ InCapPrize at the flip budget). **Result reframes the problem favorably:** META's 26 flips are ALL unprofitable (cost/prize 1.82–2.17×) and CODE's 18 are too (1.55–2.17×) — gate suppression is only ~14 % of the attacker budget (the hurdle takes the rest), so gates convert attacks on gated classes into money-losing griefing. META's 1.19 % is unprofitable griefing tripping the raw adoption count, NOT a profitable exploit — **the gate mechanism is working, not broken.** The genuinely PROFITABLE exploits are on the UNGATED classes: PARAM (16 profitable flips incl. near-free belief-capture, cost/prize→0.00) and TREASURY≤1 % (4, min 0.09×). Conclusion: gates DO deter profitable capture (validated by META/CODE = 0 profitable), so extending them to TREASURY≤1 % and PARAM is re-validated as the mechanism; the at-risk depth-measure remains the candidate for PARAM's near-free belief-capture specifically. Open spec question: should the 15 §4.9 exit criterion count unprofitable griefing or measure the profitable-exploit rate (D-4 deters MEV/profit; TM-18 deep-pocketed griefers are an accepted residual)? G0/S4 stay 🔨.

**What comes next:** User decision: (i) exit-criterion interpretation — profitable-exploit rate vs raw adoption count (SQ to log); (ii) then gate TREASURY≤1 %+PARAM (deters profit) ± the at-risk measure for belief-capture; implement → re-calibrate → publish → flip ✅

## G0+S4 (SQ-271 R1 at-risk measure — implemented,…

G0+S4 (SQ-271 R1 at-risk measure — implemented, re-calibrated, REVERTED as a dead end)

R1 (at-risk contest capital, commit `94c91d2`) passed its seeded go/no-go probe (all six profitable PARAM belief-capture flips gone) and every fast gate — but the **full 10,000-proposal re-calibration exposed it as a false victory: 0 % adoption across all classes, 100 % false-reject, 0 % decision-grade formation.** Root cause (verified in `engine.py` organic formation + the `v_min=2P` identity): the sim's organic depth is largely balanced maker-held inventory, and the `v_min` floors are calibrated on the GROSS measure — the at-risk measure changes the depth scale, so honest books can no longer reach `v_min`. Salvaging it needs a whole-scale depth re-calibration dependent on the A-2 real directional-vs-balanced split (unmeasurable pre-Phase-3). **Per user ruling, reverted `94c91d2`** (kept the sound MIN-fix `20ef665` + gate-attack sim `600575e`; artifact restored to the gate-attacking `c26b025`); reverted state re-verified green (reference 37→OK, market-core 18, sim 48, vectors fresh, fmt). Lesson: the seeded probe verified attacks-fail but not honest-forms; a valid depth-measure change must check BOTH. **Pivot to R2 = gates** (validated, no depth re-scaling). G0/S4 stay 🔨.

**What comes next:** R2: extend Survival/Security gate books to TREASURY≤1 % (the ready 36-step plan, contract v4→v5) and PARAM (S/C breach-fact gate); re-calibrate under the gate-attacking sim; profitable-exploit rate < 1 % per class → publish (V-12) → flip ✅

## G0+S4 (R2a — universal TREASURY gate + contract v5…

G0+S4 (R2a — universal TREASURY gate + contract v5 landed, verified, committed)

Codex round (25 min, completed clean): removed the ">1 % NAV" gate condition so ALL TREASURY proposals carry the four-book `(S,C)×(adopt,reject)` gate (reusing the frozen `MarketSet.gates` array), one 7,393,600-USDC floor, `INTEGRATION_CONTRACT_VERSION` 4→5 (02 §4/§8/§13 semantics; user joint sign-off under R-1). 34-file changeset across spec 00–05/08/13/14 + design pack, reference model + regenerated vectors + differential consumers, epoch-core/pallet-epoch/runtime, surface-manifest + release tests, sim. Orchestrator independently verified the load-bearing parts: contract version = 5 consistent (primitives + surface-manifest LE `0x05000000`), the `low_ask_treasury_seed_plan_opens_six_proposal_books` acceptance test passes (≤1 % NAV Treasury opens 6 books, both `GateVetoSurvival`/`GateVetoSecurity` reachable in assembled-runtime tests), reference 37, vectors fresh, doc-table, limit-coverage 178, doc-links, plan-tables, fmt — all green; agent's own run: runtime 224/pallet-epoch 84/pallet-market 55/sim 49, clippy `-D warnings`, 0/0/0 compliance. `MaxLiveMarkets=196` and SQ-272 MIN preserved. Committed. G0/S4 stay 🔨.

**What comes next:** R2b: gate PARAM (S/C breach-fact, NAV floor 1.85M→4.62M); then the gate-attacking `--full` re-calibration — profitable-exploit rate < 1 % per class ⇒ publish (V-12) → flip S4+G0 ✅

## G0+S4 (R2b — PARAM gate landed, verified, committed;…

G0+S4 (R2b — PARAM gate landed, verified, committed; every market-bearing class now gated)

Codex round (34 min, completed clean): added the four-book gate to PARAM (predicate now `PARAM|TREASURY|CODE|META`), settling on the shared deterministic `GateBreachFlags` S/C facts; PARAM 2→6 books, floor 1.85M→4.62M, folded into pre-genesis contract v5 (no v6 — frozen gate/floor arrays already represent PARAM). 28-file changeset. **Explicitly heeded the R1 lesson** — verified honest PARAM still adopts (sim organic-depth PARAM: 4 valid gates, welfare Ok, both books above v_min, Adopt; epoch-core `param_hurdle_adopts_at_spec_delta`; pallet `decide_adopts_and_only_then_enqueues`), not just that attacks fail. Orchestrator independently verified: honest-adopts tests, `param_seeds_four_gate_books` (both vetoes reachable), contract still v5, reference 38, vectors fresh, doc-table, limit-coverage 178, doc-links, plan-tables, fmt — green; agent run: runtime 227/pallet-epoch 85/pallet-market 55/sim 50, clippy, treasury 44, benchmarks. **Honest caveats (R-10):** the S/C-breach proxy's correlation with real PARAM harm and the attackable gate books remain an economic HYPOTHESIS — only the gate-attacking re-calibration confirms PARAM exploit closure; and SQ-173 (PARAM's unbacked step-9 prize proxy) gates production PARAM adoption. Committed. G0/S4 stay 🔨.

**What comes next:** R3 (decisive): gate-attacking `--full` re-calibration (~76 min); check per-class profitable-exploit rate < 1 % AND honest adoption preserved (both, per the R1 lesson) — green ⇒ export `bleavit.sim-calibration.v1`, publish V-12, full exit gate, flip S4+G0 ✅

## G0+S4 (R3 gate-attacking re-calibration — SECURITY GOAL…

G0+S4 (R3 gate-attacking re-calibration — SECURITY GOAL MET (0 profitable everywhere); PARAM gate over-rejects, META griefing residual)

Full 10,000-proposal re-calibration under R2a+R2b gates + the gate-attacking adversary (76 min). **Decisive, mixed result:** **0 profitable exploits in ALL FOUR classes** (the D-4 objective — deter profitable capture — is met system-wide), and PARAM/TREASURY decidable-harm false-pass fell **3.81 %→0 / 1.64 %→0**. **TREASURY gate fully validated:** 0 false-pass, 0 profitable, honest TREASURY adopts (25; 700 Survival + 107 Security vetoes fire). Two residuals: **(1) PARAM gate over-rejects** — its gate books get adequate contest (~15k > the 10k `gate.v_min` floor) but systematically fail validity (TWAP ~0.18, `valid=False`), so 100 % of PARAM rejects `NotDecisionGrade` with ZERO gate vetoes and 0 adoption (a PARAM-specific gate-FORMATION issue in the sim, not a security failure — TREASURY's identical machinery forms fine; needs a formation fix + re-measure). **(2) META 1.19 % raw false-pass** but **0 profitable** — the same unprofitable griefing (attacker loses ~2× prize), i.e. the open exit-criterion question (D-4/TM-18: griefing is an accepted residual). Artifact committed (honest evidence; `candidate-only`, violations name meta + sub-3P-brackets). G0/S4 stay 🔨.

**What comes next:** (a) fix PARAM gate-book formation so honest PARAM adopts while keeping its 0-profitable security benefit, re-measure; (b) resolve the META griefing exit-criterion (profitable-exploit rate vs raw adoption — log SQ); then export → publish (V-12) → flip. TREASURY gate is done.

## G0+S4 (PARAM gate over-rejection root-caused — real…

G0+S4 (PARAM gate over-rejection root-caused — real b-vs-flow convergence constraint, not a bug)

Per the user's *investigate the overshoot first* ruling, traced why honest PARAM gate books fail the `|spot−TWAP| ≤ 0.05` validity check (they sit at TWAP ~0.18, `valid=False`). **Disproven:** the informed-arrival-ramp hypothesis — truth-anchoring `informed_target` (0.5→truth) changed nothing (identical 0.18). **Also cleared:** the balanced holder is genuinely price-neutral (`execute_hold` buys equal LONG+SHORT ⇒ quote depends only on q_L−q_S). **Real cause:** a gate book opens at the neutral 0.5 but its no-breach truth is ~0.02; moving a `b=7,500` LMSR book from 0.5→0.02 needs ≈ `b·(logit0.5−logit0.02) ≈ 29,000` of directional flow, but PARAM's thin gates get only ~15–19k (`gate.v_min.param=10k`), so they stall mid-transition (~0.18) and fail convergence. TREASURY's thicker gates (~32k, `gate.v_min=25k`) just clear it — hence TREASURY validates, PARAM doesn't. So the fix is a genuine gate re-parametrization with trade-offs (lower `pol.b_gate` for gate books ⇒ easier to converge but thinner/easier to attack; or higher gate-flow floor; or open gate books nearer their expected truth; or relax the gate convergence bound) — each needs re-calibration to confirm it restores PARAM liveness WITHOUT reopening a profitable exploit. NOT a one-line bug fix; a mechanism-parameter decision. TREASURY/CODE/META remain done (0 profitable, adopt); PARAM is the scoped residual.

**What comes next:** User decision: pursue a PARAM gate re-parametrization (which lever, given the convergence-vs-attack-resistance trade-off) + re-calibrate, OR ship the TREASURY/CODE/META win and treat PARAM as a Phase-gated residual (behind SQ-173's unbacked-prize block anyway).

## G0+S4 (PARAM gate over-rejection RESOLVED — faithful…

G0+S4 (PARAM gate over-rejection RESOLVED — faithful gate-arbitrage sim fix; runtime mechanism unchanged; calibration pending)

**Refined the prior row's root cause, then fixed it as a pure sim-faithfulness change.** Confirmed the neutral-0.5 open is real in the runtime too (`market-core::open` "canonical neutral quote 04 §2"; POL seeding never touches q_long/q_short), and the full-window TWAP integrates the 0.5→truth descent. But a probe holding the CURRENT 0.5-open mechanism showed that under fast, adequate arbitrage healthy gates clear (safe 23/30) while harmful still veto — so the false-veto is substantially a **sim modeling artifact**: the sim drove gate formation with the same slow information-diffusion arrival ramp + thin flow as the nuanced decision books, when a gate's binary conditional-breach fact is instead readily arbitraged. Per the user's **minimal-fix-first** ruling (fix the sim faithfully; escalate to a 04 §2 open-prior mechanism change only if a faithful sim still over-vetoes), the fix is `simulation/…/engine.py` (+ `test_engine.py`) ONLY: gate formation gains a corrective-arbitrage leg using the SAME `(b·ln2+contest)/2·window·elasticity` model the attacker must overcome in `_apply_gate_attack`, run AFTER the noise leg each block so a standing arbitrageur settles the spot to the true breach probability alongside the TWAP (the informed leg also targets truth directly for gates). At the UNCHANGED `pol.b_gate=7,500`: safe 40/40 no-veto + 40/40 valid, harmful 38/40 veto + 40/40 valid — a clean discriminator, not the always-veto artifact. **No spec/parameter/invariant change**: an earlier "need `pol.b_gate`→3,500" was itself an artifact of a first-draft arbitrage-before-noise ordering that left the spot unsettled; corrected, the original depth is best for validity ⇒ no 08/13 NAV-floor cascade, no vector regen, no runtime change (GATE_B, contract v5, SQ-272 MIN untouched). Two engine tests had asserted the drag bug (vetoing genuinely-SAFE seeds); updated to genuinely breach-elevated seeds. All 50 sim tests green; vectors `--check` clean. SUPERSEDES the prior row's "genuine b-vs-flow re-parametrization" framing.

**What comes next:** Security+liveness batch calibration RUNNING (confirm decidable-harm false-pass < 1% per class now the gate discriminates instead of masking, AND PARAM adopts); then full 10k `run-calibration.py --full` (violations empty), export `bleavit.sim-calibration.v1`, run `check-phase0-exit.py`, publish V-12 values, flip S4+G0 ✅. NOT yet committed.

## G0+S4 (full 10k under the faithful gate — liveness…

G0+S4 (full 10k under the faithful gate — liveness RESTORED, but calibration honestly RED; the always-veto gate had been MASKING the true security state)

Full `run-calibration.py --full` (10,000 proposals, 5,351 s) regenerated `phase0-calibration.json` (uncommitted). **Liveness fully restored:** every class adopts (PARAM 160 / TREASURY 214 / CODE 187 / META 222; PARAM was 0). **But three normative violations:** META decidable-harm false-pass **1.31 %** (≥ 1 %; PARAM 0.69 / TREASURY 0.35 / CODE 0.12 all ✓); `sub_3p_every_class_clean=false`; one CODE wrong-PASS envelope violation. **Decisive economic split (realized cost vs prize):** only **4 genuinely PROFITABLE flips** — 3 PARAM (cost/prize 0.35–0.49, prizes 30–46k = the small unbacked capability-envelope proxies ⇒ the **SQ-173** residual that already blocks production PARAM) + **1 CODE** (proposal 5996, `th1_displace_hold`, cost/prize 0.30, `gate_books=0` ⇒ a pure decision-book attack that flipped at realized 79,973 BELOW the model's manip-floor 85,945 ⇒ the envelope violation; AttackCost̂ 1.49M massively over-predicts). **META's 1.31 % is 100 % unprofitable griefing (0 profitable);** all other sub-3P flags (TREASURY 12 / CODE 27 / META 30) are griefing (cost ≥ prize). **Root insight:** R3's always-veto gate vacuously passed sub_3p (0 adoptions ⇒ 0 measurable flips); the faithful gate reveals the true manipulation resistance for the first time — the calibration's `sub_3p` criterion (calibration.py:409, flags ANY flip below 3P *budget*) + the raw < 1 % rate both count griefing, which D-4 (profit) + TM-18 (deep-pocket griefing accepted) do not. So the faithful fix is CORRECT (it exposed reality); the milestone cannot cleanly flip.

**What comes next:** User decision needed: (a) investigate the CODE-5996 manip-floor under-prediction + pin the intended exit criterion (profitable-exploit rate vs the current strict sub-3P-budget/raw-rate) BEFORE a fix path; (b) rule the exit gate on profitable-exploit rate (TM-18) + fix CODE-5996 + scope PARAM behind SQ-173 → re-calibrate → flip; or (c) deeper manip-floor/sizing work so nothing flips below 3P budget. Artifact left uncommitted/red; sim fix (engine.py+test) green and self-contained.

## G0+S4 (investigate-first: δ-calibration path is a…

G0+S4 (investigate-first: δ-calibration path is a NO-OP; the real finding is a structural marginal-proposal sizing gap)

Per the user's *investigate first* ruling. **CODE-5996 characterized:** `effect_stratum=sub_half_delta`, true_effect −0.0084 (marginal welfare harm) but a 269,504 capability envelope; the honest market barely rejects (accept.full 0.508 vs reject.full 0.520), so a ~4k spend flips it — `AttackCost̂` 1.49M massively over-predicts because it assumes a full-δ displacement while a near-boundary proposal needs a fraction. All 4 profitable flips are the same shape (marginal harm, near-boundary). **δ-calibration ruled a NO-OP (empirical):** proposal harm is generated as `magnitude = decision_delta × stratum_multiple` (proposals.py:176), so raising `DELTA_FLOORS` rescales the population identically — cheapest-flip budgets are byte-identical at δ×1.0/×1.5/×2.0/×3.0. (An earlier δ×1.5-on-a-fixed-proposal probe wrongly suggested closure; it did not rescale, so it was misleading — corrected here.) So 15 §4.9's "calibrate δ to false-pass < 1%" is scale-invariant in THIS sim's effect model and cannot move the metric. **Real conclusion:** R3's always-veto gate had vacuously masked a genuine structural gap — marginal (near-decision-boundary) proposals are inherently cheap to flip, and step-9 `AttackCost̂` over-allows their prize/capability-envelope; δ is not the lever. S4/G0 cannot cleanly flip this session; needs a deliberate mechanism decision (AttackCost̂/sizing treatment of marginality, and/or the sim effect-model's absolute-vs-∝δ faithfulness, and/or a criterion ruling on marginal-harm-at-resolution + scope PARAM behind SQ-173). Faithful-gate liveness fix stays valid/committable; calibration artifact regenerates red from the fix.

**What comes next:** **User ruled PARK CLEANLY (2026-07-19).** Faithful-gate sim fix committed (engine.py + test_engine.py; 27 engine/market/agents tests green); the transient red 10k artifact was reverted to prior committed bytes (regenerate via `run-calibration.py --full` to reproduce the marginal-flip finding on resume — it is NOT committed to avoid churning a superseded red state). S4/G0 do NOT flip. **Resume = the marginal-proposal mechanism decision:** AttackCost̂/sizing treatment of near-boundary marginality (05 §5.6 / 08 §5) vs the sim effect-model's ∝δ-vs-absolute faithfulness (which makes 15 §4.9 δ-calibration a no-op) vs a criterion ruling (marginal-harm-at-resolution within tolerance + scope PARAM behind SQ-173 + META as TM-18 griefing).

## G0+S4 (V-12 decoupled-δ recalibration +…

G0+S4 (V-12 decoupled-δ recalibration + profitable-exploit criterion — IN FLIGHT, neither milestone flips yet)

**The prior "δ is a no-op" conclusion tested the WRONG lever.** Raising `DELTA_FLOORS` rescales the sim population (harm ∝ δ) — a no-op; but the DECOUPLED decision margin δ (the one that flows into `decide()`) is a genuine lever. Implemented under a user AskUserQuestion ruling (profitable-exploit criterion + genesis-only propagation): **(1)** sim-faithfulness fix — proposal true-effect anchored to a frozen `EFFECT_SCALE`, decoupled from `decision_delta` (byte-identical at the old calibration); **(2)** V-12 `dec.delta` uniform 1.5× raise → 0.0225 / 0.0375 / 0.060 / 0.090 (13 §1 genesis + constitution genesis 22.5M/37.5M/60M/90M on the 1e9 grid; 0.005 kernel floor unchanged, its raise logged as a deferred question); **(3)** 15 §4.9 profitable-exploit criterion (`calibration.py` griefing-by-cost exclusion — sub-3P/AttackCost̂ scores realized cost < prize only). Evidence: δ×1.5 mini-calibration (N=3000) — every class decidable-harm false-pass < 1 %, envelope clean, the 14 residual sub-3P flags ALL griefing (cost 1.79–2.23× prize, δ-immune across ×1.5/×2.0); CODE-5996 (last session's one profitable exploit) now costs 2.49× the prize. Green: reference-model 38 + vectors + doc-table, targeted Rust constitution/epoch/treasury (189), all 7 sim modules (marginal seed 31→38; 2 bracket tests reworked to the griefing path), limit-coverage 178, phase-gates 68. Liveness reported honestly: honest clearly-good adoption ~28 % → ~16 % (not the R1 reject-everything mode). RUNNING at session pause: full 10k `run-calibration.py --full`, full Rust workspace gates, spec-review, 10⁷ sweep pre-generated (740 MB).

**What comes next:** Confirm the full 10k publishes (0 profitable exploits, per-class < 1 %) → commit → export `bleavit.sim-calibration.v1` → `check-phase0-exit.py` full exit → flip S4 + G0 ✅; if the full 10k surfaces a profitable flip the N=3000 mini-run missed, iterate δ per class or re-open.

## G0+S4 (full 10k #1 → one PARAM residual → δ ×2.5…

G0+S4 (full 10k #1 → one PARAM residual → δ ×2.5 per-class bump; re-run IN FLIGHT)

The first full 10k (PARAM δ×1.5 = 0.0225; 5,657 s) came back **candidates-only**: gate A (decidable-harm false-pass) GREEN for all four classes (param 0.495 / trs 0.145 / code 0.135 / meta 0.563 %, all < 1 %), envelope clean, 63 griefing diagnostics correctly excluded — but **one** profitable sub-3P flip survived: **PARAM id=3568**, a marginal (|true_effect| 0.016 < δ) near-boundary proposal on an unbacked capability-envelope prize (SQ-173, ask=0), flipping at 0.35× the prize. Root cause: PARAM's small floor (0.0225) leaves marginal flips cheap where CODE's 0.060 does not. **User ruled (AskUserQuestion): raise PARAM δ to 0.0375 (×2.5).** Proven via the exact `_threshold_brackets` logic to close id=3568 (0.35× → 6.06× prize griefing; sharp threshold between 0.030 still-profitable and 0.0375); raising δ only ever hardens flips, so no new profitable flips appear. Propagated PARAM 0.0225→0.0375 across `treasury.py`, doc-13, constitution genesis (37.5M), `EpochParams::DEFAULT` + 3 re-tuned decision tests, design-kit; vectors regenerated + `--check` green. Full 10k **re-run launched** at PARAM δ=0.0375. Spec-reviewer (0 blocker/2 major/3 minor) all fixed; SQ-273 (1× vs 3× exit threshold) logged.

**What comes next:** Confirm the re-run publishes (0 profitable exploits, per-class < 1 %) → `--check` (digest match) → commit → export `bleavit.sim-calibration.v1` → `check-phase0-exit.py` full exit (pre-generated 10⁷ sweep) → flip S4 + G0 ✅.

## **G0 + S4 ✅ — full 10k #2 PUBLISHED at PARAM δ=0.0375;…

**G0 + S4 ✅ — full 10k #2 PUBLISHED at PARAM δ=0.0375; Phase-0 exit PASSED.**

The re-run (PARAM δ×2.5 = 0.0375; 5,527 s) returned **`designation: published`, `violations: []`**: decidable-harm false-pass < 1 % all four classes (param **0.000** / trs 0.145 / code 0.135 / meta 0.563 %), `sub_3p_every_class_clean` **true**, 0 envelope violations, 63 griefing diagnostics, every class adopting (58/156/139/171 — liveness preserved). id=3568 closed (PARAM gate A → 0.000 %). `run-calibration.py --check` green (24 byte-exact subsample rows + Merkle + **source-digest match** — the `proposals.py` revert was byte-exact). Full Rust workspace gates green (**1,364 tests**, every leg, at PARAM δ=0.0375); reference-model 38 + vectors + doc-table, sim 50, limit-coverage 178, doc-links, PLAN tables all green. Flipped **S4 ✅ + G0 ✅** in this commit; the export of `bleavit.sim-calibration.v1` + `check-phase0-exit.py` full clean-tree exit (git_commit-bound, pre-generated ≥ 10⁷ sweep) confirm **`phase0_exit: true`** against the committed evidence.

**What comes next:** Ratify the V-12 Decision-log amendment + SQ-273 (1× vs 3× exit threshold); production PARAM adoption stays SQ-173-gated (unbacked prize); next milestone per PLAN (Track F / B-residuals).

## G0 (post-merge CI fix — stale weight-regression ack…

G0 (post-merge CI fix — stale weight-regression ack removed)

PR #105 CI turned `check-weight-regression.py` red: `STALE ACKNOWLEDGEMENT` for `pallet_futarchy_treasury.rs::execute_coretime_renewal`. Root cause: PR #112 (`077a055`, now this branch's merge-base with `origin/main`) introduced the SQ-261 B12 placeholder weight AND its ack in the same commit, so on this branch the treasury weight file is byte-identical to the base ⇒ zero regression ⇒ the ack has nothing to attach to ⇒ stale (the checker fails stale acks by design, "stale acknowledgements must be removed"). Fix = the tool's own prescribed remedy: removed the single stale line from `tools/ci/weight-regression-acks.toml` (file back to header/format comments only). No context lost — the SQ-261 conservative-placeholder rationale lives in the weight-fn body (`pallet_futarchy_treasury.rs:156-159`) and `configs.rs:4090`. Verified: checker PASS vs the exact CI base `077a055` (exit 0), PASS vs default merge-base, 16 self-test cases green; no test or spec doc pins the ack line. CI-tooling only — no runtime/spec/weight change; SQ-261 release-blocker (pending regenerated treasury benchmarks) is unaffected.

**What comes next:** Commit + push per the fix request; confirm the weight-regression leg goes green on the PR #105 CI re-run.

## G0 (CI fix — `property-suites`…

G0 (CI fix — `property-suites` runner-lost-communication, sharded)

User reported PR #105 red on **Property suites (full count)** (paste truncated mid-`market-core` i12). Diagnosis via `gh`: **not a test-logic failure** — the job failed after **1h23m** with GitHub's annotation *"The hosted runner lost communication with the server … starves it for CPU/Memory"* (resource starvation on a single marathon job). Evidence it is infra, not a regression: the **07-18 run's same job PASSED in 1h11m** (that run went red only on the now-fixed weight-regression leg); the market/ledger/constitution property files are byte-identical to `main`, and the branch's only `market-core`/`futarchy-fixed` deltas (SQ-231/SQ-271) are **purely additive** (new pure fns `contest_capital`/`liquidity_hat` + one test — the LMSR buy/sell/seed path i12 exercises is unchanged). Reproduced green locally: i12 at 10⁶ = 160 s, constitution 10⁶ = 55 s. This is exactly the CI-runtime risk the **S1** row pre-flagged ("1–3 h each on hosted runners"). **Fix (no case-count reduction — every suite still runs at ≥10⁶ per PR):** (1) sharded the `property-suites` job into a per-crate matrix `[ledger, market, constitution]` on fresh parallel runners (`fail-fast: false`, `timeout-minutes: 60`); `tools/ci/property-gates.sh` gained an optional shard argument (no arg = all, so the local all-in-one gate is unchanged). (2) Dropped the redundant standalone PT-5b invocation — `pt5b_mock_sweeps_interleave_with_late_claims` already runs inside the `property_tests` batch, so the second call was ~18 min of pure duplication. **Verified:** script dispatch + guard (bogus shard and `PROPTEST_CASES=5` both exit 2); `ci.yml` parses; the active ruleset "Main protection" has **no required-status-check-by-name**, so the matrix-renamed checks do not break the merge gate. Peak per-shard runtime is ~20–35 min with fresh memory (down from an 83 min single-runner marathon). CI-topology only — no runtime/spec/test-logic change.

**What comes next:** Commit + push; confirm the three shards go green in parallel, each well under the 60 min timeout.
