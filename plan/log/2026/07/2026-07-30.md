# Session log — 2026-07-30

## **`benchmark-smoke` caught a weight I forgot to regenerate — and regenerating found the fixture was under-measuring the same call**

SQ-524 added `do_keeper_rebate` to `reconcile_insurance` and I committed it **without regenerating `pallet_futarchy_treasury`**. CI's `benchmark-smoke` failed on exactly that, at the cheap `--steps 2 --repeat 1` fidelity, because `reconcile_insurance` is a **constant-weight** function and those are hard-gated at any fidelity — the case AGENTS.md predicts a per-commit run catches, doing precisely its job. Worth pairing with SQ-522, filed hours earlier: that row records the *complementary* hole, where component-bearing drift stays advisory at 2x1 and can only fail at release. Both facts are the same design, seen from its two sides. **Regenerating then exposed a second defect the failure had hidden.** The fixture seeded the INSURANCE surplus so the overflow fires, but never funded the keeper meter — so `do_keeper_rebate` short-circuited as the structural no-op `absent_keeper_rebate_param_is_a_structural_noop` documents, and the measurement charged the rebate's parameter **reads** with none of its payout **writes**. The tell was `FutarchyTreasury::State (r:1 w:0)`: a rebate that pays must write. Adding `prime_keeper_rebate()` — the pattern `prune_coretime_quote` and its sibling already use — moves writes 4 -> 8 and ref_time +46.7 %, so the first regeneration would have shipped a weight understating the paying path by half its writes. Final: reads 5 -> 17, writes 4 -> 8, PoV 7,404 -> 26,846; no ack needed (no merge-base baseline). **The lesson is one I had just written down and then repeated:** SQ-520 closed a fixture that measured the wrong path, and one commit later I added a rebate whose fixture measured the wrong path. A benchmark that exercises a call is not the same as a benchmark that exercises its **worst** path, and the cheap check for it is to look at whether the storage lines contain the writes the feature is supposed to perform. Verified at CI's own fidelity: `--check --steps 2 --repeat 1` PASS, plus `--changed` exit 0 (fmt, clippy, 411 runtime + 90 treasury tests).

**What comes next:** #196 CI green; then SQ-523, the 12 audit minors, and merge

## **`benchmark-smoke` failed again on the fix commit — five pallets were stale, not one, and my verification was scoped to the wrong thing**

The previous row's fix (`16dc545`) did **not** clear CI. The failure was on that commit, and the reason is a process defect worth more than the fix: I verified with `regenerate-weights.py --check --pallet pallet_futarchy_treasury`, but CI's `benchmark-smoke` runs the same gate over **all 31 pallets**. A per-pallet check can only ever confirm the pallet I already suspected, so it was structurally incapable of catching this — the same shape as trusting a scoped grep. Reproducing the gate at its real scope named the set immediately: **`pallet_constitution`, `pallet_execution_guard`, `pallet_guardian`, `pallet_registry`, `pallet_welfare`**. I let the full sweep finish before touching anything rather than fixing the first hit, precisely to avoid a third round trip. **The drift has two distinct mechanisms, and separating them is the useful part.** (i) *SQ-518 was under-scoped*: `execution_guard` and `guardian` show `FutarchyTreasury::PendingMainCredit` at **`r:1 w:1`** — they slash bonds to INSURANCE, so they run the 08 §1.2 overflow. I had regenerated only `pallet_epoch` and `pallet_oracle`, the two pallets whose *adapters* I edited, not noticing those adapters are reached from other pallets' dispatches. (ii) *E3's counter is read runtime-wide*: `registry`, `welfare` and `constitution` show the same key at **`r:1 w:0`** — read-only, because `nav()` reads it and `nav()` is consulted from dispatches all over the runtime. A new storage item behind a widely-used helper invalidates weights far outside its own pallet, which is the generalisable finding and belongs with SQ-522's family. One acknowledgement added, for `qualify_recovery_image` (**ref_time only**, +13.9 % at the 4 MiB component high): `pallets/execution-guard/` and `crates/execution-guard-core/` are byte-identical to the merge base and no commit touches that function — it was re-measured purely as a file-neighbour of the two slash paths, its storage dimensions did not move at all, and ref_time is the dimension the drift tool calls advisory for exactly this 127 %-spread reason. **State at this row, so nothing is assumed:** the five weight files and the ack are regenerated and in the working tree, `check-weight-regression.py`, `check-weight-storage-bounds.py` and `check-generated-weights.py` all exit 0, PLAN tables green — and the confirming sweep has now reported: **`--check --steps 2 --repeat 1` over all 31 pallets, 0 stale, PASS** — the gate reproduced at its own scope rather than at the scope of my hypothesis, which is the whole point of this row.

**What comes next:** Commit; then SQ-523, the 12 audit minors, and #196 merge

## **SQ-523 closed — and the fix I had guessed at last session was wrong about which precondition was missing**

The previous attempt aborted on the SDK fixture's `.unwrap()` and I inferred a cause list: `SafetyFilter` admission of `remark`, the `Operational` class path, `pallet_assets` provider/consumer refs for a synthetic caller. **None of those was it.** The tell was that `test_run` is *not* benchmark-gated, so it can be driven from a plain runtime test in the same `development` genesis the bencher builds — and instrumenting it took one run to answer what a session of guessing had not: `Invalid(Payment)` bare, `Invalid(Payment)` with the rate alone, **`OK`** once the caller held USDC. Two preconditions, both absent from genesis, neither on my list. `AssetTxBenchmarkHelper` now seeds the governed `fee.vit_usdc` rate and funds the payer, and `create_asset_id_parameter` returns `usdc_location()` because `LiveFeeConversion` rejects everything else — the stock synthetic id would have measured the conversion's **`Err` arm** and weighted the paying path with a cheap early return. **The measurement is worse than the estimate the row carried.** SDK `()`: 5 reads / **2 writes** / **3,675 B** proof. Measured here: 5 reads / **4 writes** / **7,404 B** — a **2.01x PoV understatement**, +3,729 B on *every* USDC-paid extrinsic. My own row said "roughly two reads and two writes" and was silent on proof size, which is the dimension that actually bounds a parachain block; writes it got right. Storage lines reconcile exactly to the E3 seams (`Constitution::Params` r:1 for the rate; `ForeignAssets` r:3 w:3 for withdraw plus the resolve into `MAIN`; `PendingMainCredit` r:1 w:1 for recognition). **The durable half is the spec.** 15 §4.5 mandated benchmarks "for every call and hook" — a transaction extension is neither, so this surface was outside the mandate entirely, and the runtime's registry comment had rationalised the gap in words that were true before E3. Amended under R-1: the mandate names extensions, and a new bullet gives the two reasons this class is worse than a call (the post-dispatch refund subtracts the same `WeightInfo` it charged, so it is identically zero on the charged path; and the weight is charged per *extrinsic*, so error scales with occupancy) plus the fixture rule — upstream fixtures assume the **reference** runtime's admissible inputs and MUST be re-pointed where this runtime narrows them. **Verification:** three fixtures generated at the committed 50x20 fidelity; purity, storage-bounds and regression gates exit 0 (new file, so no ack needed); the drift gate re-run at its own scope — **32** pallets, not 31 — which has since reported **PASS, 0 stale** (this clause was deliberately written as pending and is now settled; R-10, after calling a gate green in advance one session ago); and a named test, `the_usdc_fee_extension_benchmark_fixture_measures_the_paying_path`, pinning both preconditions *and* that the benchmarked path really moves custody and NAV — so the next break reads as a named failure rather than an unexplained pipeline abort. Also confirmed: `benchmark-smoke` **passed** on `6ff19bc`, so the five-pallet fix held.

**What comes next:** The 12 spec-audit minors; re-review; #196 merge

## **Four of the twelve spec-audit minors closed — and one of them was a spec/implementation divergence, not a wording nit**

Taken while the 32-pallet drift gate ran, so none of it cost serial time. (1) **15 §1's copy of L-7 was missing `- min_balance`.** The owning row (03 §9) has carried the term since E1 and the shipped `try-state` hook asserts it, so this was doc-only — but doc 15 §1 is what the try-state coverage rule is written against, and it mandated a **weaker** bound than the invariant states: `Preservation::Preserve` makes at most `balance - min_balance` movable, so the weak form admits an accrual the sweep could not transfer in full. Two copies of one invariant disagreeing is the defect, independent of which copy the code followed. (2) **I-31 did not mention the freeze**, so it still read as an unconditional "never touches escrow" claim after SQ-517 made it conditional on I-4; the condition is now stated where the invariant lives, not only in 03. (3) **The oracle reporter-bond slash had no test.** SQ-518 wired the 08 §1.2 overflow into *both* USDC inflows, and every existing assertion covered only the proposal-bond leg — a one-sided regression is invisible in the `confiscated_usdc()` sum, which is exactly what that helper's own doc warns about. New runtime test drives `RuntimeOracleCustody::slash_insurance` through real custody and asserts confiscation, `T_ins`, NAV recognition and the zero no-op. (4) **The SQ-518 ack claimed "O(1), no scan" and that was wrong**: `pallet_oracle::apply_custody` calls `slash_insurance` *inside* its reporter and watchtower loops, so the overflow can run once per slashed seat. The conclusion survives but for a reason the ack had not given — every iteration touches the **same** keys and the benchmark counts distinct keys, so the storage totals are seat-count invariant; only ref_time scales, and the fixture already seeds `MAX_REPORTERS_BOUND - 1`. Corrected in place. Also deleted three **obsolete** `pallet_welfare` acknowledgements the gate had been printing as cleanup-when-convenient. **Keeper (separate workspace), the fifth minor:** the planner consulted no freeze latch at all, so after SQ-517 it would submit `sweep_redemption_fees` and `sweep_revenue` into an active `PB-LEDGER-FREEZE` and pay for the refusals; the same audit found `crank_observe` had been exposed to the market latch since long before Track E. All three are now gated on per-pallet `FrozenUntil` reads. Two judgement calls recorded in the code: an **unreadable** latch decodes to `None` and submits — identical to the pre-change behaviour, whereas treating it as frozen would let a persistent read failure stall the revenue cranks indefinitely — and the boundary is `now < until`, the exact negation of the pallets' own guard, so the keeper never suppresses a call the chain would accept on the block a freeze lapses. Both pinned by tests, along with the independence of the two latches in both directions. Keeper gates green: fmt 0, clippy `-D warnings` 0, 85 tests. **One gate gap noted, not filed:** `clippy` never runs with `--features runtime-benchmarks` (CI and `rust-workspace-gates.sh` both use the bare `--workspace --all-targets`), so benchmark-only code — including the fixture this row adds — is unlinted. Running that combination locally surfaces six pre-existing errors in `tests_b15_recovery.rs`, `tests_migration_guard.rs` and `configs.rs:7651`, none of them from this session; the gap is real but it is a CI-scope question rather than a Track E defect, so it is recorded here rather than widened into this PR.

**What comes next:** The remaining 8 minors; re-review; #196 merge

## **The two remaining spec-audit minors were both worse than filed — and one of them turned out to be a single arithmetic slip wearing two disguises**

The audit listed "two 08 §10 figures that do not derive from their own tables — 8.2 vs 9.7 years, 77,864 vs 65,864" as separate items. They are **one** defect. `77,864 = 65,864 + keeper.budget_epoch`: §10.6 took the keeper cost **gross** (`580,320 x 0.09` = 52,229) while also keeping the separate 12,000 `KEEPER` row that the gross figure already contains. And "8.2 years" is that same number annualized (`77,864 x 17.393` = 1,354,279) and divided into the identical `25M - 13,862,944` = 11.14M envelope from which the table's own 9.7 yr cell is computed. Neither 77,864 nor 1,354,279 appears in any §10.1 table — the literal symptom the audit named, and the reason a figure should always be shipped with its quotient. **Corrected to 62,281/epoch and 9.7 years**, and 62,281 is not merely 65,864 either: the sentence names *collators, keepers and the `ops.*` lines*, so the two rows a quiet epoch genuinely does not incur come out (`REWARDS`, paid only on `Executed`; the POL divergence, which needs a seeded book). **Direction recorded because it is the part that matters for trust:** both errors ran **conservative** — an overstated cost base, a shorter runway — so no self-funding claim was ever stronger than the arithmetic supported. The stale `1,354,279 / 4-9x short` pair in PLAN's own session log is corrected to `1,145,562 / 3.1-7.6x`, matching the Track E section that had it right the whole time. **The second minor, 03 §3's `ProtocolAccounts`, is the more instructive one.** It enumerated six sub-accounts and asserted "this enumeration is normative and is the same closed set §5.3a(1) names". The runtime's set also holds the ledger sovereign, treasury `MAIN`, the per-market **book** namespace (only the *fee* side was ever named), and seven pallet sovereigns (market, epoch, execution guard, oracle, welfare settlement, incident registry, milestone registry) — **ten the list never named**. The implementation is right; the spec's closure claim was wrong, and wrong in the dangerous direction, since a reader checking whether an account may be charged would read absence from a closed list as licence. Fixed by making the **ownership criterion** normative and marking the list as its derived membership. **This is now the third example-list-claiming-closure in two rounds** (06 §6.3's frozen calls, 15 §1's L-7 copy, this one), which makes it a defect *shape* in this doc set rather than three coincidences: prose that both enumerates and claims closure should state the rule and mark the list as derived.

**What comes next:** Commit; exhaustive gate; spec-reviewer; #196 merge

## **Round-5 spec-reviewer: 0 blockers, 2 majors, 12 minors — and for the third round running, every serious finding was in my justifying prose, not the code**

The reviewer explicitly confirmed the *code* clean everywhere it checked: ledger fee arithmetic, the pair-leg fee base, L-7's `try-state` bound, the coupling screen, the INSURANCE target/overflow, and the generated weights — and that binding `pallet_asset_tx_payment::WeightInfo` strictly **increases** the charge on both paths, so no undercharge was introduced. **Both majors were mine, both real.** (1) *The `ProtocolAccounts` "criterion" I wrote to replace the under-inclusive list was itself broken, twice.* Read literally it is **vacuously satisfied** by accounts holding no positions — the treasury `KEEPER`/`ORACLE`/`REWARDS`/`COLLATOR` sub-accounts and the guardian sovereign — which `contains` excludes; read structurally it is **circular**, because the §5.4 `ProtocolDestination` refusal I cited as its support is a *consequence* of membership, not a test for it. I had over-corrected: an under-inclusive list is a smaller error than a criterion that decides the wrong set. §3 now says the list **is** the definition, records both failed derivations so neither is retried, and states the excluded R-4 accounts explicitly — their absence means §5.4's transfer refusal does not cover them, filed as **SQ-525** (bounded: escrow still backs the positions, the loss is the sender's, and it is pre-existing rather than Track E's). (2) *15 §4.5's new bullet said a narrowed fixture "measures the rejection path".* It does not — it **aborts**, which is the very symptom that left SQ-523 unresolved for a session. I wrote the false mechanism into normative text days after diagnosing the true one. Reworded to the real disjunction: assert-success fixtures abort, non-asserting ones mis-measure. **Nine of the twelve minors were further false claims of mine**, and three are worth naming because they invert the story I was telling: the "carried by the extrinsic base weight" note was **never** true (not "true before E3") — `weight()` has always returned `WeightInfo::...`, so `()` bound the charge to the SDK reference from B1a; `pallet_asset_tx_payment` is **not** the first non-dispatchable weight entry (`cumulus_pallet_parachain_system` already carries three `block_weight_tx_extension_*`); and the understatement **predates E3** — the `ForeignAssets`-over-`Assets` swap and the governed-rate read are E1-or-older, with E3 adding only ~511 B and 2 writes of the 3,729 B gap. Also: three PLAN figures (`V*` 164.2M, 1.39×, 132 %) were still the retired 1,354,279 denominator even after I claimed to have purged it — corrected to 138.9M / 1.17× / 156 %, which 08 §10.4/§10.5 had right all along; the `adjudicate` ack's ref_time-coverage evidence was wrong (the fixture passes `reporter_wrong = false`, so exactly one overflow runs, and seat count drives the comparison scan rather than the overflow count — restated as a residual, not coverage); the keeper README and test name over-claimed the freeze scope (`execute` and seeded-book `sweep_dust*` are deliberately not gated); the new fixture test did not pin the departure the doc calls load-bearing (now asserts `create_asset_id_parameter` returns USDC — and writing that assertion found it did not compile under `--features runtime-benchmarks`, a combination no gate runs); and `.claude/rules/` rule 10 had not been re-trued with 15 §4.5. All 14 addressed. Gates after the fixes: runtime 413, ledger-core 15+5, keeper 85, fmt/clippy 0, `--tests --features runtime-benchmarks` 0. **Committed `f590557`; the exhaustive no-argument `rust-workspace-gates.sh` then passed — exit 0, 92 test binaries, 3,155 tests, 0 failures, 0 errors**, covering the release/`runtime-benchmarks`/`try-runtime` builds (419 try-runtime tests), the bootstrap/Phase-4 profile matrix, the keeper leg, `no_std`, weight storage-bounds (22 annotations), generated-weight purity (366 functions, 2 justified `pallet_xcm` overrides) and limit-coverage. **CI on `f590557`: 13 of 14 green at the time of this commit, including `Benchmark smoke` — the job that failed on both prior commits — with only the `Model checking (TLC)` leg still running; it exercises `models/tla/*`, which this branch does not touch.

**What comes next:** Confirm TLC; #196 merge

## E1–E4 closed; **E5 opened**

**#196 merged (`76ee39e`), so E1, E2, E3 and E4 are ✅** — with their evidence recorded per row (#195 `bdec21d` + the #197 hotfix `af2e5a0` for E1, #196 for the rest). Then answered a question about POL/INSURANCE spend and the annual cost base from 08 §1–§10 and 13 §1, and **put the result on the plan as milestone E5 — "reduce the recurring cost base"**. **What the reading established.** POL is not an expense at all since E1: the §3 commitment revolves and only the live-branch divergence loss is consumed — ≈ 18,830 USDC/yr, **1.6 %** of the base. INSURANCE has **no recurring spend**: two exits only (`sweep_insurance` under a TREASURY decision, and the automatic/cranked above-`T_ins` overflow), and 08 §1.2/§10.6 forbid repurposing it as an operating buffer. So the entire cost base is elsewhere — and it is **94.5 % two lines**, keepers (79.3 %) and collators (15.2 %), with `ops.keepers` alone at **61.1 %** buying what 08 §6.3 calls chart density rather than decision quality. Both keeper lines are linear in `1 / mkt.obs_interval`. **E5's row carries the five levers, the two refusals (`REWARDS`, POL divergence — they buy the mechanism), the unavoidable line absent from 08 §10.1 (`ops.coretime`'s `broker.renew`), and the derived consequence**: levers 1 + 3 move `V*` from 138.9M to 68.0M, i.e. from 1.17× a saturated 5×TREASURY slate to 0.89× a saturated 5×PARAM one, runway 9.7 → 19.9 yr — a change in what the endowment bridges *to*, reached without a fee-rate raise. Marked a values-layer milestone under R-1/R-2: derive every figure, pick none, and do not touch the frozen 08 §4.1 class floors. **Raised SQ-526** (batch E, 10 rows): `mkt.obs_interval`'s registry max is 50 and the staleness threshold is `MKT_STALE_GAP_BLOCKS = 50` — a **kernel constant**, CODE-only — so the top of the row's own admissible range guarantees staleness events, and unlike the two couplings 13 rule 7 screens, nothing refuses it at the amendment boundary. Safe error direction (extensions and rejects are status-quo defaults), so it is a question, not a defect; E5 targets 20 and leaves 2.5× margin. Also superseded the stale "do not merge #196" hand-off at the top of Current focus while **keeping its three durable lessons** (the `fuzz/`/`keeper/` workspaces are invisible to the main gate; `check-weight-regression.py` is blind to functions its own PR base introduces; CI green is not the merge bar while a commissioned review is still running). **No spec and no code changed** — PLAN.md only. Gates run: `check-plan-tables.py` OK, `check-spec-question-batches.py` OK (379 rows, 15 open, 10 batches).

**What comes next:** E5: measure the real `crank_observe` fee from the committed weights to re-base `keeper.rebate` (its `[VERIFY]` is now resolvable for the first time), then derive the `mkt.obs_interval` operating point and re-derive the 08 §10.1/§10.4/§10.5/§10.6 tables

## E5 (pass 1)

**Built the executable model first, then let it find the defects.** New `reference_model.sustainability` implements 08 §10's cost/revenue/runway arithmetic — discharging doc 08's own standing mandate that "all worked arithmetic … MUST be reproduced by the Phase-0 reference model", which §10 had never satisfied — with **33 tests pinning every published figure** in §10.1–§10.6. It reproduced all of them exactly, then its admissibility check immediately surfaced **SQ-527** (08 §6.2 asserts the keeper meter covers a load 52.80/epoch larger than the budget, and §6.3 makes exhaustion a *latch*). Three research agents ran in parallel — spec outflows, code outflows, weight-derived fee measurement — and converged independently on the same conclusion: **the fee basis was fabricated**. That opened **SQ-531** (rebate ≈ 1,058× the real fee; 79.3 % of the cost base), **SQ-528** underneath it (`FeeMultiplierUpdate = ()` → `FixedU128::default()` = 0 → **weight unpriced from block 2**, a DoS subsidy on a PoV-bound chain), and **SQ-530** in parallel (four fail-soft paths double-counting `PendingMainCredit` → cumulative NAV inflation, the over-permissive direction). Every fix has a regression that **fails before and passes after**, verified in that order: the multiplier test printed `light=108157120 heavy=108157120` for a 149× weight difference; the treasury test showed the counter surviving at 1,000 USDC. An adversarial Codex review (read-only per R-13) **killed the `Pays::No` design** I had drafted — correctly: `keeper.rebate` is shared across many pallets' cranks, so blanket retirement needs per-dispatch useful-success proofs. Dropped it; the basis correction made it moot. **Gates, all green and all with real exit codes** (an earlier `| tail` pipeline masked cargo's status and was re-run rather than trusted): `cargo test --workspace` **exit 0, 1,744 passed, 0 failed** · fmt 0 · clippy `-D warnings` 0 · keeper workspace **85, exit 0** · reference-model **117** · simulation OK · vector freshness 0 · limit-coverage 0 (`unwired = 0`) · PLAN tables · batch index · doc links 84. **Cost base 1,145,562 → 239,728/yr; runway 9.7 → 46.5 yr, or 101.9 yr with `collator.comp_epoch` at its registry minimum. The ≥ 25-year goal is met against the *binding* arming floor, at zero assumed revenue.**

**What comes next:** E5 pass 2: take `collator.comp_epoch` → 500 (two PARAM amendments), phase-gate the eight unsized `ops.*` lines, decide SQ-532 (the keeper meter can no longer bind) and SQ-529 (congestion pricing / unpriced `proof_size`); the `mkt.obs_interval` lever is now worth ~1,300/yr and is **not** worth governance

## E5 (pass 1 closeout)

Added the durable half of SQ-531: `the_seeded_keeper_rebate_tracks_the_measured_crank_fee` recomputes the fee from the **live dispatch info** of a real `crank_observe` and fails if the seeded rebate drifts more than an order of magnitude either side of its 3× target. Correcting the number alone would not have stopped the error recurring — the fee moves whenever weights are regenerated, and nothing tied the two together. Band is deliberately wide because the row is a governed PARAM with its own [1×, 10×] envelope and the USDC leg still moves with the `[VERIFY at TGE]` price; it is a drift alarm for the two-to-four-orders-of-magnitude failure that actually happened. **Also recorded honestly: the first exhaustive-gate run returned 101, not 0.** A `-D warnings` clippy error (`fee_vit as u128` where `Balance` is already `u128`) in the test above — added *while the gate was reading the tree*, which is the hazard CLAUDE.md's stop-guard note describes from the other direction. Fixed and the gate re-run on a stable tree rather than reported from the first run. **Declined `collator.comp_epoch` → 500** (Decision log): largest remaining line at 72.6 % of the base, but the only lever here whose error direction is unsafe and which no evidence anchors.

**What comes next:** E5 pass 2: phase-gate the eight unsized `ops.*` lines; decide SQ-532 and SQ-529; the oracle-line rebate path (08 §6.3) has **no meter at all** — six permissionless cranks draw on `ORACLE` with only the funded balance bounding them, which the corrected rebate makes small but still unbounded ; SQ-533 filed

## E5 (pass 1 verified)

**Exhaustive gate GREEN on the third run: `tools/ci/rust-workspace-gates.sh` (no arguments) exit 0, 3,168 tests, 0 failures, limit coverage `unwired = 0`.** The first two runs are reported rather than discarded: run 1 exited **101** on a `-D warnings` clippy error (`fee_vit as u128` where `Balance` is already `u128`) in a test added *while the gate was reading the tree*; run 2 exited **1** on `fmt --check` for the same test. Both were real, both were mine, and the lesson is the one CLAUDE.md's stop-guard note states from the other direction — do not mutate the tree a gate is reading. Post-gate edits were Markdown and Python only (08 §10.1 ops phase-gating, PLAN SQ-533, the reference-model package export and two test corrections), none of which the Rust legs read, so the exit-0 result stands for the shipped Rust state. Reference-model suite now **118** tests: added `test_the_collator_lever_is_available_but_not_taken`, which pins the 130,447/yr the declined lever is worth and asserts both that declining it leaves the arming floor short of 25 yr and that taking it clears it — so the one open values decision is exact rather than narrative. Also corrected a test that asserted the goal against a lowered `collator.comp_epoch` I had decided **not** to ship; it now asserts the shipped point (46.5 yr to the CODE seeding floor) and states the shipped break-even turnover honestly (tau ~ 3.6, verified 3.558).

**What comes next:** E5 pass 2 — all of it governance/doc work, no further code: phase-gate funding of the eight unsized `ops.*` lines, decide SQ-529 / SQ-532 / SQ-533, and put `collator.comp_epoch` to launch governance with operator quotes
