# Session log — 2026-07-21

## **B14** — genesis endowment of the R-4 protocol accounts

Ten statically derived protocol accounts genesis-endowed with exactly `min_balance` USDC; `TreasuryPotFunding::fund` flipped `Expendable` → `Preserve` so MAIN's floor actually binds. Regression proven both ways (`accounts: vec![]` restored ⇒ `Token(NotExpendable)`). **The milestone's open sub-question — whether R-4 can bind book accounts — was answered against the convenient reading.** Per-market accounts cannot be genesis-endowed, but a Baseline book *does* hold plain USDC (it retains the sell-side fee), and a binary search against real `pallet-assets` custody showed every Baseline sell from `MinTrade` = 1 USDC to ≈6.35 USDC was failing: 6,349,782 planck errors, 6,349,783 passes retaining exactly `min_balance`. Doubly masked — `pallets/market/src/mock.rs` genesis-endows its shared BOOK account, and the in-memory differential oracle models no custody, so `market-core`'s own `minimum_trades_with_dust_fees_are_admissible` asserts the trade routes while production rejects it. Fixed by endowing the Baseline book at Seed from `POL_BASELINE` inside epoch `tick`'s storage layer; `Expendable` was evaluated and rejected because it folds the dust into the debit and sweeps the accrued fee into the sovereign where L-2 cannot see it. Spec trued per R-1 across 03 §1/§7, 04 §6.1, 08 §1.4; no 02 change, no contract bump. Work split across four Codex subagents (audit, runtime genesis, deploy tooling, book endowment) with the audit run read-only and independently. **Then dual review (spec-reviewer + adversarial Codex) caught a critical defect in my own fix and two spec over-claims, all repaired before merge:** the Baseline endowment was a hard `?` that, with `POL_BASELINE` funded to exactly `min_balance + headroom`, reverted the entire epoch-tick batch permanently — now best-effort + idempotent behind an affordability pre-check, with the per-book allowance added to the POL floor telemetry that made the cliff reachable. Both reviewers independently disproved the amendment's R-6 recovery claim (`recover_foreign` refuses `Usdc`), and the blanket preserve-MUST is now qualified for the XCM-addressable `TREASURY` seam. Rebased onto main after #120/#121 (batches B5/B6) landed; three conflicts resolved (PLAN.md, allocations template, chain-spec validator — all both-added, both sides kept). Also trued AGENTS.md's limit-coverage manifest line (178/66 → 179/67, drift from B12). **Post-PR Codex review found a P1 in the new chain-spec gate** — it checked only 2 of the 4 `foreignAssets.assets` tuple fields, so a spec naming an external `owner` (installed by `pallet-assets` genesis as issuer/admin/freezer) or clearing `is_sufficient` would have passed the release gate; the fixture itself used ALICE as owner. Now fully gated for USDC and DOT, +5 negative tests, reported payload verified rejected. Deploy suite 77 → 82. Gates: `rust-workspace-gates.sh` green · deploy suite 67 tests · PLAN tables · doc links · SQ batches · runbooks · limit-coverage 179/179

**What comes next:** B14 closes; next is the open batch work (B5/B6) and the X-row backlog led by SQ-40, SQ-36 and SQ-309

## — (Batch D: doc-truing; no milestone)

User: *"Pull the latest main… Resolve batch D by using multiple codex subagents intelligently. Don't ask questions. Decide on your own. Open a PR when ready."* **Method:** all 30 rows verified against the implementation by five parallel Codex passes (read-only, file:line evidence) **before any spec text was written** — the discipline B4/B6 established. It paid again: verification **broke or materially corrected 13 of 30 rows**. **Result: 26 ratified, 1 closed with no amendment, 3 moved to C.** Batch D closes at 0. The corrections mattered more than the ratifications. **SQ-32's own premise was false** — it asserted that governance/scheduler enactment bypasses `BaseCallFilter`; the pinned `pallet-scheduler` uses ordinary *filtered* dispatch, which is precisely why the runtime carries a closed bare-values-enactment admission set (`classifier.rs:988-1043`), and 06 §3.3 now documents the real two-path enforcement rather than a property `Contains<RuntimeCall>` cannot express. **SQ-171 was the largest amendment**: "market buyers recover par under VOID" was simply false — recovery is the D-1 *neutral value*, equal to `cost` only at the neutral prior 0.5 — and the false claim had propagated into **two decision-record entries (D-1, D-3)**, G-3, 03 §5.5/§6.4/§11, 04 §6.1/§6.2, 11 §11.6, TH-10 and I-2; all nine corrected. **Five rows split doc-half-here / code-half-to-X**, raising SQ-313…SQ-316 — of which **SQ-314 is a truth defect**: `void_cohort` overwrites `proposal.decision` for Measuring cohort members and emits `ProposalForceRejected`, so the archive records a rejection the market never produced, contradicting 07 §10's "decisions already made stand". **Three rows were refused as genuinely frozen-surface** and routed to C (SQ-83 event field sets, SQ-94 the `QueuedExecution` direct-read value, SQ-166 a new `RejectReason` variant) — SQ-83's "no contract bump" claim was false and SQ-94's "code is right" claim was too (it still carries the retired `pre_upgrade_checkpoint`). **Five values calls ruled** (Decision log). **Mid-session merge:** `origin/main` advanced to #122 (B14, R-4 genesis endowment) touching docs 03/04/08; one conflict in 08 §1.4 where both sides extended the same paragraph in disjoint places — resolved by keeping **both**, and B14's new R-4 rule was then applied *to* this batch's own amendment (`sweep_insurance` must preserve `min_balance`, since an endowment binds only where every custody path preserves). Docs only; no Rust touched; `INTEGRATION_CONTRACT_VERSION` stays **5**. Open **131 → 108**, resolved 175 → 202. **Dual adversarial Codex review of the batch's own amendments: 5 blockers / 12 majors, all triaged, every valid finding fixed in-session.** Three blockers were self-inflicted — the SQ-171 amendment conflated the pre-trade quote with the realized average execution price (LMSR convexity means a buy opening at 0.5 recovers strictly less than `cost`), it carried forward the false "complete pairs recover par" claim for *same-branch* sets (`merge_scalar` pays no USDC — the FE was about to render that as a headline 100 %), and 08 §5.4's floor illustrations were shown not to clear sizing at all once §5.2's `sec.flow_cap` ceiling is applied, in this batch's own (a) **and** the pre-existing (b). Majors: 06 §3.3's pseudocode still admitted `scheduler`/`sudo_as` that its own table denied; SQ-154's origin table had false cells (`vest_other` is not self-scoped) and four omissions; `referenda.submit` was missing from the carrier set; "admission set derived from §3.2" was untrue. Four findings were accepted as *correct-but-implementation-incomplete* rather than wrong (SQ-78, SQ-162, SQ-250, SQ-304) — already carried by SQ-313/314/316 and SQ-66. **Codex PR review of #123: two valid P2s, both fixed** — the VOID step-1 heading still promised 100 % over `merge_scalar` (split into `merge` + a consolidation step 1a), and the derived `docs/design/claude-design-kit/` pack was stale (verbatim copy regenerated, four distillations corrected). That pass also caught SQ-1's now-obsolete E-numbering caution in the kit and a sibling of SQ-236 — `oracle.recompute_proof(round_id, proof)` in 07 §5/§9 and 11 §11.8.1 vs the shipped `(component, epoch, spec_version, proof)` triple — both fixed. Gates (re-run after the fix round): doc links · runbooks 13/20 · PLAN tables · batch index (310 rows) · limit coverage 179 keys/0 unwired · phase-gates 69 · reference-model 7 + LMSR doc table + vector corpus · deploy 63 · release 71 · env 51 · monitoring 68 · ci suites.

**What comes next:** Batch **C** (the single v6 bump — now **35** rows after D routed three to it), then **X** (**73** rows, led by SQ-40, SQ-36 and the new SQ-314). O3 remains the only non-FE milestone before G2.

## — (Treasury health cluster: SQ-205, SQ-207, SQ-180;…

— (Treasury health cluster: SQ-205, SQ-207, SQ-180; worktree `treasury-health`)

Task: resolve the three-row treasury cluster containing the flagged live release blocker. **All three rows verified against HEAD before any code was written; two of the three briefs were wrong on a load-bearing point.** **SQ-207 — the brief asked for a `BudgetLine::Insurance` variant; 08 §1.2 explicitly rejects one** ("modelling an inflow-fed reserve as a line inverts the direction of every §1.1 control"), and 08 §1.4 says `sweep_insurance` "takes no budget line by design". Following the brief would have added a variant to a **persisted** enum (`TreasuryState.lines`/`vit_lines`) and forced this repo's first storage migration for nothing. Implemented per spec instead: call index 11, `FutarchyTreasury` origin, INSURANCE → `MAIN` only, `Preservation::Preserve` via a new `InsuranceSweep` seam, `InsuranceSwept` on the *transient* core `Event` (not in `TreasuryState`), plus the 06 §3.2 authority row, classifier arm, PoV/S5 inventory rows, benchmark and weights. **No storage shape changed; no `StorageVersion` moved; no 02 bump** (02 §6's rule leaves the event outside the ingest set). **SQ-180 — resolved**: bounded `PhaseArmingGate` on `pallet-constitution`, consulted before any write, bit 3 checked against **both** Code and Meta so the higher floor binds; runtime binds `FutarchyTreasury::ensure_nav_floor`. The row asked for `flag_nav_floor` too, but 08 §4.2's "event + extrinsic error" cannot both survive one dispatch — documented rather than faked. Disarming is never gated. **SQ-205 — the release blocker stays open, deliberately.** The seam is built and its atomicity proven (fallible `ReserveHealthSink`, edge-triggered, inside an explicit `with_storage_layer` so it also covers the non-extrinsic `reserve_probe_result` path; `set_reserve_impaired` made fallible — it had been swallowing persist failures). But binding it live would have been **strictly worse than the defect it fixes**: `RuntimeProbeDispatch = ()` in production and `XcmConfig::ResponseHandler = PolkadotXcm` (`configs.rs:754`) rather than `ProbeAwareResponseHandler`, so `reserve_probe_result` has zero production callers and no probe *pass* can ever be recorded — while `crank_reserve_probe` still latches fails regardless of `live()`. `unhealthy` is therefore a one-way latch any keeper can pull in ~2 probe intervals, and 08 §1.2 zeroes `spendable_nav` while it is set: a permanent, permissionless, chain-wide funding halt. Production binds `()`, `cfg(test)` binds the real sink, and **SQ-380** was raised for the unfinished B4 probe binding that actually unblocks the row. A runtime test pins the trapdoor so it fails loudly the day the dispatcher goes live. **Failure-first evidence:** the 3 oracle atomicity tests and the 4 constitution gate tests were each confirmed red against unfixed code before the fix (invocation disabled, real panics captured); the SQ-207 tests fail to compile without the new dispatchable. New tests: oracle 4, constitution 7, treasury 5, runtime 11 in the new `runtime/bleavit-runtime/src/tests_treasury_health.rs` (`tests.rs` untouched but for one now-fallible call site). PLAN's A13 Notes list was also missing three rows that name A13 as owner (SQ-158, SQ-180, SQ-207) — corrected. **A spec-compliance review then found 3 blockers in this branch's own change — all in exhaustive-match sites I had missed, all failing *closed*, one punitively.** `sweep_insurance` was absent from (a) `leaf_enabled`'s `TreasurySpend` capability arm, so `call_enabled` fell to the fail-closed `_` arm and `static_check` returned `SlashAll(ConstitutionViolation)` — **a lawful sweep would have confiscated 100 % of the proposer's intake bond**; (b) `derived_treasury_ask`, so the ask was `None` → `Refund(ProcessHold)` and `in_cap_prize` `None`, blocking Adopt at sizing; (c) `derive_resource_inner`, so the leaf was `Unclassifiable` and T4 cancelled the payload. Root cause of the miss: the first tests constructed `Origin::FutarchyTreasury` **directly** and never walked T4 screening. Fixed all three, and (c) required an R-1 amendment — 05 §1.4's key table is normative and exhaustive, so the leaf needed a real family: new singleton **`0x0B` Insurance reserve**, with the `0x0A` "one admissible zero-outflow Treasury leaf" sentence corrected to two. Added 3 screening-path tests (resource key, `static_check` → `Eligible`, `in_cap_prize` → `Some`), each confirmed red against the unfixed arms. The review's other findings also fixed: the `set_phase_flag` benchmark exercised `SUDO_PRESENT` (bit 4), which enters the gate **zero** times, so it was re-pointed at `CODE_META_ARMED` and both weight files raised (with a scoped `weight-regression-acks.toml` entry); the `sweep_insurance` benchmark could not execute in the assembled runtime (INSURANCE holds only `min_balance` under 03 §7 R-4, so `Preserve` refuses) — new `BenchmarkHelper::prime_insurance_custody`; `flag_nav_floor`'s doc comment falsely claimed a production caller (corrected — it still has none, now SQ-381); `pallet_constitution::Error::NavFloorUnmet` was unreachable dead metadata (the gate now maps onto it); negative-origin coverage widened to the full 06 §3.2 matrix row; `surface-manifest.json` re-trued. **The review argued SQ-205 should be closed now** by gating `crank_reserve_probe` on `ProbeDispatch::live()` and binding the sink — declined, because that decides SQ-385 (07 §8's "absence is never healthy" vs "not measuring") by implementation on a solvency path; it independently **confirmed** the SQ-380 facts. Five questions raised: **SQ-381…SQ-385**. Gates: `cargo fmt --all -- --check` clean · `cargo clippy --workspace --all-targets -D warnings` 0 · `cargo test --workspace` **1,172 passed / 0 failed** · weight-regression PASS WITH ACKNOWLEDGEMENTS (new `sweep_insurance` allowed; `set_phase_flag`'s intentional rise scoped in `weight-regression-acks.toml`) · limit-coverage 179 keys / 0 unwired · reference-model 39 + vectors + LMSR doc table · PLAN tables · batch index (318 rows, 68 open). Two runtime exhaustiveness gates correctly caught the new call and were updated (PoV inventory 104 → 105; S5 classification row).

**What comes next:** **SQ-380 is the gating follow-up** — until B4's production probe binding lands (response handler + real `ProbeSink` + live `XcmProbeDispatcher`, re-verified against live Asset Hub as V-6 did), SQ-205 cannot close and the `treasury.reserve_health_unwired` release blocker stands. Consider also whether `crank_reserve_probe` should refuse to advance state at all while `!ProbeDispatch::live()` (G-1).

## — (Batch C: integration contract v6 — **CLOSED**)

User: *"Resolve batch C completely by using multiple codex subagents intelligently… Decide on your own."* **All 35 rows disposed; open 108 → 77 on the merged base; contract bumped 5 → 6.** Seven parallel Codex verification agents checked every row against the code before any amendment was written, and **18 of 32 did not survive it**: 6 were already resolved by contract v4 and never closed out (a fifth of the batch was phantom work), and 12 carried false or stale load-bearing claims — SQ-212's "300×" is 100,000×; SQ-106's composite-vs-sequence residual does not exist; SQ-217 names two non-constants and misses the real one; SQ-210's out-of-domain sentinel claim is false; SQ-134's "permanent" latch is persistent-until-next-write. Two findings the rows understated carried the batch: **SQ-134** — a governance writer could **suppress a live pending upgrade**, refuting 02 §12's own risk text, rooted in a real §12 self-contradiction (offset 112 "current" in the field table, "target" a paragraph later); ruled to *currently installed, always*, with normative guard ownership, a merge-preserving `set_release_channel`, the latching predicate deleted, and new **I-30** in try-state. **SQ-88** — 05 §5.3's Baseline-carry path is **unreachable in production** (the adapter reads a cohort ring that cannot settle in time; the pallet test passes only via mock injection). **Process finding:** doc authors were launched before the code and two refused to document unshipped types — correctly; the order was flipped to code-first and the re-run reported **zero discrepancies**. Merged main's B14 (`73b9f6d`) mid-batch (one PLAN.md conflict; `tests.rs` auto-merged despite +540 lines there, re-verified green); B14 corroborated the SQ-108 ruling and broadened its facts to ten genesis-endowed accounts. Code: `f13f5d8` (v6 surface) + `9099d6e` (ReleaseChannel + registry events). Gates: fmt clean · clippy `-D warnings` 0 · `cargo test --workspace` 0 over 69 suites · release tooling 71/71 · plan-tables · batch index · runbooks.  **Dual review then found defects in the batch's own amendments — the fourth batch running for which that is true.** An adversarial Codex pass returned 4 blockers / 3 majors and a read-only spec-reviewer audit returned 0 blockers / 3 majors / 6 minors, corroborating each other on three findings. All fixed in-session: the release **surface manifest declared v6 but carried none of the 23 newly contracted events** (now 25 entries across five pallets, every layout verified against built v6 runtime WASM metadata, plus a test that parses 02 §6 so the drift fails mechanically); **`set_release_channel` never stamped `updated_at`** — it stored whatever block number the caller supplied, so my own §12 text claiming "each writer MUST stamp it" was an over-claim and four tests actively pinned the wrong behaviour (the dispatch path now stamps it and the tests pin that a caller value is ignored); **02 §6's inclusion rule contradicted 08 §1.4** — criterion (b) as first written would have contracted the keeper events that 08 explicitly keeps treasury-owned, so it is narrowed to a *frontend read obligation*, which then correctly pulls in `NavHaircutFlagged` (08 §1.2(4) requires the FE to surface it) and correctly leaves the keeper pair out; the "diagnostics named explicitly below" promise was false and is replaced by a rule; doc 03's `VaultState` never gained `BaselineSettled`; AGENTS.md and the derived design kit still published v5 with the removed `Pending`/`Failed` variants; four legacy epoch rows omitted the shipped `pid` under a strengthened "field shapes" claim; and the PoV total was off by the ring's one-byte length prefix (61,700 → **61,701**). **One review finding was itself wrong and is recorded as such:** the stale 81-byte `CohortSummary` weight bounds are **pre-existing** — the type was already 158 B at `2b1eb82` and batch C never touched it — so it is raised as **SQ-317** rather than absorbed here, together with two genuine batch-C weight residuals (a hand-edited `pallet_constitution` weight and un-re-benchmarked registry cranks). Changing 15 §1's heading for I-30 then broke a runbook anchor, which the runbook gate caught — the same class of self-inflicted gate break B6 hit.

**What comes next:** 3 rows stay **open in X** carrying their scope: SQ-88 (Baseline-carry source + real-schedule test), SQ-213 (`backing_complete` carry parity), SQ-312 (attestor for-cause removal — ruled option (a), deliberately not frozen in 02 §7.5 since a prose-only amendment would specify behaviour the runtime cannot perform; it still blocks the SQ-97 relaxation). Backlog is now **D** (30) and **X** (72); no ratify or contract batch remains.

## — (PR #124 review fixes + benchmark-smoke repair; no…

— (PR #124 review fixes + benchmark-smoke repair; no milestone)

**Codex P2 on PR #124 (valid, and understated).** It reported that `RolloverExhausted` (contract v6) was missing from the v6 client artifacts. Verified: true, and the root cause was upstream of what it flagged — the variant had been added to **02 §2 and the code but never to `05` §1.3**, the *owning* section for `RejectReason`, whose producer map is explicitly normative ("an implementation MUST NOT emit a variant from any site not listed below"). Shipped code emitted a variant the owning doc did not define, and 02 and 05 disagreed about a frozen enum. Fixed at the root: 05 §1.3 gains the variant + its producer row; **§2.1 gains T26** (`Screening → Cancelled(RolloverExhausted)`, full refund) — the transition SQ-166 originally asked for and which the batch had left out of the "exhaustive" table — plus the §2.2 mermaid edge, the T6 row, and the rollover-exhaustion paragraph; code/spec citations re-pointed T6 → T26. Rather than hand-patch the manifest, dumped **real v15 metadata from the built runtime** and diffed every entry through the release pipeline's own `surface_layout`/`compare_layout`: **10 stale layouts, not the 7** implied — 8 caused by this branch (7 `RejectReason` + `event.guardian.action`, which shipped in `84b92a5` already missing `ActivatePlaybook.target`) and **2 pre-existing since B8 `d7a727c`** (`storage.guardian.members` typed `[[u8;32];7]` vs the real `[Option<[u8;32]>;7]`; `event.system.code_updated` recorded field-less vs the real `hash: H256`). All 10 rewritten from metadata, re-audited to **0 mismatches**; the 2 inherited ones were fixed rather than filed because the correct value is mechanically determined, not a judgement call. Design kit trued (16 → 17 reasons). **Separately, `Benchmark smoke` was red — on two benchmarks, one of them this branch's own.** Benchmark bodies execute only under `runtime-benchmarks` via the bencher, so `cargo test --workspace` cannot see either. **(1) `pallet_futarchy_treasury::fund_budget_line`** (`100000010000 != 100000000000`, inherited from main): B14 genesis-endows every 03 §7 R-4 custody account with `min_balance` (`USDC_CENT` = 10 000), so the Keeper payout pot no longer starts at zero on the real runtime — the absolute-balance assertion was stale, not the runtime; re-expressed as a funded **delta**, equally strict and valid under both the endowed runtime and the un-endowed mock. **(2) `pallet_constitution::set_release_channel`** (`1 != 9`, **introduced by batch C**): SQ-134 made this call merge rather than replace, so `updated_at` is chain-stamped and `spec_version` guard-owned — but the benchmark still asserted the caller's forged values round-tripped. Now `assert_ne` against the forged 9 and 3, which is the actual SQ-134 property; equality asserts are wrong here because the surviving values are environment-owned (the dispatching block, and the *installed* `spec_version` — 0 under the mock, 1 on the real runtime), and the harness does not hold one block height across setup/dispatch/verify. The exact stamp stays pinned by `pallets/constitution/src/tests.rs`. **Method note worth keeping:** the targeted re-run of (1) passed and would have been taken as done — the full `--all` sweep is what surfaced (2), and PR #124's own CI independently listed exactly these two. Verified by reproducing CI's command verbatim against a `runtime-benchmarks` wasm with the CI-pinned `frame-omni-bencher 0.22.0`: **321 benchmarks / 30 pallets green**, then full `rust-workspace-gates.sh` green (74 suites).

**What comes next:** — (no milestone opened; PR #124 review response)

## Batch X wave 1 (code)

**All 77 batch-X rows verified against HEAD before anything was written — 27 carried load-bearing facts that were wrong; the review round then found 1 blocker and 6 majors and backed 2 of the 9 code fixes out.** Eight parallel read-only passes, then five parallel implementation lanes plus a runtime integrator. **16 rows disposed:** 7 disproved and closed with no code (SQ-93, SQ-142, SQ-177, SQ-190, SQ-257, SQ-302, SQ-307 — SQ-307 had stopped tracing one layer short of the `WelfareLedger` adapter that has composed the latch since B10), **7** fixed in code (SQ-40, SQ-42, SQ-127, SQ-144, SQ-313, SQ-314, SQ-315). **SQ-66 and SQ-197 were reverted by the review round and reopen** — SQ-197's fix foreclosed 13 §115's ordinary-amendment path and made both Phase-3 caps permanently un-raisable; SQ-66 shipped a `CountedStorageMap` with no migration, a prune blind to cohort-less epochs, and a +3733 % `create_market` read regression. The review also caught a **red CI gate the batch never ran** (`check-weight-regression.py` is its own CI job, absent from `rust-workspace-gates.sh` — now wired in) and split 2–1 on SQ-314's partition, which ships on the conservative cohort-membership reading with the contested population raised as SQ-319. Six new rows: SQ-318…SQ-323, of which **SQ-320** (an epoch that never forms a cohort strands its Baseline holders forever) is the most severe thing found and not fixed. **25 ruled** under the standing delegation and **19 routed** to nine new milestones (**A12, A13, B15–B20, O6**; B15–B19 fulfil the rows batch B2 promised and never wrote). The batch's biggest find was a **spec self-contradiction batch D introduced**: 05 §2.1 required preserving an already-decided proposal's `DecisionOutcome` through cohort VOID while §7(4) required overwriting it — 03 §2.3 and 07 §10 back §2.1, so §7(4) was amended and `void_cohort` now partitions the affected set (SQ-314, a truth defect: the archive was recording rejections the market never produced). SQ-40 was the other severe one — an undefined prize proxy failed the `decide` *dispatch* instead of returning the spec's terminal `Reject(SecuritySizing)`, so the proposal could never resolve. Spec amendments: 05 §7(4) and 05 §1.4 (the new singleton `0x0A` XCM trap-recovery family, SQ-244/SQ-316, spec-ahead-of-code). No 02 surface touched ⇒ no contract bump (still v6). Removing `WrongBranch` shifts ledger FRAME error indices 12→11 … 19→18 — verified not a frozen-02 surface, and a new metadata test pins the ordered error surface.

**What comes next:** Wave 2: the runtime/XCM cluster — SQ-129, SQ-205, SQ-207, SQ-244/SQ-316, SQ-308 lead on severity

## tooling — CI parity checker

**`tools/ci/check-ci-parity.py`** — the direct remediation of the Codex finding on PR #125. Adding `check-weight-regression.py` to `rust-workspace-gates.sh` broke the Rust CI job: the checker defaults its base to `git merge-base HEAD origin/main`, the dedicated `Weight regression` job resolves it only because it sets `fetch-depth: 0`, and the Rust job's bare `actions/checkout@v7` fetches just the triggering commit. Every local gate run stayed green because the worktree had `origin/main` fetched — **running the same script locally is structurally blind to this class of failure**. The checker runs each environment-sensitive gate in the working tree and again in a shallow single-branch clone shaped like `actions/checkout@v7`, and fails on any that diverge. Its `GUARDED` suppressions re-read the guard's own source pattern and lapse when the guard is deleted — the first draft trusted the allowlist blindly and reported PASS on exactly the state it was built to catch, which is pinned as a regression test. Deliberately **not** a CI job (CI already is the CI environment); its 16 unit tests run in the tooling-suites job.

**What comes next:** Wave 2 of batch X — the runtime/XCM cluster

## tooling — CI parity checker (review round)

**Codex review on PR #126 found the checker had rebuilt its own blind spot one level up (P2, valid).** `gate_commands` deduplicated call sites on the **command alone**, while `GUARDED` is keyed on `(script, checker)` precisely so a new unguarded invocation elsewhere still fails. Because `rust-workspace-gates.sh` is scanned first and carries the *guarded* `check-weight-regression.py`, an unguarded copy added later to another gate script would have been discarded before `resolve_guard` was ever consulted — the run would report the guarded site as handled and pass. The code's own stated invariant was false. Dedup key widened to `(command, guard_reason)`, so equivalent sites still collapse (three standalone gates are also embedded in `rust-workspace-gates.sh` and must not run twice) while differing guard handling keeps both. Regression test asserts both call sites survive; it fails `1 != 2` against the pre-fix code. Also recorded an honest residual: a guarded and an unguarded call **in the same script** are indistinguishable to pattern-matched guard detection, so that case is documented as out of scope rather than left looking covered.

**What comes next:** Wave 2 of batch X — the runtime/XCM cluster

## follow-up to batch X wave 1

**Removed three now-expired weight-regression acknowledgements.** They were correct while #125 was open — the `pallet_epoch` measurement corrections genuinely regressed against the pre-batch base — and the checker's acks are designed to **expire mechanically**, so once #125 merged they had to go. The asymmetry is easy to miss: on `main` the checker resolves its base to main's *parent*, where the regressions still exist, so main stayed green; every PR branched off main resolves to the merge commit, sees no regression, and fails with STALE ACKNOWLEDGEMENT. Left in place they would have blocked every subsequent PR.

**What comes next:** Wave 2 of batch X

## tooling — doc-links gate scoping

**`tools/ci/check-doc-links.py` was scanning git-excluded paths, producing ~356 phantom failures no CI run could ever reproduce.** The walk is a filesystem walk (deliberately — an uncommitted doc must still have its links checked), but `SKIP_DIRS` listed only `.git`/`target`/`node_modules`, so it descended into every nested worktree under `.claude/worktrees/` and read their stale `docs/` copies. Those copies also defeat `link_base`, whose verbatim special case is anchored at *this* checkout's `VERBATIM_DIR` and cannot match a copy under another worktree — so every verbatim link in them resolved against the wrong directory and was reported missing. CI never saw any of it (clean checkout, no worktrees), which made it local-only noise: the worst kind, because a gate that cries wolf on a developer's machine is a gate they stop reading, and this one guards the living documents. Fixed by asking git itself (`git check-ignore -z --stdin`, one batched call) rather than hardcoding a directory name, so `.gitignore`, `.git/info/exclude` and any future excluded path are all honored while untracked-but-not-ignored files stay checked; absent git the checker falls back to checking everything (over-report, never under-report). The skipped count is now printed — a shrinking denominator must never be silent. **The checker had no tests at all**; added `tools/ci/tests/test_check_doc_links.py` (6 cases, driving the real script as a subprocess against throwaway git repos) pinning both directions: excluded files skipped, untracked files still checked, broken links still failing. Both new cases were confirmed red against the pre-fix script (`1 != 0 : git-excluded file was still checked`). Gates: `tools/ci` suite 66 tests OK; checker clean at 82 files.

**What comes next:** Fourth instance this session of a gate that looked green while measuring the wrong thing — after the stale epoch weights, the unrun weight gate, and the parity checker's dedup

## — (batch X: oracle / registry / attestor cluster; no…

— (batch X: oracle / registry / attestor cluster; no milestone)

Ten rows worked (SQ-293, SQ-294, SQ-295, SQ-296, SQ-312, SQ-262, SQ-263, SQ-248, SQ-249, SQ-215). **Verification first, on all of them, before any code** — four parallel read-only passes with file:line evidence — and it again changed the picture on a third of the cluster. **Two rows arrived already resolved** (SQ-248, SQ-249, both ratified 2026-07-20) and were context, not work. **One row is half disproved: SQ-215.** Its \"13 §4 must own the attestor bounds, per bounds rule 3\" premise is false twice over — rule 3 is the client *machine-readability* rule, and rule 1 carries an explicit carve-out putting per-pallet storage bounds with their owning pallets (02 §7), which is exactly where `MAX_ATTESTORS`/`MAX_ATTESTATIONS` live. **No 13 §4 row was added**, avoiding pointless limit-coverage churn; the liveness-trap half is real and its residual is scoped. **One row's premise inverts: SQ-312.** It reads as though SQ-97's dispatch relaxation is in force and needs protecting; 06 §7 and 09 §1.2(5) both say in normative text that it is **NOT in force**, and the runtime accordingly reads the live roster. Needs contract **v7**, so deliberately not implemented — but verification found a real doc defect: 09 §2.4's *Gating* bullet stated the relaxed split flatly, contradicting both, and is now trued (plus the stale conflict note in `pallets/execution-guard`). **Citations were stale on three rows** (SQ-294, SQ-295, SQ-296 — six `configs.rs` line numbers, `registry-core` `required_bond` and `resolve_challenge`), and **SQ-295's quoted doc sentence does not exist**: 07 §5.4 was already narrowed by batch D. **Shipped: SQ-262 (resolved).** Seating no longer destroys liability — a departing attestor with an unsettled liability is retained as an inactive `AttestorInfo` row, using the already-frozen 02 §7.5 `active` field, so **no storage reshape and no contract change**. Verification refuted the row's own \"bond locked in limbo\" framing (attestor bonds are arithmetic-only; no custody exists), and the real harm is worse than recorded: the challenge could never terminate, permanently suppressing the attestation from quorum and shielding the attestor from a strike — SQ-248's recorded residual. Fixed a second, unrowed defect in the same call: `false_count` reset to 0 for continuing members, so one no-op re-election wiped every strike and 06 §7's ejection threshold was unreachable. Two try-state invariants added. **Shipped: SQ-295 (half).** The classifier labelled registry `resolve_challenge` `CallDomain::Public` while the runtime restricts it to `EnsureOracleResolution` — the authority matrix imposed no constraint and the call rode the proxy/multisig wrapper set that rejects `oracle.adjudicate`. Both instances now carry the real domain, admitted in `is_values_enactment_leaf`. Defence-in-depth restored, not a live escalation (the pallet `EnsureOrigin` always bound). **Blocked, with the blocker now precise: SQ-294 + SQ-296.** 07 §7's \"recompute_proof, **else** escalation\" collapses — registry filings have no `formula_ref`, so the replacement path *is* escalation, which needs SQ-174 and S4. `StakeAtRisk` is a two-valued sentinel (`Balance::MAX`/0) that overflows `round_bond`, so no bond can be value-scaled off it today. Removing the authority without a replacement would be strictly worse: `crank_close` never auto-resolves a `Challenged` filing, so stranding it wedges `close_epoch`, the aggregate and the welfare input. **Not implemented, deliberately: SQ-293 + SQ-263** (both re-verified TRUE on every claim; milestone A12). No custody type is wired into either pallet, so this is milestone-sized, and the attestor leg hits an unanswered spec question rather than a wiring task — raised as **SQ-340** rather than invented (R-2). Reachable-today holes recorded: registration stakes and attestor challenges take no funds at all; the round-bond hole is masked only by SQ-174's sentinel. **Raised: SQ-340** (attestor bond consent — blocks SQ-263's attestor leg), **SQ-341** (07 §6.3's admission rule enforced nowhere; `MetricSpec` has no `Δs_max` field, `can_admit_attested_component` is test-only dead code), **SQ-342** (two unreachable `pallet-attestor` error variants vs 15 §4.1). Open 69 → 71 (1 resolved, 3 raised). Gates: `cargo fmt --all --check` clean · `cargo clippy --workspace --all-targets -D warnings` 0 · `cargo test --workspace` **69 suites, 0 failed** · `check-weight-regression.py` PASS (315 functions) · limit-coverage 179 keys / 0 unwired · `fuzz-gates.sh` exit 0 · PLAN tables · batch index 320 rows.

**What comes next:** **SQ-294 + SQ-296 land together** once SQ-174 supplies a frozen exposure source and S4 the calibrated bps; SQ-295's ruled endpoint rides them. **SQ-340 must be ruled before SQ-263's attestor leg**; SQ-263's oracle leg is unblocked and is the highest-value next increment. SQ-215's reap belongs on the guard's existing `cleanup_terminal` seam with a re-benchmark.

## SQ-320 (batch X wave 2) — **in flight, not done**

**Spec amendments landed; implementation and gates still running on `fix/sq-320-orphan-baseline`.** Recorded here per R-3/R-10 so the next session inherits true state — this row does **not** claim a green milestone. Design ruled: a permissionless `pallet-epoch::finalize_epoch_baseline(epoch)` crank settling an orphaned Baseline at the neutral `s = 0.5`. The T20-chokepoint alternative was rejected on three grounds, the third decisive: it taxes `tick`/`decide`/`force_reject_process_hold` weight (likely tripping the 10 % regression gate on `tick` proof size, buying an ack row that expires on merge), it needs `WelfareOps` threaded through ~8 core functions, and it develops a reachability hole the moment 08 §4.3's zero-qualified-proposal Baseline is honored — there is no T20 to hang the settlement on in a proposal-free epoch. **Verification corrected three load-bearing facts in the SQ-320 row** (see the row): `BASELINE_BOOKS = 4` is dead code, the enforced caps are 196, and the real harm is a permanent `pol.b_baseline` POL commitment that irrecoverably lowers the `spendable_nav` gating **all** future proposal funding. The spec lane found a third blocking passage nobody had listed (06 §3.1) and established that `PB-LEDGER-FREEZE` is a far broader trigger than the row described, making freeze-callability normative rather than a nicety. New row **SQ-324** raised: 08 §4.3's "must exist even in an epoch with zero qualified proposals" is not implemented — books are created lazily in `open_markets` — but the sentence's own rationale is about funding independence, so it is logged as an ambiguity, not a defect. No 02 surface touched; contract stays **v6**.

**What comes next:** Land SQ-320 once gates are green, then unblock SQ-66 (which carries the same corrected four-slot error); oracle/registry/attestor and XCM clusters running in parallel worktrees

## living docs — permission surface

**`.claude/settings.json`'s `ask` list was emptied (the standing `Bash(git push*)` prompt removed by the user), which falsified CLAUDE.md's "`git push` always asks".** Recorded per R-3 because it is a tracked, shared file and a workflow change, not a personal preference — `.gitignore` excludes `.claude/settings.local.json` for that, but not `settings.json`. CLAUDE.md re-trued, with the distinction made explicit: the permission prompt is gone, but **AGENTS.md R-9 still governs the judgement** — commit and push only under an ask or standing instruction, never publish or tag without an explicit one. Removing the prompt removed the reminder, not the rule. No other living document referenced the push prompt.

**What comes next:** Nothing owed; noted so a future session does not read CLAUDE.md's old line as still describing the gate

## SQ-320 follow-up (15 §4.1 TLA⁺ Baseline liveness +…

SQ-320 follow-up (15 §4.1 TLA⁺ Baseline liveness + keeper crank) — **both closed**

**Both verification obligations the SQ-320 fix created are discharged (R-8), with runner output recorded rather than asserted.** **(1) 15 §4.1's normative Baseline-settlement-liveness obligation.** `models/tla/proposal/Proposal.tla` carried **zero** occurrences of Baseline, so the model was red against its own spec. Liveness is the only detector that can exist here: a stranded Baseline vault is fully collateralized, satisfies I-1 and I-4, and lets full-pair holders exit at par via `merge_baseline` — **no conservation invariant could ever have caught SQ-320**, which is exactly why the amendment put the obligation on the model. The obligation is a *possibility* statement (AG EF `Settled`) — branching-time, with no direct expression in TLC's linear-time property language — so it is discharged as an equivalent safety invariant `BaselineNeverStranded` over a four-field Baseline sub-machine (`state`, `via`, `epochPast`, `doubleSettle`), resting on two facts every run re-establishes: the new `TerminalityAlwaysReachable` (fact (i) made mechanical instead of argued — every reachable non-`Terminal` state is `Absent`, `PreExecutedNonTerminal` or `Measuring`, the three families that close into `Terminal`) and `AdvanceEpoch` being unconditionally enabled while the vault is `Open`. `BaselineSettledExactlyOnce` is the converse guard — the vault settles once, via one of the three normative transitions, never before its cohort could still form. **Non-vacuity proved in both directions, as the runner demands:** `MutationOrphanBaseline.cfg` deletes the §7(6) crank and TLC violates `BaselineNeverStranded`; `MutationBaselineLiveState.cfg` gives the crank the rejected `is_live_state` terminal reading and TLC violates `BaselineSettledExactlyOnce`; and three reachability witnesses violate as required — `WitnessBaselineOrphanCrank.cfg` on the exact interleaving 15 §4.1 names (T1, then T20 before any `Measuring`, epoch past, crank settles), plus the measured-T19 and cohort-void discharges. Floor raised 33,350 → 70,600 (half the new Small count, the README's stated rule); Small 66,709 → 141,218 distinct states, Full 264,241 → 545,562, ForceRerun 568,235 → 1,169,316. **Recorded honestly:** the model opens the Baseline vault in `Init`, which over-approximates the runtime's lazy T7 seed inside `open_markets`, so a mutation trace can strand a vault after a T2 withdrawal the chain would never have opened one for. Sound (03 §5.2 and 05 §7(6) both make an absent vault a no-op) and documented in the model and its README. **(2) The keeper planner arm** (01 §4.2 — the keeper cranks every permissionless extrinsic). `finalize_epoch_baseline` rides the existing `Role::Settle` rather than a new role: 05 §6 makes it the second `pallet-epoch` trigger of the one `compute_settlement → settle_baseline` chain under the same single SettleAuthority — a second trigger, never a second authority — and a new role would have falsified `deploy/runbooks/RB-KEEPER.md`'s shipped-role list, outside this change's edit scope. Metrics therefore come for free, per-role as 12 §6.3 requires. The trigger is the shipped three-part precondition narrowed by the one cheap necessary read (a new `ConditionalLedger::BaselineVaults` extraction): plan only against a still-`Open` vault whose epoch is strictly past, carries no `CohortInfo`, and holds no non-terminal proposal — never a standing per-block no-op. Priority 150, above `cleanup`, because it writes the terminal latch the Baseline sweep and book reap both require. **Checked, not assumed:** `settle_cohort` settles Baseline(e) as its last cursor target and only then archives the summary and drops `CohortInfo`, so no reachable state has an archived summary over a still-`Open` vault — which is what makes the omitted `RecentCohortSummaries` read sound.

**What comes next:** Nothing owed on either obligation. `tools/verify/run-model-checks.sh` green end-to-end and the keeper leg of `tools/ci/rust-workspace-gates.sh` run verbatim (fmt, clippy `--locked`, test `--locked`) — the runtime-workspace legs are untouched by this change, which lands only in `models/` and `keeper/`

## Batch X — A13 constitution/params cluster (SQ-36,…

Batch X — A13 constitution/params cluster (SQ-36, SQ-117, SQ-150, SQ-158; SQ-186 partial)

**Four rows resolved, one partially (blocked on a contract bump); verification-before-implementation held.** All five rows re-checked against HEAD first — every ruling's premise survived, and the SQ-186 residual the row already flagged (metadata exposure) was confirmed to need a 02 §9 name → v7 bump, so only its non-02 half shipped. **SQ-36:** `ledger.pos_dep` frozen `max == min == 0.1 USDC` in `constitution-core::genesis_params`; 13 §1 + new 13 §2 freeze note record the L-2/L-6 misappropriation hazard (a transfer deleting one entry while creating another over-refunds the old owner while L-2 stays balanced). **SQ-117:** `keeper.rebate` genesis-seeded (3×/1×/10×) off a single kernel fee-basis constant `KEEPER_REBATE_FEE_BASIS_USDC`; **value stays `[VERIFY]`** (placeholder 0.03 USDC, 08 §6.2) pending launch `fee.vit_usdc_rate` — mechanism built, no calibrated number invented (V-44). **SQ-158:** `dis.merit_min` seeded as an independent META lever (floor `orc.bond_floor`, ceiling `Balance::MAX`, ×2, 2-epoch); `any_open_dispute_touching` now binds `round.bond >= frozen_b1.max(live dis.merit_min)`, fail-closed on malformed schedules preserved; 07 §12 + 13 §1 trued. **SQ-150:** `can_amend_registry` is `FutarchyMeta`-only; kernel rows immutable (`KernelBoundImmutable`); `amend_registry` removed from `is_values_enactment_leaf` (I-8 crossing closed); 06 §2.1/§3.2 trued; the three `#[ignore]`d S5 regressions **replaced** by real dispatch/classifier pins + pallet positive-META/negative-values/negative-kernel cases; `AmendRegistryScope` conditional retired. Every regression written first and confirmed red pre-fix. Gates green: `fmt` · `clippy -D warnings` (workspace) · `check-limit-coverage` (180 keys) · limit-coverage unittests · targeted `cargo test` (pallet-constitution 47, constitution-core, runtime SQ suites) — full `cargo test --workspace` + weight-regression + plan-table/batch checkers reported in the handoff. **SQ-186 residual (metadata-constant exposure) left open for the contract-bump PR.** New genesis keys shift no stored shape (genesis fixtures regenerated); `SingleBlockMigrations = ()` untouched.

**What comes next:** Next: SQ-186 metadata exposure rides the v7 contract-bump batch; A13's remaining reward-line/POL-scaling rows (SQ-173, SQ-270, SQ-298, SQ-300) unstarted

## A12 (welfare-input half) — SQ-79, SQ-82, SQ-201 fixed;…

A12 (welfare-input half) — SQ-79, SQ-82, SQ-201 fixed; SQ-195, SQ-181 verified and left open

Five rows taken verification-first on branch `fix/welfare-inputs`. **Three fixed, each with the pre-fix failure captured verbatim before the fix was written.** **SQ-79** — gate-market settlement resolved a cohort whose entire e+1…e+2 window carried no daily observation to "no breach" (measured: `Ok(FixedU64(1000000000))`, both gates false), paying an adopt-favourable claim out of absent data. 05 §4.7 gained the normative zero-sample paragraph; `welfare_core::gate_window_outcomes` now refuses (`GateWindowUnsampled`), scoped to gate books so scalar/Baseline still settle and the 02 §4 view default is untouched; partial coverage stays deliberately unclassified and is pinned by a test. Entry *presence* in the frozen `GateBreachFlags` already distinguished sampled from unsampled, so **no 02 §7.4 shape changed**. **SQ-82** — the live residual is the boot-at-zero ambiguity, not the activation relaxation the headline suggests: `current_epoch == 0` is satisfied by both the genesis build and a live `register_spec` against an unset clock, and the latter measurably inherited the relaxation (`Ok(())` for `activation_epoch = 1` at clock 0). `register_metric_spec` now takes an explicit `Registration::{Genesis, Live}`; 05 §4.4 amended. **SQ-201** — confirmed `pallets/epoch/src/lib.rs:1206` is the only `Welfare::prune` caller and it sits behind the cohort-reap branch, so a cohortless epoch is unreachable by cleanup; measured 120 cohortless rolls growing gate history to peak 120 against a bound of 20, and the 21st snapshot failing `TooManySnapshots`. Spec first (05 §3.3 pruning-ownership paragraph, protected set fixed to the *existing* `current - 19` window so the roll path can retire nothing reap would have kept), then `prune_epoch_roll` — cursor-bounded, oldest-first, snapshot-deadline epoch protected — wired from the runtime seam tick already calls every roll, so **`pallets/epoch` was not touched**. No storage item added or reshaped anywhere in this branch, so **no migration and no `StorageVersion` bump**. **Two rows deliberately left open with corrected premises (R-10).** **SQ-195**: the row assumes only the *store* is missing; in fact the probe is not dispatched in production at all (`RuntimeProbeDispatch = ()`, `ResponseHandler = PolkadotXcm`), so the store would have no writer and 07 §8's "no probe sent ⇒ fail" would drive `R = 0` every day — a permanently-set C breach settling every gate book as breached from a mechanism never armed. That is fail-destructive, so no code and no spec edit; blocked on the B4/oracle cluster. Day attribution is *not* the blocker (`xcm_traffic_epoch_and_day()` already does it). **SQ-181**: real, but larger than a defect fix — 05 §4.6 normalization is entirely unimplemented (`prior_bounds` stored, read by nothing) and production genesis registers no `MetricSpec` at all, so the P pillar has no normalized form to deliver and nothing consumes any input today. Scope split recorded in the row. **New:** SQ-420 (stale `prune_xcm_traffic` trait name + the `pallet_epoch::tick` weight now understating the added bounded prune — release-blocking regeneration). Spec-reviewer verdict **COMPLIANT** (0 blocker / 0 major / 2 minor, both already SQ-420); it also caught that 07 §10 has no *automatic* VOID for a keeper-unsampled window, so the §4.7 text was corrected to describe the real recovery and **SQ-421** raised for the automation question. Gates green: fmt · clippy `-D warnings` · `cargo test --workspace` · runtime-benchmarks build · try-runtime suite (250) · weight-regression · limit-coverage (179/179) · reference-model 39 + vector freshness + doc-table · fuzz-gates · plan-tables · spec-question batches.

**What comes next:** SQ-195 unblocks with the B4/oracle probe wiring; SQ-181 needs the 05 §4.6 normalization layer first; SQ-420(b) before release

## PLAN truing — B17 status, SQ-317 headline

**Two PLAN rows were carrying facts the code disproves; both corrected after independent verification, no production change.** (1) **B17 was ⬜ but is done.** Its row owns exactly SQ-127 and SQ-144, both of which read `resolved 2026-07-21 (batch X) — fixed in code`; the anchor is live and covered — `PreMigrationAnchor` storage at `pallets/execution-guard/src/lib.rs:496`, captured `:610`, killed on both terminal paths `:1067`/`:1511`, try-state `:1983`, regression at `pallets/execution-guard/src/tests.rs:1245`. Batch X wave 1 delivered the milestone and never flipped its status, so the roadmap overstated remaining Track-B work by one. (2) **SQ-317's headline is false at HEAD.** It claims epoch and execution-guard weights "still declare `max_size = 2593` and a `3088` proof contribution"; neither literal appears anywhere under `runtime/bleavit-runtime/src/weights/`, and those two files are the *only* ones dated `2026-07-21` — wave 1 regenerated exactly them. The row's own status paragraph already conceded this, but the headline would have sent an agent to re-fix the two current files while the genuinely stale ones (`pallet_constitution.rs` 2026-07-16, `pallet_registry.rs` 2026-07-17) went untouched. Corrected in place; SQ-317 stays **open** because the substantive regeneration is still owed, and it must be scheduled **last** in its wave since every cluster that changes a benchmarked call invalidates it.

**What comes next:** Continue wave 1: treasury reserve-health (SQ-205 release blocker), oracle/registry custody, XCM caps, env-evidence depth — all in parallel worktrees off main

## **O6** ✅ (batch X: SQ-203, SQ-204, SQ-283)

**Zero-Rust cluster: the environment-evidence bundle's two execution-depth gates plus the relay-finality blind spot.** Verification came before implementation and all three rows survived, but two were *worse* than written. **SQ-203** — the row said card assertions do not execute; verification also found `--include-gated` missing from `report_only_reasons`, so a release-tier run could emit `bleavit.env-evidence.v1` naming a scenario whose card had never run. Fixed by giving every scenario card a machine-readable ` ```card-assertions ` block (one entry per numbered step, exactly one of `execute` / `blocked_on` / `discharged_by: try-state`), executing it in the runner, and recording the `card` check only when every assertion ran; `validate_release_evidence_completeness` refuses evidence lacking it and `validate-environments.py` gates the shape. All 10 cards encoded from their own prose + NOTE(B7) blockers — every one still carries `blocked_on` steps, so the suites stay gated, but now mechanically and self-expiring instead of by a hand-maintained `gated_on` string. **SQ-204** — the row said the closing try-state leg was deferred; in fact `TRY_STATE_CHECK` was *required* by the completeness check while **no code path ever set it**, so evidence emission was structurally impossible. Both halves landed: `try-runtime-cli` v0.10.1 + digest pinned in `pins.env` and installed by `fetch-binaries.sh` (V-40 — resolved against the live release API, no sidecar exists so the digest was computed from the fetched asset), and the keep-alive `zombienet test` lacks, read from the **pinned CLI's own source** rather than upstream docs (V-41): `--monitor` is on `spawn` only, `test` accepts a running-network spec, the orchestrator writes `zombie.json` — hence spawn-monitor → drill → closing check → group teardown. Chopsticks needed no keep-alive (the runner owns that process). The endpoint comes from the topology's pinned `rpc_port` and **fails closed** when none exists. **SQ-283** — confirmed with file:line evidence that the chain exporter is anchored entirely on the *parachain* finalized head, so `bleavit_chain_tick_lag_blocks` freezes rather than fires during a relay GRANDPA stall, and that zero relay series existed anywhere. Landed a **separate** exporter (separate process/endpoint/failure domain, polling not subscribing — a finalized-head subscription is silent during exactly the monitored event), a 21st 12 §6.3 row bound to RB-KEEPER with Infrastructure escalation, and the full wiring (coverage gate 20→21, inventory, rule, Prometheus job, runbook, READMEs). **The persistence window was deliberately left `[VERIFY]`** at a conservative 1,800 s (V-42): it depends on observed healthy relay behaviour that no local source can supply, so inventing a calibrated number would have violated R-2. Also trued the stale "20 §6.3 rows" claims in AGENTS.md and README.md. Gates green: env validator · env tests **72** (was 51) · monitoring **88** (was 68) · alert coverage 21 rows/35 metrics · runbooks 13/21 · deploy 82 · release 71 · ci 60 · doc links · PLAN tables · batch index (66 open / 251 resolved). **Honest limits:** nothing here ran against a live environment — no zombienet or chopsticks binary, no built runtime Wasm, no relay endpoint exists in this worktree, so the new execution paths are covered by fakes and unit tests and get their first real exercise at G2. The 5 chopsticks-execution tests still skip locally because the installed `websockets` is **9.1**, not the pinned 15.0.1 (`websockets.sync.client` does not exist below 12), and installed `pyyaml` is 5.4.1 rather than 6.0.2.

**What comes next:** G2's first real execution of the two new legs; Ops to calibrate the V-42 window; `bleavit-xcm.toml` needs a pinned collator `rpc_port` before drill 07 can run its closing check

## X (spec questions)

**Batch X wave 2 — the XCM cluster: SQ-129, SQ-244, SQ-316, SQ-308 all verified real at HEAD, then fixed.** Verification first, per the batch's own lesson: every row was reproduced with a test that failed before any code was written, and one row's premise was materially corrected — SQ-129's spec side had **already** been amended normative on 2026-07-20 (09 §5.2 "Both caps MUST be enforced before any local mint"), so this was a pure implementation lag, not an open question. **SQ-129:** a `DenyOverCapInflows` barrier entry now refuses over-`phase3.dep_cap` inbound programs before VM construction, using a new pure `inflow_admissible` read; the deleted test had pinned the exact permanent stranding the spec forbids (a cap refusal trapping under Asset Hub's origin, unreclaimable by its beneficiary). `ClaimAsset` stays exempt (§5.2 mint-step scope, SQ-253) and `phase3.tvl_cap` keeps its binding mint-leg point. **SQ-244/SQ-316:** wave 1's 05 §1.4 `0x0A` singleton landed in code — footprint arm, zero-outflow ask arm, and a Treasury-only capability arm whose necessity was confirmed empirically (PARAM admitted `claim_assets` through the generic Public fallback before the fix — the I-8 class confusion, live). Proven end-to-end through submit→screen→qualify→queue→execute. **SQ-308:** screening now mirrors the guard's `enqueue` domain preconditions via a shared `domains_admissible` helper, making `decide` total; the canonical top-level upgrade payload is pinned eligible so the mirror cannot over-reject. **The review round earned its keep again:** an adversarial spec-review of the barrier found **two blocker evasions** in the first cut of the SQ-129 gate — a `WithdrawAsset`-fed program (fills holding with no mint) and `DepositReserveAsset{dest}` (a second metered local deposit leg, reached with an empty beneficiary list and a vacuously-true `all()`) — both of which reproduced as real mints-then-strands, and **one wrong disposition**: SQ-308 shipped a 100 % slash where 05 §2.1's T4 taxonomy mandates refund ("confiscation requires a verified culpable act"). All three fixed before commit, with the two evasions pinned by regressions that failed against the first cut. Gates green incl. the full `rust-workspace-gates.sh`; `phase3.dep_cap`'s limit-coverage marker was re-bound to a test covering both live enforcement points and its registry `reason` re-trued; `0x0A` added to the 05 §1.4 singleton-collision test. Four surface/taxonomy questions the review surfaced are logged as SQ-360…SQ-363 rather than resolved unilaterally.

**What comes next:** X batch: 69 rows (65 after closing 4, +4 raised)
