# Session log — 2026-07-26

## **SQ-494 ✅** (branch…

**SQ-494 ✅** (branch `feat/sq494-money-settled-hold-scope`)

**The predicate change is three lines; the row's real content was a benchmark that had never measured the predicate at all.** 07 §12 held a decision on any challenged round above the merit floor, with no exclusion for one whose money leg is already neutral — so a §11(1)-retained round kept holding for its whole retention window while I-18 had already fixed the value it was protecting. Closed by testing the spec's own definition of non-money-bearing (a settled `ComponentValues` entry for the round's triple), which also keeps the decision path off the oracle's internal latch and its storage shape — so this landed **independently of #175** rather than waiting on it, and the exclusion sits *after* the merit floor so a sub-merit round never pays the read. **One of the row's own words was wrong in the safe-sounding direction, and I only caught it by following `process_hold` to its consumer:** it reaches `Rejected(ProcessHold)`, which is terminal (T10/T20). Proposals reaching their decide window are **killed and must be resubmitted**, not deferred until the dispute clears — so the §11(4) griefer whose price is stated as \"buys only status-quo settlement\" was also buying the death of every proposal consuming that component. **Then the fixture gap, the fifth instance of that shape:** `decide`'s benchmark seeded zero `Rounds`, so the entire §12 predicate ran against an empty map. Honest fixture moves `decide` 270 → 655 reads, decomposing exactly as 128 scanned + 64 schedules + 64 component values + 1 param — **only the 64 component-value reads are this change**. Two fixture errors were caught and corrected along the way: seeding *qualifying* disputes flipped `decide` onto the reject path and failed with `TooManyCohorts` (which is the defect itself refusing to let its own benchmark run), and seeding all 128 rounds on one frozen version measured 655 reads for a state 02 §7.2's own 16 × ≤4 × ≤2 decomposition says cannot exist. The right fixture is 64/64 across two versions, merit-clearing and money-settled — the true worst case, because `.any()` short-circuits on the first holding dispute. **A 13 §5 derivation reopened and was re-derived rather than relaxed:** the `decide` PoV pin fired exactly as its comment says it should. 231,055 → 404,514 B, ceiling 384 → 512 KiB, which is 10.3 % of the normal-class budget beside `settle_cohort(5)`'s 9.8 % — ~8× headroom, so the pin stays a detector. 13 §5 item 1 now names the round scan and states why \"bounded regardless of map ceiling\" still holds: `MAX_ROUNDS` is a kernel constant, not a growing map. Runtime **368** green, 69 workspace suites green, fmt/clippy clean, limit coverage 188/188, PoV budgets pass, weight regression PASS WITH ACKNOWLEDGEMENTS. The exclusion is mutation-checked in both regressions.

**What comes next:** **Open batch-X rows: 5** — SQ-181, SQ-234, SQ-303, SQ-490, SQ-493. Next per the verified order: **SQ-493** (07 §10's two-consecutive-flag renormalization has no reader of `flagged`; note SQ-492's epoch cutoff now bounds how much flagged history a design can rely on, and its per-component carry checkpoint is the one entry that always survives), then SQ-234 → SQ-181 → SQ-303 → SQ-490's sweep.

## **SQ-494 post-merge: `origin/main` folded in after #175…

**SQ-494 post-merge: `origin/main` folded in after #175 landed**

**The merge is where the two rows' weight numbers finally separated, and the result vindicates re-measuring rather than resolving by hand.** `pallet_epoch.rs` was regenerated on *both* branches, so the conflict had no correct textual resolution — I took main's file as the base and regenerated on the merged state. Against the true post-merge base (`61f3e94`) **only `decide` regresses**: `drive_oracle_boundaries` lands within 0.3 % of the value SQ-492 already merged, which is exactly right — its cost is now the baseline, and this PR's delta is its own. Resolving the weights by hand would have shipped whichever branch's numbers won the merge, and neither was correct for the combined state. Both acknowledgements expired with the merge (eighth and ninth occurrence), so both were cleared and only `decide`'s re-derived. **One rationale trued rather than left stale:** the fixture seeds all 128 rounds on one frozen version because 02 §7.2's 4-settling-epoch factor did not hold before SQ-492's reaper existed. It holds now, so 128-on-one-version is no longer reachable and the fixture is **conservative rather than exact**. It stays — the larger figure cannot under-charge, and re-measuring downward would trade a safety margin for a smaller number — but the comment and 13 §5 say which of those two things it is. 69 suites green, fmt/clippy clean, limit coverage 188/188, PoV budgets pass, weight regression PASS WITH ACKNOWLEDGEMENTS.

**What comes next:** **Open batch-X rows: 5** — SQ-181, SQ-234, SQ-303, SQ-490, SQ-493. Next: SQ-493's implementation, verified and scoped with zero unknowns on `feat/sq493-consecutive-flag-renormalization`.

## B1b residual (SQ-234)

**SQ-234 done — the entrenched enactment delay now guarantees four epoch boundaries at any legal `epoch.length`.** `min_enactment_period` moves from a static `4 × 21 d` to `4 × constitution_core::EPOCH_LENGTH_CEILING_BLOCKS` (168 d). The mechanics were verified before choosing: `pallet-referenda` reads the delay once at bake time, so a live-length derivation stays shaveable by a post-approval lengthening, and an exactly-four dispatch gate has no approval-epoch anchor to hang on; the ceiling is kernel-bounded, so sizing against it is a static guarantee. 06 §2.1's cell and a new §2.4 paragraph carry the rule and its cost (delay doubles at the default length; every `entrenched`-class row inherits it in both directions). Tests: the track pin moves 84 → 168 with a derivation assertion, plus a live-registry test asserting ≥ 4 boundaries at both bounds and that the row is `kernel_bounded`. Runtime 367 / constitution 60+21 green.

**What comes next:** SQ-181 → SQ-303 → SQ-490

## Track A residual (SQ-493)

**SQ-493 done — 07 §10's two-consecutive-flag renormalization implemented, spec'd and differentially verified.** `SnapshotContexts` records each snapshot's flagged set plus the `I_e`/tunables needed to replay that epoch (pallet-internal, 1:1 with `Snapshots`, paired both ways in try-state); `MetricInputs::flagged_components` reads the oracle's settled flags; `compute_settlement` drops components flagged in two consecutive epochs of the cohort's window and recomputes both `W` values with renormalized weights, declining any drop that would empty a gate group and renormalizing the P/A composite when a pillar goes dark. 07 §10 gained five normative points, 05 §4.4 the arithmetic + the new exactness rule, 15 §1 the try-state pairing. Contract unchanged at **v15** (02 §6 puts welfare events outside the ingest set). Reference model implemented the rule independently and the corpus grew 4 rows incl. a controlled live-gate A/B pair, replayed bit-exactly by `welfare-core`. Also fixed: a latent 1-ulp error in every single-term weighted-geometric group (invisible to any differential, since both implementations reproduce it), and `record_snapshot`'s benchmark fixture, which fabricated 1.0 values and skipped all the aggregation — sixth fixture-instead-of-work instance. Weights regenerated for `pallet_epoch` + `pallet_welfare` (4 acknowledged regressions, 21–26 %); the 13 §5 `settle_cohort(5)` PoV pin holds unchanged. Raised SQ-497 (welfare write amplification + per-proposal settlement redundancy, efficiency only). **Codex review P1 accepted and fixed** (`7d2bbe0`): `MigrateWelfareSnapshotContextsV1` backfills the context for snapshots an upgrading chain already retains, without which the try-state pairing rejects the upgrade; plus a `needless_return` in the `runtime-benchmarks` arm that only the exhaustive gate's feature set could see.

**What comes next:** SQ-234 → SQ-181 → SQ-303 → SQ-490

## SQ-234 review round

**Three Codex findings on #178, all valid.** The serious one is mine to own: `epoch.tick` is permissionless, so a block-denominated delay guarantees four epoch-*lengths*, not four crossings — my 06 §2.4 sentence claimed crossings. The sizing stands (nothing block-denominated can do better) but the text now states the boundary, and the residual is recorded as **SQ-498** with the two-phase arm/apply design costed. Also moved the epoch ceiling to `futarchy_primitives::kernel` beside the existing floor — the symmetry I had spotted and then ignored — and made the ratio assertion profile-aware, since `fast-timing` compresses the registry ceiling while every 06 §2.1 track period stays at release values.

**What comes next:** finish SQ-490 → SQ-181 (milestone-sized) → SQ-303 → SQ-498 decision

## licence attribution

**LICENSE copyright holder becomes "Bleavit Contributors" (PR #179), and the two places that would have silently disagreed with it were brought along.** `polkadot-omni-node-lib` composes `bleavit-node`'s `CliConfig::author()` with `copyright_start_year()` into the binary's `--version` attribution, so leaving it at "Bleavit" would have shipped a node stating a copyright holder different from the licence it is distributed under; `node/bleavit-node/Cargo.toml`'s `authors` had the same drift. Also restored the two-space `<year>  <name>` spacing the GPL's own appendix template (LICENSE:655) uses and the file had before, so the filled-in line still matches the template beside it. No code behaviour, no spec surface, no contract change.

**What comes next:** merge #179 → finish SQ-490

## SQ-490 (partial)

**SQ-490's sharpest instance closed in code, on `feat/sq490-weight-sweep` (no PR yet).** `pallet_attestor`'s `remove_for_cause` and `reap_attestation` — both live, the latter permissionless — had **no benchmarks at all** while `weights.rs` declared them with hand-written round numbers. Both now benchmarked at worst case, with a `prime_terminal_proposal` seeding seam so `reap_attestation` reaches its work instead of failing its precondition. Found and fixed a second instance of the same shape: the attestor's pallet-only benchmark suite was red and unexecuted (no holds at genesis, challenger unfunded) — 33/33 now, from 28/33. Also pruned the eleventh weight-ack expiry and folded main into both open branches. **Owed:** regenerate `pallet_attestor.rs`, the 19 upstream files, the `collator_compensation` extraction, and the gate redesign.

**What comes next:** finish SQ-490 → SQ-181 (milestone-sized, see row) → SQ-303

## SQ-490 item (3)

**The A13 hand-splice footgun is removed by benchmarking the term, not wrapping it.** The row asked for a runtime-side `WeightInfo` wrapper; reading `check-weight-regression.py` showed that would have moved the term outside the gate's parser (it skips bodies with no literal `from_parts` and keeps only the last impl per file), so `collator_compensation` is now a real `#[block]` benchmark at 120 payees, composed at each `#[pallet::weight]` — the idiom conditional-ledger already uses. **Validation says the estimate it replaces understated ref_time ~2.4x** (1,800 -> 4,264 us) on three permissionless cranks. The fixture's own assertion caught a silent no-op first time round (`Preservation::Preserve` starves the last of 120 transfers when custody holds exactly the pool). Repaired the I-20 class-fit sweep, which would otherwise have started certifying one addend instead of the dispatched weight. **Items (1)+(2), the 21-pallet regeneration, were still running at write time and are not committed** — see the resume pointer.

**What comes next:** finish the regeneration sweep -> re-true `pov_budgets` + 13 §5 -> gates -> SQ-490 item (4)

## SQ-490 review round

**Two Codex findings on #180, both valid.** The P1 is the one worth remembering: the `reap_attestation` benchmark I added to fix fixture-instead-of-work had that same hole — `Revocations` left empty, so a `retain` over the full vector, a second scan of it, and the aggregate's decode/re-encode were all free on a permissionless call. Saturating it correctly required reading `try_state`'s pairing invariant and noticing that `retain` runs *before* the `still_present` check, which is what lets the target's own revocation be seeded without destroying the liability-release arm. The P2 showed the new purity gate caught only half the defect it was built for — a splice into an already-generated function keeps its measured line — now closed by a grammar check with zero false positives over all 356 functions.

**What comes next:** regenerate `pallet_attestor`, re-gate, then SQ-490 item (4)

## SQ-490 item (4), partial

**Two of the gate redesign's five parts landed on `feat/sq490-gate-redesign`.** (a) The ref_time floor was **1 µs** while the sweep's measured jitter deltas ran to **48 µs** — raised to 250 µs, and the mechanism already existed: `exceeds_limit` always required both a relative and an absolute trigger, so this is a constant with evidence attached, not new logic. (e) Acknowledgements now pin the accepted values, which is what makes an obsolete entry safe to warn about rather than fail on: unpinned, it would authorize a later unrelated regression under someone else's justification. That ends the post-merge prune the row counted thirteen times. The checker prints the paste-ready line so the four integers are never transcribed by hand. **Its own self-test caught the semantic change** — a fixture whose evaluated delta sat between the old and new floors — and the fixture was scaled to preserve its intent rather than the floor weakened to suit it. One of my new assertions was also simply wrong about the code: a vanished function *with* an acknowledgement is an acknowledged **removal**, not a stale entry; the genuinely dangerous case is an entry naming something absent from both sides, and the test now asserts that.

**What comes next:** finish item (4): metadata-derived class-fit list, per-pallet failure isolation, per-function provenance

## **SQ-490 ✅ — item (4) closes the row** (branch…

**SQ-490 ✅ — item (4) closes the row** (branch `feat/sq490-gate-redesign`)

**The row is closed, and two of my own design generalisations had to be measured out of it before it could be.** Item (4) shipped in five parts. (a) `REF_TIME_FLOOR` 1 µs → **250 µs**. (e) Acknowledgements are **value-pinned**, so an *obsolete* entry warns instead of failing — the change that ends the post-merge prune. (b) The I-20 class-fit inventory is **derived from runtime call metadata** (`GetCallMetadata`) instead of a hand-written list plus a hand-written count of that same list; two hands maintaining one fact could catch a *dropped* entry and never a *never-added* one, which is how three live dispatchables went unswept. The derivation found errors immediately: `pallet_market` has **6** dispatchables, not 9 — `create_market`, `seed` and `close` are internal operations driven through the `configs.rs` adapters the epoch cranks call, measured end-to-end by the enclosing crank, so charging them again would double-count. Falsifiability checked by deleting a live dispatchable from the sweep and confirming the exact diagnostic. (c)+(d) A new `tools/ci/regenerate-weights.py` **re-measures every pallet and diffs per function**, closing what `check-weight-regression.py` structurally cannot reach: it compares two *committed* artifacts, so it sees a regeneration and never the absence of one. Wired into the `benchmark-smoke` job, which until now executed every benchmark and **discarded the results** — it could see a benchmark that crashes, never one that disagrees. **Two corrections, both from measurement, both against me.** First I compared the generator's intercept and slope separately; the first full sweep returned **42 `STALE` lines across 12 pallets** and most were not drift — the generator re-splits one measured cost between intercept and slope, and `set_candidacy_bond` moves its proof slope 901→1,306 with worst-case writes fixed at **201**. Comparing **worst-case totals** cut it to **13 lines across 2 pallets**. Second, I generalised \"storage is fidelity-invariant\" from two pallets; re-measuring `pallet_collator_selection` split it cleanly — constant-weight functions agree **2/2**, component-bearing ones differ **5 of 9**, and `new_session` loses its per-candidate write slope entirely (worst-case writes **100** at 50×20 against **3** at 2×1). A 2-point fit can miss a linear term and missing it *understates*, so component functions are hard-gated only at matching fidelity. Constant-weight totals stay hard at any fidelity, which is what keeps the cheap per-commit run worth running — the original 8-vs-261-reads defect was constant-weight. **The real understatements the gate found on its first honest run, now fixed:** `pallet_guardian::approve_action` 93→**101** reads / 45→**51** writes, `ratify_action` 30→**33** reads, `uphold_veto` 127→**138** reads / 75→**83** writes, and `pallet_futarchy_treasury::note_collator_block` 8→**10** reads / 6→**7** writes — three governance calls and one treasury call charging less than they perform. Both pallets regenerated at their own 50×20 fidelity; three pinned acknowledgements record that the weight rose because the *declaration* was wrong, not the code. **A hazard neither I nor the audit caught, found by reading the sweep output:** `pallet_xcm::execute` and `teleport_assets` hold deliberate fail-closed `Weight::MAX` sentinels (09 §6.2) that the parser skips, so a `--write` would have replaced them with measured values. The tool now preserves every function declared in `generated-weight-overrides.toml` — which also means it mechanically enforces \"do not regenerate `pallet_guardian` before SQ-500\", proven in practice: the regeneration left the 220 r / 150 w envelope byte-identical. **Codex audit: 5 findings, all addressed** — its top one was the slope hole I had already found by measurement (independent convergence). The work list now comes from the runtime's own `--list` (a benchmark added since the last generation could previously be skipped silently), preservation became a **declared, justified, expiring** exemption in the new `tools/ci/weight-preservation.toml` rather than a self-authorising comment, aliases sharing a weights module must declare identical call sets, and function extraction is scoped to the last `WeightInfo` impl. Amended 15 §4.5 normatively for all of it. **Verification:** exhaustive Rust gate `REAL_GATE_EXIT=0`; `check-ci-parity.py` PASS (12 gates); `tools/ci/tests` **131 OK**; `pov_budgets` 10/10; growth gate PASS WITH ACKNOWLEDGEMENTS (354 shared functions); purity gate 356 functions / 3 overrides; drift gate PASS on both regenerated pallets. **And the component rule earned itself immediately:** `pallet_epoch` was re-checked at its own **50×20** fidelity and **passes** — so `settle_cohort`'s proof slope moving 30,754 → 32,104 at 2×1 was a fit artifact, not drift. Had I kept the first design and hard-gated component slopes at reduced fidelity, that would have been a false failure on the heaviest crank in the system, which is exactly how a gate teaches people to ignore it.

**What comes next:** **SQ-500 raised** (guardian per-block hook undercharged ~6×; the follow-up this row owned — \"extend the `on_initialize` fixture\" — is **superseded**, since extending it would only make the undercharge visible, not safe). Next PR: **SQ-499 + SQ-500** together, then SQ-497 + SQ-498, then SQ-303, then SQ-181 (milestone-sized by its own text).

## SQ-499 + SQ-500

**Both closed** (branch `feat/sq499-sq500-crank-bounds`). **SQ-499:** `tick`/`decide`/`settle_cohort` now return `DispatchResultWithPostInfo` and refund the A13 `collator_compensation()` pre-charge on every path that did not pay — the pre-charge itself is untouched, so 13 §5's pinned ceilings do not move. Each pre-charge lives in exactly one helper used by both the attribute and the body; a second copy that drifted would under-refund, the unsafe direction. Early `?` exits keep the full charge (safe) and say so at each site. **SQ-500:** the sweep is bounded at 10 with carry-over, the `VetoReviewReferenda::iter_keys()` map scan is replaced by a snapshot built from the already-loaded pending set (which is all the core ever asks about), and `reap_failed_actions` is bounded behind a cursor. **Three things the work corrected.** (1) The row's mechanism was wrong: the §5.2 allowances mean a council *cannot* approve 128 actions in one epoch — the saturated case is reached by accumulation plus a multi-epoch clock jump, not by one epoch's burst. (2) My own \"weight-neutral\" claim was true of three dimensions and false of the fourth: reads 220→100, writes 150→101 and proof 183,055→125,304 B all *fall*, while ref_time rises 1.95→5.63 ms — the hand-written envelope had over-declared storage and under-declared time by ~2.9x. (3) The fixture cannot make all ten settles schedule a recall, and that is a property of the system, not the fixture: a recall is funded from the slash pool, a seat's 50,000 VIT bond absorbs exactly two 50 % slashes, and a settle draws from five approvers at once, so a fresh council's bonded capital funds the first two settles of a batch. Asserting ten would need bond capital the chain cannot hold, which inflates a *mandatory* charge with unreachable state.

**What comes next:** SQ-497 + SQ-498 in one PR, then SQ-303, then SQ-181

## SQ-497

**Closed** (branch `feat/sq497-welfare-efficiency`). Welfare's whole-state mirror now writes only the difference, and cohort settlement computes its score once per batch instead of once per proposal. Measured: `register_spec` 77 → 2 writes, `record_snapshot` 80 → 6, `record_daily_gate` 81 → 6; `settle_cohort`'s per-proposal ref_time 711 → 363 µs and its worst case at the cohort bound 11.30 → 7.71 ms. The seam takes the **targets**, not a score, so 05 §6's \"epoch may request a settlement, never choose it\" survives the batching. Two things worth carrying: the diff compares against *stored* values rather than the loaded core clone, because the core → stored conversions change representation; and a `*-core` trait moved, so the separate `fuzz/` workspace was rebuilt and its gate run.

**What comes next:** SQ-303, then SQ-181

## SQ-303

**Closed** (branch `feat/sq303-budget-screen`). The `RederiveBudgets` screening obligation is implemented as a value test rather than the artifact/verifier the row listed as owed — see the Decision log row for why that machinery could not have delivered the property. Verified the derivation reproduces 08 §4.1's published table: every frozen literal sits 0.39–30.07 USDC **above** the exact value, so the table really is this derivation and the conservative direction is what makes `derived <= frozen` the right comparison. Frozen floors moved to `futarchy_primitives::kernel::CLASS_NAV_FLOOR_USDC` so the treasury gate and the constitution screen read one home. One trap worth carrying: the pallet's `set_param` works directly on `Params` storage and does **not** go through the core aggregate, so the screen has to exist on both paths — `constitution_core::class_floors_survive` is the single home of the arithmetic, called from the aggregate and from the runtime's `BudgetDerivationGuard`.

**What comes next:** SQ-181 (milestone A12) is the last batch-X row

## SQ-501 raised + A14 scoped (branch…

SQ-501 raised + A14 scoped (branch `docs/sq181-scope-handoff`)

**Two review rounds, seven valid findings, and the second round overturned a design decision I had been about to take.** The post-merge Codex review of #184 found 13 §5 still carrying the *direction* rule the SQ-303 value screen replaced, so the document stated both at once — and found the SQ-303 closure claiming three over-rejections removed when two were: `epoch.length` is an occupancy key and was never freed. Both corrected, the residual refusal re-specified against what can actually lift it (comparable constants, not the artifact verifier that SQ-303 had just shown cannot move a compile-time bound), and raised as **SQ-501**. One defect the review missed: #184 orphaned the old screen's doc block onto `is_coverage_input`, which had since been documenting itself with a stale SQ-303 paragraph. **Then the review of #185 corrected the SQ-181 plan itself.** I had reasoned that no conforming genesis `MetricSpec` set could ship without the qualified-users sketch, and was weighing a `P`-pillar re-weighting to get around it. The premise was false — `register_spec` validates and stores records without consulting `RuntimeMetricInputs` — so the set registers before any producer exists and no protocol change is needed. Four further P1s were valid: the inventory named eight of ten producers, A12 was ✅ while owning milestone-sized work, PLAN was restating normative `Π`/`P` semantics against R-4, and no session-log row existed. **A full inventory then disproved the survey's own headline.** The seven natively-bounded components were called "mechanical over state that already exists"; not one of them is. The authorship accumulator `U`/`D_eff`/`K` need is killed by the collator payout before a snapshot can read it and has no day dimension; `F`'s gap is computed as a local and discarded, and neither `target` nor `Λ_max` is a 13 key; `D_eff` needs a phase ordinal `PhaseFlags` cannot supply; `E` is blocked on `collator.bond`, still `[VERIFY]` and unseeded; `H` needs an `on_finalize` sampler because `BlockWeight` is killed each block; `Π` has no counter, window or bound anywhere. Scope now split: **A14** (the seven) and **SQ-502** (the `P` pillar plus 05 §4.6's unimplemented normalization kernel). No production code changed in this branch.

**What comes next:** **A14 — implement, starting with the shared `(epoch, day)` authorship series that `U`/`D_eff`/`K` all need.** Rule `Π` and the `U` denominator before their code. `E` stays fail-closed until `collator.bond` is seeded.

## **A14 🔨 — the three SQ-181 rulings land; producers in…

**A14 🔨 — the three SQ-181 rulings land; producers in flight**

**The rulings were the real blockers, and none of them needed a new 13 key.** New 05 §4.3.2 (PR #186) fixes the three components that carried a formula and no source. **`U`:** "scheduled slots" read as parachain blocks makes the ratio identically 1, because `epoch.length` is itself denominated in them — so slots are **relay** slots, measured by `relay_parent_number` advancement, and an empty block is one whose extrinsics are all inherents. **`F`:** `target = 1` and `Λ_max = kernel::DEAD_MAN_RELAY_BLOCKS`, so `F` reaches 0 exactly where §4.8 already declares the chain stalled; both derived, so R-2's necessity test refuses them as tunables. *(The `Λ_max` half of that sentence is **withdrawn** — see the next row. It was falsified before #186 merged: `F` aggregates by median, which cannot see the tail the dead-man latches on, so no value of `Λ_max` makes the two agree. What merged is `target = 1` plus `F` decided out of v1. Annotated in place rather than rewritten, per this log's append-only rule.)* Also recorded: the median is over the window, so the gap *series* must be retained — the dead-man's boolean cause bit is not enough. **`Π`:** an event counts iff the runtime detected a violation of an assumption it holds unconditionally, the fallback discarded correctness-relevant state, **and** no defined path restores what was lost. The third clause excludes bounded-maintenance backpressure, which is load-bearing: four events zero `Π`, and `Π` can pull `C` into a §4.7 breach flag that arms the guardian's `suspend_on_gate`. **Three implementation agents are in flight** on their own branches — the `(epoch, day)` collator-authorship series plus `K`; SQ-501's occupancy value screen; and SQ-502's 05 §4.6 normalization kernel with reference-model parity. None is reviewed or landed, and nothing from them is claimed here. **Tree state, honestly:** the main worktree sits on `feat/a14-authorship-series-k` with that agent's uncommitted edits, and its branch carries a stray commit (`e1346bd`) created when an orchestration mistake let a `git add -A` sweep in-flight agent files into a docs commit — the doc content was rebuilt cleanly as #186 and the stray commit must be stripped when that branch is reviewed. No production code is on `main` from this session.

**What comes next:** **Review and land the three agent branches, in this order:** SQ-501 (self-contained), then the authorship series + `K`, then the normalization kernel — the last invalidates the Merkle-bound `phase0-calibration.json` if it touches `welfare.py`, which is a separate decision and must not be regenerated silently. Then the remaining A14 producers: `U` and `F` share the relay-gap series the §4.3.2 ruling now requires; `D_eff` needs a rollout-phase ordinal `PhaseFlags` cannot supply; `E` stays fail-closed until `collator.bond` is seeded.

## **A14 🔨 — `K` shipped; `F` decided out of v1**

**`K` is live** (#187): 05 §4.3's collator-set adequacy now has a producer, over a new per-`(epoch, day)` collator-authorship series that `U` and `D_eff` will share. The treasury's existing accumulator could not serve — `pay_collator_compensation` `take()`s and kills it before the epoch's snapshot is recorded, and it has no day dimension; it is a payment ledger and those kill semantics are right for one. Series follows `XcmTraffic`/`ReserveProbeDaily` exactly, reusing the *shared* prefix index and reaper. Overflow drops in the safe direction (fewer distinct authors lowers `K`/`D_eff`/`U`), zero `n_min` leaves the component absent rather than dividing. Measured 9.17 µs / 7,808 B / 2r2w. Gates all exit 0. **`Λ_max` decided (#186), and the answer is not a number.** Both error directions are unsafe — too large and a degraded chain never trips the `S` gate; too small and noise VOIDs cohorts and arms the guardian against a healthy chain. `[VERIFY]` + fail-closed is not an alternative, for a mechanical reason: a registered component with no value makes `record_snapshot` refuse, so a genesis set containing `F` could never record a snapshot at all — welfare wedged, not degraded. Registration is the choice point, so **`F` is admissible but not registered in v1**. Structurally free: `S` is a `min` carrying no weights (the weight-sum rule binds `C`/`P`/`A`, never `S`), and liveness stays priced by `U` for the count and the dead-man for the tail. `F` keeps id 11 and becomes registrable as a later `MetricSpec` version once Phase-2 evidences `Λ_max`. **Found, not fixed:** `record_daily_gate` does not bound `day` by the epoch's real length, so for a day the epoch never had `X` reads 1, `R` refuses and `K` reads 0 — three components disagreeing about a non-existent day. Not live (no registered spec carries id 6). Belongs in 05 §4.3, not three independent runtime patches.

**What comes next:** **A14 remaining: `U`, `D_eff`, `E`, `H`, `Π`** — `F` is out of v1. `U` needs the relay-parent delta series §4.3.2 now specifies plus an empty-block signal; `D_eff` needs a rollout-phase ordinal `PhaseFlags` cannot supply; `E` stays fail-closed until `collator.bond` is seeded; `Π` needs the counter §4.3.2's ruling now defines. Two agent branches still to review: SQ-501's occupancy value screen and SQ-502's §4.6 normalization kernel — the latter must not regenerate `phase0-calibration.json`.
