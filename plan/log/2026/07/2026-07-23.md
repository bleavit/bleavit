# Session log — 2026-07-23

## B16 terminal recovery / Phase-4 bridge

**Implemented the paired primary/recovery runtime product in an isolated worktree based on current main, without touching the dirty reserve-probe branch.** Added bootstrap/Phase-4 and recovery profiles, exact same-commit release pairing, booted metadata/`:code` validation, zero-MBM recovery images, the one-shot Phase-3→4 META bridge, bounded permissionless recovery qualification, cached static checks, frozen terminal eligibility, inherent-only scheduling/application, relay-Abort restoration, keeper planning, strengthened try-state and corruption coverage, and regenerated execution-guard/epoch weights. Ordinary MBMs fail closed until a release-specific cutpoint repair exists. Focused verification is green: execution guard 77, bootstrap runtime 294, bootstrap-recovery 297, Phase-4-recovery 12, keeper 50, release tooling 83, all four strict-clippy profile combinations, all four release profile builds and both zero-MBM proofs. The exhaustive local workspace gate was stopped after confirming its host-only libclang workaround, per the short-feedback-loop policy; GitHub CI is authoritative and the PR remains draft until every required check is green. Final review found the recovery metadata must be a frontend descriptor input because recovery becomes live; contract v9 now binds both metadata blobs before eligibility, and the stale ordinary-MBM/attestation/runbook text and mandatory-weight envelopes were corrected.

**What comes next:** Address the follow-up CI/review findings and mark ready only after all required CI checks are green; A13/SQ-173 still gates operational SQ-104 closure

## B16 draft PR #140 CI follow-up

The first CI pass correctly rejected two intentional execution-guard weight increases and every now-stale SQ-483 acknowledgement inherited from main's pre-merge branch state. Removed all 17 stale acknowledgements and added exactly two B16-scoped entries: `apply_authorized_upgrade` for recovery-image validation/binding and the 4 MiB code path, and `execute` for atomic qualification transfer/terminal scheduling. `check-weight-regression.py` now passes against current main with only those two acknowledgements and the three genuinely new guard functions.

**What comes next:** Push the focused fix; keep #140 draft and require a fresh all-green CI head

## B16 PR #140 final local remediation

Closed the final review findings before republishing the draft: contract v9 now binds both live primary/recovery metadata blobs, release and runbook prose matches the paired-image/terminal-recovery mechanism, and mandatory hook/transition weights and PoV envelopes account for qualification plus recovery commits. Focused verification is green: default runtime **294**, recovery runtime **297**, the complete four-profile test/clippy/check/release matrix (including both exact zero-MBM recovery proofs), release tooling **83**, weight regression with only the two B16 acknowledgements, doc links **82**, runbooks **13/21**, PLAN tables, question batches **338/63/275**, formatting, and diff check.

**What comes next:** Push #140, keep it draft while GitHub CI is pending, and finish/mark it ready only when every required check is green

## B16 PR #140 strict-Clippy follow-up

GitHub's exhaustive Rust job found five test-only lints that the profile matrix's library-focused Clippy legs did not cover: four redundant `clone()` calls after `Attestation` became `Copy`, and one constant-false assertion. Replaced them mechanically without changing behavior. The exact `cargo clippy --workspace --all-targets -- -D warnings` command is now green, as are all **294** default runtime tests.

**What comes next:** Push the test-only correction and require a fresh all-green PR #140 head before stacking B15

## B16 PR #140 contract-pin follow-up

The next exhaustive Rust run passed the runtime changes and exposed one stale constitution test literal: the branch correctly advertises integration contract v9, while `contract_version_and_bounds_reexports_hold` still expected v8. Updated the test and its reason to the paired primary/recovery metadata commitment.

**What comes next:** Push the corrected contract pin and require a fresh all-green PR #140 head before marking ready

## B16 PR #140 recovery-surface review fix

Closed the Codex P1 that recovery metadata was packaged but bypassed the frozen-surface drift gate. The chainHead recorder now checks every metadata-dependent manifest entry against the paired recovery blob, records its hash and complete pass/fail coverage beside the primary fixtures, and includes each recovery layout verdict in the corresponding transcript. Strict assembly binds that report to the shipped recovery metadata, rejects incomplete recovery-surface coverage as corruption, and exposes any required recovery drift as a release gap; the tag workflow passes the exact paired blob explicitly. Focused release tooling is green (**86** tests), plus Python compile and diff check.

**What comes next:** Re-run PR #140 CI on the integrated head; mark ready only after every required check is green

## B16 PR #140 keeper review fix

Closed the Codex P2 that the keeper scheduled `qualify_recovery_image` for every non-terminal CODE/META proposal, including ordinary META payloads with no recovery descriptor. The finalized-state extractor now reads each bounded CODE/META proposal's exact `(payload_hash, payload_len)` preimage, decodes its top-level `RuntimeCall` batch against live metadata, and marks qualification work only when exactly one `system.authorize_upgrade` and one `execution_guard.commit_recovery_image` leaf are present; missing, malformed, duplicate or trailing-byte payloads fail closed. Planner coverage pins both the real upgrade case and ordinary-META suppression. Keeper tests are green (**52**).

**What comes next:** Integrate with the recovery-surface fix, push, and require fresh all-green CI before marking #140 ready

## B16 PR #140 epoch-test CI follow-up

The exhaustive workspace test leg exposed two stale epoch fixtures after the B16 contract and try-state hardening: one still pinned integration contract v8, and one seeded a `Trading` proposal without the exact qualification preimage request now required by the pallet invariant. Updated the pin to v9 and installed the request in the positive try-state fixture before its deliberate metric-spec corruption.

**What comes next:** Run the focused epoch suite, push the test-only correction, and require a fresh all-green #140 head

## B16 PR #140 final independent-review remediation

The final independent spec pass found **2 blockers / 0 majors / 1 minor**. Fixed the direct-System Phase-4 hole at the stable2606-compatible relay-application callback: both the normative public call and the guard wrapper remain Pending/abortable while Cumulus holds the candidate, then exact installed-code validation atomically moves the bridge to Scheduled, arms `OnlyInherents`, and marks the one-shot migration before the next `on_runtime_upgrade`; mismatched pending bridge state is refused fail-closed. Fixed mandatory undercharging by summing the generated recovery qualifier, frame-system authorize/apply weights and conservative fixed bookkeeping/cursor proof, with an absolute-floor regression. Added keeper integration coverage over an extracted real runtime metadata blob for exact preimage tuple-key encoding and descriptor-pair detection while preserving legal sibling calls; ordinary META, duplicates, malformed and trailing bytes suppress work. Focused gates green: default runtime **293/294** on the first run with the sole stale pre-GoAhead assertion then corrected; both boundary tests green; Phase-4 direct GoAhead transition green; mandatory budget regression green; keeper **54** + strict Clippy; formatting and diff check.

**What comes next:** Re-run the complete local/runtime profile gates, obtain a clean re-review, push one remediation commit, reply/resolve any resulting review threads, and require fresh all-green #140 CI before stacking B15

## B15 — SQ-233 ledger/oracle follow-up (parked, uncommitted)

Implemented the reviewed design for exact O(1) ledger reconciliation, target-specific unsampled-gate VOID latches, and live-conditioned three-way LedgerFreeze effects; generated ledger/epoch/guardian weights and added adversarial origin, migration, one-window/scalar-only, target, early-lift/re-engage and all-16-tuple tests. Three review rounds found and the session fixed intermediate-v0 try-state, partial stale-tuple clearing, out-of-phase latch deletion, clean-effect renewal and VOID-in-flight sourcing. The authoritative workspace gate then exposed the governing blocker rather than being waived: `registered_mbms_obey_the_b16_lockdown` fails because B16 deliberately permits **zero** registered MBMs until SQ-309's inherent-only recovery image ships. The draft ledger backfill also failed final review because typed prefix iterators can skip corrupt rows/do unbounded reads and storage reads precede weight precharge; market/guardian weights need a final regeneration after fixes. Work is preserved uncommitted on `fix/reserve-probe-health`; no commit/push made, draft PR #139 remains at its last green head.

**What comes next:** Finish B16 SQ-309 in its own milestone/session. Then resume B15 with a raw-key, one-row, precharged fail-static backfill; real `pallet-migrations` success/stuck tests; market+guardian weight regeneration; full gates and final re-review.

## B15 — dependency-independent SQ-233 completion; PR #139 paused

Replaced the rejected typed backfill with strict raw Blake2_128Concat traversal: one key/value per step, conservative weight consumed before the storage-version read, exact key length/hash and `DecodeAll` validation, checked partial sum, and atomic final mirror/version writes. Corrupt raw keys/values fail static; a starved meter returns `InsufficientWeight` before inspecting corruption. Added real runtime-framework tests proving both full onboarding/completion and corrupt-row `Stuck` + PB-MIGRATION halt with v0 state preserved. Regenerated guardian and market weights at 50×20 on the existing reference host; the market sweep corrected a stale saturated `try_state` read count (24,464→28,944) and is scoped in the regression acknowledgements. Focused verification green: ledger **68**, epoch **101**, guardian **47**, welfare **64**; real runtime migration success/stuck; clippy `-D warnings` across the affected pallets/runtime; ledger no_std; ledger try-runtime migration tests; guardian/market benchmark calibration; weight-regression gate; fmt/diff checks. No commit or push: the full runtime gate intentionally remains red at B16's zero-MBM integrity assertion until the paired release-specific repair exists. Draft PR #139 is paused at its last green remote head.

**What comes next:** Finish PR #140 in its isolated worktree and require all CI green. Then stack #139, implement/exhaustively test the ledger cutpoint recovery, run the final B15 compliance/gate sweep, push, and mark ready only on full green CI.

## B15 cutpoint recovery on merged B16 base

Restored the paused B15 work after PR #140 merged and completed the release-specific ledger recovery product. The primary registers exactly one bounded `TotalEscrowed` MBM (`897` rows / `<900` steps); the paired recovery profile registers zero SDK MBMs, validates every retired active/stuck cursor shape, restarts the read-only scan under a runtime-local cursor, and keeps `OnlyInherents` until the exact mirror/version plus guard terminal callback clear every lock atomically. Seven runtime recovery regressions cover all valid cutpoints, syntactically valid stale progress, malformed cursors/rows, overflow and the release row bound. Review found and this checkpoint fixed recovery detector undercharging, terminal guard-finalization undercharging, stale profile-specific tests and four reserve-probe spec inconsistencies. A new executable 50×20 benchmark saturates the 32-entry guard queue and generates `finalize_recovery_application` at 519,299,000 ps plus 55 reads / 47 writes / 91,839 B estimated proof; the composed per-step charge fits the migration service budget. SQ-484/SQ-485 provenance and the release blocker text are now truthful. Runtime profile gates pass across bootstrap, Phase-4, recovery and combined profiles (322 default; 328 recovery; exact zero-MBM recovery proofs); release tooling **86**, weight regression, PLAN tables/batches and diff checks pass. Final compliance review: **0 blockers / 0 majors / 0 minors**.

**What comes next:** Commit this reviewed checkpoint, merge current `origin/main`/resolve PR #139 conflicts, then run the complete post-merge workspace/parity/environment sweep before pushing and answering the Codex review thread.

## B15 PR #139 post-#140 synchronization and full closeout

Committed the reviewed implementation (`c4acbd5`), merged `origin/main` at the PR #140 squash (`2aae113`) and resolved all squash-vs-history conflicts into a tree byte-identical to the reviewed pre-merge state (`4369bf6`). The first post-merge Rust gate correctly found that #140 made its two execution-guard regression acknowledgements stale; removed exactly those entries, then the complete remainder passed: weight comparison over **320** shared functions, release/benchmark/try-runtime/no-std builds, **329** try-runtime tests, keeper **54**, reference model **39**, and limit coverage **184/184**. CI parity passed **10/10** in a checkout-shaped clone. Tooling/environment gates passed: deploy **83**, release **86**, phase-gates **69**, CI **66**, monitoring **105**, env **72** (5 skipped), structural environment validation, alert coverage **21/45**, runbooks **13/21**, doc links **82**, PLAN tables and question batches **340/63/277**. Formal verification passed every model floor (largest ledger scope **10,063,640** distinct states; proposal ForceRerun **1,169,316**) and every expected-violation witness; simulation **50** (1 skipped), supply-chain and all three fuzz corpus/smoke targets are green. The mandatory one-million-case property gate is also green: ledger PT-1…PT-8 **13**, market I-12/I-13 **3**, constitution I-6/I-7 **4**. GitHub CI and CodeQL passed every check on pushed head `e53087c`; PR #139 is mergeable/clean. The sole valid Codex P2 was fixed in README/release surfaces, answered with verification evidence and resolved; no review threads remain. Live Asset Hub provisioning/calibration and release-train Zombienet evidence remain V-45/G2+ evidence, outside this code closeout.

**What comes next:** Publish this closure-only PLAN update, require the fresh head to pass CI, then mark PR #139 ready.

## Standing PR handoff instruction + B18 orientation

The user merged PR #139 after its significant code head had passed the complete local and GitHub matrices, correctly declining to wait for an identical exhaustive rerun triggered only by the final `PLAN.md` closure commit. Recorded this as AGENTS.md R-12 and a Decision-log entry: mark such PRs ready and continue; never use the rule to waive meaningful-change gates or ignore failures; poll CI at most once per five minutes when polling is useful. Fast-forwarded local `main` to merged `ba27dfa` and opened `feat/guardian-review-b18` for the next eligible non-frontend, non-G2+ milestone.

**What comes next:** Read B18's owning 06 §5.4, 05 §2.1 T12/T13/T24 and the relevant 02/13/15 slices before implementation.

## B18 guardian-review semantics and ratification binding

Implemented and tested the separate `GuardianReviewWindows`/`GuardianReviewDeadlines` joins, strict review deadline, veto survival through ordinary failure, T12/T24 custody cleanup, proposer-only exact ratification binding, fail-closed migrations and try-state joins. Reconciled D-5/G-6 stale queue-time wording across the normative and derived docs. The complete Rust workspace gate is green: the first run removed 18 stale pre-B15 weight acknowledgements, then the final run passed all formatter, clippy, workspace, release/profile, keeper, reference-model and limit-coverage legs. The final audit also fixed conservative epoch-binder/guardian-hook weight envelopes, a saturated benchmark fixture, a bounded maintenance snapshot (avoiding repeated full-map scans), and the preimage lookup length cap; focused regressions are guardian **53**, epoch **103**, execution guard **72**, runtime **326**.

**What comes next:** Commit and push the B18 PR, mark it ready without waiting on a redundant documentation-only CI rerun, then inspect and answer any valid Codex connector review threads.

## B19 attestor lifecycle and PR #141 handoff

Implemented native attestor/challenger holds, bounded independent liabilities, cause-aware removal with durable revocation, terminal permissionless reap, and separate live-roster queue vs record-only execute quorum. Contract v10 and the full normative/derived documentation fan-out landed in commit `4c5de2c`; targeted core **11**, pallet **26**, and runtime **326** suites passed, followed by changed-crate Clippy and one exhaustive `rust-workspace-gates.sh` pass. Added the locked `--changed [PACKAGE...]` local gate mode; no duplicate Cargo jobs were run. Pushed and opened [PR #141](https://github.com/bleavit/bleavit/pull/141), then marked it ready for review. The connector returned no review comments or threads, so there was nothing valid to reply to. Final PLAN/docs-only closure follows R-12 without another exhaustive Cargo run.

**What comes next:** Continue with B20 (SQ-107) after PR #141 review/merge; poll CI no more than once per five minutes if needed

## B19 PR #141 Codex P1 remediation

The connector review's P1 was valid: `FailedExecuted` remains retryable, so it must not satisfy either `has_executed` or `is_terminal` for attestor custody. Removed it from both runtime predicates and added `failed_execution_remains_reap_protected_during_retry_window`, pinning cause revocation plus `ProposalNotTerminal`. Commit `ea330b0` pushed to #141; targeted runtime **327**, pallet-attestor **26**, attestor-core **11**, and changed-crate Clippy all pass. Replied to the Codex connector comment with the evidence and resolved the thread. No exhaustive rerun was started for this focused correction.

**What comes next:** Continue with B20 (SQ-107) after PR #141 review/merge; poll CI no more than once per five minutes if needed

## B20 Phase-4 community distribution implementation

Implemented the treasury-owned bounded community schedule path for SQ-107. The pallet records an idempotent exact arming block, tracks the remaining 250M VIT allocation and lifetime schedule count, validates the derived pot/beneficiary/minimum/duration/boundary conditions, calls a frame-free adapter before counter mutation, and emits an operational event. Runtime wiring binds the real pallet-vesting `VestedTransfer`, the exact Phase-4 migration hook, PARAM classification/resource derivation and the 4,096 bound; the spec and derived design fan-out are aligned without a 02 contract bump. Focused pallet **59**, runtime **328**, changed-scope Clippy, formatting and limit coverage **185/185** are green.

**What comes next:** Run the one exhaustive Rust gate, then complete final docs-only closure, commit/push and open the stacked draft PR; inspect/reply to any Codex connector review comments.

## B20 exhaustive gate and closure

The gate initially found and this session fixed two integration omissions (B20 associated types in the standalone XCM mock and the benchmark module's `Get` import) plus one stale composed-migration assertion (treasury storage v2). The final exhaustive `tools/ci/rust-workspace-gates.sh` run is green: workspace tests (runtime **328**), weight regression (**325** shared functions; four new B19/B18/B20 functions allowed and the pre-existing B18 acknowledgement), release/runtime-benchmarks/try-runtime/no_std profiles, all runtime profile/try-state suites, keeper **54**, reference-model **39**, doc-table agreement and limit coverage **185/185**. Final PLAN/docs-only gates are the remaining handoff check; no Cargo rerun is needed for them.

**What comes next:** Commit/push the coherent B20 branch and open a stacked draft PR; inspect the connector review surface and reply/resolve any valid Codex comments.

## B20 draft PR handoff

Committed the verified B20 implementation and final gate evidence as `269773f`, pushed `feat/community-distribution-b20`, and opened draft [PR #142](https://github.com/bleavit/bleavit/pull/142) against `feat/guardian-review-b18` so the review diff is B20-only. Connector timeline, reviews and thread-aware GraphQL reads are empty; there are no Codex bot comments to fix or reply to. This closure-only PLAN update is not followed by another exhaustive Cargo run under R-12.

**What comes next:** Leave #142 draft while the next review arrives; answer any future valid Codex connector comments with evidence and resolve their threads after fixes.

## A12 oracle registration-stake custody slice (parked at the safe boundary)

Added `OracleCustody` and runtime `ForeignAssets` wiring for reporter/watchtower registration stakes, a dedicated `bl/oracl` sovereign account, atomic underfunded-registration rollback, aggregate registration-liability `try_state`, and stored-stake reduction on second reporter offense / two-epoch watchtower liveness slash. Focused oracle **112/112**, runtime registration success + refusal tests, changed-scope Clippy, formatting and diff checks are green. No Zombienet/Chopsticks run: this slice changes pre-genesis bounded custody and does not exercise deployed/network behavior. The A12 review found the remaining kernel inseparable — current auto-escalation has no signed `counter_report` owner and the spec lacks challenger/ejection/late-verdict disposition — so no round-bond or StakeAtRisk path was enabled.

**What comes next:** Resume A12 with the contract/spec kernel: frozen per-measurement StakeAtRisk snapshots, signed counter-report ladder with durable challenger ownership, d20 bond-only retention, then registry/versioned source successors. Run the one exhaustive gate only after that coherent kernel lands; no draft PR was opened for this partial slice.

## A12 signed escalation / round-bond kernel (contract v11)

Added signed `counter_report` consent, durable challenger identity across rounds, explicit challenger-default settlement, real round-bond custody and 40/60 terminal routing, bounded `MoneySettled` d20 retention with late bond-only verdicts, and the durable-challenger quorum regression. Regenerated the runtime oracle weights and added the benchmark custody priming seam; oracle-core **22/22**, pallet-oracle **112/112**, runtime oracle **14/14**, changed-scope Clippy, oracle benchmark smoke **11/11**, formatting and diff checks are green. The frozen integration contract now includes `SettlePath::ChallengerDefault` as **v11**, with 00/02/runtime/tests/design-kit fan-out. No connector review comments exist yet because this branch has no PR; no CI polling or network drill was performed.

**What comes next:** Run the one exhaustive gate on the coherent A12 kernel, update final PLAN/docs-only status under R-12 without waiting for redundant CI, commit/push and open a draft PR, then inspect and reply to any connector-bot comments. Continue with SQ-174/175/181/182/195/294/295/296 and explicit ejection disposition; keep frontend/G2+/Paseo/mainnet out of scope.

## A12 final verification and close-out

The exhaustive `tools/ci/rust-workspace-gates.sh` run is green end-to-end after fixing the all-targets Clippy borrows, the runtime PoV census (**110**), the constitution v11 assertion, nine bounded oracle weight-regression acknowledgements, and the marked `RoundNotFound` limit-coverage witness. Final runtime feature variant: **337** tests; keeper **54/54**; limit coverage **185/185**, 0 unwired. A12 is ✅. This final PLAN/docs-only update is intentionally made without a redundant Rust rerun or CI wait under R-12; no network drill applies.

**What comes next:** Commit/push and open the draft PR, then inspect and reply to any connector-bot comments. Continue with the parked A12 successors (SQ-174/175/181/182/195/294/295/296 and explicit ejection disposition); keep frontend/G2+/Paseo/mainnet out of scope.

## A12 draft PR publication

Committed as `e241c48` (`feat(oracle): custody-backed signed escalation (A12)`), pushed `feat/oracle-custody-a12`, and opened draft **PR #143**. The required thread-aware connector scan found no conversation comments, reviews or inline threads, so no bot fix/reply/resolution was needed. No CI polling was performed; the final exhaustive gate remains the verified local result recorded in V-51.

**What comes next:** Keep PR #143 draft until the next review pass; address only valid actionable threads if they appear, then continue with the parked A12 successors.
