# Session log — 2026-07-27

## **A14 🔨 / SQ-502 — spec layer merged, four code PRs in…

**A14 🔨 / SQ-502 — spec layer merged, four code PRs in review**

**Merged: #186, #185, #188** — 05 §4.3.2 (the `U`/`Π` source rulings, `F` decided out of v1, the measurable day set, `H`'s two-dimensional reduction), 05 §4.6's normalization rulings + 15 §4.4's extended bit-identity clause, the A14 milestone row, and SQ-501/SQ-502 with their Decision-log entries. `cargo check --workspace --all-targets` on the merged content: 0 errors. **Four code PRs open, each with an open review finding being fixed:** #189 (SQ-501), #187 (`K`), #190 (`U`), #191 (`H`+`Π`). All four are behind `main` and must be rebased before their gates are re-run. **The sharpest open item is #189's.** Its soundness argument rested on `epoch.slots`/`dec.window`/`epoch.length` being creation-time-pinned per 13 §3.1, leaving `mkt.obs_interval` the only live-consumed key. `dec.window` is **not** pinned in the code — `RuntimeInDecisionWindow::contains` reads it live — so 13 §3.1 and the in-flight paragraph both claim pinning the runtime does not implement, and a legal `mkt.obs_interval` *raise* can manufacture registry headroom that a `dec.window` raise then spends against a still-live 31-book cohort (146,103 against the frozen 133,920). Resolution is either pinning `dec.window` per cohort or treating it as live-consumed and screening against the in-flight maximum book count. **Three findings recur as one defect class** (now six instances): a benchmark fixture that reaches an expensive branch but not as often as the system allows — `RuntimeMetricInputs::onchain_components` returns fabricated values under `runtime-benchmarks`, so production prefix scans are never measured and the declared weight charges for none of them.

**What comes next:** **Rebase each branch onto `main`, re-run its gates after the rebase, then merge #189 → #187 → #190 → #191.** Confirm by direct enumeration that the three merges retired the six "§4.3.2 does not exist / A14 not in PLAN" findings. Then: the `SnapshotDeadline` stale-`due_epoch` latch defect (R-7, permanently latched dead-man), `Π`'s six uninstrumented sites (the `execution-guard` `MigrationHalt` pair bypasses `MigrationHaltSources`), and the doc-12 alert row for `IntegrityFailureRecorded`. `D_eff` unblocks on #187 but also needs a rollout-phase ordinal `PhaseFlags` cannot supply; `E` stays blocked on unseeded `collator.bond`.

## **SQ-501 closed; three defects raised that no PR was…

**SQ-501 closed; three defects raised that no PR was looking for**

**#189 merged (`ed23c80`)**, and its audit overturned the premise the fix rested on: `dec.window` has **two consumers with opposite behaviour** — the metered decision-critical classification reads it live, while `register_decision_window` stores `start = decide_at − decision_window` at registration and every TWAP accrual read uses *those* boundaries. So 13 §3.1's pin was real for accrual and never covered the classification, and pinning it would have changed consensus behaviour to make true a sentence nobody asserted — the defective artifact was the doc paragraph, not the read. Composing the screen against in-flight book counts made the value test exact and **deleted** the previous pass's `obs_interval_lowering_refused` rule; no key now carries a direction rule. `FULL_EXIT=0` on the final tree, after a real `FULL_EXIT=101` (`E0061` in a `runtime-benchmarks`-gated helper) — establishing the reading that **exit 2 means flock-starved, exit 101 means real**. **#190 is fixed, gated (exhaustive exit 0) and pushed**: the `(epoch, day)` window is captured once in the parachain inherent and block-stamped, so a boundary block carrying `tick` no longer splits across two epochs, and `U`'s epoch value is a bounded O(1) on-write aggregate rather than a 256-key fold (~645 KB of proof for a three-field sum, ~6× `record_snapshot`'s PoV). **Three new rows, none of them from the work the agents were sent to do.** **SQ-503**: `prune_xcm_traffic` clears every family co-indexed on `XcmTrafficEpochs` while `tick`'s fixture seeds only `XcmTraffic`, so `tick` declares `(r:512 w:512)` for one family and nothing for the rest — five unmeasured against one measured once A14 lands, and *cross-branch* by construction, since each PR adds one family to a fixture already short by one. **SQ-504**: the guard's fail-static `MigrationHalt` latches are **erased, not merely unobserved** — the runtime drives the same flag from a source bitmap the guard never writes, and `MigrationStatusHandler::completed()` calls `migration_completed()` (which may latch) then `clear_migration_halt_sources` on the **next statement**; the existing test encodes the bug. **SQ-505**: 13 of 15 gated `MarketAccess` arms bypass their production bodies under `runtime-benchmarks`, so `tick` declares zero `Market::` items and `decide` one of 63. **SQ-505 was wrong twice and corrected twice** — an automated line→function map mis-attributed the arms, and the first pass missed that `open_markets` carries a written rationale; the fix pattern already exists in-repo (`cohort_exposure` walks its fold deliberately, because returning the fixture measured Milestone at 99 reads against Incident's 119 and the 99 overwrote the 119, SQ-489). **SQ-181 narrowed** to the one item no milestone owned: no production genesis preset registers any `MetricSpec`, so every A14 producer changes nothing observable.

**What comes next:** **Rebase #187, #190 and #191 onto `ed23c80`, re-run each branch's gates after the rebase, then merge #187 → #190 → #191.** Re-benchmark `weights/pallet_welfare.rs` on every rebase rather than hand-merging it. Then the four owed items, in order: SQ-503 (one PR covering every co-indexed family), SQ-504, the `SnapshotDeadline` activation-tie wedge (`register_metric_spec` has no cross-version check on max `activation_epoch`, so two lawful registrations wedge the deadline permanently — then the dead-man latches and later goes **silent**), and SQ-181's genesis spec set. **Repo CI was queue-stalled with zero active runners throughout**, so #189 and #190 rest on local exhaustive gates.

## **A14 ✅ — four producers shipped; the queue's two…

**A14 ✅ — four producers shipped; the queue's two sharpest defects came from review, not gates**

**Merged: #185, #186, #188, #189, #187, #190, #192, #191.** `K`, `U`, `H` and `Π` are live as bounded per-`(epoch, day)` series on the shared index, each with a fail-soft writer, bidirectional try-state and measured weights. `F` ruled out of v1 (05 §4.3.2 — `Λ_max` has no anchor and both error directions are unsafe, so registration is the choice point); `E` and `D_eff` carried as blocked on non-code inputs. **Every review finding across the four PRs was real, and the two that mattered most were found by reading generated weights against `main` rather than by any gate.** `record_snapshot` shipped declaring **107 reads while performing 621**: the rebase inherited #187's and #190's walk-and-discard fixture lines but not `H`/`Π`'s, so both 256-day prefixes folded uncharged — and the growth-only regression gate structurally cannot see a read that was never measured. `pallet_migrations::exec_migration_fail` was under-declared 4 → 8 reads because A14 made the execution-halt activation edge record a `Π` event. Also settled by measuring at `main` rather than reasoning: a `pallet_xcm` write decrease (real, attributed) and three clippy combo failures (pre-existing). **Process facts worth more than any row:** a harness-reported exit 0 stood in for a flock-refused run **five** times this session — **exit 2 means starved, 101 means real**; `--all-targets` does not imply `--all-features`, which is how an `E0061` in a `runtime-benchmarks`-gated helper survived every hand-run check; and git's conflict list is not the set of files carrying markers after a multi-commit rebase, since `git add -A` stages the difference. Three rows were raised and then **not** worked, by explicit instruction to converge: SQ-503, SQ-504, SQ-505.

**What comes next:** **SQ-503 first** (one PR over every co-indexed family plus the two folds still unmeasured in `record_snapshot`; `pallets/epoch/src/benchmarking.rs` was deliberately left untouched by all three branches for it), then the `SnapshotDeadline` activation-tie wedge, then SQ-504, SQ-505, SQ-502 and SQ-181's genesis `MetricSpec` set. Repo CI was queue-stalled for most of this session, so several branches rest on local exhaustive gates — re-check the merged `main` once runners are free.

## **Backend security and code review (audit branch; no…

**Backend security and code review (audit branch; no milestone advanced)**

Repo-wide security/correctness review of implemented non-frontend code at `efaf866`, six workstreams + adversarial verification of every candidate (34 workflow agents, 0 errors; 3 Codex `gpt-5.6-sol`/`xhigh` runs, 2 of which had to be re-issued after an upstream content filter aborted them — disclosed in the report). **Fixed 5**, two of them high: the execution-guard fail-static latch that erased itself (repaired by ordering the source clear before `migration_completed`, which adds no storage work), the metric-spec activation tie that permanently wedged `SnapshotDeadline` and latched the dead-man (admission control at the single write site **plus** a `try_state` uniqueness check for ties that predate it), the inoperative ed25519 small-order rejection in the attestation monitor, the vacuous-pass path in `check-generated-weights.py`, and six missing origin-misuse tests. 12 new test functions plus three new surfaces in an existing exhaustive test, each failing at baseline. **The final Codex adversarial pass over the diff returned `no-ship` and was right on all four counts** — the first halt fix introduced a `RECOVERY_PIN_HALT` bit whose exit no production path could reach (a permanent chain halt in place of a fail-open; withdrawn for the ordering fix), the activation check broke all three welfare benchmark setups because the local changed-scope gate does not build `--features runtime-benchmarks`, pre-existing ties were undetected, and the withdrawn design added unmeasured storage work to a benchmarked mandatory path. All four addressed before the PR. **Left open and flagged:** AUD-NUM-001 (treasury value-bearing calls move no asset) and AUD-NUM-002 (decision-window staleness misses the terminal gap) — both high, both confirmed, both needing feature-shaped fixes outside a hardening pass, so the PR is a **draft**. Report: `docs/reviews/backend-security-and-code-review-2026-07-27.md`.

**What comes next:** Triage the two open highs into their own milestones — AUD-NUM-001 before TREASURY arming (G5), AUD-NUM-002 before Phase 3 — then RT-01 (META upgrades unexecutable) and the WGT-01/02/03 benchmark-fidelity set. **No milestone status changed by this review.**

## **Backend security review follow-up — the two open…

**Backend security review follow-up — the two open highs (audit branch; no milestone advanced)**

User instruction: fix AUD-NUM-002 then AUD-NUM-001, same branch/PR. **AUD-NUM-002 (04 §7 window staleness).** Staleness was charged only when a new observation arrived, so the interval between a window's last observation and its close was never measured — a book that went quiet before the vote closed still graded decision-grade, with `close_spot` read from the same unmoved quote the TWAP carries (so convergence could not see it either) and coverage passing a terminal gap of ~6 h at `dec.coverage` = 95 %. Adopt-favourable, the direction G-1 forbids. The mirror leg over-counted: gaps were measured from the un-clipped previous observation, charging a window for quiet time that preceded it. Fixed by clipping every measurement to the window and charging the terminal gap inside the existing seal `mutate` (no storage read or write added, so no benchmarked weight moves). **The report's stated reason for deferring this was partly wrong and is corrected in §8.2:** the reference model implements only the *book-level* gap counter and had no window-aware derivation at all, so no existing vector encoded the rule — the corpus change is purely additive (97 insertions, 0 deletions; every prior row byte-identical). Added `market_core::is_stale_gap`/`window_stale_events`, an independent `TwapAccumulator.stale_events_in` in the reference model, and a new `window_stale_scenarios` corpus family (5 rows) replayed against both the batch rule and the pallet-shaped incremental path. **AUD-NUM-001 (treasury value-bearing calls).** Closed fail-closed rather than by building custody: 08 §1.4 explicitly defers outflow custody wiring for pot-less lines to the A9 fungibles follow-up, so implementing it would be milestone work. New `OutflowCustody` seam (`()` = unwired = refuse); the runtime binds it unwired, so `spend`/`claim_stream`/`issue_vit`/`recover_foreign` return `OutflowCustodyUnwired` instead of reporting movements that never happened — and `claim_stream` no longer consumes a recipient's vested entitlement it cannot pay. The guard is a pure predicate, so no weight moves. **Consequence recorded deliberately:** an armed TREASURY decision's `spend` leg now reverts at execution and its NAV obligation stays pending until terminal dequeue (asserted, including that nothing is stranded); `queued_treasury_outflows_mirror_enqueue_execute_and_terminal_dequeue` was updated to encode that, with the `runtime-benchmarks` branch showing what the assertion becomes once custody lands. 11 new tests across market-core, pallet-market, the reference model, pallet-futarchy-treasury and the runtime; the four pallet-market cases were each verified to fail with the fix reverted. Gates: changed-scope Rust gate exit 0 over market-core/pallet-market/pallet-futarchy-treasury/bleavit-runtime; runtime 406/406; treasury 76 (90 with benchmarks); reference model 58/58; vector freshness, LMSR doc table, limit coverage (193 keys), generated weights, weight storage bounds, plan tables all green; weight regression PASS with no new acknowledgement. `--features runtime-benchmarks` runtime tests sit at the pre-existing 331/66 (unchanged by this work; CI only *builds* that feature). **Two near-misses caught by the gates and repaired before the PR update, both the repository's own recurring shapes:** the `Config` addition broke `runtime/bleavit-xcm`'s test runtime (green under every targeted gate; only the exhaustive gate caught it, exit 101), and the new `Error` variant was first inserted mid-enum, which would have shifted every later variant's SCALE index for the keeper and the monitoring exporters — moved to the end per 02 §13's append-only rule.

**What comes next:** Custody wiring for the four treasury legs (08 §1.4's A9 follow-up) is now a hard prerequisite for TREASURY arming (G5) rather than a silent gap — the calls refuse until it lands. Then RT-01 (META upgrades unexecutable) and the WGT-01/02/03 benchmark-fidelity set. **No milestone status changed by this work.**

## **Benchmark-smoke CI repair on the audit branch (no…

**Benchmark-smoke CI repair on the audit branch (no milestone advanced)**

User reported the branch red. `Benchmark smoke` (15 §4.5 weight-drift gate) failed on the pushed state; every other CI job passed, and the job is green on `main` at the baseline `efaf866` and the four commits before it, so the branch caused it. **Root cause is not drift — `pallet_welfare::register_spec` could not be benchmarked at all**, so the gate correctly refused an unmeasurable function with no `weight-preservation.toml` entry. AUD-4's activation-tie admission check forced a `full_specs` fixture change (activation `1 + version`, seeding versions 1..15 at epochs 2..16); the extrinsic under measurement then registers version 16 at `CurrentEpoch + 2`, which in the **assembled runtime** (epoch 0) is exactly 2 and ties version 1 — rejected by the very check the fixture was changed to satisfy. The **pallet mock** advances `CurrentEpoch` in setup, so `bench_register_spec` passed there (111/111) and proved nothing: a third instance this session of correct-in-mock, wrong-in-runtime. Fixed by deriving the extrinsic's activation floor from the seeded fixture rather than the clock — `max(CurrentEpoch + 2, highest_seeded_activation + 1)` — which keeps the 05 §4.6 two-epoch lead, cannot collide under any clock, and moves only the activation value, so the measured worst case stays the full 16-version history scan. **Standing lesson recorded: for any benchmark-fixture change, `cargo test --features runtime-benchmarks` against the pallet mock is not evidence — only `frame-omni-bencher` against the built runtime wasm is, and no local gate script runs it** (`rust-workspace-gates.sh` builds the benchmark runtime but never measures with it).

**What comes next:** Re-measurement of the six touched pallets against a freshly built benchmark wasm was in flight at hand-off (`regenerate-weights.py --check --steps 2 --repeat 1`); confirm it returns 0, then push the branch and let CI re-run `Benchmark smoke`. If any welfare total genuinely moved, regenerate at the **committed** 50x20 fidelity — never at the smoke gate's 2x1, which can lose a linear term the release job re-checks. Then the two open deferred-High fixes are complete and the PR's draft condition no longer applies. **No milestone status changed.**

## **Reference-model CI repair on the audit branch (no…

**Reference-model CI repair on the audit branch (no milestone advanced)**

User reported the `Reference model` job red; the failing step was **Run tooling tests**, not the model itself (reference-model tests and the economic simulation both passed). Cause: the new `window_stale_scenarios` corpus family (AUD-NUM-002) was unregistered in the 09 §7.1 Phase-0 exit gate, whose `CORPUS_FAMILY_BINDINGS` is exhaustive by design and fails closed on an unmapped family — the gate working exactly as intended. Bound the family to the `market-core-twap-vectors` leg (the same `crates/market-core/tests/twap_vectors.rs` binary that replays `twap_scenarios`/`contest_scenarios`) and raised that leg's `minimum_tests` floor 2 -> 3 to match the binary's real test count, so a dropped family replay fails instead of passing on the two that remain; the three test-side mirrors (`CORPUS_FAMILY_CONSUMERS`, the stub's `TEST_OUTPUTS` count, the expected count/floor pair) were updated with it. **Root cause of the miss: after changing `tools/reference-model/generate-vectors.py` I ran the reference-model suite and `--check` but not the seven `tools/*/tests` suites** — the standing lesson already in memory as *pre-push: run repo gates, not just suites*, now with a third instance. All seven suites plus `validate-environments.py` are green (deploy 83, reference-model 7, release 86, phase-gates 69, env 72, ci 137, monitoring 107).

**What comes next:** Push and let CI re-run. Then the two deferred-High fixes are complete and the PR's draft condition no longer applies. **No milestone status changed; G0 remains parked pending S4** — registering a corpus family keeps an existing gate true and is not G0 implementation.

## — (whole-repository security review at `8a95ef4`;…

— (whole-repository security review at `8a95ef4`; report-only, no milestone advanced)

User asked for a security review, then widened it from the branch diff to the whole repository at `main`. Local `main` was fast-forwarded `efaf866` → `8a95ef4` so the review target and the reported line numbers match. **Method:** eight parallel audit agents by subsystem (ledger+treasury+inflow-caps; guard+constitution+guardian+origins; market+oracle+numerics; welfare+epoch+registry+attestor; runtime+XCM+node+runtime-api; keeper+fuzz+binary-fetch; release+monitoring+deploy; CI+gates+reference-model+simulation) producing 41 candidates, then 23 independent verification agents re-deriving each from source with an explicit instruction to find the missed compensating control and to adjudicate the DoS/hardening/test-only/trusted-input exclusions rather than assume them. SDK-dependent claims were checked against pinned crate sources (`frame-executive` 48.0.0, `pallet-migrations` 19.0.0, `pallet-assets` 52.0.0, `pallet-xcm` 29.0.0, `sc-chain-spec` 51.0.0, `sc-service` 0.60.0, `subxt` 0.50.2, `frame-decode` 0.17.2) — several findings turned on that in both directions. **Result: 0 critical / 5 high / 7 medium; 29 of 41 candidates refuted; 2 more verified but dropped at confidence 7 and recorded sub-threshold.** Three of the five highs are permanent unrecoverable wedges with no on-chain governance exit (MAX-01 welfare snapshot capacity, MAX-02 attestor registry bound, MAX-03 the Phase-3→4 recovery lane that provably cannot fire); the other two are integrity failures in controls believed to work (MAX-04 `codeSubstitutes` bypassing the `:code` binding, MAX-05 the rerun re-certifying a manipulated TWAP). The recurring Medium shape is one shape: a control deriving its verdict from the artifact it audits (MAX-06, MAX-07, MAX-11, MAX-12). Full report with per-finding verification notes, the refutation record and the attacked-and-held list in `docs/reviews/opus-5-max-security-review-2026-07-27.md`; Audit-log row points at it. **Report-only: no source, test, workflow, weight or `docs/architecture/` file was modified — the only changes are the new report and these two PLAN.md rows.**

**What comes next:** User decides which findings to act on and in what order; the report's §9 suggests MAX-03 first (no attacker, fires through the intended upgrade path, lands unattended in a ≥72 h TOCTOU window), then MAX-01/MAX-02 (one extrinsic each, and MAX-02's fix also closes MAX-09). MAX-01/02/03 also want a `Spec questions` decision on whether the three unrecoverable-wedge findings are in scope as asset destruction or re-rated as availability — this review took the former reading and each verifier was asked to argue the latter. MAX-03 is additionally an R-1 deviation from 09 §3.2, which puts both recovery causes in one lane and nowhere declares them exclusive.

## **Whole-repository security review remediation — all 12…

**Whole-repository security review remediation — all 12 findings fixed in one PR (`fix/opus5-max-security-review`; no milestone advanced)**

User instruction: "fix all issues in one PR". All 12 confirmed findings plus both sub-threshold items are closed, each with a regression test written to fail at baseline. **The one genuinely spec-level change is MAX-01's bound.** 13 §4 states the snapshot bound in *epochs* while `Snapshots` is keyed `(epoch, spec_version)` and capacity is enforced against records, so the epoch bound had to carry 05 §3.3's `epoch.horizon_k ≤ 2` multiplicity to mean the same thing — and 05 §269 says outright that *every* derived `× 2` rests on that ceiling. `MAX_SNAPSHOTS` 20 → 40; the retained epoch window is unchanged, so `MAX_GATE_FLAGS` and the 21-epoch shared prefix index keep their values, but they had to be **decoupled** from `MAX_SNAPSHOTS` first — including the runtime's prune cutoff, which computed `current − (MAX_SNAPSHOTS_BOUND − 1)` and would otherwise have silently doubled the retained window to 39 epochs. `MaxSnapshots` is a metadata constant the frontend reads, so this rides as **integration contract v16** (02 §9/§13, 05 §4.6, 13 §4 all updated; sign-off taken under the standing R-1 delegation). **Three fixture classes broke and every one was informative.** (i) 25 runtime tests attested against synthetic pids — the MAX-02 existence check is exactly right and the fixtures were unfaithful; a `seed_live_proposal` helper had to reproduce `seed_queued_epoch_proposal`'s full shape, because a hand-rolled `Proposals` row fails the *epoch* pallet's try-state. (ii) The `attest` benchmark seeded all 255 records to one attestor, which the fair-share quota now refuses, and it attested a pid the assembled runtime does not carry — both the SQ-489/SQ-490 fixture-instead-of-work shape, repaired with a distributed ledger fixture and a `prime_live_proposal` seam. (iii) Two market tests hand-set `q_long` without moving `last_quote_1e9`; the new try-state consistency assertion caught them, which is the assertion doing its job. **A near-miss of the same family:** `record_snapshot`'s admissibility check was first placed in the pallet *before* the core call, where it shadowed `SpecNotFound`/`SpecNotActive` — five tests said so. Moved into the core after those checks, which also means the frame-free differential oracle models the rule. **Two defects caught only by the exhaustive gate, both this repository's recurring shapes.** (i) The new `prime_frozen_cohorts` benchmark seam used `pallet_epoch::MAX_COHORT_PROPOSALS`, which does not exist — the real name is `..._BOUND`. Every targeted check was green because the helper is `runtime-benchmarks`-gated and only the **wasm** build compiles it; `--all-targets` does not imply `--all-features`, the memory note's exact trap, and the fix was verified with an explicit `--features runtime-benchmarks,try-runtime` check. (ii) A metadata test still asserted contract version 15 — the bump needs six call sites, not one. Also worth recording: `rust-workspace-gates.sh` reported **exit 101** twice while the harness summarised the wrapper as exit 0, so the exit code was read from the script itself both times. **Gates:** exhaustive `rust-workspace-gates.sh` (exit 0, 92 suites, 0 errors), fuzz gates, all seven `tools/*/tests` suites, reference model 58/58 + vector freshness, simulation 53, limit coverage, generated weights, weight storage bounds, plan tables, spec-question batches, runbooks, alert coverage, `validate-environments.py` — green. **Weights: re-measured, not reasoned about.** MAX-01 doubles the welfare snapshot fixtures and adds a cohort-schedule scan to two cranks, and MAX-02 redistributes the `attest` ledger fixture and seeds a live proposal — so `regenerate-weights.py --check --changed` was run against a freshly built benchmark runtime over all six touched pallets rather than assuming. It returned **PASS: committed storage dimensions match a fresh measurement**; only advisory ref_time notes moved, which is what the smoke gate's 2×1 fidelity produces against a 50×20 header. No committed weight artifact needed regenerating. `run-calibration.py --check` is red identically before and after this branch (recorded config vs executable defaults; pre-existing, verified by stashing).

**What comes next:** Push, open the PR and let CI run — in particular `Benchmark smoke`, which repeats the weight re-measurement, and the release-blocking `Component weights at committed fidelity` job, whose demotion rule MAX-11 changed. **No milestone status changed by this work** — it is remediation of the 2026-07-27 review, not milestone implementation.

## **Codex connector review of PR #194 — 3 findings, all…

**Codex connector review of PR #194 — 3 findings, all valid, all fixed (no milestone advanced)**

**The P1 I got wrong was the bound itself, and the P1 that mattered most was a gate I had run against the wrong artifact.** (1) `MAX_SNAPSHOTS` was sized `retention × horizon_k` = 40, but `record_snapshot`'s admissible set is `frozen_spec_versions(e) ∪ {active(e)}` and the active version **need not be in the frozen set**: `cohort_consumes_measurement` admits exactly the cohorts created in `[e−k, e−1]`, so they carry the versions active at `e−1` and `e−2`, and a version activating at `e` itself is a lawful third — reachable through two ordinary `register_spec` calls activating in consecutive epochs, no attacker and no cadence rule against it. Sizing at `× k` would have re-created the same wedge one activation cadence later. Now `retention × (k + 1)` = **60**, with the reasoning stated normatively in 05 §4.6 and 13 §4 and a regression that fills all 20 retained epochs at all three versions. Dropping the active version from the union instead is not available: it is the only version `note_snapshot_recorded` advances the deadline on, so refusing it *is* the wedge — pinned by its own test. (2) **My weight verification was invalid.** `regenerate-weights.py --check --changed` had reported PASS, but the `runtime-benchmarks` build could not compile the branch at the time (the `MAX_COHORT_PROPOSALS` typo), so it re-measured a **stale wasm** and the drift was invisible. Rebuilt and re-measured: `record_snapshot` 621 → 706 reads (+80 from the bound, +5 for the `CohortSchedules` walk), `record_daily_gate` 106 → 186, `register_spec` 94 → 174, `attest` 7 → 8. Regenerated at the committed 50×20 fidelity with three value-pinned acknowledgements whose deltas are fully accounted for by named keys, and the two obsolete acks deleted. (3) 02 §9 still declared `INTEGRATION_CONTRACT_VERSION = 15` on four pallet rows while the headline said 16 — the table contradicted itself. **Fixing the bound then exposed a fourth defect the stale wasm had hidden: `pallet_epoch::settle_cohort` could not be benchmarked at all**, because its fixture seeded one `GateBreachFlags` entry per snapshot and 60 records overflowed the 20-entry epoch-keyed bound; the epoch fixture's `assert_eq!(snapshots, gate_epochs.len())` encoded the same conflation. Both re-expressed per key shape.

**What comes next:** Push and let CI re-run — `Benchmark smoke` repeats the weight measurement and the release-blocking `Component weights at committed fidelity` job exercises MAX-11's changed demotion rule. **Standing lesson, third instance this session and now recorded twice over: a weight `--check` that returns PASS is evidence only if the benchmark runtime it measured was built from the working tree.** The tool re-measures against whatever wasm exists; build it explicitly first.
