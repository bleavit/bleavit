# Session log — 2026-07-17

## B5 generated-weight component repair (milestone remains 🔨)

Reintroduced the dispatch-charged linear components in seven runtime benchmarks and scaled their admissible fixtures: epoch tick/cohort items, execution-guard call count/runtime-code bytes, and oracle proof bytes/round-close batch. Preserved saturated cross-pallet aggregates; the Code execute fixture reaches 16 calls and 64 KiB at the upper point, and the apply fixture constructs an exact-size valid runtime-version Wasm through 2 MiB. Regenerated **only** `pallet_epoch`, `pallet_oracle`, and `pallet_execution_guard` runtime weight files with 50 steps × 20 repeats; all seven generated methods now accept `u32`, and `cargo check -p bleavit-runtime` is green. Updated one stale runtime test to compare the guardian hook against the configured generated `WeightInfo` rather than the old hand-seeded type. Final gates: release runtime-benchmarks build PASS; fmt PASS; clippy `-D warnings` PASS; workspace tests PASS. No production semantics, architecture, CI/tooling, commit, push, or stash action.

**What comes next:** Review the complete generated weight/PoV set and close the staged weight-regression scope before marking B5 ✅.

## B5 bounded-preimage PoV remediation (milestone remains 🔨)

Closed the adversarial preimage PoV path in production: execution-guard fetches now require the recorded length, the runtime queries `PreimageFor` by exact `(hash, len)` and refuses lengths above the 64 KiB kernel cap before reading bytes, and every epoch adapter forwards the proposal's recorded length. Guard enqueue was already capped; epoch submission now rejects oversized recorded payloads as `BadProposalShape`, with tick/decide also failing closed for legacy malformed state. Added a guard regression proving a recorded-length mismatch rejects before fetch. Applied measured-PoV overrides only to epoch `tick`/`decide` and guard `execute`, retained 64 KiB worst-case fixtures, then regenerated only the epoch and execution-guard weights at 50 steps × 20 repeats. New worst-case estimates are **210,585 B**, **183,055 B**, and **212,943 B** respectively. The pinned 10 MiB relay PoV ceiling and 75% normal ratio yield a **7,864,320 B** normal proof budget; all **94** generated custom-pallet functions fit (largest 359,385 B). Gates green: release runtime-benchmarks build, generated-weight runtime check, fmt, clippy `-D warnings`, workspace tests, and try-runtime. No architecture, CI/tooling, commit, push, or stash action.

**What comes next:** Close the remaining staged weight-regression scope before marking B5 ✅.

## B5 dual-review remediation (milestone remains 🔨)

Fixed four review findings. Epoch benchmark fixtures now note one distinct 64 KiB payload per `tick` item; regenerated charged proof is `183,055 + 68,329*n` B (`tick(10) = 866,345 B`). Guard `execute` pre-charges the 25%-of-block payload-weight ceiling and returns post-dispatch refunds based on actual nested dispatch weight when the dispatcher preserves post-info (declared fallback otherwise), including consumed inner weight on every post-dispatch failure; the production classifier remains outside this job's scope and currently takes that conservative fallback. Epoch now atomically requests a preimage when qualification succeeds, retains ownership through Qualified/Trading/Extended/Rerun, releases on every pre-queue terminal, and hands ownership to the guard at enqueue; SQ-106 is resolved with mock-counter, unnote-refusal, and try-state coverage. The runtime oracle hardcode now uses `kernel::ORC_REPORT_WINDOW_BLOCKS`; grep found an unused same-semantic `oracle-core::REPORT_WINDOW_BLOCKS = 28_800` outside the authorized paths. Regenerated only the epoch/guard weight files at 50 steps × 20 repeats. Gates green: release runtime-benchmarks build; fmt; clippy `-D warnings`; workspace tests; try-runtime. No architecture, CI/tooling, commit, push, or stash action.

**What comes next:** Close only after the remaining staged weight-regression scope has been reviewed.

## B9 (PR #62 — rebase onto post-#59 main + Codex-connector review: 2×P1 + 2×P2, all four fixed)

**Rebase:** `origin/main` moved to `e9feffd` (PR #59, CI wasm-targets/protoc); only the append-only PLAN.md session log conflicted — both rows kept. **Bot P2 #1 (frozen-contract violation, the important one):** the earlier fix round had repurposed the 02 §7.4/05 §4.7 `GateBreachFlags` day bitmap to also mark healthy sampled days (its duplicate-rebate detector) — consumers would misread healthy days as breaches. Reverted to breach-days-only semantics (regression pins it) and added the pallet-internal bounded `SampledGateDays: map EpochId → [u32; 2]` marker (registry-`AckRecords` pattern; pruned with gate history; try-state covered) as the repeat-safe rebate gate. **Bot P1 #1:** the keeper's `daily_gate_candidates` was always empty (daily S/C samples never recorded → absent samples default unbreached downstream); now planned from `SampledGateDays` over the bounded finalized-epoch lookback, with the older-metadata one-time `not-yet-plannable` degradation intact. **Bot P1 #2:** the keeper hardcoded the 10-block observation grid; it now resolves `mkt.obs_interval` (+ `dec.window`, `res.probe_int`, `res.probe_to`) from live `Constitution.Params` via exact dynamic decodes of the constitution-core `ParamRecord` shape — precedence override > live > documented default (SQ-126 narrowed). **Bot P2 #2:** `execution_guard.expire_failed_execution` + `reject_stale` now pay fail-soft General rebates on terminal state-advancing success only (no-drain coverage). One Codex round (gpt-5.6-sol/xhigh) + its internal review (0 blocker/0 major); full gates re-run green on the rebased tree (workspace + keeper + benchmarks leg + no_std + I-24 + reference-model + doc links).

**What comes next:** Push the rebased branch + fixes to PR #62 (force-with-lease); reply to the four bot comments

## B7 (Zombienet + Chopsticks environment definitions → ✅, worktree `milestone-B7`, PR opened this session)

Shipped the 15 §4.7 / 02 §11 release-artifact environment set. **zombienet/**: `bleavit-local` (4 relay validators, 3 `bleavit-node` collators, para 4242) + `bleavit-xcm` (+ AH 1000, Coretime 1005, both HRMP directions) over generated `paseo-local` specs; the 02 §11 genesis-overrides patch (no 13-tunable compressed; dead-man 4,800 stays kernel); 9 drills + 9 js helpers: smoke, collator-loss (pre/post height deltas), keeper-loss (loud B9 gate), dead-man + coretime-renewal-under-dead-man (stall measured as a real ≥4,800-relay-block finality gap via RPC), PB-MIGRATION retry+rollback through the real guardian 5-of-7 propose/approve workflow, XCM v5 failure/trap/recovery on the spec-legal AH-keyed route, expedited-CODE-under-freeze (negative early-apply + full 43,200-block D-14 wait), 907,200-block 3-epoch G1 soak. **chopsticks/**: base + 10 scenarios — upgrade transition (guard-try-state-exact images), StaleQueue via `reject_stale` (genuinely stale constraint), VOID epoch (I-1-consistent `redeem_void` fixture), manufactured precondition failures, all six 06 §6.2 playbooks incl. PB-LEDGER-FREEZE entry/expiry + the 09 §4 coretime exemption. **tools/env/**: six-pin provenance home (zombienet v1.3.133 + in-repo binary sha256 — no upstream sidecars; chopsticks 1.5.0; polkadot-stable2603-1; paseo chain-spec-generator **repinned v2.4.1→v1.9.2 tag+commit** after the adversarial review proved the package absent from the v2.x line), digest/commit hard-failing fetch/generate scripts (chain-spec-builder 17.0.0 zero-exit-on-error guarded), structural validator (drill/playbook inventory, para ids, HRMP directions, zombienet js-path resolution semantics, localhost-only, single-homed pins) + 10 tests + CI `environments` job. **Process:** Codex author (gpt-5.6-sol/xhigh) → dual review (spec-reviewer 1 blocker/6 major/5 minor: inverted-D-14 drill 08, try-state-violating guard fixtures, unreachable StaleQueue, wrong-epoch-length soak, spec-illegal trap recovery, single-signed guardian dispatch + adversarial Codex 6 blockers/11 majors: broken zndsl helper paths, phantom CSG package, vacuous height/time assertions, sidecar theater, event races) → one Codex remediation round (its edits landed; the run itself died on a moderation false-positive at its final SCALE-check step) → focused spec-reviewer re-pass: **0 blocker/0 major, 123/123 injected cells byte-verified**; its 4 residuals (in-repo zombienet digest — functionally tested accept+reject, half-authority stall-timeout arithmetic, two README parameter numbers, V-row collision) fixed directly + re-gated. V-21…V-24; SQ-127…SQ-130. Gates green: env validator + 10 tests · deploy 16 (1 skipped) · doc links · bash -n · node --check · fmt (Rust untouched). Living docs re-trued (AGENTS repo row, deploy README pointer).

**What comes next:** B5 or B6 next (both unblocked); B8 consumes the B7 artifact set + owns suite execution/publication (15 §1 env-CI try-state + 15 §5 content-addressing); G1 runs the Phase-1 drills; A8/A11 runtime wiring unlocks the gated drill assertions; nit: `tools/deploy/generate-chain-specs.sh` lacks the delete-first builder guard

## B7 (PR #63 — merge onto post-#62 main + Codex-connector review: 2×P1, both TRUE, fixed)

**Merge:** `git merge origin/main` after B9 (#62, keeper) landed — one conflict block pair in the append-only PLAN.md (Current focus + session log; **my SQ-115…118 renumbered → SQ-127…130** because B9 claimed SQ-115…126; no V-row collision). **Bot P1 #1 (recovery-claim origin):** the drill-07 recovery leg signed `polkadotXcm.send` as Alice on Asset Hub, so the message would arrive descended to Alice's account origin and could never match the trap keyed under AH's bare chain origin — the send is now dispatched through the local preset's **sudo** (Root → bare chain origin, no `DescendOrigin`), with a loud gate when sudo/send are absent; README matrix row re-trued. **Bot P1 #2 (unstageable renewal):** drill 05 dispatched `execute_coretime_renewal(1)` against a genesis that cannot carry a quote or a funded `ops.coretime` line (quotes are noted only by the runtime-internal B4 re-read seam; budget lines are deliberately never chain-spec-fundable — reviewed treasury rule), so the step had no passing path. The helper now has **two forms**: *staged-renewal* (quote + funded line present → success + `CoretimeRenewalCalled`, the 09 §7.1 Phase-1 exit form) and *exemption-reachability* (today's runtime → the dispatch must traverse the staged dead-man freeze into treasury logic and fail exactly `futarchyTreasury.RenewalWindowClosed`, proving the 09 §4 exemption; any filter/freeze rejection fails the drill). Also re-trued the keeper-loss matrix row (B9 landed — topology wiring of the keeper process is the named follow-up). Gates green: env 10 · validator · deploy 16 (1 skipped) · doc links · bash -n · node --check · fmt.

**What comes next:** Reply to the two bot comments; B8 next on this track

## B5 (dual review → fix rounds → merge with main → ✅)

Closed B5 after the R-6 dual review (native `spec-reviewer` 0 blocker/3 major/7 minor + adversarial Codex 4 blocker/3 major/4 minor; substantial overlap). **Every actionable finding fixed with regressions:** (1) the tick benchmark's shared-preimage fixture undercharged PoV ~4× — distinct 64 KiB payloads per item, regenerated slope 68,329 B/item; (2) `execute()` charged a static weight excluding the dispatched batch — now pre-charges `prop.max_weight` + overhead (2.83 MiB proof ceiling, fits the class) and refunds actuals via `DispatchResultWithPostInfo`; (3) the 06 §4 qualification-time preimage pin was missing (a proposer could unnote mid-trading → forced ConstitutionViolation) — pin at T5, release at every pre-queue terminal, atomic T9 handoff (SQ-140 resolved); (4) the CI runners lacked the wasm target (fixed; omni-bencher pinned 0.15.0); (5) the regression parser read only the FIRST `from_parts` (the proof term parsed as zero!) and its floors hid slope regressions — full-expression parser + worst-case-total gating + removal acks + `HEAD~1` main fallback, 16 self-tests; (6) `pallet_migrations` generated weights were silently unused; (7) minors: 13 §5.7 checkpoint shape (04 §7, `u32` block), guardian `Suspended`≠`Queued` status mapping, `ORC_REPORT_WINDOW_BLOCKS` kernel single-home, ack-file example path, exact-94 PoV inventory pin. **Not actioned by design:** the bare-`system.apply_authorized_upgrade` filter conflict is SQ-93 (extended; joint sign-off) — B5 does not unilaterally flip a filter A11/B1a committed. **Merged origin/main** (B2/B3/B4/B7/B9 landed concurrently): 19 code hunks + PLAN resolved by composition (keeper rebates × pre-charge/refund execute; rebates × pinning tick; live epoch clock supersedes the B9-era pending clock for the keeper meter; vesting/pot-accounts/probe seams kept; pallet count 40), SQ-106…112 renumbered → SQ-131…137. One codex-session usage-limit outage mid-merge was recovered by resolving the conflicts directly. Orchestrated with **Codex subagents** throughout (wiring, 121-benchmark repair, component restore, preimage/pov_mode fix, regression-script overhaul, CI leg — 7 write jobs + 1 adversarial review). Gates green on the merged tree (fmt · clippy `-D warnings` · workspace tests · runtime-benchmarks · try-runtime · no_std · reference-model · doc-table · doc links · weight-regression self-tests + PASS).

**What comes next:** **B6** (upgrade path e2e — the last ⬜ B-milestone before B8); user ratifications: SQ-93 (apply surface, joint sign-off), SQ-147…SQ-149, SQ-117 (`keeper.rebate` seed from the measured fee basis)

## B5 (post-merge rebate-weight calibration — closes the merge re-review's one major)

A focused Codex re-review of the merge commit found the compositions clean except one real major: the B9 keeper-rebate call sites execute treasury work (aggregate load + Params reads; funded: USDC transfer + meter writes) that the pre-merge generated weights never measured — exactly the calibration B9 had deferred to B5. Fixed with a shared `runtime-benchmarks`-gated helper (seeds `keeper.rebate` = 1 USDC, funds the KEEPER__/ORACLE__ lines + pots, fresh meter, asserts the line was actually debited) wired into all **20** rebate-bearing benchmarks across 8 pallets; the 8 weight files regenerated (50×20). Every rebated crank now carries the `FutarchyTreasury::State`/`Constitution::Params`/`ForeignAssets::*` proof rows (e.g. `market::crank_observe` charged proof 8,799 → 27,020 B); several charged estimates were already conservative enough to stand; the pov_budgets pins needed no change and the normal-class sweep stays green. Gates green (fmt · clippy `--locked -D warnings` · workspace `--locked` tests · try-runtime · regression self-tests).

**What comes next:** Push + PR. B5 stays ✅

## B6 (upgrade path e2e → ✅, worktree `milestone-B6`, PR opened this session)

Wired `ExecutionGuard` at frozen index 62 into the real runtime — two-phase 09 §2 authorize/apply through real dispatch (internal Root = exactly the committed `system.authorize_upgrade`, I-10; apply non-Root, filter-gated by real `PendingUpgrade` + a five-condition Cumulus preflight so a doomed apply can't consume the authorization, V-25), attestor 2-of-N + live capability gating at execute, ReleaseChannel writer (a) on `constitution-core`-single-homed 02 §12 offsets, production `pallet-migrations` + PB-MIGRATION bridge (failed-step, `>900`-block stall detector — `MIGRATION_STALL_BLOCKS` — source-scoped `MigrationHalt`, scoped cursor retirement), relay-Abort status-quo cleanup (`UpgradeAborted`, tolerant writer-(a) clear), try-runtime CI legs, sproof-driven e2e (`set_validation_data` with `GoAhead`/`Abort`) + the mandated negatives (early apply boundary, unattested/quorum-loss CODE, wrapper backdoors, SQ-104 denials). Codex-orchestrated (author → dual review → fix → focused re-pass → fix 2 → final verify, ending 0 blockers; last 2 majors + 1 minor fixed by hand). A11 gained additive `validation_code_applied`/`ScheduledUpgrade`/`UpgradeAborted`/`on_upgrade_aborted`. R-1: 06 §3.2 guard-call rows, 13 migration rows. SQ-131…118 + SQ-104 extension; V-25. Gates green (workspace · runtime 57/59 · wasm legs · no_std · reference-model 21 · doc links).

**What comes next:** Wire A8 `pallet-epoch` (slot 61, swaps the fail-closed enqueue/handoff seams); then B5 (weights/PoV) or B7 (zombienet — owes the snapshot try-runtime leg + real-MBM failure drill)

## S1 (TLA⁺ models + property suites + ledger differential → ✅, worktree `milestone-S1`)

Implemented all of S1 with **four parallel Codex authors (gpt-5.6-sol/xhigh)** on disjoint file sets over a harness the orchestrator pre-built (pinned TLC v1.7.4 + sha256 in `tools/env/pins.env` (V-30), `tools/verify/run-model-checks.sh` with per-model floors, CI jobs `model-checking`/`property-suites`, pre-wired dev-deps so the sandboxed agents never touch manifests). Authors delivered: `Ledger.tla` (full 03 op alphabet, D-1 flooring live), `Proposal.tla` (all 24 T-rows), PT-1…PT-8 + I-12/I-13/I-6/I-7 suites + `tools/ci/property-gates.sh`, and the 64-scenario ledger sequence differential (schema v4) — the differential immediately caught **4 real ledger-core defects** (stale terminal supply decrements; Python matched 03 §6). **Dual review** (spec-reviewer 0 blocker/1 major/5 minor + adversarial Codex **5 blockers/12 majors/3 minors, do-not-ship**) — the reviewers CONTRADICTED each other on two blockers (I-14 vacuity, T23-after-expiry); the orchestrator adjudicated both against the model by direct inspection. Remediation: 4 parallel fix agents + orchestrator surgery — the de-vacuized ledger model exploded to 315M states, fixed with a **fingerprint-view partition** (edge-label audit vars out of the view; pure-state invariants at view scopes ≤10M distinct, label invariants in no-view Audit scopes, witnesses no-view — a first witness run PROVED the naive view unsound: reject-edge label invariants silently unchecked) plus `WITNESS_CONFIGS`/`-metadir` runner upgrades; the honest I-12/I-13 suites then exposed **2 market-core production defects** fixed by the orchestrator with Decision-log rulings (payout-sized sell merges per 04 §6.1; directionally-rounded TWAP lower clamp per the exact-envelope reading the Python reference already used). **Focused re-pass: 19/20 FIXED, 0 NOT-FIXED**; its 1 new major (zero-payout legal sells tripping the ledger's zero-transfer rejection — all zero legs now skipped, deterministic extreme-price regression + honest adapter) and 4 minors (fabricated `WrongBranch` witness deleted — dead core variant, SQ-170 extended; stale comments; gitignore) fixed in-session. R-1: **I-27 amended** (03 §5.4 sweeps dispatchable in Voided post-archive-delay — Decision log); B7's env-validator pin set extended for the TLA keys. SQ-150…SQ-170 logged. **Gates on final code, all green:** workspace 67 suites · model gate 7 main configs (ledger 26,944/2,861,160/155,183/1,882,843/10,063,640; proposal 66,709/264,241 distinct) + 16 witness/mutation violations · property gate at PROPTEST_CASES=1,000,000 in 50m25s local · reference-model 26 + freshness + doc-table · env validator + 10 tests · deploy · doc links. **Honest caveats:** the two new CI jobs are heavy on hosted runners (likely 1–3 h each; cadence call → B8); Baseline vault excluded from Ledger.tla (documented; owned by try-state/PT/differential); I-3's post-decision half is a documented cross-model interface assumption.

**What comes next:** Commit + PR. Then A8 runtime wiring (slot 61) or B5/B8; S2 owns fuzz targets, S3 the generated limit-coverage suite

## S1 (PR #89 merge + review fixes)

**Merge onto post-B5/B8 main:** resolved six conflict files (both CI job sets kept; gitignore/gate-table/layout unions; generator imports; pallet test modules alongside B5's `BenchmarkHelper`); **SQ/V renumbered** to clear main's concurrent claims — S1's SQ-135…144 → **SQ-150…159**, tla2tools pin V-26 → **V-30** (B5/B8 took SQ-135…149/V-26…29), incl. the two code-comment references; merged-tree gates green (workspace 71 legs incl. B8's supply-chain cargo-audit over the new proptest tree, weight-regression, quick suites). **PR #89 review fixes:** CodeQL workflow-permissions → `contents: read`. Codex P2 (PT-2 wrapper profile injected the SHORT complement) → PT-2(b) now drives the REAL 04 §6.1 buy accounting (split cost+fee, mirror credit, inventory delivery, revenue recycling; recovery through the buyer's own claims only) — which exposed that **15 §4.3 PT-2(a)'s "net delta = −fees only" cannot hold off the neutral price** (D-1 pays `floor(a/4) + floor(cost/2)`; 04 §6.2's neutral-value-plus-premium text is the precise one) → suite asserts the 04 §6.2-exact recovery; **SQ-171** logged (15 §4.3/I-2(b) wording needs a reconciling amendment); mirror-shave mutation red-then-green. Codex P2 (guardian `force_rerun` unexplored) → modeled behind `ForceRerunModeled` per the SQ-161 discipline: default configs keep the strict 05 §2.1 reading byte-identically (66,709/264,241 distinct), new `ForceRerun.cfg` (568,235 distinct) re-checks every invariant under the 06 §5.3 reading with FR as its own labeled row (queued-mandate cancellation barred from execution — I-15; shared one-rerun budget; retrospective review), + 2 reachability witnesses + an I-15 force-cancel mutation (15 witness legs total). CI on the merged push: 11/14 green incl. the 10⁷ sweep, benchmark smoke, supply chain, weight regression; heavy trio (workspace/model/property) pending at log time.

**What comes next:** Watch remaining CI + review comments; reply to the two bot comments. **Post-fix:** all four bot comments fixed + replied (f46f411 — real PT-2 buy accounting exposing the 15 §4.3 fee-only over-claim → SQ-171; `ForceRerunModeled` configs; CodeQL permissions + cleartext-rename); the Codex cloud agent then merged post-#67/#68/#91 main into the branch (a901d23, verified: my side preserved incl. the TLA pin keys; local gates re-run green). CodeQL alert 12 (cleartext-logging, `owner`-field name heuristic on a public-fixture test index) closed by de-interpolating the panic (fcfb8cc). Two further main merges: S5 (#86; S1's SQ renumbered a second time 150…160 → 155…165) and S3 (#88; third renumber → **SQ-161…171**, V-30 unchanged). Noted for the next doc-curator pass on main: main itself now carries duplicate SQ-150…152 rows (S5's #86 vs the #68 run-evidence set — a collision merged on main, not introduced here)

## B6 (PR #65 — merge onto post-#62/#63 main + Codex-connector review: 2×P1, both TRUE, fixed)

**Merge:** resolved the four conflict hunks (configs/tests import unions, CI job union — libclang+protobuf install with the B6 try-runtime leg name, PLAN Current-focus/milestone/SQ/session-log unions) and **renumbered the B6 identifier batch around main's B7/B9 allocations** (SQ-115…118 → had collided twice → final **SQ-131…SQ-134**; V-21 → **V-25**), incl. every code-comment/AGENTS reference. **Bot P1s:** (1) applied-path cleanup could wedge — a lawful writer-(b) `set_release_channel` rewrite mid-flight made `on_upgrade_applied` fail its channel check after the relay already installed `:code`, retaining `PendingUpgrade` forever (unlike Abort, an applied upgrade can't be retried); the applied clear is now **tolerant** exactly like the abort clear (mismatched channel = writer (b)'s authoritative value, left untouched; guard records the application; no halt) — regression `applied_cleanup_survives_a_writer_b_release_channel_rewrite`; SQ-134 row updated. (2) `UpgradeAborted` vs the 02 §6 frozen event list — resolved by the existing `UpgradeApplied` precedent (B7's SQ-130): the event stays append-only/uncontracted and **joins the SQ-130 reconciliation batch** for the next 02 amendment (version bump + joint sign-off there); no unilateral contract bump. Also wired the B9 `KeeperRebate = FutarchyTreasury` seam into the guard's runtime Config (main added the Config type mid-flight) and resolved the guard-mock keeper-sink conflict. Gates re-run green post-merge (full script incl. keeper leg · runtime 59 / 61 w/ try-runtime · guard 42 · reference-model · doc links).

**What comes next:** Merge PR #65; then wire A8 `pallet-epoch` (slot 61); B5/B8 next

## S2 (cargo-fuzz targets → ✅, worktree `milestone-S2`, PR opened this session)

Built `fuzz/` — a separate nightly-pinned (`nightly-2025-11-24`) cargo workspace (`exclude = ["keeper", "fuzz"]`) with three invariant-asserting libFuzzer targets for the 15 §4.5 areas: `payload_scale_decode` (raw bounded decode + a structured `Payload` `Arbitrary` reaching the guard admit/bound paths), `nested_wrapper_filter` (I-8/I-10/I-11 — ordered structural oracle + error-variant compare **and** an independent `independent_admits` property predicate over both origin paths + a hand `wrapper_admission_truth_table`; A4-deferred fuzz obligation discharged), `lmsr_trade_paths` (I-12 `drain ≤ ceil(b·ln2)` over real market-core trades across a seed-selected book kind — Decision/Gate/Baseline, incl. the historically-buggy `sell_baseline` — atomic mock ledger, domain-edge kernel probe). Curated 15-seed corpora; `tools/ci/fuzz-gates.sh` + CI job `fuzz` (smoke → git-ignored dir, committed corpus stays curated). **Fuzz-found harness bug fixed** (sell the depleted book can't collateralize → `Error::Ledger`; harness now tolerates any typed-`Err` trade as a rolled-back no-op; `depletion_sell_rejection` regression). **Production blocker surfaced by the decode-fuzz lens + fixed (both reviewers):** guard `decode_batch` unbounded-recursion preimage decode (audit-scope-A G-1/R-7) → kernel `MAX_PAYLOAD_DECODE_DEPTH = 256` + `decode_all_with_depth_limit` + runtime regression `deep_preimage_batch_decode_fails_closed_at_the_depth_limit`; SQ-225, R-1/R-7 decision-log entry. Process notes: the Codex author run tripped the standing "fuzz/cyber" moderation false-positive after applying a partial skeleton (orchestrator completed authoring); the delegated harness-strengthening test-engineer was very slow and initially taken over by hand for the oracle-independence/structured-payload/tightened-bound majors, then its Gate/Baseline coverage additions landed and were re-gated + integrated (a stale-monitor race — the file edits were coherent on top of the hand work and passed the full smoke, so kept). Reviews: spec-reviewer (0 S2-blockers / 1 major = the guard fix / 4 minor) + adversarial (guard blocker + oracle-independence + payload-generator + Gate/Baseline-coverage majors) — **all blockers/majors addressed**: guard hardened, oracle made genuinely independent (property predicate + hand truth-table + error-variant compare), payload target given a structured generator, all three book kinds fuzzed, I-12 slack tightened to the exact ceiling, rule-4 `USDC` single-homed. 10 fuzz unit tests. Gates green (root workspace script incl. runtime 60 / 62 try-runtime + guard 42 + keeper + reference-model 21 + no_std + doc links · fuzz workspace fmt/clippy/10 tests + 3-target build + corpus regression (15 seeds) + 90s smoke ×3, 0 crashes). SQ-225…SQ-227 logged.

**What comes next:** Open the S2 PR; remaining follow-ups: a runtime-linked decode target (SQ-226), I-13 TWAP fuzz home (SQ-227); then S1/S3/S5 or the A8/A11 runtime wiring

## B8 (release-artifact publication pipeline → ✅, worktree `milestone-B8`, PR opened this session)

Shipped the complete E15 pipeline (inventory in the B8 milestone row). **Process:** 2 parallel Codex authors (gpt-5.6-sol/xhigh; disjoint file ownership) — Job A: sweep + supply-chain legs; Job B: release workflow + `tools/release/` (real-node end-to-end dry-run in-session: 89 surface entries recorded, readiness gaps attributed B2/A8/A11/B7). **Dual review:** spec-reviewer **0 blocker/4 major/3 minor** (independently re-derived the 02 §12 raw key against five Substrate anchors; verified the 11 `FutarchyApi` encodings and the tag path's unavoidable strictness) + adversarial Codex **11 findings**. **Fix round 1 (Codex, 16 items):** atomic draft→verify→publish, prerelease-until-mirror-evidence, metadata-derived layout expectations + SQ-101 Location strict-fail, §8/§9 constant surface completion, `bleavit.env-evidence.v1` contract, audit disclosure in the manifest, sweep **cost** family (composed 8-ulp bound), `:code`↔wasm binding, environments inventory, recorder deadlines, node-boot retries, tag-path tooling suites, `sweep.yml` kernel-change cadence, real event bytes, single-read shard hashing, content-addressed manifest copies. **Focused re-review: 12 residuals, 9 fixes NOT CLOSED** — fix round 2 hit the **Codex usage limit (nothing ran)**, so the orchestrator implemented all 11 actionable residuals manually (publish-job repo context + probe differentiation; speculative layouts dropped from blocked entries — frozen from the real runtime when their milestone lands; metadata-independent raw ReleaseChannel fixture + offset validation; exact-key USDC value assertions; `system_properties` identity row; evidence↔release-commit binding; metadata.scale↔runtime-info binding; raw-form chain-spec `:code`; live-env profile rule; send/classic-RPC deadline coverage; non-null event bytes on both transports) + 1 documented non-fix (sweep cost bound deliberately stricter than the in-crate trade-path allowance). **Verified:** full workspace gates ×3 · full 10⁷-point sweep ×2 (second incl. cost rows: worst log2 0/ln 1/cost 2 ulp, V-26) · 53 release + 6 sweep + 16 deploy unittests · real-node e2e (corrupt-wasm fail-closed; ReleaseChannel + properties recorded; USDC → SQ-101; unblocked events all carry bytes) · corpus `--check` byte-identical. SQ-135…SQ-139 logged; Decision-log row for the audit-waiver/two-step-publication/layout-freezing/evidence-contract rulings.

**What comes next:** Reply to PR review; B7 implements the `bleavit.env-evidence.v1` contract; B2/A8-A11 wiring turns the attributed readiness gaps green; SQ-135/SQ-136/SQ-139 need user ratification; clear audit.toml entries at the next stable2603 maintenance bump

## B8 (release-artifact publication pipeline → ✅, worktree `milestone-B8`, PR opened this session)

Shipped the complete E15 pipeline (inventory in the B8 milestone row). **Process:** 2 parallel Codex authors (gpt-5.6-sol/xhigh; disjoint file ownership) — Job A: sweep + supply-chain legs; Job B: release workflow + `tools/release/` (real-node end-to-end dry-run in-session: 89 surface entries recorded, readiness gaps attributed B2/A8/A11/B7). **Dual review:** spec-reviewer **0 blocker/4 major/3 minor** (independently re-derived the 02 §12 raw key against five Substrate anchors; verified the 11 `FutarchyApi` encodings and the tag path's unavoidable strictness) + adversarial Codex **11 findings**. **Fix round 1 (Codex, 16 items):** atomic draft→verify→publish, prerelease-until-mirror-evidence, metadata-derived layout expectations + SQ-101 Location strict-fail, §8/§9 constant surface completion, `bleavit.env-evidence.v1` contract, audit disclosure in the manifest, sweep **cost** family (composed 8-ulp bound), `:code`↔wasm binding, environments inventory, recorder deadlines, node-boot retries, tag-path tooling suites, `sweep.yml` kernel-change cadence, real event bytes, single-read shard hashing, content-addressed manifest copies. **Focused re-review: 12 residuals, 9 fixes NOT CLOSED** — fix round 2 hit the **Codex usage limit (nothing ran)**, so the orchestrator implemented all 11 actionable residuals manually (publish-job repo context + probe differentiation; speculative layouts dropped from blocked entries — frozen from the real runtime when their milestone lands; metadata-independent raw ReleaseChannel fixture + offset validation; exact-key USDC value assertions; `system_properties` identity row; evidence↔release-commit binding; metadata.scale↔runtime-info binding; raw-form chain-spec `:code`; live-env profile rule; send/classic-RPC deadline coverage; non-null event bytes on both transports) + 1 documented non-fix (sweep cost bound deliberately stricter than the in-crate trade-path allowance). **Verified:** full workspace gates ×3 · full 10⁷-point sweep ×2 (second incl. cost rows: worst log2 0/ln 1/cost 2 ulp, V-25) · 53 release + 6 sweep + 16 deploy unittests · real-node e2e (corrupt-wasm fail-closed; ReleaseChannel + properties recorded; USDC → SQ-101; unblocked events all carry bytes) · corpus `--check` byte-identical. SQ-131…SQ-135 logged; Decision-log row for the audit-waiver/two-step-publication/layout-freezing/evidence-contract rulings.

**What comes next:** Reply to PR review; B7 implements the `bleavit.env-evidence.v1` contract; B2/A8-A11 wiring turns the attributed readiness gaps green; SQ-131/SQ-132/SQ-135 need user ratification; clear audit.toml entries at the next stable2603 maintenance bump

## B8 (PR #64 — rebase onto post-#63 main + Codex-connector review: 2×P2, both TRUE, fixed)

**Rebase:** `origin/main` moved to `67f2457` (B7, PR #63 — the zombienet/chopsticks environment definitions). Conflicts were the append-only PLAN.md blocks (Current focus, milestone table, session log — both sides kept) and the AGENTS.md `zombienet/` row (B7's new text merged with B8's evidence-gate note). **ID collision resolved by renumbering the B8 entries** (B7 claimed SQ-127…SQ-130 and V-21…V-24 on main): B8's SQ-127…SQ-131 → **SQ-131…SQ-135**, V-21 → **V-25**; code references only SQ-101, so the renumber is doc-only. **Bot P2 #1 (fixture-report binding):** the assembler trusted `fixtures-report.json` — a stale/truncated fixture dir claiming `missing: []` could pass strict assembly with transcripts from another runtime or partial 15 §5(4) coverage. New `validate_fixture_binding` (corruption-class — fails even dry-runs): report schema pinned, report `metadata_sha256` must equal the shipped `metadata.scale`, `recorded ∪ missing` must equal the surface manifest's exact entry-id set, and the transcript file set must match the manifest one-for-one (`--surface-manifest` arg; 4 regression tests). **Bot P2 #2 (ReleaseChannel schema bumps):** the validator rejected any schema byte ≠ 1, which would brick every release at the first legitimate 02 §12 append-only extension; it now accepts any schema value while still requiring the frozen v1 prefix (≥ 168 bytes) — per 02 §12's "any other value ⇒ layout extended append-only, prefix still valid" (2 regression tests). Re-verified: **58** release-tool tests; real-output assembly (binding passes on genuine recorder output, zero corruption; B7's now-present env dirs gap precisely on the missing per-release `run-evidence.json`; the expected dry-run `runtime.commit_binding` gap shows the commit binding live); doc links; full workspace gates on the rebased tree.

**What comes next:** Push force-with-lease; reply to the two bot comments (the bot's review itself hit its usage limit — treat it as possibly partial)

## B8 (PR #64 — sweep.yml CI failure: relative `BLEAVIT_SWEEP_DIR`)

The first real `full-sweep` CI run failed: the workflows pass a **workspace-relative** `BLEAVIT_SWEEP_DIR=release-work/sweep`, but cargo runs test binaries from the **package** root (`crates/futarchy-fixed/`), so the checker looked for `crates/futarchy-fixed/release-work/sweep` — local verification had always used absolute scratchpad paths, masking it (and the same pattern would have failed `release.yml`'s tag path). Reproduced locally with the exact CI invocation, then fixed in the checker itself (covers both workflows): a relative `BLEAVIT_SWEEP_DIR` now resolves against the workspace root via `CARGO_MANIFEST_DIR/../..`. Regression test added on the Python side running the tiny-sweep checker with a workspace-relative dir (the exact CI shape). `release-work/` added to `.gitignore` (local dry-runs dirtied the tree). Verified: the previously failing invocation now passes against the full 10⁷ corpus (8.8 s); sweep suite 7/7; fmt + clippy `-D warnings` green.

**What comes next:** Push; watch the PR checks run green end-to-end

## B8 (PR #64 — `Rust workspace` CI failure: runner disk exhaustion)

On `5a59587` five of six checks passed — **incl. the full 10M-point sweep in 4m55s** (the relative-path fix held) — but `Rust workspace` died at 1m42s with the runner's own worker process crashing on "No space left on device" (it never reached the gate script; the same commit passes the full gate locally). Cause: the workspace build writes **~35 GB** (`target/` 35G + keeper 1.7G measured locally) against a stock runner's ~14 GB free; earlier greens rode on rust-cache reducing writes plus runner-image variance. Fix: a plain-script "Free runner disk space" step (no third-party action, consistent with the pinning discipline — drops dotnet/Android/ghc/ghcup/swift/CodeQL + docker images, ~30 GB) before the toolchain/cache steps of every full-workspace job (`ci.yml` `rust`, `release.yml` `gates` + `artifacts`), plus job-level `CARGO_INCREMENTAL: 0` (incremental artifacts roughly double target size for zero CI benefit; `build-runtime.sh` already exported it). Workflow-contract test pins the step on all three jobs. YAML parse + 59 release-tool tests green.

**What comes next:** Push; re-watch the PR checks; if the freed ~44 GB still proves tight, split clippy/test into separate jobs or prune target between gate phases

## B8 (PR #64 — second rebase, onto post-#65 main)

`origin/main` moved again to `923df38` (B6, PR #65) while the disk-fix push was in flight, re-conflicting the PR — **a conflicted PR gets no `pull_request` workflow runs at all** (GitHub cannot build the test-merge commit), which is why `8dba6f0`'s checks never started. Rebased all four B8 commits onto post-B6 main; conflicts were the append-only PLAN.md blocks only (both sides kept). **Second ID renumber** (B6 claimed SQ-131…SQ-134 + V-25 on main): the B8 entries move SQ-131…135 → **SQ-135…SQ-139** and V-25 → **V-26** — the full chain is original SQ-127…131/V-21 → post-B7 SQ-131…135/V-25 → final **SQ-135…139/V-26**; earlier session-log rows record the intermediate steps as they happened; code references only SQ-101 throughout. AGENTS.md's two references re-pointed. Doc links + tooling suites re-verified on the rebased tree.

**What comes next:** Force-push (with-lease); watch the full check set — the disk-space fix gets its first real CI exercise on this push

## B1b (A8 runtime wiring — implementation + compliance remediation, left 🔨)

Wired `Epoch: pallet_epoch = 61` with the frozen surface and a Params-backed production Config; replaced the named A8 pending clock/authority/handoff/enqueue/guardian/reporting/registry/window seams; added the complete market read/open/rerun surface and paired dual-mint seeding; made qualification/guard preimages durable; implemented real I-9 enqueue/callback flow, exact treasury payload sizing, bounded epoch timing catch-up, reporting/registry windows, oracle dispute checks, live phase/capability/attestation gates, guardian delay/force-rerun effects, real review submission/deposit refund and durable review deadlines; extended the classifier across all 14 epoch calls and added runtime e2e/negative/dead-man/fan-out/genesis/try-state tests. Review remediation fixed stale-read I-9 rejection, close-spot capture, rerun reset/POL duplication, permanent rerun holds, spec-version ambiguity, merit-bond dispute filtering and benchmark no_std imports. Canonical gates green (V-31; focused runtime 69, epoch 64, market 34, guard 42, guardian 37, registry 31; try-runtime 71; docs links). **Not marked ✅:** compliance review exposed the production blockers and backing gaps now logged SQ-172…SQ-187; all ambiguous adoption inputs fail closed. Required temporary handoff/report: `CODEX_REPORT_A8_WIRING.md`. No architecture docs changed; no commit made.

**What comes next:** Resolve or explicitly re-scope SQ-176/SQ-178/SQ-179 before ✅; then close the remaining fail-closed SQs by their named owners.

## B1b (A8 remediation round 1 — all findings fixed, left 🔨)

Implemented all 15 R1 findings: optional queue-time/later-bound ratification; rerun-preserved ratification/attestation/single continuous guard pin; terminal qualification-request cleanup across tick-T20/direct-T20/void and Qualified/Trading/Extended; explicit proposal-end Baseline reads; forward contest accrual and boundary sealing; unique active MetricSpec qualification; real bounded USDC proposal-bond escrow with insufficient/duplicate/late-withdraw/full-refund/full-static-slash/live-partial/NotDecisionGrade/T18-once/T22-release tests; durable guardian slash/review cleanup/`ReviewFailed` despite recall failure; live bounded/coupled `gate.v_min`; fail-closed dispute merit; read-only recursive live-meter preview and batch-derived meter declarations; guardian-only force-rerun event; classifier scope correction. Added SQ-183…SQ-190, resolved SQ-179 and narrowed SQ-178. Gates green V-32 (runtime 83/85 try-runtime; epoch 65; market 38; guard 42; guardian 37; constitution 38; keeper 34; reference 21; docs links). No architecture docs changed; no commit made.

**What comes next:** B1b remains 🔨 only on SQ-172 canonical resource keys, SQ-176 downstream guardian effects, and SQ-178 track/held-seat-bond/recall substrate; preserve the other fail-closed SQs.

## B1b (A8 remediation round 2 — all findings fixed, left 🔨)

Implemented every R2 item: explicit eligible/full-slash/refund static-check taxonomy with exact T4/preimage/MetricSpec/bond-drift dispositions; canonical global bond-order qualification plus stale-withdraw closure; universal guard terminal cleanup across rerun Reject/T20, never-queued ratification, pre-queue reject/withdraw and VOID; explicit sealed-window immutability across same-end shared Baselines; synchronous PB-ORACLE-VOID cancellation of the bounded queue-capable set (including FailedExecuted), exact `voided` archive summaries, immediate cohort/proposal reap, bounded tick-driven stale-intake T20 cleanup and a 40-VOID boundedness regression. Added SQ-191…SQ-194. Final dual re-review found 0 blocker/0 major. Gates green V-33 (runtime 92/94 try-runtime; epoch 67; market 40; guard 42; keeper 34; reference 21; docs links). No architecture docs changed; no commit made.

**What comes next:** B1b remains 🔨 exactly for SQ-172, SQ-176 and SQ-178; retain SQ-191…SQ-194 pending rulings and reconcile 02 §7.1's settlement-only archive wording when 02 is next jointly amended.

## B5 (PR #66: merge main's B6/B8 + Codex-connector P1 fix)

**Merged origin/main again** (B6 upgrade-path-e2e + B8 release pipeline landed concurrently; PR #66 went CONFLICTING): 22 hunks across 8 files resolved by composition — B6's runtime was built WITHOUT the A8 epoch wiring, so ours supersedes its pending-world adapters (`PendingEpochHandoff`/`ExecutionParams`/`RuntimeBatchDispatcher`/1-arg-fetch `RuntimePreimages` deleted; the live seams + bounded fetch + generated weights kept) while B6's genuinely-new surface was adopted wholesale: the two new guard Config seams (`Capabilities`, `UpgradeSchedule`), the migrations bridge (`MigrationHaltSources`/cursor-retire/`OnSystemEvent`), the **09 §2.2 mature-admission of bare `system.apply_authorized_upgrade`** (B6 implemented the SQ-93 resolution — supersedes our permanent denial and largely settles our SQ-93 extension), the PR #65-P1 tolerant release-channel clears (applied + aborted, ported into our raw-offset writer), and its remediation-aware apply path (stuck-cursor retire before `can_set_code`). Merge-fallout fixes with B6's own e2e suite kept green (70 runtime tests): capability checks un-folded from `rederive_call` (the guard's ordered list owns `CapabilityDenied`; folding surfaced the wrong item) but kept in `safety_filter` (grouped under one check item), B6's dispatch-time error string adopted, guard `EpochHandoff` gained cfg(test)-only no-op fallbacks for B6's synthetic-pid fixtures, three `assert_noop!`s updated for the B5 refund post-info, exec-guard + epoch weights **regenerated** against the B6-changed code. **SQ/V renumbered again** to clear main's concurrent claims: our SQ-131…140 → **SQ-140…149**, V-25…27 → **V-27…29**. **Codex-connector P1 on PR #66 fixed:** `live_execution_freeze()` + the guardian gate trigger scanned ALL retained `GateBreachFlags` epochs, so a historical breach record froze queued execution indefinitely and kept authorizing `suspend_on_gate` after recovery (06 §5 auto-release) — both now read only the CURRENT epoch's record (`current_epoch_gate_breach()`), with a live-vs-historical regression test. One codex usage-limit outage mid-merge; conflicts resolved directly. Gates green on the merged tree.

**What comes next:** Push; reply to the connector comment; watch CI + further review rounds

## — (Dependabot configuration, unplanned)

Added `.github/dependabot.yml` with separate policies for the pinned root Cargo workspace, the keeper Cargo workspace, and GitHub Actions; corrected the supplied indentation into valid YAML and preserved the requested schedules, limits, grouping, allow-lists, and commit prefixes. YAML parse and repository document-link validation passed.

**What comes next:** Open a draft PR; milestone focus remains unchanged.

## — (Dependabot simplification, unplanned)

After PR #69 merged, replaced the detailed Dependabot policies with the user's minimal three-entry configuration: monthly root Cargo updates, weekly keeper Cargo updates, and weekly GitHub Actions updates. Corrected only the supplied YAML indentation; no extra policy keys remain. YAML parse and repository document-link validation passed.

**What comes next:** Open a new draft PR; milestone focus remains unchanged.

## — (Security policy, unplanned)

Added the repository-level `SECURITY.md` with private vulnerability-reporting instructions, report-content guidance, supported-version expectations, response targets, and responsible-disclosure rules. Repository document-link validation passed.

**What comes next:** Open a draft PR; milestone focus remains unchanged.

## S5 (wrapper/filter exhaustiveness → ✅, worktree `milestone-S5`)

Tests-only S5 layer over the real runtime (Codex-orchestrated: author → dual review — spec-reviewer 1 blocker/2 major/4 minor + adversarial Codex 2 blockers/4 majors/1 minor — → remediation → re-review 0/0/0): hand-pinned 242-row metadata inventory with bidirectional tripwires; carrier detection extended to generic `type_params` + opaque-name detection after the adversarial blocker (finds `referenda.submit` via `Bounded<RuntimeCall>` and `pallet_xcm.execute` via `DoubleEncoded`/`VersionedXcm`; both pinned semantic-carrier rows; `approve_as_multi` negative control proven at real dispatch); ACTIVE full origin-aware authority matrix over all 8 class origins with compile-time-exhaustive origin universe and per-wrapper 06 §3.3 treatments (same-origin-only recursion for utility/`with_weight`/sudo — SQ-152 resolved by both reviewers' spec reading, wrong-criterion test deleted); `WrapperShape` constructor closure + generated compositions; kernel-exact budget boundaries; real-dispatch `CallFiltered` negatives; inventory-derived values-set equality; full-inventory I-8 proof. 19 tests: 15 active, 4 ignored regressions pinning the 2 real findings — SQ-150 (contested three-way 06 §2.1/06 §3.2/13-rule-7 `amend_registry` contradiction; I-8 crossing real today) + SQ-151 (`ForeignAssets.create` CV-gated but unreachable, fail-closed). SQ-150…SQ-154 logged. Gates green: fmt · workspace clippy `-D warnings` · workspace tests · runtime 74/4-ignored · wasm/try-runtime legs · reference-model · doc links.

**What comes next:** User rulings on SQ-150/SQ-151 (production fix milestone) + SQ-152 reading confirmation; then A8 runtime wiring / B5 / B8

## S5 (PR #86 — merge onto post-#64/#66 main + Codex-connector remediation, IN PROGRESS)

**Merge committed (`faef3eb`):** PLAN union (Current focus / SQ table / session log), **SQ-135…SQ-139 renumbered → SQ-150…SQ-154** (main's B5/B8 claimed 135–149) in PLAN + both S5 test files. Main's A8 epoch wiring tripped the S5 exhaustiveness test exactly as designed (`Epoch.decide` unpinned — 84 passed/1 failed). **In flight (Codex subagent):** pin all `Epoch.*` inventory rows + behavioral coverage (incl. the new GuardianHold/EmergencyPlaybook leaves), and fix the connector **P2** (SQ-151): add `ForeignAssets.create` to `is_values_enactment_leaf` (adjudicated minimal production fix; pallet `CreateOrigin` stays the second check), activate the ignored regression, drop the values-set exception. Connector **P1** (SQ-150 I-8 crossing) adjudicated as policy-reply: pre-existing, pinned by loud ignored regression + exact-skip-list active test, blocked on the user's 06 §2.1/06 §3.2/13-rule-7 ruling — CI stays green per R-9.

**What comes next:** Finish Codex round, update SQ-151 status + this log, re-run full gates, push, reply to both PR comments

## S5 (PR #86 — merge adaptation + connector P2 fix COMPLETE)

Codex subagent pinned all 14 `Epoch.*` inventory rows (9 Public keeper/callback cranks, `set_next_epoch_length` CV, 3 GuardianHold holds, `void_cohort` EmergencyPlaybook — inventory 242→256) with behavioral pins across all 8 class origins, and shipped the adjudicated SQ-151 fix (`ForeignAssets.create` → `is_values_enactment_leaf`; regression activated with real-dispatch proof both ways; values-set equality exception-free). **The exhaustiveness suite then caught the same defect class in main's fresh A8 wiring — `epoch.set_next_epoch_length` CV-gated but scheduler-unreachable — fixed by the same one-arm admission (orchestrator-applied, in-pallet `ConstitutionalValuesOrigin` verified as the second check).** Connector P1 (SQ-150) answered by policy reply: pre-existing contested I-8 crossing, pinned ignored + exact-skip-list active test, blocked on the user's 06 §2.1/§3.2/13-rule-7 ruling. Gates: fmt · runtime clippy `-D warnings` · runtime 87 passed/3 ignored; full workspace script re-run pre-push.

**What comes next:** Push, reply to both PR comments, watch CI to green

## B7 follow-up (SQ-139 evidence producer → ✅, worktree `B7-evidence-producer`, PR opened this session)

Shipped the `bleavit.env-evidence.v1` producer (inventory in the extended B7 milestone row): `tools/env/run-evidence.py` + validator-pinned `tools/env/suites.json` + config/tier hardening in `validate-environments.py` + release.yml/ci.yml wiring + README truth-updates (post-B6 runnability rows). **Process:** Codex author (per orchestrator design brief; all 4 gates green first pass) → **dual review**: spec-reviewer **0 blocker/5 major/3 minor** (+3 spec questions → SQ-202/SQ-203/SQ-204) + adversarial Codex **5 blockers/7 majors/1 minor** → one consolidated Codex fix round (A1–G3: release-tier evidence completeness — gated skips now block evidence; tools/env cleanliness + manifest/pins digests in evidence; env gates into the tag pipeline's gates job; chopsticks genesis/wasm-override/`:code`-injection rejection + live `:code` binding + port probe + db uniqueness/purge; process-group teardown on every exit path; unconditional prior-evidence invalidation; post-suite binding re-verification; BaseException-safe all-or-nothing emission; tier map pinned in the validator; drill-08 gate restored; npx pre-warm; contract-extras documented) → orchestrator verification pass (2 hand-fixes: bogus third forbidden-key entry; `checks` over-claim in no-wasm report rows). **Deliberate deferrals, logged loudly:** closing try-state leg (SQ-204) and card-depth execution (SQ-203) — both stated in the env READMEs. **Verified:** 45 env + 61 release + 16 deploy + 7 reference-model unittests (websockets 15.0.1 venv — keystone test validates produced evidence through the real consumer), `validate-environments.py`, py_compile, `bash -n`, `node --check` on all 9 helpers, doc links. No Rust surface touched (workspace gates unaffected). SQ-202…SQ-204; Decision-log row for the four producer rulings.

**What comes next:** Ratify SQ-139/SQ-202/SQ-203/SQ-204; A8 wiring un-gates 04/05/09 (and B9-topology/SQ-101 work un-gates 03/07) so a tag run can first produce evidence; try-runtime-cli leg (SQ-204) remains the standing B7/B8 follow-up

## B7 follow-up (PR #68 — rebase onto post-#66 main)

`origin/main` moved to `bbb5017` (B5 weights/PoV, PR #66) before checks could run, leaving PR #68 CONFLICTING (and, per the B8 precedent, a conflicted PR gets no workflow runs — no Codex-connector review ran either). Rebased the squashed producer commit onto post-B5 main; the only conflict hunk was the append-only session-log tail (both rows kept). **Third-party ID renumber:** B5 claimed SQ-140…SQ-149 on main, so the producer's spec questions moved SQ-140…142 → **SQ-202…SQ-204** (PLAN.md + AGENTS.md references; code references only SQ-139/SQ-101). ci.yml auto-merged (B5 relocated the `environments` job; the producer's websockets/py_compile additions applied on top). Re-verified on the rebased tree: 45 env + 61 release unittests, `validate-environments.py`, py_compile, doc links.

**What comes next:** Force-push (with-lease); watch checks + the Codex-connector review now that the PR is mergeable

## B7 follow-up (PR #68 — Codex-connector review: 2×P1, both TRUE, fixed)

The connector bot raised **2×P1** — the mechanical-enforcement form of the two findings the dual review had deferred by documentation (SQ-203/SQ-204): (P1-A) a Chopsticks scenario "pass" (boot/state/blocks/`:code`) must not certify the 15 §4.7 card-depth dry run; (P1-B) 15 §1 try-state never runs, so evidence must be blocked, not caveated. **Fixed by a Codex fix round + orchestrator verification:** all 10 scenario rows now carry `gated_on: ["SQ-203 card-depth execution"]` (`base` stays ungated — it claims only boot-depth); `validate_release_evidence_completeness` additionally requires an executed `try-state` check on every passing row — no driver can set it until the SQ-204 try-runtime-cli leg lands and there are **no bypass flags**, so evidence emission is now mechanically impossible for both kinds (previously only contingently impossible via the 03/07 gates); tests reworked (keystone consumer test → direct `emit_evidence` calls with synthetic try-state rows; new CLI tests: fully-green run refuses with the SQ-204 message; default chopsticks run skips the 10 gated scenarios and refuses evidence); READMEs + `tools/release/README.md` updated to the mechanical wording. Gates green (47 env + 61 release unittests, validator, py_compile — venv websockets 15.0.1).

**What comes next:** Rebase onto post-#69 main (Dependabot row union) → force-push → reply to both bot comments; the SQ-204 try-runtime-cli leg + SQ-203 card execution now gate first evidence alongside 03/07

## SQ-101 (Location-keyed `ForeignAssets` → ✅, worktree `SQ-101`)

Re-keyed the runtime `ForeignAssets` instance from local `u32` to XCM v5 `Location` per 02 §7.4/§8/X-11a. Reused `bleavit_xcm::identity::usdc_location`; pinned its canonical 10-byte SCALE encoding and the exact `ForeignAssets.Asset`/`Metadata` raw keys; updated genesis, fee conversion, rebates and both benchmark helpers; unblocked the three release-manifest rows without weakening their layouts/exact keys/expected values; re-encoded the VOID Chopsticks fixture; re-trued `tools/release/README.md` (SQ-101 no longer a fail-closed blocker; B7 bullet now names the missing run-evidence, not the definitions). No pallet or XCM-executor wiring changed. `staging-xcm 22.0.0` has unconditional serde derives and no `serde` feature, so that requested flag was correctly omitted; Cargo.lock gained the required local dependency edge. **Process:** single Codex author (companion runtime, high effort) → native `spec-reviewer` dual pass (**0/0/1 minor**: the benchmark helper re-derived the canonical Location shape inline; fixed by adding parametric `bleavit_xcm::identity::asset_hub_asset_location(index)` and routing `usdc_location()` + both benchmark helpers + the negative fee test through it; the reviewer independently re-derived both Chopsticks raw keys byte-for-byte). **Verified:** full workspace gates ×2 (fmt · clippy `-D warnings` · workspace tests · release/benchmarks/try-runtime wasm · no_std) — second run post-fix; runtime 60/60 + try-runtime 62/62; bleavit-xcm 33/33; reference-model 21/21 + vector `--check` byte-identical + doc-table; deploy/reference/release (59) tooling suites; env validator; doc links; **real-node e2e**: `generate-chain-specs.sh` output passes both deploy profiles with Location-keyed genesis (serde round-trip through the wasm `GenesisBuilder`), `extract-metadata.py` `:code`↔wasm binding green, `record-chainhead-fixtures.py` records **all three SQ-101 rows** (layout match + exact-key values decimals 6 / min_balance 10⁴), recorded surface 89→99, remaining 57 gaps all attributed B2/A8/A11. Local-env note: the full gates need `LIBCLANG_PATH` pointing at a dir with an unversioned `libclang.so` on this machine (only `libclang.so.1` is installed).

**What comes next:** PR opened this session; B2/A8/A11 own the remaining readiness gaps; B4 owns the executor wiring the re-key unblocks.

## SQ-101 (PR #67 — rebase onto post-B5 main)

`origin/main` moved to `bbb5017` (B5 benchmarks/weights + the A8/A11 runtime wiring), re-conflicting PR #67 (a conflicted PR gets no workflow runs). Rebased the single SQ-101 commit: textual conflicts were the three runtime import blocks (union: main's new `Epoch`/`ExecutionGuard`/`EpochConfig`/`ExecutionGuardConfig`/`RuntimeDispatcher` items kept, `USDC_ASSET_ID`/`USDC_LOCATION` dropped for `usdc_location`/`USDC_LOCATION_ENCODED`), the append-only PLAN.md blocks (both sides kept), and Cargo.lock (main's taken; the `bleavit-xcm` edge regenerated, `--locked` clean). **One semantic conflict git merged silently:** B5's new `runtime-benchmarks` seams (`benchmark_keeper_rebate_hooks!` pot funding + `pallet_registry::BenchmarkHelper::funded_account`) minted with the deleted `USDC_ASSET_ID` at four sites — re-keyed to `usdc_location()`. Re-verified on the rebased tree: full workspace gates (exit 0, incl. benchmarks/try-runtime/no_std wasm legs over B5's merged benchmark code); runtime 71/71 (B5 tests + the SQ-101 key pins); release tools 59; env validator; doc links.

**What comes next:** Force-push with lease; CI + connector review get their first run on the rebased PR

## SQ-101 (PR #67 — Benchmark-smoke CI failure fixed)

The rebased PR's first CI exercise failed one benchmark: `pallet_assets::migration_v2_foreign_asset_set_reserve_weight` (B5's `--all` smoke). Root cause, reproduced locally: upstream's migration-weight benchmark asserts `Asset::iter_keys().next()` equals its own created asset — an empty-map assumption; our development genesis registers the 02 §8 Location-keyed USDC by design, and under Location keys the USDC storage hash (`484550ec…`) sorts before the benchmark asset's (u32 keys had passed by hash luck, not correctness). The weight fn belongs to pallet-assets's **v2 storage migration, which a chain born at the current storage version never executes**, so the smoke now excludes exactly that one id via `--exclude-extrinsics` with an in-workflow justification comment — every executable call/hook weight stays covered (15 §4.5); hash-lottery-shaping the benchmark Location was rejected as fragile. Verified with the exact CI invocation locally: 311 benchmarks execute, exit 0. A separately reported `test_manifest_ids_are_unique_and_sorted`/`tooling-suites` failure matches no job or test in this repo (release-tools suite 59/59 locally) — not reproducible, treated as a mis-report.

**What comes next:** Push; watch the full check set on the re-push

## S2 (PR #92 — merge onto post-B5/B8/SQ-101/S5/S3 main)

`origin/main` advanced 9 commits while PR #92 was open (B5 #66 benchmarks/weights, B8 #64 release pipeline, SQ-101 #67 Location-keyed ForeignAssets, B7-evidence #68, **S5 #86 wrapper/filter negative suites**, **S3 #88 generated limit-coverage**, Dependabot/Security). Three `git merge origin/main` rounds (main moved twice more — S5 then S3 landed mid-merge): the code auto-merged cleanly both times — my decode-depth hardening (`futarchy-primitives::kernel::MAX_PAYLOAD_DECODE_DEPTH`, `execution-guard::decode_batch` → `decode_all_with_depth_limit`, the runtime regression) landed intact because B5's preimage-PoV fix touched the *fetch* path (fetch-by-recorded-len), not `decode_batch`'s body; conflicts were `.github/workflows/ci.yml` (union: kept the new `fuzz` + `supply-chain` jobs), `AGENTS.md` (main's B7-evidence `zombienet/` row + my `fuzz/` layout row), `PLAN.md` (append-only Current-focus/session-log unions), and `runtime/tests.rs` (S5's `pub(crate)` on `seed_parachain_upgrade_boundary` + my `enqueue_treasury_bytes` — both kept) (rerere-assisted). **SQ renumbered twice to clear concurrent claims** (main reached SQ-152→S5-154→S3-160→**S1-171**): my SQ-135/136/137 → SQ-153/154/155 → SQ-155/156/157 → SQ-161/162/163 → SQ-175/176/177 → SQ-215/216/217 → final **SQ-225/226/227** (sixth renumber: B2 #100 claimed 205–218) across code comments (kernel const, `decode_batch`, the runtime test) + PLAN (the three SQ rows, focus/milestone/decision-log/session-log). Re-gated the merged tree: root workspace script green (runtime 74 w/ try-runtime incl. the strengthened decode regression) + fuzz workspace fmt/clippy/10 tests + smoke. **Codex-connector review (on the pre-merge commit): 1×P1 + 1×P2, both addressed.** **P1** (ratify the 256 decode-depth before enforcing) — the constant is kernel-homed (rule 4) and mirrors substrate's conventional extrinsic decode-depth limit, not an invented value; no *spec-valid* payload changes outcome (all nest ≤4 ≪256; anything deeper is already SafetyFilter-rejected), only decode bombs go trap→`BadPreimage`; the "no observable change" wording was over-broad and is corrected in the decision log, and the doc-level ratification is exactly SQ-225. Enforcement stays live (R-7 forbids leaving a latent trap in audit-scope-A). **P2** (the regression pinned the codec API, not the guard wiring) — the runtime test now ALSO drives the over-deep preimage through the guard's real `execute`→`decode_batch` (Treasury needs no ratification/attestation) and asserts `BadPreimage`, so a revert to unbounded `Decode` fails the test; `enqueue_treasury_bytes` helper added.

**What comes next:** Force-push (with-lease); reply to the two bot comments; watch the check set

## B1b (PR #93 origin/main merge + connector P1)

Semantically resolved all 11 merge conflicts: retained main's SQ-101 `Location`-keyed `ForeignAssets` and ported A8 bond custody/POL/genesis/tests; composed main's B5 executable-benchmark harness and generated runtime weights with `pallet_epoch` at frozen index 61 (all 14 epoch benchmarks execute; epoch remains on its existing weights pending B5-style calibration); extended S5's metadata/origin suites to all 14 epoch calls; preserved every A8 R1/R2 lifecycle, custody, sealed-window and forward-contest fix; and unioned PLAN with collision-free SQ-140…SQ-194/V-31…V-33 ownership. Fixed the PR #93 bot P1 by cancelling/refunding empty or wholly class-less payloads before qualification/market creation, while preserving full slash for class-less payloads carrying false non-empty resource declarations; reworked downstream e2e coverage onto directly seeded Qualified/Trading state so real enqueue/callback assertions remain live. Final gates green: full Rust workspace script exit 0 (runtime 119 passed/3 ignored; try-runtime 121 passed/3 ignored; keeper 34; reference model 21), executable `pallet_epoch` benchmark 14/14, doc links green. Git's semantic files are resolved, but this sandbox mounts the linked worktree Git metadata read-only (`index.lock: Read-only file system`), so it could not stage the resolutions or create the requested merge and bot-fix commits; no push occurred.

**What comes next:** Restore write access to `/home/development/bleavit/.git/worktrees/pallet-epoch`, then create the merge commit and the separate exact-subject bot-fix commit described in `CODEX_REPORT_A8_MERGE.md`; keep B1b 🔨 for SQ-172/SQ-176/SQ-178.

## S3 (generated limit-coverage suite — **in progress**, worktree `milestone-S3`)

Codex author pass (gpt-5.6-sol/xhigh) landed the 15 §4.6 / I-22 CI half: `tools/limit-coverage/` (strict extractor over 13 §1/§2/§4 + coverage checker + 12 unit tests), exhaustive 159-key classification manifest `registry.toml` (43 dispatch-limit / 64 param-bounds / 46 value / 1 diagnostic / 5 unwired), committed `genesis-keys.json` fixture cross-checked by a constitution test (87 seeded of 106 doc-§1 keys; 19 `[VERIFY]`-gated rows unseeded), generated per-key amendment-bounds suite (hard min/max, max-Δ, cooldown, kernel-bounded refusal), `// limit-coverage:` markers binding 36 existing + 7 new dispatch-past-limit tests, CI legs in gate script + workflow. Gates green after authoring. **Dual review in flight:** adversarial Codex verdict 2 blockers/7 majors/2 minors (checker accepts no-op/`#[ignore]` tests; param-bounds coverage overclaims where consumer cores hardcode; phase3 caps/TwapCheckpoints wrongly classed as wired; grouped keys collapse coverage; parser drift holes; `owner = "B10"` doesn't exist); spec-reviewer pass still running.

**What comes next:** Synthesize both reviews → remediation round → re-gate → PLAN close-out → commit → PR

## S3 (generated limit-coverage suite → ✅, worktree `milestone-S3`, PR opened this session)

Close-out of the same-day in-progress row above: spec-reviewer verdict landed at **0/1/2** (extraction fidelity, the 35-key kernel-bounded set, and the "unmatched key fails CI" property all mutation-verified green; its major = the `unwired` exemption class needs ratification → SQ-155); Codex remediation applied all 11 dispositions (checker rejects `#[ignore]` + requires exactly one lexically-bound `error`/`behavior` token per dispatch-limit key; grouped registry/crank rows split; parser rejects indented/malformed rows; phase3 caps + `TwapCheckpoints` verified-then-reclassified `unwired`; behavioral bounds rebound to existing latch/eviction/neutral-settlement tests; keeper.budget wording honest; real-runtime admission test for `orc.max_proof_bytes`; 15 `consumer_binding` annotations — broader than the review claimed; unwired+consumer keys printed every run, owners PLAN-validated; gate legs unconditional; migration-bound reasons corrected). Focused spec-reviewer re-pass: **0 blockers/1 major/0 minors** — every disposition verified landed; the residual (three §2 rows still grouped: `MinTrade/MaxTrade`, `prop.max_*`, PB-LEDGER-FREEZE) fixed by hand as per-bound keys (168 total; each constituent bound now deletion-resistant with its own error token). **B10** milestone row added (wiring closure — owner of all 8 unwired + 15 consumer-binding entries); SQ-155…SQ-160 logged (renumbered at merge — see the row below). Full gates re-run green (workspace fmt/clippy/test · runtime + try-runtime suites · release/benchmarks wasm · no_std · keeper · reference-model 21 · vectors · doc table · limit-coverage 168 + 26 unit tests · doc links).

**What comes next:** Merge the S3 PR; A8 wiring (slot 61) / B5 / B8 / B10 next

## S3 (merge onto post-B5/B8 main)

`origin/main` moved twice mid-session (B8 PR #64 `d7a727c`, then B5 PR #66 `bbb5017` — B5's groundwork also landed the **A8/A11 runtime wiring**, epoch live at index 61). Merged with union resolutions: PLAN Current-focus/SQ-table/session-log blocks (both sides kept), exec-guard mock (S3 `heavy` + B5 `set_value_weighted` call variants and helpers both kept), runtime tests imports (B5's `GuardianWeightInfo` removal + S3's `Compact`/`Decode` codec needs). **ID renumber to clear main's concurrent B8/B5 claims (SQ-135…SQ-149): the S3 batch SQ-135…SQ-140 → SQ-155…SQ-160** (doc-only; no code references). Manifest re-trued against the merged runtime: `TickBatch` reason no longer claims epoch is outside `RuntimeCall` (A8 wired); dead-man/`pol.budget_epoch`/treasury-sync/phase3-cap/`TwapCheckpoints` unwired claims re-verified against the merged tree; genesis fixture + amendment suite re-checked against the merged `constitution-core`. Full gate script re-run green on the merged tree (incl. B5's weight-regression legs + the S3 limit-coverage leg).

**What comes next:** Open the S3 PR

## S3 (PR #88 — Codex-connector P1 + CodeQL bot fixes)

**CodeQL (workflow permissions):** `ci.yml` gained a workflow-level `permissions: contents: read` (every job is checkout+test only). **Codex P1 (uncovered limits pass green via `unwired`/`value`):** three-part fix. (1) The two named §4 bounds got REAL coverage — `MIGRATION_CURSOR_MAX_LEN`/`MIGRATION_IDENTIFIER_MAX_LEN` reclassified `dispatch-limit` with new real-runtime `UncheckedExtrinsic` admission tests (at-bound accepted, bound+1 rejected on the Root-gated `force_set_active_cursor`/`clear_historic`; shared byte-surgery helper). (2) `dead-man-switch` reclassified `dispatch-limit`: its enforcement consequence is dispatch-tested (real-SafetyFilter freeze under `DEAD_MAN_ENGAGED` + epoch clock pause/`BadPhase`); only the 4,800-relay-block detector stays B10 (reason says so). (3) The exemption class now **expires mechanically** — the checker fails any `unwired` key whose owner milestone is ✅ in PLAN.md (and any `consumer_binding` once B10 completes), so 'fails CI until the test exists' is deferred, never waived (SQ-155 updated; 2 new checker regressions, 23 total). Manifest: 59 dispatch-limit / 64 param-bounds / 37 value / 1 diagnostic / 7 unwired. Full gates re-run green.

**What comes next:** Reply to both bot comments; watch PR #88 checks

## S3 (second merge — post-#68 main)

PR #88 went CONFLICTING when the B7 evidence producer (PR #68, `fd939fc`) landed; per the B8 precedent a conflicted PR gets no workflow runs, so the pending Rust-workspace check was never going to start. Merged `origin/main`; the only conflict was the append-only session-log tail (both sides kept). **Third renumber of the S3 spec-question batch:** the B7 follow-up claimed SQ-150…SQ-152 on main, so S3's questions move SQ-150…SQ-157 → **SQ-155…SQ-160** (full chain: SQ-135…140 → SQ-150…155 → SQ-155…158), including the registry.toml reasons that cite the unwired-exemption and behavior-class questions. ci.yml/AGENTS.md auto-merged (workflow permissions + limit-coverage job intact). Checker + unit tests + doc links re-verified; no Rust surface touched by the incoming commit (env tooling only), so the local full-gate result from f23454b stands.

**What comes next:** Push; watch PR #88 checks restart now that the PR is mergeable

## S3 (third merge — post-#67/#91 main)

PR #88 CONFLICTING again: main gained the SQ-101 Location-keyed `ForeignAssets` re-key (PR #67) + the rerere docs note (PR #91). Only PLAN.md conflicted (Current-focus + session-log unions; runtime tests auto-merged). **No SQ renumber needed** (main still tops at SQ-152) and **no manifest fact changed**: SQ-101 re-keyed the asset id but wired no transactor — `AssetTransactor = ()` verified still true on the merged tree, so the phase3-cap `unwired` reasons stand. Runtime test suite + checker + doc links re-verified on the merged tree.

**What comes next:** Push; watch PR #88 checks

## S3 (fourth merge — post-#86 main)

PR #88 CONFLICTING again: milestone **S5** (wrapper/filter negative suites, PR #86) landed on main and claimed **SQ-150…SQ-154** — colliding both with the S3 batch AND with main's own B7-evidence rows (main now carries duplicate SQ-150…152 IDs: S5's at the table top vs B7-evidence's below; S5's numbers are cited from 16+ Rust sites incl. `#[ignore]` attributes, so they were left untouched — **the duplicate-ID defect on main is flagged here for the next session to renumber holistically**). S3's batch moved out of the way a fourth time: **SQ-153…SQ-158 → SQ-155…SQ-160** (full chain SQ-135…140 → 150…155 → 153…158 → 155…160; PLAN rows + registry.toml citations). Conflicts were Current-focus + SQ-table unions; runtime tests auto-merged (S5 added tests_s5*.rs beside the S3 markers). Checker + doc links + runtime suite re-verified on the merged tree.

**What comes next:** Push; watch PR #88 checks; next session: renumber one of main's duplicate SQ-150…152 batches

## B4-residual (seam bindings SQ-110/111/112/113/123) — 🔨 in flight, mid-session R-3 checkpoint

Worktree `B4-residual`, branch `feat/b4-residual-seams` off `d7a727c`. **Spec amendments landed first (R-1):** 13 §1 gains the four SQ-112 trader-rate PARAM keys (`xcm.dot_per_sec`/`xcm.dot_per_mb`/`xcm.usdc_per_sec`/`xcm.usdc_per_mb`, fee-sizing sim-gated); 09 §5.2 re-based `phase3.tvl_cap` on **total local USDC issuance** (SQ-111 resolution; mint step = binding point, split-path check = defense-in-depth read, SQ-105/A2 side; SQ-110 trap-plus-self-scoped-recovery reading codified); 09 §6.4 closed the `[VERIFY XCM-health availability]` — v1 X = local accepted/(accepted+failures+probe timeouts), partial by construction, R fail-static fallback load-bearing (SQ-113); 05 §4.3 X-row updated + **canonical v1 MetricId table** added (X=1, R=2, … mirrored as `futarchy_primitives::metric_ids`); 08 §1.4 gains the SQ-123 custody-sync passage (`fund_budget_line` atomically moves MAIN→pot USDC for KEEPER/ORACLE). **Two Codex authors (xhigh, write) running in parallel**: A = constitution keys + Params-backed `GovernedWeightTrader` binding + new `pallet-inflow-caps` (index 63) + `PhaseInflowCaps` adapter; B = welfare `XcmTraffic` counters + oracle `ProbeTimeoutSink` seam + treasury `PotFunding` custody sync + `metric_ids`. Scope decision: full XcmConfig assembly + SQ-101 Location re-key stay the owned follow-up (AssetTransactor remains `()`); everything not re-key-blocked binds live.

**What comes next:** Integration round (health sink→welfare, real `MetricInputs` X/R, `TreasuryPotFunding`, router wrap), dual review, gates, PLAN close-out, PR

## B4-residual Author A (SQ-110/SQ-111/SQ-112 bindings — complete)

Added the four exact 13 §1 XCM rate rows + canonical/distinct metadata coverage; bound the runtime trader to live constitution `Params` with payer-adverse two-dimensional rounding; added no-dispatchable `pallet-inflow-caps` (index 63), its Phase-3 per-account meter/try-state checks, real-USDC-issuance adapter, and runtime cap tests. `AssetTransactor` stays `()` until SQ-101, with the ready adapter documented at the binding point. **Gates green:** fmt + diff check; requested targeted clippy; requested targeted tests (runtime 62 · constitution-core 13 · constitution 38 · inflow-caps 9); inflow-caps no_std + try-runtime; full workspace clippy and tests, including the parallel author's crates. The local LLVM install required a temporary unversioned `libclang.so` symlink outside the worktree for the node legs; no repository workaround was committed. No new spec ambiguity found — code follows the 2026-07-17 amended 09 §5.2/§6.1 and 13 §1 texts.

**What comes next:** Residual-session integration/review; compose `CappedInflows` only after SQ-101 re-keys `ForeignAssets`.

## B4-residual (seam bindings SQ-110/111/112/113/123 → ✅, close-out of the in-flight row above)

**All four residual bindings landed and the milestone merged onto post-A8/A11+B5 main** (`32801ab`) in-session. **Code:** four 13 §1 trader-rate keys seeded + `GovernedWeightTrader<ConstitutionTraderRates, ()>` live in the XcmConfig; new state-only `pallet-inflow-caps` (index 63; per-account meter, issuance admission, sentinel short-circuit, dual try-state legs) + `PhaseInflowCaps` ready for SQ-101; welfare `XcmTraffic` per-epoch double map (21-epoch bounded window, prefix reaping wired via the new epoch↔welfare prune seam to cohort reap + epoch roll) fed by `HealthTrackingRouter` on both router slots + oracle `ProbeTimeoutSink` at the timeout fold, with `MetricInputs` emitting **X only** (R unbound, SQ-195; canonical `MetricId` table 05 §4.3/13 §3.4/`metric_ids`); treasury custody sync (`PotFunding`: `fund_budget_line` → atomic MAIN→pot USDC, `Expendable`, rollback tested). Epoch/day attribution reads the live `EpochOf` clock (A8). **Process:** 2 parallel Codex authors (xhigh) → integration round → dual review — `spec-reviewer` **0/0/6** + adversarial Codex **2 blockers/6 majors** (obsolete-base + fail-open R; day-modulo, unbounded traffic map, cap mutability, try-state gap, sentinel growth, benchmark coverage) → merge+fix round (all fixed or SQ-logged) → focused re-pass found **1 new blocker** (welfare prune-cutoff off-by-one → settlement deadlock at ~epoch 21 → dead-man; fixed to the 05 §3.3 `e−20` reading + sustainability regression test, arithmetic double-checked) + 3 minors (SourceClass filter, 13-citation comments, R-liveness note) — all fixed. **Recovered a Codex-side PLAN.md revert** (the merge+fix job restored PLAN.md from the index, wiping the then-unstaged SQ/Decision-log rows — re-applied; only PLAN.md was affected, verified file-by-file). SQ-195…SQ-201 raised; SQ-110/111/112/113/123 resolved; five Decision-log amendment rows. **Gates green on the final tree** (`rust-workspace-gates.sh` exit 0: fmt · clippy `-D warnings` · workspace tests incl. welfare 41/runtime 81+83 try-runtime · release/benchmarks/try-runtime builds · no_std · I-24 lint · keeper 34 · reference-model 21 + doc-table · weight-regression vs main 305 fns PASS · doc links).

**What comes next:** Open PR; follow-ups: SQ-101 re-key composes `CappedInflows`/trader revenue/probe dispatch; B5-recal measures the new runtime-boundary costs; A8 v1 spec registration excludes R (SQ-195); user ratification for SQ-195…SQ-201

## B4-residual (PR #90 — weight regeneration surfaced a real PoV defect) — 🔨 in flight, R-3 checkpoint

The honest re-benchmark of the bot's P1 measured terminal `settle_cohort` at a **13.67 MB proof — over the 7.86 MB normal-class budget** (worst case: 21-epoch × 256-day XcmTraffic backlog ≈ 5,648 writes) — the two pov_budgets runtime tests correctly fail; the unbounded backlog drain also violates I-20's cursor-bounding. Treasury P2 regeneration is fine (`fund_budget_line` +5 writes, acked, within class). **Bounded-drain redesign dispatched (Codex, xhigh):** `prune_xcm_traffic` drains ≤ 2 retired epoch prefixes per call; the drain moves to a stateless `tick`-path hook (fires every crank until backlog gone — crossing-detection could miss backlog); welfare try-state relaxed to structural bounds (index ≤ 21, epochs ≤ current, bijection, no all-zero); tick + terminal settle_cohort benchmarks measure the capped drain; `pallet_epoch` weights re-regenerated.

**What comes next:** On green: commit, merge `fd939fc` (union-resolve PLAN/AGENTS — only both-changed files, 1 hunk), full gates, push, reply to both bot comments, final close-out row

## B4-residual (PR #90 — bot findings fixed, bounded drain landed; close-out of the two checkpoint rows above)

**Both Codex-connector findings resolved with locally regenerated weights** (same host/recipe as B5's calibration): P2 — `fund_budget_line` weight now carries the ForeignAssets custody transfer (6 reads/6 writes, worst-case ref-time ×2.1, acked); P1 — became a real design fix: the honest terminal-prune benchmark measured a **13.67 MB proof (over the 7.86 MB normal class)**, so the drain was redesigned cursor-bounded per I-20 — `prune_xcm_traffic` drains ≤ 2 retired epoch prefixes per call, invoked statelessly on every `tick` until backlog empties (a 21-epoch backlog drains in 11 ticks, tested); welfare try-state relaxed to structural bounds (index ≤ 21, epochs ≤ current, bijection, no all-zero); tick + terminal `settle_cohort` benchmarks measure the capped drain; `pallet_epoch` weights regenerated — **terminal `settle_cohort` proof now 301,825 B, `tick(10)` 866,345 B, both within class; PoV tests green**. Acks: tick (bounded catch-up), settle_cohort (capacity histories + 2 prefixes), treasury transfer. **Full gate script exit 0.**

**What comes next:** Merge `fd939fc` (PLAN/AGENTS union), push, reply to both bot comments

## B4-residual (PR #90 — merge onto post-B7-evidence/SQ-101 main)

`origin/main` moved three times while the bot-finding fixes ran: `fd939fc` (B7 run-evidence producer), `cd33da3` (rerere guidance — enabled locally per the new R-9 note), and **`322540f` — the SQ-101 Location re-key landed** (`ForeignAssets` now keyed by the frozen XCM `Location`; `USDC_ASSET_ID` deleted). Merged with conflicts in PLAN.md (session-log union) and runtime `tests.rs` (import block: main's Location-keyed list + our `InflowCaps`); semantic pass converted our added call sites (`TreasuryPotFunding`, `UsdcIssuance` adapter, runtime tests) to `usdc_location()` per main's idiom. **Verified on the merged tree:** runtime 82 tests · env/release/deploy/reference-model tooling suites OK · doc links · weight-regression PASS WITH ACKNOWLEDGEMENTS · full `rust-workspace-gates.sh` run recorded in the close-out below. **SQ-101's landing unblocks the B4 composition follow-up** (`CappedInflows` transactor over `PhaseInflowCaps`, trader revenue routing, probe/renewal dispatchers) — still deliberately out of this PR's scope.

**What comes next:** Push; reply to both bot comments; composition follow-up is next session's candidate

## B4-residual (PR #90 — second post-open merge: S5 negative suites)

`origin/main` moved again (`70abcf8`, S5 wrapper/filter negative suites + metadata filter-exhaustiveness over `RuntimeCall`). Merged; only PLAN.md conflicted (session-log union — rerere preimage recorded). The S5 exhaustiveness suites pass unchanged over our tree (state-only `pallet-inflow-caps` has no dispatchables to classify): runtime 99 tests green, fmt/clippy clean, full gate script re-run recorded here on completion.

**What comes next:** Push after gates; watcher continues (bot comments · conflicts · per-push CI)

## B4-residual (PR #90 — third post-open merge: S3 limit-coverage suite)

`origin/main` moved again (`85b8967`, S3: generated dispatch-limit registry — every 13-registry key must be classified in `tools/limit-coverage/registry.toml`, unmatched keys fail CI; new milestone **B10** owns the surfaces S3 proved unwired). Merged (PLAN union only — rerere). **Our four `xcm.*` trader-rate keys classified `param-bounds`** (amendment bounds auto-covered by the generic per-key set_param suite; not dispatch limits — the consumer is the payer-adverse trader, pass path composes post-SQ-101 under B10) + added to the regenerated 91-key `genesis-keys.json`; the two `phase3.*` `unwired` reasons refreshed to name the B4-residual meters and the pending SQ-101-composition/SQ-105 dispatch legs (owner stays B10 — the checker validates owners against PLAN milestone ids). Checker exit 0 · constitution 40 · runtime 104 (S3+S5 suites) green; full gate script re-run recorded on completion.

**What comes next:** Push after gates; watcher continues

## S2 (PR #92 — fourth post-open merge: B4-residual #90)

`origin/main` advanced again (`63c8172`, B4-residual: Params-backed `TraderRates`, inflow-cap meters, XCM health→welfare, treasury custody sync). Code auto-merged (`futarchy-primitives`, `runtime/tests.rs`, `Cargo.toml`) — my decode-depth hardening intact; only the Decision-log region of PLAN.md conflicted (append-only union: my S2 decode-depth entry kept above #90's five B4-residual rulings). **No SQ renumber** this round — #90 stayed within SQ≤160 and does not touch my SQs (then 161/162/163, now 175/176/177 after the S1 merge). Re-gated: fmt · runtime crate test green (my decode regression + the S3/S5 suites coexist).

**What comes next:** Force-push (with-lease); watch checks

## S2 (PR #92 — fifth post-open merge: S1 #89)

`origin/main` advanced again (`ecc8a22`, S1: TLA⁺ models + ≥10⁶-case property suites + ledger differential). Code auto-merged (my decode-depth hardening intact; S1 touches `models/`/`tools/verify/`/ledger-side suites, not my files); conflicts were `README.md` (union: kept main's `zombienet/` + new `models/` rows and my `fuzz/` row) and `PLAN.md` (Current-focus + Decision-log unions; milestone-table dedup — main's S1 ✅ + my S2 ✅). **S1 claimed SQ-161/162/163**, so my three renumber a fourth time → **SQ-175/176/177** (a small deliberate buffer above main's SQ-171 to break the concurrent-milestone collision cycle) across code (kernel const, `decode_batch`, the runtime test) + PLAN. Re-gated: fmt · doc links · runtime crate test green.

**What comes next:** Force-push (with-lease); watch checks

## S2 (PR #92 — post-merge CI repair: stale weight acks + S1 payout-sized-merge oracle drift)

Two CI failures surfaced after the #90/#89 merges. **(1) Weight gate:** the merge-base moved to `ecc8a22`, which already contains the B4-residual epoch/treasury weights, so the three acks (`pallet_epoch` `tick`/`settle_cohort`, `pallet_futarchy_treasury` `fund_budget_line`) went stale — removed from `tools/ci/weight-regression-acks.toml`; gate now `PASS` (305 shared fns, no acks). **(2) Fuzz oracle:** S1's payout-sized merge (market-core sells now merge exactly `net + fee == proceeds`, not the seller's full `amount`, leaving surplus legs as book inventory) broke `lmsr_harness_covers_gate_and_baseline_books` — Decision/Gate books absorb the composition shift inside the summed `scalar + liquid` legs, but the unbranched Baseline model still subtracted `amount`. Deleted the harness's kind-split `inventory_outflow_on_sell` (every sell now drains tracked inventory by gross `proceeds`; derivation re-checked per-leg against `sell_branch`/`sell_gate`/`sell_baseline`) and moved the sell-side rationale onto `inventory_inflow_on_buy`'s doc. **`tools/ci/fuzz-gates.sh` exit 0** (fmt · clippy `-D warnings` · 10 oracle tests · 3 target builds · corpus regression `-runs=0` · smoke) + weight gate PASS.

**What comes next:** Push (with-lease); watch remaining checks

## — (dependency-PR triage, unplanned)

Triaged the 15 open `chore(deps)` Dependabot PRs; grouped them by actual coupling rather than one-per-PR. **#81–85** each bump a `=`-exact polkadot-stable2603 pin (`sp-core`, `pallet-timestamp`, `sp-genesis-builder`, `frame-try-runtime`, `pallet-transaction-payment-rpc-runtime-api`) — the pin regime exists precisely to stop this (root `Cargo.toml` header, a 2026-07-15 Codex adversarial-review finding), so they are un-mergeable by construction → `ignore` block added to `.github/dependabot.yml` (Unplanned changes); closures pending user go-ahead. **#72** (`dtolnay/rust-toolchain` 1.89.0→1.100.0) is genuinely red (3 jobs): **Rust 1.100.0 does not exist** — `static.rust-lang.org` 404s it, current stable is **1.97.1 (2026-07-14)**; verbatim CI: `error: could not download nonexistent rust version 1.100.0-x86_64-unknown-linux-gnu`. A phantom `1.100.0` **branch** on the action repo (its version refs are branches, not tags, so it sorts highest) is why Dependabot proposed it → close; wants an actions-side ignore too. **#71** (`download-artifact` v4→v8) is the only PR marked CLEAN yet the **most dangerous**: all 5 artifact call sites live in `release.yml`, which is tag-triggered and never runs on a PR, so its green tests nothing — and it leaves `upload-artifact@v4` at 3 sites (`release.yml:34/191/202`); the pair must move together or the release pipeline breaks at tag time. **#73/#75/#76** (checkout v4→v7, setup-python v5→v6, setup-node v4→v7) are legitimate — CI already warns `Node.js 20 is deprecated … actions/checkout@v4 … forced to run on Node.js 24`; their UNSTABLE state is only the 1h+ Rust workspace job pending, not failure. **Keeper #74+#80** (subxt/subxt-signer 0.44.3→0.50.2 — same upstream release, must be atomic; individually un-mergeable, hence their UNKNOWN mergeability) and **#77/#78/#79** (prometheus 0.14 / toml 1.x / rand 0.10, all breaking) are **not yet done**. Also found: `ci.yml:136` pins `dtolnay/rust-toolchain@stable` while 7 other sites pin `@1.89.0` — a drift hazard against the deliberate toolchain pin. **Delegation note for the next session:** four `codex:codex-rescue` agents were dispatched and did **zero work** — that agent is a fire-and-forget forwarder (hands the prompt to the companion as a *background* job, returns a task id immediately), so pairing it with `isolation: worktree` let the worktree be auto-cleaned as "unchanged" and pulled the jobs' cwd out from under them; `codex-companion.mjs task` runs **foreground by default** and should be driven directly from the session in a persistent worktree instead. **Nothing pushed, no PR closed, no milestone status touched.**

**What comes next:** Close #81–85 + #72 on user go-ahead; decide #71 (bump `upload-artifact` in lockstep vs close); land #73/#75/#76; then the keeper migrations — #74+#80 as one atomic change, then #77/#78/#79

## — (dependency-PR triage — close-out of the checkpoint row above)

**Dispositions executed. Six closed** with cited rationale: **#81–85** (each targeting an `=`-exact stable2603 pin) and **#72** (Rust 1.100.0 does not exist — `static.rust-lang.org` 404s it, stable is 1.97.1; the phantom `1.100.0` *branch* on the action repo, whose version refs are branches not tags, is why Dependabot proposed it). `.github/dependabot.yml` gains the polkadot-family Cargo ignore **and** a `dtolnay/rust-toolchain` actions ignore (this branch). **#71 — the checkpoint row's "the pair must match" claim was WRONG; corrected here:** `upload-artifact` has **no v8** (latest v7.0.1; download is v8.0.1), so the majors are legitimately offset; download v5's breaking change is **by-ID only** and both sites fetch by `name:`; nothing in v5–v8 requires a given upload major. #71 alone would in fact have worked — the real defect was leaving `upload-artifact@v4` on **deprecated Node 20**. Rebased onto main, bumped upload→v7 in the same PR (inputs `name`/`if-no-files-found`/`retention-days`/`path` all survive; v7's `archive` is opt-in and unset), retitled; **merged by the user as `7c10c50`**. **#79** — rand 0.10 moved `random_range` off `Rng` onto `RngExt`; one import swap; keeper fmt/clippy/**34 tests** green, root lockfile untouched; **merged as `b3ca843`**. **#77 (prometheus 0.14) / #78 (toml 1.x) verified green as-is** — Rust workspace passed (1h5m / 27m) and `rust-workspace-gates.sh:65-69` does run the keeper leg, so the passes are real and no code change is needed; awaiting merge. **#73/#75/#76 audited safe** — the only inputs in use are `fetch-depth`/`node-version`/`python-version` (all still supported); checkout v7's fork-PR block needs `pull_request_target`/`workflow_run` (repo has neither); setup-node v5's auto-cache needs a `packageManager` in a `package.json` (repo has none); all 15 jobs are `ubuntu-latest` so the ≥2.327.1 runner floor is met; awaiting merge. **#74+#80 (subxt 0.44.3→0.50.2) in flight** — measured **14 structural errors** (`OnlineClient::metadata()`/`::constants()` gone; `subxt::Error::{Io,Rpc}` removed — those back the transport-vs-protocol reconnect classification, which must be preserved, not merely made to compile); handed to Codex in a dedicated `deps/subxt-0.50` worktree, driven directly via foreground `codex-companion.mjs task` (see the checkpoint row's delegation note). **#71 was merged before its dry run finished**, so a fresh `release.yml` `workflow_dispatch` (`dry_run=true`) was dispatched against **merged main** to validate the artifact path — `release.yml` is tag-triggered and never runs on a PR, so no PR check ever exercised it.

**What comes next:** Land #77/#78/#73/#75/#76; confirm the main dry run green; finish #74+#80; open the PR for this branch's ignore rules; loose end: `ci.yml:136` pins `dtolnay/rust-toolchain@stable` against 7 sibling `@1.89.0` pins

## S2 (PR #92 — sixth post-open merge: dependency/CI chores)

`origin/main` advanced by 8 chore commits (#94 Dependabot ignore rules; #71 release artifact-action migration; #73/#75/#76 checkout v7 / setup-python v6 / setup-node v7; keeper bumps #77 prometheus 0.14 / #78 toml 1.x / #79 rand 0.10). Code auto-merged (keeper + workflows — none touch S2 files); only PLAN.md conflicted (session-log union: my three S2 rows + main's two dep-triage rows). **Aligned my `fuzz` job's `actions/checkout` v4→v7** — the job predates main's fleet-wide bump, which couldn't see it; its nightly `dtolnay/rust-toolchain@master` + explicit `toolchain:` pin is deliberate and untouched. Re-verified on the merged tree: doc links · weight gate PASS (305 fns, no acks) · fuzz oracle 10/10.

**What comes next:** Push; check-monitor continues on the new head

## B9 maintenance (atomic Subxt 0.50.2 update; Dependabot #74 + #80)

Updated the separate keeper workspace's `subxt` and `subxt-signer` dependencies together from 0.44.3 to 0.50.2 and re-resolved only `keeper/Cargo.lock`. Audited the upstream 0.50 migration notes (the release line jumps directly from 0.44 to 0.50; 0.50.1/0.50.2 are follow-ups) before adapting the finalized-block stream, block-scoped metadata/storage/constants, raw no-default dynamic value/key decoding, async transaction client, progress type, and nonce lookup. Replaced the removed flat `Error::{Io,Rpc}` match with an API-wrapper-aware transport classifier: backend RPC client/network failures, resource limits, and dropped subscriptions reconnect; user/serialization/deserialization/SCALE/metadata/protocol errors do not, including Subxt's stringified metadata error seam. Retry, nonce reservation, cooldown, and finality behavior remain unchanged; the eight `bleavit_keeper_*` metric names/labels (12 §6.3) are byte-identical. Subxt 0.50.2 declares Rust 1.85, below the keeper's pinned Rust 1.89 MSRV. Explicit isolation check: root `Cargo.toml` and root `Cargo.lock` have zero diff. Read-only compliance review found no remaining blocker after hardening the metadata error-envelope classifier and preserving Subxt 0.44's absent-storage semantics. **Authored by Codex on a base of `ecc8a22`, then rebased here onto post-merge main** — Codex's run predated #71/#73/#75/#76/#77/#78/#79/#94, so its "prometheus 0.13.4 unchanged" claim described the stale base; the rebase carries main's prometheus 0.14 / toml 1.x / rand 0.10 alongside subxt 0.50.2 (all four coexist in `keeper/Cargo.toml`), and `main.rs` merged with #79's `use rand::RngExt` intact — a file-copy would have silently reverted that fix. Gates re-run on the rebased tree rather than inherited from Codex's report. **Residual fragility (accepted, noted):** `metadata_rpc_transport` string-matches Subxt's stringified metadata-RPC error because 0.50 exposes no structured variant there; if upstream reformats that message the matcher fails *closed* (no failover) — a keeper liveness degradation, not a correctness or solvency risk, and covered by an adversarial test pinning a spoofed transport-like user error to non-transport.

**What comes next:** Push the branch; supersede #74 + #80 with one atomic PR (neither can land alone)

## S2 (PR #92 — seventh post-open merge: keeper subxt 0.50.2)

`origin/main` advanced once more (`e6d8058`, the atomic #74+#80 subxt migration). Keeper-only code (6 files + new `transport.rs`) auto-merged — the keeper workspace is disjoint from every S2 file; only PLAN.md conflicted (session-log union, again). Re-verified: doc links · weight gate PASS; keeper legs were gated on main's own CI and are untouched here.

**What comes next:** Push; check-monitor continues

## B1b (second origin/main merge — S1/S3/B4-residual semantic composition)

Resolved all six conflicts without choosing either side wholesale: retained every A8 R1/R2/PR-#93 lifecycle and Location-keyed bond-custody path; composed B4-residual Params-backed trader rates, inflow caps, XCM-health→welfare pruning, oracle timeout sink and MAIN→pot treasury custody sync; kept S5's 14-call Epoch inventory. Added a cross-system regression proving MAIN→KEEPER transfers cannot sweep or double-count the distinct epoch sovereign escrow, change USDC issuance, or charge `CumulativeDeposits`, while both epoch and treasury try-state remain green. S3 now honestly materializes `gate.v_min.{param,trs,code,meta}` from the singular doc row and classifies all four as genesis-seeded `param-bounds`; inventory is **175** keys (59 dispatch-limit / 72 param-bounds / 36 value / 1 diagnostic / 7 unwired), genesis fixture 95. Merge adaptations fixed S1's synthetic constitution property fixture and the shell's stale 91-record pin. Collision-free ownership: S5 SQ-150…154; S3 SQ-155…160; S1 SQ-161…171/V-30; A8 SQ-172…194/V-31…33; B4 residual SQ-195…201; B7 evidence SQ-202…204. Final gates: exact full workspace command exit 0 (runtime 136/3 ignored; try-runtime 138/3 ignored; epoch 74; keeper 34; reference model 26; S3 checker 175); docs links, diff check, fmt, marker scan, README pins and duplicate-ID audit green. No architecture docs changed. The resolution was staged and committed in-session (`3a646ef`); a third merge then took main's dependency/keeper chores (`e6d8058`) with only a session-log union conflict.

**What comes next:** B1b remains 🔨 for SQ-172/SQ-176/SQ-178; B10 owns the seven visible S3 unwired entries and 15 kernel-constant consumers.

## S2 (PR #92 — eighth post-open merge: B1b #93 epoch runtime wiring)

`origin/main` gained `73d9d2c` (B1b: epoch seams, market read surface, bond custody, I-9 e2e). Conflicts: PLAN.md ×4 (focus/milestones/2 session-log regions — unions; the **S3 milestone row took main's side**, B1b grew the limit registry 168→175 keys) + AGENTS.md (main's rewritten B7/env row kept, my `fuzz/` row re-appended). **Fifth SQ renumber:** B1b claimed SQ-175…SQ-204, colliding with my three again → **SQ-225/216/217** (chain row extended) across the SQ table (dedup — both sides' rows had briefly coexisted at 175/176/177), focus/milestone/decision-log/close-out refs, and the three code comments (`tests.rs`, `futarchy-primitives`, `execution-guard`); also fixed AGENTS.md's fuzz row still citing the round-2 number **SQ-153** → SQ-215. **One real auto-merge break:** my `enqueue_treasury_bytes` (decode-bomb helper) still used the pre-B1b test seams (`set_test_execution_payload` / `test_execution_enqueue_origin` / `RuntimeGuardParams`) that #93 deleted — ported to the sibling idiom (`seed_queued_epoch_proposal` + `RuntimeOrigin::signed(epoch_account())` + `ExecutionParams`). Verified: fmt · clippy `-D warnings` (runtime+guard+primitives, all targets) · runtime **137** · guard 47 · primitives 14 · fuzz oracle 10 · weight gate PASS · doc links · limit-coverage 175 keys + unit suite OK.

**What comes next:** Push; check-monitor continues

## B2 completion (in progress, worktree `milestone-B2-completion`)

**Session in flight — interim row (Stop-hook compliance), full close-out row follows.** Committed so far on this branch: (1) the queued 02 amendment batch as **integration-contract v4** (`6862ef8`): NavView +{spendable_nav, meter_utilization_bps, class_floors} (SQ-55), CohortSummary.proposals bound 5→12 (SQ-43), 02 §6 ledger-event completion (SQ-37), 02 §7.5 attestor storage freeze (SQ-26/SQ-2c), §9 phase-fraction/intake row split (SQ-23/SQ-24/SQ-125) + frozen metadata-constant name table (SQ-138), v4 history + 00 D-2, INTEGRATION_CONTRACT_VERSION 3→4, workspace green. (2) 15 frozen metadata constants wired (`a7f8249`; ledger MinTransfer, market MinTrade/MaxTradeRatio/MaxLiveMarkets/GatePMaxCeiling/GateEpsFloor, guard ExecutionTimelockFloor/ExecutionGraceFloor, epoch PhaseOffsets/MaxBooksPerProposal/MinEpochLength/DecisionWindowFloor/DecisionExtension/DecisionDeltaFloors/DecisionSigmaFloors) + manifest layout freezes; five proposed constants (intake bounds, dec.trailing/coverage/v_min floors) dropped by R-1 reconciliation — 13 §1 normative, they are META-amendable registry bounds binding via params() min/max, not kernel constants. Currently uncommitted: Codex author mid-run on the 7 non-epoch FutarchyApi view-assembly methods (views.rs + market-core quote helper + constitution last_change_block + treasury helpers). Orchestrated with Codex subagents (xhigh).

**What comes next:** Finish views.rs + tests; merge `feat/a8-runtime-wiring` (parallel session) when it lands and implement the 4 epoch-backed methods + the `impl FutarchyApi` block; dual review; gates; final PLAN close-out; PR

## B2 completion (in progress — addendum)

Interim addendum: the Codex author on the 7 non-epoch view methods stalled silently after ~2 h (job "running", log dead, no process); cancelled and resumed the same thread (partial edits intact: views.rs scaffolding, +516-line runtime test block, constitution-core `last_change_block` mid-fix). Resumed run is verifying; new watcher has 15-min stall detection. Uncommitted tree changes at this stop = that author's in-progress work.

**What comes next:** Land views.rs + tests green; then A8 merge + epoch methods; dual review; PR

## B1b (A8 runtime wiring — implementation + compliance remediation, left 🔨)

Wired `Epoch: pallet_epoch = 61` with the frozen surface and a Params-backed production Config; replaced the named A8 pending clock/authority/handoff/enqueue/guardian/reporting/registry/window seams; added the complete market read/open/rerun surface and paired dual-mint seeding; made qualification/guard preimages durable; implemented real I-9 enqueue/callback flow, exact treasury payload sizing, bounded epoch timing catch-up, reporting/registry windows, oracle dispute checks, live phase/capability/attestation gates, guardian delay/force-rerun effects, real review submission/deposit refund and durable review deadlines; extended the classifier across all 14 epoch calls and added runtime e2e/negative/dead-man/fan-out/genesis/try-state tests. Review remediation fixed stale-read I-9 rejection, close-spot capture, rerun reset/POL duplication, permanent rerun holds, spec-version ambiguity, merit-bond dispute filtering and benchmark no_std imports. Canonical gates green (V-27; focused runtime 69, epoch 64, market 34, guard 42, guardian 37, registry 31; try-runtime 71; docs links). **Not marked ✅:** compliance review exposed the production blockers and backing gaps now logged SQ-140…SQ-150; all ambiguous adoption inputs fail closed. Required temporary handoff/report: `CODEX_REPORT_A8_WIRING.md`. No architecture docs changed; no commit made.

**What comes next:** Resolve or explicitly re-scope SQ-144/SQ-146/SQ-147 before ✅; then close the remaining fail-closed SQs by their named owners.

## B1b (A8 remediation round 1 — all findings fixed, left 🔨)

Implemented all 15 R1 findings: optional queue-time/later-bound ratification; rerun-preserved ratification/attestation/single continuous guard pin; terminal qualification-request cleanup across tick-T20/direct-T20/void and Qualified/Trading/Extended; explicit proposal-end Baseline reads; forward contest accrual and boundary sealing; unique active MetricSpec qualification; real bounded USDC proposal-bond escrow with insufficient/duplicate/late-withdraw/full-refund/full-static-slash/live-partial/NotDecisionGrade/T18-once/T22-release tests; durable guardian slash/review cleanup/`ReviewFailed` despite recall failure; live bounded/coupled `gate.v_min`; fail-closed dispute merit; read-only recursive live-meter preview and batch-derived meter declarations; guardian-only force-rerun event; classifier scope correction. Added SQ-151…SQ-158, resolved SQ-147 and narrowed SQ-146. Gates green V-28 (runtime 83/85 try-runtime; epoch 65; market 38; guard 42; guardian 37; constitution 38; keeper 34; reference 21; docs links). No architecture docs changed; no commit made.

**What comes next:** B1b remains 🔨 only on SQ-140 canonical resource keys, SQ-144 downstream guardian effects, and SQ-146 track/held-seat-bond/recall substrate; preserve the other fail-closed SQs.

## B1b (A8 remediation round 2 — all findings fixed, left 🔨)

Implemented every R2 item: explicit eligible/full-slash/refund static-check taxonomy with exact T4/preimage/MetricSpec/bond-drift dispositions; canonical global bond-order qualification plus stale-withdraw closure; universal guard terminal cleanup across rerun Reject/T20, never-queued ratification, pre-queue reject/withdraw and VOID; explicit sealed-window immutability across same-end shared Baselines; synchronous PB-ORACLE-VOID cancellation of the bounded queue-capable set (including FailedExecuted), exact `voided` archive summaries, immediate cohort/proposal reap, bounded tick-driven stale-intake T20 cleanup and a 40-VOID boundedness regression. Added SQ-159…SQ-162. Final dual re-review found 0 blocker/0 major. Gates green V-29 (runtime 92/94 try-runtime; epoch 67; market 40; guard 42; keeper 34; reference 21; docs links). No architecture docs changed; no commit made.

**What comes next:** B1b remains 🔨 exactly for SQ-140, SQ-144 and SQ-146; retain SQ-159…SQ-162 pending rulings and reconcile 02 §7.1's settlement-only archive wording when 02 is next jointly amended.

## B2 completion (in progress — addendum 2)

Interim addendum (Stop-hook compliance; full close-out row still to come). Since addendum 1: merged `origin/feat/a8-runtime-wiring` (B1b epoch wiring, PR #93) — 6 conflicts resolved (primitives kernel constants unioned; epoch-core `push_summary` takes B1b's pid-list structure over the v4 bound 12; constitution-core `set_param` carries both the v4 `last_change_block` signature and B1b's `gate_v_min` coupling; PLAN rows both kept), full workspace green after. **All 11 `FutarchyApi` methods now implemented** in `impl_runtime_apis!` over a new `runtime/bleavit-runtime/src/views.rs`; `decision_stats` reads through one shared `assemble_decision_input_snapshot` used by both the decide crank and the view (no-drift), returning `None` on incomplete backing rather than provisional data. **Orchestrator review caught a real defect:** `proposal_summaries` mapped an absent `Ratifications` record to `NotRequired` for every class, so an unratified CODE upgrade rendered as "no ratification needed" while `execution_queue` (guard-core's projection) reported the same proposal unratified — contradictory contract surfaces on the 06 §2.2 R-1 execute precondition. Both now share the guard's canonical mapping; a regression test pins their agreement, and the Codex test that had encoded the buggy expectation is fixed to discriminate by class. Manifest: removed 23 mis-labelled per-entry `blocked_by` rows on now-wired epoch surfaces (a recorded surface's label is inert — the B1b compliance gaps correctly fail closed through the new manifest-level `release_blockers` row instead); freezing those 23 layouts from real runtime metadata is in flight, as is the spec-compliance audit.

**What comes next:** Finish the epoch layout freeze; land the spec-reviewer + adversarial Codex findings; full gates (workspace ×3, no_std, try-runtime, tooling suites, release dry-run); PLAN close-out (B2 status, Decision log for the v4 bump + the five-constant reconciliation, SQ updates); PR

## B2 completion (✅ — worktree `milestone-B2-completion`)

**Session close-out; supersedes the three interim addenda above.** B2's open follow-up is closed: **all 11 `FutarchyApi` methods are implemented** in `impl_runtime_apis!` over a new `runtime/bleavit-runtime/src/views.rs`, and the queued **02 amendment batch landed as contract v4** (SQ-2 residuals SQ-23/SQ-24/SQ-26, SQ-37, SQ-43, SQ-55, SQ-125, SQ-138, + `ParamRecord.last_change_block`; three Decision-log rows). 15 metadata constants wired; **5 draft constant names rejected** because 13 rule 7's kernel-bounded set is exhaustive and omits them — they bind via `params()` (a Codex author surfaced the contradiction by refusing to wire it). Merged `origin/feat/a8-runtime-wiring` (B1b, PR #93) mid-session for the epoch-backed methods; 6 conflicts resolved. **Two independent reviews, both fixed in-session:** `spec-reviewer` (0 blocker/6 major/7 minor) and an adversarial Codex pass (3 blocker/5 major). The reviews caught what the gates could not — `proposal_summaries` rendering an unratified CODE upgrade as `NotRequired` (contradicting `execution_queue` on the same 06 §2.2 R-1 precondition); `welfare_current` resolving `Snapshot(CurrentEpoch)` which the production path can never write, so it always returned zeros with the breach flags false (its tests masked it by writing a current-epoch snapshot); `quote` pricing trades `buy`/`sell` rejects after window close; `within_domain` conflating the per-trade bound with 02 §4's domain predicate; `max_delta` reporting the larger side of an asymmetric Factor rule (R-7); the v4 batch freezing 11 surfaces it never added to the release manifest; and `welfare_current` re-deriving the active spec instead of using the chain's fail-closed selector. **Not patched, deliberately:** SQ-205 — the oracle's authoritative reserve-health flag never reaches `treasury::set_reserve_impaired` (whose own comment claims B1a wired it), so 08 §1.2's fail-static NAV is unenforced; overriding it in `nav()` would make the API disagree with what the chain enforces, so it fails a release closed via the manifest `release_blockers` row and is attributed to B1a. **Verification discipline:** re-rendering all frozen manifest layouts against real runtime metadata caught a drift **no local test could** (the v4 `ParamRecord` change silently restaled a live frozen row — the manifest is only re-rendered at release time); re-frozen from metadata + a mutation-verified `ParamRecord` drift-lock. SQ-205…SQ-218 logged. Gates: workspace fmt/clippy/test (60 suites) · 66 release-tool tests · 149 layouts clean (only the 3 deliberate SQ-101 rows mismatch) · doc links. Orchestrated with **Codex subagents (xhigh)**: 5 authors + 1 adversarial reviewer + `spec-reviewer`; one author stalled silently and was cancelled/resumed with its context intact.

**What comes next:** Open PR. Then: SQ-205 (B1a reserve-health seam) is the highest-value follow-up — it is release-blocking and solvency-relevant; then the SQ-206…SQ-218 contract decisions (several want one 02 bump: `ParamView.max_delta` shape, `VaultState` Baseline variant, `RatificationStatus::Absent`, `quote` failure encoding, `ParamView.value` units)

## B2 (PR #100 — merge conflicts with the moved base)

`feat/a8-runtime-wiring` had merged main twice while PR #100 was open, so the PR went conflicting (9 files). Resolved: **SQ renumber** — the base renumbered B1b's SQ-140…SQ-162 → SQ-172…SQ-194 (+32, main having gained S1/S3/S5/B4-residual SQs), and the base's own table already reached SQ-204, so B2's 14 rows move **SQ-163…SQ-176 → SQ-205…SQ-218**; every reference re-pointed (manifest `release_blockers`, its test, `tools/release/README.md`, AGENTS.md, PLAN prose), and B2's references to B1b's compliance range became SQ-172…SQ-182. **SQ-101 landed on main** (#67, USDC re-keyed to the frozen 02 §8 XCM Location), so `views.rs`/`tests.rs` re-keyed off the deleted `USDC_ASSET_ID` onto `usdc_location()`, and the three formerly-blocked manifest rows are now live — **the manifest has no `blocked_by` entries left and all 149 frozen layouts re-render clean against the merged runtime (0 mismatches, 0 required surfaces absent)**. Semantic resolutions: `measured_depth` keeps B2's `Option<Balance>` (a `0` would render a fabricated measurement as observed data) **and** B5's `runtime-benchmarks` shortcut; `set_param`/`checked_update` keep the v4 `last_change_block` argument, and S1's new `constitution-core` property suite was updated to thread it (now also asserting the field is stamped on an accepted update). **Two pre-existing base failures fixed, not caused by this merge:** `test_workflows.py` still pinned `actions/setup-node@v4` after Dependabot #76 deliberately bumped the workflow to v7 — the base branch is red on those two tests today. Gates green on the merged tree: workspace fmt/clippy/test (65 suites) · 68 release-tool tests · doc links · 149/149 layouts.

**What comes next:** Push; watch PR #100 CI + the Codex-connector review; fix its findings

## B2 (PR #100 — Codex-connector review: 1×P1, TRUE, fixed)

The connector's P1 was real and economically material, and it is **B2's own regression**: unifying the grade adapter and the `decision_stats` view onto one contest-floor helper (`effective_decision_contest_floor`, commit `b1a1f4a`) also changed the **grade** semantics — the pre-B2 adapter used `map_or(base, …)`, so an unavailable `in_cap_prize` fell back to the base `dec.v_min` floor, while B2 propagated `None` and voided the grade. SQ-173 leaves the prize unbacked for **every non-TREASURY class**, so every PARAM/CODE/META proposal would have graded non-decision-grade → one extension → `Reject(NotDecisionGrade)` → **10% `IntakeSlashed`**: proposers losing bond for an input the chain, not they, are missing. `in_cap_prize`'s own comment states the intended design ("Returning None blocks Adopt at **sizing step 9**") — and `decide` does exactly that (`in_cap_prize.ok_or(BadDecisionInput)`), an error that leaves the proposal in place and retryable once SQ-173 lands; the grade check simply runs first. Fixed: the helper returns `Balance`, keeping the base floor when the prize is unavailable and `max(base, 2P)` (saturating — it can only raise the floor) when it is; the view still reports exactly what the grade enforces. Regression test added and **mutation-verified** (voiding the floor fails it). Gates green: workspace fmt/clippy/test (65 suites).

**What comes next:** Reply to the reviewer; watch for further connector findings and CI

## B2 (PR #100 — base retarget after the #93 squash)

PR #93 (B1b) squash-merged into main (`73d9d2c`), so GitHub retargeted #100 from `feat/a8-runtime-wiring` to `main` and the PR went CONFLICTING — the squash gives B1b's content a new SHA with no shared history, so git saw it "twice". Verified `git diff origin/main origin/feat/a8-runtime-wiring` is **empty** (the squash is byte-identical to the branch tip already merged here), then merged main with `-s ours` — provably lossless in exactly this case (main brings zero content this branch lacks); the resulting tree hash is unchanged. The PR diff vs main is now the pure B2 delta (50 files).

**What comes next:** Push; confirm MERGEABLE; keep watching connector + CI

## S2 (PR #92 — ninth post-open merge: B2 #100 FutarchyApi + contract v4)

`origin/main` gained `86da68c` (B2: all 11 `FutarchyApi` methods over `views.rs`, 02 contract v4, manifest re-freeze). Conflicts: PLAN.md ×2 — focus (union S2+B2; **kept my branch's B1b paragraph over main's**: #100 forked pre-renumber and its merge regressed main's focus copy to the stale SQ-140s/V-29 numbering, while my copy's SQ-172…194/V-33 matches main's own current SQ table — merging #92 heals main) and session log (union). Code (`tests.rs`, primitives, guard) auto-merged clean this time — verified by the suites, no port needed. **Sixth SQ renumber:** B2 claimed SQ-205…SQ-218 through my 215/216/217 → **SQ-225/226/227** (chain row extended; table dedup again; the eighth-merge row's fifth-renumber narrative deliberately left at its historical numbers) across table/focus/milestone/decision-log/close-out refs + the three code comments + AGENTS's fuzz row. Verified: fmt · clippy `-D warnings` (3 crates) · runtime **154** · guard 48 · primitives 17 · fuzz oracle 10 · weight PASS · doc links · limit-coverage 175 keys.

**What comes next:** Push; check-monitor continues

## — (red-gate fix on `main`, unplanned)

**`main` was red and had been since #76 merged.** `tools/release/tests/test_workflows.py` asserted the literal `actions/setup-node@v4` in two contract tests; Dependabot's `chore(deps): bump actions/setup-node from 4 to 7` (#76, `31072b8`) moved the workflows to `@v7` without touching the assertions, so `test_tag_gates_run_all_tooling_suites` and `test_release_runs_environment_evidence_before_strict_assembly` have failed on every run since — the release workflow's own contract suite, and the tag path gates on it. Fixed the brittleness rather than the number: the action **major is precisely what Dependabot's job is to move**, while what these tests actually mean to pin is that the job sets up Node at the toolchain version the contract names (`node-version: '22'`, still asserted exactly). Both now match `actions/setup-node@v\d+` via an `assertSetsUpNode` helper carrying the rationale, so the next routine action bump cannot redden the gate. Found while triaging the 25 Dependabot security alerts (adjacent, same source). `tools/release/tests` 61 green.

**What comes next:** Merge ahead of the GHSA-only supply-chain leg, which touches the same file

## — (Dependabot security-alert triage; supply-chain gate hardening, unplanned)

Triaged all **25 open Dependabot security alerts** (`wasmtime` ×16, `rustls-webpki` ×3, `hickory-proto` ×2, `yamux`, `lru`, `tracing-subscriber`, `ring`). **Result: 0 of 25 are fixable** — every one is pinned by the `=`-exact polkadot-stable2603 train or by upstream libp2p, proven empirically (each `cargo update --precise` attempt run and the verbatim blocker recorded): `sc-tracing 45.0.0` requires `tracing-subscriber "=0.3.19"` exactly; `sc-executor-wasmtime 0.44.0` requires `wasmtime "^35.0.0"` and the 35.x line has **only** 35.0.0 (no backport); `libp2p-tls 0.5.0` → `rustls-webpki "^0.101.4"` and `rcgen "^0.11.3"` → `ring "^0.16"`; `libp2p-{identify,swarm}` → `lru "^0.12.3"`; `libp2p-mdns` → `hickory-proto "^0.24.1"`; `libp2p-yamux` hard-depends on **both** `yamux012 "^0.12.1"` and `yamux013 "^0.13.3"` (0.47.0 unchanged). Alert #24 (`GHSA-3v94-mw7p-v465`) has **no patched version in existence**. **`.cargo/audit.toml` verified exactly precise** — a 1:1 bijection with the 23 firing vulnerability ids, zero stale, zero missing; the gate is **green** and correctly so (`lru` RUSTSEC-2026-0002 is `informational = "unsound"` → an allowed warning, which is why it needs no exception). **The one real defect found: a structural blind spot** — `yamux` GHSA-vxx9-2994-q338 / CVE-2026-32314 (HIGH, remote unauthenticated panic) has **no RustSec advisory**, so the 15 §4.5 cargo-audit gate cannot see it at all. Closed it with a new **GHSA-only leg** (`tools/ci/check-ghsa-only.py` + annotated `tools/ci/ghsa-waivers.toml` + digest-pinned `osv-scanner v2.4.0` in `tools/env/pins.env`), scoped to exactly the complement of cargo-audit's reach so the two lists never duplicate and the keeper stays waiver-free (its 2 OSV findings are both RustSec-carried informational). Waivers expire mechanically (SQ-155 discipline). **Reachability (all 25):** the **runtime wasm contains none of them** — every alert is collator-binary-only, 0 touch consensus/state/funds; `rustls-webpki` ×3 + `ring` are lockfile ghosts (`libp2p-tls` never compiled); `yamux`/`lru` dormant (litep2p is the default backend); the 16 `wasmtime` alerts are genuinely reachable (it executes the runtime wasm) but bounded — the collator only ever executes governance-approved wasm, and the 2 critical sandbox escapes (CVE-2026-34971, CVE-2026-34987) are aarch64-only, one of them Winch-backend-only (Substrate uses Cranelift). **Verified:** 21 `tools/ci/tests` (green/unwaived/stale/schema/duplicate legs; waivers bound to package+version; fail-closed on a failed scan; compat-parser-vs-`tomllib` agreement on 3.11; the alias-coverage regressions), `tools/env` validator + suite, `tools/release/tests` 61; `tools/release/tests` 61 green after stubbing the new leg hermetically. SQ-219 logged. Orchestrated as a 4-leg workflow + adversarial verification, which **refuted two of my own working hypotheses** (that the gate was red, and that the wasmtime advisories were availability-only).

**What comes next:** Ratify SQ-219; **B11** (PLAN row) carries the only real remediation — the stable2603 train bump closes all 16 `wasmtime` alerts, the other 9 survive any bump (upstream libp2p 0.54.1)

## B11 (scheduled; Dependabot security-alert triage close-out)

Gave the standing supply-chain obligation an owner. `.cargo/audit.toml`'s 23 waivers each carry *"remove at the next stable2603 maintenance bump"* (SQ-135) and nothing scheduled that bump; **B11** is now that row, with the evidence attached rather than re-derived later: newest front `polkadot-omni-node-lib 0.18.0` pulls **wasmtime ^36.0.7** (vs the pinned 35.0.0, a line with **no** backport), which closes **all 16 `wasmtime` alerts** — the only reachable class and the one under the collator's wasm trust boundary — and **none of the other 9**, because `sc-network 0.58.0` still declares `libp2p ^0.54.1` non-optionally and `sc-tracing 47.0.0` still pins `tracing-subscriber "=0.3.19"`. So the train bump is worth doing *and* is not a complete answer; the residual 9 split per-family: 6 libp2p-forced (`libp2p-yamux 0.47.0` still pulls yamux 0.12.1), `hickory-proto` litep2p-forced (the **default** backend pulls `hickory-resolver 0.25.2`, so litep2p-only clears nothing), `tracing-subscriber` sc-tracing-forced (`=0.3.19` exact in 47.0.0 too); B11 does **not** close SQ-135 — the mechanism stays in use (7 audit.toml entries + the yamux GHSA waiver survive). Sized milestone deliberately: ~60 `=` pins move as a unit and the FRAME API delta lands across audit-scope-A code (R-7). SQ-135 updated with the re-triage result and the B11 owner.

**What comes next:** User to schedule B11 against the other open Track-B work; ratify SQ-135 + SQ-219

## B11 (PR #99 — Codex-connector review: 2×P2, both TRUE, fixed)

Both findings target the B11 row's exit criteria and both verified against the lockfile/registry before fixing. **P2 #1 (B11 claimed to clear SQ-135):** wrong — after the bump the waiver mechanism is still in use (7 `.cargo/audit.toml` entries — `hickory-proto` ×2, `rustls-webpki` ×3, `ring`, `tracing-subscriber` — plus the yamux `ghsa-waivers.toml` entry survive; `lru` stays deliberately unwaived as informational) and ratifying the mechanism is the user's call; the row now states B11 executes the re-triage obligation and **leaves SQ-135 open**. **P2 #2 (one blanket exit condition for the 9 residuals):** wrong for two families — `tracing-subscriber` is **sc-tracing-forced** (`"=0.3.19"` exact, 47.0.0 identical; no libp2p/litep2p change touches it) and the reachable `hickory-proto 0.25.2` is **litep2p-forced** (litep2p 0.13.3 → `hickory-resolver 0.25.2` is the *default* backend, so "goes litep2p-only" clears nothing; #24 additionally has no patched version in existence); the row now carries the three per-family exit conditions (6 libp2p-forced / hickory litep2p-forced / tracing-subscriber sc-tracing-forced). Same corrections applied to this branch's own session row and the PR body. Doc links green.

**What comes next:** Merge after #97; both bot comments replied

## — (first `release.yml` execution — dry-run findings, unplanned)

Dispatched the first-ever execution of `release.yml` (`workflow_dispatch`, `dry_run=true`, sha `1d6f1bc`, run `29577250619`) to validate the #71 artifact-action migration, which `release.yml`'s tag-only trigger keeps invisible to PR CI. **Validated:** `upload-artifact@v7` (supply-chain job) and `download-artifact@v8` (artifacts job) both succeeded — the v7→v8 round-trip is proven on merged code — and the reproducible runtime build, chain-spec generate/validate, release-node build, and pinned env-binary fetch all passed. **Failed — a latent B7 env-leg defect, unrelated to the nine dep merges:** the "Generate local relay and parachain specs" step dies building the **Paseo chain-spec generator** from `target/env/paseo-chain-spec-generator-src`: `substrate-wasm-builder 28.0.0` (the pinned Paseo tree's own dependency, not this workspace's `=32.0.0`) panics `Failed to find entry for package {people,coretime,asset-hub,bridge-hub,collectives}-paseo-runtime (…/Cargo.toml)` in every runtime build script (wasm_project.rs:585). Suspects: the fetch script's tree layout vs wasm-builder's workspace-lockfile lookup, or a stale `Swatinem/rust-cache` restore under `target/env`. Until fixed, **every dry run and every real tag release fails at this step** — before evidence production. (Housekeeping: a parallel repair of #97's conflicts made here was superseded by the owning session's semantically identical resolution — same `assertSetsUpNode` outcome, same SQ-219 renumber; the accidentally recreated branch was deleted.)

**What comes next:** Paseo-CSG defect needs a B7-env owner before any tag release; main-CI validation of the merged dep train still pending (runner queue)

## — (PLAN.md table-formatting gate, user-requested, unplanned)

Fixed every GFM-broken table in PLAN.md (three blank lines orphaning B10/B11, V-25/V-30 and two Decision-log rows from their headers; SQ-54/SQ-50 missing their `Raised` cell; unescaped `|` in SQ-7/SQ-212 and two Session-log rows) and made the format mechanically un-breakable per the standing user instruction: `tools/ci/check-plan-tables.py` (strict-GFM structural checker) + `tools/ci/tests/test_check_plan_tables.py` (11 tests), a third Stop hook `guard-plan-tables.sh` in `.claude/settings.json`, and a `docs`-job CI step. Verified: checker green on PLAN.md, 32/32 `tools/ci/tests` green, hook pipe-tested on clean + broken fixtures, settings.json/ci.yml validated. Living docs re-trued (AGENTS.md Docs gate row, CLAUDE.md hook list). Details in Unplanned changes.

**What comes next:** Continue with the standing next milestones (B10/B1b residuals, S4, Track F)

## B11 (worktree `milestone-B11`, branch `chore/b11-stable2606-train-bump`)

**The SDK release line moved `polkadot-stable2603-1` → `polkadot-stable2606` as one atomic unit (D-19; V-34).** 65 `=` pin sites re-sourced from the `polkadot-sdk 2606.0.0` umbrella map (63 root + guard `sp-version` + runtime `sp-externalities`; only `sp-tracing 19.0.0` unchanged), plus `frame-omni-bencher 0.22.0` (ci.yml), `staging-chain-spec-builder 19.0.0` (deploy), `POLKADOT_SDK_TAG=polkadot-stable2606` (assets + `.sha256` sidecars verified live); vendored `core2` + `[patch.crates-io]` removed (absent from the 2606 closure — `[[patch.unused]]` proof). **Exit criteria met:** a no-ignore `cargo-audit 0.22.2` run on the new lockfile fires exactly the 7 predicted survivor IDs (8 findings — RUSTSEC-2026-0119 hits both hickory copies) and **zero wasmtime**; `.cargo/audit.toml` re-triaged from scratch (16 `wasmtime` entries deleted; survivors re-annotated per-family), each survivor re-proven pin-forced by a fresh `cargo update --precise` attempt — verbatim blockers: `tracing-subscriber`→0.3.20 refused by `sc-tracing v47.0.0` (`=0.3.19`, via `polkadot-omni-node-lib 0.18.0`); `rustls-webpki`→0.103.13 refused by `libp2p-tls v0.5.0` (`^0.101.4`, via `libp2p-quic 0.11.1` ← `libp2p 0.54.1`); `ring`→0.17.14 refused by `rcgen v0.11.3` (`^0.16`); `hickory-proto@0.25.2`→0.26.1 refused by `hickory-resolver v0.25.2` (`^0.25`, via `litep2p 0.14.3` ← `sc-network 0.58.0`); `hickory-proto@0.24.4`→0.26.1 refused by `libp2p-mdns v0.46.0` (`^0.24.1`); `yamux`→0.13.10 refused by `libp2p-yamux v0.46.0` (`^0.12.1`). `lru 0.12.5` stays unwaived (unsound-informational → allowed warning); the yamux GHSA waiver's reachability facts re-verified against `sc-network 0.58.0`/`sc-cli 0.61.0` sources. **API delta (Codex subagent, all source-grounded): 4 files.** `SchedulingSignatureVerifier = ()` (0.26.0-documented "V3 scheduling disabled" no-op; V2 candidate behavior preserved with `RelayParentOffset = 0`), `RelayParentOffsetApi::max_claim_queue_offset` → `ParachainSystem` (doc-designated wiring), `sp_core::hashing` → `sp_io::hashing` in tests with the `FutarchyApi` ID now byte-pinned (independently verified `= blake2_64("FutarchyApi")` — a stronger freeze), one latent bench-only `ParamRecord.last_change_block: 0` init; **zero new SDK call variants** (total-match classifier — `__Ignore` terminals, no wildcards — plus the S5 metadata tripwires). Behavioral replications re-verified against pinned 2606 sources, no silent drift: the V-25 five-condition upgrade preflight is condition-identical in `cumulus-pallet-parachain-system 0.29.0`, `can_set_code` unchanged, relay-Abort semantics preserved (SQ-131 intact), `pallet-scheduler 50.0.0` still filtered-dispatch (SQ-32 branch (i)), frame-system `config_preludes` 46→48 identical, XCM v5 still the latest wire version (`staging-xcm 24.0.0`). Deprecated Cumulus `StorageWeightReclaim` retained deliberately — the successor swap changes the TxExtension stack (SQ-228 logged, both upstream successor candidates named). Docs: 00 gained **D-19**; 01 §1/§5.1/§9 re-pointed; five open `[VERIFY]` tags re-targeted to stable2606 **unresolved**; 08 §2.1 `=49.0.0`; 09 §6.1/§6.4 + 05 §4.3 re-verification notes; README/AGENTS (D-1…D-19, vendor rows dropped)/fuzz-README/dependabot/manifest comments; PLAN V-34 + D-19 decision row + SQ-135 update + SQ-228. **Gates green:** `rust-workspace-gates.sh` end-to-end (70 suites, **1122 passed/0 failed**, incl. release/benchmarks/try-runtime builds + try-runtime suite, no_std, I-24, keeper, reference-model + vector freshness + doc-table, limit-coverage 175); `supply-chain-gates.sh` (root 7-ignore bijection · keeper exception-free · GHSA leg: 1 waived finding, 0 stale); `fuzz-gates.sh` (3 targets build, corpus regression, 3×30s smoke, 0 crashes); all Python tooling suites + env validator + doc links; **Dual review:** Codex author round (0 unresolved) + spec-reviewer **0 blocker/0 major/4 minor** — all four fixed in-session (this row is minor #1's evidence trail; V-34 pin-count precision; AGENTS D-range; the B11 row headline reworded from "maintenance bump" to "release-line move"). **One gate leg still red (B11 stays 🔨):** replaying the CI `benchmark-smoke` job locally on the new `frame-omni-bencher 0.22.0` pin fails **49/312 benchmarks** to execute (30 `pallet_referenda` + 7 `pallet_scheduler` + 6 `pallet_oracle` + 3 `pallet_guardian` + 1 each `pallet_epoch`/`pallet_execution_guard`/`pallet_futarchy_treasury`; the initial "upstream bench-fixture changes" hypothesis was DISPROVEN in the repair round — the 46/49 and 47/50 benchmarking sources are byte-identical; the real cause was B5-era benchmark shims lost in later runtime-integration merges plus stale cross-pallet fixtures, exposed only by actually executing the 2606 wasm). Isolation-verified runtime-wasm-side, not bencher-version-side (old 0.15.0 bencher reproduces the guardian failures against the 2606 wasm and adds a TrackedStorageKey FFI trap → the 0.22.0 pin is mandatory). Codex Round B is repairing benchmark-only.

**What comes next:** **Finish B11:** land Round B green (verify its `pallets/guardian/src/lib.rs` touch is benchmark-gated, not an R-7 production change) → re-run touched pallet gates + full `benchmark-smoke` → squash into the single atomic 01 §9 commit → open PR. Then ratify SQ-135/SQ-219; assign SQ-228 a home

## B11 Round B (benchmark execution)

Closed all **49/312** stable2606 executable-benchmark failures with benchmark-only changes and **zero new exclusions**. Source audit corrected the initial diagnosis: cached `pallet-referenda` 46/49 and `pallet-scheduler` 47/50 benchmark sources are byte-identical; the 2606 execution smoke exposed B5 shims removed by later runtime integration plus stale cross-pallet fixtures. Restored benchmark-Wasm-only Root→shared-track and scheduler origins, a fixture-only referenda timeout, and renewal/guardian dispatcher seams; completed oracle cohort/spec/exposure, guardian queue/review/funding, epoch maximum snapshot+gate, and guard failed-dispatch fixtures. Production origins, track table, dispatch behavior, storage, weights, events, and errors are unchanged. Exact CI smoke: **311 executed / 0 failed** (312 registered minus the standing `pallet_assets::migration_v2_foreign_asset_set_reserve_weight` exclusion). Verified: all seven affected pallets individually; `cargo test --workspace --locked`; clippy `-D warnings`; fmt check; and `tools/ci/rust-workspace-gates.sh` end-to-end.

**What comes next:** Squash Round A+B into the single atomic B11 commit, open/update the PR, and keep SQ-135/SQ-219/SQ-228 explicit.

## B11 close-out (delta audit + independent verification)

The Round-B benchmark repairs passed a **native spec-reviewer delta audit: 0 blocker/1 major/2 minor** — cfg-gating verified airtight (every bench arm has a byte-identical `cfg(not)` production counterpart; release wasm unaffected — `tools/release/build-runtime.sh` builds the non-benchmark recipe), guardian `BenchmarkHelper` extension properly gated, zero coverage loss, no fixture leaking into spec/metadata surfaces that ship. All three findings fixed in-session: **(major)** `RuntimeGuardianTriggers::current`'s bench arm no longer skips production's four storage reads (`PhaseFlags`/`CurrentEpoch`/`GateBreachFlags`/`MigrationHaltSources` now execute before the cfg-gated all-true overwrite — DB-op parity so the next weight regeneration cannot under-account `guardian.approve_action`); **(minor)** FIXME-owner marks on the bench no-op stubs (`BenchmarkRenewalDispatch` → SQ-205/B10; guardian effects Ok(()) → SQ-144-effects closure); **(minor)** oracle bond fixture 400,000 → 500,000 USDC (strictly above the 10,000 `orc.bond_floor` knee, matching its comment). Audit spec-questions logged: **SQ-229** (`UndecidingTimeout` has no 13 home) and **SQ-230** (pre-existing SQ-144 label collision). **Independent final verification on the finished bytes** (not the author's runs): `rust-workspace-gates.sh` exit 0 — 70 suites, **1122 passed/0 failed**; the exact CI benchmark-smoke command on the final artifact — **311 executed/0 failed** (codex's pre-fix artifact also independently re-validated at 311/0); fmt/PLAN-tables/doc-links green. B11 ✅.

**What comes next:** Squash Round A+B+delta-fixes into the single atomic 01 §9 commit, push, open the PR; user ratifications: SQ-135/SQ-219, homes for SQ-228/SQ-229/SQ-230

## B11 (PR #103 — Codex-connector review: 1×P2, TRUE, fixed)

The bot's one finding was valid: `tools/deploy/generate-chain-specs.sh` guarded the pinned `staging-chain-spec-builder` install by mere existence (`[[ ! -x "$builder" ]]`), so a stale v17 binary — a developer worktree or a restored CI `rust-cache` — would silently generate the "v19" chain specs. Fixed with the repo's own established pattern (the cargo-audit guard in `supply-chain-gates.sh`): version-substring check on `--version` output, reinstall with `--force` on mismatch; both branches behavior-tested against mock v17/v19 binaries; `bash -n` + the 16-test deploy suite green. Bot comment replied.

**What comes next:** Merge on green CI; the standing user ratifications (SQ-135/SQ-219, homes for SQ-228…SQ-230)

## G0 (in flight)

Session in progress on branch `feat/g0-phase0-exit-gate` (worktree `milestone-G0`, based on post-#103 main). Scope decided: with S4 (economic sim) still ⬜, G0 delivers the machine-checked 09 §7.1 Phase-0 exit gate — `tools/phase-gates/check-phase0-exit.py` producing `bleavit.phase0-evidence.v1`; criterion A (reference model ≡ pallets on shared vectors) executed against the real repo, criteria B/C (sim false-pass < 1 %, δ/POL calibration) consumed fail-closed from the S4-owned `bleavit.sim-calibration.v1` artifact (absent ⇒ pending-s4, never pass). Criterion-A legs independently verified green on this tree: `generate-vectors.py --check`, `check-doc-table.py`, 26 reference-model unit tests, `conditional-ledger-core` differential 3/3, `futarchy-fixed` suite (full sweep correctly ignored without a corpus dir). Codex author currently implementing the tool + tests from the orchestrator brief; only the untracked brief file exists in the tree at this log point.

**What comes next:** Land the tool + tests + CI tooling-suite wiring; spec-reviewer pass; park G0 🔨 pending S4; open the PR

## S4 (worktree `milestone-S4`, branch off `ecc8a22` — **in progress**)

Scope fixed to the 15 §4.9 Phase-0 tier + 13 sim-gated publication (V-12). Spec read (15 §4.9 · 08 §5 · 05 §5.4–5.6 · 04 · 13 · 14 TH-1/2/4/6/7). Stage-1 Codex author (gpt-5.6-sol/xhigh, 3h11m incl. one discarded too-slow calibration attempt) delivered `simulation/` (proposals/agents/market/engine/calibration/evidence, ~2,300 lines, reference model reused as the normative-math source), `tools/simulation/run-calibration.py` (`--full` 816 s / fast `--check`), 6 test files (19 tests, independently re-run green), committed `phase0-calibration.json` (12,000 proposals, seeded, byte-reproducible subsample). **The run honestly FAILS the < 1 % false-pass gate in all four classes** (1.65/2.26/5.24/6.51 %; controls at literal floors 5.8–7.1 %; 10 artifact violations; thin-market-capture depth inflation named as the driver) — recorded as *candidate* findings pending adversarial scrutiny (false-reject 79–81 % and one below-`ManipFloor̂` flip look like modeling artifacts). Candidate publication values (unapproved): sec.prize.param/code/meta 50k/300k/600k, sec.flow_cap 22. Orchestrator PLAN.md rows from mid-session were reverted by the Codex run's out-of-scope cleanup and re-applied post-hoc (this row).

**What comes next:** Dual review (spec-reviewer + adversarial Codex on the economics: agent mix, L̂ accounting, ManipFloor̂ anomaly, causal attribution); remediation; then decide stage-2 publication vs parked-with-SQ; gates; PR

## B1b (completion — SQ-172/176/178 implemented; milestone ✅)

Spec amendments committed first (`14d2eee`: 05 §1.4 canonical resource keys; 06 uphold_veto/recall/kernel-playbook substrate; 15 I-11) and flagged for ratification (Decision log ×4). Three Codex authors (xhigh; one isolated worktree; capacity errors auto-resumed): SQ-172 — footprint derivation, set-equality screening + T4 taxonomy, real locks, zero-collision test (`e01bec7`); SQ-178/189 — six-track table via `track_origins` @64, scoped origin adapters, SeatBond holds, both-deposit fronting via the guardian sovereign (fixes the latent always-times-out-and-slashes review), recall + vacancies, uphold_veto T24, `epoch.veto_upheld` demoted (`9e2ddce`); SQ-176 — five real power effects, kernel playbook routines + `EmergencyPlaybook` producer, market/ledger freeze endpoints, `set_playbook_registered` (`d33bab2`). Adversarial Codex review: 7 P1/3 P2 — 8 fixed with mutation-checked regressions (`d05f24a`; incl. constitution→entrenched escalation, PB-RESERVE market-buy bypass, screening decode depth, dup-lock wedge, uphold-vs-open-review rollback, concurrent-review slash rollback, over-bound slash evasion, intake-pause overwrite), 2 residuals → SQ-233/SQ-234. Native spec pass ran in-loop (spec-reviewer subagent died twice on exhausted usage credits). Merged post-B11 stable2606 main (`4e8d28e`, 3 semantic conflicts composed; benchmark Root fixture re-pointed at track 2). Full gates green (V-35). README/AGENTS/release-manifest rows re-trued.

**What comes next:** Open the PR; user ratification of the four Decision-log amendments; SQ-233 per-source owners; SQ-173…SQ-182 residuals per their owners.

## O4 (runbooks-as-code → 🔨, worktree `milestone-O4`)

Orchestration in flight, based on main @ 82473af: three parallel Codex authors (gpt-5.6-sol/high) writing `deploy/runbooks/` (13 runbooks RB-KEEPER…RB-RELEASE per 12 §6.3's two alert tables + §6.4 incident playbooks folded into RB-RELEASE, frozen frontmatter schema, fixed §6.1 owner/funding assignment) + `tools/deploy/check-runbooks.py` (stdlib doc-12 table-binding checker: bidirectional id↔alert-row match, byte-exact triggers, page-immediately markers, §6.1 role/line membership, body-section + link checks) + unittest suite + a `docs`-job CI step. Author C (RB-ORACLE/LEDGER/XCM/UPGRADE/RELEASE) done; A (infra + BOOTNODE/STORAGE) and B (KEEPER/INTAKE/MARKET/POL/TREASURY/GUARDIAN) still running.

**What comes next:** Harvest A/B → spec-reviewer + adversarial Codex review → fix round → gates (runbook checker, deploy tests, doc links, PLAN tables) → close-out + commit

## O4 (runbooks-as-code → ✅, worktree `milestone-O4`)

Close-out of the in-flight row above: all 3 Codex authors landed (13 runbooks + README + `tools/deploy/check-runbooks.py` + 53-test suite + `docs`-job CI step). Dual review — spec-reviewer **0 blocker/0 major/5 minor** (alert-table + §6.4 fidelity clean; every named metric/event/storage surface verified real or honestly gap-marked) + adversarial Codex xhigh **2/5/5** (RB-XCM presented the spec-mandated TREASURY-class `claim_assets` trap recovery as available while the shipped runtime fails it closed — and Signed reclaim is filtered too, SQ-235; RB-ORACLE used 07 §5.4's `adjudicate(round_id, verdict)` shorthand vs the shipped 5-arg signature, SQ-236; `Treasury.State` vs the real `FutarchyTreasury` pallet path; checker escapes: id-suffix parse, duplicate row ownership, ≥2-vs-exactly-2 tables, weak README-index/anchor/scalar checks). One Codex fix round closed **all 17 consolidated findings** (internal re-review 0/0/0), orchestrator re-verified independently. SQ-235…SQ-239 logged (also: SQ-237 RB-UPGRADE paging contradiction 09 §3.2 vs 12 §6.3; SQ-238 13-owned literals inside §6.3 trigger cells; SQ-239 per-alert ownership derivation). Gates green on the final tree: `check-runbooks.py` OK 13/20 · deploy suite 53 (1 pre-existing skip) · doc links · PLAN tables; Rust untouched. AGENTS.md repo-layout/gate rows refreshed; temp brief deleted.

**What comes next:** Suggest PR; user rulings on SQ-235 (trap-recovery wiring owner — release-relevant) and SQ-236…SQ-239; O1/O2/O3/O5 remain the Track-O backlog
