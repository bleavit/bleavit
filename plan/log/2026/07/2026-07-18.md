# Session log — 2026-07-18

## G0 (close-out — gate shipped, milestone parked 🔨 pending S4)

The Phase-0 exit gate landed: `tools/phase-gates/` (checker + README + 66-test suite, wired into the CI tooling-suites job; the checker itself deliberately not a per-commit gate — red until S4 by design). **Dual review:** Codex adversarial (4 findings — zero-match cargo filters counted as pass, evidence unbound to the tested bytes, whole-row phase classification, negative unsigned calibrations) + native `spec-reviewer` (0 blocker/2 major/4 minor — incl. criterion A overstating corpus coverage: 6 shared-vector families have no Rust consumer anywhere). All 10 findings fixed in one consolidated Codex round and independently re-verified: executed-test-count floors recorded per leg, pre+post clean-tree refusal (demonstrated live on this dirty tree), Default-cell-only sim-gated/phase classification with header validation (13's `phase3.dep_cap` tag made self-contained under R-1 — Decision log), ≥ 0 domain checks, corpus-family attestation (7 attested / 6 unattested ⇒ criterion A `pass-partial`, a non-pass), provided-but-missing sim evidence ⇒ `fail`, `--allow-dirty` only with `--reduced` and structurally unable to yield `phase0_exit: true`. Criterion-A legs verified green on this tree (vector freshness · doc-table · 26 ref-model tests · ledger-core differential 3/3 · fixed vectors 3/3 · both pallet differentials). SQ-267/268/269 + two Decision-log rows logged; README/AGENTS repo maps + gates table updated. Gates: 5 tooling suites, doc links, PLAN tables, limit-coverage 175 keys — all green.

**What comes next:** S4 (sim evidence producer; needs SQ-268/269 rulings first), SQ-267 differential-suite ownership; then G0 re-run flips ✅

## G0 (PR #105 — Codex-connector review: 1×P2, TRUE, fixed)

The bot's finding was valid fail-closed-too-far: `criterion_sim_false_pass` required `type(value) is float`, so a class with zero observed false passes encoded as JSON integer `0` — a naturally valid S4 artifact — would be rejected and could block Phase-0 exit. Fixed to treat JSON numbers uniformly (`type(value) not in (int, float)`, bools still rejected), matching the calibration path's existing semantics; detail wording now "finite number in [0,1]". Tests: integer-zero passes, in-range integer 1 still fails the strict < 0.01 threshold, bool/str/null rejected (suite 66 → 68, green). Bot comment replied.

**What comes next:** Merge on green CI; S4 + SQ-267/268/269 as logged

## S4 (dual review + remediation + merge to `82473af` — simulation done, publication parked)

spec-reviewer 0/2/5: implementation faithful, params/reuse/rounding verified, violations meaningful-but-conditional. Adversarial Codex 6/4 (in-memory re-runs): headline rates invalid as evidence (55.7 % of planted effects below δ · noise share label-only · gate vetoes unreachable with every CODE/META false pass gate-bearing · no executed spend ledger · ManipFloor̂ anomaly a wrong-REJECT category error) while the thin-capture seam SURVIVES. Remediation F1–F10 in one Codex turn (died post-work as a zombie job — cancelled + independently validated per the memory pattern): executed fee-inclusive trade ledger, real gate books with planted truths, upgrade-payload prize scope (reference-model edit, vectors byte-stable), pre-registered strata + decidable-harm gate, envelope rescoped to wrong-PASS, bracketed flip thresholds, Merkle-bound evidence. Remediated result: CODE/META 0.000 % confirmed · PARAM 3.46 %/TREASURY 1.52 % red · sub-3P TH-4 flips at 0.76–0.90×3P ⇒ SQ-231/SQ-232 logged, V-12 updated, publication parked, runtime keeps `None`. Independently verified: simulation 28 tests · reference-model 27 + vectors `--check` + doc-table · `--check` exit-1-with-structure-green · scope clean. Merged `origin/main` `82473af` (19 commits: B11 stable2606/D-19, B2 contract v4, B1b, S2 ✅, PLAN table gate) — PLAN union resolved.

**What comes next:** Merged-tree gates (python suites · limit-coverage · plan-tables · doc links · env validator); AGENTS/README/ci.yml simulation rows; commit; PR onto main

## S4 (PR #110 — Codex-connector P1 fix, **in flight**)

Bot finding accepted as real: the committed evidence artifact bound the generating interpreter tuple (Python 3.10.12) into `config_digest`/`source_model_digest`, while the new CI simulation step runs Python 3.12 — `--check` would fail `recorded config differs from executable defaults` + `config digest mismatch` in CI. Fix applied: interpreter tuple removed from both digest inputs (`config.py`), recorded instead as a top-level `provenance.python_version` block (`calibration.py`, schema bumped `bleavit.phase0-calibration.v2` → `.v3`), checker validates provenance shape but never compares it to the running interpreter (`evidence.py` — cross-version reproducibility stays enforced empirically by the byte-exact pinned-subsample replay + Merkle root), binding test rewritten (`test_runner.py`). Artifact regeneration (`--full`, 10,000 proposals) running in background — **not yet committed or verified**.

**What comes next:** On regen: `--check` under 3.10 AND 3.11 (cross-version proof) · full suite · commit · push · reply to the bot comment

## S4 (PR #110 — Codex-connector P1 fix landed)

Regeneration + verification complete: `--full` re-run (2,822 s) produced **byte-identical economics** (metrics, brackets, published block, violations, outcome-digest Merkle root all equal to the prior artifact — the interpreter coupling was pure metadata); `--check` verified under Python 3.10.12 AND 3.11.0 with zero digest/config/provenance errors and the 24-row pinned subsample re-simulating byte-exactly on both (exit 1 remains solely the recorded economic violations, by design pending SQ-231); simulation suite 28 green under 3.10, runner module (incl. the previously cross-version-failing `--check` subprocess test) green under 3.11. Committed + pushed; bot comment answered.

**What comes next:** Watch PR #110 CI (3.12 leg now decoupled); SQ-231/SQ-232 remain the milestone blockers

## G0 (merge with post-S4 main + SQ renumber — milestone stays 🔨)

Merged `origin/main` `01b3500` (S4's simulation PR #110) into `feat/g0-phase0-exit-gate`. **SQ collision resolved:** S4's session and this branch both allocated SQ-231/232 from the `82473af` base — S4's numbers stand (231 attack-flow seam, 232 Baseline floor); this branch's three renumbered off S4's claim (an intermediate SQ-233/234/235 mapping, itself superseded at the B1b merge — see the row below; final IDs **SQ-267 (unattested corpus families), SQ-268 (criterion-C key set), SQ-269 (per-class false-pass reading)**) with every branch-side reference updated (Current focus, G0 row, Decision log, session log; the PR #105 body edit noted there). **Honest G0 status after S4's landing (R-10): still parked 🔨, and now provably so** — S4's own evidence fails criterion B (PARAM 3.46 %/TREASURY 1.52 % ≥ 1 %, pending the SQ-231 mechanism ruling), criterion C's publication is parked candidates-only, no `bleavit.sim-calibration.v1` producer artifact exists (S4's `simulation/results/phase0-calibration.json` is its own Merkle-bound schema; the producer-side export conforms when S4 re-calibrates post-SQ-231), and criterion A remains `pass-partial` on the SQ-267 corpus debt. Post-merge verification: phase-gates 68 tests · all six tooling suites (incl. S4's new `simulation/tests`) · doc links · PLAN tables · limit-coverage — green.

**What comes next:** SQ-231 ruling → S4 re-calibration + `bleavit.sim-calibration.v1` export (SQ-268/269) → SQ-267 suites → full gate run flips G0 ✅

## B10 (runtime wiring closure → ✅, worktree `milestone-B10`, PR opened this session)

Five Codex work packages (gpt-5.6-sol/xhigh) landed the S3 exemption closure on current main: WP-A all 15 `consumer_binding` keys → live Params with amend-then-observe coverage at mock + real-runtime level; WP-E `TwapCheckpoints` renamed/widened to the 04 §7 two-limb shape (MarketBook 189→205 B, PoV-asserted; 13 §5 items 1/7 re-trued); WP-C production `AssetTransactor` = `CappedInflows` over Location-keyed ForeignAssets + `BleavitBarrier`/`BleavitReserves`/AssetTrap with fund-safe trap/claim recovery + the SQ-105 split gate (`InflowCapGate`, appended `InflowCapExceeded`); WP-D the 05 §4.8 detector (relay-gap ≥ 4,800 + snapshot >4 d overdue, latched, one proposal-free recovery epoch, sproof-driven e2e); WP-B guard-queue → pending-outflows mirror (payload re-derivation, `Balance::MAX` NAV poison on sync failure), market-lifecycle → POL commitments (transactional), `pol.budget_epoch` shrink-to-fit (frozen-at-Seed plans, `SlotsShrunk`, T6 deferral) — the coretime quote leg honestly refused (no spec-d quote authority) and re-owned to the new **B12** row. Merged post-B11 stable2606 main mid-session (PLAN-union + SQ renumber). Triple spec-review (0/5/2, 0/3/3, 0/3/4) + adversarial Codex (3/5/1) → 2 remediation rounds: per-game oracle schedule freeze, kernel-default reader fallbacks, stored-bond challenge floors, metadata-constant cleanup, O(1) protocol-account index, POL terminality latch + VOID resync, paused-clock snapshot suppression, `FreezeActive` attribution, minimal shrink, resume window re-registration, Baseline ring pruning, production-path limit coverage, the 09 §6.2 exit path wired, release blockers `oracle.bond_custody_absent`/`xcm.pallet_xcm_weights_placeholder`. Also fixed pre-existing red benchmarks build (missing `last_change_block`) and acked the two welfare weight regressions. Final gates green on the merged tree (workspace 979+ · runtime 182 · try-runtime suite · wasm legs · keeper · limit-coverage 175 with checker green at B10 ✅ · reference model · doc links · PLAN tables). SQ-244…SQ-263 logged; Decision log: 04 §7 naming truing + 13 §5 re-truing.

**What comes next:** SQ-256/SQ-259 02-bump batch (user sign-off); B12 rulings (SQ-268/269); weight-regeneration sweep (SQ-261); B1b residuals, S4, or Track F

## B10 (PR #107 — Codex-connector review: 2×P2, both TRUE, fix in flight)

Bot P2-1: the remediation's two trailing `RoundState` fields changed the 02 §7.2 `storage.oracle.rounds` layout while the manifest still declares contract v4 — fix relocates the per-game frozen schedule to a parallel internal pallet-oracle map and reverts `RoundState` + the manifest descriptor to the byte-exact v4 shape (also resolves SQ-259 without the 02 bump; SQ-256's event additions remain the only queued v5 item). Bot P2-2: the VOID terminal path released POL but left books `Trading`/`Extended` — a permanently-unreapable slot leak toward `MaxLiveMarkets`; fix closes the books (phase + `ClosedAt`, reusing the close path) in the same transaction. The Codex fix run (gpt-5.6-sol/xhigh) landed both with regressions (`round_state_fields_match_contract_v4`, schedule↔round try-state correspondence pair; `seeded_force_reject_void_closes_and_reaps_all_proposal_books` — closure, POL release, terminal-anchored delay, reap, index shrink) and a 0/0/0 self-review; `storage.oracle.rounds` verified byte-identical to origin/main. SQ-259 resolved by the relocation (no 02 bump). Gates re-run green (workspace clippy · six-package suites 299 · runtime 182/0/3 · benchmarks check · limit-coverage 175 · release 68). Operational note: a PLAN edit made while the run was in flight was clobbered by its snapshot-restore and re-applied — do not edit tracked files during a live Codex run.

**What comes next:** Push to PR #107 → reply to both bot comments

## B1b (PR #104 — CI red: fuzz oracle + weight gate, both fixed)

Two CI legs failed on the pushed branch: (1) the S2 fuzz workspace path-depends on `execution-guard-core`, so SQ-176's new `GuardianView::gate_suspended` trait item broke the `OpenGuardian` open-oracle without touching any `fuzz/` file — implemented (`false`, everything-open semantics); (2) the 15 §4.5 weight-regression gate correctly demanded scoped acknowledgements for the deliberate `epoch.veto_upheld` weight-function removal (call demoted to internal, T24) and the three conservative hand envelopes on `guardian.{approve_action, on_initialize, ratify_action}` (real effect dispatch/fund movement now inside; regeneration owed to the next omni-bencher sweep) — four justified rows added to `tools/ci/weight-regression-acks.toml`, checker now PASS WITH ACKNOWLEDGEMENTS.

**What comes next:** Push; watch remaining CI legs.

## B1b (PR #104 — S4 merge + benchmark-smoke remediation, in flight)

Merged post-S4 main (`d04d3b2`): PLAN session log unioned; B1b's residual SQs renumbered SQ-231/SQ-232 → SQ-233/SQ-234 to clear S4's concurrent claim (all references re-pointed). Local CI verification sweep: 10/11 jobs green (workspace, fuzz, supply-chain, reference-model, docs, TLC model-checking, weights, environments, limit-coverage, property suites at 10⁶); benchmark-smoke red on exactly 1/318 — `pallet_guardian::on_initialize`'s fixture fabricates terminal reviews without real referenda, unsettleable under the production refund discipline in the real-runtime bencher env (mock seam no-ops refunds, so the mock suite hid it). Fixed benchmark-only (`73d91f0`): the fixture now models the overdue review at its stored deadline so maintenance genuinely settles the slash/recall worst case; all nine guardian benchmarks execute and the full 318-benchmark smoke exits 0 — **11/11 CI jobs verified green locally** (S4's new simulation step included). Tooling: local omni-bencher was stale 0.15.0 (train needs 0.22.0) — force-reinstalled with the libclang shim.

**What comes next:** Push merge+fix to PR #104; watch CI.

## G0 (merge with post-B1b main — second SQ renumber; milestone stays 🔨)

Merged `origin/main` `47fd3c6` (B1b completion, PR #104) into `feat/g0-phase0-exit-gate`; conflicts confined to PLAN.md's Decision-log and Session-log tables (union — both sides kept). **Second SQ collision:** B1b's session independently renumbered its two residuals onto SQ-233/SQ-234 — the very numbers this branch took when clearing S4's claim. B1b is merged on main, so its numbers stand (233 unfed playbook triggers, 234 fixed-84 d entrenched enactment); this branch's trio renumbered again, final IDs **SQ-267 (unattested corpus families), SQ-268 (criterion-C key set), SQ-269 (per-class false-pass reading)**, every branch-side reference re-pointed (Current focus, G0 milestone row, SQ table, Decision log, session log). Post-merge verification: phase-gates 68 tests · all six tooling suites · PLAN tables · doc links — green.

**What comes next:** Update the PR #105 body to the final SQ IDs; then the standing chain — SQ-231 ruling → S4 re-calibration + `bleavit.sim-calibration.v1` export (SQ-268/269) → SQ-267 suites → full gate run flips G0 ✅

## O4 (PR #109 opened)

Pushed `feat/o4-runbooks-as-code` and opened PR #109 for the O4 commit (938d25b) on user request.

**What comes next:** Review/merge PR #109; user rulings on SQ-235…SQ-239

## O5 (monitoring/alerting) ✅

**Codex authoring round** (spec-first brief, xhigh): `deploy/monitoring/` — Prometheus scrape config (node/keeper/O5 daemons; the O3 browser-dial job is a commented seam), `rules/bleavit-alerts.yml` covering all 20 rows of the two 12 §6.3 tables (verbatim spec thresholds carried in annotations and gate-checked, exact RB-* runbook labels, `severity: page` on the two page-immediately rows), Alertmanager routing incl. the §5.2(3) release-integrity channel (paging + status page + community webhooks); `tools/monitoring/` — `chain_alerts_exporter.py` (on-chain-event alerting: `state_call` over the frozen `FutarchyApi`, raw 168-byte `ReleaseChannel` decode at the frozen twox128 key, `state_getKeysPaged` prefix counts vs metadata bounds, finalized `GuardianAction`/upgrade events; fail-closed — missing required fields degrade to absent series, never healthy zeros), `attestation_monitor.py` (12 §5.2 steps 1–3 verbatim: ≥3 gateways enforced, fetch by resolved TXID and by name, byte-compare vs the signed `release.json` per-file SHA-256 map, pure-stdlib RFC 8032 Ed25519 + minisign Ed/ED verification, ≥2 distinct attestor keys, keyring-generation triple-equality + `revoked_key_bits` enforcement per 02 §12, `manifest_txid` cross-check, hourly + finalized-head triggers, one-shot exit codes), `check_alert_coverage.py` + `series-inventory.toml` (strict §6.3 two-table extractor; every row bound to a rule with the row's runbook; every rule metric declared and introspected from the exporters' `SERIES` registries; 12 declared seams — 9 runtime-side series owned by B10, 3 probe series by O3). **Native spec-reviewer pre-✅ audit: 0 blocker/2 major/5 minor; all fixed in a Codex fix round and re-verified by the same reviewer**: Proposal-state alert rebound from the execution queue to the 64-entry intake queue (RB-INTAKE/05 T1/TH-16 coherence; SQ-240), mechanical seam expiry vs PLAN.md milestone status (S3 idiom; strict Milestones-table parser with fixtures), all 20 alert exprs test-pinned, keeper-inactivity disabled-role false-page fixed (per-role `planned_total > 0` join, semantics documented), served-state window → joint `max()` per D-6 (SQ-243), per-family scrape isolation (a bad/absent `ReleaseChannel` no longer freezes the other chain domains; `--once` returns 2 on partial scrape). Living docs updated (AGENTS gate + repo-layout rows, README row, B10/O3 seam ownership in their milestone rows). **Verified (orchestrator-independent of the author runs):** monitoring suite 59/59, alert-coverage gate green (20 rows, 31 rule metrics, 12 seams printed), deploy + release + ci + env suites green, env validator, doc links, PLAN tables, `git diff --check`. No Rust in the delta — cargo gates not run. SQ-240…SQ-243 logged; `release.json` field names remain O1-provisional (format-agnostic verdict core).

**What comes next:** O4 (RB-* runbook documents these alerts reference), O1 (freezes `release.json`), O3 (probe seam series); reply to PR review

## O5 (PR #106 — Codex-connector review: 1×P1 + 2×P2, all TRUE, fixed)

All three findings were valid and fixed in a Codex fix round, orchestrator-verified. **P1:** `BleavitDescriptorLeadTimeUncovered` joined series from two different scrape jobs with a bare `and` — Prometheus full-label-set vector matching made the result permanently empty, so the alert could never fire; fixed with set-op `and on()` matching, and every other rule expression was audited for the same cross-job hazard (none found — the other cross-job combinators are `or` unions). **P2 (events):** classic `chain_subscribeFinalizedHeads` headers carry no hash, so the exporter resolved buffered notifications against the newest `chain_getFinalizedHead` and permanently skipped `System.Events` from intervening finalized blocks (missed `GuardianAction`/`UpgradeAuthorized`/`KeeperBudgetLow` counts); fixed by resolving each notified header's own number via `chain_getBlockHash` and processing the contiguous finalized gap per block, bounded by `MAX_EVENT_CATCH_UP_BLOCKS = 512` (a commented resource bound, not a protocol value) with over-cap skips counted into `bleavit_chain_scrape_errors_total` and logged, plus stale/repeat-notification idempotence. **P2 (keeper):** the inactivity rule embedded the 3600 s threshold in the expr AND kept `for: 1h`, paging at ~2 h instead of the spec's 1 h; the `for:` clause is dropped and the restart/disabled-role guard is now `last_successful_crank_timestamp_seconds > 0` in the expr (single delay; the never-succeeded-since-restart blind spot is documented in `deploy/monitoring/README.md` and covered by the self-monitoring meta-alerts + keeper/README's per-required-role mandate). Monitoring suite 59 → **63/63**; coverage gate green (20 rows, 31 metrics); pinned-expr test updated; PLAN tables + `git diff --check` clean. Replies posted on all three review threads.

**What comes next:** Merge PR #106 when review settles; then O4 (RB-* runbooks), O1, or O3

## G0 (merges with post-O4 and post-O5 main — third SQ renumber; milestone stays 🔨)

Two more main advances merged into `feat/g0-phase0-exit-gate` in one sitting: `2ce71cc` (O4 runbooks, PR #109) and `58ae4d5` (O5 monitoring/alerting, PR #106); conflicts were PLAN.md's log/SQ tables (unions) plus, at the O5 merge, the ci.yml tooling-suite loop and the AGENTS.md Tooling-suites row (both sides' suites kept: `tools/phase-gates/tests` + `tools/monitoring/tests`). **Third SQ collision:** O4 claimed SQ-235…SQ-239 and O5 (post-renumber) SQ-240…SQ-243 — merged numbers stand; this branch's trio renumbered a third and final time to **SQ-267 (unattested corpus families), SQ-268 (criterion-C key set), SQ-269 (per-class false-pass reading)**, all branch-side references (incl. the two earlier merge rows) re-pointed, PR #105 body updated. Known in-flight claims overlapping the same range: B10 (#107) holds rows through SQ-250, G1 (#108) through SQ-238 — per the now-standing rule, whoever merges after #105 renumbers. Post-merge verification: phase-gates 68 · deploy 53 (incl. O4's runbook checker) · monitoring 63 · all other tooling suites · `check-runbooks.py` · env validator · limit-coverage 175 · PLAN tables · doc links — green.

**What comes next:** Merge PR #105 soon to stop the renumber churn; standing chain — SQ-231 ruling → S4 re-calibration + `bleavit.sim-calibration.v1` export (SQ-268/269) → SQ-267 suites → full gate run flips G0 ✅

## G0+S4 (user-directed completion drive: SQ-231 mechanism fix implemented across every layer; re-calibration **honest-red**)

User rulings this session: SQ-231 → **mechanism fix first** (no gate-weakening), SQ-244 → **bind all 6 families**, bring S4 to full completion. Delivered: **(spec)** contest-capital measure (04 §7a accumulator; 05 §5.2/§5.4/§5.6; 08 §5.2–§5.5 with the ×7 ceiling bound + §5.4(b) normative-seeding note; 13 `sec.flow_cap` gate-bearing + min 7; 14 TH-4; 15 try-state; 00 D-4 note; SQ-232 resolved → Baseline grades at `dec.v_min.trs`); **(reference model)** accumulator + `l_hat` + tri-state grading, 36 tests, vectors regenerated (LMSR/ledger families byte-identical); **(runtime, audit-scope A)** `market-core` contest capital/`liquidity_hat` + pallet accrual on the conservative per-event grid + `max(published, ×7)` fail-closed ceiling read + try-state, runtime 180/pallet-market 47 green; **(criterion A)** all **14/14 corpus families attested** — six new differential suites + `contest_scenarios`, checker bindings + floors, and the defect sweep they exposed: gate-veto ordering (wrong 10 % bond slash), first-pass-Invalid free extension, `settlement_score` ulp (fixed exactly on BOTH sides — the Python model shared the defect at the ε corner; corpus delta one field); **(evidence)** `bleavit.sim-calibration.v1` producer (12 tests, consumer round-trip proven), 10⁷-point sweep corpus pre-generated; **(S4)** sim taught the new mechanism (45 tests) + full 10,000-proposal re-calibration (4,114 s; a zombie first run was killed before it could overwrite the artifact). **Re-calibration result — still red, and informative:** PARAM **3.81 %** / TREASURY **1.64 %** (CODE/META **0.000 %**), `sec.flow_cap` candidate 29 → 15, sub-3P brackets not clean; `--check` structure green, exit 1 on the 4 recorded violations only. Reading: the churn/wash seam SQ-231 named is closed (the measure can no longer be inflated flow-cheaply), but **held-exposure self-certification survives** — an attacker holding net position through the window supplies contest capital at the cost of market risk, not 3P; CODE/META pass precisely because gate books catch what the depth certificate cannot. New SQ to be numbered post-merge (main now occupies through SQ-266).

**What comes next:** Merge post-B10/B12/B13 main (4th SQ renumber 244–247 → 267–270); then the user decision on mechanism round 2 (opposing-side/at-risk measure, PARAM/TREASURY gate books, or park)

## B10 (PR #107 — main-merge reconciliation ×2 + CodeQL)

Merged post-B1b main (`47fd3c6`) and post-O4/O5 main (`58ae4d5`) into the branch. Semantic compositions: guardian keeps B10's Params-driven `ReviewDeadlineEpochs` removal while adopting B1b's kernel `PlaybookFreezeWindowBlocks` constant (metadata surface test extended — a genuine 02 §9(2) kernel value); `rederive_call` keeps B10's structure (B1b's conditional freeze check is subsumed by `GateSuspended` at guard check 9 / `FreezeActive` at check 10; main's gate-suspension e2e passes unchanged); ledger error variants ordered main-first with `InflowCapExceeded` appended (SCALE indices preserved); the B1b ledger-freeze market test now settles the proposal before reaping (B10's `SettlementObservedAt` terminality latch); dual-test metadata reconciliation verified. SQ dedup per the main-wins convention: B10's branch rows renumbered **SQ-244…SQ-263** around main's B1b (SQ-233/234) and O4/O5 (SQ-235…SQ-243) claims — all PLAN/AGENTS/registry/release-manifest references re-pointed (S4 keeps SQ-231/232). O5↔B10 seam collision: O5 assigned 9 runtime telemetry seam series to B10, whose ✅ trips the coverage gate's mechanical expiry — re-owned to the new **B13** milestone row (series-inventory owners + checker fixtures updated; gate green 20 rows/31 metrics). CodeQL: the four `rust/cleartext-logging` PR alerts (market tests 60/146/351/2117) are name-heuristic false positives on `ensure_market_accounts_indexed`'s DispatchResult; helper renamed `ensure_market_book_indexed` (private, non-contract) — alerts close as fixed on push. Verified: workspace 65 suites green, clippy `-D warnings`, runtime 208/0/3, monitoring 63, release/limit-coverage/deploy/ci/reference-model suites, coverage gate, PLAN tables, doc links.

**What comes next:** Push; watch PR #107 CI; SQ-256/SQ-259 02-bump batch and B12/B13 remain queued

## B12 + B13 (in flight, worktree `milestone-B12-13`)

**Session in progress — WIP row for the Stop-hook contract; statuses are 🔨.** Branch reset onto post-B10 main (`80d6bb9`). SQ-245/SQ-246 ruled under the user's decide-yourself delegation and the spec edited accordingly (09 §4 quote-authority/freshness/denomination block; 08 §1.1/§1.4; three new 13 §1 TREASURY keys `ops.ct_dot_rate`/`ops.ct_fee_dot`/`ops.ct_quote_ttl`; 12 §6.3 `TelemetryApi` runtime-telemetry-source paragraph) — Decision log 2026-07-18, both rows marked ratification-requested; SQ-268/269 flipped to resolved. Pre-stubs committed (`telemetry.rs`/`tests_telemetry.rs` skeletons, stall-detector visibility) and two parallel Codex authors (xhigh) launched on disjoint file scopes from committed briefs: B12 (treasury quote lifecycle + Params keys + `XcmRenewalDispatcher` swap + classifier/S5/limit-coverage) and B13 (`TelemetryApi` + exporter families for the 9 seam series). No author output merged yet; tree contains in-flight authoring.

**What comes next:** Harvest author reports; run full gates; spec-reviewer audits (R-6); flip B12/B13 to ✅ with registry/coverage expiry proof; final session-log row; PR

## B12 + B13 (remediation in flight, worktree `milestone-B12-13`)

**WIP row 2 (Stop-hook contract; statuses remain 🔨).** Author round committed (`e963f4d`): B12 (authenticated quote lifecycle, 3 `ops.ct_*` keys end-to-end, `XcmRenewalDispatcher` swap, classifier/S5/freeze wiring, registry unwired→dispatch-limit + marked 9th-quote test, keeper planning) and B13 (non-02 `TelemetryApi` + runtime impl, ledger L-2 helper, exporter families, all 9 seams live). Orchestrator gates all green on that tree (workspace gates, fuzz, supply-chain, monitoring 65, coverage 20 rows/31 metrics, limit-coverage 178/0 unwired, reference model, tool suites, env validator, runbooks, PLAN tables). Dual R-6 spec audits done: B12 0/1/5 — the major (PB-MIGRATION leg unsatisfiable under FRAME MBM lockdown) resolved by deliberate 09 §4 scoping + 08 §1.2 reserve-health carve-out (Decision log, ratification requested), SQ-264 logged; B13 0/2/4 — 12 §6.3 amended (fail-closed boundary, numerics event-stream sourcing), SQ-265/SQ-266 ruled and logged. Single Codex remediation round (xhigh) now executing the 10-item fix list (`FIX-BRIEF.md`): DOT-planck single-homing, prune-rebate scoping, storage-version bump, freeze-leg tests, emit-don't-suppress loss, cumulative domain-rejection counter + `increase()` rule, stall union, per-component POL labels, window-bound hoist, single L-2 bookkeeping.

**What comes next:** Harvest fix round; re-run full gates; flip B12/B13 ✅; final row; commit; PR

## B12 + B13 (both → ✅, worktree `milestone-B12-13`, PR #111)

**Dual-milestone session under the user's explicit both-milestones/decide-yourself directive.** Branch reset onto post-B10 main. Rulings: SQ-245/SQ-246 (quote authority = stored ops-multisig account; DOT-planck quotes; ceil USDC debit at `ops.ct_dot_rate` + `ops.ct_fee_dot`; TTL/supersession/expiry-prune), then two parallel Codex authors (xhigh, disjoint scopes, committed briefs) implemented B12 (treasury core/pallet lifecycle, 3 new 13 §1 keys end-to-end, `XcmRenewalDispatcher` swap, classifier/S5/freeze wiring, registry → dispatch-limit + marked test, keeper planning) and B13 (non-02 `TelemetryApi`, ledger L-2 helper, exporter families, all 9 seams live). Dual R-6 spec audits (B12 0/1/5; B13 0/2/4) → four deliberate spec amendments (09 §4 freeze scope, 08 §1.2 carve-out, 12 §6.3 fail-closed boundary + numerics sourcing; Decision log ×4, ratification requested), rulings SQ-265/SQ-266, new SQ-264, and one 10-item Codex remediation round (post-fix review 0/0/0). Final gates green, orchestrator-verified on the finished tree: workspace gates end-to-end (incl. runtime release/bench/try-runtime + wasm + keeper legs), fuzz gates, supply-chain gates, monitoring 65 + coverage 20 rows/32 metrics (zero B12/B13 debt in either expiry ratchet), limit-coverage 178/0 unwired, reference model + vectors + doc table, deploy/release/env/ci suites, env validator, runbooks binding, doc links, PLAN tables. Residuals: SQ-261 weight placeholders (release-blocked), SQ-263 unchanged, SQ-264 genesis seats (fail-closed), SQ-265/SQ-266 normative tails open.

**What comes next:** Ratify the 4 Decision-log amendments + SQ-268/269 rulings; SQ-256/SQ-259 02-bump batch; SQ-261 weight sweep; B1b residuals, S4 seam (SQ-231), O1/O2/O3, or Track F

## B12 + B13 (PR #111 — Codex-connector review: 2×P2, both TRUE, fixed)

(1) **POL-floor book-kind split:** `telemetry::pol()` summed the whole treasury `pol_commitments` mirror into the `POL` component's floor, so a live Baseline book — seeded from the dedicated `POL_BASELINE` line (08 §4.3) — would fire the `pol` alert while its dedicated funding was correct. The floor is now split by book kind at the market pallet's identity-keyed source (`LivePolCommitments` + `Markets`): proposal-book obligations → `POL`; Baseline obligations plus the next-epoch standing seed headroom → `POL_BASELINE`; a missing backing book and either-bucket overflow fail closed per 12 §6.3. Telemetry test rewritten market-sourced with regression cases (kind split, both overflow paths, unbacked commitment). (2) **Treasury `STORAGE_VERSION` reverted 1 → 0:** the bump rode the `coretime_quotes` `(u32, Balance)` → `CoretimeQuote` layout change with no `OnRuntimeUpgrade`, leaving in-code v1 over untranslated state on any upgraded chain; no persistent network exists (every environment rebuilds from genesis, which stamps the then-current version) and every other pallet is v0 — the doc comment now binds the first real bump to landing together with its migration. Gates: runtime 221 · treasury 44 · market 53 · fmt · clippy `-D warnings` — green. Bot comments replied.

**What comes next:** Merge on green CI

## B12 + B13 (PR #111 — local gate sweep, weight-ack fix)

Local pre-merge run of every CI-affected gate while GitHub runners queued. Caught a red gate: `check-weight-regression.py` failed on the branch — the SQ-261 `execute_coretime_renewal` placeholder (+9150% ref_time, full 10B local-XCM envelope) had no acknowledgement, and all six pre-existing acks (B10 welfare ×2, B1b epoch/guardian ×4) had gone stale against the moved base. Fixed `tools/ci/weight-regression-acks.toml`: stale six removed, one scoped SQ-261/B12 ack added — gate now PASS WITH ACKNOWLEDGEMENTS. Also green locally: doc links/PLAN tables/runbooks, limit-coverage 178/0, env validator + monitoring 20 rows/32 metrics, reference model + simulation + tool suites + vectors + doc table, supply-chain gates; CI's differential sweep green remotely. Heavy chain: workspace gates, fuzz gates, property full-count all PASS; **benchmark smoke FAIL (5 benchmarks)** — bisected to **main's B10 merge `80d6bb9`** (merge-base runtime reproduces all 5 identically; green on pre-B10 `2ce71cc`): B10's ledger-terminal latch (`observe_proposal_terminal`/`observe_baseline_terminal` walking real `Markets` books), `FundedPolSlots` Seed-boundary population, and `SettlementObservedAt` reap anchor invalidated benchmark fixtures that fabricated bare market ids / seeded mid-Seed / never drove vaults terminal. Fixed benchmark-only: epoch tick fixture writes `FundedPolSlots` via `T::PolBudget::proposal_seed_plan`; market reap fixture reaches terminal through production resolve→settle; runtime `benchmark_ensure_market_books` (cfg runtime-benchmarks) backs `prime_decision`/`prime_settlement` ids with real books via `create_market` (live-Params b-values). Verified: full epoch+market omni-bencher legs green, runtime 221 / epoch 80 / market 53 tests, clippy `-D warnings`, fmt.

**What comes next:** PR #111 merged without these fixes → follow-up PR `fix/ci-weight-acks-and-benchmark-fixtures` off merged main carries both (weight acks + inherited benchmark repair); confirm main CI green after its merge

## G1 (Phase-1 drills — first real execution; 🔨, worktree `milestone-G1`, branch `feat/g1-phase1-drills`)

User: "Implement milestone G1 by using codex subagents intelligently … Decide on your own. Open a PR." **Outcome: drills 01/02/03 PASS 10/10 end-to-end against the real `bleavit-node` + `bleavit-keeper` binaries — the first time any committed B7 drill has ever executed — and every non-runnable drill is re-gated on verified attribution.** Three infrastructure defect families had made all execution impossible (V-35): **(1) the Paseo-CSG release blocker** from the release.yml dry-run row — root-caused (substrate-wasm-builder walks up from `OUT_DIR`; the in-repo `CARGO_TARGET_DIR` made every paseo runtime's `cargo metadata` resolve the *Bleavit* workspace) and fixed with `WASM_BUILD_WORKSPACE_HINT` in `generate-relay-specs.sh`; **(2) zndsl grammar/arithmetic** — all 9 drills carried grammar-illegal `#` comments and lacked the mandatory `Creds: config`; Zombienet timeouts are 32-bit signed ms (the 6,000,000 s toml value had collapsed to a 1 ms watchdog; drill 09's 6,001,000 s step split into 3 × ≤ 2,000,000 s); **(3) relay scheduling** — paseo-local builds `ValidatorGroups` only at 600-block session boundaries from then-active `num_cores` (genesis 0), so zombienet's injected coretime assignments were never polled (diagnosed live: `claimQueue {}`, core `queue {1,1}` untouched, groups `[]` at block 40); fixed by building the pinned CSG with paseo's `fast-runtime` (10-block sessions) + seeding `num_cores = 3` (Decision log). **Keeper topology (Codex subagent, write-mode):** real live-mode daemon as Zombienet node `keeper` via arg-translating `exec` wrapper (SIGSTOP/SIGCONT hit the keeper PID), pinned collator RPC 19944, `bleavit_process_start_time_seconds` metric for Zombienet readiness, topology/port validator + evidence-runner port-collision checks, keeper 37 tests; its sandbox denied sockets (`listen EPERM`) so the orchestrator ran the real drills. **Drill 06 first execution found 3 helper defects, fixed with the runtime as arbiter:** ss58-prefix-blind address comparison (chain 7777 vs keyring default 42 — all four helpers now derive `ss58Format` from `api.registry.chainSS58`), guardian `Members` decoding (raw core `[u8;32]` array, never ss58 — compare raw account keys), funding gated on `free` while genesis-vested guardians have zero *usable* balance (now `free − frozen`; surfaced SQ-275 — the `UnvestedFundsAllowedWithdrawReasons` carve-out is dead under the fungible fee adapter). With those fixed the 5-of-7 guardian workflow executes through propose + 3 approvals and the dispatching approval correctly refuses `TriggerInactive` — proving the drill machinery live and the drill blocked solely on **SQ-274** (no 09 §3.2 `migrations.force_failure`/`retry`; drill re-gated release-tier, evidence fail-closed with attribution). **Gate re-attributions:** 03 → `[]` (executed); 04/05 → B10 dead-man detector (A8/A11 freeze surfaces wired, `note_dead_man_engaged` has no caller); 07 → B10 AssetTransactor composition (SQ-101 landed; spec_version still 1); 09 → `[]` (armed; SQ-128 soak). READMEs re-trued (runnability matrix, NOTE(G1) authoring constraints, single-homed-pin wording). **Gates green:** env validator · 49 env tests · keeper fmt/clippy `-D warnings`/37 · js `node --check` ×9 · bash -n · doc links · PLAN tables. V-24 completed, V-36 added; SQ-274…SQ-277 raised; Decision-log row for the drill-environment rulings. **Dual review (native spec-reviewer 0/1/3 + adversarial Codex 3/5/3) — every finding fixed in-session:** coretime/AH `fast-runtime` forwarding (the TIMESLICE split major) + spec regeneration; keeper binary as evidence prerequisite/release.yml build; drill 03 re-run **12/12** with pre/post `bleavit_keeper_connected` assertions; drill 05/06 second gates declared (staged-renewal form; the 06 effects-substrate entry was later dropped at the post-B1b merge — #104 landed the substrate); drill 07 full-composition gate; validator exact-args + port-spelling hardening with regression tests (env suite now 51); SQ-274(b) filter caveat; Decision-log reword to the real 02 §11 text; drill-09 cadence-assumption note; 9/9 parse transcript. G1 stays 🔨 (G0 dep; B10 detector; SQ-274 + effects substrate (landed with B1b #104 at merge); 08 staging (B1b #104 landed the effect substrate; SQ-233 trigger feed + queue staging remain); long-horizon 04/08/09 runs; SQ-128/SQ-202/SQ-204/SQ-276/SQ-277).

**What comes next:** B10 (dead-man detector + AssetTransactor) un-gates 04/05/07; user rulings: SQ-274 (migration control surface), SQ-275 (fee carve-out), SQ-128 (soak epoch length); drill 08 staging after B1b SQ-176; then the g1-tier long runs

## G0 (merge with post-B10/B12/B13 main — fourth SQ renumber; semantic Rust merge)

Merged `origin/main` `077a055` (B10 wiring closure #107, B12/B13 #111, CI green-up #112) into the branch. First genuinely semantic merge of the series: B10's `TwapCumulative` two-limb accumulator, per-market `registered_window_lengths`, and `register_decision_window` `proposal` arg composed with the SQ-231 contest-capital work (`measured_depth` resolved to the L̂-with-ceiling form over B10's extension-aware windows; B10's gross-sum form superseded by the amended spec; both sides' new market-core functions kept; `contest_notional_blocks` → `contest_capital_blocks` rename carried through B10's telemetry tests). **Fourth SQ collision:** main now occupies SQ-240…SQ-266 (B10/B12/B13 renumbers incl. their own SQ-245/246 rulings) — this branch's four renumbered **SQ-267 (unattested corpus families), SQ-268 (criterion-C key set), SQ-269 (per-class false-pass reading), SQ-270 (08 §5.3 P-scaled seeding gap)**, all branch-side references re-pointed. Post-merge verification: workspace **69 suites all ok** (runtime 221, pallet-market 55), clippy `-D warnings` on the four merged crates, fmt, limit-coverage **178 keys/0 unwired** (main's B10/B12 closure composes with the `sec.flow_cap` min-7 row), all 8 tooling suites, doc links, PLAN tables, vector freshness — green.

**What comes next:** Close-out: honest S4/G0 statuses, PR #105 body re-point to SQ-267…270, spec-reviewer pass, fuzz-gates rerun (core surface changed), user decision on mechanism round 2

## G0+S4 (completion-drive close-out — both milestones stay 🔨 on the honest evidence)

Current focus, G0/S4 milestone rows, and the SQ table re-trued to the post-merge, post-re-calibration truth: criterion A fully closed (14/14 true equivalence), criteria B/C red — PARAM 3.81 %/TREASURY 1.64 % ≥ 1 % with CODE/META at 0.000 %. **SQ-271 logged (held-exposure self-certification, mechanism round 2)**: churn is closed, held capital is not, and the class split in the results points at gate books as the structural fix. Per the user's standing *mechanism fix first / genuine < 1 % or nothing* ruling, no gate-weakening fallback was taken; G0 and S4 park with every deliverable except the passing evidence in place (producer bridge consumer-proven, sweep corpus generated, checker able to reach `phase0_exit: true` the moment a green artifact exists). PR #105 body re-pointed to SQ-267…SQ-270.

**What comes next:** **User ruling (2026-07-18): park both milestones honestly and merge PR #105 as the fail-closed gate it is** — mechanism round 2 (SQ-271; gate-book extension favored by the 0.000 % CODE/META evidence) gets its own session. `tools/ci/fuzz-gates.sh` re-run green on the final tree (market-core surface change revalidated). Then: SQ-271 session → S4 re-calibration → V-12 publication → full gate run flips S4+G0 ✅

## G0+S4 (SQ-271 mechanism round 2 — multi-Codex diagnosis + Phase-1 fidelity fixes; IN FLIGHT, uncommitted)

User directive: solve the PARAM/TREASURY sub-1 % problem using multiple Codex subagents. Ran a 4-agent read-only Codex diagnosis fan-out (TREASURY gate-extension plan; PARAM belief-capture real-vs-artifact; PARAM defense design; independent blind red-team). **Two structural findings, both orchestrator-code-verified:** (a) the sim's attacker never touches the gate books (`_gate_books` is organic-only, `_apply_pair_attack` moves only the decision pair), so CODE/META's 0.000 % and any gate-based fix are UNVALIDATED against a gate-suppressing adversary; (b) a three-way step-9 depth divergence — the sim uses `min(contest_accept, contest_reject)`, the runtime `measured_depth` **sums** the two books, and the reference `l_hat` takes a pre-aggregated scalar so the SQ-267 vectors never exercised the aggregation. 08 §5.4's worked example counts a single `dec.v_min` (400k, not 800k), so MIN is normative and the runtime SUM is an audit-scope-A under-security bug that makes the deployed mechanism MORE vulnerable than the failing sim shows. PARAM's real seam is balanced settlement-riskless inventory counted as security depth; the sub-2k "flips" are sub-hurdle artifacts (corrects the earlier framing). **Phase 1 launched (two write-capable Codex agents, disjoint dirs):** the runtime MIN-fix (13-file changeset — a shared `decision_pair_contest_capital` reduction across reference/runtime/sim, spec 00/04/05/08, plus a new asymmetric-contest differential vector) **completed (18m53s), was independently re-verified by the orchestrator (reference 36, differential decision/treasury vectors, runtime pair-minima pin, fmt/clippy, calibration `--check` digest-consistent), and committed as SQ-272**; the sim gate-attack agent's wrapper was killed by accident and its task orphan-hung with no edits left, re-launched fresh on the committed tree. **SQ-272 logged + resolved (MIN under R-7, mutation-locked by the asymmetric vector).** G0/S4 stay 🔨.

**What comes next:** Independently re-verify the MIN-fix (confirm its new vector FAILS against a sum aggregation — the regression proof), commit it and log SQ-272; re-launch the sim gate-attack agent fresh; re-measure gates-under-attack (decisive: do gates survive sub-3P?); then the TREASURY full 4-book gate (contract v4→v5, joint sign-off) plus the PARAM at-risk depth-measure fix; re-calibrate; if genuinely < 1 % per class, the publication pipeline flips both ✅

## G0+S4 (SQ-271 — sim gate-attack hardening recovered + committed; gate-attacking re-calibration running)

The re-launched sim gate-attack agent **failed at 36 min during final verification**, but its changeset was complete and green (48 sim tests), so it was RECOVERED not re-run: `_gate_attack_targets`/`_apply_gate_attack`/`_allocate_attack_budget`/`_refresh_gate_evidence` in `engine.py` give the attacker ONE shared budget allocated across the decision pair and the four gate books, with real corrective flow, re-suppression only after executed flow (no free suppression), conserved budget, and the `attack_cost_validation` bracket search now includes gate-suppression cost. Seeded META/TH-4 demo: an organic `GateVetoSecurity` is **suppressed at 0.5×3P** (sub-3P), though full `Adopt` still needs the welfare hurdle (3×3P there). Committed `600575e`. **Decisive `--full` re-calibration running (detached, ~50 min)** to regenerate the artifact under the gate-attacking sim: does aggregate gated-class decidable-harm false-pass now leak sub-3P (gates do NOT robustly resist) or stay < 1 % (gates hold ⇒ TREASURY 4-book gate validated)? G0/S4 stay 🔨.

**What comes next:** On re-calibration: read gated-class rates + gate-suppression brackets → if gates hold, implement the TREASURY 4-book gate (contract v4→v5) + PARAM at-risk measure; if gates leak sub-3P, the PARAM at-risk depth-measure becomes primary for both classes. Then commit the regenerated artifact, iterate to genuine < 1 %, publish (V-12), flip S4+G0 ✅
