# Session log — 2026-07-31

## E5 (weight regeneration)

**CI on `5726f68`: 19/20 SUCCESS, `Benchmark smoke` FAILURE — and it caught a real, expected consequence of the SQ-530 fix.** The drift gate re-measured every pallet and reported **12 `STALE` functions, every one of them `worst_case.writes N -> N+1`**: `crank_observe`, `reconcile`, `sweep_dust`, `sweep_dust_baseline`, `record_snapshot`, `record_daily_gate`, `ack_observed` (both the oracle and registry weights), `crank_close`, `reap_epoch`, `drive_oracle_boundaries`, `collator_compensation`. That is exactly the `PendingMainCredit::kill()` the fix adds to the four paths that bypass `persist`, propagated to every dispatch that reaches a rebate or reward payout. **The fix is right and the extra write is the true cost**, not an artifact: a conditional kill would trade one unconditional write for a read *plus* a write in the worst case, which is the case benchmarks measure, so it would be dearer and would risk the SQ-523 understatement shape. Regenerating the 7 owning pallets at the **committed 50×20 fidelity** rather than CI's 2×1 — a 2-point fit can lose a linear term outright, and the release-blocking `Component weights at committed fidelity` job compares slopes against whatever fidelity the header records. This is also the answer to why `--changed` would not have worked: the source that moved is `pallets/futarchy-treasury`, while the weights that moved live in seven other pallets that merely *call* the rebate sink.

**What comes next:** Land the regenerated weights, re-run the drift gate, confirm CI 20/20

## E5 (review response)

**Two P1s from the #198 Codex connector, both correct, both landed.** **(1) The keeper rebate is not price-invariant.** I had written in three places — the kernel doc comment, 08 §6.2 and the 13 §1 row — that SQ-531 fixed the *ratio* so 3× held "at any price". It does not: the fee is weight-fixed in **VIT** while `keeper.rebate` is a **stored USDC** row, so at 4× the derivation price the rebate is **0.75×** the fee and cranking becomes loss-making. I caught this independently minutes before the review landed, which is worth recording only because it means the claim was wrong on its own terms and not merely contested. All three sites corrected, the unsafe direction (VIT appreciation) named, and the mechanism filed as **SQ-534** — screen the pair at the amendment boundary in both directions, the shape rule 7 already uses for `ledger.redeem_fee` ≤ `mkt.fee`. **(2) Self-funding was conditioned on depth when it depends on occupancy.** I called a five-slot PARAM slate at the `dec.v_min` floor "the least activity at which the chain decides anything", which conflates two axes: five slots at the floor is minimum *depth*; minimum *activity* is **one** proposal, and held capital scales with occupancy. At one occupied slot the chain earns 70,311 against a 239,728 base and does not self-fund under either cost base. Break-even at τ = 3 is **six** slots — above `epoch.slots` = 5, so unreachable at the default slate size; it comes inside the slate only via the collator lever (3 slots) or at the measured τ = 5.8 (3 slots). 08 §10 now carries the occupancy table and states plainly that the unbounded-runway conclusion **rests on sustained slate utilisation**, a demand assumption nothing here evidences (SQ-506). New `break_even_slots()` helper makes the claim machine-checked; suite at **120**.

**What comes next:** Land the regenerated weights + these corrections, re-run drift and CI

## E5 (occupancy analysis)

**CI 20/20 SUCCESS on `a427ac9`**, including `Benchmark smoke` — the job the SQ-530 write broke. Both Codex P1 threads replied to and closed (four duplicate replies posted by a loop whose comment list grew mid-stream were deleted rather than left). **Fixing the occupancy P1 exposed its mirror, running the other way.** `break_even_slots` held the FULL-slate cost fixed while varying revenue occupancy — charging five slots' proposer rewards against one slot's trading. Two §10.1 rows are per-proposal rather than standing (`REWARDS`, paid only on `Executed`; the realized POL divergence, which needs a seeded book), so `cost_at_occupancy()` now scales them and holds the rest. **The consistent reading is the most decision-relevant result of the whole milestone**: at the shipped cost base break-even is **7** occupied slots at τ = 3 — above `epoch.slots` = 5, so unreachable without also amending that key — while at `collator.comp_epoch` = 500 it is **1**. The reason is that `ops.collators` is 72.6 % of the base and **standing**: it bills whether or not the chain decides anything, which is the wrong shape for a protocol whose revenue is activity-linked. That reframes the declined lever: the question is not "is 500 USDC/epoch enough to run a node" but "does the protocol carry a standing obligation large enough to require sustained full-slate demand". 08 §10 now states it that way and still declines to answer it (R-2: unsafe direction, no evidence anchor), which is the right division of labour with launch governance. Suite at **122**.

**What comes next:** Land and re-verify; then E5 pass 2 is governance work only

## E5 (self-check)

**CI ALL SUCCESS on `5006ee6`.** Requested a fresh `@codex review` of the final state, since the design moved substantially past the commit the connector had reviewed. While drafting that request I flagged my own POL split as the part I was least sure of — and it was wrong. `cost_at_occupancy` treated the POL divergence row as **six equal parts** (five proposals + one standing Baseline), reasoning from 08 §10.5's observation that the Baseline's cash equals PARAM's per-proposal cash. That is a **different quantity**: §10.5 measures custody *at risk*, where a branched proposal contributes only half its commitment because one `split` funds a branch pair. Realized divergence loss is `b·[ln2 − H(p)]` and therefore linear in `b`, so the row splits by total `b`: a PARAM proposal carries `2·pol.b + 4·pol.b_gate` = 50,000 against the Baseline's 25,000, making the Baseline **1/11** of a five-slot row, not 1/6. Now derived from the registry values rather than hardcoded. **Immaterial to every conclusion** — ~1,400/yr on a 239,728 base, and the consistent break-even is unchanged at 7/3 slots shipped and 1/1 with the collator lever — but wrong, and the kind of wrong that is only ever caught by writing down why you believe something. Suite at **123**.

**What comes next:** Await the Codex re-review; address whatever it raises

## E5 (re-review response)

**Codex re-review of the final state: 1 P1 + 5 P2, and all six were valid.** **(a) P1 — the POL split.** It reviewed the commit still carrying my 1/6 reading, but pushed further than my own `b`-weighted fix: realized loss is `b·[ln2 − H(p)]` and 04 §12 gives *different prices* per book type, so even a `b`-split is first-order. Rather than invent prices the spec does not pin, I **bounded** it — driving the whole POL row to both extremes (fully scaling, fully standing) changes the published break-even in **one of four cells, by one slot**. The conclusions do not rest on the split, and that is now a test rather than an assurance. **(b) P2 — keeper cost is not standing.** Correct and I had it wrong: 13 §5 item 4 derives observation load from `epoch.slots·6 + 1`, so it scales with occupancy, and my version also *capped* it at five slots while the break-even search evaluated six through twelve. Fixed and tested. **(c)–(e) Three P2s: §10.4's crossover table, §10.6's quiet epoch and §10.6's endowment answer were all still on the superseded 1,145,562 basis** while the new headline figures sat elsewhere in the same section — so a reader following the normative tables reached the opposite conclusion from the prose. All three regenerated (`V*` 29.1M/13.2M; quiet epoch 62,281 → **10,200**; runway 9.7 → **46.5 yr**), with the superseded rows retained and struck rather than deleted. **(f) P2 — my own drift-alarm test still carried the price-invariance framing** it was written to replace: it hardcodes 0.05 USDC/VIT, so it cannot speak to price at all. Rescoped in its doc comment to a **weight**-drift alarm, with SQ-534 named as the thing that would actually guard the ratio. Suite at **125**.

**What comes next:** Land, re-verify, re-run CI

## S6 (reference-model grounding)

**Three areas of normative prose that no executable artifact covered are now Python, and running them found one spec defect and four facts the documents state nowhere.** The brief was to ground uncovered spec prose rather than to implement a milestone, so the target was chosen by measuring coverage: the reference model cited 05 §4.x, 07 §8/§10/§11, 08 §1–§10 and 13 §1/§5 item 5, and cited **nothing** from 05 §2–§3, 07 §5–§6 or 13 §5 items 1–4. Those three gaps are where the spec makes claims *about behaviour over time* — wedges, ladders, admission screens — which is exactly what prose is worst at holding and what an executable model checks for free. **`lifecycle.py` + 40 tests** (05 §2–§3): the T1–T26 table as data, with termination, terminality and I-15 **computed** from it (93 reachable configurations, no cycle, `{Settled, Cancelled}` absorbing, `Executed` unreachable from every rejection path); §2.2's mermaid block parsed out of the document to check its own "every edge below appears above and vice versa" claim (30 = 30, exact); and §3.3's three wedges simulated — `k = 3` wedges `qualify` from epoch 4 and never recovers, cutoff `current − 20` jams `record_snapshot` at epoch 20, `MAX_SNAPSHOTS = 20 × k` jams at epoch 13 where `20 × (k + 1)` saturates at exactly 60. **`disputes.py` + 47 tests** (07 §5–§6, §11): every published figure pinned, and §6.3's directional amendment screen implemented **alongside the non-directional one it forbids**, so "tests only `coverage ≥ required` freezes both inputs" is demonstrated rather than asserted. **`occupancy.py` + 28 tests** (13 §5): both laundering cases to the unit, then a search of §5's own claim — the registry-only screen breaks in **two** lawful amendments (shorter than either worked case: `epoch.slots` 5→3 alone buys enough headroom for `dec.window`'s full 20 % max-Δ, real load 160,704 vs the 133,920 envelope), the composed screen breaks in none to depth 8. **SQ-535 raised, doc half fixed.** 13 §3.1 rendered the decision window as the kernel fraction `[15/21, 18/21)` while 05 §3.1 — which owns the schedule — anchors it absolutely to Trade close. Executably, the two agree at **exactly one** of the 19,201 lawful `epoch.length` values. Not a live defect (the keeper reads `ordered[5]`/`ordered[6]` and skips the entry), but `phase_offsets::DECIDE_WINDOW_NUM` is published in the 02 §9 `Epoch::PhaseOffsets` list, so the contract half needs a bump and is filed in batch X. **Four derived facts**, all consequences of the documents' own numbers: the honest challenger's break-even confidence is **5/7**, not 1/2 (§5.5 pays the winner 40 %, INSURANCE 60 %); §6.2's `17.5 %` is a constant only above `StakeAtRisk = 400,000` (175 % at 40k, where `orc.bond_floor` binds); 13 §5 item 2 caps `epoch.slots` at **5**, making seven of that row's twelve published values nominal; and item 1's frozen **2,240** dominates a reachable maximum of **1,064**. None is a defect — the first two are safe directions and the last two are deliberate conservatism — but none was written down. **Gates:** reference-model 240/240 (was 125), simulation 53/53, vector freshness, LMSR doc-table, PLAN tables, spec-question batches, limit coverage 194 keys / unwired 0 and byte-identical before and after the 13 edit. **No Rust touched**, so the Rust gates are not implicated

**What comes next:** SQ-535's contract half, whenever the next `INTEGRATION_CONTRACT_VERSION` bump lands. The same coverage measurement leaves **06 (governance tracks, intake economics, guardians) and 09 (execution guard, inflow caps, phase table) with no Python at all** — 06 §4's slot-monopolization pricing and 09 §5.2's Phase-3 exposure caps are the next two with real arithmetic behind them

## S6 (Codex review response)

**Three P2 findings on PR #200, all three valid, all three fixed — and the review caught the one thing this milestone was supposed to be immune to.** S6's whole argument is that executing prose beats reading it; finding (2) is a case where I executed the prose, got the right numbers, and then *wrote an assertion that did not test the claim*. **(1) T21 was unconditional.** 05 §2.1: T21 "fires iff markets were deployed and the vault is open", and *Terminal states* names "`Rejected` where no vault exists (pre-Seed rejections via T20)". My model let `Submitted --T20--> Rejected --T21--> Measuring --T19--> Settled` with no T7 anywhere, so the closure suite validated a strictly larger machine than the spec's and could not have caught a pre-Seed regression. Vault state (`none`/`open`/`voided`) is now part of a `Config`; T7 opens, T20 voids an open one, T21 requires open. `is_terminal(config)` replaces the state-only `TERMINAL_STATES`, and the terminal set now **computes** to the document's own list. Reachable configurations 93 → 107 — the count went *up* while the reachable *behaviour* shrank, which is the giveaway that the vault was carrying real information the old model discarded. **(2) The `k = 3` wedge claim is false — spec corrected.** Codex's counterexample was exact: epoch 4 fails, epoch 5 retires cohort 0 and admits, and failures recur at period 5. Executed over 200 epochs, `k = 3` admits **4 in 5 forever**. `simulate_cohorts` had recorded only the *first* failure, and my test then asserted `all(count == cap)` over the live counts — which proves saturation and says nothing about admission. Two different quantities, one of which I checked and the other of which I claimed. 05 §3.3 now states the true failure mode (a permanent, unrecoverable 1-in-`k+2` forfeit of measurement capacity, unrecoverable because a cohort is per-epoch) with the superseded sentence quoted; the kernel ceiling stands unchanged on the corrected argument. `CohortRun` records admission per epoch so the conflation is not reachable again. **(3) `InFlight.readable()` missed 13 §5's second refusal cause.** The sentence names two — "state unreadable, **or a bound already exceeded**" — and I implemented one. Not theoretical: a live `epoch.length` of 604,821 is 21 blocks over the maximum *and* still a multiple of 21, so every downstream exactness check passed it, and a `dec.window` reduction then composed to an envelope under its frozen figure and returned **admitted** — compensation inside the derivation hiding an already-out-of-bounds live value, which is precisely the fallback G-1 forbids. `refusal_reason()` now screens both fields against 13 §1 and 05 §3.1 before `compose()` runs, and a test pins that lawful in-flight states still compose so the refusal is not over-broad. **What I take from it:** two of the three are the *same* mistake in different clothes — asserting a property adjacent to the one claimed (saturation for admission; readability for establishability). An executable model removes the excuse of vagueness; it does not remove the need to check that the assertion matches the sentence. **Gates:** reference-model **245/245** (240 → 245), simulation 53/53, vector freshness, LMSR doc-table, doc links, PLAN tables, spec-question batches, limit coverage 194 keys / unwired 0 and byte-identical across both spec edits. No Rust changed

**What comes next:** Push the fixes to PR #200 and let CI settle. Then SQ-535's contract half at the next `INTEGRATION_CONTRACT_VERSION` bump; 06 and 09 remain the two documents with no Python at all

## E5 (pass 2 — the collator anchor)

**Reversed pass 1's one declined decision, because the reason for declining did not survive being checked.** Pass 1 refused to touch `collator.comp_epoch` under R-2: unsafe error direction **and** no evidence anchor. R-2's escalation test is a conjunction, and the second clause was a claim about *this repository* that I had generalised into a claim about the world. Searching for the evidence took one round: **Polkadot OpenGov referendum #1870**, passed and executed, funds 38 system-parachain collators at **$250/collator/month** ($307.24 fully loaded) — **211.97 USDC/epoch** at a 21.0-day epoch, making the D-15 seed of 2,000 **9.44×** a governance-approved rate for the same job. Checked the premise that makes the comparison sound rather than assuming it: `OnChargeTransaction = FungibleAdapter<Balances, ()>` drops the imbalance, so Bleavit collators earn **nothing** from fees or tips — the same position that makes Polkadot's system parachains treasury-funded. **Reseeded to the registry minimum 500** (2.36× the anchor), taking the bound rather than a chosen intermediate, because R-2 says derive and *take the lowest lawful value, then show it clears the anchor* is a derivation where 1,000 would be a pick. **Cost base 239,728 → 109,281/yr; runway to the binding 21.26M arming floor 15.6 → 34.3 yr at zero revenue; `V*` 29.1M → 13.2M; quiet epoch 10,200 → 2,700; consistent break-even 7 occupied slots (unreachable, above `epoch.slots` = 5) → 1.** Built the anchor into the model first so the number was machine-checked before anything depended on it (`polkadot_collator_rate_epoch`, `collator_anchor_multiple`; 7 new tests, suite **132**), and found while doing so that **nothing in the workspace pinned the *value* of any genesis PARAM row** — the key set is fixture-asserted, values are not — so a genesis test now pins this one and the shape of its safety argument. Also caught and fixed my own conflation of the two break-even readings (consistent vs fixed-full-slate cost) while restating the tests: 1 slot and 3 slots respectively, and 08 §10 now states both rather than the flattering one. Spec updated across 00 D-15, 13 §1 (+ a normative derivation note), 08 §2.4/§10.1/§10.4–§10.6 and 12 §6.1, superseded rows struck rather than deleted. **Opened on its own branch** (`feat/e5-collator-anchor`) rather than on #198, at the user's instruction that they would merge #198 themselves.

**What comes next:** Land pass 2; E5's remaining items are SQ-529/532/533/534 decisions and phase-gated funding of the eight unsized `ops.*` lines

## E5 (pass 2 — review response)

**The #199 Codex review returned a P1 and it was a real error in my reasoning, not a wording problem.** I had justified seeding `collator.comp_epoch` at its registry floor partly on the 08 §2.4 fail-soft payout: "underfunding leaves the accumulator pending, so the failure mode is a delayed payment, not a lost one". That conflates two different failures. Checked it against the code rather than accepting or dismissing the finding: `collator_compensation` computes the pool **from the configured value**, and a successful payout **kills** the accumulator. So fail-soft catches an underfunded *line* — the treasury cannot pay the pool the value implies, and the claim survives for a retry — and does nothing about an underpriced *row*, which pays out in full and retains no unpaid difference. **Underpricing degrades to collators leaving, not to a delayed payment.** The claim was load-bearing and appeared in five places (13 §1, 08 §2.4, 00 D-15, the constitution-core genesis comment, a model test docstring); all five now state the distinction explicitly rather than dropping the clause silently. **What replaces it** is the part of the argument that was always doing the work — the 2.36× margin — plus a **normative pre-arming gate**: before the bonded permissionless collator set is armed, the rate MUST be re-verified against operator quotes for *this* chain. That is where the anchor's known weakness bites (referendum #1870's operators already run Polkadot infrastructure, so they quote a **marginal** cost), and 12 §6.1 schedules that transition at **Phase 4+**, so the gate is owed early rather than eventually. Filed **SQ-537** for the residue the P1 exposed: no 12 §6.3 alert row covers collator-set attrition, so the *reactive* half of the safety argument has no detector and would surface as a liveness incident after the fact. Also took the two P2s, both self-inflicted by this PR: my new Verification-log row reused **V-40** (already the try-runtime-cli check) and is renumbered **V-61**, and §10.4's cross-reference still sent readers to the superseded 7-and-3 break-even pair.

**What comes next:** Re-verify and push; E5's remainder is SQ-529/532/533/534 and the phase-gated `ops.*` lines

## E5 (pass 2 — stress-testing my own headline)

**Checked whether SQ-536's headline survives the collator growth the specification itself mandates. It does not, and 08 §10.5 now says so.** The 34.3-year zero-revenue runway to the binding 21.26M arming floor is stated at the **launch** count of 5. 12 §6.1 mandates **8–12** bonded permissionless collators from Phase 4+, and because SQ-536 put `collator.comp_epoch` on its registry floor the only remaining lever on that line is the **count** — a liveness posture, not an economics choice. At zero revenue the runway is 34.3 yr at 5, 27.7 at 8, **24.5 at 10 and 22.0 at 12**: the 25-year horizon holds at the launch count and **fails at the mandated ceiling**. Found by asking what my own new claim depended on rather than by review. **The right conclusion is not that the endowment is undersized**, and the table says so explicitly: break-even occupancy stays inside the lawful five-slot slate at *every* mandated count (1 slot at 5 collators, 4 at 12, both at τ = 3), so zero-revenue is the pessimistic bound and what the numbers actually show is that mature operation depends on revenue rather than on the endowment — §10's standing conclusion, now with the count at which it stops being optional. Pinned by a new model test asserting **both** halves, so neither the failure at 10/12 nor the mitigating break-even can be quoted alone. Suite **133**.

**What comes next:** Land with the P1 fix; E5's remainder is SQ-529/533/534 and the phase-gated `ops.*` lines

## E5 (pass 2 — second review round)

**A second P1, the same shape as the first, and it was right.** Round one caught me justifying the floor seed with the fail-soft payout, which protects an underfunded *line* rather than an underpriced *row*. My fix scoped the replacement gate to the **permissionless** transition, reasoning that "the launch set is invulnerable, so the market rate does not bind while it is the whole set". That leans on a mechanism that protects something else in exactly the same way: **invulnerability fixes an account's *selection* status — it exempts it from `CollatorSelection`'s candidate churn — and does not oblige anyone to keep authoring at a rate they will not accept.** An invulnerable operator who stops running the node stops producing blocks like any other. Worse, the launch operators are the **most** likely to be standing up infrastructure for this chain specifically, which is precisely the case the marginal-cost anchor does not cover — so scoping to Phase 4+ would have let genesis ship at 500 with no evidence that the people actually authoring blocks accept it. **The gate is now pre-launch and covers every collator set**, the invulnerable one included, as a release-checklist blocker rather than a best-effort check; the 500 seed stands as the *anchored default the launch set is quoted against*, not as a substitute for a quote. Propagated to 13 §1, 12 §6.1, 08 §2.4, 00 D-15, the genesis code comment and the model test docstring. **Also took the P2 on my own audit trail:** the Decision-log row still listed fail-soft among the load-bearing reasons, contradicting the corrected spec text — so PLAN was preserving a rejected justification as the governing decision, which is the exact R-10 failure. The row now states both withdrawn justifications explicitly rather than quietly dropping them, because what was rejected is the part a later session needs.

**What comes next:** Re-verify and push; then E5's remainder is SQ-529/533/534 and the phase-gated `ops.*` lines

## E6

**Proposal author/funder split, PR #201 (4 commits).** `epoch.submit` required `proposal.proposer == who`, so authoring a payload and locking the 1k-50k USDC class bond had to be the same address -- an intersection gating all ~87 decisions/yr. Signer is now the **funder** (hold lands on the signer, so no extrinsic can enlist a third party's balance); `proposal.proposer` stays the **author**. Spec: 05 §1.5 (new) binds every right to one identity by who bears the loss, 06 §4 re-keys rule 4, 02 goes v17 -> **v18** (trailing `funder`, rule-3 append), 14 TH-16 and 15 §4.2 updated. Code: primitives, epoch-core, pallet-epoch, runtime views; `ProposalBond.proposer` -> `.funder`. **The cap re-keying is a defect closure, not a preference** -- `epoch-core:776` counted the author, safe only while submit forced author == signer, so the split without it lets one funder back 16 free authors and take the whole 64-entry queue. Six tests, all split-identity; the two cap tests are **two-sided mutation-proven** (reverting the predicate fails both). Gates: clippy clean, pallet-epoch 118/118, bleavit-runtime 416/416, benchmarks cfg compiles.

**What comes next:** **E6 is 🔨, not ✅.** Open: the 15 §4.2 runtime-level assertion that the 08 §1.1 reward reaches the author -- `mark_executed` calls `ledger.resolve` so it needs the full qualification fixture, and the one existing e2e fixture is TREASURY with `ask = 0`, reward 0. Build a PARAM fixture or give the Treasury one a non-zero Ask. Also unrun: exhaustive `rust-workspace-gates.sh` (CI on #201 is the first full pass) and `regenerate-weights.py --check --changed`.

## E6 (closeout)

**Rebased #201 onto `48e59f3` and closed every open E6 obligation; E6 is ✅.** Conflicts were both in PLAN.md against E5 pass 2 (#199) — the E5 row and four session-log rows — resolved by taking upstream's and keeping E6's. **The parked reward obligation needed a PARAM fixture, and that was the whole blocker:** TREASURY/CODE pay `min(0.05 %·Ask, 25k)`, which is 0 at the `ask = 0` every existing end-to-end fixture carries, so the earlier attempt could not observe a payout at all and was deleted rather than contorted; PARAM's flat `trs.reward.param` is `ask`-independent. Execution settles reward and bond release in one transaction, so the one fixture pins the whole 05 §1.5 split by exact equality — author credited the reward and only the reward, funder the bond and only the bond. Mutation-proven. **Two defects found by running gates the earlier commits had not.** (1) The `runtime-benchmarks` build was **red**: both `Proposal` constructions in `configs.rs` are behind that cfg, so `clippy --workspace --all-targets` never compiled them and the earlier "benchmarks cfg compiles" claim was not evidenced by any command run — #201 as first pushed would have failed CI's Rust workspace job. (2) try-state never bound bond custody to the funder; the split made a mis-keyed bond representable and every other assertion still passes on one. Added, plus the general rule in 15 §4.1. Four fixtures setting `proposer` alone were building records `submit` cannot produce. **Weights: real drift, +2,080 B `worst_case.proof_size` on 11 functions = 65 × 32**, one extra `AccountId32` propagating through `MaxEncodedLen`. Regenerated at the committed **50×20**, not the 2×1 check fidelity — at 2×1 the component fit collapses (`settle_cohort` read 2.5 × 10¹⁹) and committing that would have destroyed a linear term the growth-only gate cannot see. Also added the 06 §4 slash-incidence test, which needed a new mock recorder: `slash_to_insurance` names no account, so incidence is only visible as the shortfall in what the funder is released.

**What comes next:** Gates: fmt; clippy `--workspace --all-targets -D warnings`; pallet-epoch **120/120**; bleavit-runtime **417/417**; release `runtime-benchmarks` build exit 0; weight purity, storage bounds, limit coverage (`unwired = 0`), PLAN tables, batch index. Exhaustive `rust-workspace-gates.sh` not run locally — CI on #201 is that pass.

## E6 (weights, second pass)

**The first weight commit was incomplete and would have failed CI's benchmark-smoke gate.** Regenerating `pallet_epoch` alone was the obvious move and the wrong one: `Epoch::Proposals`/`IntakeProposals` are read by the benchmarks of `pallet_attestor`, `pallet_execution_guard`, `pallet_guardian` and `pallet_market` as well, and each of those files records the item's `max_size` in its own proof annotations. All five carried **326**; the widened record makes it **358**. **Why `--changed` structurally cannot find this:** it selects pallets whose *local source* moved, and none of the four had any source change — the driver was a type in `futarchy-primitives`, one dependency edge away. A shared-type change moves proof sizes in every pallet that *reads* the affected storage, so the pallet set must be derived from the storage item rather than from the diff. **The cheap decisive check is not re-running benchmarks:** the committed `max_size: Some(N)` annotations encode `MaxEncodedLen` directly, so one grep across the weight files yields the complete affected set — and confirms exhaustiveness, since exactly two storage items hold a `Proposal` and both live in `pallet-epoch`. Regenerated all four at their committed 50×20; two carried genuinely stale worst cases (`pallet_attestor::remove_for_cause` +1.1 %, `pallet_execution_guard::ratify` +0.8 % and `qualify_recovery_image`), two moved only the annotation. Also confirmed the two separate workspaces are unaffected: `keeper/` and `fuzz/` both depend on `futarchy-primitives` but neither constructs or names a bare `Proposal`.

**What comes next:** 51 annotations across five files now read 358, none left at 326; weight purity (366 functions, 2 justified overrides) and storage-bound gates pass; PLAN tables green.

## E6 (weights, third pass — the gate I had not run)

**`check-weight-regression.py` showed the PR would fail CI, and my own weight commits were the cause.** It is not part of `rust-workspace-gates.sh`, so a green workspace gate says nothing about it. **Two distinct errors, both mine.** (a) Two of the four extra pallets should never have been regenerated: `pallet_guardian` and `pallet_market` had **zero** proof-size change, only a stale `max_size` *comment*, and rewriting them produced +54 % / +65 % `ref_time` regressions on code this branch does not touch. The rule is narrower than "regenerate the readers" — regenerate when a **charged dimension** moves; a stale annotation over unchanged charges is not that. (b) The remaining regressions (`epoch::void_cohort` +51 %, `execution_guard::execute` +58 %) were **measurement error**: same host and CPU as the committed values, but a full 32-pallet benchmark sweep had been left running concurrently with the regeneration (found at 204 % CPU, 58 min in). Killed it and re-measured idle — the gate went to **zero regressions** over 364 shared functions, storage dimensions identical throughout because those are load-independent and `ref_time` is not. **Writing acknowledgements was considered and rejected:** the repo has that pattern and even a precedent entry citing re-measurement variance, but an acknowledgement is for a change one understands and intends; these were artifacts of how the tool was run, and a 58 % inflated `ref_time` would permanently overcharge a real dispatch while the prose justifying it made the weight set less trustworthy. **Never benchmark under concurrent load.** E6 was also flipped ✅ before this gate ran — premature, since the milestone's own Verify column names weight regeneration; reverted to 🔨 and restored only once the gate passed.

**What comes next:** All five files re-measured idle and coherent: 51 `max_size` annotations at 358, none stale; weight regression, purity, storage-bound and PLAN-table gates green. Remaining: CI on #201 (`Benchmark smoke` + `Rust workspace`), then mark ready for review.

## E6 (contract-surface sites the bump missed)

**CI's `Reference model` job failed: `tools/release/tests` asserted `17 != 18`.** 02 §13's v17 entry claims "exactly **one** literal now exists" for the contract version — true of the Rust workspace, false of the **release surface manifest**, which is outside it and carries the surface in three places: the declared `integration_contract_version`, the SCALE-encoded `constant.identity.contract_version` layout (`0x11000000` → `0x12000000`), and the `storage.epoch.proposals` layout string, which spells out **every field of `Proposal`** and so needed the trailing `funder`. All three fixed derivationally — version read from the Rust constant, bytes computed from the version, field position taken from the verified declaration order — rather than typed by hand. **Only two of the three are test-covered:** the layout string is checked against a *booted node* at release time, so a wrong field there fails no local gate, which is exactly why it was derived rather than eyeballed. Two process points for the next bump: 02 §13's one-literal claim needs the qualifier "in the Rust workspace", and the gate that catches this lives in the **Reference model** CI job, a name that gives no hint it gates the integration contract.

**What comes next:** All seven tooling suites green (deploy 96, reference-model 12, release 86, phase-gates 74, monitoring 107, ci 143, env), reference model 253, vector freshness, doc table and env validator green. Remaining: CI `Benchmark smoke` + `Rust workspace` on #201, then mark ready for review.

## E6 (PoV pin; and the pattern behind all three CI…

E6 (PoV pin; and the pattern behind all three CI failures)

**`Rust workspace` failed on `pov_budgets::decide_and_settle_cohort_pov_pinned_below_map_scaling`:** `settle_cohort(5)` proof size 727,942 against a pinned 725,862. **+2,080 — the same 65 × 32** seen throughout: the call touches `Epoch::Proposals` at r:33 w:32 and the E6 `funder` field adds 32 B to the estimator's per-key envelope on each access. The pin is derived from `get_dispatch_info()`, so it tracks the generated weights. Updated, with the reconciliation written into the doc comment in that file's existing convention of narrating each move's cause; `decide` is genuinely unmoved because its value is dominated by the collator-compensation term rather than its own proposal reads. Legitimate to update rather than masking: the record really grew, the test exists to catch *unexplained* drift, and both real ceilings (`decide` ≤ 1024 KiB, `settle_cohort(MAX)` ≤ 1280 KiB) still pass with wide margin. **The process error, recorded because it caused all three CI failures and not just this one:** weights were regenerated four times and the suite consuming them was never re-run — the earlier "417/417" predates the first regeneration. Each failure was a *consumer* one dependency edge away from the change: the PoV pin consumes generated weights, the release manifest consumes the contract constant, four pallets' benchmarks consume `Epoch::Proposals`. Widening a shared type reaches further than its diff, and targeted per-package gates are structurally blind to that.

**What comes next:** Runtime suite back to **417/417**. `tools/ci/rust-workspace-gates.sh` is running **in full** — the right answer to three consumer-side misses is the exhaustive gate, not a fourth targeted re-run. **In-flight at this write:** `pov_budgets.rs` is edited and uncommitted alongside this PLAN entry; nothing else is dirty at that write. **`tools/ci/rust-workspace-gates.sh` then passed in full, exit 0** — workspace tests, the release / `runtime-benchmarks` / `try-runtime` builds and try-runtime suite, `no_std`, runtime-profile matrix, weight purity and storage bounds, limit coverage, keeper (85) and reference model (253). Landed as `1a75ef4`. Next: CI on #201, then mark ready for review.

## supply chain (not an E6 regression)

**CI's `Supply chain` job went red on `RUSTSEC-2026-0222`, published the same day** — `wasmtime 36.0.12`, "Stores can mix up type indices between engines", severity 3.8 (low). **Not caused by this branch:** `main` carries the identical `wasmtime 36.0.12` and this branch changes no dependency file, so `main` is equally red; the advisory DB is fetched live, so a gate that passed an hour earlier can fail on an unchanged tree. Fixed rather than waived, because unlike every entry in `.cargo/audit.toml` this one has a **semver-reachable** patch: `sc-executor-wasmtime 0.47.0` takes `^36`, so `cargo update -p wasmtime --precise 36.0.13` is a lockfile-only move. Verified confined — the diff touches only the `wasmtime*` crates and `winch-codegen`, no polkadot-sdk crate moves, and both workspaces still satisfy `cargo metadata --locked`. A waiver would have been the wrong instrument: `.cargo/audit.toml`'s convention is that every entry states the pin that *forces* it, and nothing forces this one.

**What comes next:** `supply-chain-gates.sh` green locally (4 allowed unmaintained/unsound warnings, the yamux GHSA waiver still applying). R-12 requires fresh evidence for a dependency change, so the exhaustive `rust-workspace-gates.sh` is re-running over the bumped lockfile.

## E6 (close-out)

**All 20 CI jobs green; PR #201 marked ready for review, `mergeStateStatus: CLEAN`.** Longest legs: `Benchmark smoke` 1h19m, `Rust workspace` 1h08m, `Model checking (TLC)` 1h31m, `Analyze (rust)` 30m. The exhaustive `rust-workspace-gates.sh` also re-ran locally over the bumped lockfile at exit 0, so the dependency change carries its own fresh evidence per R-12. **Four CI failures came out of this branch and they divide cleanly.** Three were mine and shared one shape — a *consumer* one dependency edge away from the change: the PoV pin consumes generated weights, the release surface manifest consumes the contract constant, four other pallets' benchmarks consume `Epoch::Proposals`. Widening a shared type reaches further than its diff, and per-package verification is structurally blind to that; the remedy was the exhaustive gate, which should have preceded the first push rather than following three red runs. The fourth was not mine (`RUSTSEC-2026-0222`, same-day, `main` equally affected). **Two judgement calls taken against the cheaper option, both recorded above in full:** weights re-measured on an idle machine rather than acknowledging CPU-contention artifacts (the repo has that pattern and it would have passed), and wasmtime patched rather than waived (a waiver in `.cargo/audit.toml` documents an *unreachable* fix; this one is reachable).

**What comes next:** E6 ✅. #201 ready for review and mergeable. Next session: merge #201, then the next pending milestone whose *Depends* are ✅.

## tooling (carried into #201 at user request)

**`rust-toolchain.toml` gains the `rust-analyzer` component.** Authored in another session and carried here rather than discarded, at the user's explicit instruction (2026-07-31). Independent of E6. Verified before accepting: `main` does not have it; the identical change exists as `4154b3d` on the unmerged `explainer` branch only; `rust-analyzer` is a standard rustup component available (and already installed) for the pinned 1.89.0, so the CI toolchain step cannot fail on it; and `fuzz/rust-toolchain.toml` is a separate nightly pin that this does not touch. Channel and targets unchanged, so no rebuild and no effect on the `=`-exact SDK pins.

**What comes next:** Committed on this branch as its own commit so it can be reverted independently of the milestone.

## — (dev tooling, unplanned)

Editor-only fix landed here at the user's request rather than on `feat/e7-coretime-escalation`, where it was first made: added `rust-analyzer` to `rust-toolchain.toml`'s `components` so VS Code stops falling back to its bundled server (see Unplanned changes). Reverted on the E7 branch, so this worktree is now its only home. **Written by a different session while E6's exhaustive gate was running** — the toolchain line is the only file it touched besides this log, and the component was already installed locally, so no cargo/rustup work was triggered.

**What comes next:** Commit the toolchain line as its own `chore`, kept out of the E6 pin-fix commit

## E7 (SQ-540(a) — built, then withdrawn)

**Built the ORACLE-line meter, then deleted it on reading the spec section that owns it.** The sweep ranked the unmetered ORACLE rebate as SQ-540's clearest omission and its largest available reduction, reasoning by analogy from `keeper.budget_epoch`. I implemented it — `oracle_meter` mirroring the keeper meter in `futarchy-treasury-core`, Low/Exhausted events, the pallet field, the `TreasuryParams` seam — and it compiled. It is now stashed rather than shipped, because **08 §1.1's normative *Where per-line budgets bind* already answers the question**: each line's per-epoch budget is enforced by its consuming mechanism, and "lines carrying a `[VERIFY]`-gated budget in 13 §1 are **allocation-only** until Phase-2/3 ops sizing". §10.6 puts the `ORACLE` line in the eight "not sizeable from any evidence in this repository". So the funded balance *is* the specified bound; an `oracle.budget` value would have been invented against an explicit refusal to size it (R-2), and it buys **no cost reduction at all** — governance controls refills either way, so a rate cap moves when money leaves, not how much. The analogy to `keeper.budget_epoch` was the whole error: I checked that a counterpart key was missing and not why it was missing. Recorded as a ruling in SQ-540 rather than a silent revert, since the next session would otherwise re-derive the same wrong finding from the same sweep.

**What comes next:** Take the actual runway constraint: coretime

## E7 (SQ-541 — the renewal-timing result)

**My own SQ-538 arithmetic was wrong, and correcting it produced the largest cost reduction found so far: +18.3 years of runway and 62× on cumulative coretime spend, from a scheduling choice.** SQ-538 modelled `broker.renew` as compounding at the per-period cap without bound and published 14.3 years on it. Checked against `pallet-broker` 0.28.0 — **the version this repo's own `Cargo.lock` pins**, so it was checkable evidence sitting unread (V-62) — the rule is `min(leadin_factor(t)·end_price, max(prev·(1+bump), end_price))` with `leadin_factor` decaying **100 → 1** across the leadin. Three things follow that the first pass denied: the ratchet **saturates** (the `min` clamps to the open market); it tracks the market **down** as well as up; and **the saturation multiple is chosen by when the renewal is submitted**. The interlude — the obvious moment, since only renewals may take cores there — ratchets to **100× the market floor**; the end of the leadin pays `end_price` exactly, every period, and *un-ratchets* a saturated price in one call. At §10.1's base with a 4,000 USDC/yr floor: **14.7 → 33.1 years** zero-revenue runway, **6,252,022 → 100,000 USDC** over 25 years — the difference between missing and meeting the 25-year goal, with no parameter touched. Landed as spec (08 §10.1 retracts its "the one line that cannot be reduced" claim; 09 §4 states the operating rule) + procedure (RB-TREASURY) + 7 model tests whose leadin curve is asserted against the pallet's own unit test. **Deliberately no on-chain enforcement**: escalation already surfaces as an `ops.coretime` refill request, and the one failure this call may not risk is `SoldOut`, so the rule is "as late as the observed core margin allows, never later" and a missed renewal outranks any price argument. Suites: reference-model **264** (was 258), runbook binding 13/21 green.

**What comes next:** Rust gates for the untouched-code claim, then SQ-540(b) — the reserve-haircut carve-out divergence, now the largest open item

## E7 (SQ-540(b) — reserve-haircut carve-outs)

**The sweep's third finding, and like its first it did not survive contact with the code — but this time the code was right and the spec was wrong.** 08 §1.2 says "exactly two bounded maintenance debits" survive the reserve-health flag; the implementation runs collator compensation and proposer rewards through it as well. The sweep called that a scope-A divergence to be closed "either way". **Neither** direction it offered was correct. Collator compensation is a real third carve-out on D-9's own logic — no pay, no authors, no blocks, no reserve probe, and the freeze becomes self-sealing — bounded to its own `COLLATOR` pot. Proposer rewards are not a carve-out at all but a crystallized obligation, in the class §1.2 already exempts as existing stream claims. What decided it was checking *deferred vs lost*, the same question that caught two P1s on the collator work: `mark_executed` fires `let _ = do_proposer_reward(...)` once and drops the result deliberately, so a haircut gate would **destroy** the reward rather than defer it — permanent forfeiture over a condition the proposer neither caused nor can influence. "Making the code match the spec" would have been the unsafe move. 08 §1.2 now carries three carve-outs with the wedge rationale and a separate paragraph on why rewards are not one; pinned by a regression that asserts the flag blocks discretionary `spend` first, so the two positives cannot pass vacuously. `futarchy-treasury-core` 31 tests, fmt + clippy clean.

**What comes next:** E7 residue: SQ-540 (c) community-VIT bound, (d) POL rerun row, (e) the XCM `TakeRevenue` sink via `deposit_asset`

## E7 (joining SQ-541 to SQ-536's qualification)

**Caught my own headline over-claiming by composition, before review did.** SQ-541 says late coretime renewal takes the runway 14.7 → 33.1 years and "meets the 25-year goal". SQ-536's own stress test says the goal already fails at 10 and 12 collators, which 12 §6.1 mandates from Phase 4+. Both were computed at the **launch** count of 5, both are true there, and read together they claim something neither establishes. Joined and pinned: at 4,000 USDC/yr coretime the cheap policy gives **33.1 / 26.9 / 23.9 / 21.5** years at 5 / 8 / 10 / 12 collators — so it clears 25 at the launch count and at 8, and **does not clear it at 10 or 12**. The expensive policy clears at no mandated count (14.7 → 13.1), so renewal timing is never what causes the miss. Also pinned the shape, which is the non-obvious part: **what the policy buys shrinks as `C` grows** (+18.3 yr at 5, +8.4 at 12), because a larger constant base ends the runway before the ratchet has time to saturate — the test asserts that erosion is monotone rather than asserting four independent numbers. 08 §10.1 carries the joint table; §10.5's standing conclusion is reinforced, not replaced — at the mandated count this depends on revenue, and break-even stays inside the lawful five-slot slate throughout. Suite **265**.

**What comes next:** E7 residue: SQ-540 (c) community-VIT bound, (d) POL rerun row, (e) the XCM `TakeRevenue` sink via `deposit_asset`

## E7 (SQ-540(e) — quantifying the discarded XCM fee)

**The chain charges for every inbound XCM message and throws the payment away, and it is worth the entire cost base at ~206 messages/day.** `Trader = GovernedWeightTrader<ConstitutionTraderRates, ()>` and `TakeRevenue for ()` drops the collected assets on `Drop`. Quantified rather than asserted, and every input is in-repo: `UnitWeightCost = (10⁹ ref-time, 64 KiB proof)` per instruction under `FixedWeightBounds`, 13 §1 rates of 50 USDC/s and 5 USDC/MiB ⇒ **0.3625 USDC/instruction**, **1.45 USDC** per minimal four-instruction reserve transfer. Break-even **206/day** (launch base) and **321/day** (12-collator ceiling); 100/day is ≈ 48 %. Stated as a break-even, never as revenue, because volume is the one input this repo cannot supply. Landed in 08 §10.2 as the third instrument with both caveats: the flat 64 KiB proof term is **86 %** of the fee, so this is a property of the *weigher* rather than of measured resource use, and the model asserts that split so benchmarked bounds cannot silently invalidate the paragraph. **Not implemented, and the writeup says exactly why the obvious implementation is wrong** — tracing where the money would land found the requirement my reverted `mint_into` attempt missed twice over: `deposit_asset` alone puts the fee in the custody pot, where it never reaches `main_usdc`, **never appears in NAV**, and trips the §6.3 drift alarm; it must be paired with the existing `credit_main`/`PendingMainCredit` seam. Suite **269**.

**What comes next:** Implement (e) as its own unit: `deposit_asset` + `credit_main` for USDC, custody-only for DOT, with runtime tests and the full Rust gate

## E7 (SQ-540(e) — implemented)

**The XCM execution fee is now treasury revenue instead of litter, and the test that blocked the first attempt turned out to be the one worth listening to.** Shipped `FeesToTreasury` in `bleavit-xcm` and bound it through `XcmConfig`'s own generic `Assets`, so the capped production executor and the recovery executor each route through the transactor they already use rather than through a hardcoded one. USDC is deposited **and** recognized via the existing `credit_main`/`PendingMainCredit` seam; DOT is deposited only, because 08 §1.2 marks DOT at 0 in NAV and crediting it would inflate NAV by exactly the term the treasury refuses to count. Recognition strictly follows custody, so the path cannot make the ledger claim more than the pot holds (G-1, I-33), and a failed deposit degrades to the old discarding behaviour rather than to an unbacked claim. **The holding is deposited whole**: `AssetsInHolding` carries real `ImbalanceAccounting` objects and `into_assets_iter` drops them, so filtering per asset would have destroyed the value being captured — the USDC portion is measured with the borrowing iterator first. **`production_xcm_remote_keyed_trap_is_recoverable_only_by_asset_hub_claim` failed again**, as it did on the reverted attempt, and this time I read it instead of routing around it: its assertion that "only paid recovery fees are removed" encoded the burn. Two messages in that test pay fees, which is why my first baseline was exactly half the delta. It now asserts the stronger thing — issuance preserves the whole reclaimed amount, the fee reaches treasury custody, **and** it is recognized as internal credit. Also corrected 08 §10.2, which I had written hours earlier saying the fee "is currently discarded": that is now false, and the paragraph records what discarding actually meant (not destroyed value, but value stranded off-ledger as surplus reserve backing, invisible to `nav()`). Gates: workspace `cargo test` green (**417** runtime, **62** xcm), fmt + workspace clippy clean.

**What comes next:** E7 residue: SQ-540 (c) community-VIT bound and (d) POL rerun row

## E7 (SQ-540 (c) and (d) — closing the sweep)

**The last two sweep findings, and the pattern held to the end: one was already-documented design, one was half-right and the surviving half was worth fixing.** **(c)** the 250M VIT community allocation "escaping every treasury control" — not a defect. 08 §2.2 already names `create_community_schedule` "the one bounded exception to the ordinary treasury-outflow origin" and lists its control set; the code implements all six bounds exactly, in the safe order (vested transfer *before* decrementing the remaining allocation). The sweep made the same category error as (a): NAV rolling caps are USDC-outflow controls, VIT is marked 0 in NAV **by design**, so this allocation carries a finite-pot + lifetime-count bound rather than a rate bound. Recorded one non-obvious property the spec states only implicitly — a single schedule may take the entire remaining allocation, since `amount ≤ remaining` is the only size bound. **(d)** rerun top-ups uncharged against `pol.budget_epoch` — also already normative (08 §4.4, with its structural-2× argument). **But its second half was real**: §10.1's divergence row is the no-rerun corner and nothing said so, so the published `C` understated by up to its own divergence row. Divergence is linear in `b` and T13 doubles `b`, so a fully rerun slate is **+18,830/yr = +17.2 % of `C`**, runway 34.3 → **29.2 yr**; joint worst corner (12 collators + all reruns) is `C` = 188,986 and **19.4 yr**. Now modelled, labelled in §10.1, and stacked honestly — it does not reverse §10.5's conclusion, and the coretime-timing term stays larger than the rerun term even in that corner. **SQ-540 is closed: (a) ruled, (b) ruled + spec fixed, (c) ruled, (d) half-ruled + modelled, (e) implemented.** Suite **273**.

**What comes next:** E7 is clear; next is whatever PLAN's next pending milestone selects

## E7 (CI concurrency)

**Fixed the thing that made me cancel CI runs by hand three times in one session.** `ci.yml` had no `concurrency` group, so every push to a branch queued another full workspace build alongside the ones already in flight — three runs competing for the same runners, two of them answering a question about a tree nobody would merge. Added `group: ${{ github.workflow }}-${{ github.ref }}` with `cancel-in-progress` **conditioned on the ref**, so PR branches supersede and `main` never does: on `main` each push is a distinct commit whose run is the record for that commit, not a draft. Job ids are untouched, which matters because branch protection pins `benchmark-smoke` by id (R-12). Noticed in an earlier session and left as a proposal; implemented now because the manual cancellations were the third repetition.

**What comes next:** —

## E7 (closing the loop on the runway goal)

**Connected the E7 wiring to the goal it was for, and the answer is smaller than it looks.** The zero-revenue reading fails at the 12 §6.1 mandated ceiling — 21.5 yr at 12 collators under the cheap coretime policy, 19.4 with a fully rerun slate — and that pessimistic bound stands. But the XCM instrument E7 just stopped discarding closes it at **50 inbound messages/day (25.4 yr)** and makes the endowment **never draw down at all at 321/day**, its own §10.2 break-even. Landed in 08 §10.5 with the table and with both disclaimers attached: it is *required traffic*, not a forecast (volume is market behaviour the protocol does not control, and belongs beside `τ`), and it is computed with instruments **A and B set to zero**, so any positive trading revenue lowers the message rate needed — the terms are additive, `R_total = R(H) + R_C`. Pinned by `test_the_captured_fee_closes_the_gap_at_the_mandated_collator_count`, which asserts the 25-year clearance, the indefinite case and monotonicity in traffic. Suite **274**.

**What comes next:** —

## E7 (living-document sync, R-3)

**Two operating facts this session changed or learned, both recorded where the next session will hit them.** (1) `sweep.yml` got the same `concurrency` group as `ci.yml` — it is a **90-minute** job, so three pushes could hold three runners for an afternoon answering questions about trees nobody will merge. AGENTS.md R-12 now states the behaviour, because a run showing `cancelled` right after you push is now *expected* and would otherwise read as a failure; `main` still never cancels, since there each run is the record for its own commit. (2) The `libclang` prerequisite grew a caveat: `/tmp` is swept on this box, so a symlink dir created early in a session can vanish before the exhaustive gate runs, and it surfaces as a `clang-sys` panic that reads like a toolchain problem. Also recorded the two traps beside it — point at a real `libclang-14.so.1`, and verify with `ls -lL`, which follows the link, not `ls -l`, which happily shows a dangling one. Cost this session real minutes; costs the next one none.

**What comes next:** —

## E7 (derived-pack and contract-version drift, R-3)

**Checked the R-3 obligations my spec edits triggered and found three drifts, none of them mine — which is why they were still there.** AGENTS.md forbids leaving `docs/design/claude-design-kit/` stale after a spec change, and a prior Codex review had already caught it once. (1) Both **VERBATIM copies were a full milestone behind**: docs 10/11 moved on 2026-07-29 with E1 and the kit last moved 2026-07-28, so the pack was missing real frontend-*normative* content — the redemption-fee display rules, the `NavView`-unchanged ruling, `split_gate`/`merge_gate`/`redeem_gate` in the S4 call set, and the two new revenue cranks. Regenerated both and asserted the bodies are now byte-identical to their sources. (2) The kit's **parameter distillation had no `ledger.redeem_fee` row at all**, while the copy it ships now *mandates* the frontend read that key — added, with the part that is easy to get backwards spelled out: `net` is the headline on the five charged calls, and `redeem`/`redeem_void`/every `merge*` are exempt and must still read as par. (3) AGENTS.md claimed **contract v13** in two places against an actual `INTEGRATION_CONTRACT_VERSION = 17`. **Caught one of my own errors on the way**: my first regeneration stamped the headers "contract v32", because the regex `INTEGRATION_CONTRACT_VERSION[^0-9]*\K[0-9]+` matches the `32` in `u32` before it reaches the value — fixed to v17 and re-verified.

**What comes next:** —

## E7 (mutation-testing my own coretime tests)

**Wrote property tests for the coretime transcription, claimed in their docstring that they would catch a swapped `min`/`max`, then checked — and they did not.** Swapping the two clamps degenerates `coretime_renewal_price` to "always charge `sale_price`", and that degenerate form satisfies **every bound I had written**: `price ≤ sale_price` holds with equality, `price ≥ end_price` holds because the leadin factor is ≥ 1, and monotonicity-in-`prev` holds vacuously because a constant list is sorted. All three of my new tests passed against the mutant; what actually killed it was the *point-value* interlude test asserting `path[0] == 1.03`. So the properties were decoration and the docstring was an unchecked claim — the exact failure mode the repo's TLA witness configs exist to prevent, reproduced by me one file over. **Replaced with an anti-vacuity test** that requires each clamp to be the *binding* one somewhere: the ratchet must put the price **strictly below** the sale price and **strictly increasing** in `prev` early in the leadin (killing "always `sale_price`"), and strictly above `end_price` (killing "always `end_price`"), while the market clamp must make `prev` stop mattering at the ceiling. Re-verified by mutation: the bounds test still passes against the mutant — and now *says so* — while the anti-vacuity test fails it. Suite **277**.

**What comes next:** —

## E7 (SQ-542 — the next lever, refused on evidence)

**Decomposed `C` after E7's cuts to find the next lever, found one worth 40 % of the cost base, and the arithmetic says do not pull it.** At the 08 §10.1 PARAM ceiling `trs.proposer_reward` is **43,482 USDC/yr — 39.8 %**, exactly level with `ops.collators` and larger than every other row combined; and like `collator.comp_epoch` before SQ-536 it has **no derivation anywhere in the doc set** — three appearances, all of them restatements of the number. The obvious move is a cut. **The evidence points the other way.** 08 §7.1 refunds a rejected proposal *in full* ("rejection is information"), so the slash attaches only to non-decision-grade outcomes, and EV per submission is `reward·P(adopt) − 0.1·bond·P(non-DG)`. At the committed Phase-0 rates that is **−47.8 USDC** against a break-even adopt rate of **11.89 %** — and a cut to the 13 §1 hard-min would put break-even at **118.9 %**, unprofitable even if every proposal were adopted. So the unsafe direction here is **downward**, which is the opposite of the collator case. Filed as SQ-542 rather than acted on: R-2's escalation test is a conjunction and both halves hold. **Also caught myself writing a tautological assertion** (`x*10 == x*10`) in the test that was supposed to quantify the counterfactual — one hour after committing a fix for exactly that failure mode — replaced with the hard-min break-even, which is decisive rather than decorative. Suite **280**.

**What comes next:** —

## Security fix (oracle)

**Two confirmed vulnerabilities fixed in one PR; contract v19.** Found by a whole-repository security review of `main` (`055cf54`; rebased onto `48e59f3`) run as five scoped agents plus an adversarial refutation pass per candidate — 2 of 6 candidates survived at confidence ≥ 8, and the other four are recorded as refuted with reasons. **HIGH — self-challenge:** `challenge` never checked that the challenger differs from the round's reporter, and a reporter default settled the challenger's counter-value forward with `flagged: false`, bypassing the watchtower quorum. One account could `utility.batch_all([report, challenge])` (both leaves are `CallDomain::Public`, so the wrapper is admissible and the front-running window is zero), lock out every honest challenger with `AlreadyChallenged`, and — `adjudicate` being unreachable below `round_cap` — have its own false value land unflagged in `C_attested` and thus `W`. Net against 07 §6.3's worked example: **+102,000 instead of the intended −90,000, at 8.6 % of the required ladder.** Fixed on the **value** side (a default now takes the §10 neutral carry-last/flagged path at every round) because the money side is closed to repair by §5.3's own no-unfunded-debit rule; round-1 defaults additionally pay no bounty. **MEDIUM — offense-ladder reset:** `deregister_reporter` + `register_reporter` erased 07 §3's ladder for two extrinsics' cost, making the second-offense slash and third-offense ejection unreachable; a retained bounded `ReporterRecords` store now carries the count across exit and makes ejection permanent, mirroring the attestor's SQ-262 fix. Deregistration is also now blocked while the account is a live round's *challenger* — a third defect found on the way. **Verification:** `oracle-core` 37→52 tests, `pallet-oracle` 120→126, all green; fmt + clippy `-D warnings` clean on both crates; release-tooling suite 86 green; plan-tables, spec-question-batches, limit-coverage (unwired = 0, no registry churn) and doc-links all green. The pre-existing test `quorum_finalizes_and_challenge_supersedes_ack_requirement` **failed against the fix while asserting `ChallengerDefault`** — that is the captured baseline, and it was rewritten deliberately rather than silently. **Reported honestly, not absorbed:** `cargo test -p pallet-oracle --features runtime-benchmarks` has a pre-existing failure (`bench_report`, "fixture must exercise the value-scaled path above the floor knee") that reproduces identically on unmodified `HEAD`; this PR adds no new failures (137 passed / 1 failed both ways). **Also fixed in passing:** `surface-manifest.json` pinned a stale four-variant `SettlePath` on the `ComponentValues` storage row while the event row pinned five — a release-time metadata binding would have failed on it; both are now test-pinned.

**What comes next:** Land the regenerated `pallet_oracle` weights (`load()` now reads `ReporterRecords` on every dispatch, so all 12 functions move; `crank_round_close`'s fixture was also reseeded onto the previously-unmeasured default branch, which is now the heavier arm) and run the one exhaustive `rust-workspace-gates.sh` on the coherent post-rebase state. Weights are now regenerated and the `crank_round_close` ref_time regression is acknowledged and value-pinned; PR #203 is open.
