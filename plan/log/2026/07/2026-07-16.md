# Session log — 2026-07-16

## A7 (PR #55 — rebase onto post-#54 main + Codex-connector P1/P2)

Rebased `feat/a7-pallet-welfare` onto post-#54 `main` (A5/#52 oracle-v3, A3/#53 market, A6/#54 registry had landed after A7's 53abb11 base). `Cargo.lock` auto-merged; only PLAN.md conflicted (Current focus + milestone table + Spec questions + Decision/Session logs). **Renumbered the A7 spec questions SQ-58…61 → SQ-77…80** (+ the `// SQ-58`/`// SQ-59` code tags → `// SQ-80`/`// SQ-79`) to clear the collision with main's A5 (58–63)/A3 (64–67)/A6 (68–76). **Fixed both `chatgpt-codex-connector` comments on PR #55:** **(P1)** `record_snapshot`/`record_daily_gate` accepted a caller-supplied `epoch` with no clock check, so an early/future crank could lock a wrong `W` or consume the bounded 20-entry window before the real counters exist (05 §4.6 winsorizes over *finalized* epochs). **TRUE, fixed:** a pallet `ensure!(epoch < CurrentEpoch, EpochNotFinalized)` guard on both cranks (kept in the shell, not the pure core — it is a live-clock admission policy, not a replayable state-machine rule, rule 9); +2 regression tests, and the ext-builder clock raised to a finalized horizon so the shell≡core differential neutralizes the pallet-only guard. **(P2)** genesis specs were forced to `activation ≥ current+2` = 2 (registered at the epoch-0 sentinel), so `record_snapshot(1, genesis)` was rejected and W₁ was uncomputable — contradicting 05 §4.6's "computable from epoch 1" cold start. **TRUE, fixed:** `register_metric_spec` relaxes the lead to `activation ≥ 1` at genesis (`current_epoch == 0`; no in-flight cohorts to protect, I-16), and the mock genesis + differential seed now declare `activation = 1`; +1 core test (genesis-active-from-1 + post-genesis-lead kept) + 1 pallet test (cold-start) + the before-activation test re-pointed at a post-genesis version. **SQ-81/82 raised** (B1a tick increment-then-record ordering + whole-epoch daily granularity; epoch-0 genesis-sentinel contract). Gates re-run green: fmt · clippy `-D warnings` · `cargo test --workspace` (**498**; welfare **32 pallet + 18 core**; 35 w/ benchmarks) · runtime-benchmarks · try-runtime · no_std · reference-model 21/21 + vectors + doc-table. A7 stays ✅.

**What comes next:** Force-push #55 + reply to the two Codex comments; then A8/A11 per Track-A DoD, then B1a

## A8 + A11 (in progress — NOT yet ✅)

Session implementing **both A8 (`pallet-epoch`) and A11 (`pallet-execution-guard`) production FRAME shells over their frame-free cores in one run**, per explicit user directive ("Implement milestone A8 and then milestone A11 by using codex subagents intelligently … Ignore the instructions to not be allowed to complete two milestones in one run"). Synced onto post-#55 `main` (A7 landed); working branch `feat/a8-a11-epoch-execution-guard`. Deep recon complete (epoch-core / execution-guard-core public APIs, the shell-over-core pattern from welfare/registry/guardian/treasury, and the 05/09/02/13/15 spec surfaces). Orchestration = **Codex subagents at `gpt-5.6-sol` / `xhigh` effort, write-mode** (user-specified), each driven by a spec-grounded brief I authored (`CODEX_BRIEF_A8.md` / `CODEX_BRIEF_A11.md`, temporary files at repo root — to be deleted before commit). **A8 codex job running** (replacing the core's in-process `&mut LedgerState`/`&mut WelfareState` with frame-free trait seams; no double-settle per 05 §6; rule-4 params; `EpochOf` kept byte-for-byte per 02 §7.1). A11 brief ready (audit-scope-A / R-7: the real-dispatch surface + I-9/I-10/I-11/I-19 adversarial suites). **Nothing marked ✅ yet — both pallets are mid-implementation; A8 and A11 stay 🔨.** Working tree is dirty with in-flight pallet edits + the two temp brief files.

**What comes next:** A8 codex completes → review (spec-reviewer + codex adversarial + full gates) + harden → launch A11 codex → review/harden (R-7 rigor) → set A8/A11 ✅ in PLAN.md + Session/Decision/Spec-question logs → delete temp briefs → commit both milestones → open PR

## A8 (`pallet-epoch` FRAME pallet + dual review + 2 remediation rounds → ✅)

Implemented A8 as a production `#[frame_support::pallet]` over `epoch-core` using **Codex subagents (gpt-5.6-sol/xhigh, write-mode)** per the user directive, driven by spec-grounded briefs I authored. **Core refactor** (behavior-preserving): `&mut LedgerState`/`&mut WelfareState` → frame-free `LedgerOps`/`WelfareOps`/`EpochParams` seams; **welfare-only settlement** (05 §6, no double-settle); rule-4 constants single-homed. **Shell:** frozen 02 §7.1 storage (monotone `NextProposalId`), 13 origin-checked calls (**no `EnsureRoot`**; incl. `void_cohort`/`veto_upheld`), the 11-step `decide()` (I-14/I-15) from injected seams, `try_state` I-16/I-21, genesis/mock/benchmarks/weights. **Review = dual (native `spec-reviewer` 1 blocker/1 major/5 minor + Codex adversarial 3 blocker/12 major/2 minor), triaged by me; a focused Codex re-review then found 1 blocker/5 major in the newly-added complex logic — all fixed across 2 codex remediation rounds with regression tests.** Notable fixes: **stale-clock re-anchored** (was `submitted_at` → force-VOIDed every healthy proposal at ~d7; now an epoch-staleness latch), **T_dec** (`dec.window/BLOCKS_PER_DAY`, R-7), **gate-requirement frozen at Seed** (a vetoed Treasury proposal could Adopt on a NAV shift), plus reason-ordering / ProposalId-monotonicity / metric_spec-binding / T20-single-event / rerun-I16 / Baseline-carry / dead-man-pause / T5-T6 / void_cohort. Deferred (documented): bond custody → B-track (A10 precedent), PoV weights → B5, seam wiring + SafetyFilter rows → B1a. **SQ-83…SQ-92** logged. Gates green (workspace **557**; pallet-epoch **53**; reference-model 21). Codex visibility note: each codex run is a detached job polled via a job-id (`! /codex:status <id>` shows live progress). Committed `feat(epoch): production pallet-epoch over the core (A8)`.

**What comes next:** A11 (`pallet-execution-guard`, audit-scope-A/R-7) by the same pattern; then refresh Current focus + open the PR covering A8+A11

## A11 (`pallet-execution-guard`, audit-scope-A/R-7 — in progress)

A8 committed (`d51d8d5`, gates green). A11 implementation launched on a **Codex subagent (gpt-5.6-sol/xhigh, write-mode)** against the spec-grounded `CODEX_BRIEF_A11.md` + the preemptive "lessons from A8" note (all-checks-before-side-effects; freeze-at-queue-time don't-live-recompute; clean up auxiliary tracking on every terminal transition). Target: production `#[frame_support::pallet]` over `execution-guard-core` — the 13-item `execute()` dispatch list, class origins + the two allowlisted internal-Root upgrade calls (I-10, the only `EnsureRoot` in the workspace), the two-phase authorize/apply upgrade flow + bonded attestation + `DescriptorLeadTime`, the `ExecutionRecords` ring, and the audit-critical real-`RuntimeCall`-dispatch surface (the one thing the core only models), with the I-9/I-10/I-11/I-19 R-7 adversarial suites. **A11 stays 🔨 — mid-implementation; nothing marked ✅.** Each codex run is a detached job polled via a job-id (`! /codex:status <id>` shows live progress).

**What comes next:** A11 codex completes → R-7 review (native `spec-reviewer` + Codex adversarial) → remediate findings → set A11 ✅ + refresh Current focus → commit → push → open the PR covering A8+A11

## A11 (`pallet-execution-guard` FRAME pallet + dual review + remediation → ✅)

Implemented A11 as a production `#[frame_support::pallet]` over `execution-guard-core` (audit-scope-A, R-7) with **Codex subagents (gpt-5.6-sol/xhigh)**: the audit-critical real-`RuntimeCall`-dispatch surface (class origins + internal Root for ONLY `authorize_upgrade`, non-Root apply), the 13-item ordered `execute()` checks, two-phase upgrade + attestation + `DescriptorLeadTime`, atomic batch + terminal cleanup. **Recovered a mid-run codex worker death** — the companion left the job "running" while the worker had died at the fmt step; the partial impl already compiled + passed 31 R-7 tests, so I finished fmt/clippy/gate myself and added a **liveness-aware poller** (checks the worker PID) so a silent death can't masquerade as running again. **Dual review** (spec-reviewer 0 blocker/1 major/4 minor + Codex adversarial **2 blocker/3 major**) — all fixed in one codex remediation round: the **queue-exhaustion blocker** (completed the A8↔A11 `dequeue_terminal` handoff so `epoch.tick` T15/T16/T22 don't leak guard slots), **execute-time I-19 quorum re-check**, internal-Root tightened to the single `authorize_upgrade` (non-Root permissionless apply), rule-4 single-homing. A **focused Codex re-review** then caught that R-4 (aggregate ≤16-call bound) was fixed in the pallet but NOT the frame-free core (per-top-level budget reset) — **I fixed it directly**: a shared-budget `origins-core::SafetyFilter::validate_batch` + a core regression test, restoring differential parity. Best-effort `utility.batch`/`force_batch` atomicity → documented B1a SafetyFilter constraint; weights → B5. SQ-93…SQ-97 logged. Gates green (fmt · clippy `-D warnings` · workspace test · pallet-execution-guard 39/44 · pallet-epoch 57/71 · runtime-benchmarks · try-runtime · no_std · reference-model 21 · doc-table). Committed `feat(execution-guard): production pallet-execution-guard over the core (A11)` (includes the epoch R-2 handoff + the origins-core aggregate-budget entry).

**What comes next:** **Track A is complete (A1–A11 all ✅).** Push the branch + open the PR covering A8+A11. Next: **B1a** runtime assembly — wire the guard's `Dispatcher`/SafetyFilter/attestor/epoch seams, enforce the best-effort-wrapper rejection (SQ-96), and the class-origin `RuntimeCall` projection

## A8+A11 (PR #56 — Codex-connector review: 1×P1 + 2×P2, all TRUE, fixed)

Triaged the three `chatgpt-codex-connector` comments on PR #56 (A8 `pallet-epoch` + A11 `pallet-execution-guard`); **all three hold true against the spec, all fixed with regression tests.** **(P1 — dead-man converted freezes into rejections, 05 §4.8)** the permissionless `tick` fed `process_hold = dead_man_engaged() ‖ ledger_frozen() ‖ stale` into `epoch-core::tick`, which **force-rejected/voided every non-terminal proposal** while the dead-man switch was engaged (ahead of the recovery no-op) — the opposite of §4.8's freeze-queue / extend-windows / pause-clock, and outside 05 §2.1's T20 tick-force-reject triggers (VOID / stale-epoch only; dead-man→Reject is a *decide()*-time effect, 05 §5 step 2). **Fixed:** the T20 tick force-reject now fires only on `self.ledger_frozen ‖ self.stale_process_hold(pid)`, while **dead-man FREEZES** the machine (`NoOp`, alongside `recovery_epoch.is_some()`). The ledger-freeze force-reject is **deliberately preserved** (06 §6.3 "resolve to status quo" + D-1 par-void is a defensible status-quo resolution; `frozen_seed_force_reject_does_not_deploy_markets` stays green) — the dead-man/ledger asymmetry is the fix's crux. `decide_with`'s dead-man→Reject(ProcessHold) is untouched. The conflated `process_hold` field was removed from `TickInputs` (the core now derives freeze-vs-reject from its own authoritative `dead_man_armed`/`ledger_frozen`/`stale_epoch_cutoff` state). **(P2 ×2 — leaked A11 guard entries)** `veto_upheld` (T24, on a `Queued`→`delay_once`→`Suspended` proposal — the guard entry deliberately persists through `Suspended`) and the direct `force_reject_process_hold` (T20 on `Queued`/`FailedExecuted`) drove the epoch terminal **without** `T::ExecutionGuard::dequeue_terminal`, leaking the queue entry / preimage-pin / resource-locks and tripping the guard's terminal-entry try-state. **Fixed:** both wrappers now dequeue in lockstep (idempotent `do_dequeue_terminal`, matching the existing `expire_or_stale_queue`/`retry_exhausted_to_measurement` pattern). **+3 regression tests** (`dead_man_freezes_live_proposals_instead_of_force_rejecting`; `veto_upheld_releases_the_queued_guard_entry`; `direct_force_reject_process_hold_releases_the_guard_entry` for Queued & FailedExecuted); the shell↔core T20 differential updated to mirror the new pallet-level dequeue seam. Gates green: fmt · clippy `-D warnings` · `cargo test --workspace` · pallet-epoch **60** + epoch-core · runtime-benchmarks · try-runtime · no_std · reference-model 21 + vectors + doc-table. **Independent `spec-reviewer` re-pass: 0 blocker / 0 major / 1 minor** — all three fixes confirmed spec-compliant (freeze validated end-to-end: `latch_stale_epoch` is dead-man-guarded so staleness can't accrue during a pause; `decide_with`'s dead-man→Reject untouched); the 1 minor (stale `dequeue_terminal` doc-comment listing only T15/T16/T22) fixed in-session; the reviewer's pre-existing ledger-freeze-mechanism question logged as **SQ-98** (not introduced by this fix). A8/A11 stay ✅.

**What comes next:** Commit + push to PR #56; reply to the three Codex comments; then B1a

## — (no milestone; repo hygiene, concurrent with the B1a Codex job running in another session)

`LICENSE` still carried the unfilled FSF GPL-3.0 boilerplate placeholder (`Copyright (C) <year>  <name of author>`) in the "How to Apply These Terms to Your New Programs" section. Filled it in, per user direction, as `Bleavit, a futarchy-governed Polkadot parachain` / `Copyright (C) 2026  Bleavit` — matching GitHub's standard convention of only touching that one placeholder line, leaving the FSF preamble and the terminal-interaction example untouched. No pallet/crate/doc code touched; not gate-relevant.

**What comes next:** Uncommitted. Confirm with the user whether to commit standalone or alongside PR #56, and whether `Cargo.toml`'s `[workspace.package]` should also gain an explicit `authors` field / per-crate copyright headers (raised, not decided)

## B1a (runtime-level FRAME assembly) ✅ + PR #57 merge onto post-#56 main + Codex-bot triage

Turned `runtime/bleavit-runtime` from the frame-free B1 composition model into a **real Cumulus parachain runtime**, orchestrated with **Codex subagents** (user directive). Scope: `construct_runtime!` over the **10 production pallets** (A1–A7/A9/A10) + the standard/system/Cumulus set. **A8/A11 landed on `main` (#56) while this PR was open** — the runtime keeps their `construct_runtime!` index slots (61/62) + **fail-closed pending seam adapters** (`PendingA8Authority`/`PendingEpochClock`/`PendingReporting`/`PendingRegistryEpoch`/`PendingGuardian*`/`PendingUpgradeProvider` — every one G-1); **wiring the two now-available pallets is the immediate follow-up**, deliberately kept out of this PR to preserve its reviewed scope. **Built via a Codex `task`** to a spec-first brief (scope rulings + classifier domain principles): a first run stopped on the sandbox's missing crates.io; I fixed the environment by writing the runtime `Cargo.toml`/workspace pins myself and pre-`cargo fetch`-ing the full offline closure, then resumed Codex to author `lib.rs`/`configs.rs`/`classifier.rs`/`genesis.rs`/`apis.rs`/`tests.rs`/`build.rs`. **`BaseCallFilter = SafetyFilter<BleavitSafetyClassifier>`**: an exhaustive (no `_`) `RuntimeCall`→`CallDomain` projection = the I-8/I-10 compile-time filter-exhaustiveness; borrow-walked depth/count budget; D-13 nobody row (system storage/code calls, `pallet_xcm` send/force_*, asset + balances force_*, UMP send) denied bare and under every closed wrapper; SQ-32 realized as branch (i) (scheduler verified to apply the filter → closed values-enactment-leaf allowlist). **Dual review (native `spec-reviewer` + Codex `codex:codex-rescue` adversarial) + the PR #57 Codex-connector bot (1 P1-tracks + 1 P1-phase3 + 1 P2-cancel/kill).** **Fixed in-session with regressions:** **(P1)** `sudo.sudo_as` denied — it forges an arbitrary `Signed(who)`, letting the founding multisig impersonate the welfare-settlement sovereign to drive `ledger.settle_*` (defeats 06 §3.1 one-path) or steal VIT; `sudo`/`sudo_unchecked_weight` (Root dispatch) stay recursed (**SQ-99**). **(major)** referenda support curves were floor/ceil-swapped in `make_linear` → `Perbill::sub` underflow (panic/wrap) leaving **every values track unable to confirm** — swapped to ceil=high/floor=low + a curve-threshold test. **(major + bot P1-tracks, SQ-103)** the five `ConstitutionalValues` referenda tracks collapse onto one track (stock referenda routes by origin) → an entrenched-scope action could enact at a weaker threshold; the shared CV track now uses the **strongest/entrenched thresholds** (80% / 20%→10% / 4-epoch) so nothing enacts below its bar (over-strict-but-safe; true per-track discrimination = the values-layer milestone). **(bot P2)** `referenda.cancel/kill` added to the values-enactment leaf set (CancelOrigin/KillOrigin = ConstitutionalValues, else scheduled cancel/kill is `CallFiltered`). **(major)** CONST/entrenched `set_param` added to the leaf set. **(P2)** try-runtime `on_runtime_upgrade`/`execute_block` now `expect` on `Err` instead of masking failures as `Weight::MAX`. **(P2)** genesis seeds `PhaseFlags = SHADOW_MODE|SUDO_PRESENT` to match the sudo key. **(P3)** `PendingA8Authority` accepts its bench sentinel under `runtime-benchmarks` only. Also fixed a **latent no_std defect** B1a first surfaced (`pallets/{attestor,welfare}/benchmarking.rs` used `Vec`/`vec!` without an `alloc` import). **Deferred (out of B1a scope, SQs):** the **bot's phase3-cap P1** is an A2-ledger/B4-XCM obligation (09 §5.2; no live inflow path in B1a — **SQ-105**), USDC Location-key (**SQ-101** → B4, the one open blocker), treasury accounting-mirror custody (A9 design), SQ-82 epoch-0 relaxation (A8). **R-1:** no `docs/architecture/` edits — all divergences fail-closed/stricter-than-spec, logged **SQ-99…SQ-105** (renumbered from the drafted SQ-83…88 to clear the collision with #56's A8/A11 review, which claimed SQ-83…98). **Merge:** `git merge origin/main` — only PLAN.md conflicted (Current focus + Spec questions + Session log); #56's `origins-core`/`futarchy-primitives` additions are non-breaking for the runtime. **Gates green post-merge:** fmt · clippy `-D warnings` · `cargo test --workspace` · runtime tests + try-runtime · release wasm + runtime-benchmarks wasm builds · reference-model 21/21 · doc links.

**What comes next:** Reply to the 3 PR #57 bot comments; then **wire A8/A11 into the runtime** (Config + swap pending seams + SQ-96 best-effort-batch rejection in the guard payload path); B4 resolves SQ-101; B2 wires `FutarchyApi`.

## B1a immediate A8/A11 wiring follow-up

Wired `pallet-epoch`/`pallet-execution-guard` into the production Cumulus runtime at frozen indices 61/62; replaced the named authority/clock/reporting/registry/guardian/metric/upgrade pending adapters with live sources wherever the runtime has one; added the atomic preimage pin→enqueue→terminal-unpin ownership path, A8↔A11 callbacks, live attestor quorum, class-origin dispatch, internal Root only for exact `authorize_upgrade`, non-Root apply, and SQ-96 best-effort-wrapper rejection. Added genesis entries, frozen-index/seam/fail-closed runtime regressions, feature legs, and the epoch benchmark module's missing no_std `alloc` imports exposed by the runtime-benchmarks Wasm build. Missing bounded sources remain explicit G-1 adapters and are recorded in SQ-140…SQ-146. Independent compliance close: **0 blockers / 0 unlogged majors**. Gates: fmt PASS; clippy `-D warnings` PASS; workspace tests PASS (**635 passed, 0 failed**, including runtime **38/38**); runtime-benchmarks check PASS (one pre-existing guardian benchmark unused-import warning); try-runtime check PASS. No architecture or pallet public-surface changes; no commit/push/stash.

**What comes next:** **B5 may proceed** while B4/values-layer work owns the logged missing-source questions.

## B5 runtime benchmark execution repair (milestone remains 🔨)

Repaired all 121 failures in the initial runtime-level benchmark census without changing production semantics: benchmark-only successful origins cover closed assets/scheduler/referenda seams; bounded `BenchmarkHelper` fixtures hydrate the real registry, epoch, oracle, execution-guard, guardian, and welfare cross-pallet state; conditional-ledger setup now respects the runtime ED/USDC configuration. Worst-case fixtures fill every collection the measured path touches, including the real Code authorization/ratification/attestation/spacing/checkpoint path and the nested epoch/guard/welfare aggregates; structurally incompatible simultaneous limits are documented beside the fixtures. No pallet was removed from `define_benchmarks!`. The source-fresh release Wasm ran all **312** defined benchmarks with exit 0 and no failure marker. Independent final spec/compliance review: **0 blockers / 0 majors**. Final gates: fmt PASS; clippy `-D warnings` PASS; workspace tests PASS; try-runtime check PASS. No architecture, CI/tooling, commit, push, or stash action.

**What comes next:** Generate/review the final B5 weights and PoV budgets, then close the already-staged weight-regression scope before marking B5 ✅.

## B2 (`futarchy-runtime-api` declaration crate → ✅)

Implemented B2 as the `runtime-api/` crate per 02 §3 ("Declared in the `runtime-api/` crate"): `sp_api::decl_runtime_apis!` over all 11 `FutarchyApi` methods byte-for-byte (names, param names/types, return types, order, doc comments) on the 02 §4 view types single-homed in `futarchy-primitives` since M1 — no type redeclared; every collection bound traces to `bounds::*` (rule 4), with `MAX_QUEUED_EXECUTIONS = bounds::MAX_LIVE_PROPOSALS` re-derived exactly like `execution-guard-core::MAX_QUEUE` (fixing the one literal the authoring pass introduced); `sp-api = "=41.0.0"` pinned after verifying the stable2603 umbrella manifest (V-15). Authored by a **Codex subagent** (8 min, gates green first pass) to a spec-cited brief; dual-reviewed: **spec-reviewer 0 blocker / 0 major / 0 minor** (full method-by-method 02 §3 comparison), **Codex adversarial** → its 2 high findings are the pre-existing frozen-contract defects **SQ-55**/**SQ-43** (B2 correctly declares contract v3 as written — conservative R-2; `NavView` extensions can land append-only per 02 §13(3); both now ride the 02-amendment batch needing **user sign-off before B1a implements `nav()`/`recent_cohorts()`**), its 1 medium answered in-session with a **FRAME-`BoundedVec` cross-encoding test** (`bounded_collections::BoundedVec<T, ConstU32<N>>` — byte-identical encodings, cross-decoded both directions, every 02 §3 collection), and the TypeInfo/metadata-shape residual logged as **SQ-106** (golden runtime-API metadata test decided at B1a). Tests: 5 suites (11-method mock-impl callability via `ApiRef`, blake2-64 API ID + `VERSION == 1` freeze, bound freeze vs the 02 §3 table, per-view SCALE round-trips, cross-encoding). Gates: `rust-workspace-gates.sh` exit 0 · runtime-api 5/5 · `--no-default-features` check · docs-links lint · reference-model 21/21. Living docs: AGENTS.md/README.md repo maps gained the `runtime-api/` row; B2's dependency edge re-scoped ("B1a implements what B2 declares").

**What comes next:** B1a — runtime assembly (`construct_runtime!` + `impl_runtime_apis!` over this declaration + Track-A seams); land the SQ-55/SQ-43/SQ-2/SQ-23/SQ-37 02-amendment batch (user sign-off) before B1a wires `nav()`/`recent_cohorts()`

## B3 (node, chain specs, genesis allocation/vesting) — 🔨 in flight, this row is the mid-session R-3 checkpoint

Fast-forwarded the `milestone-B3` worktree onto post-#57 main (B1a ✅ unblocks B3). **Feasibility + node done:** the stable2603 node stack is unresolvable from crates.io (every `core2` version yanked; `sc-network → litep2p → multihash → core2`) — fixed by vendoring the exact published `core2-0.4.0` package (`vendor/core2-0.4.0`, sha256 in vendor/README.md) behind `[patch.crates-io]`; `node/bleavit-node` authored as a thin `polkadot-omni-node-lib =0.14.1` branding (runtime ships in the chain spec, NOT the node — 02 §11), builds and runs (`bleavit-node 0.1.0`); required stable2603 **maintenance-pin adoptions** `staging-xcm-executor =25.0.0→=25.1.0` + transitive `polkadot-runtime-parachains 25.0.0→25.0.1` (01 §9 regime; to be Verification-logged at close). **R-1 spec edits:** 08 §2.1 `[VERIFY]` resolved — SDK `pallet-vesting =46.0.0` adopted over an in-pallet store, founding-team curve pinned to the conservative single-schedule reading (zero before the 1-y cliff, linear to exactly 4 y; the two-schedule catch-up composition is rejected because pallet-vesting's genesis lock is replace-not-accumulate → the cliff tranche would be spendable at genesis); 01 §5.1 pallet-map row + 13 identity/bounds rows added; README/AGENTS repo maps re-trued. **In flight:** a Codex `task` (write-mode, monitored with a PID-liveness poller) authoring genesis presets per the 08 §2.1 allocation table (MAIN/community/incentives pots on `bl/trsry`-derived accounts; Charlie/Dave vested; Alice/Bob ops), the pallet-vesting runtime wiring (index 14, D-13 classifier rows denying `force_vested_transfer`/`force_remove_vesting_schedule` for all origins), `deploy/` chain-spec pipeline (pinned chain-spec-builder 17.0.0, bootnode manifests + 02 §10 threshold validator, ss58-7777 registry artifact) and CI cache/target updates; release runtime wasm + chain-spec-builder install running in parallel.

**What comes next:** Codex finishes → gates → dual review (spec-reviewer + Codex adversarial) → dev-node block-production smoke test → PLAN close-out (SQs, Decision/Verification logs) → commit → push → PR

## B3 (node, chain specs, genesis allocation/vesting) ✅

**Close-out of the row above — B3 is done end-to-end.** **Codex authoring round** (to a spec-first brief): `pallet-vesting =46.0.0` wired (index 14, rule-conformant Config incl. the para-block `BlockNumberProvider` R-7 rationale; D-13 nobody rows for `force_vested_transfer`/`force_remove_vesting_schedule`; classifier arm exhaustive), genesis presets restructured to the exact 08 §2.1 five-group allocation (const-asserted sum; sp_keyring-verified dev publics; single-schedule team vesting), `deploy/`+`tools/deploy` pipeline (generator + validator + manifests + allocation template + ss58 artifact), CI cache/target/libclang/deploy-tests. Codex **caught a polarity error in my brief** (`UnvestedFundsAllowedWithdrawReasons` names the lock-BYPASS set — the brief's `except(TRANSACTION_PAYMENT)` would have permitted transfers of unvested funds) and authored a bespoke `VestingFeeAdapter` to preserve fee-payability past the lock. **Dual review** (native spec-reviewer: 0 blocker/2 major/5 minor; Codex adversarial: 3 high/2 medium) converged on that adapter (normative-text contradiction + divergence from the pinned SDK's `TxPaymentCredit` escrow contract) → **removed** per the strict ruling (stock `FungibleAdapter` restored; strictness test-pinned; USDC fee path per 08 §9; Decision log + SQ-109). Other findings all fixed in-session: validator now requires **≥8 distinct bootnode peer IDs** (one collator could previously masquerade as eight endpoints), validates the **full genesis-allocation patch** (paseo/polkadot profiles REQUIRE it; byte-level pot identity after I corrected the remediation's literal-string address check — the runtime serializer emits prefix-42 encodings of the same accounts, caught by regenerating instead of trusting hand-edited artifacts), allocation template pins the three derived pots (custody mandate), `--locked` on every dependency-resolving cargo invocation (cargo-audit stays B8 per SQ-16/SQ-27), deploy-tests in CI, 08 §2.1 floor-tail + fee-strictness amendments. **Verified:** full workspace gates green ×2 (fmt · clippy `-D warnings` · workspace tests incl. the node · no_std · reference-model 21/21 · doc-table · doc links); runtime 38 + try-runtime 39 + deploy-validator 13 suites; chain-spec regeneration + validation clean; **dev node imports 35+ blocks from the freshly generated spec** (manual seal). V-8/V-15 logged; Decision-log row; SQ-107…SQ-109.

**What comes next:** Push branch + open the B3 PR; reply to any Codex-connector PR findings; then the A8/A11 runtime-wiring follow-up, then B2. ss58-registry submission (deploy/ss58) before Phase 2 — O3

## B3 (PR #60 — merge onto post-#58 main + Codex-connector review: 2×P1, both TRUE, fixed)

**Merge:** `git merge origin/main` after B2 (#58, `runtime-api/`) landed — conflicts in Cargo.toml (members: keep both `runtime-api` + `node/bleavit-node`), PLAN.md (B2 row + session log; **my SQ-106…108 renumbered → SQ-107…109** because B2 claimed SQ-106), README/AGENTS repo maps (B3 rows kept, `runtime-api/` row added; the stale "FutarchyApi is B2" runtime gloss re-trued to "runtime-side `impl_runtime_apis!` is the open follow-up"). **Bot P1 #1 (CI-red deploy tests):** `ValidateGenesisTests` read the gitignored generated `deploy/chain-specs/out/bleavit-dev.json`, so the new CI step failed `FileNotFoundError` on any fresh checkout — the fixture is now **synthesized from the validator's own constants** (pots/schedule/totals; well-known dev accounts checksum-verified through the validator's ss58 decoder), and the generated-artifact check survives as a `skipUnless(exists)` integration test (the generator script still validates the real artifacts directly). Verified by simulating a fresh checkout: 16 tests, OK (1 skipped). **Bot P1 #2 (genesis para id):** the validator checked only the top-level `para_id`, so a release spec whose `genesis.runtimeGenesis.patch.parachainInfo.parachainId` was absent or still the 4242 fixture would pass while the runtime (which reads `staging_parachain_info` from genesis, not the extension) booted configured for a different parachain — `validate_genesis` now requires `patch.parachainInfo.parachainId` to exist and equal the top-level `para_id` (02 §8), with presence + mismatch regression tests (16/16). Generated dev/local specs re-validated OK. Full workspace gates re-run green on the merged tree (now incl. `runtime-api`, under `--locked`).

**What comes next:** Push the merge + fixes to PR #60; reply to the two bot comments

## B1a CI follow-up (PR #59, post-B3 conflict resolution)

Merged `origin/main` at `7354d5c` (B3, PR #60). Resolved `.github/workflows/ci.yml` by retaining both runtime Wasm targets from PR #59 and main's Rust cache/libclang setup. Resolved `PLAN.md` from main's deduplicated history, then restored only PR #59's unique current status. No protocol behavior changed. Full `tools/ci/rust-workspace-gates.sh` passed on the combined tree (using the installed LLVM 14 library through a temporary unversioned `libclang.so` symlink; CI installs `libclang-dev`).

**What comes next:** Commit and push the conflict resolution; monitor PR #59 checks.

## B1a CI follow-up (PR #59, `litep2p`/`protoc`)

GitHub Actions reached the B3 `litep2p v0.13.3` build and failed because the Ubuntu runner had no `protoc`. Updated the Rust job's native dependency step to install `protobuf-compiler` alongside `libclang-dev`; no Rust or protocol behavior changed. Full `tools/ci/rust-workspace-gates.sh` passed locally with `protoc` available.

**What comes next:** Commit, push, and monitor PR #59.

## B4 (XCM layer → ✅, worktree `milestone-B4`, PR opened this session)

Shipped `runtime/bleavit-xcm` — the B4 XCM layer as a runtime-independent library crate (B1a wires it; B1a concurrent in another session). **Content:** 09 §6.1 default-deny barrier (`Transact`/`UnpaidExecution` refused at any depth, fail-closed decode bounds, closed instruction allowlist, AH/relay/Coretime origins only, no unpaid path/superuser conversion), exact USDC/DOT matchers + reserves (teleports locally disabled), governed-rate 2-D trader with refund ledger, 07 §8 probe (builder + querier-authenticated router, `1<<63` query-id partition vs `pallet-xcm`, appendix `SetFeesMode jit→RefundSurplus→DepositAsset→ReportError` so a pass proves the full round-trip), 09 §4 coretime funding leg on the **verified relay-teleport route** (V-19), 09 §5.2 caps (global at the mint leg fail-fast, per-account transactional at deposit), `classify_pallet_xcm_call` (exit dest pinned to AH; `claim_assets` self-scoped Signed; rest denied, exhaustive), local-only health sink/router, I-24 CI lint in the gate script, chain-identity constants (AH 1000 / Coretime 1005 / PalletInstance 50 / index 1337) single-homed in `futarchy-primitives` with the 13 §3.5 home. **Two xcm-free seams added to reviewed pallets** (the dual review's shared major — no pass-path was reachable by assembly alone): `pallet_oracle::ProbeDispatch` (crank fires the send once per fresh probe; send failures swallowed → fail-static timeout) and `pallet_futarchy_treasury::RenewalDispatch` (core returns the paid quote; dispatch `Err` rolls the extrinsic back → keeper retry), plus ready `XcmProbeDispatcher`/`XcmRenewalDispatcher` impls. **Process:** Codex authored to a spec-quoting brief (29 tests, first pass); dual review — `spec-reviewer` 0 blocker/3 major/5 minor + adversarial Codex (xhigh) **7 blocker/5 major** (querier forgery, cap-after-mint, Coretime `UntrustedReserveLocation`, stranded probe surplus, id collision, no refund ledger, unbounded exit dest, trapped-assets rule unimplementable) — one Codex remediation round + my spec-side R-1 amendments (09 §6.1 trapped-assets → self-scoped recovery; 09 §6.1 teleport-scope clarification; 09 §4 two-leg route; 13 §3.5 rows; 07 §8 final probe shape); focused `spec-reviewer` re-pass: **all 12 CLOSED, 0 blocker/0 major**, its 4 minors (saturating weight sub, distinct local exec limit, health-hook derivation note, V-19 row) fixed in-session. **Verification:** V-3/V-6 closed, V-16…V-19 added; SQ-110…SQ-114 raised, SQ-53 re-scoped. **Gates green:** fmt · clippy `-D warnings` · `cargo test --workspace` (bleavit-xcm 33 · oracle 75 · treasury 28) · no_std (bleavit-xcm/oracle/treasury) · `runtime-benchmarks,try-runtime` leg · full `rust-workspace-gates.sh` incl. the new I-24 lint · reference-model 21 + vectors + doc-table · doc links. Living docs re-trued (README/AGENTS runtime rows).

**What comes next:** B1a wires the seams (needs new 13 §1 trader-rate keys first — SQ-112; caps meters SQ-110/SQ-111; health→welfare binding SQ-113); B7/B8 own the E21 Zombienet/Chopsticks reserve-transfer artifacts and the live multi-chain fee-sizing check (re-review residual)

## B1a CI follow-up (PR #59, post-B4 conflict resolution)

Merged `origin/main` at `14234f3` (B4, PR #61). The only conflict was the append-only `PLAN.md` session log; retained B4's completion entry and both PR #59 CI entries. The workflow merged cleanly and retains Rust 1.89.0, both Wasm targets, Rust caching, `libclang-dev`, and `protobuf-compiler`. No protocol behavior changed by the resolution. Full `tools/ci/rust-workspace-gates.sh` passed on the combined tree, including `bleavit-xcm` 33/33.

**What comes next:** Commit, push, and monitor PR #59.

## B9 (keeper reference implementation → ✅, worktree `milestone-B9`, PR opened this session)

Shipped both keeper halves via parallel Codex subagents (gpt-5.6-sol/xhigh). **On-chain (SQ-49 resolved):** 08 §6.3 keeper meter in treasury core+pallet (per-epoch tranches over `keeper.budget`, latch-once `KeeperBudgetLow`→`KeeperBudgetExhausted`, post-exhaustion payment latch, oracle-line leg), frame-free `keeper::{CrankClass, KeeperRebateSink}` in primitives, `RebatePayout` USDC seam from the `KEEPER__`/`ORACLE__` pots with try-state custody-drift alarm, and strictly state-advancing fail-soft rebate sites in all 8 crank pallets + runtime bindings. **Off-chain:** `keeper/bleavit-keeper` — separate cargo workspace (subxt-dynamic; root `exclude` + own CI gate leg), pure tested planner + race-tolerant submitter + 12 §6.3 metrics + operator README. **Triple review** (2× spec-reviewer + adversarial Codex: 2 blockers/7 majors/11 minors combined) → 2 Codex fix rounds (9 on-chain: daily-gate drain, registry close farm, `ProbeDispatch::live()` no-op-probe gate, 07 §4 `ack_observed` rebates, latch-once extension/reap parity, guard-reap de-rebate, Low-before-Exhausted + latch, custody alarm, settle_cohort coverage; 6 keeper: stale-TWAP wiring, signer-required live mode, metadata `TickBatch`, probe-constant docs, branch tests) + **1 surgical revert** (off-chain review's empty-epoch-close major was wrong at the pallet layer — A6's `FilingCount` ruling; a duplicate concurrent Codex fix job was detected and cancelled mid-flight) → re-review 0 blocker/0 major. SQ-115…SQ-126 logged. Gates green: full workspace · keeper workspace (27) · runtime-benchmarks · try-runtime · no_std · I-24 · reference-model 21/21 · doc links.

**What comes next:** Open B1a A8/A11 wiring (binds `InDecisionWindow` + meter epoch clock + epoch/guard rebate seams); B5 (weights incl. registry `WeightInfo=()`, `keeper.rebate` calibration [VERIFY]); B6/B7; RB-KEEPER runbook (O4)
