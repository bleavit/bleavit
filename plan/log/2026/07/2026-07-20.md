# Session log — 2026-07-20

## G1 (Phase-1 drills forward — SQ-274 resolved; drills…

G1 (Phase-1 drills forward — SQ-274 resolved; drills 01/02/03/06 pass; 🔨)

User: "implement milestone G1 by using multiple codex subagents intelligently … Decide on your own. Push to the existing G1 PR." **Merged newest main (#105 G0+S4 ✅), satisfying G1's G0 dependency**; append-only PLAN conflict resolved; four G1 SQs renumbered SQ-267→274 / 268→275 / 269→276 / 270→277 (main claimed 267–273). **Outcome: release-tier drills 01/02/03/06 PASS end-to-end against the freshly-rebuilt merged runtime; SQ-274 resolved.** **WP-1 (drill-07):** retired the stale `spec_version === 1` readiness sentinel → capability probe on the genesis-registered canonical Location-keyed USDC (SQ-101). **WP-2 (Codex-A + orchestrator; SQ-274):** the PB-MIGRATION drill stages the guard trigger `MigrationHalt = true` at genesis via a new benign `pallet-execution-guard` `migration_halt` genesis field (defaults false; the drill spec stays PLAIN so the pinned zombienet 1.3.133 schedules it — a raw storage-injected spec is not schedulable, the first-run finding) — no production fault-injection surface (R-7). Drill 06 passes 3/3: `assert-halt` (staged trigger, storage-key self-checked) + the guardian 5-of-7 recovery whose dispatching approval correctly **fails closed** with `Other` (the Migration playbook has no EmergencyPlaybook-safe call — distinct from the pre-SQ-274 `TriggerInactive`). Two helper defects fixed with the runtime as arbiter: `.key()`-already-returns-hex, and the `[Option<AccountId>; 7]` membership decode (B1b vacancy shape). **WP-3 (Codex-B):** drill-05 staged-renewal exit-form code (stage/execute/probe legs note a live quote as the genesis authority); its funded `ops.coretime` line + treasury custody are not chain-spec-fundable (unreachable `FutarchyTreasury` origin) so they need genesis seeding, and the run is g1-tier (~8 h dead-man) — the gate names both; drill-05 stays gated. **Spec-pipeline fix (drill-regen blocker):** the merged runtime's B1b seat-bond genesis assert panicked because `development_genesis` funds only 4 of the 7 seeded guardians — now genesis-funds all seven bonds by MOVING VIT out of the Alice ops stand-in, preserving the exact 08 §2.1 1,000,000,000-VIT / 150M-ops / 200M-founding totals the deploy validator enforces. **Drill 07:** the sentinel is retired but the first real run surfaced the pinned Paseo `asset-hub-paseo-runtime` trapping on its parachain-system inherent (`AbortedDueToTrap`) — a Paseo-CSG/system-runtime issue (not Bleavit); re-gated with attribution. **SQ rulings (decide-yourself):** SQ-274 resolved, SQ-275/276/277 ruled, SQ-278 raised (no-effect-playbook revert vs `FailedAction`). Node + runtime Wasm rebuilt; specs regenerated; execution-guard tests **51/0** incl. the new `genesis_migration_halt` test. Gates green (env validator · 51 env tests · js/bash syntax · doc links · PLAN tables · suites.json · deploy validators). G1 stays 🔨.

**What comes next:** Dual review (spec-reviewer + adversarial Codex) on the final tree; then push to the G1 PR + update its title/description. Residuals to ✅: the long-horizon runs (04 ~8 h, 08 ~3 day, 09 ~63 day SQ-128), drill-05 genesis seeding + run, drill-07 Asset Hub topology, drill-08 staging (SQ-233), evidence emission (SQ-202/SQ-204/SQ-203)

## G1 (spec-review + SQ-128 soak ruling — follow-up, pushed)

Native spec-reviewer on the SQ-274 change: **0 blocker / 2 major / 3 minor**, no safety defects — the `migration_halt` genesis field is G-1/R-7-safe (defaults false, only ever *engages* a freeze), touches no frozen 02 surface, no try-state coupling; the seat-bond reallocation preserves the 08 §2.1 1B/150M/200M totals; SQ-278's revert-on-no-effect is a legitimate accountability question, not a defect. The two majors are **doc-reconciliation gaps** (the ruled behaviour is sound but 09 §7.1 / 06 §6.2 / 09 §3.2 still describe the pre-SQ-274 PB-MIGRATION) — logged **SQ-279/280/281** for a deliberate R-1 doc pass. Minor fix applied: `validate-chain-spec.py` now also validates `bleavit-drill-migration.json` (the exact artifact drill 06 boots). **SQ-128 ruled (user chose the fast-timing test runtime):** the 63-day release-cadence soak is infeasible (kernel `MIN_EPOCH_LENGTH_BLOCKS = 201,600` ⇒ ≥42 days even at the floor; the 6 s Aura slot can't go sub-second; Bleavit is fully block-denominated) **and** redundant with G2's ≥6-epoch Paseo requirement — so drill 09's release-cadence run is subsumed by G2 and the G1 local drill becomes a **compressed machinery proof** via a feature-gated fast-timing test runtime, scoped as a focused audit-scope-A follow-up (cfg-gating ~15 `futarchy-primitives` kernel timing constants, consumed by reference-model/fuzz/TLA/property suites — R-7 strictest treatment). Gates re-verified green (env validator · 51 env tests · doc links · PLAN tables · suites.json).

**What comes next:** Implement the fast-timing test runtime (the drill-09 compressed local soak); the SQ-279/280/281 09 §7.1 / 06 §6.2 / 09 §3.2 doc reconciliation (deliberate R-1 pass); then the long-horizon 04/08 runs, drill-05/07 residuals, evidence emission

## G1 (drill-09 fast-timing test runtime — SQ-128…

G1 (drill-09 fast-timing test runtime — SQ-128 implemented; drill 09 PASSES 6/6; 🔨)

User: "Do the next step" (implement the user-selected fast-timing runtime). **Outcome: drill 09 PASSES 6/6 against a new default-off `fast-timing` compressed-epoch test runtime — three real 84-block epochs (`epoch.length = 21 × FAST_DAY(4)`) advanced unattended in ~29 min** (para height 84→168→252 at ~7.4 s/block, the topology keeper cranking `Epoch.tick` and finalizing each tick; `epoch.epochOf().index ≥ 3` asserted). Two parallel Explore-agent coupling audits reduced the ruling's ~15-constant pre-estimate to a **minimal, release-invariant set** — the empty-progression drill triggers no proposal/market/oracle/execution/freeze path, so only the epoch clock compresses. Gated behind default-off `fast-timing`: two `futarchy-primitives` kernel floors (`MIN_EPOCH_LENGTH_BLOCKS`, `DECISION_WINDOW_FLOOR_BLOCKS`) + three `constitution-core` registry seeds (`epoch.length`/`dec.window`/`dec.trailing`), all derived from one knob `kernel::FAST_DAY_BLOCKS = 4`; forwarded primitives → constitution-core → pallet-constitution → runtime. `BLOCKS_PER_DAY` and every emergency/execution/oracle/dead-man window stay at production values (cannot fire in a 252-block drill; `attack_cost_hat`/`t_dec` never invoked ⇒ D-4 economics untouched). **Release-invariance proven** under default features: the `production_epoch_timing_floors_are_frozen` guard test + the unchanged `genesis_registry_matches_13_1_row_encodings` byte-assert + genesis-keys fixture; `cargo test --workspace` never enables the feature; each `cfg(not(fast-timing))` arm is textually identical to the frozen 13 §1 value. **Wiring:** fast wasm built into a separate `target/fast-timing` dir (never clobbers the release wasm); `generate-relay-specs.sh` → `bleavit-drill-fast.json` (`chain-spec-builder --verify` re-ran `EpochParams::validate` over the compressed genesis) + `zombienet/networks/bleavit-fast.toml`; the drill-09 zndsl retargets the fast network (84/168/252 gates) and `assert-epoch-progression.js` now reads the LIVE `epoch.schedule().length` (cadence-agnostic — asserts identically vs the release runtime); `suites.json` drill 09 un-gated (timeout 3600 s). Gates green: pallet-epoch 85/0, pallet-constitution 40/0 (incl. genesis byte-assert), the primitives guard test, limit-coverage 178/0, fmt/clippy clean, no_std + fast-timing compose, env validator + 51 env tests, PLAN tables.

**What comes next:** Push to the G1 PR (#108) + refresh title/description; then the g1-tier long runs (04 ~8 h, 08 ~3 day), drill-05 genesis seeding + run, drill-07 Asset Hub topology, drill-08 staging (SQ-233), evidence emission (SQ-202/SQ-204/SQ-203), and the SQ-279/280/281 doc reconciliation

## G1 (fast-timing extended to drills 04 & 08 — timing…

G1 (fast-timing extended to drills 04 & 08 — timing proven; drill-04 SQ-282 surfaced; 🔨)

User: "Can we also use fast-runtime for 04 and 08? … I don't want to wait 3 days and also not 16 hours." Extended the default-off `fast-timing` feature (SQ-128) to two more `futarchy-primitives::kernel` constants after an Explore coupling audit (both compress in isolation — one production consumer each, no invariant/property/try-state/TLA/limit-coverage magnitude dependence): **`DEAD_MAN_RELAY_BLOCKS` 4,800→48** (drill 04) + **`DESCRIPTOR_LEAD_TIME_BLOCKS` 43,200→`3×FAST_DAY`=12** (drill 08). Release-invariant: `cfg(not(fast-timing))` arms byte-identical, `production_epoch_timing_floors_are_frozen` now pins 4,800/43,200, the two `assert_eq!(…,43_200)` re-export pins `#[cfg(not)]`-gated, a `Some(4_800)` runtime-test literal → kernel expr (R-4), a clippy `assertions_on_constants` fast-arm nit fixed; fast wasm rebuilt + `chain-spec-builder --verify` genesis check green. **Drill 04's first-ever run: the finality stall compressed ~16 h→~8 min (reached gap 48 in ~8 min — timing PROVEN) but `assert-dead-man engaged` FAILED (result 16/17).** Root cause **SQ-282** (raised, decide-yourself): `observe_dead_man` fires on a relay-**parent** jump between parachain blocks (outage/catch-up), not a relay-**finality** stall where the parachain keeps producing (liveness `pre-fault,2` passed) — the open 05 §4.8 `F`-signal `[VERIFY]` (`PersistedValidationData` exposes `relay_parent_number`, not the relay finalized head); **independent of the timing change, which is correct**. Drill 04 re-gated on SQ-282. **Drill 08**: the js reads the live `descriptorLeadTime` (no 43,200 hardcode) so its ~3-day D-14 wait is gone; it stays gated only on freeze/expedited genesis staging (SQ-233). Gates green: fast+default clippy/test/fmt (primitives+constitution+pallet-constitution), limit-coverage 178/0, reference-model 38, env validator + 51 env tests, PLAN tables; full `rust-workspace-gates.sh` **green** (fmt · clippy -D warnings workspace+keeper · `cargo test --workspace` incl. runtime 229/0 · runtime release/benchmarks/try-runtime builds · no_std · reference-model 38 + doc-table + vectors 23 · limit-coverage 178/0).

**What comes next:** The SQ-282 resolution (collator-outage drill scenario, or a relay finalized-head detector input if the state proof exposes it — audit-scope-A); then drill-05/07/08 staging, evidence emission (SQ-202/204/203), and the SQ-279/280/281 doc reconciliation

## G1 (Phase-1 exit REACHED — SQ-282 resolved, drills…

G1 (Phase-1 exit REACHED — SQ-282 resolved, drills 04/05 pass; ✅)

User: "Finish milestone G1 by using codex subagents where appropriate. Don't ask questions. Decide on your own. Commit and push to the PR when ready." **Outcome: the 09 §7.1 line-282 Phase-1 exit drill set passes end-to-end (01/02/03/06 + compressed 09 6/6 prior · 04 dead-man 18/18 · 05 coretime-under-dead-man 19/19) → G1 ✅.** **SQ-282 resolved (decide-yourself):** three parallel Explore agents verified the stable2606 SDK — the parachain runtime CANNOT observe the relay GRANDPA finalized head (`PersistedValidationData` = four fields, no `well_known_keys` finality key, `cumulus_pallet_parachain_system` surfaces only the relay best/anchor), so a finalized-head detector is impossible and a pure relay finality stall is not on-chain-detectable. Closed the 05 §4.6 `F` `[VERIFY]`, re-scoped 05 §4.8/13 §2/14 TH-37 to the observable relay-parent-gap trigger (the detector code was already `relay_parent_gap`-based and correct — no runtime-code change), assigned the finality-stall residual to an off-chain monitor (SQ-283, O5) + XCM-isolation bound. **Drill 04 re-pointed** to a real collator outage (pause all 3 collators → relay best advances ≥ 48 → catch-up jump engages the dead-man); asserts the durable bit-6 + `incident_active` latch (the acute queue-freeze is transient under the compressed catch-up, so it stays unit-tested via `dead_man_pauses_phase_and_rejects_submission`) → 18/18 incl. recovery-epoch clear. **Drill 05** proves the D-9 coretime-renewal freeze-exemption via a probe (`execute_coretime_renewal` → `RenewalWindowClosed`) under the engaged dead-man, on a new fast-timing spec that genesis-seeds the coretime quote authority + renewal account through the EXISTING treasury genesis fields (no treasury code change); the probe reads no raw state → 19/19. **SQ-279/280/281 reconciled** (06 §6.2 + 09 §3.2/§7.1/§2.1: no guardian-dispatchable migration-recovery call; freeze = automatic `MigrationHalt` bridge; MBM lockdown = inflow-freeze; retry/rollback = expedited-CODE lane) + 5 stale section-refs. **Native spec-reviewer: 0 blocker** (1 major finality-alert pointer → SQ-283 + honest re-word; 2 minor citation fixes applied). **Scope ruling (decide-yourself, ratifiable):** drill 08 = Phase-2 (09 §7.1 line 283), drill 07 = Phase-3 (line 284), env-evidence bundle = release-train (G2+, SQ-203/204) — none is a Phase-1 gate. No runtime-code change; no 02 change; docs gates green (doc-links · PLAN tables · runbooks · alert-coverage · env-validator). Subagents: 3 Explore (SDK/Paseo-trap/evidence) + spec-reviewer.

**What comes next:** G2 (Phase-2 Paseo): ≥6 epochs + upgrade e2e; drill 08 staged-freeze (SQ-233 genesis pattern), drill 07 AH via option-d westend-local, the env-evidence bundle (SQ-203/204), SQ-283/284

## — (Spec-questions triage sweep; no milestone)

User: "resolve everything before G2 … specifically everything in the spec questions SQ milestones … not the front end", then chose the triage sweep as the entry point and delegated ruling batches. **Investigation:** the pre-G2 non-FE scope is now exactly **O3 + the SQ backlog** (O1 deps F11, O2 deps FE-P7 — both FE-blocked; G2 itself deps the F-launch set). **Sweep:** 16 stale/partial rows verified against code by 4 parallel Explore subagents with file:line evidence — **13 closed** (SQ-47, 49, 53, 81, 96, 105, 118, 143, 155, 159, 230, 267, 271) and **4 narrowed to their exact residual** (SQ-75 registry wrapper-negative coverage gap — not a live vulnerability, shape denials pinned; SQ-82 ambient-sentinel coupling; SQ-142 only `published_flow_per_day` unwritten, kernel rides the specified L̂/2 fallback; SQ-180 shrink-to-fit complete but `NavFloorUnmet` never fires — `flag_nav_floor`/`ensure_nav_floor` have zero production callers). Open **203 → 190**, resolved 76 → 89. **One production change:** `pallet-epoch` `GenesisConfig::default()` `index: 0 → 1` + pinning test, closing SQ-82's named failure mode (a chain spec omitting the `epoch` patch section booted on the pre-launch sentinel). **Added:** a mechanically-checked resolution-batch index assigning all 190 open rows to exactly one of B1–B6 (ratify-as-shipped, ~95 rows, no production code), D (doc-truing, 26), C (contract v6, 30), X (code, 39). Three delegated rulings logged (Decision log). Gates green: `cargo fmt --all --check` · `cargo clippy --workspace --all-targets -D warnings` · `cargo test --workspace` **69 suites / 1,098 passed / 0 failed** · limit-coverage (178 keys, 0 unwired, 0 consumer-binding — corroborating the SQ-155/SQ-159 closures) · deploy tooling 53 · PLAN tables · doc links · batch-coverage check.

**What comes next:** B1–B6 ratification batches (largest unblock, ~95 rows); then C (contract v6 — note the contract is already at **v5**, so the next bump is v6, and AGENTS.md's "v4" wording is stale); then X led by the release blockers (SQ-205, SQ-263, SQ-261, the SQ-173… adoption-input family) and the liveness traps (SQ-215, SQ-235); O3 is the only non-FE milestone left before G2

## — (batch B1 · ratify 05/03; no milestone)

User: "continue with the fix (first ratification batch)". Worked all 13 B1 rows against the code before amending anything (1 Explore subagent, 11 claims verified with file:line). **8 ratified with spec amendments** — 05 §3.3 retention arithmetic (SQ-200), 05 §4.8 recovery epoch / snapshot-overdue suppression / resume straddle (SQ-90, SQ-254, SQ-255), 05 §2.1 T4 disposition taxonomy / T5-T6 ordering / T20 stale-epoch anchor (SQ-191, SQ-91, SQ-86), 06 §2.2 guard-record reaping ownership (SQ-192). Verification improved five of them beyond the row text (dead-man/recovery suppression + self-clearing on T20; `submit`/`settle_cohort` in the recovery blocked set; per-proposal rollover allowance vs the 08 §4.4 shrink; narrow pause predicate; in-place window extension rather than re-registration). **3 refused ratification and reclassified to X** — **SQ-92 is a real defect with stranded funds**: 03 §2.3/§5 normatively require `settle_baseline(epoch, 0.5)` under epoch VOID (and justify `BaselineState` having no `Voided` variant by it), but `void_cohort` never invokes it and `settle_cohort` cannot reach a reaped Void cohort, so the Baseline vault stays `Open` forever and single-sided holders can never redeem (full-pair holders still exit at par via `merge_baseline`, which is why solvency invariants stay green); SQ-98 has a real tick-vs-decide divergence (tick voids at par, `decide_with` resolves Reject into `Measuring`) that the row asserted away; SQ-79 is a live G-1 question — settling gate markets "no breach" on absent data is adopt-favourable, and `SampledGateDays` makes the conservative option cheap (per-epoch, settlement-readable, no 02 change). **2 left open in B1** (SQ-187, SQ-188) pending a rerun/guardian verification pass. Open 190 → 182. Gates: PLAN tables · doc links · batch coverage (182/182) · no Rust touched. **Codex-connector P2 on PR #114 — valid, fixed before merge.** The bot caught the SQ-90 amendment ratifying recovery exits the code still blocks. Verified both halves against `epoch-core` and found it understated: withdrawal is not merely blocked for pre-pause proposals but **categorically closed all recovery** (`withdraw` needs `proposal.epoch == CurrentEpoch`; resume increments to R = P+1 at `lib.rs:608-614` and `submit` is refused, so the guard is unsatisfiable for every proposal), while expiry is half-open — the dedicated `expire_or_stale_queue` entry point stays callable, only the automatic `tick` sweep no-ops (`lib.rs:1430`). Execution callbacks and guardian transitions confirmed genuinely open (no recovery guard on any of the nine `recovery_epoch` sites). 05 §4.8 restated; **SQ-285 raised** for the G-1 question rather than ratifying it. Open 182 → 183. Same over-reach pattern the batch's own three refusals caught — worth noting that adversarial review found a fourth the in-loop verification missed.

**What comes next:** Land the SQ-92 fix as its own audit-scope-A milestone (ledger review); then B2 (ratify 06/09, 15 rows); SQ-187/SQ-188 need the rerun verification pass first

## — (batch B2 · ratify 06/09; no milestone)

User asked whether B2 could be worked independently of B1. It could — B1 had already merged as PR #114 (`3af5aeb`), so the worktree was fast-forwarded and B2's 15 rows are disjoint from B1's. Worked all 15 against the code before amending anything (**6 parallel `spec-reviewer` passes**, every claim file:line-evidenced; the 4 most decision-relevant re-verified by hand). **8 ratified with 10 spec amendments** — 06 §3.3 node-counting + corrected pseudocode (SQ-31), 06 §6.2 no-effect activation + expiry bounds (SQ-278, SQ-45a), 06 §6.3 renewal edge (SQ-45b), 06 §7 held-bond basis + 13 §1 note (SQ-248), 09 §1.2(11) decode depth + new 13 §2 kernel row (SQ-225), 09 §2.1(7) relay-Abort arm (SQ-131), 09 §2.3 `updated_at` (SQ-133), 09 §3.2 trigger-list amendment (SQ-132a–c), 09 §5.2 split/mint scopes (SQ-252, SQ-253). **6 refused to X** (SQ-45 residual (c), SQ-97, SQ-127, SQ-129, SQ-132 residual (d), SQ-144) and **1 to C** (SQ-134). Three refusals are serious: SQ-97 is a one-attestor-departure kill switch on ratified upgrades; SQ-127 means the PB-MIGRATION audit anchor never exists when the halt needs it; SQ-129 leaves an ordinary over-cap Phase-3 deposit permanently unrecoverable from this chain's side and needs a user ruling among three mechanisms. **1 new row:** SQ-289 (screening ≠ guard `enqueue` preconditions ⇒ `decide` reverts permanently after Adopt). Open 183 → 176, resolved 97 → 105. **Not quite docs-only:** the new 13 §2 `MAX_PAYLOAD_DECODE_DEPTH` row made the 15 §4.6 / I-22 limit-coverage gate fail (it demands a `registry.toml` classification for every 13-registry key), so the batch also adds that entry as a `dispatch-limit` bound to `behavior = decode_all_with_depth_limit` under the SQ-156 error/behavior split, plus a `// limit-coverage:` marker on the existing `deep_preimage_batch_decode_fails_closed_at_the_depth_limit` test — a comment, no production code. The key-cell wording was also reworded so the extractor reads one key, not the compound `MAX_PAYLOAD_DECODE_DEPTH/MAX_EXTRINSIC_DEPTH`. Gates: PLAN tables ✅, doc links ✅, runbooks ✅, batch coverage **176/176** ✅, limit coverage ✅ (179 keys, dispatch-limit 66 → 67, 0 unwired), limit-coverage unit suite 23 ✅, deploy tooling 53 ✅, `cargo fmt --all --check` ✅, the marked decode test ✅. **R-6 spec-reviewer pass over the finished diff: 0 blocker / 5 major / 8 minor, all 13 fixed on-branch** — the majors were a depth bug I introduced in the §3.3 pseudocode rewrite (`descend` without a paired restore let depth accumulate across *siblings* instead of along the path, rejecting shapes the real filter accepts), an RB-UPGRADE contradiction with the new no-effect rule (it demanded a `PlaybookActivated`/`ReviewScheduled` trail that now never exists), the abort occasion missing from 09 §2.3's self-described *exhaustive* writers sentence (fixable in-batch, since 09 is outside the versioned surface — only 02 §12's twin waits for C), an over-broad "one observable consequence" claim that ignored nested `batch_all` payloads losing a leaf slot per wrapper, and the `UpgradeAborted` batch home (already folded into SQ-193 mid-review). **Codex PR review (#115): 1 finding (P2) — the same §3.3 depth bug, already fixed in `223469e`; replied on the PR.** **SQ-129 then ruled under explicit user delegation** ("decide on your own what is best for the protocol"): both caps bind before the mint, so a cap refusal never traps locally; `SetAssetClaimer` re-keying was rejected on verified evidence (a recovery claim's deposit leg is metered by the same cumulative cap, so it re-traps forever) and AH-governance-only recovery was rejected as enshrining fund-stranding. Spec amended; implementation owed in X, so the row stays open.

**What comes next:** Implement SQ-129's pre-mint beneficiary check in `bleavit-xcm` (X); then batch B3 (ratify 07, 9 rows); the X queue leads with SQ-97, SQ-127, SQ-129

## — (SQ-92 + SQ-98 code fixes; no milestone)

User: "create a new PR and branch for fixing SQ-92 and SQ-98 to fix batch B1". Branch `fix/sq-92-sq-98-void-baseline-freeze` off `main`. **SQ-92** — wired the 03 §2.3/§5 Baseline VOID settlement through a new `WelfareOps::settle_baseline_void`, preserving the "welfare is sole settlement authority" seam (epoch requests, never scores); score single-homed as `kernel::VOID_BASELINE_SCORE`; `LedgerSettlement::baseline_open` precondition keeps the VOID infallible on the two benign paths without hiding real errors. **SQ-98** — closed the tick-vs-decide freeze divergence by giving `decide_with` `tick`'s exact predicate. **A third defect fell out of SQ-92's wiring** — a latent `pallet-market` defect — `observe_baseline_terminal` never closed the book, so the first settlement on the VOID path (where nothing else closes it) produced a try-state violation, the 03 §9 drift class that arms I-4 — fixed by mirroring `observe_proposal_terminal`. **Two subagents.** An adversarial reviewer was tasked to *refute* the SQ-98 direction; it could not, but found three real defects in the patch, all fixed: eight dangling doc-03 §7.4/§7.5 refs (six pre-existing), an undocumented `ConstitutionViolation` precedence consequence (now normative in 06 §6.3), and a stale TLA⁺ T20 binding. **The TLA fix was attempted and reverted** — admitting `Decide` for T20 makes a Decide/Trading→Rejected/ProcessHold step match both T10 and T20, breaking `Cardinality(matches) = 1`; the model has no freeze variable so the edge is unreachable anyway. Recorded as a known gap with the reasoning commented in the model. A test-engineer subagent authored the end-to-end regressions. **Residuals raised, not ruled:** SQ-286 (freeze sweep scope + freeze-vs-dead-man precedence), SQ-287 (decide-time preimage slash 10 % vs 06 §4(5)'s 100 %, pre-existing) and SQ-288 (no genesis USDC endowment for the ledger sovereign, against 03 §7 R-4). Open 183 → 184 (two closed, three raised). Gates: rust-workspace-gates.sh ✅ (fmt · clippy --all-targets -D warnings · cargo test --workspace · release/runtime-benchmarks/try-runtime/no_std runtime builds · limit-coverage 178/178) · run-model-checks.sh ✅ (all main configs above their distinct-state floors; all 15 witness configs still violate) · fuzz-gates.sh · reference model 38 + LMSR doc tables · PLAN tables · doc links · runbooks · SQ batch coverage 184/184

**What comes next:** B1's tail is SQ-187/SQ-188, still pending the rerun/guardian verification pass; then B2 (ratify 06/09, 15 rows)

## — (SQ cleanup pass; B1 closed)

User challenged the growing backlog — *"Why did you add new SQ questions instead of just reducing"* — and the challenge was correct. Audited my own four residuals: three were rulable and should not have been filed, one was mis-filed as a question when it is a defect. Ruled **SQ-285/286/287** as shipped with spec amendments (05 §4.8, 06 §4, 06 §6.3), reclassified **SQ-288** to new milestone **B14** (genesis USDC endowment vs 03 §7 R-4). Then closed B1's two deferred rows with real verification rather than ratification: **SQ-188** traced through `epoch-core` (both rerun producers set the same `rerun` flag — `:1141` and `:1171` — uniqueness guarded at `:1136-1137`, one consumer at `:1786`/`:1861`); **SQ-187** traced through `pallet-market` (`DecisionWindows` keyed by boundary triple + `DecisionWindowOwners` per proposal; reruns register fresh windows; `consume_decision_windows` takes every window a proposal owns; reference-counted eviction; the 8-boundary ceiling checked pre-mutation with an existing regression test, and `shift_decision_window` exists to avoid a transient ninth). Both under-documented, not defective — 05 §5.4 and 04 §8.4 now pin them. **Batch B1 ✅ closed, all 13 rows disposed.** Open 184 → 178. Docs-only pass, no Rust touched. Gates: PLAN tables · doc links · runbooks · limit-coverage 178/178 · batch coverage 178/178

**What comes next:** Batch **B2** (ratify 06/09, 15 rows) is next; milestone B14 is now queued in Track B

## — (batch B2 closure; 5 rulings, code owed in X)

User: *"B1 batch fully closed now. Please bring B2 batch to the same state… Decide on your own what is best for Bleavit. Push to your existing PR."* Merged `origin/main` (PRs #117 SQ-92/SQ-98 code fixes, #119 B1 closure) into the B2 branch — three conflicts, incl. doc 06 §6.3 where main's rewritten freeze bullets needed the SQ-45(b) renewal rule re-applied on top. **Caught an SQ-id collision the merge had hidden:** this branch and main both minted `SQ-286` for different questions; mine renumbered to **SQ-308**. That motivated `tools/ci/check-spec-question-batches.py` — PLAN.md has claimed since the triage sweep that its batch index is "checked mechanically" and no script backed the claim; the new gate enforces unique ids, every open row in exactly one batch, no batch naming a closed or non-existent row, and reads a 0-row batch's Members cell as prose (19 unit tests, wired into the docs CI job). Then closed B2 properly: five parallel spec-reviewer passes over the six refused residuals, five rulings landed in 06/09/13, three of the refusals' own claims retracted, five milestones (**B15**–**B19**) scheduled for the code, and four rows raised — **SQ-309** (the `OnlyInherents` recovery trap) is the serious one. Gates: `cargo fmt`, `cargo test --workspace` (green at the merge baseline), PLAN tables, doc links, batch index, limit coverage (179 keys, 0 unwired), runbooks (13/20), tooling suites. **R-6 review of my own amendments then returned 22 deviations including 3 blockers — two of them defects I had just introduced** (a conditional for-cause revocation that left the SQ-97 relaxation strictly weaker than what it replaced; an unsatisfiable try-state implication) plus broken R-1 consistency across 15 I-19, 14 TH-28 and 11 §11.5. All 3 blockers and the 12 majors fixed in the same session before closing; see the Decision log row. Third time today that adversarial review caught me — the pattern is the argument for keeping it mandatory.

**What comes next:** B3 (ratify 07, 9 rows). In **X**, SQ-309 now leads: it must be answered before any multi-block migration ships.

## — (batch B3 · ratify 07, plus 6 successor fixes; no…

— (batch B3 · ratify 07, plus 6 successor fixes; no milestone)

User: \"fix batch B3 — do you depend on B1 or B2?\", then \"can't you fix all of them… I want it reduced\", then \"decide on your own what is best for the protocol\". **Dependency answer: no** — B1 amended 05 §3.3/§4.8/§2.1 + 06 §2.2 and is already merged; B2's 15 rows cite 06/09/02/13 §2/15 and none cite 07. **Method:** every row verified against code before amendment — 3 parallel verification subagents, a Codex adversarial pass on the two hardest findings, and an adversarial review of the amendments themselves. Verification corrected or broke **5 of 9** row premises; the review then caught a **blocker in my own amendments** (registry ack row given the oracle's frozen `WindowAcknowledged`, pre-empting SQ-68) plus 3 majors — all fixed, incl. amending 05 to own the `target` field 07 had declared remotely. **7 ratified · SQ-116 split-ruled · SQ-70 refused.** **6 successors fixed in-session rather than carried:** SQ-289 (`div_ceil` + pinning test), SQ-297 (`reap_epoch` → `General`), SQ-291 (zero target fail-closed at **both** entry points — the agent correctly extended it to `file`, since close-only rejection wedges `FilingCount` immortal → permanent `TooManyLiveEpochs`), SQ-290 (non-tracking negative tests + widened registry.toml reasons); SQ-292 folded into SQ-175; SQ-76 reclassified D → X with its \"reuse `ledger.archive_delay`\" option foreclosed. **Backlog 178 → 173** (merged onto main after B1 closed at 178; B3's own baseline was 183). **Two hard findings ruled, not parked:** SQ-293's design pass proved the escalation ladder unimplementable as written — both defects amended without a contract bump, implementation fully specified and sized as its own audit-scope-A milestone; SQ-296 ruled toward value-scaled filing bonds, with the §12 merit-floor objection checked and **dismissed** (ProcessHold gates on `is_oracle_round()`), bps owned by S4, blocked on SQ-174. Gates: `cargo fmt --all --check` ✅ · `cargo clippy --workspace --all-targets -D warnings` ✅ · `cargo test --workspace` **69 binaries / 1,105 passed / 0 failed** ✅ · pallet-registry `runtime-benchmarks` 42 ✅ · limit-coverage 178 keys, 0 unwired ✅ · limit-coverage tests 23 ✅ · PLAN tables ✅ · batch coverage 178/178 ✅ · doc links ✅. **Merged onto post-#117/#119 main in-session:** main closed B1 and landed the SQ-92/SQ-98 code fixes while this branch was open, colliding on SQ allocation — main's SQ-286/287/288 win, so this branch's nine new rows were renumbered **+3 → SQ-289…SQ-297** across PLAN.md and eight code/config files (main's own SQ-286 reference in `crates/epoch-core` deliberately untouched). Four PLAN.md conflict hunks resolved keeping both sides.

**What comes next:** B2 (ratify 06/09, 15 rows); SQ-293 implementation + the SQ-92 ledger fix as audit-scope-A milestones; SQ-296 and SQ-76 residuals both gated on SQ-174 / new 13 keys

## — (Batch B4 ratification: ratify 04/08, market and…

— (Batch B4 ratification: ratify 04/08, market and treasury economics; no milestone)

User: "fix batch B4" plus a dependency question. **Dependency answer:** none — the batch index is a partition, B4's owning docs (04, 08) are touched by no other batch, and this worktree's HEAD *is* B1's commit, so B1's eight amendments were already in the base. **Method:** all 23 rows verified against the implementation by five parallel subagents with file:line evidence before any amendment was written, plus independent re-verification of the three most consequential calls. **Result: 12 ratified, 3 closed, 8 refused (35 %).** The refusal rate is the finding — the triage sweep's ratify-as-shipped classification did not survive verification, so B2/B3/B5/B6 should not be trusted without the same pass. Ratified: SQ-39, SQ-41, SQ-51, SQ-52, SQ-56, SQ-115, SQ-119, SQ-120, SQ-122, SQ-185, SQ-247, SQ-258 (13 amendments across 03/04/05/08/13; see Decision log). Closed without amendment: SQ-65 (B1b already did it), SQ-85 and SQ-184 (obsoleted by contract v5, which deleted the TREASURY ask/NAV threshold). Refused and moved to X: SQ-36, SQ-40, SQ-42, SQ-64, SQ-66, SQ-114, SQ-207, SQ-251 — led by SQ-40 (non-TREASURY proposals fail the `decide` dispatch instead of returning `Reject(SecuritySizing)`, so they never resolve; the reference model separately adopts at prize 0) and SQ-36 (one in-bounds META raise of `ledger.pos_dep` permanently wedges every count-decreasing ledger call). SQ-298 raised: `ORACLE`, `REWARDS` and `ops.collators` carry normative budgets with no consumer implementation. A closing adversarial Codex pass over the finished rulings caught one defect in this batch's own amendment (the §4.1 multi-slot sentence contradicted §2.5's "Phase-4 arming floor" wording — both reworded), completed SQ-185 by truing 05 §5.6's `V_win` gloss, and raised SQ-299/SQ-300/SQ-301. Open 178 → 168 on the merged base (15 closed, 10 raised); batch coverage 168/168. Rebased onto main after #117/#119; ten new rows renumbered SQ-298…SQ-307 for the concurrent SQ-286/287/288 collision, and **SQ-307** raised — applying the ratified reap-ordering rule to `d77b6f5` exposes an unlatched epoch-VOID Baseline settlement that permanently wedges the Baseline book. Gates: PLAN tables, doc links, runbooks, reference-model 38/38, batch coverage 173/173; docs + one reference-model docstring, no Rust touched.

**What comes next:** B3 (ratify 07, 9 rows) — unblocked and untouched; then owners for the B4-sourced X rows, led by SQ-40 and SQ-36

## Batch B5 (ratify 12 — ops, monitoring, release evidence)

**All 16 rows disposed; open 165 → 149 on the merged base.** Verification first, ratification second (the B1 lesson): three parallel read-only audits gathered file:line evidence for every row before any amendment was written, and **6 of 16 rows described the code inaccurately** — SQ-109 (fee strictness is emergent from `FungibleAdapter`, not from the vesting withdraw-reason set, which literally permits fee payment), SQ-137 (all 90 event entries record the *same* `System.Events` blob, so "real bytes" is not per-variant evidence), SQ-124 (`for: 1h` is neither Alertmanager's nor shipped), SQ-238 ("600 blocks" is 12 §1.4(6)'s own deadline, not a 13/08 value), SQ-239 (the checker enforces §6.1 pair membership, not the default rule; RB-BOOTNODE spans two owners), SQ-243(b) (the alert cannot fire — its series is an O3 seam with no producer). Each was ruled on what ships. **2 rows strengthened past ratification** where fail-closed was also silent: SQ-242 fixes a ≥ 2 release-key signature floor (monitor floor 1 → 2, still operator-supplied), SQ-264 makes the coretime seats explicit unfilled genesis seats + a Phase-3 gate. **3 consumer gaps closed rather than ratified**: env-evidence `tier` produced-but-unvalidated (a `g1` file satisfied the release gate), the signature floor, and the missing genesis seats. SQ-241 settled in doc 12 alone — **no 02 edit, contract stays v5**; SQ-135 written source-agnostic so SQ-219 (B6) stays open. Amendments: 08 §2.1 · 09 §3.2, §4 · 12 §1.4, §3.1, §6.1, §6.3, §6.5 · 15 §4.5, §5. Mechanical follow-through on 12 §6.3's byte-bound tables: `check-runbooks.py` taught comma-separated runbook annotations (+3 tests), RB-UPGRADE → `severity: page` + frontmatter/index/escalation, RB-INTAKE/RB-KEEPER trigger cells, and the page-domain test now **derives** its expected set from doc 12 instead of a pinned literal. Derived docs re-trued (`deploy/monitoring/README.md`, `deploy/runbooks/README.md`, `keeper/README.md`, `tools/release/README.md`), including **seven stale `seam — B10` labels** in the monitoring row map that B13 had already closed — only the 3 O3 bootnode seams remain. **A self-review pass against the code caught four over-claims in the author's own first draft**, all corrected before review: 15 §4.5 named the fuzz workspace as separately audited (only root + keeper are); 15 §4.5 claimed mechanical stale-waiver expiry for both legs (only the GHSA leg has it — the `.cargo/audit.toml` list rides the from-scratch re-triage); 12 §3.1 said an absent series \"alerts\" (absence-alerting is an operator meta-monitoring duty, explicitly not a §6.3 row); and 12 §6.3's trigger-cell rule as first written condemned the TWAP row's 96 % early-warning margin above `dec.coverage`, which is a monitoring choice rather than a restatement and is now carved out explicitly. Two changes were delegated to parallel subagents and both mutation-checked their tests; the genesis-seat validator leg is profile-gated after the agent found an ungated check would break the G1 drill spec pipeline. No runtime code touched. **An independent adversarial review then found three real defects in the amendments themselves**, all fixed before merge: 15 §5 claimed byte-for-byte validation stops a placeholder environment directory — **empirically false**, since `artifact_hashes: {}` satisfies set-equality against an empty directory (guard added, mutation-checked); condition (3) of the tiering rule made `suites_skipped` normatively load-bearing while the paragraph beside it forbade consumers to depend on non-frozen fields (promoted to a frozen, validated field); and 12 §6.3's keeper bullet declared *the* blind spot when the shipped rule has **two** — a `connected == 0` keeper is silenced entirely. Two review findings were accepted rather than fixed and are recorded honestly: the new spanning-runbook escalation MUST is review-enforced, not CI-enforced (a §6.3-domain→§6.1-row mapping is not mechanically derivable without new frontmatter), and `verify-release` remains an unbuilt tool that doc 12 already binds in four other places (O1/F13). A pre-existing RB-BOOTNODE defect the review surfaced was also fixed: it told operators no Prometheus series name exists for the dial/retention probes when three are named in the rules — they are unfed O3 seams, which is the operationally important fact. **A Codex PR review then raised two valid P2s, both fixed:** the new coretime-seat validator rejected only `None` and `TODO` while every *other* account field in the same function is format-checked through the existing `ss58_account_id` helper — so a seated-but-malformed ceremony output (a non-SS58 authority, a 3-byte renewal account) would have cleared the release gate and failed only at genesis build; both seats are now decoded per their real genesis encoding (`AccountId` / `[u8; 32]`), with 6 malformed cases tested and both guards mutation-checked. And `tools/release/README.md` still documented `suites_skipped` as a consumer-tolerated extra after this session made it a required validated field, so an operator following the documented contract would have produced evidence that fails strict assembly — the contract block and the frozen-field prose now match the validator, including the non-empty `artifact_hashes` rule. Gates green: runbooks · alert coverage · spec-question batches (149 open, all assigned) · limit coverage · deploy 63 · monitoring 68 · release 71 · env 51 · ci · reference-model · doc links · PLAN tables.

**What comes next:** Batch B6 (ratify 13/15, 19 rows) — the last ratify batch; then D, C, X

## — (Batch B6: ratify 13/15 — parameters and the testing…

— (Batch B6: ratify 13/15 — parameters and the testing regime)

User: "Complete batch B6 fully. Decide on your own." **Verified all 19 rows against the implementation before writing any spec text** (5 parallel subagents, file:line evidence) — and verification broke or corrected **9 of 19**. Three carried false load-bearing facts: **SQ-273**'s "14 griefing flips" is **62** in the ratified artifact (14 was a superseded N=3000 run) and its "thin-capture/gate-suppression" label is wrong (displace-and-hold 29 / thin-capture 21 / belief-capture 12, zero baseline-suppression); **SQ-269** dropped **decidable-harm** from its quote of 15 §4.9, the qualifier separating a passable gate from one CODE (1.99 %) and META (2.22 %) fail; **SQ-227** asked for a home for I-13's fuzz obligation when I-13 already had a ≥10⁶-case property suite against an exact-rational oracle (landed in S1 the same day the S2 reviewer raised it, different worktree) that had itself found and fixed the `pow_1e9` drift defect — so 15 §1's I-13 Verification column was corrected away from "fuzz" instead of inventing an obligation. Six more corrected: SQ-226 (pin stable2603 → stable2606; "cannot link" → deliberately does not), SQ-157 (`frn.window` is 13 §3.4 and an alias of the covered §2 `dec.extension`), SQ-149 (`13 §5.1` does not exist; 09 §7.3 unrelated), SQ-229 (13 has no every-value-needs-a-row rule — that is AGENTS.md; and the value is not derived from 06 §2.1), SQ-265 (`MIGRATION_FAILURE_HALT` is not an input to the union), SQ-268 (timing premise stale and inverted — the producer had frozen and passes). **Refused: SQ-158** split — `grd.bond` ratified, `dis.merit_min` → batch **X**, since ratifying the hardcoded `B_1` equality would silently delete an independent values-layer raise that 07 §12 *and* 13 §1's metadata both assert. **New finding:** `UndecidingTimeout` == the entrenched track's 7-day prepare period exactly; verified benign under `pallet-referenda`'s `!in_queue` exemption but pinned in 13 §3.4 as `UndecidingTimeout ≥ max(track prepare period)`. **18 amendments** across 15 (§1 I-13/I-22, §4.5, §4.6, §4.9), 13 (rules 6/7, §1 notes, new §3.4 row), 09 §7.1, 12 §6.3; one `fuzz/README.md` citation fix (SQ-135 → SQ-225). One subagent finding **rejected as a false positive**: "01 §9 is a broken citation" — 01 §9 spans 258–284 and line 279 *is* the normative pinning regime, cross-referencing 15 §4.5 back. **Dual review (native `spec-reviewer` 1 blocker/1 major/7 minor + adversarial Codex 2 blockers/3 majors) — all fixed in-session.** The blocker was self-inflicted: appending the SQ-194 rationale to 13 rule 6 **broke the G0 Phase-0 gate**, whose extractor bound the class-suffix list with a greedy `$`-anchored `(.*)` and read the prose as phantom suffixes (`tools/phase-gates/tests` red). Fixed structurally + regression test. Codex found **two over-ratifications in my own amendments**: the unseeded-rows note claimed constants-API satisfaction for `grd.bond` **/ allowances** when only `GuardianBond` is metadata-exposed (narrowed to the bond; allowance exposure left explicitly unresolved), and the sentinel note claimed "every inflow is admissible" when `phase3.tvl_cap`/`phase3.dep_cap` gate independent checks (containment lifts only with both). Codex also exposed a real cross-doc contradiction the batch surfaced: **05 §5.2** asserted `gate.v_min = 0.1 · dec.v_min` as an identity while the key is amendable within the band — 05 §5.2 amended to bind the live value. Majors: `sec.prize` is 3 keys not "per binding class"; the I-22/§4.6 CI claim narrowed to what CI actually establishes (classification + enabled marker + lexical binding), with "drives a dispatch past it" kept as the review half; enforcement unit is the independent *dispatch limit*; the fuzz MUST scoped to model-vs-production shapes (LMSR fuzzes the production path); TM-18 → canonical TH-6. **One reviewer finding rejected as a false positive** (`01 §9` is not a broken citation — line 279 inside §9 *is* the pinning regime, cross-referencing 15 §4.5 back), and one reviewer restatement was itself wrong (the artifact is 0.000/0.145/0.135/0.563 % decidable-harm, not "0.000000 for all four"). PLAN staleness trued: registry 175 → 179 keys / 0 unwired / 98 seeded (179 on the merged base — a sibling batch added a key); AGENTS.md's stale S4 line ("PARAM 3.46 %/TREASURY 1.52 % fail") replaced with the committed artifact's passing per-class figures. Open **149 → 131**, resolved 157 → 175 (counts on the merged post-B2/B3/B4/B5 base, per `tools/ci/check-spec-question-batches.py`); B1–B6 are now all closed. **Not docs-only after review:** the phase-gate regex fix + its regression test, and a new runtime test machine-checking the 13 §3.4 `UndecidingTimeout` constraint. **Merged post-B2/B3/B4 main** (siblings #115/#116/#118) — resolved per-row rather than per-side: rows the siblings resolved (SQ-119, SQ-193, SQ-225) keep their text, B6's keep mine, and the batch index takes main's D/C/X reclassifications with SQ-158 added to X; B2's new 09 §1.2(11) decode-depth pin reinforces this batch's 15 §4.5 fuzz-boundary rule. **Merged post-B5 main** (#121) — SQ-264 takes B5's resolution, SQ-265/SQ-266 keep B6's; B5's new 12 §6.3 *Row semantics* block covers disjoint rows (keeper budget, intake queue, keeper inactivity, ownership, ReleaseChannel lag, served-state window) so there was no contradiction, but two adjacent parallel "what the terse table left implicit" lists was poor structure — B6's two bullets were **folded into B5's list** with their SQ tags preserved, leaving one coherent block. With B6 closed, **all six ratify batches B1–B6 are done.** **Codex PR review of #120 then landed one valid P2, fixed:** the unseeded-rows note had made `[VERIFY]` status the exemption, contradicting the sentinel paragraph beneath it (the phase-3 caps are tagged *and* seeded) and blessing the derived-at-consumer shape SQ-158 had just refused. Re-derived the discriminator from `genesis-keys.json`/`registry.toml` — it is whether the value **gates a dispatch** — and rewrote the note, adding derivation-is-a-defect so SQ-158's refusal is now normative text. Gates: `cargo fmt --all --check` · `cargo clippy -p bleavit-runtime --all-targets -D warnings` · runtime lib **230 passed/0 failed** · phase-gates **69** · limit-coverage 179/179 (0 unwired) · PLAN tables · doc links · runbooks

**What comes next:** **All six ratify batches are closed.** What remains: D (doc-truing, 30), C (contract v6, 32 — SQ-228's `TxExtension` migration was assigned there), X (code, 69 — led by SQ-205/SQ-263/SQ-261 and the adoption-input family)
