# Session log — 2026-07-25

## A13 (🔨) — R-6 round 3; SQ-195

**Round 3 caught a defect the previous round's "fix" introduced, for the third time running — and this one was a reversal of a correct behaviour, so it is the most important finding of the session.** Round 2's reviewer read `reference-model`'s `decision.decide` signature, saw no `upgrade_payload` argument, and concluded the oracle applies 08 §5.2's `trs.cap_proposal · NAV` floor to **every** CODE/META proposal. That finding was accepted, and the runtime plus 08 §5.2, 05 §5.6 and 13 §1 row 87 were all changed to match. **It was wrong.** The engine expresses the condition *through its caller*: `decide` passes `spendable_nav` straight through, so a caller signals "not an upgrade" by passing `spendable_nav = 0` — precisely what the 15 §4.9 Phase-0 simulation engine does for non-upgrade CODE/META proposals (`engine.py`, `proposals.py`: a governed `upgrade_payload_fraction`, 50 % by default), and the published artifact the `sec.prize.*` defaults were adopted from contains **20 `upgrade_payload: false` cases**. So the unconditional reading silently invalidated the evidence the adoption itself rests on. Reverted: the runtime's `carries_upgrade_payload` is restored, all three spec sites say "for upgrade payloads" again, and the convention is now documented **at the divergence source** (`decision.py`) with an explicit "do not 'fix' this into an unconditional floor" note, since two independent reviewers have now misread that signature. The differential pin asserts **both** sides — non-upgrade **200**, upgrade **500** — using real `system.remark` and `system.authorize_upgrade` preimages, so neither direction can regress unnoticed. **The process lesson is worth more than the fix:** a reviewer finding is evidence, not a verdict, and this one was accepted without checking what the *callers* of the oracle do. The session's own earlier claim of having "verified" the change was also overstated and is corrected in the SQ-173 row. **SQ-195 implemented** on top of that: a `ReserveProbeDaily` double map keyed exactly like `XcmTraffic`, sharing its bounded epoch index and retired by the same cursor-bounded walk (I-20/I-21); an oracle `probe_outcome` seam fired on every scored slot — success response, error response, folded timeout, and folded no-attempt slots; day attribution through the established `xcm_traffic_epoch_and_day` precedent; and the runtime `R` binding, which is **unavailable before `ReserveProbeArmed`** and only then scores 1 on a recorded pass, 0 otherwise. That arming gate is the row's own warning honoured: scoring absence as 0 on an unarmed chain would set the C breach flag out of a mechanism that never ran — fail-destructive, not fail-safe. A failed day stays failed (recovery is never retroactive), and the epoch projection for 05 §4.4's `C_e` is the **minimum over recorded days**, the only aggregation consistent with 07 §8's fail-static rule. Runtime **346**, welfare **64**, oracle **113**, fmt clean, docs and phase-gate checks green.

**What comes next:** Re-run the exhaustive gate and a fourth R-6 pass, then SQ-234, the SQ-174 keystone and its dependents (SQ-296 → SQ-294/295), SQ-141/175/181/341, SQ-461, SQ-303.

## A13 (🔨) — R-6 round 4

**Fourth consecutive round to find something real, and all three findings were in this session's own SQ-195 code.** **BLOCKER — a partially-covered epoch read as healthy.** The first epoch projection was the minimum over *recorded* days, so an epoch with one passing day and every other day unrecorded returned `R = 1`: exactly the "absence is never healthy" violation 07 §8 forbids, and it would have raised settlement-time `C_e` (05 §4.4) on an epoch the probe barely ran in. Replaced with a **cover-complete** projection — healthy only if every probe day the epoch should have had recorded a pass, with the expected count derived from the epoch's own length so a governed `epoch.length` change cannot silently shrink the requirement. Pinned by `sq195_epoch_projection_requires_complete_day_cover`, verified to fail against the defective version (`1` where the fix gives `0`). **MAJOR — wrong day attribution.** Outcomes were attributed to the *answering* block, so a probe opened before a day boundary and answered after it landed on a day it never measured while its own day stayed unrecorded. The seam now carries the attempt's **opening** block, captured before the core clears `pending_since`. **MAJOR — try-state violation on correct state.** The writer indexed an epoch in `XcmTrafficEpochs` without creating an `XcmTraffic` row, while `do_try_state` required every indexed epoch to have traffic — so a reserve-only outcome (a budget refusal happens *before* the router is ever observed) would have failed try-state on state that is entirely valid. The invariant now accepts either backing and validates `ReserveProbeDaily` in both directions. The reviewer also observed that a multi-day outage collapsed into one record, losing day identity; each folded slot now records its own day, bounded by `MAX_FOLDED_PROBE_SLOT_RECORDS`, and truncation is safe precisely because the cover-complete projection already fails unrecorded days. One further self-inflicted defect surfaced in the gate: inserting the new helper directly above `metric_components` captured the `#[allow(dead_code)]` that belonged to it, leaving that function dead under `runtime-benchmarks`; both now carry their own attribute. **Rounds 2, 3 and 4 each found a defect introduced by the previous round's fix, and two of those were mine** — the cadence is doing exactly what R-6 exists for, and every one of these would otherwise have shipped. Runtime **347**, welfare **64**, oracle **113**, gate-equivalent clippy clean.

**What comes next:** Exhaustive gate, then a fifth R-6 pass. A13 stays 🔨 until a round returns clean. Then SQ-234, the SQ-174 keystone (→ SQ-296 → SQ-294/295), SQ-141/175/181/341, SQ-461, SQ-303.

## A13 (🔨) — R-6 round 5

**Fifth consecutive round to find real defects, all three again in SQ-195.** **BLOCKER — counting passes is not covering days.** `passed >= expected_days` was satisfiable by passes recorded on out-of-range days while the epoch's own probe days went unprobed: "absence is never healthy" defeated by arithmetic rather than by a missing check. The projection now walks `first_day..last_day` and requires each day to carry a recorded pass. Pinned by `sq195_projection_covers_days_not_just_a_pass_count`, verified to fail against the count version (`1` where the cover check gives `0`). **MAJOR — cross-epoch misattribution.** `epoch_and_day_at` clamped an opening block that predates the live epoch to day 0 of the *current* one, which an earlier comment in this session wrote off as "conservative". It is not: it records a failure against a day the probe never measured **and** leaves the real day unrecorded. It now returns `None` and records nothing, letting the cover check fail the true epoch on its missing day — genuinely fail-static. **MAJOR — pre-arm days counted as required.** 07 §8 scores **zero** pre-arm slots, but the measured range started at day 0, so an epoch in which the probe armed late could never read healthy however completely its post-arm days passed. A new `ReserveProbeArmedAt` records the latch block and the range starts there. One reviewer claim was **stale** — it read `8ddab75`, before the vacuity floor landed, so "zero coverage returns healthy" was already fixed; that was checked rather than accepted, which is the lesson round 3 taught about treating findings as evidence rather than verdicts. **Separately, one defect was found by inspection rather than by review or suite:** `expected_days = length / BLOCKS_PER_DAY` is zero for any epoch shorter than the frozen 14,400-block day — the shape the default-off `fast-timing` build produces — so `passed >= 0` reported an epoch healthy with no probe recorded at all. Release timing never reaches that branch, but the G1 drills run the compressed runtime and would have been handed false confidence. Floored at one day and pinned. **SQ-195 has now needed four corrective passes**; every version was more correct than the last and every defect was invisible to a fully green suite, which is the argument for the cadence rather than against it. Runtime **349**, oracle **113**, welfare **64**, exhaustive gate green (90 suites, 0 failures).

**What comes next:** A sixth R-6 pass. A13 stays 🔨 until a round returns clean. Then SQ-234, the SQ-174 keystone (→ SQ-296 → SQ-294/295), SQ-141/175/181/341, SQ-461, SQ-303.

## A13 (🔨) — probe-migration latch hygiene

**Found by inspection while R-6 round 6 ran.** `MigrateOracleReserveProbeV1` kills `ReserveProbeArmed` but did not kill the new `ReserveProbeArmedAt` (SQ-195), leaving a measured-range start from a probe generation the migration had just retired. Harmless in effect — `R` renders unavailable while unarmed, so the stale block is never read before the next arming overwrites it — but it contradicted the migration's own stated contract ("leave `ReserveProbeArmed` false so the next readiness-qualified crank establishes the real cadence"). Both latch components are now cleared together, the weight accounts for the second write, and `post_upgrade` asserts the clean slate rather than assuming it. Runtime **357** under `try-runtime`; no other suite affected.

**What comes next:** Round 6's verdict, then the exhaustive gate and push. A13 stays 🔨 until a review round returns clean.

## A13 (🔨) — R-6 round 6

**Sixth round; three findings, all in SQ-195, one of them introduced by round 5's own fix — and then this session's own new test found a seventh the review had missed.** **BLOCKER — an empty measured range returned `Some(true)`.** Round 5's fix reasoned that a probe arming inside an epoch's trailing partial day measured no completed cadence slot, "so there is nothing to score", and returned healthy. That is the same absence-is-healthy defect in a new guise: `R = 1` for an epoch the probe never measured. It now returns `None` — not measured — exactly as an unarmed chain does, so the crank fails status-quo-safe. **MAJOR — pre-roll passes were discarded.** Round 5's fix returned `None` for any opening block predating the live epoch, which throws away a real, timely pass. `EpochTimings` retains prior epochs, so attribution now searches the ring: neither clamping to the wrong day nor dropping the outcome, both of which shipped earlier in this session. **MAJOR — upgrade compatibility.** A chain armed under a runtime predating `ReserveProbeArmedAt` has no latch block; the fallback treats the whole epoch as measured, which requires *more* coverage rather than less, and is now documented as the deliberate conservative direction. **Found by this session's own regression, not by the review:** `metric_components` flattened the **unavailable** signal (`None`) to a breach (`0`), which would have settled gate books as breached out of an epoch the probe never measured. Unavailable and breached are distinct facts and the seam now keeps them so — an unrecorded *day* is still a fail per 07 §8, resolved by the daily caller, while an unmeasurable *epoch* is absent from the component set entirely. **Standing back: five of six rounds have found defects in one function.** `reserve_probe_epoch_value` has been rewritten four times; every version was more correct than the last and every defect was invisible to a fully green suite. The rest of the branch — SQ-173, SQ-383, SQ-197, SQ-318, SQ-319, SQ-382 and the three ratifications — has been stable since round 3 and has passed the exhaustive gate repeatedly. If a further round is not clean, the right move is to split SQ-195 onto its own branch rather than let one metric projection hold eight resolved rows. Runtime **351**, oracle **113**, welfare **64**, exhaustive gate green (90 suites, 0 failures).

**What comes next:** Round 7. If clean, mark PR #159 ready and flip A13 ✅; if not, split SQ-195 out and land the settled work. Then SQ-234, the SQ-174 keystone (→ SQ-296 → SQ-294/295), SQ-141/175/181/341, SQ-461, SQ-303.

## A13 (🔨) — R-6 round 7

**First round with `BLOCKER: none`.** The reviewer independently confirmed the three properties the previous rounds had churned on: `epoch_and_day_at` maps retained epochs correctly and returns `None` for gaps and aged-out epochs; `reserve_probe_epoch_value` cannot return `Some(true)` unless every day in its measured range is recorded `true`; and an absent component makes snapshot aggregation fail with `MissingComponent` — verified independently here too, since that is what makes dropping `R` fail the crank status-quo-safe rather than silently computing `C_e` without it. **One MAJOR, correct and narrow:** the pre-arm exemption bound the *epoch* projection but not the *daily* one. `daily_components` converted every unrecorded day to `R = 0` on the strength of the **global** armed latch, which says the probe is armed *now* — not that it was armed on the day being scored — so a late daily crank could classify time before the mechanism existed as a reserve outage and set a `C_daily` breach flag from it (07 §8; 15 I-24). Both granularities now share one rule via `reserve_probe_daily_value`. A design consequence worth recording: for an epoch whose timing has aged out of the retained ring, the daily value is **unavailable** rather than a failure, because pre-arm days cannot be distinguished without timing — the honest answer, and status-quo-safe. Seven SQ-195 regressions now pin the arming gate, day resolution, non-retroactive failure, complete day cover, sub-day vacuity, count-vs-cover, the pre-arm exemption at both granularities, and the unavailable-vs-breach distinction. Runtime **352**; exhaustive gate running against this state.

**What comes next:** Push, then round 8. With blockers cleared, A13 is in reach and the SQ-195 split contingency looks unnecessary. Then SQ-234, the SQ-174 keystone (→ SQ-296 → SQ-294/295), SQ-141/175/181/341, SQ-461, SQ-303.

## A13 (🔨) — R-6 round 8

**No blockers for the second round running; two MAJORs, and this session had analysed that exact asymmetry last round and wrongly concluded it was unreachable.** The reviewer found the path missed: `MigrateOracleReserveProbeV1` clears the arming latch but **not** `ReserveProbeDaily`, so a **retired probe generation's** outcomes survive into the next one. Combined with the daily rule consulting the *record* before the *range*, that let a stale or out-of-range day manufacture a false `C_daily` breach — a keeper submitting day 200 of a 21-day epoch scored `R = 0` (07 §8; 05 §4.4/§4.7). Both findings share one root, so the fix is structural rather than another patch: the measured range is **single-homed** in `reserve_probe_measured_range` and *both* granularities consume it, with the range deciding membership before any record is read. The two rules now agree by construction instead of by inspection — which is exactly what failed. The migration additionally clears the day outcomes alongside the latch, with a `post_upgrade` assertion, and its weight accounts for the extra write. **Process note worth keeping:** twice this session a "not reachable in practice" judgement was wrong. When two paths must agree, making them *share* the deciding logic beats reasoning that they coincide. Eight SQ-195 regressions now pin the behaviour; the newest was verified to fail against the superseded record-before-range ordering (`Some(0)` where the fix gives unavailable). Runtime **353**, **359** under `try-runtime`; exhaustive gate running against this state.

**What comes next:** Push, then round 9. Two consecutive blocker-free rounds put A13 in reach. Then SQ-234, the SQ-174 keystone (→ SQ-296 → SQ-294/295), SQ-141/175/181/341, SQ-461, SQ-303.

## A13 (🔨) — pre-round-9 tidy

Removed `pallet_welfare::reserve_probe_epoch_tally`: after round 8 single-homed the measured range, it had become an unused public API that read `ReserveProbeDaily` **without** that range check — precisely the foot-gun round 8's two MAJORs exploited. Its only remaining users were two assertions in the runtime welfare-input tests, re-pointed at the range-respecting `reserve_probe_daily` reader. The store now has exactly **one** production consumer, `reserve_probe_daily_value`, and the epoch rule reads through the same range, so no caller can bypass it. Runtime **353**, welfare **64**, fmt clean.

**What comes next:** Round 9's verdict (it explicitly asks whether A13 may be marked ✅, with SQ-303/SQ-486/SQ-186 named as out-of-scope successors so the reviewer can object if it disagrees), then the exhaustive gate and push.

## A13 (🔨) — R-6 round 9

**Third consecutive blocker-free round; two MAJORs, and both came from this session's own earlier fixes interacting.** **The sub-day floor fought the pre-arm exclusion.** The vacuity floor (`last = max(1, …)`) landed in one round and the pre-arm range start in another; together, for a sub-day epoch with `armed_at` past its end, `armed_at` still floor-divides to day 0 — so the floor manufactured a required day 0 and reported an **entirely unmeasured epoch as failed**, i.e. settleable on data that never existed (07 §8; 05 §4.4). The unmeasured case is now decided *before* the floor applies; verified to read `failed` without the guard and `unmeasured` with it. **The migration weight was understated by orders of magnitude:** `ReserveProbeDaily::clear(u32::MAX, None)` can remove a full retained-epoch window × 256 day keys, but six flat writes were charged and the removal count discarded (15 I-20; 09 §1.1). It is now charged from the actual `MultiRemovalResults`. The MINOR was the tally accessor already removed in the working tree — a fair catch, since the review ran against a commit that did not include it. **Two of this session's own fixes combining into a third defect is the clearest signal that this projection accumulated complexity faster than it was being reasoned about**; nine regressions now pin it, each verified against the specific defect it guards. The reviewer also confirmed explicitly that **SQ-303, SQ-486 and SQ-186 are appropriately outside A13 scope**, so none of them holds the milestone. Runtime **354**, exhaustive gate green (90 suites, 0 failures).

**What comes next:** Round 10. Three blocker-free rounds and an explicit scope confirmation put A13 within one clean round. Then SQ-234, the SQ-174 keystone (→ SQ-296 → SQ-294/295), SQ-141/175/181/341, SQ-461, SQ-303.

## **A13 ✅** — R-6 round 10

**`A13 MAY BE MARKED DONE` — 0 blockers / 0 majors / 0 minors / 0 nits.** The reviewer verified `reserve_probe_measured_range` across every requested boundary (`armed_at` exactly at `epoch_end`, inside the trailing partial day, before `timing.start`, absent; `length` an exact day multiple and one block over), confirmed the daily and epoch projections **cannot disagree** because they share that range, and confirmed the migration clear is bounded by 21 retained epochs × 256 day keys and now charges its actual removal reads/writes (15 I-20; 09 §1.1). A13 is flipped ✅ with the exhaustive Rust gate green on the final state. **Ten rounds is the story of this milestone.** Rounds 2–9 each found something real, and the review's value was not evenly spread: it caught a chain-bricking recovery-lane blocker (the SQ-383 gate also bound `TerminalRecoveryTransition`, where nothing can fund NAV), a genesis-only migration that would have failed every PARAM/CODE/META proposal forever on an upgraded chain, a rounding divergence from the reference model, and a case where a fix of this session's **reversed a correct behaviour** and silently invalidated the Phase-0 calibration the adoption rests on. Three later defects came from two of this session's own earlier fixes interacting rather than from the original design. Nine SQ-195 regressions pin the result, each verified in both directions against the specific defect it guards. Two process lessons are recorded in the rows themselves: **a reviewer finding is evidence, not a verdict** (round 2's blocker was a misread signature, accepted too readily), and **when two paths must agree, share the deciding logic rather than reasoning that they coincide** (twice a "not reachable in practice" judgement here was wrong).

**What comes next:** Mark PR #159 ready. Then SQ-234 (epoch-bound entrenched enactment), the SQ-174 keystone (frozen `StakeAtRisk` → SQ-296 → SQ-294/295), SQ-141/175/181/341, SQ-461, and SQ-303's artifact/verifier. SQ-486 and SQ-186 await user decisions.

## A13 (✅) — Codex connector review on PR #159

**Two P1 findings from the connector bot, both genuine, both fixed** — and the process failure is worth recording alongside them: the PR was marked ready and the session moved on without ever checking for the review, so the comments sat unnoticed. A standing instruction now covers it (poll for the connector review in parallel and resolve genuine findings before treating a PR as handed off). **P1 — the effective probe outcome was not propagated.** `reserve_probe_result_with_params` deliberately converts a success into a **failure** when it lands at or after `res.probe_timeout` or while the live probe configuration is structurally invalid, but it returned `Result<(), _>` and the pallet forwarded the caller's *reported* `passed` to the welfare sink. Welfare would then have recorded a passing day for a day the health state had already scored a fail (07 §8) — the two records disagreeing about the same slot. The core now returns the effective outcome and the pallet forwards that; pinned by `reserve_probe_result_returns_the_effective_outcome`, verified to fail when the core returns `passed` instead. **P1 — the folded-slot loop's writes were unweighted.** A keeper returning after an outage folds up to `MAX_FOLDED_PROBE_SLOT_RECORDS` (32) slots, each invoking the runtime sink and mutating two welfare items, while `crank_reserve_probe` declared a fixed generated weight measured on the single-invocation timeout path — up to 64 unaccounted reads/writes (15 §4.5). The declared weight now carries the full worst-case addend and the dispatch refunds the slots it did not record via `PostDispatchInfo`, following this runtime's own `execute` pre-charge/refund precedent so a healthy crank is not priced at an outage's cost. **Also self-inflicted and recorded:** the SQ-174 exhaustive gate run was invalidated by editing `pallet-oracle` while it was in flight — the same "never edit tracked files while a job runs in this tree" rule that memory already carries for Codex jobs applies to the gate. That run's result is void and the gate is being re-run on the coherent state. Runtime **355**, oracle **113**, oracle-core **25**, fmt clean.

**What comes next:** Re-run the exhaustive gate on the coherent state, push, and reply in both PR threads. Then SQ-234 (needs a real epoch-boundary enactment mechanism — its ruling forecloses the block-denominated shortcut), SQ-141/175/181/182/341, SQ-461, SQ-303. SQ-296 (and behind it SQ-294/295) plus SQ-486 and SQ-186 are decision-gated, not code-gated.

## — (dependency batch, unplanned)

**Resolved all six open `chore(deps)` PRs (#160–#165) as one consolidated batch.** Diagnosis first: the single red job on every one of them (`Weight regression`) was inherited, not caused — their runs predate `60495b5` (#159) by six hours and were checked against a main that still held 15 acknowledgements, 4 already expired for any branch cut from it; #159 pruned the acks file to zero, so rebasing is the whole fix. Consolidated because all five Cargo PRs rewrite the same `keeper/Cargo.lock` with overlapping transitive edits, making sequential merges a chain of forced rebases at ~2.5 h of CI each. One keeper-workspace `cargo update` reproduced the exact union of the five targets (anyhow 1.0.104, serde 1.0.229, clap/clap\_derive 4.6.4, futures 0.3.33 family, tokio 1.53.1, `syn 3.0.3` added), all within existing caret ranges; #160's `actions/setup-python` 6→7 rode along across all 10 call sites (v7's only breaking change, the `pip-install` input, is unused here). Root `Cargo.lock` untouched. Local evidence: keeper `cargo metadata --locked` + `fmt` + `clippy -D warnings` + 56 tests green; supply-chain gate green; weight-regression gate green against the new main. **Then, on user request, fixed the recurrence at source:** all three `.github/dependabot.yml` ecosystems now carry a catch-all `groups:` block, so the bot batches its own PRs from next week's run onward; `applies-to` stays at its `version-updates` default so security bumps remain individually reviewable (R-7), and both `ignore` lists are untouched.

**What comes next:** Await CI on the consolidated PR, merge it, then close #160–#165 as superseded; first grouped Dependabot run due the week of 2026-07-27 (keeper cargo + github-actions, weekly) — confirm it opens one PR per ecosystem, not per dependency

## — (SQ-486; post-#159 merge)

**PR #159 merged as `60495b5`** — A13 and its 21 disposed rows are on main, both Codex connector P1 fixes included. Work continued on a **fresh branch off the new main** rather than stacked on the merged one (the squash-merge would have orphaned the base and re-inflated the diff), and the post-merge weight-regression check was run as the standing rule requires: **PASS**, with no acks to expire because #159 removed all fifteen stale ones. **SQ-486 decided and adopted at ×16, and the row's own recommendation was framed backwards.** It called ×7 "the conservative reading" and asked for a user decision; the artifact inverts that. The 15 §4.9 security criterion was met **at the ×16 calibration** (`violations: []`, all four classes clean), from the 0.995 quantile of min-book contest capital over decision-pair depth `2b` — 15.637 over 4,303 samples — while 13 §1 says of ×7 that "below ×7 the ceiling could reject honest exactly-grade proposals". Shipping ×7 meant shipping an unvalidated value at a known liveness hazard: **conservatism on this key buys no validated safety.** Adopted with a **×32 max**, because the unsafe direction is *upward* and the amended R-2 forbids leaving it unbounded. **Two rules amended by explicit user instruction and applied immediately:** R-2 now permits a genuinely required new parameter, earned in order — prove necessity by enumerating existing keys and preferring reuse, *derive* the value from a kernel constant/existing key/published evidence, state which invariants move at each bound and which error direction is unsafe, then record it; and R-1 now says to **decide and proceed** on values, rulings and contract bumps rather than escalating. The first rule immediately caught an error of my own: **SQ-296 was never decision-blocked** — 07 §7 requires §6 value-scaled bonds and §6.1's formula already uses `orc.bond_bps`, live at 250 bps, so the rate existed one section over and no new key is needed. That correction unblocks SQ-296, SQ-294 and SQ-295 as code. Also corrected: the limit-coverage registry called `sec.flow_cap` `diagnostic` on a reason the SQ-231 amendment falsified; reclassified, and the `diagnostic` class is now empty. Constitution **60**, runtime **355**, limit coverage **186/186**.

**What comes next:** Gate and push SQ-486. Then SQ-186 (contract v12→v13, decided), SQ-296 → SQ-294/295, SQ-141/175/181/182/341, SQ-461, and the two large mechanisms SQ-234 and SQ-303.

## — (SQ-486 + SQ-186)

**Two rows decided rather than escalated, per the amended R-1.** **SQ-486** adopts `sec.flow_cap` at the Phase-0 calibration ×16 with a ×32 max; the row's "×7 is conservative" framing was backwards, since the 15 §4.9 criterion was met **at ×16** (`violations: []`) while 13 §1 says ×7 "could reject honest exactly-grade proposals" — conservatism there bought a liveness hazard, not validated safety. **SQ-186** ships as **integration contract v13**: `Epoch::TreasuryBondAskBps` exposes `kernel::TREASURY_BOND_ASK_BPS = 50` on 02 §9's frozen list, purely additive so `transaction_version` is untouched (§13 rule 7). Its regression asserts the **live runtime metadata** rather than the Rust constant, because a frontend hardcode is precisely what the exposure exists to prevent. Version bumped across primitives, the five 02 references, the v13 history entry, the release surface manifest and its test, all four Rust assertions and the derived design pack (whose `prop.bond` row now names the constant and says not to hardcode the slope). **Process failure worth recording, because it was the third repeat:** the running exhaustive gate was invalidated *again* by editing tracked Rust files mid-run — this time while explicitly narrating "holding cargo until it finishes", which confused *not invoking cargo* with *not changing its inputs*. The rule is about the tree, not the command. The void run was killed rather than waited out, and `pkill -f cargo` proved too broad: it also killed every stale waiter loop and left two orphaned profile builds alive, which had to be reaped by PID before a clean re-run — the same orphan pattern memory already records for Codex jobs. Runtime **356**, pallet-epoch **104**, constitution **60**, primitives **21**, release tooling **86**, docs and batch-index checks green.

**What comes next:** One exhaustive gate on the coherent state, then push. Then SQ-296 (using `orc.bond_bps` as 07 §7 prescribes) → SQ-294/295, SQ-141/175/181/182/341, SQ-461, and the two mechanisms SQ-234 and SQ-303.

## — (SQ-486 + SQ-186; Codex connector review on PR #166)

**Exhaustive gate green (exit 0) on the SQ-486 + SQ-186 state, PR #166 opened, and this time the connector watch was armed at the same moment** rather than after the fact — the standing instruction worked, the review landed on the second poll. **Four findings, all four verified genuine, all four fixed.** **P1 — `sec.flow_cap` was unreachable on an upgraded chain.** 13 §1's *Existing-chain introduction of new rows* is normative and I shipped the genesis row without its migration; `MigrateConstitutionSecurityPrizeV3` seeds only `sec.prize.*`. The failure mode is quieter than SQ-173's and so worse to leave: `sec_flow_cap_1e9` falls back to the compile-time default table, so the gate kept computing the correct ×16 ceiling and nothing looked broken — while `Params` held no record, leaving a row `FutarchyApi::params` cannot surface and `amend_registry` cannot move inside its ×7–×32 bounds. A gate-bearing ceiling governance cannot reach is not a governed parameter. Added `MigrateConstitutionSecurityFlowCapV4` (insert-if-absent, Constitution `3 -> 4`, try-runtime pre/post) in all three profile tuples, advanced the pallet's in-code `STORAGE_VERSION` to 4 so migrated and freshly-genesised chains agree, and added the **try-state invariant** that makes the class non-reintroducible — `do_try_state` now requires the record, precisely *because* the consumer degrades gracefully and a test on the derived ceiling would pass either way. Three regressions assert on the **record**, not the ceiling. **P1 — the release surface manifest still encoded contract v12** (`0x0c000000`) after the top-level version moved to 13, which would fail `metadata_surface_coverage`. The reason the suite stayed green is the lesson: `test_surface_manifest.py` pinned the same stale literal, so test and manifest agreed with each other and neither matched the runtime. The assertion now **derives** the expected SCALE bytes from the manifest's declared version. **P2 — `TreasuryBondAskBps` had no manifest entry**, so a release could pass the surface check with the exact v13 addition absent; added as `constant.epoch.treasury_bond_ask_bps`. **P2 — AGENTS.md still said v12** in two places including "the next bump is v13"; now v13 / next v14 (R-3). **One defect the review did not name, exposed by the first:** `sec_flow_cap_1e9`'s doc comment still claimed the row was "deliberately absent from the seeded genesis registry", falsified by SQ-486 — rewritten. A test-count discrepancy was chased rather than assumed: `cargo test -p bleavit-runtime` reports 359 while the gate reported 363, and the cause is `--workspace` feature unification enabling `try-runtime` (366 = 363 + these three), so the figure reported for #166 was correct in its context. Constitution **60**, runtime **359** / **366** with `try-runtime`, release tooling **86**, fmt + clippy clean, and a full feature-combination sweep over bootstrap/phase-four/recovery × try-runtime × runtime-benchmarks (the two apparent failures are pre-existing deliberate `compile_error!` guards against invalid profile combinations).

**What comes next:** SQ-296 on its own branch off main: value-scale the filing bond at creation via §6.1's `orc.bond_bps`, exposure = every cohort consuming the measurement epoch for Incident \(`I` is a welfare **multiplier**, not a `MetricId`\), Milestone fail-closed until SQ-175 binds its component. Then SQ-294/295, SQ-141/175/181/182/341, SQ-461, SQ-234, SQ-303.

## SQ-296 (spec half shipped; code in flight) — A12

**SQ-296's spec half is complete; the code is being implemented and this row is a clean park, not a claim of done.** An independent read-only audit was run *before* writing anything, and it confirmed all five design premises with citations while surfacing three amendment sites I had not found: 15 I-28's custody wording also called the filing bonds "flat", 07 carries its own parameter-summary row with the flat amounts, and — the useful one — **11 §300 already reads "filing bond balance (value-scaled per 07)"**, so the frontend workflow doc was written for this regime and needs no change. That is independent corroboration of the ruling rather than my own reasoning echoed back. The audit also found there are **two** `required_bond` functions (a private one in `registry-core` with a single caller, and a separate pallet helper), and that `pallet-registry`'s `Config` has **no** associated type supplying an exposure quantity — so the seam is new work, added as a kind-aware `EpochContext::cohort_exposure(kind, epoch) -> Option<Balance>` plus `RegistryParams::bond_bps()`. Amendments landed across 07 (a new normative filing-bond note carrying the formula, the exposure sets and the freeze rule; the `file`/`challenge_filing` cells; the fixed-window and §12 merit-floor rationales re-based; errors and both audit-concern lists; the §7 parameter summary), 13 §1 (the `reg.bond_*` role change and `orc.bond_bps`'s second consumer) and 15 I-28. Ruled and recorded: **no contract bump** — 02 §7 freezes registry storage and events, not errors, so `ExposureUnavailable` rides along as a trailing variant, and trailing is mandatory since shifting error indices is metadata-visible. Doc links clean; limit coverage **186/186** with `orc.bond_bps`'s registry consumer recorded. **Also this session:** PR #166's connector review produced four findings, all four verified genuine and fixed — the sharpest being a real 13 §1 violation \(a newly consumed key shipped without its existing-chain migration\), whose failure mode was *quiet* because the consumer falls back to the compile-time default, so the gate stayed correct while `Params` held no record. Closed with `MigrateConstitutionSecurityFlowCapV4`, a `STORAGE_VERSION` bump to 4, three regressions asserting on the **record** rather than the derived ceiling, and a new try-state invariant. Two defects the review did not name were found alongside: a doc comment SQ-486 had falsified, and a manifest test that pinned the same stale hex as the manifest — so the suite was green while neither matched the runtime. **Main moved to `3621bb9`** (PR #167, Dependabot batch: keeper bumps, setup-python v7, Dependabot grouping); it touches only workflows, `keeper/Cargo.lock`, dependabot config and PLAN, so no conflict with either branch, but both need a rebase and a fresh weight-regression check. The rebase was deliberately **not** done yet: a Codex job is writing code files in this worktree, and yanking the tree out from under a running job is the same mistake that voided three gates earlier today.

**What comes next:** Review Codex's SQ-296 diff (verify the error variant is **trailing**, the rounding test would fail under `div_floor`, and the freeze path is untouched), rebase both branches onto `3621bb9`, re-run weight regression, then one exhaustive gate and a PR. Then SQ-294/295, SQ-141/175/181/182/341, SQ-461, SQ-234, SQ-303.

## SQ-296 (code shipped, **not closed**) + SQ-489 raised —…

SQ-296 (code shipped, **not closed**) + SQ-489 raised — A12

**SQ-296's implementation is in and all seven required regressions pass, but the row stays open on a weight defect the review found — and no gate would have caught it.** Delegated implementation returned the formula matching `oracle_core::round_bond` exactly (`checked_mul` → `div_ceil(10_000)` → `max` after rounding), a kind-aware `EpochContext::cohort_exposure` seam, `RegistryParams::bond_bps()` bound to live `orc.bond_bps`, Incident's all-cohort fold and Milestone's production `None`. Reviewed rather than accepted: **both new error variants are trailing** (verified — shifting error indices would be metadata-visible even without a contract bump), the creation-time freeze is untouched, and the rounding assertion was **mutation-checked** — swapping `div_ceil` for `/` makes `sq296_fractional_base_unit_rounds_bond_up` fail, so it is not vacuous. The `Some(0)` vs `None` distinction is load-bearing and correct: no consuming cohorts means zero exposure and therefore the floor, which is a different fact from exposure being undeterminable. **The defect, now SQ-489:** `file` gained a bounded cohort/vault walk worth **≤ 28 reads** against a declared **87**, and the runtime's `runtime-benchmarks` impl returned a fixture *instead of* walking — so the generated weight never saw it. Up to a **~32 %** understatement on a permissionless call, past the 15 §4.5 10 % threshold, and **invisible to benchmarking by construction**, which is why every gate stayed green. The same hole is **pre-existing on `oracle::report` since SQ-174**, when `StakeAtRisk` stopped being a two-valued sentinel and began reading vaults — my own earlier change. Partially mitigated: the fold is now single-homed in `incident_cohort_escrow` and the benchmark arm *executes* it rather than skipping it, so a benchmark reaching that arm measures the walk. That is necessary and **not sufficient** — it sits behind `has_exposure`, so the honest state is a precisely quantified, rowed gap, not a fix. The overstated comment claiming the walk was now measured was corrected in the same pass rather than left standing. Understating dispatch weight on a permissionless call is a block-capacity DoS surface (R-7), so **SQ-296 does not close until SQ-489 does.** registry-core **11**, pallet-registry **40** (7 new), runtime **357** (2 new), fmt and clippy clean across all three crates; both default and `runtime-benchmarks` profiles compile.

**What comes next:** Close SQ-489: seed the worst case (4 cohorts × 5 vaulted proposals) in the runtime benchmark setup for **both** `file` and `report`, regenerate `weights/pallet_registry.rs` and `weights/pallet_oracle.rs` from a real run, then rebase both branches onto `3621bb9`, re-run weight regression, one exhaustive gate, and PRs for SQ-296 + SQ-489 together. Then SQ-294/295 \(both sequenced behind SQ-296\), SQ-141/175/181/182/341, SQ-461, SQ-234, SQ-303.

## SQ-489 (in flight) — A12

**Park note, not a result: the working tree carries a running job's in-flight edits, and nothing here is verified yet.** SQ-489's fix is being implemented — seed the worst-case exposure (4 cohorts × 5 vaulted proposals) through the registry's existing `prime_epoch` benchmark seam and the oracle's equivalent, remove both `runtime-benchmarks` fixture short-circuits so the fold is actually walked, and regenerate `weights/pallet_registry.rs` + `weights/pallet_oracle.rs` with `frame-omni-bencher` (installed and confirmed at `~/.cargo/bin`). At park time the job is past seeding \(`pallets/oracle/src/benchmarking.rs` and `runtime/bleavit-runtime/src/configs.rs` modified\) and in its verification phase; the release build plus two benchmark runs is the long pole. **Deliberately instructed not to write `tools/ci/weight-regression-acks.toml`**: both calls will show weight *increases*, which is the correct outcome of fixing an understated weight, but an ack asserts an increase is justified and that assertion must not be made on numbers nobody has read — the same failure shape as SQ-489 itself, one layer up. The tree was left untouched while the job runs; editing under a running job voided three gates earlier today and this one is rewriting generated weight files. **PR #166** stands at 6 CI jobs passing, 13 pending, **0 failures**, with a connector **re-review requested** — justified rather than routine, because the fix for round 1's migration finding itself *added* a migration (`MigrateConstitutionSecurityFlowCapV4`) and a `pallet-constitution` `STORAGE_VERSION` bump to 4, so the highest-risk part of that PR is now code that has never been reviewed.

**What comes next:** Review the regenerated weights before anything else — confirm the `file`/`report` deltas are the fold landing and not something else moving, and only then decide whether a regression ack is warranted and what it must say. Then rebase both branches onto `3621bb9`, re-run weight regression, one exhaustive gate, and PRs for SQ-296 + SQ-489 together. SQ-294/295 unblock behind them; then SQ-141/175/181/182/341, SQ-461, SQ-234, SQ-303.

## **SQ-296 ✅ + SQ-489 ✅** — A12

**Both closed; exhaustive gate exit 0.** SQ-296 ships the value-scaled filing bond on §6.1's existing rate — no new parameter, because 07 §7 already said "§6 value-scaled bonds" and §6.1's rate is `orc.bond_bps`. **SQ-489 is the more instructive half, and it is three of my own defects in one family.** The row's own derived bound was wrong: it reasoned "≤ 28 reads" from 13 §4 and the raw measurement was **32**, so the hand-written `DbWeight` addend I had considered as a shortcut would have shipped a still-understated weight *while looking rigorous* — the argument for measuring rather than deriving, made concretely. Then two things only reading the numbers could catch: the two registry instances share one `WeightInfo`, only the Incident fixture had been removed, so Incident measured **119** against Milestone's **99** — and `--unsafe-overwrite-results` let the 99 overwrite the 119, silently restoring a 20-read undercharge on the live path; and `challenge_filing` rose 12 reads without touching the fold at all, because `RegistryParams::bond_bps()` routed one value through the **12-key** `OracleParams` aggregate, making every aggregate-loading registry call pay 11 wasted `Constitution::Params` reads. Fixing that dropped `file` from 119 to **108** and showed the fold's true cost is **21** reads, not 32 — had I accepted "over-charging is the safe direction", 11 wasted reads would have been baked into the weight permanently. Final: `file` 87→108, `challenge_filing` 80→81, `crank_close` 88→89, `report` 492→515. **Two of the three acknowledged regressions were already on main, and both are mine:** `reserve_probe_result` and `crank_reserve_probe` omit `Epoch::Schedule`, `Welfare::XcmTrafficEpochs` and `Welfare::ReserveProbeDaily` — that last store added by SQ-195 in PR #159, merged with the *declared* pallet weight adjusted and the *generated* runtime weight never regenerated. Each ack carries its own reason, not one blanket justification, because they are three different situations and a shared excuse would have hidden that. **The through-line worth carrying forward:** in all three cases the weight and the code stopped agreeing in a way no gate could observe — a fixture that returns a value instead of doing the work, and a declared weight adjusted without regenerating the generated one. Both branches were also rebased onto `b742e00` after PRs #167/#168 merged; the PLAN session-log conflict is the known append-only shape \(keep both, chronological\) and rerere now has it. registry-core **11**, pallet-registry **40**, pallet-oracle **113**, runtime **357**, storage bounds **21/21**, limit coverage **186/186**.

**What comes next:** Push and open the SQ-296 + SQ-489 PR with the connector watch armed. Then SQ-294/295 \(unblocked — SQ-296 was their dependency\), SQ-141/175/181/182/341, SQ-461, SQ-234, SQ-303. Consider a follow-up sweep for the SQ-489 pattern across every pallet whose runtime impl has a `runtime-benchmarks` fixture arm.

## **SQ-296 ✅ (re-closed)** — A12

**Correcting the earlier row that marked SQ-296 done: the connector P1 on PR #169 was right, and the row was reopened before being closed again on a corrected rate.** The defect was subtle and exactly what this row's original title warned of. Reusing `orc.bond_bps` made the filing bond *proportional* to exposure but not *covering*: §6.3's rule is stated over the **whole ladder** — `(2^R_max − 1) · orc.bond_bps ≥ Δs_max`, 17.5 % at defaults — and by retiring escalation I removed the ladder while keeping only round one's 2.5 %. An unchallenged false S1 zeroes `I`, hence `c_settlement`, moving far more than 2.5 % of exposure, and an upheld filing is refunded, so the attacker pays nothing at all. I had read the title's "the underlying economics may not hold" as being about the *rate*, which I had resolved, rather than about *coverage*, which I had not. **Fix:** `CoverageBps = (2^orc.rounds − 1) · orc.bond_bps` — the terminal-stack equivalent posted up front, because a one-round game has no escalation to build the stack. Derived from two existing keys, so still no new parameter and R-2 step 1 still resolves by reuse; the reviewer's alternative (an independently calibrated key) was declined on that basis. Their other suggestion — derive from maximum settlement impact — is the correct long-run rate and is blocked on a real gap rather than a preference: `Δs_max` has no MetricSpec field, which is **SQ-341**. `RegistryParams::bond_bps` was renamed `coverage_bps` so the name cannot re-suggest the round-1 rate to the next reader. Malformed `orc.rounds` degrades to the tightest lawful ladder \(×3\), never a zero multiple that would price every filing at the floor \(G-1\). Expected bonds moved ×7 across all seven regressions, and the round-up test was **re-mutation-checked** at the new rate — still fails under `div_floor`, and now detects the off-by-one directly instead of via floor-masking at the knee. **Liveness cost stated rather than buried:** an honest filer posts ~17.5 % of exposure where an oracle reporter initially posts 2.5 %; that asymmetry is inherent to a one-round game and R-7 prefers the reading that cannot leave an unbacked claim. Weights unaffected \(no storage-access change\); weight regression still PASS WITH ACKNOWLEDGEMENTS. Branch rebased onto `8b64f49` after #166 merged — during which my append-both conflict resolver, applied without inspecting each hunk, duplicated the batch-X **index** row three times; correct for the append-only session log, wrong for a single-row index. Caught by the batch checker and fixed. registry-core **11**, pallet-registry **40**, runtime **361**, fmt/clippy clean.

**What comes next:** Exhaustive gate, push, un-draft #169. Then SQ-294/295 \(unblocked\), SQ-141/175/181/182/341, SQ-461, SQ-234, SQ-303, plus the SQ-489-pattern sweep over every pallet whose runtime impl has a `runtime-benchmarks` fixture arm.

## — (session park; SQ-296 ✅ / SQ-489 ✅ pushed)

**Clean park with exact resume state, not a result row.** #169 is pushed, ready for review, exhaustive gate exit 0 (registry-core 11, pallet-registry 40, runtime 361, all four profiles, limit coverage 186/186 with `diagnostic=0` confirming SQ-486 is on main, weight regression PASS WITH ACKNOWLEDGEMENTS); at park time CI reads 8 pass / 9 pending / 0 fail and a connector re-review watch is armed. Tree clean, nothing in flight. **Next item is SQ-294, and it is now unblocked but was deliberately not started:** it is audit-scope-A work \(constrain registry terminal resolution\) and starting it without room to review it properly is precisely how today's three weight/economics defects reached a green gate. **SQ-294 resume notes:** `resolve_challenge` destructures `Challenged { challenger, .. }` and **discards the `evidence_hash` it stored at challenge time** — evidence is committed then ignored at the decisive moment. The oracle counterpart is constrained \(`hash_evidence(proof) == round.evidence_hash` ⇒ `EvidenceMismatch`, grid-bounded value, `round >= round_cap`, live challenger required\); the registry has none of that and `recompute_proof` has **zero** occurrences under `pallets/registry/` or `crates/registry-core/` despite doc comments promising the path. 07 §7's "recompute_proof where the spec permits, **else** terminal escalation" collapses because (a) is not generally available — a registry filing is a bonded claim about an off-chain fact with no `formula_ref`. **Do not simply remove the authority:** `crank_close` deliberately never auto-resolves a `Challenged` filing, so `resolve_challenge` is the only exit from `Challenged`, and stranding it wedges `close_epoch`, the epoch aggregate and the welfare input. The tractable first step is binding the stored `evidence_hash` at resolution, mirroring the oracle's check. **SQ-295** rides with it \(move registry resolution off the `OracleResolution` track; its classifier half already shipped\). Also open and worth doing early: the **SQ-489-pattern sweep** — enumerate every pallet whose runtime `Config` impl has a `#[cfg(feature = "runtime-benchmarks")]` arm returning a fixture *instead of* performing the work, since two of the three defects found today were exactly that and nothing in CI can see them.

**What comes next:** SQ-294 → SQ-295, then SQ-141/175/181/182/341, SQ-461, SQ-234, SQ-303, and the fixture-arm sweep. Watch #169's CI and connector re-review; resolve genuine findings before treating it as handed off.

## **SQ-294 ✅ + SQ-295 ✅** (branch…

**SQ-294 ✅ + SQ-295 ✅** (branch `feat/sq294-registry-terminal-resolution`, off merged main)

**Both closed, and the row's premise was sharper than written: neither of 07 §7's two stated closure paths exists.** `recompute_proof` needs a `formula_ref` a bonded off-chain-fact claim has none of (§9); escalation into §5 step 4 needs a `(component, epoch)` game an Incident filing has no component for, because `I` is a scalar multiplier and not a `MetricId` (05 §4.4). So the `OracleResolution` verdict the code always used is the real terminal path — and §7 had never named the extrinsic at all. Ruled: keep the authority (it is the only exit from `Challenged`, and an unresolved challenge resolves *favourably* through welfare's pull-side default rather than neutrally) and bind its discretion: restate the challenge's committed `evidence_hash`, wait out the counter-round window, forfeit the **stored** bond. `Challenged{window_end}` had been stored with **no reader anywhere** (`crank_close` did `let _ = window_end`), so a verdict could land in the same block as the challenge; `_ => WindowOpen` also conflated "still `Filed`" with "already terminal" and now reports `AlreadyFinal`. SQ-295 rode along: its deferral reason ("enumerating the registry calls would legitimize SQ-294's unconstrained call") is discharged once the call is constrained, so §5.4's "only" is now scoped to the game and both calls are enumerated in 06 §3.2. Routing the registry off the track was considered and rejected — it would rebuild the electorate, thresholds and pre-cohort conviction snapshot the oracle track already provides, and that snapshot is exactly what a verdict on a money-settling epoch needs. **Two defects of my own found while doing it, both on main.** (1) **main is red**: #169's `Benchmark smoke` job failed and the PR merged anyway — the job runs ~1 h and reported afterwards, so at its current runtime it cannot gate a merge. Cause was my SQ-296 value-scaling: one fixture account posts ~68 bonds, which stopped fitting 1,000,000 USDC, so setup died with `Token(FundsUnavailable)`. The fixture now funds the worst case it claims to measure. (2) #169's three weight-regression acks became **stale the moment it merged**, and the checker fails on stale entries — so every branch cut from main failed that gate until it deleted them. Deleted. Second time today that expiry cost a detour. **Also a near-miss worth recording:** I launched `--extrinsic resolve_challenge --output <the real pallet_registry.rs>`, which would have rewritten the file containing only that one function and silently dropped the other six — the SQ-489 trap. Killed before the build finished, file verified untouched, re-run to a scratch path, and every function checked to have *not decreased* before copying it in. Weights: uniform +1 read across all seven functions; for `resolve_challenge` that is exactly the added `Self::now()` (82→83), while the uniform part is unexplained — most plausibly the larger fixture mint changing balance encodings and the trie layout, recorded as a hypothesis, not a finding. Safe either way: every value moved conservatively and the gate passes with **no acknowledgements**. Runtime **361**, registry **42**, registry-core **11**, fmt + clippy + limit-coverage + doc gates green.

**What comes next:** Push, open the PR (it repairs main's red `Benchmark smoke` as well as closing SQ-294/295), then **SQ-182** (most self-contained: wire the d20 money-deadline → neutralization callback and retain contested rounds for bond-only verdicts). **Scoped the remaining cluster, and it changes the batching: SQ-175 and SQ-341 must ship together.** Both add a field to the frozen versioned `MetricSpec` — SQ-175 the milestone `target` (05 §4.3 already names it; the schema home is settled, the code is not), SQ-341 the mandatory `Δs_max` that 07 §6.3's admission rule needs — so doing them separately means two SCALE changes to the same struct and two contract bumps, i.e. exactly today's v13/v14 collision repeated. SQ-175 additionally has a second half no MetricSpec field fixes: the Milestone instance's aggregate is read nowhere in the runtime (`configs.rs` pulls `Aggregates` for the Incident instance only, and the push seam is a deliberate no-op), so the A-pillar component is unfed end-to-end. **SQ-141 is ruled but its code is entangled with those two**: `Aggregates` is keyed by epoch alone while 07 §2 requires per-version games, so the registry key needs versioning — and that is the same `(epoch, spec_version)` surface SQ-175's target binding lands on. Suggested order: SQ-182 → SQ-175 + SQ-341 + SQ-141 as one MetricSpec/versioning PR (contract v14) → SQ-234 (contract v15) → SQ-181 → SQ-303 → SQ-490's sweep. #170 is 15/3/0 with the connector P1 fixed; **#171 also repairs main's red `Benchmark smoke`**, so it should land before any further branch is cut.

## **SQ-461 ✅** (branch…

**SQ-461 ✅** (branch `feat/sq461-live-meter-admission-preview`, off main — deliberately not SQ-294/SQ-234)

**Sequencing decision first, because it shaped the pick.** SQ-294 lands in the exact 07 §7 paragraphs and `pallets/registry` files #169 rewrote, and SQ-234's ruled design needs contract **v14** while #169 carries the v12→v13 bump — the same constant, test name and 02 §13 slot. Both would have meant hand-resolving prose conflicts, so with #169 in flight the honest move was a row that touches neither: SQ-461. **The row undersold itself.** It was filed as a view-hint refinement with "no lost enforcement"; verification against 09 §1.2(7) showed the meters *do* bind but in the wrong *place* — treasury/issuance contention reached step (12), whose atomic rollback records the T18 failure and drops the proposal onto `failed_at + RETRY_WINDOW` instead of the full grace window §1.2(7) grants. **My first draft was wrong in the interesting direction, and the existing suite caught it.** Wiring the whole treasury dry-run into step (7) flattened every error into `MetersBlocked`: `execute_under_constitution_dead_man_reports_freeze_active_and_preserves_queue` went red because `fund_budget_line` carries no meter at all, so its `InsufficientFunds` was being reported as meter contention *and* preempting the (10) freeze check a frozen chain must report. Narrowed to `MeterExhausted`/`IssuanceCapExceeded`; everything else stays with step (12), where a bounded retry is right for a defect waiting cannot fix. The second narrowing is the D-9 lane: the leg must exclude `code.spacing`, or an expedited emergency CODE payload inside its spacing window would be refused by the very check meant to protect throughput. Both narrowings are pinned, and all four new/updated assertions were verified to fail under mutation of either leg. Runtime **355**, guard **74**, fmt + clippy clean on the three changed crates. **Weights: regeneration flagged 6 functions past the 10 % gate, and the delta is not mine.** Rather than reason about it — the mistake made on SQ-489, where a derived estimate was wrong in the unsafe direction — the pallet was benchmarked a *second* time with the tree reset to main: every regenerated value at main already matched or exceeded the value this branch commits (`ratify` 30M vs 29M, `reject_stale` 702M vs 660M, reads identical), and `execute`'s proof already contained `FutarchyTreasury::State`, so the step-(7) preview adds **no read, no write, no PoV**. So main ships understated execution-guard weights today, drifting since 2026-07-23. Raised as **SQ-490**: `check-weight-regression.py` compares committed-to-committed and therefore detects a regeneration but never its absence, and `Benchmark smoke` runs at `--steps 2 --repeat 1` without comparing — so this class is invisible until an unrelated PR happens to regenerate a file, and the other 30 weight files are unaudited. That is the **third instance today of one shape**: SQ-195, SQ-489 and now this are all cases where the artifact and the thing it describes stop agreeing exactly where no gate looks.

**What comes next:** Fuzz gate green (exit 0). **#169 merged as `dee2df2` while this was in flight, so the branch was rebased onto it** — three PLAN conflicts, each inspected rather than auto-resolved: the batch-X index resolved to **10** open exactly as predicted (drop SQ-461, add SQ-490), main's closed SQ-486 row was kept over the branch's stale open copy, and the session log was unioned. The acks file conflict was the documented expiry case: **#169's three acknowledgements are stale the moment it merges** — their regressions are now *in* main, and the checker fails on stale entries — so they were deleted and only SQ-461's six remain. The gate now resolves its base to `dee2df2` and passes. **Rebasing also means SQ-294 and SQ-295 are unblocked**: the 07 §7 / `pallets/registry` collision that made this session pick SQ-461 over them no longer exists. Next: exhaustive gate on the rebased state (it must also validate the auto-merge of `configs.rs`/`tests.rs` against #169's registry work) → PR → SQ-294 → SQ-295, then SQ-234, SQ-141/175/181/182/341, SQ-303, SQ-490's sweep of the other 30 weight files, and the `runtime-benchmarks` fixture-arm sweep — `queue_time_check` is confirmed to be another instance of it

## **#172 merged**; v14 cluster started, spec-only slice…

**#172 merged**; v14 cluster started, spec-only slice landed

**#172 is on main as `f33c338`** (SQ-182 ✅ + SQ-491 ✅, three Codex P1s fixed, exhaustive gate exit 0). Its three weight acknowledgements went stale the instant it merged — **third occurrence this week** — so they were cleared as the first commit on this branch rather than after the gate failed. **Started the v14 cluster and landed only its spec prerequisite, deliberately.** A read-only scoping audit against HEAD produced the implementation order and turned up two live doc defects, both verified and both fixed here: 07 §6.3 cited `Δs_max` as "a mandatory MetricSpec field per **05 §12.4-equivalent**" when **05 has no §12 at all** and its §4.4 field list omits the field entirely — so the rule the code is supposed to enforce was anchored to nothing; and §6.3's "the 150 bps hard min keeps the left side ≥ 10.5%" holds only at `R_max = 3`, while 13 §1 admits `orc.rounds ∈ [2,4]` where the same floor gives 450 bps. 05 §4.4 now carries `delta_s_max_bps` with its basis-point units, its `0 < Δs_max ≤ 10,000` bound and its attested-only scope; §6.3 points at §4.4 and states that the rule is evaluated against live params. **Also raised SQ-495** from the same reading: §6.3 gates admission *once*, and nothing re-checks it when `orc.bond_bps` or `orc.rounds` is later lowered within bounds — a component admitted at coverage 1,750 bps keeps settling money at 450 bps with no event, gate or try-state signal. §6.1 names that hazard for open games and closes only that half. **The code slice was started and then reverted on purpose:** adding the two trailing `MetricSpec` fields broke 3 construction sites, then more in the runtime tests, and the fields are only coherent alongside their `register_metric_spec` validation and the v13→v14 bump. Half a frozen-surface change is worse than none, so the tree is left green with the spec correct — which is the order the audit recommends, since nothing else is verifiable until the docs say what is being implemented. Open batch-X rows: **11**.

**What comes next:** **Hand off to a fresh session for the v14 cluster.** Resume notes in the row below.

## SQ-182/SQ-491 — Codex review round (3 P1, all valid)

**All three findings were real, and the third forced a design change I would not have reached by reasoning.** (1) *The cursor is not evidence for a cohort.* A cohort forming after one of its measurement deadlines was driven had no `CohortSchedule` when `expected_components` resolved, so that drive covered none of its components — and my `measurement >= cursor` optimisation then skipped exactly those epochs, wedging `record_snapshot` on `MissingComponent` permanently. The guard is deleted; every live cohort's due epochs are re-driven unconditionally. (2) *The liveness latch was not attributed to an epoch.* After an arithmetic catch-up it describes the interval since the last sweep, so a round from epoch 5 would charge idle seats for epoch 8 — the over-charging my own new §4 ruling forbids. Split along ownership: the oracle keeps the round fact, the caller supplies `attributable`, and an unattributable sweep charges nobody and resets nothing. (3) *The boundary callbacks were benchmarked against empty oracle maps* — one `Oracle::Rounds` read against a 128-entry bound. Priming honestly is **arithmetically impossible inline**: 13 §5 pins `decide` at 231,055 B under a 384 KiB ceiling and the oracle aggregate alone is 356,514 B. So the driver became its own permissionless crank (`epoch.drive_oracle_boundaries`, index 17), benchmarked with the aggregate primed to bounds, classified Public in the SafetyFilter matrix, registered in the S5 call inventory and planned by the keeper. **Nothing was lost by decoupling it**, because the ordering never rested on intra-dispatch sequencing — `record_snapshot` fails closed — and 07 §11(1) calls this a *crank* while §13 gives the oracle no hooks, so this is the shape the spec asked for all along. My inline design had been defending an ordering property that the fail-closed reads already provided. Two new oracle-core tests cover the unattributable sweep and the retained-round exclusion; the epoch tests moved to the crank and gained a direct cursor-is-not-evidence regression.

**What comes next:** Regenerate `pallet_epoch` weights for the new crank, rerun the exhaustive gate, push. Then the SQ-175 + SQ-341 + SQ-141 contract-v14 cluster (scoping done — see the audit notes in the session).

## **SQ-182 ✅ + SQ-491 ✅** (branch…

**SQ-182 ✅ + SQ-491 ✅** (branch `feat/sq182-money-deadline-neutralization`)

**Both closed, and an adversarial spec review caught that my first cut of SQ-491 would have shipped the appearance of a fix.** The seam is now real: `OracleAccess` gains undefaulted `note_settle_deadline(m)` and `note_epoch_boundary(ended)`, driven from all three clock-syncing entry points via a bounded resumable `OracleDeadlineCursor` + `LastWatchtowerSweep`, with `settle_cohort` also driving its own cohort's due measurement epochs so the guarantee holds by construction rather than resting on the catch-up budget. **Three things the review changed, all verified before acting on them.** (1) **`had_open_round` cannot be derived by a caller.** My adapter inferred it from rounds keyed to `ended_epoch - 1`; `settle_at` *removes* a cleanly closed round, so the predicate reads `false` in the healthy case and the `!had_open_round` arm **resets** the inactive counter — §4's charge/slash/eject would have stayed unreachable, i.e. SQ-491 unfixed behind a green gate. Replaced with an oracle-core latch (`round_activity` / pallet `RoundActivity`), and the parameter left the seam entirely: a caller-supplied liveness fact is a caller-supplied slash. (2) **`expected_components` was unfiltered**, so the newly-live deadline crank would have written neutral *flagged* `ComponentValues` rows for class-1/2/3 components — the declared input to §10's renormalization and, for gate inputs, its VOID escalation. Filtered to `SourceClass::Attested`, the predicate `onchain_components` already used 200 lines above. Latent before this PR only because the callback had no caller. (3) **My ordering justification named the wrong consumer.** `settle_cohort` reads the welfare snapshot, not `ComponentValues`; the real chain is `note_settle_deadline(N-1)` → `record_snapshot(N-1)` → `settle_cohort(N-3)`, each fail-closed. Comment and 07 §11 corrected. Two further review findings were **refuted** by checking: `TICK_BATCH`/`REAP_BATCH`/`MAX_NON_TERMINAL_COHORTS` are kernel-only with no 13 §4 or registry rows, so the new bound needed no registry entry *for that reason* — but 13 §2's crank-batch row does carry `TickBatch`/`ReapBatch`/`settle_cohort`, so it got one there anyway, with a limit-coverage classification, a marked test, and the value **derived** from `MAX_NON_TERMINAL_COHORTS` instead of picked. **A design trap avoided:** initializing the cursor abreast of the clock (no migration) looked cleaner until it showed it would claim coverage it never provided and silently skip a mid-flight cohort's earlier deadlines. It defaults to 0 and replays at bounded no-op cost instead. **An error-preemption repeat avoided:** my first `OracleDeadlinePending` guard fired ahead of the core's phase check and turned three existing tests' `BadPhase`/`Ok` into the new error — the exact SQ-461 shape — so the guard was replaced by the per-cohort drive, which needs no new error at all. Five mutations each fail exactly one of the five new epoch tests. **Rowed, not folded in: SQ-492** (a retained round whose verdict never lands holds its bond stack forever — §11(1)'s reaping window has no driver; R-7) and **SQ-493** (§10's two-consecutive-flag renormalization has no reader of `flagged`, so a twice-failed component keeps voting its stale value at full weight — reachable in practice now that the flag writer has a caller). Two adjacent questions ruled inline instead of rowed (dead-man pause shifts the deadline conservatively; a registry filing is not a §4 round).

**What comes next:** Regenerate `pallet_epoch` + `pallet_oracle` weights (both gained storage), run the exhaustive gate once, push and open the PR. Then SQ-175 + SQ-341 + SQ-141 as the contract-v14 PR, SQ-234 (v15), SQ-181, SQ-303, SQ-490's sweep.

## — (SQ-182 scoping; **SQ-491 raised**)

**Stopped before implementing, because verification changed the shape of the work.** SQ-182 reads as "assign the keeper/tick trigger"; the oracle side turns out to be *finished* — `note_settle_deadline(m)` resolves the expected-component set and calls `force_neutralize_expired`, and its doc comment states the epoch pallet drives it, exactly as 07 §11(1) requires. It has **zero callers**, so the money deadline is unenforced end to end. The real gap is the **seam**: `pallets/epoch::OracleAccess` carries a single method (`any_open_dispute_touching`), so nothing connects the epoch clock to the oracle at any boundary. **That search turned up a second unwired callback the row never mentioned, now SQ-491:** `note_epoch_boundary` — the 07 §4 watchtower liveness sweep that charges inactivity and slashes/ejects on a second consecutive miss — is called only from `pallets/oracle/src/tests.rs`. So watchtower stake discipline has never run in production: a bonded seat can miss every acknowledgment indefinitely without charge or ejection, which leaves §4's censorship-repair incentive unfunded and D-18's bonded-quorum assumption unbacked. It is R-7-adjacent, and it needs the *same* seam, so the two must ship together — one `OracleAccess` extension, two call sites (rollover, d20 Housekeeping), one epoch-tick weight regeneration, plus the §11-vs-05 §6 ordering proof against `settle_cohort`. Deliberately parked rather than started: adding a Config seam to the epoch pallet and wiring two settlement-path callbacks is milestone-shaped, and beginning it without room to review the result is how the two defects earlier today reached a green gate. Nothing is half-applied — this commit is PLAN-only, tree otherwise clean.

**What comes next:** Implement SQ-182 + SQ-491 as one PR off a green main: extend `OracleAccess`, wire the rollover and d20 call sites, prove the ordering against `settle_cohort`, check whether A12's "d20 retained rounds" already covers bond-only late verdicts, regenerate the epoch weights. Then SQ-175 + SQ-341 + SQ-141 as the contract-v14 PR, SQ-234 (v15), SQ-181, SQ-303, SQ-490's sweep.

## **SQ-341 ✅ + SQ-496 ✅**, contract **v14** landed…

**SQ-341 ✅ + SQ-496 ✅**, contract **v14** landed (branch `feat/sq175-341-141-metricspec-v14`)

**The bound I needed for SQ-141 was not provable, and chasing that turned up a chain-wide wedge.** Sizing the re-keyed `Aggregates` needs "≤ 2 concurrent frozen versions" — a factor four documents assert (02 §7.2, 07 §7/§12, 13 §4) and nothing enforces. It is `epoch.horizon_k`, which 13 §1 published with a hard max of **4** while 05 §3.3's own decomposition (`k measuring + 1 awaiting oracle + 1 settling ≤ 4`) forces `k ≤ 2`, and `EpochParams::validate` mirrored the wrong bound. At `k = 3` steady state needs five concurrent cohorts, so within a few epochs **every `qualify` fails `TooManyCohorts` permanently** — reachable by a lawful META+values amendment inside the key's own published range, with no warning at the amendment boundary. Ceiling is now derived (`MAX_NON_TERMINAL_COHORTS − 2`) and **kernel-bounded** so it cannot be raised back; 05 §3.3 states the version-bound coupling normatively so the four dependent bounds stop resting on an assumption (**SQ-496**). Side effect worth knowing: with band `[1,2]` and max-Δ 1 the Δ limit is unreachable, so the generated registry suite now skips that leg **with a proof** (`span ≤ delta`) rather than panicking — the first key where max-Δ is vacuous by construction. **SQ-341 closed, with its own premise corrected.** Both halves of 07 §2(5) are enforced at `register_metric_spec` behind an injected `AttestedAdmission`, so genesis and live dispatch share one path; `orc.n_min` gets its **first consumer of any kind** and moves `param-bounds → dispatch-limit` with a marked test. The row expected the gate to be scoped to spec sets containing attested components — but 05 §4.3 makes every A-pillar component attested and §4.4 requires the A weights to sum to 1, so **no valid spec set escapes it**: seating the oracle is a precondition of the *first* MetricSpec, now normative in §2(5). Before adding a third consumer I single-homed the §6.3 coverage derivation in `oracle_core::coverage_bps`; its two existing copies needed *opposite* correct failure directions on a malformed ladder, so it returns `Option` and each call site states its own. Live-parameter halves are deliberately **not** re-checked in try-state — that would turn a lawful amendment into a chain halt (SQ-495, open). Contract **v13 → v14** for the two trailing `MetricSpec` fields and `RegistryEpochClosed.spec_version`; **v13's missing 00 decision-record entry backfilled** (an R-1 fan-out miss in the SQ-186 PR). Workspace **69 suites green**, fmt/clippy clean, limit coverage 187/187, all doc gates pass.

**What comes next:** **SQ-141 next on this branch** — the `(epoch, spec_version)` re-key itself, whose design is now settled: `Filings` and `FilingCount` stay keyed by epoch (filing ids stay unique, so 02's ten filing events are untouched) because `file` requires `now ≤ filing_window_end` and `close_epoch` requires `now > filing_window_end` — **no filing can ever arrive after any version of that epoch has closed**, so dropping the shared count at the first close is safe. Only `Aggregates` is re-keyed (`MAX_AGGREGATES` 4 → 8). Then SQ-175's consumer (`milestone_target`, the Milestone aggregate → A pillar), and the fail-closed `incident_multiplier` reader 07 §7 now mandates.

## **SQ-141 ✅ + SQ-175 ✅** — the contract-v14 cluster…

**SQ-141 ✅ + SQ-175 ✅** — the contract-v14 cluster closes (branch `feat/sq175-341-141-metricspec-v14`)

**The re-key was smaller than scoped, and the two things that nearly went wrong were both in the *reader*, not the key.** Scope first: only `Aggregates` is keyed `(epoch, spec_version)` (`MAX_AGGREGATES` 4 → 8). `Filings` and `FilingCount` stay epoch-keyed, so filing ids stay unique and **none of the ten 02 §6 filing events is touched** — only `RegistryEpochClosed` gains the version. Dropping the shared count at the first version's close is safe because `file` requires `now ≤ filing_window_end` while `close_epoch` requires `now > filing_window_end`: **no filing can arrive once any version has closed**, so the id-collision window the counter appears to guard does not exist. **First near-miss — the fail-closed reader would have wedged the chain.** Refusing on every missing aggregate looks obviously right and is not: `close_epoch` deliberately refuses a never-filed epoch (anti-griefing), so most epochs never carry a record and **no snapshot could ever be taken**. The rule is two-way — no registry footprint ⇒ the neutral 1.0 is honest, because the absence *is* the evidence; a footprint lacking this version ⇒ refuse. Two runtime tests failing is what surfaced it, and 07 §7 was corrected from the absolute form I had written hours earlier. **Second near-miss — an independent review found a hole the re-key itself opened**, running both directions at once: because the first close drops the epoch-wide `FilingCount`, reaping that version while a sibling still held a non-terminal filing erased the epoch's only remaining evidence of having been filed — stranding the sibling's `close_epoch` **forever** *and* flipping the reader to the favourable neutral branch for a version whose filings were still in storage. Fixed by ordering the reap behind sibling closes (`ReapNotDue`), pinned by a regression that was mutation-checked (weakened the guard, watched it fail, restored it). **Writing that regression then found two more defects of the same family, both mine**: the `try_state` live-XOR-closed partition was no longer exhaustive — the first close releases the epoch-wide count, so a sibling's filings are legitimately neither, and try-state failed on a healthy chain — and the reader's evidence was scoped to the *epoch* instead of the *version*, which stranded a version that legitimately has no filings behind its sibling's footprint. Both are the same lesson three times over: **the versioned lifecycle has a third state the single-key one never had, and every predicate written against the old two-state model is wrong in a way that only shows up mid-lifecycle.** **SQ-175 closed with it:** `milestone_target` is a real per-version read of the new frozen field instead of the constant 0 it returned since B1b, and `cohort_exposure(Milestone, ·)` is determinable for the first time — it folds exactly the cohorts whose frozen spec carries the component, and still returns `None` rather than `Some(0)` when none does. **Weights:** four pallets regenerated at the pinned 50×20 with nine scoped acknowledgements, every one explainable (+4 aggregate keys per registry call; `reap_epoch` +83% for the sibling walk and per-filing ack clears; `record_snapshot`/`register_spec` for the fail-closed read and the admission gate). The `pallet_epoch` hand-edit trap (SQ-490) was re-spliced and the function set diffed before and after to prove nothing else moved; the `register_spec` benchmark needed a real `seat_oracle()` seeding seam rather than a stubbed admission, on SQ-489's precedent. Root workspace **69 suites green**, keeper **64/64**, fmt/clippy clean, limit coverage 187/187, weight regression PASS WITH ACKNOWLEDGEMENTS, all doc gates pass.

**What comes next:** **Open batch-X rows: 8** — SQ-181, SQ-234, SQ-303, SQ-490, SQ-492, SQ-493, SQ-494, SQ-495. Next per the standing order: SQ-234 (needs contract v15, unblocked now that v14 has landed) → SQ-181 → SQ-303 → SQ-490's weight sweep, whose scope grew again: this session regenerated four more pallets, leaving ~24 of the ~31 weight files never re-measured.

## **Codex review of PR #173 — 2 findings, both valid,…

**Codex review of PR #173 — 2 findings, both valid, both fixed**

**P1 was a griefing surface the re-key itself opened.** `close_epoch` validated no version at all: once an epoch had any filing and its window ended, the per-version filing filter was empty for *any* unfrozen version number, so the fold recorded the neutral "no filings ⇒ 1" aggregate for it — and each bogus close consumed one of the eight `MAX_AGGREGATES` slots **and** satisfied the `NothingToClose` disjunct for the next, letting a signed caller fill the map and make the legitimate version's close fail `TooManyAggregates` **permanently** (nothing reaps an aggregate whose epoch never closes). Gated on the frozen version set, with a second disjunct for a version that already holds filings — which is what keeps a *late* close legal once the cohorts settle and `CohortSchedules` is pruned, and cannot be abused because filings only exist under a version that passed the same gate at `file`. Mutation-checked. **P2:** the admission gate counted `Reporters::count()`, but 07 §3 requires "≥ 3 registered reporters **with full stakes**" — and `record_reporter_offense` halves the stake on a second adjudicated-false report and leaves the reporter *registered* (ejection is the third). A half-collateralized seat could therefore admit an attested component. Now counts only seats holding `orc.reporter_stake`; 07 §2(5) states the distinction normatively. Both weight-affecting; `pallet_registry`/`pallet_welfare` regenerated and the acknowledgements re-stated.

**What comes next:** **Also re-verified all eight open batch-X rows against HEAD and corrected four.** SQ-181's "keeps those vectors empty" has been false since the day it was raised (`X` and `R` are live, epoch and daily) — and the dominant blocker it never named is that **no production genesis preset registers any MetricSpec**, so wiring sources alone changes nothing observable. SQ-303's "dependency of SQ-180" framing is stale (SQ-180 closed 2026-07-21, the §4.2 gate is live), its 13 §5 quotation is dangling, and the trigger set is 12 keys not 10. SQ-490's `pallet_execution_guard` claim is resolved, the unaudited set is 20 files not ~28, and a sharper instance surfaced: **`pallet_attestor` carries two hand-written weight functions with no benchmark at all, one of them a permissionless `ensure_signed` call**. SQ-234's "needs contract v15" does not survive verification — no 02 §2–§12 surface is structurally implicated.

## **SQ-495 ✅** (branch…

**SQ-495 ✅** (branch `feat/sq495-coverage-amendment-screen`)

**The row the verification sweep ranked first, and the only one reaching the unbacked-claim class.** 07 §6.3's coverage rule was checked once at admission and never again, while both its inputs stayed lawfully amendable — `orc.rounds` has no max-Δ at all — so a single META amendment could drop a live component's coverage from 1,750 bps to 450. Closed with a `CoverageGuard` screen at the governance boundary, built on the existing `BudgetDerivationGuard` seam; the key set is single-homed in `constitution-core::is_coverage_input` and the rate derivation stays in `oracle_core::coverage_bps`. **Both of the implementation's own defects were caught by its own tests, and both are recurrences of shapes this repo has already recorded.** The first cut compared a raw `Perbill` (parts per billion) against a `Δs_max` in basis points — SQ-212's 100,000× trap — which made every proposed rate look far more generous than it is and would have shipped a screen that waves through exactly what it exists to refuse. Then the first weight regeneration reported **no regression at all**, because the `set_param` benchmark uses a synthetic key that short-circuits the screen: it measured a call that never does the work. Re-pointed at a real coverage key with a saturated MetricSpec set, the honest number is **3 → 21 reads**. That is the third instance of the fixture-instead-of-work shape (SQ-489, SQ-490, now this), and it is worth noting the failure mode is *silence* — a green gate reporting no change. Root workspace **69 suites green**, fmt/clippy clean, limit coverage 187/187, weight regression PASS WITH ACKNOWLEDGEMENTS, doc gates pass. Branch opened with the merge-expired v14 acks cleared as its first commit (fourth occurrence of that shape).

**What comes next:** **Open batch-X rows: 8 → 7.** Next per the verified order: **SQ-492** (retained rounds hold real custody that never releases, in a 128-slot registry with no reaper), then **SQ-494** — whose "8-day freeze" is *indefinite* at HEAD precisely because SQ-492's reaper is missing, so SQ-492 must land first — then **SQ-493**, then **SQ-234**.

## **Codex review of PR #174 — 2 findings, both valid,…

**Codex review of PR #174 — 2 findings, both valid, both fixed**

**P1 is the instructive one: my screen contradicted the sentence the same PR made normative.** Testing `coverage >= required` absolutely freezes **both** inputs permanently in exactly the under-covered state the screen exists to leave — a state a chain can arrive in across an upgrade — because no single lawful step necessarily restores full coverage. The reviewer's arithmetic is exact: a 10,000-bps component stranded at `(2, 150)` reaches only 2,250 by raising `orc.rounds` to 4 and only 900 by raising `orc.bond_bps` to its `Factor(2)` limit, so every repair is refused. That is a worse wedge than the defect I set out to fix. Any **non-decreasing** amendment is now permitted, compared on *coverage* rather than key value since coverage is monotone increasing in each input; only a further lowering is refused. 07 §6.3 states the recovery direction explicitly, because leaving it to be inferred is what I got wrong. **P2:** `CoverageBreaksAdmission` was inserted before `PhaseCapRaiseRefused` in both enums, shifting that variant's SCALE discriminant — and the core enum's own comment claimed the preceding discriminants were stable while the insertion was mid-enum. Both appended last now.

**What comes next:** **Handoff point.** #173 merged; #174 carries SQ-495 with both review findings resolved. `feat/sq492-round-retention-deadline` is cut off main and carries the SQ-492 verification + custody ruling as a docs-only commit, ready to implement.

## **SQ-492 ✅** (branch…

**SQ-492 ✅** (branch `feat/sq492-round-retention-deadline`, contract **v15**)

**Two reapers the spec names and no code drove — and the one folded in as an aside turned out to be the larger.** The rowed defect is real and exactly as verified: `crank_round_close`'s third money-settled arm handles a round at `R_max` with a live challenge by `i += 1; continue`, which is correct while a verdict can still land and permanent when one never does — bonds held against 07 §13's custody identity, one of the 128 game slots occupied, one abandoned dispute per slot. Closed by stamping each retained game with `retain_until = neutralized_at + kernel::ORC_RETENTION_BLOCKS`, **derived** from the `OracleResolution` track's own 0/7 d/1 d schedule rather than picked, and bound to `configs::TRACKS[5]` by a runtime test so a later track retune cannot silently shorten retention. The **ruling held under implementation**: expiry refunds both stacks. It needed a third `BondDisposition` rather than a second bool, because `wins && refund` has no meaning and R-7 says not to encode impossible states. **The aside was the bigger defect.** `reap_component` had no production caller in *either* pallet, so `ComponentValues` grew ~16 entries/epoch against a 128 bound, after which every settlement fails `AlreadyFinal` — a total oracle wedge inside the first year of mainnet. The obvious fix (drive `reap_component` from cohort settlement, as 07 §13 says) is **unimplementable**, and finding out why corrected the spec: §11's own SQ-182 resolution establishes that `settle_cohort` reads the welfare *snapshot* and never this map, so §13's stated trigger named a caller with no reason to know which triples to retire. The real last consumer is `record_snapshot(m)` in epoch `m+1`, so the reaper became an epoch cutoff on the boundary crank, and 07 §13 + 02 §7.2 now say that. **A per-version cutoff was designed and rejected** before the epoch cutoff: scoping the sweep to the snapshot's own version looks safer, and leaves ≤16 stale entries per *retired* MetricSpec version forever — unbounded across activations, which is the defect again in slow motion. The epoch cutoff has one hazard (a sibling version whose snapshot lags), and it is unreachable: at `current − 3` that version's snapshot is 60 days overdue and 05 §4.8's snapshot-overdue trigger wedged the chain long before. **Reviewing my own diff before the gate finished found the defect the tests did not.** The reaper's cutoff and the retention window are independent clocks, and nothing stopped the sweep from retiring the settled value a *still-retained* round is resting on — which is exactly what makes that round non-money-bearing past its deadline (I-18; the 07 §13 try-state invariant). It would have left a live `Rounds` entry with no counterpart. In production the windows never overlap (retention ~8 days, cutoff three ~20-day epochs back), which is precisely why it had to be a guard and not an argument: the overlap needs only a stalled close crank. Guarded in both layers, with a mutation-checked regression in each — the pallet's fires through `do_try_state`, which is where the damage would actually have surfaced. Cost: one `MoneySettled` read, +1% on the crank. **And a second self-review finding: the expiry would have been the only unpaid advance this crank makes.** Rebate eligibility is keyed on `ComponentSettled`/`RoundEscalated`/`WindowExtended`, and expiry settles no component — the money leg went neutral at d20 — so the one call that frees a `MAX_ROUNDS` slot and releases two stacks from custody earned nothing. A permissionless crank nobody is paid to run is a liveness assumption, not a mechanism; `RetentionExpired` joins the progress set and 07 §11(1) says so. **The fixture-instead-of-work trap, caught by design this time rather than after a green run.** The boundary crank's benchmark seeded no `ComponentValues` at all, so the new sweep would have scanned an empty prefix and reported no weight change — the fourth instance of this shape (SQ-489, SQ-490, SQ-495). `prime_oracle_state` now seeds a stale batch and the benchmark **asserts** the sweep retired a full `ComponentReapBatch`. The honest number is `drive_oracle_boundaries` +32 writes (exactly one batch) and +11.85% ref_time, acknowledged. `MoneySettled`'s value also grew 1,026 → 1,538 B, and its benchmark fixture was left empty on that path too, so a synthetic full-value envelope was added — every oracle call moved a percent or two, none past the limit. Root workspace **69 suites green**, keeper builds clean, fmt/clippy clean, limit coverage **188/188**, weight regression PASS WITH ACKNOWLEDGEMENTS, doc/table/batch gates pass. Five mutations (reaper disabled, `>=` → `>`, refund → forfeit, retained-round guard removed, rebate arm removed) each fail exactly the intended test. Branch opened with the merge-expired v14 acknowledgements cleared as its first commit — fifth occurrence, and #174's own acknowledgement expired again when main was folded in (sixth). **PR #175 review — 2 P1s, one refuted by measurement, one valid and instructive.** The refuted one predicted the reaper's full-map scan would add ~203 KiB and push the crank past its PoV budget; measured both ways, the estimate is **356,514 B at 48 and at 128 seeded entries — identical**, because `ComponentValues` is already accounted at its ceiling by the `Oracle::load()` scans the deadline drives perform, so the extra 80 reads add nothing to the figure the budget test checks. The *fixture* point underneath it was right and adopted anyway. **The valid one is the better finding:** 07 §10 settles a failed component at \"its last valid value\", read out of the settled history — and an unqualified cutoff erases that history for a component no cohort consumed for longer than the retention window, so the next missed report would carry the neutral 0.5 instead of the real last value, moving `W`. Unreachable before this PR precisely because nothing ever reaped; **introducing the reaper is what created it**, which is the whole shape of the risk in adding a reaper to a system that never had one. Each component's newest value is now an exempt carry checkpoint, per component rather than per `(component, version)` so it cannot grow with MetricSpec activations. **The fixture then caught itself:** saturating the map to 128 with one value per component made *every* entry a checkpoint, the sweep retired nothing and the benchmark failed on its own assertion — the assertion earning its keep on the first regeneration after it was written. Two generations per component fixes it. Honest number: +27.63 % ref_time, two thirds of which is not the sweep but the previously-empty fixture finally measuring the `load()` scans against the real bound.

**What comes next:** **Next: SQ-494**, which this row unblocks and whose \"8-day freeze\" claim it makes true (it was indefinite at HEAD). Then SQ-493, SQ-234, SQ-181, SQ-303, SQ-490's sweep (now ~22 of ~31 weight files never re-measured). **One residual recorded, not fixed:** the keeper counts a retained round as due from the moment it is neutralized, so it resubmits a futile `crank_round_close` every `cooldown_depth` blocks for the whole retention window — bounded now where it was unbounded, and closing it properly needs `OracleRoundView` to express retention, i.e. a contract question for whichever row next touches that view.
