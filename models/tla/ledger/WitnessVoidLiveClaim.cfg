\* Must violate NoVoidedLiveUnpairedRedemption: a live gate leg pays > 0.
SPECIFICATION Spec

CONSTANTS
  Holders = {h1}
  ResolveAuthority = resolveAuthority
  SettleAuthority = settleAuthority
  MaxEscrow = 4
  MaxAmount = 4
  Amounts = {4}
  ScoreScale = 3
  ScoreGrid = {0, 1, 3}

INVARIANT NoVoidedLiveUnpairedRedemption
