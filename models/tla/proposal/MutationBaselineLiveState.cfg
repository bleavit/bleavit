\* Expected to fail: the rejected design in which the §7(6) crank reuses
\* `is_live_state` for its terminal check, so `Submitted`/`Screening` count as
\* terminal. A proposal keeps its stamped epoch across the boundary, so the
\* crank settles the Baseline while the cohort can still form, and the later
\* `settle_cohort` leg finds an already-`Settled` vault -- stranding the whole
\* cohort instead of one holder.
CONSTANTS
    MUTATE_I14 = FALSE
    MUTATE_T16_ENQUEUE = FALSE
    MUTATE_FORCE_CANCEL_EXECUTE = FALSE
    MUTATE_ORPHAN_BASELINE = FALSE
    MUTATE_BASELINE_LIVE_STATE = TRUE
    ForceRerunModeled = FALSE

SPECIFICATION SmallSpec
INVARIANT BaselineSettledExactlyOnce
