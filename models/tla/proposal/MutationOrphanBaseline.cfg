\* Expected to fail: the pre-SQ-320 machine, with 05 §7(6)'s permissionless
\* `finalize_epoch_baseline` deleted. The T20 interleaving 15 §4.1 names
\* explicitly -- every proposal force-rejected before any reaches `Measuring`,
\* so no cohort ever forms -- then leaves the epoch's Baseline vault `Open`
\* with no discharge enabled, and `BaselineNeverStranded` must catch it.
CONSTANTS
    MUTATE_I14 = FALSE
    MUTATE_T16_ENQUEUE = FALSE
    MUTATE_FORCE_CANCEL_EXECUTE = FALSE
    MUTATE_ORPHAN_BASELINE = TRUE
    MUTATE_BASELINE_LIVE_STATE = FALSE
    ForceRerunModeled = FALSE

SPECIFICATION SmallSpec
INVARIANT BaselineNeverStranded
